openapi: 3.0.0
info:
  title: Zoho Books API
  version: '3.0'
  description: Zoho Books REST API (merged from official OpenAPI specs)
servers:
- url: https://www.zohoapis.com/books/v3
  description: Zoho Books API v3
tags:
- name: organizations
  description: Organizations Module
- name: estimates
  description: Estimates Module
- name: contact-persons
  description: Contact-Persons Module
- name: contacts
  description: Contacts Module
- name: sales-order
  description: Sales-Order Module
- name: invoices
  description: Invoices Module
- name: sales-receipt
  description: Sales Receipt Module
- name: delivery-challans
  description: Delivery Challans Module
- name: recurring-invoices
  description: Recurring-Invoices Module
- name: credit-notes
  description: Credit-Notes Module
- name: customer-debit-notes
  description: Customer Debit Notes Module
- name: customer-payments
  description: Customer-Payments Module
- name: expenses
  description: Expenses Module
- name: recurring-expenses
  description: Recurring-Expenses Module
- name: retainer-invoices
  description: Retainer-Invoices Module
- name: purchase-order
  description: Purchase-Order Module
- name: bills
  description: Bills Module
- name: recurring-bills
  description: Recurring-Bills Module
- name: vendor-credits
  description: Vendor-Credits Module
- name: vendor-payments
  description: Vendor-Payments Module
- name: custom-modules
  description: Custom Module Configuration
- name: custom-module-records
  description: Custom Module Records
- name: bank-accounts
  description: Bank-Accounts Module
- name: bank-transactions
  description: Bank-Transactions Module
- name: bank-rules
  description: Bank-Rules Module
- name: chart-of-accounts
  description: Chart-Of-Accounts Module
- name: journals
  description: Journals Module
- name: fixed-assets
  description: Fixed-Assets Module
- name: base-currency-adjustment
  description: Base-Currency-Adjustment Module
- name: projects
  description: Projects Module
- name: tasks
  description: Task Module
- name: time-entries
  description: Time-Entries Module
- name: items
  description: Items Module
- name: users
  description: Users Module
- name: pricelists
  description: PriceLists Module
- name: locations
  description: Locations Module
- name: currency
  description: Currency Module
- name: taxes
  description: Taxes Module
- name: opening-balance
  description: Opening-Balance Module
- name: reporting-tags
  description: Reporting Tags Module
- name: integration
  description: integration Module
paths:
  /organizations:
    x-mcp-group:
    - Organizations
    post:
      tags:
      - organizations
      operationId: create_organization
      summary: Create an organization
      description: Create an organization.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-organization-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-organization-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - organizations
      operationId: list_organizations
      summary: List organizations
      description: Get the list of organizations.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-organizations-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
  /organizations/{organization_id}:
    x-mcp-group:
    - Organizations
    put:
      tags:
      - organizations
      operationId: update_organization
      summary: Update an organization
      description: Update the details of an organization.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-organization-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-organization-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - organizations
      operationId: get_organization
      summary: Get an organization
      description: Get the details of an organization.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-organization-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - name: organization_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the organization. Note: This value is a set
        of numeric characters.'
      example: '10229182'
  /estimates:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: create_estimate
      summary: Create an Estimate
      description: Create an estimate for your customer.
      parameters:
      - name: send
        in: query
        description: 'Send the estimate to the contact person(s) associated with the
          estimate.Allowed Values: <code>true</code> and <code>false</code>'
        required: false
        schema:
          type: boolean
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto estimate number generation for this estimate. This
          mandates the estimate number.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-estimate-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    get:
      tags:
      - estimates
      operationId: list_estimates
      summary: List estimates
      description: List all estimates with pagination.
      parameters:
      - name: estimate_number
        in: query
        description: Filter or search estimates by their unique estimate number. Supports
          variants such as <code>estimate_number_startswith</code> and <code>estimate_number_contains</code>
          for partial matches. Useful for quickly locating a specific estimate or
          a group of estimates with similar numbering.
        required: false
        schema:
          type: string
        example: EST-00002
      - name: reference_number
        in: query
        description: Filter or search estimates by their reference number. Supports
          variants such as <code>reference_number_startswith</code> and <code>reference_number_contains</code>
          for partial matches. Useful for linking estimates to external systems, customer
          requests, or internal workflows.
        required: false
        schema:
          type: string
        example: QRT-12346
      - name: customer_name
        in: query
        description: Filter or search estimates by the customer's name. Supports variants
          such as <code>customer_name_startswith</code> and <code>customer_name_contains</code>
          for partial matches. Useful for quickly locating all estimates associated
          with a specific customer or group of customers with similar names.
        required: false
        schema:
          type: string
        example: Bowman & Co
      - name: total
        in: query
        description: Filter or search estimates by the total amount of the estimate.
          Supports variants such as <code>total_less_than</code>, <code>total_less_equals</code>,
          <code>total_greater_than</code>, and <code>total_greater_equals</code> for
          range-based queries. Useful for finding estimates within a specific budget
          or identifying high-value quotes.
        required: false
        schema:
          type: number
          format: double
        example: 40.6
      - name: customer_id
        in: query
        description: 'Filter or search estimates by the unique customer ID. Use the
          <code>customer_id</code> returned by the Contacts API for the same organization.
          This helps you retrieve all estimates linked to a specific customer. Note:
          This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: 982000000567001
      - name: item_id
        in: query
        description: 'Filter or search estimates by the unique item ID. Use the <code>item_id</code>
          returned by the Items API for the same organization. This allows you to
          find all estimates that include a specific product or service. Note: This
          value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: ''
      - name: item_name
        in: query
        description: Search estimates by item name.Variants <code>item_name_startswith</code>
          and <code>item_name_contains</code>
        required: false
        schema:
          type: string
        example: ''
      - name: item_description
        in: query
        description: Search estimates by item description.Variants<code>item_description_startswith</code>
          and <code>item_description_contains</code>
        required: false
        schema:
          type: string
        example: ''
      - name: custom_field
        in: query
        description: Search estimates by custom field.Variants<code>custom_field_startswith</code>
          and <code>custom_field_contains</code>
        required: false
        schema:
          type: string
        example: ''
      - name: expiry_date
        in: query
        description: The date of expiration of the estimates
        required: false
        schema:
          type: string
        example: '2013-11-30'
      - name: expiry_date_start
        in: query
        description: Filter estimates with an expiry date on or after the specified
          date. Useful for defining the start of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-01'
      - name: expiry_date_end
        in: query
        description: Filter estimates with an expiry date on or before the specified
          date. Useful for defining the end of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-30'
      - name: expiry_date_before
        in: query
        description: Filter estimates with an expiry date strictly before the specified
          date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: expiry_date_after
        in: query
        description: Filter estimates with an expiry date strictly after the specified
          date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: date
        in: query
        description: Search estimates by estimate date.Variants <code>date_start</code>,
          <code>date_end</code>, <code>date_before</code> and <code>date_after</code>
        required: false
        schema:
          type: string
        example: '2013-11-18'
      - name: date_start
        in: query
        description: Filter estimates with an estimate date on or after the specified
          date. Useful for defining the start of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-01'
      - name: date_end
        in: query
        description: Filter estimates with an estimate date on or before the specified
          date. Useful for defining the end of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-30'
      - name: date_before
        in: query
        description: Filter estimates with an estimate date strictly before the specified
          date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: date_after
        in: query
        description: Filter estimates with an estimate date strictly after the specified
          date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: status
        in: query
        description: Search estimates by status.Allowed Values<code>draft</code>,
          <code>sent</code>,<code> invoiced </code>, <code>accepted</code>, <code>declined</code>
          and <code>expired</code>
        required: false
        schema:
          type: string
        example: draft
      - name: filter_by
        in: query
        description: Filter estimates by status.Allowed Values <code>Status.All</code>,
          <code>Status.Sent</code>, <code>Status.Draft</code>, <code>Status.Invoiced</code>,
          <code>Status.Accepted</code>, <code>Status.Declined</code> and <code>Status.Expired</code>
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Perform a keyword search across estimate number, reference number,
          or customer name. Use this parameter to quickly find estimates that match
          any of these fields, making it easier to locate relevant records with minimal
          input.
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: Sort estimates. Allowed Values <code>customer_name</code>, <code>estimate_number</code>,
          <code>date</code>, <code>total</code> and <code>created_time</code>
        required: false
        schema:
          type: string
        example: created_time
      - name: zcrm_potential_id
        in: query
        description: 'Potential ID of a Deal in CRM. Note: This value is a set of
          numeric characters.'
        required: false
        schema:
          type: string
        example: 460000000026049
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-estimates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    put:
      tags:
      - contacts
      operationId: update_estimate_using_custom_field
      summary: update an Estimate using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        an estimate by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding estimate will be retrieved and updated. Additionally, there
        is an optional X-Upsert header. If the X-Upsert header is true and the custom
        field's unique value is not found in any of the existing estimates, a new
        estimate will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-estimate-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}:
    x-mcp-group:
    - Estimates
    put:
      tags:
      - estimates
      operationId: update_estimate
      summary: Update an Estimate
      description: Update an existing estimate. To delete a line item just remove
        it from the line_items list.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto estimate number generation for this estimate. This
          mandates the estimate number.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-estimate-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
    get:
      tags:
      - estimates
      operationId: get_estimate
      summary: Get an estimate
      description: Get the details of an estimate.
      parameters:
      - name: print
        in: query
        description: Print the exported pdf.
        required: false
        schema:
          type: boolean
        example: ' '
      - name: accept
        in: query
        description: 'Get the details of a particular estimate in formats such as
          json/ pdf/ html. Default format is json.Allowed Values: <code>json</code>,
          <code>pdf</code> and <code>html</code>'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    delete:
      tags:
      - estimates
      operationId: delete_estimate
      summary: Delete an Estimate
      description: Delete an existing estimate.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.DELETE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimate/{estimate_id}/customfields:
    x-mcp-group:
    - Estimates
    put:
      tags:
      - estimates
      operationId: update_custom_fields_in_estimate
      summary: Update custom field in existing estimates
      description: Update the value of the custom field in existing estimates.
      parameters:
      - name: estimate_id
        in: path
        required: true
        description: 'Unique identifier of the estimate. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: 982000000567011
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields_update'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-estimate-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
  /estimates/{estimate_id}/status/sent:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: mark_estimate_sent
      summary: Mark an estimate as sent
      description: Mark a draft estimate as sent.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-an-estimate-as-sent-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/status/accepted:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: mark_estimate_accepted
      summary: Mark an estimate as accepted
      description: Mark a sent estimate as accepted if the customer has accepted it.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-an-estimate-as-accepted-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/status/declined:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: mark_estimate_declined
      summary: Mark an estimate as declined
      description: Mark a sent estimate as declined if the customer has rejected it.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-an-estimate-as-declined-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/submit:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: submit_estimate
      summary: Submit an estimate for approval
      description: Submit an estimate for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-an-estimate-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/approve:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: approve_estimate
      summary: Approve an estimate.
      description: Approve an estimate.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/email:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: email_estimate
      summary: Email an estimate
      description: Email an estimate to the customer. Input json string is not mandatory.
        If input json string is empty, mail will be send with default mail content.
      parameters:
      - name: attachments
        in: query
        description: Files to be attached to the email
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-an-estimate-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-an-estimate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    get:
      tags:
      - estimates
      operationId: get_estimate_email
      summary: Get estimate email content
      description: Get the email content of an estimate.
      parameters:
      - name: email_template_id
        in: query
        description: 'Get the email content based on a specific email template. If
          this param is not inputted, then the content will be based on the email
          template associated with the customer. If no template is associated with
          the customer, then default template will be used. Note: This value is a
          set of numeric characters.'
        required: true
        schema:
          type: string
        example: 982000000000079
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-estimate-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/email:
    x-mcp-group:
    - Estimates
    post:
      tags:
      - estimates
      operationId: email_multiple_estimates
      summary: Email multiple estimates
      description: Send estimates to your customers by email. Maximum of 10 estimates
        can be sent at once.
      parameters:
      - name: estimate_ids
        in: query
        description: 'Comma separated estimate ids which are to be emailed. Note:
          This value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-multiple-estimates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /estimates/pdf:
    x-mcp-group:
    - Estimates
    get:
      tags:
      - estimates
      operationId: bulk_export_estimates_as_pdf
      summary: Bulk export estimates
      description: Maximum of 25 estimates can be exported in a single pdf.
      parameters:
      - name: estimate_ids
        in: query
        description: 'Comma separated estimate ids which are to be emailed. Note:
          This value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-export-estimates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /estimates/print:
    x-mcp-group:
    - Estimates
    get:
      tags:
      - estimates
      operationId: bulk_print_estimates
      summary: Bulk print estimates
      description: Export estimates as pdf and print them. Maximum of 25 estimates
        can be printed.
      parameters:
      - name: estimate_ids
        in: query
        description: 'Comma separated estimate ids which are to be emailed. Note:
          This value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-print-estimates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/address/billing:
    x-mcp-group:
    - Estimates
    put:
      tags:
      - estimates
      operationId: update_estimate_billing_address
      summary: Update billing address
      description: Updates the billing address for this estimate alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/address/shipping:
    x-mcp-group:
    - Estimates
    put:
      tags:
      - estimates
      operationId: update_estimate_shipping_address
      summary: Update shipping address
      description: Updates the shipping address for an existing estimate alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-shipping-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-shipping-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/templates:
    x-mcp-group:
    - Estimates
    get:
      tags:
      - estimates
      operationId: list_estimate_templates
      summary: List estimate template
      description: Get all estimate pdf templates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-estimate-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/templates/{template_id}:
    x-mcp-group:
    - Estimates
    put:
      tags:
      - estimates
      operationId: update_estimate_template
      summary: Update estimate template
      description: Update the pdf template associated with the estimate.
      parameters:
      - name: template_id
        in: path
        required: true
        description: 'Unique identifier of the estimate template. Note: This value
          is a set of numeric characters.'
        schema:
          type: string
        example: 982000000000143
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-estimate-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/comments:
    x-mcp-group:
    - Estimates
    get:
      tags:
      - estimates
      operationId: list_estimate_comments
      summary: List estimate comments & history
      description: Get the complete history and comments of an estimate.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-estimate-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.READ
    post:
      tags:
      - estimates
      operationId: create_estimate_comment
      summary: Add Comments
      description: Add a comment for an estimate.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comments-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.CREATE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /estimates/{estimate_id}/comments/{comment_id}:
    x-mcp-group:
    - Estimates
    put:
      tags:
      - estimates
      operationId: update_estimate_comment
      summary: Update comment
      description: Update an existing comment of an estimate.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: 982000000567019
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.UPDATE
    delete:
      tags:
      - estimates
      operationId: delete_estimate_comment
      summary: Delete a comment
      description: Delete an estimate comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: 982000000567019
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.estimates.DELETE
    parameters:
    - name: estimate_id
      in: path
      required: true
      description: 'Unique identifier of the estimate. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567011
    - $ref: '#/components/parameters/organization_id'
  /contacts/contactpersons:
    x-mcp-group:
    - Contact Persons
    post:
      tags:
      - contact-persons
      operationId: create_contact_person
      summary: Create a contact person
      description: Create a contact person for contact.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-contact-person-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-contact-person-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /contacts/contactpersons/{contact_person_id}:
    x-mcp-group:
    - Contact Persons
    put:
      tags:
      - contact-persons
      operationId: update_contact_person
      summary: Update a contact person
      description: Update an existing contact person.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-contact-person-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-contact-person-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.UPDATE
    delete:
      tags:
      - contact-persons
      operationId: delete_contact_person
      summary: Delete a contact person
      description: Delete an existing contact person.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-contact-person-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.DELETE
    parameters:
    - name: contact_person_id
      in: path
      required: true
      description: Unique identifier of the contact person.
      schema:
        type: string
      example: 460000000026051
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/contactpersons:
    x-mcp-group:
    - Contact Persons
    get:
      tags:
      - contact-persons
      operationId: list_contact_persons
      summary: List contact persons
      description: List all contacts with pagination.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-contact-persons-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    parameters:
    - name: contact_id
      in: path
      required: true
      description: Unique identifier of the contact.
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/contactpersons/{contact_person_id}:
    x-mcp-group:
    - Contact Persons
    get:
      tags:
      - contact-persons
      operationId: get_contact_person
      summary: Get a contact person
      description: Get the contact person details.
      parameters:
      - name: contact_person_id
        in: path
        required: true
        description: Unique identifier of the contact person.
        schema:
          type: string
        example: 460000000026051
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-contact-person-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    parameters:
    - name: contact_id
      in: path
      required: true
      description: Unique identifier of the contact.
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/contactpersons/{contact_person_id}/primary:
    x-mcp-group:
    - Contact Persons
    post:
      tags:
      - contact-persons
      operationId: mark_contact_person_primary
      summary: Mark as primary contact person
      description: Mark a contact person as primary for the contact.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-primary-contact-person-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_person_id
      in: path
      required: true
      description: Unique identifier of the contact person.
      schema:
        type: string
      example: 460000000026051
    - $ref: '#/components/parameters/organization_id'
  /contacts:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: create_contact
      summary: Create a Contact
      description: Create a new contact with comprehensive business information. This
        operation allows you to create a customer or vendor by providing details such
        as contact name, company information, addresses, contact persons, payment
        terms, tax settings, and custom fields. The created contact can be used for
        generating invoices, bills, estimates, and other business transactions. The
        system automatically assigns a unique contact ID.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-contact-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-contact-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    get:
      tags:
      - contacts
      operationId: list_contacts
      summary: List Contacts
      description: Retrieve a comprehensive list of all contacts with advanced filters.
        This operation supports multiple search criteria including contact name, company
        name, address, email, phone, and general text search. You can filter contacts
        by status (active, inactive, duplicate, CRM) and sort by various fields. The
        response includes essential contact information, financial data including
        outstanding amounts and credit limits, and pagination details for efficient
        data retrieval.
      parameters:
      - name: contact_type
        in: query
        description: 'Search contacts by contact type. Allowed Values: <code>customer</code>,
          <code>vendor</code>.'
        required: false
        schema:
          type: string
        example: customer
      - name: contact_name
        in: query
        description: 'Search contacts by contact name. Max-length [100] Variants:
          <code>contact_name_startswith</code> and <code>contact_name_contains</code>.
          Max-length [100]'
        required: false
        schema:
          type: string
        example: Bowman and Co
      - name: company_name
        in: query
        description: 'Search contacts by company name. Max-length [100] Variants:
          <code>company_name_startswith</code> and <code>company_name_contains</code>'
        required: false
        schema:
          type: string
        example: Bowman and Co
      - name: first_name
        in: query
        description: 'Search contacts by first name of the primary contact person.
          Max-length [100] Variants: <code>first_name_startswith</code> and <code>first_name_contains</code>'
        required: false
        schema:
          type: string
        example: Will
      - name: last_name
        in: query
        description: 'Search contacts by last name of the primary contact person.
          Max-length [100] Variants: <code>last_name_startswith</code> and <code>last_name_contains</code>'
        required: false
        schema:
          type: string
        example: Smith
      - name: address
        in: query
        description: 'Search contacts by any of the address fields. Max-length [100]
          Variants: <code>address_startswith</code> and <code>address_contains</code>'
        required: false
        schema:
          type: string
        example: 4900 Hopyard Rd
      - name: email
        in: query
        description: 'Search contacts by email of the primary contact person. Max-length
          [100] Variants: <code>email_startswith</code> and <code>email_contains</code>'
        required: false
        schema:
          type: string
        example: willsmith@bowmanfurniture.com
      - name: phone
        in: query
        description: 'Search contacts by phone number of the primary contact person.
          Max-length [100] Variants: <code>phone_startswith</code> and <code>phone_contains</code>'
        required: false
        schema:
          type: string
        example: +1-925-921-9201
      - name: filter_by
        in: query
        description: 'Filter contacts by status. Allowed Values: <code>Status.All,
          Status.Active, Status.Inactive, Status.Duplicate, Status.PortalEnabled,
          Status.PortalDisabled, Invoice.OverDue, Invoice.Unpaid, Status.CreditLimitExceed
          and Status.Crm</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Search contacts by contact name or notes. Max-length [100]
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort contacts. Allowed Values: <code>contact_name, first_name,
          last_name, email, outstanding_receivable_amount, created_time and last_modified_time</code>'
        required: false
        schema:
          type: string
        example: contact_name
      - name: zcrm_contact_id
        in: query
        description: 'CRM Contact ID for the contact. Note: This value is a set of
          numeric characters.'
        required: false
        schema:
          type: string
        example: 460000000026049
      - name: zcrm_account_id
        in: query
        description: 'CRM Account ID for the contact. Note: This value is a set of
          numeric characters.'
        required: false
        schema:
          type: string
        example: 460000000026049
      - name: zcrm_vendor_id
        in: query
        description: 'CRM Vendor ID for the contact. Note: This value is a set of
          numeric characters.'
        required: false
        schema:
          type: string
        example: 460000000026049
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-contacts-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    put:
      tags:
      - contacts
      operationId: update_contact_using_custom_field
      summary: Update a contact using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a contact by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding contact will be retrieved and updated. Additionally, there
        is an optional X-Upsert header. If the X-Upsert header is true and the custom
        field's unique value is not found in any of the existing contacts, a new contact
        will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-contact-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-contact-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}:
    x-mcp-group:
    - Contacts
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
    put:
      tags:
      - contacts
      operationId: update_contact
      summary: Update a Contact
      description: Update an existing contact with comprehensive business information.
        This operation allows you to modify all contact details including basic information,
        addresses, contact persons, payment terms, tax settings, and custom fields.
        For contact person, you can add new contact persons, update existing ones,
        or remove them by excluding them from the contact_persons list.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-contact-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-contact-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.UPDATE
    get:
      tags:
      - contacts
      operationId: get_contact
      summary: Get Contact
      description: Retrieve comprehensive details of a specific contact. This operation
        provides complete contact details such as basic information, addresses, contact
        persons, payment terms, tax settings, custom fields, and financial data including
        outstanding amounts, credit limits, and transaction history.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-contact-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    delete:
      tags:
      - contacts
      operationId: delete_contact
      summary: Delete a Contact
      description: Delete an existing contact.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-contact-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.DELETE
  /contacts/{contact_id}/active:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: mark_contact_active
      summary: Mark as Active
      description: Mark a contact as active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-active-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/inactive:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: mark_contact_inactive
      summary: Mark as Inactive
      description: Mark a contact as inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-inactive-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/portal/enable:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: enable_contact_portal
      summary: Enable Portal Access
      description: Enable portal access for a contact.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/enable-portal-access-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/enable-portal-access-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/paymentreminder/enable:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: enable_contact_payment_reminder
      summary: Enable Payment Reminders
      description: Enable automated payment reminders for a contact.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/enable-payment-reminders-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/paymentreminder/disable:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: disable_contact_payment_reminder
      summary: Disable Payment Reminders
      description: Disable automated payment reminders for a contact.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/disable-payment-reminders-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/statements/email:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: email_contact_statement
      summary: Email Statement
      description: Email statement to the contact. If JSONString is not inputted,
        mail will be sent with the default mail content.
      parameters:
      - name: start_date
        in: query
        description: If start_date and end_date are not given, current month's statement
          will be sent to contact. Date format [yyyy-mm-dd]
        required: false
        schema:
          type: string
        example: ''
      - name: end_date
        in: query
        description: End date for the statement. Date format [yyyy-mm-dd]
        required: false
        schema:
          type: string
        example: ''
      - name: multipart_or_formdata
        in: query
        description: Files to be attached along with the statement.
        required: false
        schema:
          type: string
        example: file
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-statement-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-statement-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    get:
      tags:
      - contacts
      operationId: get_contact_statement_mail
      summary: Get Statement Mail Content
      description: Get the statement mail content.
      parameters:
      - name: start_date
        in: query
        description: If start_date and end_date are not given, current month's statement
          will be sent to contact. Date format [yyyy-mm-dd]
        required: false
        schema:
          type: string
        example: ''
      - name: end_date
        in: query
        description: End date for the statement. Date format [yyyy-mm-dd]
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-statement-mail-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/email:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: email_contact
      summary: Email Contact
      description: Send email to contact.
      parameters:
      - name: send_customer_statement
        in: query
        description: Send customer statement pdf with email.
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-contact-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-contact-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/comments:
    x-mcp-group:
    - Contacts
    get:
      tags:
      - contacts
      operationId: list_contact_comments
      summary: List Comments
      description: List recent activities of a contact.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-comments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/address:
    x-mcp-group:
    - Contacts
    get:
      tags:
      - contacts
      operationId: get_contact_address
      summary: Get Contact Addresses
      description: Get addresses of a contact including its Shipping Address, Billing
        Address and other additional addresses.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-contact-addresses-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    post:
      tags:
      - contacts
      operationId: add_contact_address
      summary: Add Additional Address
      description: Add an additional address for a contact using the arguments below.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-additional-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-additional-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/address/{address_id}:
    x-mcp-group:
    - Contacts
    put:
      tags:
      - contacts
      operationId: update_contact_address
      summary: Edit Additional Address
      description: Edit the additional address of a contact using the arguments below.
      parameters:
      - name: address_id
        in: path
        required: true
        description: 'Unique identifier of the address. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: 1053791000000186000
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/edit-additional-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/edit-additional-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.UPDATE
    delete:
      tags:
      - contacts
      operationId: delete_contact_address
      summary: Delete Additional Address
      description: Delete the additional address of a contact.
      parameters:
      - name: address_id
        in: path
        required: true
        description: 'Unique identifier of the address. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: 1053791000000186000
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-additional-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.DELETE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/refunds:
    x-mcp-group:
    - Contacts
    get:
      tags:
      - contacts
      operationId: list_contact_refunds
      summary: List Refunds
      description: List the refund history of a contact.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-refunds-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/track1099:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: track_contact_1099
      summary: Track 1099
      description: 'Track a contact for 1099 reporting: (Note: This API is only available
        when the organization''s country is U.S.A).'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/track-1099-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/untrack1099:
    x-mcp-group:
    - Contacts
    post:
      tags:
      - contacts
      operationId: untrack_contact_1099
      summary: Untrack 1099
      description: 'Use this API to stop tracking payments to a vendor for 1099 reporting.
        (Note: This API is only available when the organization''s country is U.S.A).'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/untrack-1099-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /contacts/{contact_id}/receivables/unusedretainerpayments:
    x-mcp-group:
    - Contacts
    get:
      tags:
      - contacts
      operationId: get_unused_retainer_payments
      summary: Get Unused Retainer Payments
      description: Retrieve information about unused retainer payments for a specific
        contact. This endpoint returns details of retainer payments that have been
        made but not yet applied to invoices, providing insight into available credit
        balances from retainer payments.
      parameters:
      - name: currency_id
        in: query
        description: 'Currency ID to filter unused retainer payments by specific currency.
          Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000000097'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-unused-retainer-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.READ
    parameters:
    - name: contact_id
      in: path
      required: true
      description: 'Unique identifier of the contact. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 460000000026049
    - $ref: '#/components/parameters/organization_id'
  /salesorders:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: create_sales_order
      summary: Create a sales order
      description: Create a sales order for your customer.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto sales order number generation for this sales order.
          This mandates the sales order number.
        required: false
        schema:
          type: boolean
        example: false
      - name: can_send_in_mail
        in: query
        description: Can the file be sent in mail.
        required: false
        schema:
          type: boolean
        example: true
      - name: totalFiles
        in: query
        description: Total number of files.
        required: false
        schema:
          type: integer
        example: 0
      - name: doc
        in: query
        description: Document that is to be attached
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-sales-order-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    get:
      tags:
      - sales-order
      operationId: list_sales_orders
      summary: List sales orders
      description: List all sales orders.
      parameters:
      - name: sort_column
        in: query
        description: Specify the column field for sorting sales order results. Available
          options include <code>customer_name</code>, <code>salesorder_number</code>,
          <code>shipment_date</code>, <code>last_modified_time</code>, <code>reference_number</code>,
          <code>total</code>, <code>date</code>, and <code>created_time</code>.
        required: false
        schema:
          type: string
        example: created_time
      - name: search_text
        in: query
        description: General search parameter for cross-field text matching across
          sales order number, reference number, and customer name fields. Enables
          unified search functionality for quick order identification and customer
          order lookup operations.
        required: false
        schema:
          type: string
        example: ''
      - name: filter_by
        in: query
        description: 'Filter sales order by status. Allowed Values: <code>Status.All</code>,
          <code>Status.Open</code>, <code>Status.Draft</code>, <code>Status.OverDue</code>,
          <code>Status.PartiallyInvoiced</code>, <code>Status.Invoiced</code>, <code>Status.Void</code>
          and <code>Status.Closed</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: salesorder_number
        in: query
        description: 'Filter sales orders by sales order number using various matching
          operators. Available variants include <code>salesorder_number_startswith</code>,
          <code>salesorder_number_not_in</code>, <code>salesorder_number_in</code>,
          and <code>salesorder_number_contains</code>. Maximum search length: 100
          characters.'
        required: false
        schema:
          type: string
        example: SO-001
      - name: item_name
        in: query
        description: 'Filter sales orders by line item name using various matching
          operators. Available variants include <code>item_name_startswith</code>,
          <code>item_name_not_in</code>, <code>item_name_in</code>, and <code>item_name_contains</code>.
          Maximum search length: 100 characters.'
        required: false
        schema:
          type: string
        example: ''
      - name: item_id
        in: query
        description: 'Filter sales orders by specific line item identifier. Retrieves
          all sales orders containing a particular product or service item based on
          its unique identifier for inventory tracking and item-based reporting purposes.
          Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000017088'
      - name: item_description
        in: query
        description: 'Filter sales orders by line item description text using various
          matching operators. Available variants include <code>item_description_startswith</code>,
          <code>item_description_not_in</code>, <code>item_description_in</code>,
          and <code>item_description_contains</code>. Maximum search length: 100 characters.'
        required: false
        schema:
          type: string
        example: ''
      - name: reference_number
        in: query
        description: Filter sales orders by external reference number using various
          matching operators. Available variants include <code>reference_number_startswith</code>,
          <code>reference_number_not_in</code>, <code>reference_number_in</code>,
          and <code>reference_number_contains</code>.
        required: false
        schema:
          type: string
        example: REF-001
      - name: customer_name
        in: query
        description: 'Filter sales orders by customer name using various matching
          operators. Available variants include <code>customer_name_startswith</code>,
          <code>customer_name_not_in</code>, <code>customer_name_in</code>, and <code>customer_name_contains</code>.
          Maximum search length: 100 characters.'
        required: false
        schema:
          type: string
        example: SAF Instruments Inc
      - name: total
        in: query
        description: Filter sales orders by total amount using various range operators.
          Available variants include <code>total_start</code>, <code>total_end</code>,
          <code>total_less_than</code>, <code>total_less_equals</code>, <code>total_greater_than</code>,
          and <code>total_greater_equals</code>.
        required: false
        schema:
          type: number
          format: double
        example: 12400
      - name: date
        in: query
        description: 'Filter sales orders by creation date using various date range
          operators. Available variants include <code>date_start</code>, <code>date_end</code>,
          <code>date_before</code>, and <code>date_after</code>. Default date format:
          <code>yyyy-mm-dd</code>.'
        required: false
        schema:
          type: string
        example: '2014-07-28'
      - name: date_start
        in: query
        description: Filter sales orders with creation date on or after the specified
          date. Useful for defining the start of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-01'
      - name: date_end
        in: query
        description: Filter sales orders with creation date on or before the specified
          date. Useful for defining the end of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-30'
      - name: date_before
        in: query
        description: Filter sales orders with creation date strictly before the specified
          date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: date_after
        in: query
        description: Filter sales orders with creation date strictly after the specified
          date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: shipment_date
        in: query
        description: 'Search sales order by sales order shipment date. Variants: <code>shipment_date_start</code>,
          <code>shipment_date_end</code>, <code>shipment_date_before</code> and <code>shipment_date_after</code>.
          Default date format : <code>yyyy-mm-dd</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: expiry_date_start
        in: query
        description: Filter sales orders by sales order shipment date on or after
          the specified date. Useful for defining the start of a date range. Use yyyy-mm-dd
          format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-01'
      - name: expiry_date_end
        in: query
        description: Filter sales orders by sales order shipment date on or before
          the specified date. Useful for defining the end of a date range. Use yyyy-mm-dd
          format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-30'
      - name: expiry_date_before
        in: query
        description: Filter sales orders by sales order shipment date strictly before
          the specified date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: expiry_date_after
        in: query
        description: Filter sales orders by sales order shipment date strictly after
          the specified date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: status
        in: query
        description: 'Search sales order by sales order status. Allowed Values: <code>draft</code>,
          <code>open</code>, <code>invoiced</code>, <code>partially_invoiced</code>,
          <code>void</code> and <code>overdue</code>.'
        required: false
        schema:
          type: string
        example: open
      - name: customer_id
        in: query
        description: 'Filter sales orders by specific customer identifier. Retrieves
          all sales orders associated with a particular customer account for customer
          relationship management, order history tracking, and customer-specific reporting
          and analysis purposes. Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000017138'
      - name: salesperson_id
        in: query
        description: 'Filter sales orders by specific sales representative identifier.
          Retrieves all sales orders assigned to a particular salesperson for performance
          tracking, commission calculations, sales territory management, and individual
          sales reporting purposes. Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000000097'
      - name: salesorder_ids
        in: query
        description: 'ID''s of the salesorder [Comma seperated values] . <code>Maximum
          Length : 200</code>. Note: This value is a set of numeric characters separated
          by comma.'
        required: false
        schema:
          type: string
        example: 460108000000072000
      - name: zcrm_potential_id
        in: query
        required: false
        description: 'Unique identifier of the potential in CRM. Note: This value
          is a set of numeric characters.'
        schema:
          type: string
        example: '460000000033001'
      - name: last_modified_time
        in: query
        description: Last Modified time of the Sales Order
        required: false
        schema:
          type: string
        example: 2014-08-25T11:23:26+0530
      - name: accept
        in: query
        description: 'Get the details of a particular sales order in formats such
          as json/ pdf/ html. Default format is json. Allowed Values: <code>json</code>,
          <code>csv</code> , <code>xml</code>, <code>xls</code>, <code>xlsx</code>,
          <code>pdf</code>, <code>jhtml</code> and <code>html</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: print
        in: query
        description: Print the exported pdf. <code> It will be used when accept =
          pdf and atleast one value in salesorder_ids  </code>
        required: false
        schema:
          type: boolean
        example: false
      - name: customview_id
        in: query
        description: 'ID of the customview. Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000004000597'
      - name: page
        in: query
        description: Specify the page number for paginated results retrieval. Controls
          which subset of sales order records to return, enabling efficient data handling
          for large result sets. Default value is 1 for the first page.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Specify the maximum number of sales order records to return per
          page. Controls result set size for optimal performance, memory usage, and
          user experience.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-sales-orders-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    put:
      tags:
      - sales-order
      operationId: update_sales_order_using_custom_field
      summary: Update a sales order using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a sales order by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding sales order will be retrieved and updated. Additionally,
        there is an optional X-Upsert header. If the X-Upsert header is true and the
        custom field's unique value is not found in any of the existing sales orders,
        a new sales order will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-sales-order-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}:
    x-mcp-group:
    - Sales Order
    put:
      tags:
      - sales-order
      operationId: update_sales_order
      summary: Update a sales order
      description: Update an existing sales order. To delete a line item just remove
        it from the line_items list.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto sales order number generation for this sales order.
          This mandates the sales order number.
        required: false
        schema:
          type: boolean
        example: false
      - name: can_send_in_mail
        in: query
        description: Can the file be sent in mail.
        required: false
        schema:
          type: boolean
        example: true
      - name: totalFiles
        in: query
        description: Total number of files.
        required: false
        schema:
          type: integer
        example: 0
      - name: doc
        in: query
        description: Document that is to be attached
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-sales-order-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    get:
      tags:
      - sales-order
      operationId: get_sales_order
      summary: Get a sales order
      description: Get the details of a sales order.
      parameters:
      - name: print
        in: query
        description: Print the exported pdf.
        required: false
        schema:
          type: boolean
        example: false
      - name: accept
        in: query
        description: 'Get the details of a particular sales order in formats such
          as json/ pdf/ html. Default format is json. Allowed Values: <code>json</code>,
          <code>csv</code> , <code>xml</code>, <code>xls</code>, <code>xlsx</code>,
          <code>pdf</code>, <code>jhtml</code> and <code>html</code>.'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    delete:
      tags:
      - sales-order
      operationId: delete_sales_order
      summary: Delete a sales order
      description: Delete an existing sales order. Invoiced sales order cannot be
        deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.DELETE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorder/{salesorder_id}/customfields:
    x-mcp-group:
    - Sales Order
    put:
      tags:
      - sales-order
      operationId: update_salesorder_customfields
      summary: Update custom field in existing salesorders
      description: Update the value of the custom field in existing salesorders.
      parameters:
      - name: salesorder_id
        in: path
        required: true
        description: 'Unique identifier of the sales order. Note: This value is a
          set of numeric characters.'
        schema:
          type: string
        example: '460000000039129'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-salesorder-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
  /salesorders/{salesorder_id}/status/open:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: mark_sales_order_as_open
      summary: Mark a sales order as open
      description: Mark a draft sales order as open.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-a-sales-order-as-open-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/status/void:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: mark_sales_order_as_void
      summary: Mark a sales order as void
      description: Mark a sales order as void.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/mark-a-sales-order-as-void-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-a-sales-order-as-void-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/substatus/{status_code}:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: update_sales_order_sub_status
      summary: Update a sales order sub status
      description: Update a sales order sub status.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    example: 0
                    readOnly: true
                  message:
                    type: string
                    example: Sales Order status updated successfully.
                    readOnly: true
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - name: status_code
      in: path
      required: true
      description: Unique code of the status of a sales order.
      schema:
        type: string
      example: cs_openshi
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/email:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: email_sales_order
      summary: Email a sales order
      description: Email a sales order to the customer. Input json string is not mandatory.
        If input json string is empty, mail will be send with default mail content.
      parameters:
      - name: salesorder_id
        in: query
        description: 'ID of the Sales Order. Note: This value is a set of numeric
          characters.'
        required: false
        schema:
          type: string
        example: '460000000039129'
      - name: attachments
        in: query
        description: Attachments of the Sales Order
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: send_attachment
        in: query
        description: Send the sales order attachment a with the email.
        required: false
        schema:
          type: boolean
        example: false
      - name: file_name
        in: query
        description: Name of the file.
        required: false
        schema:
          type: string
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-a-sales-order-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    get:
      tags:
      - sales-order
      operationId: get_sales_order_email
      summary: Get sales order email content
      description: Get the email content of a sales order.
      parameters:
      - name: email_template_id
        in: query
        description: 'Get the email content based on a specific email template. If
          this param is not inputted, then the content will be based on the email
          template associated with the customer. If no template is associated with
          the customer, then default template will be used. Note: This value is a
          set of numeric characters.'
        required: false
        schema:
          type: string
        example: 460000000001287
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-sales-order-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/submit:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: submit_sales_order
      summary: Submit a sales order for approval
      description: Submit a sales order for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-a-sales-order-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/approve:
    x-mcp-group:
    - Sales Order
    post:
      tags:
      - sales-order
      operationId: approve_sales_order
      summary: Approve a sales order.
      description: Approve a sales order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/pdf:
    x-mcp-group:
    - Sales Order
    get:
      tags:
      - sales-order
      operationId: bulk_export_sales_orders_as_pdf
      summary: Bulk export sales orders
      description: Maximum of 25 sales orders can be exported in a single pdf.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-export-sales-orders-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /salesorders/print:
    x-mcp-group:
    - Sales Order
    get:
      tags:
      - sales-order
      operationId: bulk_print_sales_orders
      summary: Bulk print sales orders
      description: Export sales orders as pdf and print them. Maximum of 25 sales
        orders can be printed.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-print-sales-orders-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/address/billing:
    x-mcp-group:
    - Sales Order
    put:
      tags:
      - sales-order
      operationId: update_sales_order_billing_address
      summary: Update billing address
      description: Updates the billing address for this sales order alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/address/shipping:
    x-mcp-group:
    - Sales Order
    put:
      tags:
      - sales-order
      operationId: update_sales_order_shipping_address
      summary: Update shipping address
      description: Updates the shipping address for this sales order alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-shipping-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-shipping-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/templates:
    x-mcp-group:
    - Sales Order
    get:
      tags:
      - sales-order
      operationId: list_sales_order_templates
      summary: List sales order templates
      description: Get all sales order pdf templates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-sales-order-templates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/templates/{template_id}:
    x-mcp-group:
    - Sales Order
    put:
      tags:
      - sales-order
      operationId: update_sales_order_template
      summary: Update sales order template
      description: Update the pdf template associated with the sales order.
      parameters:
      - name: template_id
        in: path
        required: true
        description: 'Unique identifier of the sales order template. Note: This value
          is a set of numeric characters.'
        schema:
          type: string
        example: '460000000021001'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-sales-order-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/attachment:
    x-mcp-group:
    - Sales Order
    get:
      tags:
      - sales-order
      operationId: get_sales_order_attachment
      summary: Get a sales order attachment
      description: Returns the file attached to the sales order.
      parameters:
      - name: preview
        in: query
        description: Check if preview of the Sales Order is required
        required: false
        schema:
          type: boolean
        example: false
      - name: inline
        in: query
        description: Check if inline response is required
        required: false
        schema:
          type: boolean
        example: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-sales-order-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    post:
      tags:
      - sales-order
      operationId: add_sales_order_attachment
      summary: Add attachment to a sales order
      description: Attach a file to a sales order.
      parameters:
      - name: attachment
        in: query
        description: The file that is to be added as an Attachment in the Sales Order
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: can_send_in_mail
        in: query
        description: Can the file be sent in mail.
        required: false
        schema:
          type: boolean
        example: true
      - name: doc
        in: query
        description: Document that is to be attached
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: totalFiles
        in: query
        description: Total number of files.
        required: false
        schema:
          type: integer
        example: 0
      - name: document_ids
        in: query
        description: 'ID''s of the document. Note: This value is a set of numeric
          characters characters separated by comma.'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-a-sales-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    put:
      tags:
      - sales-order
      operationId: update_sales_order_attachment_preference
      summary: Update attachment preference
      description: Set whether you want to send the attached file while emailing the
        sales order.
      parameters:
      - name: can_send_in_mail
        in: query
        description: Can the file be sent in mail.
        required: true
        schema:
          type: boolean
        example: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-attachment-preference-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    delete:
      tags:
      - sales-order
      operationId: delete_sales_order_attachment
      summary: Delete an attachment
      description: Delete the file attached to the sales order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.DELETE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/comments:
    x-mcp-group:
    - Sales Order
    get:
      tags:
      - sales-order
      operationId: list_sales_order_comments
      summary: List sales order comments & history
      description: Get the complete history and comments of sales order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-sales-order-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.READ
    post:
      tags:
      - sales-order
      operationId: add_sales_order_comment
      summary: Add comment
      description: Add a comment for a sales order.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.CREATE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesorders/{salesorder_id}/comments/{comment_id}:
    x-mcp-group:
    - Sales Order
    put:
      tags:
      - sales-order
      operationId: update_sales_order_comment
      summary: Update comment
      description: Update existing comment of a sales order.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: '460000000048023'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.UPDATE
    delete:
      tags:
      - sales-order
      operationId: delete_sales_order_comment
      summary: Delete a comment
      description: Delete a sales order comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: '460000000048023'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.salesorders.DELETE
    parameters:
    - name: salesorder_id
      in: path
      required: true
      description: 'Unique identifier of the sales order. Note: This value is a set
        of numeric characters.'
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /invoices:
    x-mcp-group:
    - Customer Debit Notes
    post:
      tags:
      - customer-debit-notes
      operationId: create_customer_debit_note
      summary: Create a customer debit note
      description: Create a customer debit note for additional charges or adjustments
        to be made to the original invoice.
      parameters:
      - name: send
        in: query
        description: Send the debit note to the contact person(s) associated with
          the debit note. Allowed values <code>true</code> and <code>false</code>.
        required: false
        schema:
          type: boolean
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto debit note number generation for this debit note.
          This mandates the debit note number. Allowed values <code>true</code> and
          <code>false</code>
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-customer-debit-note-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-customer-debit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    get:
      tags:
      - customer-debit-notes
      operationId: list_customer_debit_notes
      summary: List customer debit notes
      description: Get a list of customer debit notes with helpful pagination, filtering,
        search, and sorting features. Perfect for viewing your debit note data in
        organized ways, whether you need to find specific debit notes or browse through
        your records.
      parameters:
      - name: item_name
        in: query
        description: Search debit notes by item name. Filters debit notes based on
          product or service names in debit note line items. Supports <code>item_name_startswith</code>
          and <code>item_name_contains</code> variants. Maximum length is 100 characters.
          Useful for finding debit notes with specific products or services.
        required: false
        schema:
          type: string
        example: Desktop Units
      - name: item_id
        in: query
        description: Search debit notes by item ID. Filters debit notes based on the
          unique identifier of specific products or services in line items. Use the
          GET `/items` API to find available item IDs. Useful for finding all debit
          notes containing a specific product or service.
        required: false
        schema:
          type: string
        example: 982000000030049
      - name: item_description
        in: query
        description: Search debit notes by item description. Filters debit notes based
          on detailed descriptions of products or services in line items. Supports
          <code>item_description_startswith</code> and <code>item_description_contains</code>
          variants. Maximum length is 100 characters. Useful for finding debit notes
          with specific item descriptions or technical specifications.
        required: false
        schema:
          type: string
        example: ''
      - name: customer_name
        in: query
        description: Search debit notes by customer name. Filters debit notes based
          on the business or individual name associated with the debit note. Maximum
          length is 100 characters. Useful for finding all debit notes for a specific
          customer or generating customer reports.
        required: false
        schema:
          type: string
        example: Bowman & Co
      - name: email
        in: query
        description: Search debit notes by customer email address. Filters debit notes
          based on the email address of the customer or contact person. Maximum length
          is 100 characters. Useful for finding debit notes for specific customers
          or generating customer segment reports.
        required: false
        schema:
          type: string
        example: ''
      - name: total
        in: query
        description: Search debit notes by total amount. Filters debit notes based
          on the final debit note amount including taxes, discounts, and adjustments.
          Useful for finding debit notes within specific price ranges or identifying
          high-value transactions.
        required: false
        schema:
          type: string
        example: 40.6
      - name: balance
        in: query
        description: Search debit notes by outstanding balance. Filters debit notes
          based on the remaining unpaid amount owed by the customer. Useful for finding
          overdue debit notes, tracking receivables, or generating aging reports.
        required: false
        schema:
          type: string
        example: 40.6
      - name: custom_field
        in: query
        description: 'Search debit notes by custom fields.Variants: <code>custom_field_startswith</code>
          and <code>custom_field_contains</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: date
        in: query
        description: 'Search debit notes by debit note date. Filters debit notes based
          on the date they were created. Use yyyy-mm-dd format. Supports variants:
          <code>date_start</code>, <code>date_end</code>, <code>date_before</code>
          and <code>date_after</code>. Useful for finding debit notes within date
          ranges or specific time periods.'
        required: false
        schema:
          type: string
        example: '2013-11-17'
      - name: due_date
        in: query
        description: 'Search debit notes by due date. Filters debit notes based on
          the payment due date. Use yyyy-mm-dd format. Supports variants: <code>due_date_start</code>,
          <code>due_date_end</code>, <code>due_date_before</code> and <code>due_date_after</code>.
          Useful for finding overdue debit notes or debit notes due within specific
          periods.'
        required: false
        schema:
          type: string
        example: '2013-12-03'
      - name: created_date
        in: query
        description: 'Search debit notes by creation date. Filters debit notes based
          on the date they were created. Use yyyy-mm-dd format. Supports variants:
          <code>created_date_start</code>, <code>created_date_end</code>, <code>created_date_before</code>
          and <code>created_date_after</code>. Useful for finding debit notes created
          within specific time periods or date ranges.'
        required: false
        schema:
          type: string
        example: '2013-11-17'
      - name: last_modified_time
        in: query
        required: false
        description: 'Search debit notes modified after a specific time. Filters debit
          notes based on their last modification timestamp. Use YYYY-MM-DDTHH:MM:SS-UTC
          format. Useful for finding recently updated debit notes or syncing data
          changes. Example : 2023-11-18T02:02:51-0800, 2025-19-19T02:02:51%2B0800'
        schema:
          type: string
        example: 2013-11-18T02:02:51-0800
      - name: status
        in: query
        description: 'Search debit notes by status. Filters debit notes based on their
          current state in the billing workflow. Allowed values: <code>sent</code>,
          <code>draft</code>, <code>overdue</code>, <code>paid</code>, <code>void</code>,
          <code>unpaid</code>, <code>partially_paid</code> and <code>viewed</code>.
          Useful for finding debit notes in specific stages of the billing process.'
        required: false
        schema:
          type: string
        example: draft
      - name: customer_id
        in: query
        description: Search debit notes by customer ID. Filters debit notes based
          on the unique identifier of the customer for whom the debit note was created.
          Use the customer ID returned by the Contacts API to find all debit notes
          for a specific customer.
        required: false
        schema:
          type: string
        example: 982000000567001
      - name: filter_by
        in: query
        description: Filter needs to be  <code>Type.DebitNote</code>.
        required: false
        schema:
          type: string
        example: Type.DebitNote
      - name: search_text
        in: query
        description: Search debit notes by debit note number, purchase order, or customer
          name. This parameter performs a general search across multiple debit note
          fields. Maximum length is 100 characters. Useful for quick searches when
          you know part of the debit note information but are unsure which specific
          field to search.
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort debit notes by specific column. Allowed values: <code>customer_name</code>,
          <code>debit_note_number</code>, <code>date</code>, <code>due_date</code>,
          <code>total</code>, <code>balance</code> and <code>created_time</code>.
          Use this parameter to organize debit note results in a specific order for
          better data analysis and reporting.'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number to be fetched from the paginated results. Default
          value is 1. Use this parameter along with per_page to navigate through large
          sets of debit note data. Useful for implementing pagination in your application.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page. Use this parameter
          to control the size of each page of results. Maximum value is 200. Useful
          for optimizing data transfer and improving application performance.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-customer-debit-notes-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    put:
      tags:
      - invoices
      operationId: update_invoice_using_custom_field
      summary: Update an invoice using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        an invoice by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding invoice will be retrieved and updated. Additionally, there
        is an optional X-Upsert header. If the X-Upsert header is true and the custom
        field's unique value is not found in any of the existing invoices, a new invoice
        will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}:
    x-mcp-group:
    - Invoices
    parameters:
    - $ref: '#/components/parameters/invoice_id'
    - $ref: '#/components/parameters/organization_id'
    put:
      tags:
      - invoices
      operationId: update_invoice
      summary: Update an invoice
      description: Update an existing invoice. To delete a line item just remove it
        from the line_items list.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto invoice number generation for this invoice. This
          mandates the invoice number. Allowed values <code>true</code> and <code>false</code>
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    get:
      tags:
      - invoices
      operationId: get_invoice
      summary: Get an invoice
      description: Get the details of an invoice.
      parameters:
      - name: print
        in: query
        description: Print the exported pdf.
        required: false
        schema:
          type: boolean
      - name: accept
        in: query
        description: Get the details of a particular invoice in formats such as json/
          pdf/ html. Default format is json. Allowed values <code>json</code> <code>pdf</code>
          and <code>html</code>
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    delete:
      tags:
      - invoices
      operationId: delete_invoice
      summary: Delete an invoice
      description: Delete an existing invoice. Invoices which have payment or credits
        note applied cannot be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
  /invoices/{invoice_id}/status/sent:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: mark_invoice_sent
      summary: Mark an invoice as sent
      description: Mark a draft invoice as sent.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-an-invoice-as-sent-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/status/void:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: mark_invoice_void
      summary: Void an invoice
      description: Mark an invoice status as void. Upon voiding, the payments and
        credits associated with the invoices will be unassociated and will be under
        customer credits.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/void-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/status/draft:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: mark_invoice_draft
      summary: Mark as draft
      description: Mark a voided invoice as draft.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-draft-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/email:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: email_invoices
      summary: Email multiple invoices
      description: Send invoices to your customers by email. Maximum of 10 invoices
        can be sent at once.
      parameters:
      - name: invoice_ids
        in: query
        description: 'Comma separated invoice ids which are to be emailed. Note: This
          value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-invoices-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-invoices-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /invoices/fromsalesorder:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: create_invoice_from_salesorder
      summary: Create an instant invoice
      description: ' Create an instant invoice for all the confirmed sales orders
        you have selected.'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-instant-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: salesorder_id
      in: query
      required: true
      description: 'Unique identifier of the sales order from which the invoice has
        to be created. Note: This value is a set of numeric characters.'
      schema:
        type: string
      example: 2000000014088
    - $ref: '#/components/parameters/organization_id'
  /invoices/mapwithorder:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: map_invoice_with_salesorder
      summary: Associate invoices with sales order
      description: Associate one or more existing invoices with a sales orders.
      parameters:
      - name: invoice_ids
        in: query
        description: 'Array of invoice IDs to be associated with a sales order. Note:
          This value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: 982000000567114,982000000567115,982000000567116
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/map-invoice-with-salesorder-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
  /invoices/{invoice_id}/submit:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: submit_invoice
      summary: Submit an invoice for approval
      description: Submit an invoice for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-an-invoice-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/approve:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: approve_invoice
      summary: Approve an invoice.
      description: Approve an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/email:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: get_invoice_email
      summary: Get invoice email content
      description: Get the email content of an invoice.
      parameters:
      - name: email_template_id
        in: query
        description: 'Get the email content based on a specific email template. If
          this param is not inputted, then the content will be based on the email
          template associated with the customer. If no template is associated with
          the customer, then default template will be used. Note: This value is a
          set of numeric characters.'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-invoice-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    post:
      tags:
      - invoices
      operationId: email_invoice
      summary: Email an invoice
      description: Email an invoice to the customer. Input json string is not mandatory.
        If input json string is empty, mail will be send with default mail content.
      parameters:
      - name: send_customer_statement
        in: query
        description: Send customer statement pdf a with email.
        required: false
        schema:
          type: boolean
        example: ' '
      - name: send_attachment
        in: query
        description: Send the invoice attachment a with the email.
        required: false
        schema:
          type: boolean
        example: ' '
      - name: attachments
        in: query
        description: Files to be attached to the email
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-an-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/paymentreminder:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: remind_customer_for_invoice_payment
      summary: Remind Customer
      description: Remind your customer about an unpaid invoice by email. Reminder
        will be sent, only for the invoices which are in open or overdue status.
      parameters:
      - name: send_customer_statement
        in: query
        description: Send customer statement pdf a with email.
        required: false
        schema:
          type: boolean
        example: ' '
      - name: attachments
        in: query
        description: Files to be attached to the email
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/remind-customer-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/remind-customer-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    get:
      tags:
      - invoices
      operationId: get_payment_reminder_mail_content_for_invoice
      summary: Get payment reminder mail content
      description: Get the mail content of the payment reminder.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-payment-reminder-mail-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/paymentreminder:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: bulk_invoice_reminder
      summary: Bulk invoice reminder
      description: Remind your customer about an unpaid invoices by email. Reminder
        mail will be send, only for the invoices is in open or overdue status. Maximum
        10 invoices can be reminded at once.
      parameters:
      - name: invoice_ids
        in: query
        description: 'Array of invoice ids for which the reminder has to be sent.
          Note: This value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: ''
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-invoice-reminder-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /invoices/pdf:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: bulk_export_invoices_as_pdf
      summary: Bulk export Invoices
      description: Maximum of 25 invoices can be exported in a single pdf.
      parameters:
      - name: invoice_ids
        in: query
        description: 'Comma separated invoice ids which are to be export as pdf. Note:
          This value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-export-invoices-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /invoices/print:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: bulk_print_invoices
      summary: Bulk print invoices
      description: Export invoices as pdf and print them. Maximum of 25 invoices can
        be printed.
      parameters:
      - name: invoice_ids
        in: query
        description: 'Export invoices as pdf and print them. Maximum of 25 invoices
          can be printed. Note: This value is a set of numeric characters separated
          by comma.'
        required: true
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-print-invoices-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/paymentreminder/disable:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: disable_invoice_payment_reminder
      summary: Disable payment reminder
      description: Disable automated payment reminders for an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/disable-payment-reminder-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: Unique identifier of the invoice.
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/paymentreminder/enable:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: enable_invoice_payment_reminder
      summary: Enable payment reminder
      description: Enable automated payment reminders for an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/enable-payment-reminder-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/writeoff:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: write_off_invoice
      summary: Write off invoice
      description: Write off the invoice balance amount of an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/write-off-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/writeoff/cancel:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: cancel_write_off_invoice
      summary: Cancel write off
      description: Cancel the write off amount of an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/cancel-write-off-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/address/billing:
    x-mcp-group:
    - Invoices
    put:
      tags:
      - invoices
      operationId: update_invoice_billing_address
      summary: Update billing address
      description: Updates the billing address for this invoice alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/address/shipping:
    x-mcp-group:
    - Invoices
    put:
      tags:
      - invoices
      operationId: update_invoice_shipping_address
      summary: Update shipping address
      description: Updates the shipping address for this invoice alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-shipping-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-shipping-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/templates:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: list_invoice_templates
      summary: List invoice templates
      description: Get all invoice pdf templates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-invoice-templates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/templates/{template_id}:
    x-mcp-group:
    - Invoices
    put:
      tags:
      - invoices
      operationId: update_invoice_template
      summary: Update invoice template
      description: Update the pdf template associated with the invoice.
      parameters:
      - name: template_id
        in: path
        required: true
        description: 'Unique identifier of the invoice template. Note: This value
          is a set of numeric characters.'
        schema:
          type: string
        example: 982000000000143
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-invoice-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/payments:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: list_invoice_payments
      summary: List invoice payments
      description: Get the list of payments made for an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-invoice-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/creditsapplied:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: list_invoice_credits_applied
      summary: List credits applied
      description: Get the list of credits applied for an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-credits-applied-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/credits:
    x-mcp-group:
    - Invoices
    post:
      tags:
      - invoices
      operationId: apply_credits_to_invoice
      summary: Apply credits
      description: Apply the customer credits either from credit notes or excess customer
        payments to an invoice. Multiple credits can be applied at once.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/apply-credits-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/apply-credits-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/payments/{invoice_payment_id}:
    x-mcp-group:
    - Invoices
    delete:
      tags:
      - invoices
      operationId: delete_invoice_payment
      summary: Delete a payment
      description: Delete a payment made to an invoice.
      parameters:
      - name: invoice_payment_id
        in: path
        required: true
        description: 'Unique identifier of the invoice payment. Note: This value is
          a set of numeric characters.'
        schema:
          type: string
        example: 982000000567192
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/creditsapplied/{creditnotes_invoice_id}:
    x-mcp-group:
    - Invoices
    delete:
      tags:
      - invoices
      operationId: delete_invoice_applied_credit
      summary: Delete applied credit
      description: Delete a particular credit applied to an invoice.
      parameters:
      - name: creditnotes_invoice_id
        in: path
        required: true
        description: 'Unique identifier of the credit note invoice. Note: This value
          is a set of numeric characters.'
        schema:
          type: string
        example: '982000000567172'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-applied-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/attachment:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: get_invoice_attachment
      summary: Get last attached attachment
      description: Returns the file attached to the invoice.
      parameters:
      - name: preview
        in: query
        description: Get the thumbnail of the attachment.
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-invoice-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    post:
      tags:
      - invoices
      operationId: add_invoice_attachment
      summary: Add attachment to an invoice
      description: Attach a file to an invoice.
      parameters:
      - name: can_send_in_mail
        in: query
        description: True to send the attachment with the invoice when emailed.
        required: false
        schema:
          type: boolean
        example: true
      - name: attachment
        in: query
        description: 'The file to be attached.Allowed Extensions: <code>gif</code>,
          <code>png</code>, <code>jpeg</code>, <code>jpg</code>, <code>bmp</code>
          and <code>pdf</code>'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    put:
      tags:
      - invoices
      operationId: update_invoice_attachment_preference
      summary: Update attachment preference
      description: Set whether you want to send the attached file while emailing the
        invoice.
      parameters:
      - name: can_send_in_mail
        in: query
        description: Boolean to send the attachment with the invoice when emailed.
        required: true
        schema:
          type: boolean
        example: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-attachment-preference-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    delete:
      tags:
      - invoices
      operationId: delete_invoice_attachment
      summary: Delete last attached attachment
      description: Delete the file attached to the invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/documents/{document_id}:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: get_invoice_document_details
      summary: Retrieve Invoice Document
      description: Retrieve a specific document attached to an invoice with advanced
        retrieval options.
      parameters:
      - name: invoice_id
        in: path
        required: true
        description: 'Unique identifier of the invoice. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
      - name: document_id
        in: path
        required: true
        description: 'Unique identifier of the document. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
      - name: responseformat
        in: query
        description: Specify the response format (e.g., json, pdf, html)
        required: false
        schema:
          type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-invoice-document-response'
            application/pdf:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    delete:
      tags:
      - invoices
      operationId: delete_invoice_document
      summary: Delete Invoice attachment
      description: Delete a specific document attached to an invoice. This operation
        permanently removes the document from the invoice and cannot be undone. Only
        documents that are not system-generated can be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-invoice-document-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - name: document_id
      in: path
      required: true
      description: 'Unique identifier of the document. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567200
    - $ref: '#/components/parameters/organization_id'
  /invoices/expenses/{expense_id}/receipt:
    x-mcp-group:
    - Invoices
    delete:
      tags:
      - invoices
      operationId: delete_invoice_expense_receipt
      summary: Delete the expense receipt
      description: Delete the expense receipts attached to an invoice which is raised
        from an expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-the-expense-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: expense_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: '460000000028080'
    - $ref: '#/components/parameters/organization_id'
  /invoice/{invoice_id}/customfields:
    x-mcp-group:
    - Invoices
    put:
      tags:
      - invoices
      operationId: update_custom_fields_in_invoice
      summary: Update custom field in existing invoices
      description: Update the value of the custom field in existing invoices.
      parameters:
      - $ref: '#/components/parameters/invoice_id'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-invoice-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
  /invoices/{invoice_id}/comments:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: list_invoice_comments
      summary: List invoice comments & history
      description: Get the complete history and comments of an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-invoice-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    post:
      tags:
      - invoices
      operationId: add_invoice_comment
      summary: Add comment
      description: Add a comment for an invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /invoices/{invoice_id}/comments/{comment_id}:
    x-mcp-group:
    - Invoices
    put:
      tags:
      - invoices
      operationId: update_invoice_comment
      summary: Update comment
      description: Update an existing comment of an invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    delete:
      tags:
      - invoices
      operationId: delete_invoice_comment
      summary: Delete a comment
      description: Delete an invoice comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: 982000000567019
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: invoice_id
      in: path
      required: true
      description: 'Unique identifier of the invoice. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567114
    - name: comment_id
      in: path
      required: true
      description: 'Unique identifier of the comment. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000567019
    - $ref: '#/components/parameters/organization_id'
  /share/paymentlink:
    x-mcp-group:
    - Invoices
    get:
      tags:
      - invoices
      operationId: generate_invoice_payment_link
      summary: Generate payment link
      description: This API generates a payment link for the invoice with an expiry
        date.
      parameters:
      - $ref: '#/components/parameters/transaction_id'
      - $ref: '#/components/parameters/transaction_type'
      - $ref: '#/components/parameters/link_type'
      - $ref: '#/components/parameters/expiry_time'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payment-link-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.ALL
    parameters:
    - $ref: '#/components/parameters/transaction_id'
    - $ref: '#/components/parameters/transaction_type'
    - $ref: '#/components/parameters/link_type'
    - $ref: '#/components/parameters/expiry_time'
    - $ref: '#/components/parameters/organization_id'
  /salesreceipts:
    x-mcp-group:
    - Sales Receipt
    post:
      tags:
      - sales-receipt
      operationId: create_sales_receipt
      summary: Create a sales receipt
      description: Create a sales receipt for immediate payment transactions.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto sales receipt number generation for this sales receipt.
          This mandates the sales receipt number.
        required: false
        schema:
          type: boolean
        example: false
      - name: can_send_in_mail
        in: query
        description: Send the sales receipt to customer via email.
        required: false
        schema:
          type: boolean
        example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-sales-receipt-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-sales-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    get:
      tags:
      - sales-receipt
      operationId: list_sales_receipts
      summary: List sales receipts
      description: List all sales receipts.
      parameters:
      - name: receipt_number
        in: query
        description: Search receipt by receipt number. Filters receipts based on their
          unique identifier. Supports <code>receipt_number_startswith</code> and <code>receipt_number_contains</code>
          variants. Maximum length is 100 characters. Useful for finding specific
          receipts or receipts with similar numbering patterns.
        required: false
        schema:
          type: string
        example: INV-00003
      - name: item_name
        in: query
        description: Search receipt by item name. Supports <code>item_name_startswith</code>
          and <code>item_name_contains</code> variants. Maximum length is 100 characters.
        required: false
        schema:
          type: string
        example: Hard Drive
      - name: sort_column
        in: query
        description: Specify the column field for sorting sales receipt results. Available
          options include <code>customer_name</code>, <code>receipt_number</code>,
          <code>date</code>, <code>total</code>, and <code>created_time</code>.
        required: false
        schema:
          type: string
        example: created_time
      - name: filter_by
        in: query
        description: 'Filter sales receipts by status. Allowed Values: <code>ThisWeek</code>,
          <code>ThisMonth</code>, <code>ThisQuarter</code>, <code>ThisYear</code>,<code>PreviousDay</code>,
          <code>PreviousWeek</code>, <code>PreviousMonth</code>, <code>PreviousQuarter</code>,
          <code>PreviousYear</code>, <code>Status.All</code>, <code>Status.Draft</code>,
          <code>Status.Sent</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: customer_id
        in: query
        description: Filter sales receipts by specific customer identifier.
        required: false
        schema:
          type: string
        example: '460000000017138'
      - name: date
        in: query
        description: 'Filter sales receipts by date. Available variants include <code>date_start</code>,
          <code>date_end</code>, <code>date_before</code>, and <code>date_after</code>.
          Default date format: <code>yyyy-mm-dd</code>.'
        required: false
        schema:
          type: string
        example: '2014-07-28'
      - name: total
        in: query
        description: Filter sales receipts by total amount using various range operators.
          Available variants include <code>total_start</code>, <code>total_end</code>,
          <code>total_less_than</code>, <code>total_greater_than</code>.
        required: false
        schema:
          type: number
          format: double
        example: 1000
      - name: page
        in: query
        description: Specify the page number for paginated results retrieval. Default
          value is 1 for the first page.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Specify the maximum number of sales receipt records to return
          per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-sales-receipts-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /salesreceipts/{sales_receipt_id}:
    x-mcp-group:
    - Sales Receipt
    put:
      tags:
      - sales-receipt
      operationId: update_sales_receipt
      summary: Update a sales receipt
      description: Update an existing sales receipt.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-sales-receipt-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-sales-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    get:
      tags:
      - sales-receipt
      operationId: get_sales_receipt
      summary: Get a sales receipt
      description: Get the details of a sales receipt.
      parameters:
      - name: accept
        in: query
        description: 'Get the details of a particular sales receipt in formats such
          as json/ pdf/ html. Default format is json. Allowed Values: <code>json</code>,
          <code>pdf</code>, <code>html</code>.'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-sales-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    delete:
      tags:
      - sales-receipt
      operationId: delete_sales_receipt
      summary: Delete a sales receipt
      description: Delete an existing sales receipt.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-sales-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: sales_receipt_id
      in: path
      description: Unique identifier of the sales receipt.
      required: true
      schema:
        type: string
      example: '460000000039129'
    - $ref: '#/components/parameters/organization_id'
  /salesreceipts/{sales_receipt_id}/email:
    x-mcp-group:
    - Sales Receipt
    post:
      tags:
      - sales-receipt
      operationId: email_sales_receipt
      summary: Email a sales receipt
      description: Email a sales receipt to the customer.
      parameters:
      - name: attach_pdf
        in: query
        description: Send the sales receipt pdf with the email.
        required: false
        schema:
          type: boolean
        example: false
      - name: sales_receipt_id
        in: path
        description: ID of the sales receipt to be emailed
        required: true
        schema:
          type: string
        example: '460000000039129'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-a-sales-receipt-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-a-sales-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
  /deliverychallans:
    x-mcp-group:
    - Delivery Challans
    get:
      tags:
      - delivery-challans
      operationId: list_delivery_challans
      summary: List delivery challans
      description: Retrieve a list of delivery challans with pagination. Filter by
        status, customer, date, and more.
      parameters:
      - $ref: '#/components/parameters/organization_id'
      - name: sort_column
        in: query
        description: Specify the column to sort Delivery challans by. Allowed values
          <code>customer_name</code>, <code>deliverychallan_number</code>, <code>total</code>,
          <code>date</code>, <code>created_time</code>, <code>last_modified_time</code>.
        required: false
        schema:
          type: string
        example: created_time
      - name: sort_order
        in: query
        description: Sort order (A for Ascending, D for Descending). Allowed values
          <code>A</code> (ascending), <code>D</code> (descending).
        required: false
        schema:
          type: string
        example: D
      - name: filter_by
        in: query
        description: Filter delivery challans by status. Allowed values <code>Status.All</code>,
          <code>Status.Draft</code>, <code>Status.Open</code>, <code>Status.Returned</code>,
          <code>Status.Delivered</code>, <code>Status.Invoiced</code>, <code>Status.PartiallyInvoiced</code>.
        required: false
        schema:
          type: string
        example: Status.All
      - name: search_text
        in: query
        description: Search delivery challans by challan number or customer name.
        required: false
        schema:
          type: string
        example: DC-00001
      - name: customer_id
        in: query
        description: Filter by ID of the Customer for whom the delivery challan is
          created.
        required: false
        schema:
          type: string
        example: '982000000567001'
      - name: status
        in: query
        description: Search Delivery challans by delivery challan status. Allowed
          values <code>draft</code>, <code>open</code>, <code>void</code>, <code>fulfilled</code>,
          <code>invoiced</code>, <code>partially_invoiced</code>.
        required: false
        schema:
          type: string
        example: open
      - name: page
        in: query
        description: Page number to be fetched.
        required: false
        schema:
          type: integer
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-delivery-challans-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.READ
    post:
      tags:
      - delivery-challans
      operationId: create_delivery_challan
      summary: Create a delivery challan
      description: Create a new delivery challan for a customer. The customer ID is
        required.
      parameters:
      - $ref: '#/components/parameters/organization_id'
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto number generation for this delivery challan. Allowed
          values <code>true</code> and <code>false</code>.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-delivery-challan-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.CREATE
  /deliverychallans/{deliverychallan_id}:
    x-mcp-group:
    - Delivery Challans
    get:
      tags:
      - delivery-challans
      operationId: get_delivery_challan
      summary: Retrieve a delivery challan
      description: Retrieve the details of an existing delivery challan.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      - name: print
        in: query
        description: Print the delivery challan in PDF format. Allowed values <code>true</code>
          and <code>false</code>.
        required: false
        schema:
          type: boolean
        example: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.READ
    put:
      tags:
      - delivery-challans
      operationId: update_delivery_challan
      summary: Update a delivery challan
      description: Update an existing delivery challan.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-delivery-challan-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.UPDATE
    delete:
      tags:
      - delivery-challans
      operationId: delete_delivery_challan
      summary: Delete a delivery challan
      description: Delete an existing delivery challan.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.DELETE
  /deliverychallans/{deliverychallan_id}/status/open:
    x-mcp-group:
    - Delivery Challans
    post:
      tags:
      - delivery-challans
      operationId: mark_delivery_challan_as_open
      summary: Mark a delivery challan as open
      description: Change the status of a delivery challan to open.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.CREATE
  /deliverychallans/{deliverychallan_id}/status/delivered:
    x-mcp-group:
    - Delivery Challans
    post:
      tags:
      - delivery-challans
      operationId: mark_delivery_challan_as_delivered
      summary: Mark a delivery challan as delivered
      description: Change the status of a delivery challan to delivered.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.CREATE
  /deliverychallans/{deliverychallan_id}/status/returned:
    x-mcp-group:
    - Delivery Challans
    post:
      tags:
      - delivery-challans
      operationId: mark_delivery_challan_as_returned
      summary: Mark a delivery challan as returned
      description: Change the status of a delivery challan to returned.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.CREATE
  /deliverychallans/{deliverychallan_id}/status/undelivered:
    x-mcp-group:
    - Delivery Challans
    post:
      tags:
      - delivery-challans
      operationId: mark_delivery_challan_as_undelivered
      summary: Mark a delivery challan as undelivered
      description: Change the status of a delivery challan to undelivered.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.CREATE
  /deliverychallans/return:
    x-mcp-group:
    - Delivery Challans
    put:
      tags:
      - delivery-challans
      operationId: return_delivery_challans
      summary: Return delivery challans
      description: Partially return one or more delivery challans by specifying the
        line items and quantities to return.
      parameters:
      - $ref: '#/components/parameters/organization_id'
      - name: deliverychallan_ids
        in: query
        required: true
        description: Comma-separated IDs of delivery challans to mark as returned.
        schema:
          type: string
        example: '982000000567001'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/return-delivery-challan-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.UPDATE
  /deliverychallans/undo/return:
    x-mcp-group:
    - Delivery Challans
    put:
      tags:
      - delivery-challans
      operationId: undo_return_delivery_challans
      summary: Undo return of delivery challans
      description: Undo a previously applied return for one or more delivery challans.
      parameters:
      - $ref: '#/components/parameters/organization_id'
      - name: deliverychallan_ids
        in: query
        required: true
        description: Comma-separated IDs of delivery challans to undo return.
        schema:
          type: string
        example: '982000000567001'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.UPDATE
  /deliverychallans/{deliverychallan_id}/attachment:
    x-mcp-group:
    - Delivery Challans
    post:
      tags:
      - delivery-challans
      operationId: add_attachment_to_delivery_challan
      summary: Add attachment to a delivery challan
      description: Attach files to an existing delivery challan (maximum of 20 files).'
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                attachment:
                  type: string
                  format: binary
                  description: File to attach. Max size 10 MB.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.CREATE
  /deliverychallans/{deliverychallan_id}/documents/{document_id}:
    x-mcp-group:
    - Delivery Challans
    get:
      tags:
      - delivery-challans
      operationId: get_delivery_challan_attachment
      summary: Get a delivery challan attachment
      description: Retrieve an attachment.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - name: document_id
        in: path
        required: true
        description: Unique identifier of the document.
        schema:
          type: string
        example: '982000000567020'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: Returns the file content.
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.READ
    delete:
      tags:
      - delivery-challans
      operationId: delete_delivery_challan_attachment
      summary: Delete a delivery challan attachment
      description: Delete an attachment.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - name: document_id
        in: path
        required: true
        description: Unique identifier of the document.
        schema:
          type: string
        example: '982000000567020'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.UPDATE
  /deliverychallans/templates:
    x-mcp-group:
    - Delivery Challans
    get:
      tags:
      - delivery-challans
      operationId: list_delivery_challan_templates
      summary: List delivery challan templates
      description: Retrieve a list of available templates for delivery challans.
      parameters:
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-templates-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.READ
  /deliverychallans/{deliverychallan_id}/templates/{template_id}:
    x-mcp-group:
    - Delivery Challans
    put:
      tags:
      - delivery-challans
      operationId: update_delivery_challan_template
      summary: Update a delivery challan template
      description: Assign a different template to an existing delivery challan.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - name: template_id
        in: path
        required: true
        description: Unique identifier of the template.
        schema:
          type: string
        example: '982000000567030'
      - $ref: '#/components/parameters/organization_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.UPDATE
  /deliverychallans/{deliverychallan_id}/address/shipping:
    x-mcp-group:
    - Delivery Challans
    put:
      tags:
      - delivery-challans
      operationId: update_delivery_challan_shipping_address
      summary: Update shipping address
      description: Update the shipping address of an existing delivery challan.
      parameters:
      - $ref: '#/components/parameters/deliverychallan_id'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-shipping-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/action-delivery-challan-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.deliverychallans.UPDATE
  /recurringinvoices:
    x-mcp-group:
    - Recurring Invoices
    post:
      tags:
      - recurring-invoices
      operationId: create_recurring_invoice
      summary: Create a Recurring Invoice
      description: Creating a new recurring invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-recurring-invoice-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    get:
      tags:
      - recurring-invoices
      operationId: list_recurring_invoices
      summary: List all  Recurring Invoice
      description: List the details of all recurring invoice.
      parameters:
      - name: recurrence_name
        in: query
        description: Unique name for the recurring profile given by the user. Max-length
          [100]
        required: false
        schema:
          type: string
        example: MonthlyInvoice
      - name: item_name
        in: query
        description: 'Search recurring invoices by item name. Variants: <code>item_name_startswith</code>
          and <code>profileitemname_contains</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: item_description
        in: query
        description: 'Search recurring invoices by item description. Variants: <code>item_description_startswith</code>
          and <code>item_description_contains</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: customer_name
        in: query
        description: Name of the customer to whom the recurring invoice is raised.
        required: false
        schema:
          type: string
        example: John Smith
      - name: line_item_id
        in: query
        description: The line item id
        required: false
        schema:
          type: string
        example: '982000000567021'
      - name: item_id
        in: query
        description: Unique string generated for the item for which invoice is to
          be sent.
        required: false
        schema:
          type: string
        example: '90300000081501'
      - name: tax_id
        in: query
        description: ID of the tax or tax group associated to the recurring invoice.
        required: false
        schema:
          type: string
        example: '903000000000356'
      - name: notes
        in: query
        description: A short note for the recurring invoice.
        required: false
        schema:
          type: string
        example: Offer for the referral
      - name: start_date
        in: query
        description: The date on which the recurring invoice starts.
        required: false
        schema:
          type: string
        example: '2016-06-12'
      - name: end_date
        in: query
        description: The date on which the recurring invoice expires.
        required: false
        schema:
          type: string
        example: '2017-06-12'
      - name: customer_id
        in: query
        description: Customer ID of the customer for whom the recurring invoice is
          raised.
        required: false
        schema:
          type: string
        example: '903000000000099'
      - name: status
        in: query
        description: Status of the recurring invoice. This can be <code>active</code>,
          <code>stopped</code> or <code>expired</code>.
        required: false
        schema:
          type: string
        example: active
      - name: filter_by
        in: query
        description: 'Filter  Recurring invoices by any status or payment expected
          date.Allowed Values: <code> Status.All</code>, <code>Status.Active</code>,
          <code> Status.Stopped</code>, <code>Status.Expired</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Search invoices by invoice number or purchase order or customer
          name. Max-length [100]
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: Column used to sort the recurring invoices list.
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-all-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    put:
      tags:
      - recurring-invoices
      operationId: update_recurring_invoice_using_custom_field
      summary: Update a recurring invoice using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a recurring invoice by providing its API name in the X-Unique-Identifier-Key
        header and its value in the X-Unique-Identifier-Value header. Based on this
        value, the corresponding recurring invoice will be retrieved and updated.
        Additionally, there is an optional X-Upsert header. If the X-Upsert header
        is true and the custom field's unique value is not found in any of the existing
        recurring invoices, a new recurring invoice will be created if the necessary
        payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-recurring-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /recurringinvoices/{recurring_invoice_id}:
    x-mcp-group:
    - Recurring Invoices
    put:
      tags:
      - recurring-invoices
      operationId: update_recurring_invoice
      summary: Update Recurring Invoice
      description: Update the recurring invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-recurring-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    get:
      tags:
      - recurring-invoices
      operationId: get_recurring_invoice
      summary: Get a Recurring Invoice
      description: Get the details of a recurring invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    delete:
      tags:
      - recurring-invoices
      operationId: delete_recurring_invoice
      summary: Delete a Recurring Invoice
      description: Delete an existing recurring invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: recurring_invoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the recurring invoice.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /recurringinvoices/{recurring_invoice_id}/status/stop:
    x-mcp-group:
    - Recurring Invoices
    post:
      tags:
      - recurring-invoices
      operationId: stop_recurring_invoice
      summary: Stop a Recurring Invoice
      description: Stop an active recurring invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/stop-a-recurring-invoice-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stop-a-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: recurring_invoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the recurring invoice.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /recurringinvoices/{recurring_invoice_id}/status/resume:
    x-mcp-group:
    - Recurring Invoices
    post:
      tags:
      - recurring-invoices
      operationId: resume_recurring_invoice
      summary: Resume a Recurring Invoice
      description: Resume a stopped recurring invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/resume-a-recurring-invoice-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/resume-a-recurring-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: recurring_invoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the recurring invoice.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /recurringinvoices/{recurring_invoice_id}/templates/{template_id}:
    x-mcp-group:
    - Recurring Invoices
    put:
      tags:
      - recurring-invoices
      operationId: update_recurring_invoice_template
      summary: Update Recurring Invoice template
      description: Update the pdf template associated with the recurring invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-recurring-invoice-template-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-recurring-invoice-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: recurring_invoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the recurring invoice.
      example: '90300000072369'
    - name: template_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the recurring invoice template.
      example: '90300000001336'
    - $ref: '#/components/parameters/organization_id'
  /recurringinvoices/{recurring_invoice_id}/comments:
    x-mcp-group:
    - Recurring Invoices
    get:
      tags:
      - recurring-invoices
      operationId: list_recurring_invoice_history
      summary: List Recurring Invoice History
      description: Get the complete history and comments of a recurring invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-recurring-invoice-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - name: recurring_invoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the recurring invoice.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: create_credit_note
      summary: Create a credit note
      description: Create a new credit note to record credits issued to customers
        for returned items, overpayments, or adjustments. Supports multi-currency
        transactions, custom line items, tax calculations, and workflows.
      parameters:
      - name: invoice_id
        in: query
        description: Invoice ID of the required invoice.
        required: false
        schema:
          type: string
        example: '90300000079426'
      - name: ignore_auto_number_generation
        in: query
        description: Set to true if you need to provide your own credit note number.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-credit-note-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    get:
      tags:
      - credit-notes
      operationId: list_credit_notes
      summary: List all Credit Notes
      description: Retrieve a paginated list of credit notes with comprehensive filtering,
        sorting, and search capabilities. Use query parameters to filter by date,
        status, amount, customer details, items, taxes, and custom fields.
      parameters:
      - name: creditnote_number
        in: query
        description: Filter credit notes by specific credit note number. Use this
          parameter to search for a particular credit note by its unique number. Max-Length
          [100]
        required: false
        schema:
          type: string
        example: CN-29
      - name: date
        in: query
        description: Filter credit notes by the date they were raised. Use yyyy-mm-dd
          format to search for credit notes created on a specific date.
        required: false
        schema:
          type: string
        example: '2016-06-05'
      - name: status
        in: query
        description: 'Filter credit notes by their current status. Allowed values:
          <code>open</code>, <code>closed</code>, <code>void</code>, or <code>draft</code>.'
        required: false
        schema:
          type: string
        example: draft
      - name: total
        in: query
        description: Filter credit notes by their total amount. Use this parameter
          to search for credit notes with a specific total value.
        required: false
        schema:
          type: number
          format: double
        example: 450
      - name: reference_number
        in: query
        description: Filter credit notes by their reference number. Use this parameter
          to search for credit notes with a specific reference number. Max-Length
          [100]
        required: false
        schema:
          type: string
        example: INV-384
      - name: customer_name
        in: query
        description: Filter credit notes by customer name. Use this parameter to search
          for credit notes associated with a specific customer. Max-Length [100]
        required: false
        schema:
          type: string
        example: Bowman Furniture
      - name: item_name
        in: query
        description: Filter credit notes by item name. Use this parameter to search
          for credit notes containing specific items. Max-Length [100]
        required: false
        schema:
          type: string
        example: kit
      - name: customer_id
        in: query
        description: Filter credit notes by customer ID. Use this parameter to search
          for credit notes associated with a specific customer. Retrieve customer
          IDs from the contacts API.
        required: false
        schema:
          type: string
        example: '903000000000099'
      - name: item_description
        in: query
        description: Filter credit notes by item description. Supports variants <code>item_description_startswith</code>
          and <code>item_description_contains</code> for flexible searching. Max-length
          [100]
        required: false
        schema:
          type: string
        example: ''
      - name: item_id
        in: query
        description: Filter credit notes by item ID. Use this parameter to search
          for credit notes containing a specific item. Retrieve item IDs from the
          items API.
        required: false
        schema:
          type: string
        example: '90300000081501'
      - name: line_item_id
        in: query
        description: Filter credit notes by line item ID. Use this parameter to search
          for credit notes containing a specific line item within the credit note.
        required: false
        schema:
          type: string
        example: 903000006245
      - name: tax_id
        in: query
        description: Filter credit notes by tax ID. Use this parameter to search for
          credit notes with a specific tax applied. Retrieve tax IDs from the taxes
          API.
        required: false
        schema:
          type: string
        example: '903000000000356'
      - name: filter_by
        in: query
        description: 'Filter credit notes by status using predefined status values.
          Allowed values: <code>Status.All</code>, <code>Status.Open</code>, <code>Status.Draft</code>,
          <code>Status.Closed</code>, and <code>Status.Void</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Search credit notes using text search across multiple fields.
          Searches credit note number, customer name, and reference number. Max-length
          [100]
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort credit notes by specified column. Allowed values: <code>customer_name</code>,
          <code>creditnote_number</code>, <code>balance</code>, <code>total</code>,
          <code>date</code>, and <code>created_time</code>.'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number for pagination. Specify which page of results to
          retrieve. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records per page for pagination. Controls how many
          credit notes are returned in each page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-all-credit-notes-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    put:
      tags:
      - credit-notes
      operationId: update_credit_note_using_custom_field
      summary: Update a credit note using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a credit note by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding credit note will be retrieved and updated. Additionally,
        there is an optional X-Upsert header. If the X-Upsert header is true and the
        custom field's unique value is not found in any of the existing credit notes,
        a new credit note will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-credit-note-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}:
    x-mcp-group:
    - Credit Notes
    put:
      tags:
      - credit-notes
      operationId: update_credit_note
      summary: Update a credit note
      description: Details of an existing creditnote.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Set to true if you need to provide your own credit note number.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-credit-note-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.UPDATE
    get:
      tags:
      - credit-notes
      operationId: get_credit_note
      summary: Get a credit note
      description: Details of an existing creditnote.
      parameters:
      - name: print
        in: query
        description: 'Export credit note pdf with default print option. Allowed Values:
          <code>true</code>, <code>false</code>, <code>on</code> and <code>off</code>'
        required: false
        schema:
          type: boolean
        example: true
      - name: accept
        in: query
        description: 'You can get credit note details as json/pdf/html. Default format
          is html. Allowed Values: <code>json</code>, <code>pdf</code> and <code>html</code>'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    delete:
      tags:
      - credit-notes
      operationId: delete_credit_note
      summary: Delete a credit note
      description: Delete an existing credit note.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.DELETE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/email:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: email_credit_note
      summary: Email a credit note
      description: Email a credit note.
      parameters:
      - name: customer_id
        in: query
        description: Customer ID of the customer for whom the credit note is raised.
        required: false
        schema:
          type: string
        example: '903000000000099'
      - name: attachments
        in: query
        description: The files to be attached with the email.
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-a-credit-note-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    get:
      tags:
      - credit-notes
      operationId: get_credit_note_email
      summary: Get email content
      description: Get email content of a credit note.
      parameters:
      - name: email_template_id
        in: query
        description: Get the email content based on a specific email template. If
          this param is not inputted, then the content will be based on the email
          template associated with the customer. If no template is associated with
          the customer, then default template will be used.
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/status/void:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: mark_credit_note_void
      summary: Void a Credit Note
      description: Mark the credit note as Void.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/void-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/status/draft:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: mark_credit_note_draft
      summary: Convert Credit Note to Draft.
      description: Convert a voided credit note to Draft.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/convert-credit-note-to-draft-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/status/open:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: mark_credit_note_open
      summary: Convert to Open
      description: Convert a credit note in Draft status to Open.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/convert-to-open-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/submit:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: submit_credit_note
      summary: Submit a credit note for approval
      description: Submit an estimate for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-a-credit-note-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/approve:
    x-mcp-group:
    - Credit Notes
    post:
      tags:
      - credit-notes
      operationId: approve_credit_note
      summary: Approve a credit note.
      description: Approve a credit note.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/emailhistory:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: get_credit_note_email_history
      summary: Email history
      description: Get email history of a credit code.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/address/billing:
    x-mcp-group:
    - Credit Notes
    put:
      tags:
      - credit-notes
      operationId: update_credit_note_billing_address
      summary: Update billing address
      description: Updates the billing address for an existing credit note alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.UPDATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/address/shipping:
    x-mcp-group:
    - Credit Notes
    put:
      tags:
      - credit-notes
      operationId: update_credit_note_shipping_address
      summary: Update Shipping address
      description: Updates the shipping address for an existing credit note alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-shipping-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-shipping-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.UPDATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/templates:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: list_credit_note_templates
      summary: List credit note template
      description: Get all credit note pdf templates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-credit-note-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/templates/{template_id}:
    x-mcp-group:
    - Credit Notes
    put:
      tags:
      - credit-notes
      operationId: update_credit_note_template
      summary: Update a credit note template
      description: Update the pdf template associated with the credit note.
      parameters:
      - name: template_id
        in: path
        required: true
        description: Unique identifier of the credit note template.
        schema:
          type: string
        example: '90300000001336'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-credit-note-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.UPDATE
    parameters:
    - name: creditnote_id
      in: path
      required: true
      description: Unique identifier of the credit note.
      schema:
        type: string
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/invoices:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: list_invoices_of_credit_note
      summary: List invoices credited
      description: List invoices to which the credit note is applied.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-invoices-credited-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    post:
      tags:
      - credit-notes
      operationId: apply_credit_note_to_invoice
      summary: Credit to an invoice
      description: Apply credit note to existing invoices.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/credit-to-an-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/credit-to-an-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/invoices/{creditnote_invoice_id}:
    x-mcp-group:
    - Credit Notes
    delete:
      tags:
      - credit-notes
      operationId: delete_invoice_of_credit_note
      summary: Delete invoices credited
      description: Delete the credits applied to an invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-invoices-credited-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.DELETE
    parameters:
    - name: creditnote_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note.
      example: '90300000072369'
    - name: creditnote_invoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note invoice.
      example: '982000000567172'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/comments:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: list_credit_note_comments
      summary: List credit note comments & history
      description: Get history and comments of a credit note.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-credit-note-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    post:
      tags:
      - credit-notes
      operationId: add_credit_note_comment
      summary: Add a comment
      description: Add a comment to an existing credit note.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-a-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/comments/{comment_id}:
    x-mcp-group:
    - Credit Notes
    delete:
      tags:
      - credit-notes
      operationId: delete_credit_note_comment
      summary: Delete a Comment
      description: Delete a credit note comment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.DELETE
    parameters:
    - name: creditnote_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note.
      example: '90300000072369'
    - name: comment_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the comment.
      example: '982000000570001'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/refunds:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: list_credit_note_refunds_of_all_credit_notes
      summary: List credit note refunds
      description: List all refunds with pagination.
      parameters:
      - name: customer_id
        in: query
        description: Customer ID of the customer for whom the credit note is raised.
        required: false
        schema:
          type: string
        example: '903000000000099'
      - name: sort_column
        in: query
        description: 'Sort refunds list. Allowed Values: <code>refund_mode</code>,
          <code>reference_number</code>, <code>date</code>, <code>creditnote_number</code>,
          <code>customer_name</code>, <code>amount_bcy</code> and <code>amount_fcy</code>'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number for pagination. Specify which page of results to
          retrieve. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records per page for pagination. Controls how many
          credit notes are returned in each page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-credit-note-refunds-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/refunds:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: list_credit_note_refunds_of_a_credit_note
      summary: List refunds of a credit note
      description: List all refunds of an existing credit note.
      parameters:
      - name: page
        in: query
        description: Page number for pagination. Specify which page of results to
          retrieve. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records per page for pagination. Controls how many
          credit notes are returned in each page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-refunds-of-a-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    post:
      tags:
      - credit-notes
      operationId: create_credit_note_refund
      summary: Refund credit note
      description: Refund credit note amount.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refund-credit-note-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refund-credit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.CREATE
    parameters:
    - name: creditnote_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /creditnotes/{creditnote_id}/refunds/{creditnote_refund_id}:
    x-mcp-group:
    - Credit Notes
    get:
      tags:
      - credit-notes
      operationId: get_credit_note_refund
      summary: Get credit note refund
      description: Get refund of a particular credit note.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-credit-note-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.READ
    put:
      tags:
      - credit-notes
      operationId: update_credit_note_refund
      summary: Update credit note refund
      description: Update the refunded transaction.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-credit-note-refund-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-credit-note-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.UPDATE
    delete:
      tags:
      - credit-notes
      operationId: delete_credit_note_refund
      summary: Delete credit note refund
      description: Delete a credit note refund.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-credit-note-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.creditnotes.DELETE
    parameters:
    - name: creditnote_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note.
      example: '90300000072369'
    - name: creditnote_refund_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the credit note refund.
      example: '982000000567158'
    - $ref: '#/components/parameters/organization_id'
  /invoices/{debit_note_id}:
    x-mcp-group:
    - Customer Debit Notes
    parameters:
    - $ref: '#/components/parameters/invoice_id'
    - $ref: '#/components/parameters/organization_id'
    put:
      tags:
      - customer-debit-notes
      operationId: update_customer_debit_note
      summary: Update a customer debit note
      description: Update an existing customer debit note. To delete a line item just
        remove it from the line_items list.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto debit note number generation for this debit note.
          This mandates the debit note number. Allowed values <code>true</code> and
          <code>false</code>
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-customer-debit-note-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-customer-debit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    get:
      tags:
      - customer-debit-notes
      operationId: get_customer_debit_note
      summary: Get a customer debit note
      description: Get the details of a customer debit note.
      parameters:
      - name: print
        in: query
        description: Print the exported pdf.
        required: false
        schema:
          type: boolean
      - name: accept
        in: query
        description: Get the details of a particular debit note in formats such as
          json/ pdf/ html. Default format is json. Allowed values <code>json</code>
          <code>pdf</code> and <code>html</code>
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-customer-debit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    delete:
      tags:
      - customer-debit-notes
      operationId: delete_customer_debit_note
      summary: Delete a customer debit note
      description: Delete an existing customer debit note. Debit notes which have
        payment or credits note applied cannot be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-customer-debit-note-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
  /customerpayments:
    x-mcp-group:
    - Customer Payments
    post:
      tags:
      - customer-payments
      operationId: create_customer_payment
      summary: Create a payment
      description: Create a new payment.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-payment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.CREATE
    get:
      tags:
      - customer-payments
      operationId: list_customer_payments
      summary: List Customer Payments
      description: List all the payments made by your customer.
      parameters:
      - name: customer_name
        in: query
        description: 'Search payments by customer name. Variants: <code>customer_name_startswith</code>
          and <code>customer_name_contains</code>. Max-len [100]'
        required: false
        schema:
          type: string
        example: Bowman Furniture
      - name: reference_number
        in: query
        description: 'Search payments by reference number. Variants: <code>reference_number_startswith</code>
          and <code>reference_number_contains</code>. Max-len [100]'
        required: false
        schema:
          type: string
        example: INV-384
      - name: date
        in: query
        description: Search payments by payment date.
        required: false
        schema:
          type: string
        example: '2016-06-05'
      - name: amount
        in: query
        description: 'Search payments by payment amount. Variants: <code>amount_less_than</code>,
          <code>amount_less_equals</code>, <code>amount_greater_than</code> and <code>amount_greater_equals</code>'
        required: false
        schema:
          type: number
          format: double
        example: 450
      - name: notes
        in: query
        description: 'Search payments by customer notes. Variants: <code>notes_startswith</code>
          and <code>notes_contains</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: payment_mode
        in: query
        description: 'Search payments by payment mode. Variants: <code>payment_mode_startswith</code>
          and <code>payment_mode_contains</code>'
        required: false
        schema:
          type: string
        example: cash
      - name: filter_by
        in: query
        description: 'Filter payments by mode.Allowed Values: <code>PaymentMode.All</code>,
          <code>PaymentMode.Check</code>, <code>PaymentMode.Cash</code>, <code>PaymentMode.BankTransfer</code>,
          <code>PaymentMode.Paypal</code>, <code>PaymentMode.CreditCard</code>, <code>PaymentMode.GoogleCheckout</code>,
          <code>PaymentMode.Credit</code>, <code>PaymentMode.Authorizenet</code>,
          <code>PaymentMode.BankRemittance</code>, <code>PaymentMode.Payflowpro</code>,
          <code>PaymentMode.Stripe</code>, <code>PaymentMode.TwoCheckout</code>, <code>PaymentMode.Braintree</code>
          and <code>PaymentMode.Others</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: Sort the list of payments by the specified column.
        required: false
        schema:
          type: string
        example: created_time
      - name: search_text
        in: query
        description: Search payments by reference number or customer name or payment
          description. Max-length [100]
        required: false
        schema:
          type: string
        example: ''
      - name: customer_id
        in: query
        description: 'Customer ID of the customer involved in the payment. Note: This
          value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '903000000000099'
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-customer-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.READ
    delete:
      tags:
      - customer-payments
      operationId: bulk_delete_customer_payments
      summary: Bulk delete Customer payments
      description: Delete multiple customer payments.
      parameters:
      - name: payment_ids
        in: query
        description: 'Comma-separated list of payment IDs to be deleted. Note: This
          value is a set of numeric characters separated by comma.'
        required: true
        schema:
          type: string
        example: 9030000079467,9030000079468,9030000079469
      - name: bulk_delete
        in: query
        description: Flag to indicate bulk delete operation
        required: true
        schema:
          type: boolean
        example: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-delete-customer-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.DELETE
    put:
      tags:
      - customer-payments
      operationId: update_customer_payment_using_custom_field
      summary: Update a payment using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a payment by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding payment will be retrieved and updated. Additionally, there
        is an optional X-Upsert header. If the X-Upsert header is true and the custom
        field's unique value is not found in any of the existing payments, a new payment
        will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-payment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /customerpayments/{payment_id}:
    x-mcp-group:
    - Customer Payments
    put:
      tags:
      - customer-payments
      operationId: update_customer_payment
      summary: Update a payment
      description: Update an existing payment information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-payment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.UPDATE
    get:
      tags:
      - customer-payments
      operationId: get_customer_payment
      summary: Retrieve a payment
      description: Details of an existing payment.
      parameters:
      - name: accept
        in: query
        description: Format of the response. Allowed values are <code>json</code>
          and <code>pdf</code>. Default is <code>json</code>.
        required: false
        schema:
          type: string
          enum:
          - json
          - pdf
          default: json
        example: json
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/retrieve-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.READ
    delete:
      tags:
      - customer-payments
      operationId: delete_customer_payment
      summary: Delete a payment
      description: Delete an existing payment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.DELETE
    parameters:
    - name: payment_id
      in: path
      required: true
      description: 'Unique identifier of the payment. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: '9030000079467'
    - $ref: '#/components/parameters/organization_id'
  /customerpayments/{customer_payment_id}/refunds:
    x-mcp-group:
    - Customer Payments
    get:
      tags:
      - customer-payments
      operationId: list_customer_payment_refunds
      summary: List refunds of a customer payment
      description: List all the refunds pertaining to an existing customer payment.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-refunds-of-a-customer-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.READ
    post:
      tags:
      - customer-payments
      operationId: create_customer_payment_refund
      summary: Refund an excess customer payment
      description: Refund the excess amount paid by the customer.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refund-an-excess-customer-payment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refund-an-excess-customer-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.CREATE
    parameters:
    - name: customer_payment_id
      in: path
      required: true
      description: 'Unique identifier of the customer payment. Note: This value is
        a set of numeric characters.'
      schema:
        type: string
      example: '9030000079467'
    - $ref: '#/components/parameters/organization_id'
  /customerpayment/{customer_payment_id}/customfields:
    x-mcp-group:
    - Customer Payments
    put:
      tags:
      - customer-payments
      operationId: update_custom_fields_in_customer_payment
      summary: Update custom field in existing customerpayments
      description: Update the value of the custom field in existing customerpayments.
      parameters:
      - name: customer_payment_id
        in: path
        required: true
        description: 'Unique identifier of the customer payment. Note: This value
          is a set of numeric characters.'
        schema:
          type: string
        example: '9030000079467'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields_update'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-customerpayment-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.UPDATE
  /customerpayments/{customer_payment_id}/refunds/{refund_id}:
    x-mcp-group:
    - Customer Payments
    get:
      tags:
      - customer-payments
      operationId: get_customer_payment_refund
      summary: Details of a refund
      description: Obtain details of a particular refund of a customer payment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/details-of-a-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.READ
    put:
      tags:
      - customer-payments
      operationId: update_customer_payment_refund
      summary: Update a refund
      description: Update the refunded transaction.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-refund-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.UPDATE
    delete:
      tags:
      - customer-payments
      operationId: delete_customer_payment_refund
      summary: Delete a Refund
      description: Delete refund pertaining to an existing customer payment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.customerpayments.DELETE
    parameters:
    - name: customer_payment_id
      in: path
      required: true
      description: 'Unique identifier of the customer payment. Note: This value is
        a set of numeric characters.'
      schema:
        type: string
      example: '9030000079467'
    - name: refund_id
      in: path
      required: true
      description: 'Unique identifier of the refund. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: '3000000003017'
    - $ref: '#/components/parameters/organization_id'
  /expenses:
    x-mcp-group:
    - Expenses
    post:
      tags:
      - expenses
      operationId: create_expense
      summary: Create an Expense
      description: Create billable or non-billable expense.
      parameters:
      - name: receipt
        in: query
        description: 'Expense receipt file to attach. Allowed Extensions: <code>gif</code>,
          <code>png</code>, <code>jpeg</code>, <code>jpg</code>, <code>bmp</code>,
          <code>pdf</code>, <code>xls</code>, <code>xlsx</code>, <code>doc</code>
          and <code>docx</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    get:
      tags:
      - expenses
      operationId: list_expenses
      summary: List Expenses
      description: List all the Expenses with pagination.
      parameters:
      - name: description
        in: query
        description: Search expenses by description.Variants <code>description_startswith</code>
          and <code>description_contains</code>. Max-length [100]
        required: false
        schema:
          type: string
        example: Marketing
      - name: reference_number
        in: query
        description: Search expenses by reference number. Variants <code>reference_number_startswith</code>
          and <code>reference_number_contains</code>. Max-length [100]
        required: false
        schema:
          type: string
        example: ''
      - name: date
        in: query
        description: Search expenses by expense date. Variants <code>date_start</code>,
          <code>date_end</code>, <code>date_before</code> and <code>date_after</code>.
          Format [yyyy-mm-dd]
        required: false
        schema:
          type: string
        example: 2013-11-18 00:00:00+00:00
      - name: status
        in: query
        description: Search expenses by expense status. Allowed Values <code>unbilled</code>,
          <code>invoiced</code>, <code>reimbursed</code>, <code>non-billable</code>
          and <code>billable</code>
        required: false
        schema:
          type: string
        example: unbilled
      - name: amount
        in: query
        description: 'Search expenses by amount. Variants: <code>amount_less_than</code>,
          <code>amount_less_equals</code>, <code>amount_greater_than</code> and <code>amount_greater_than</code>'
        required: false
        schema:
          type: number
          format: double
        example: 112.5
      - name: account_name
        in: query
        description: Search expenses by expense account name. Variants <code>account_name_startswith</code>
          and <code>account_name_contains</code>. Max-length [100]
        required: false
        schema:
          type: string
        example: Rent
      - name: customer_name
        in: query
        description: 'Search expenses by customer name. Variants: <code>customer_name_startswith</code>
          and <code>customer_name_contains</code>. Max-length [100]'
        required: false
        schema:
          type: string
        example: Bowman & Co
      - name: vendor_name
        in: query
        description: 'Search expenses by vendor name. Variants: <code>vendor_name_startswith</code>
          and <code>vendor_name_contains</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: customer_id
        in: query
        description: 'ID of the expense account. Note: This value is a set of numeric
          characters.'
        required: false
        schema:
          type: string
        example: 982000000567001
      - name: vendor_id
        in: query
        description: 'ID of the vendor the expense is made. Note: This value is a
          set of numeric characters.'
        required: false
        schema:
          type: string
        example: ''
      - name: recurring_expense_id
        in: query
        description: 'Search expenses by recurring expense id. Note: This value is
          a set of numeric characters.'
        required: false
        schema:
          type: string
        example: 982000000000190
      - name: paid_through_account_id
        in: query
        description: 'Search expenses by paid through account id. Note: This value
          is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: 982000000567250
      - name: search_text
        in: query
        description: Search expenses by account name or description or <code>customer
          name</code>  or <code>vendor name</code>. Max-length [100]
        required: false
        schema:
          type: string
        example: Rent
      - name: sort_column
        in: query
        description: Sort expenses.Allowed Values <code>date</code>, <code>account_name</code>,
          <code>total</code>, <code>bcy_total</code>, <code>reference_number</code>,
          <code>customer_name</code> and <code>created_time</code>
        required: false
        schema:
          type: string
        example: total
      - name: filter_by
        in: query
        description: Filter expenses by expense status. Allowed Values <code>Status.All</code>,
          <code>Status.Billable</code>, <code>Status.Nonbillable</code>, <code>Status.Reimbursed</code>,
          <code>Status.Invoiced</code>and <code>Status.Unbilled</code>
        required: false
        schema:
          type: string
        example: Status.Billable
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-expenses-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    put:
      tags:
      - expenses
      operationId: update_expense_using_custom_field
      summary: Update an expense using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a expense by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding bill expense be retrieved and updated. Additionally, there
        is an optional X-Upsert header. If the X-Upsert header is true and the custom
        field's unique value is not found in any of the existing expenses, a new expense
        will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /expenses/{expense_id}:
    x-mcp-group:
    - Expenses
    put:
      tags:
      - expenses
      operationId: update_expense
      summary: Update an Expense
      description: Update an existing Expense.
      parameters:
      - name: receipt
        in: query
        description: 'Expense receipt file to attach. Allowed Extensions: <code>gif</code>,
          <code>png</code>, <code>jpeg</code>, <code>jpg</code>, <code>bmp</code>,
          <code>pdf</code>, <code>xls</code>, <code>xlsx</code>, <code>doc</code>
          and <code>docx</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: delete_receipt
        in: query
        description: Delete the existing receipt. If this parameter is set to true,
          the existing receipt will be deleted and the receipt provided in the request
          will be attached to the expense. If this parameter is not sent in the request,
          then the existing receipt will be retained and the new receipt provided
          in the request will be ignored.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.UPDATE
    get:
      tags:
      - expenses
      operationId: get_expense
      summary: Get an Expense
      description: Get the details of the Expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    delete:
      tags:
      - expenses
      operationId: delete_expense
      summary: Delete an Expense
      description: Delete an existing expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.DELETE
    parameters:
    - name: expense_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000030049
    - $ref: '#/components/parameters/organization_id'
  /expenses/{expense_id}/comments:
    x-mcp-group:
    - Expenses
    get:
      tags:
      - expenses
      operationId: list_expense_comments
      summary: List expense History & Comments
      description: Get history and comments of expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-expense-history-and-comments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    parameters:
    - name: expense_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000030049
    - $ref: '#/components/parameters/organization_id'
  /employees:
    x-mcp-group:
    - Expenses
    get:
      tags:
      - expenses
      operationId: list_employees
      summary: List employees
      description: List employees with pagination.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-employees-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    post:
      tags:
      - expenses
      operationId: create_employee
      summary: Create an employee
      description: Create an employee for an expense.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-employee-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-employee-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /employees/{employee_id}:
    x-mcp-group:
    - Expenses
    get:
      tags:
      - expenses
      operationId: get_employee
      summary: Get an employee
      description: Get the details of the employee.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-employee-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    parameters:
    - name: employee_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: '982000000030040'
    - $ref: '#/components/parameters/organization_id'
  /employee/{employee_id}:
    x-mcp-group:
    - Expenses
    delete:
      tags:
      - expenses
      operationId: delete_employee
      summary: Delete an employee
      description: Delete an existing employee.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-employee-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.DELETE
    parameters:
    - name: employee_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: '982000000030040'
    - $ref: '#/components/parameters/organization_id'
  /expenses/{expense_id}/receipt:
    x-mcp-group:
    - Expenses
    get:
      tags:
      - expenses
      operationId: get_expense_receipt
      summary: Get an expense receipt
      description: Returns the receipt attached to the expense.
      parameters:
      - name: preview
        in: query
        description: Get the thumbnail of the receipt.
        required: false
        schema:
          type: boolean
        example: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-expense-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    post:
      tags:
      - expenses
      operationId: create_expense_receipt
      summary: Add receipt to an expense.
      description: Attach a receipt to an expense.
      parameters:
      - name: receipt
        in: query
        description: 'Expense receipt file to attach. Allowed Extensions: <code>gif</code>,
          <code>png</code>, <code>jpeg</code>, <code>jpg</code>, <code>bmp</code>,
          <code>pdf</code>, <code>xls</code>, <code>xlsx</code>, <code>doc</code>
          and <code>docx</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-receipt-to-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    delete:
      tags:
      - expenses
      operationId: delete_expense_receipt
      summary: Delete a receipt
      description: Delete the receipt attached to the expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-receipt-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.DELETE
    parameters:
    - name: expense_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000030049
    - $ref: '#/components/parameters/organization_id'
  /expenses/{expense_id}/attachment:
    x-mcp-group:
    - Expenses
    post:
      tags:
      - expenses
      operationId: add_expense_attachment
      summary: Add attachment to an expense
      description: Attach one or multiple files to an expense. This endpoint allows
        you to attach various types of documents to support your expense records.
        Returns document details for the uploaded attachments.
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/add-attachment-to-an-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-an-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    parameters:
    - name: expense_id
      in: path
      required: true
      description: 'Unique identifier of the expense. Note: This value is a set of
        numeric characters.'
      schema:
        type: string
      example: 982000000030049
    - $ref: '#/components/parameters/organization_id'
  /recurringexpenses:
    x-mcp-group:
    - Recurring Expenses
    post:
      tags:
      - recurring-expenses
      operationId: create_recurring_expense
      summary: Create a recurring expense
      description: Create a recurring expense.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-recurring-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    get:
      tags:
      - recurring-expenses
      operationId: list_recurring_expenses
      summary: List recurring expenses
      description: List all the Expenses with pagination.
      parameters:
      - name: recurrence_name
        in: query
        description: 'Search recurring expenses by recurring expense name. Variants:
          <code>recurrence_name_startswith</code> and <code>recurrence_name_contains</code>.
          Max-length [100]'
        required: false
        schema:
          type: string
        example: Monthly Rental
      - name: last_created_date
        in: query
        description: 'Search recurring expenses by date on when last expense was generated.
          Variants: <code>last_created_date_start</code>, <code>last_created_date_end</code>,
          <code>last_created_date_before</code> and <code>last_created_date_after</code>
          . Format [yyyy-mm-dd]'
        required: false
        schema:
          type: string
        example: 2013-11-18 00:00:00+00:00
      - name: next_expense_date
        in: query
        description: 'Search recurring expenses by date on which next expense will
          be generated. Variants: <code>next_expense_date_start</code>, <code>next_expense_date_end</code>,
          <code>next_expense_date_before</code> and <code>next_expense_date_after</code>
          . Format [yyyy-mm-dd]'
        required: false
        schema:
          type: string
        example: 2013-12-18 00:00:00+00:00
      - name: status
        in: query
        description: Search expenses by expense status. Allowed Values <code>active</code>,
          <code>stopped</code> and <code>expired</code>
        required: false
        schema:
          type: string
        example: active
      - name: account_id
        in: query
        description: Unique identifier of the expense account for the line item.
        required: false
        schema:
          type: string
        example: 982000000561057
      - name: account_name
        in: query
        description: Search expenses by expense account name. Variants <code>account_name_startswith</code>
          and <code>account_name_contains</code> . Max-length [100]
        required: false
        schema:
          type: string
        example: Rent
      - name: amount
        in: query
        description: 'Search expenses by amount. Variants: <code>amount_less_than</code>,
          <code>amount_less_equals</code>, <code>amount_greater_than</code> and <code>amount_greater_than</code>'
        required: false
        schema:
          type: number
          format: double
        example: 112.5
      - name: customer_name
        in: query
        description: 'Search expenses by customer name. Variants: <code>customer_name_startswith</code>
          and <code>customer_name_contains</code> . Max-length [100]'
        required: false
        schema:
          type: string
        example: Bowman & Co
      - name: customer_id
        in: query
        description: Search expenses by customer id.
        required: false
        schema:
          type: string
        example: 982000000567001
      - name: paid_through_account_id
        in: query
        description: Search expenses by paid through account id.
        required: false
        schema:
          type: string
        example: 982000000567250
      - name: filter_by
        in: query
        description: Filter expenses by expense status. Allowed Values <code>Status.All</code>,
          <code>Status.Active</code>, <code>Status.Expired</code> and <code>Status.Stopped</code>
        required: false
        schema:
          type: string
        example: Status.Billable
      - name: search_text
        in: query
        description: Search expenses by account name or description or <code>customer
          name</code>  or <code>vendor name</code>. Max-length [100] .
        required: false
        schema:
          type: string
        example: Rent
      - name: sort_column
        in: query
        description: Sort expenses.Allowed Values <code>next_expense_date</code>,
          <code>account_name</code>, <code>total</code>, <code>last_created_date</code>,
          <code>recurrence_name</code>, <code>customer_name</code> and <code>created_time</code>
        required: false
        schema:
          type: string
        example: account_name
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-recurring-expenses-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    put:
      tags:
      - recurring-expenses
      operationId: update_recurring_expense_using_custom_field
      summary: Update an recurring expense using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a recurring expense by providing its API name in the X-Unique-Identifier-Key
        header and its value in the X-Unique-Identifier-Value header. Based on this
        value, the corresponding recurring expense will be retrieved and updated.
        Additionally, there is an optional X-Upsert header. If the X-Upsert header
        is true and the custom field's unique value is not found in any of the existing
        recurring expenses, a new recurring expense will be created if the necessary
        payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-recurring-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /recurringexpenses/{recurring_expense_id}:
    x-mcp-group:
    - Recurring Expenses
    put:
      tags:
      - recurring-expenses
      operationId: update_recurring_expense
      summary: Update a recurring expense
      description: Update a recurring expense.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-recurring-expense-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.UPDATE
    get:
      tags:
      - recurring-expenses
      operationId: get_recurring_expense
      summary: Get a recurring expense
      description: Get the details of the recurring expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    delete:
      tags:
      - recurring-expenses
      operationId: delete_recurring_expense
      summary: Delete a recurring expense
      description: Deleting an existing recurring expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.DELETE
    parameters:
    - name: recurring_expense_id
      in: path
      required: true
      description: Unique identifier of the recurring expense.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringexpenses/{recurring_expense_id}/status/stop:
    x-mcp-group:
    - Recurring Expenses
    post:
      tags:
      - recurring-expenses
      operationId: stop_recurring_expense
      summary: Stop a recurring expense
      description: Stop an active recurring expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stop-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    parameters:
    - name: recurring_expense_id
      in: path
      required: true
      description: Unique identifier of the recurring expense.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringexpenses/{recurring_expense_id}/status/resume:
    x-mcp-group:
    - Recurring Expenses
    post:
      tags:
      - recurring-expenses
      operationId: resume_recurring_expense
      summary: Resume a recurring Expense
      description: Resume a stopped recurring expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/resume-a-recurring-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.CREATE
    parameters:
    - name: recurring_expense_id
      in: path
      required: true
      description: Unique identifier of the recurring expense.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringexpenses/{recurring_expense_id}/expenses:
    x-mcp-group:
    - Recurring Expenses
    get:
      tags:
      - recurring-expenses
      operationId: list_child_expenses_of_recurring_expense
      summary: List child expenses created
      description: List child expenses created from recurring expense.
      parameters:
      - name: sort_column
        in: query
        description: Sort expenses.Allowed Values <code>next_expense_date</code>,
          <code>account_name</code>, <code>total</code>, <code>last_created_date</code>,
          <code>recurrence_name</code>, <code>customer_name</code> and <code>created_time</code>
        required: false
        schema:
          type: string
        example: account_name
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-child-expenses-created-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    parameters:
    - name: recurring_expense_id
      in: path
      required: true
      description: Unique identifier of the recurring expense.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringexpenses/{recurring_expense_id}/comments:
    x-mcp-group:
    - Recurring Expenses
    get:
      tags:
      - recurring-expenses
      operationId: list_recurring_expense_history
      summary: List recurring expense history
      description: Get history and comments of a recurring expense.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-recurring-expense-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.expenses.READ
    parameters:
    - name: recurring_expense_id
      in: path
      required: true
      description: Unique identifier of the recurring expense.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: create_retainer_invoice
      summary: Create a retainerinvoice
      description: Create a retainer invoice for your customer.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto invoice number generation for this invoice. This
          mandates the invoice number. Allowed values <code>true</code> and <code>false</code>
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-retainerinvoice-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-retainerinvoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    get:
      tags:
      - retainer-invoices
      operationId: list_retainer_invoices
      summary: List a retainer invoices
      description: List all retainer invoices with pagination.
      parameters:
      - name: print
        in: query
        description: Print the exported pdf.
        required: false
        schema:
          type: boolean
      - name: sort_column
        in: query
        description: 'Sort retainer invoices.Allowed Values: <code>customer_name</code>,
          <code>retainer invoice_number</code>, <code>date</code>, <code> due_date</code>,
          <code>total</code>, <code>balance</code> and  <code>created_time</code>'
        required: false
        schema:
          type: string
        example: created_time
      - name: filter_by
        in: query
        description: 'Filter invoices by any status or payment expected date.Allowed
          Values: <code> Status.All</code>, <code>Status.Sent</code>, <code> Status.Draft</code>,
          <code>Status.OverDue</code>, <code>Status.Paid</code>, <code>Status.Void</code>,
          <code>Status.Unpaid</code>, <code>Status.PartiallyPaid</code>,<code> Status.Viewed</code>
          and <code>Date.PaymentExpectedDate</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_order
        in: query
        description: The order for sorting
        required: false
        schema:
          type: string
        example: D
      - name: page
        in: query
        description: Number of pages
        required: false
        schema:
          type: integer
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-a-retainer-invoices-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}:
    x-mcp-group:
    - Retainer Invoices
    put:
      tags:
      - retainer-invoices
      operationId: update_retainer_invoice
      summary: update a retainerinvoice
      description: Update an existing invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-retainerinvoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-retainerinvoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    get:
      tags:
      - retainer-invoices
      operationId: get_retainer_invoice
      summary: Get a retainer invoice
      description: Get the details of a retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-retainer-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    delete:
      tags:
      - retainer-invoices
      operationId: delete_retainer_invoice
      summary: Delete a retainer invoice
      description: Delete an existing retainer invoice. Invoices which have payment
        or credits note applied cannot be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-retainer-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/status/sent:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: mark_retainer_invoice_sent
      summary: Mark a retainer invoice as sent
      description: Mark a draft retainer invoice as sent.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-a-retainer-invoice-as-sent-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/templates/{template_id}:
    x-mcp-group:
    - Retainer Invoices
    put:
      tags:
      - retainer-invoices
      operationId: update_retainer_invoice_template
      summary: Update retainer invoice template
      description: Update the pdf template associated with the retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-retainer-invoice-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - name: template_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice template.
      example: 982000000000143
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/status/void:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: mark_retainer_invoice_void
      summary: Void a retainer invoice
      description: Mark an invoice status as void. Upon voiding, the payments and
        credits associated with the retainer invoices will be unassociated and will
        be under customer credits.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/void-a-retainer-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{reatinerinvoice_id}/status/draft:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: mark_retainer_invoice_draft
      summary: Mark as draft
      description: 'Mark a voided retainer invoice as draft. '
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-draft-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: reatinerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: '982000000567114'
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{reatinerinvoice_id}/submit:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: submit_retainer_invoice
      summary: Submit a retainer invoice for approval
      description: Submit a retainer invoice for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-a-retainer-invoice-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: reatinerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: '982000000567114'
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{reatinerinvoice_id}/approve:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: approve_retainer_invoice
      summary: Approve a retainer invoice.
      description: Approve a retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-a-retainer-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: reatinerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: '982000000567114'
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/email:
    x-mcp-group:
    - Retainer Invoices
    post:
      tags:
      - retainer-invoices
      operationId: email_retainer_invoice
      summary: Email a retainer invoice
      description: Email a retainer invoice to the customer. Input json string is
        not mandatory. If input json string is empty, mail will be send with default
        mail content.
      parameters:
      - name: send_customer_statement
        in: query
        description: Send customer statement pdf a with email.
        required: false
        schema:
          type: boolean
        example: ' '
      - name: send_attachment
        in: query
        description: Send the retainer invoice attachment a with the email.
        required: false
        schema:
          type: boolean
        example: ' '
      - name: attachments
        in: query
        description: Files to be attached to the email
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-a-retainer-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-a-retainer-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    get:
      tags:
      - retainer-invoices
      operationId: get_retainer_invoice_email
      summary: Get retainer invoice email content
      description: Get the email content of a retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-retainer-invoice-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/address/billing:
    x-mcp-group:
    - Retainer Invoices
    put:
      tags:
      - retainer-invoices
      operationId: update_retainer_invoice_billing_address
      summary: Update billing address
      description: Updates the billing address for this retainer invoice alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/templates:
    x-mcp-group:
    - Retainer Invoices
    get:
      tags:
      - retainer-invoices
      operationId: list_retainer_invoice_templates
      summary: List retainer invoice templates
      description: Get all retainer invoice pdf templates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-retainer-invoice-templates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/attachment:
    x-mcp-group:
    - Retainer Invoices
    get:
      tags:
      - retainer-invoices
      operationId: get_retainer_invoice_attachment
      summary: Get a retainer invoice attachment
      description: Returns the file attached to the retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-retainer-invoice-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    post:
      tags:
      - retainer-invoices
      operationId: add_retainer_invoice_attachment
      summary: Add attachment to a retainer invoice
      description: Attach a file to an invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-attachment-to-a-retainer-invoice-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-a-retainer-invoice-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/documents/{document_id}:
    x-mcp-group:
    - Retainer Invoices
    delete:
      tags:
      - retainer-invoices
      operationId: delete_retainer_invoice_attachment
      summary: Delete an attachment
      description: Delete the file attached to the retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - name: document_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice document.
      example: 982000000567115
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/comments:
    x-mcp-group:
    - Retainer Invoices
    get:
      tags:
      - retainer-invoices
      operationId: list_retainer_invoice
      summary: List retainer invoice comments & history
      description: Get the complete history and comments of a retainer invoice.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-retainer-invoice-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.READ
    post:
      tags:
      - retainer-invoices
      operationId: add_retainer_invoice_comment
      summary: Add comment
      description: Add a comment for a retainer invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.CREATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /retainerinvoices/{retainerinvoice_id}/comments/{comment_id}:
    x-mcp-group:
    - Retainer Invoices
    delete:
      tags:
      - retainer-invoices
      operationId: delete_retainer_invoice_comment
      summary: Delete a comment
      description: Delete a retainer invoice comment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.DELETE
    put:
      tags:
      - retainer-invoices
      operationId: update_retainer_invoice_comment
      summary: Update comment
      description: Update an existing comment of a retainer invoice.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.invoices.UPDATE
    parameters:
    - name: retainerinvoice_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the retainer invoice.
      example: 982000000567114
    - name: comment_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the comment.
      example: 982000000567019
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: create_purchase_order
      summary: Create a purchase order
      description: Create a purchase order for your vendor.
      parameters:
      - name: attachment
        in: query
        description: 'Allowed Extensions: <code>gif</code>, <code>png</code>, <code>jpeg</code>,
          <code>jpg</code>, <code>bmp</code>, <code>pdf</code>, <code>xls</code>,
          <code>xlsx</code>, <code>doc</code> and <code>docx</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto purchase order number generation for this purchase
          order. This mandates the purchase order number.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-purchase-order-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    get:
      tags:
      - purchase-order
      operationId: list_purchase_orders
      summary: List purchase orders
      description: List all purchase orders.
      parameters:
      - name: purchaseorder_number
        in: query
        description: 'Search purchase order by purchase order number. Variants: <code>purchaseorder_number_startswith</code>
          and <code>purchaseorder_number_contains</code>. Example: Locate specific
          PO by number or partial match.'
        required: false
        schema:
          type: string
        example: PO-00001
      - name: reference_number
        in: query
        description: 'Search purchase order by reference number. Variants: <code>reference_number_startswith</code>
          and <code>reference_number_contains</code>. Example: Monitor external vendor
          order numbers.'
        required: false
        schema:
          type: string
        example: ER/0034
      - name: date
        in: query
        description: 'Search purchase order by creation date. Use YYYY-MM-DD format
          for exact date matching. Example: Retrieve POs created on specific date.'
        required: false
        schema:
          type: string
        example: '2014-02-10'
      - name: date_start
        in: query
        description: Filter purchase orders with creation date on or after the specified
          date. Useful for defining the start of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-01'
      - name: date_end
        in: query
        description: Filter purchase orders with creation date on or before the specified
          date. Useful for defining the end of a date range. Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-30'
      - name: date_before
        in: query
        description: Filter purchase orders with creation date strictly before the
          specified date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: date_after
        in: query
        description: Filter purchase orders with creation date strictly after the
          specified date (exclusive). Use yyyy-mm-dd format.
        required: false
        schema:
          type: string
          format: date
        example: '2013-11-17'
      - name: status
        in: query
        description: 'Search purchase order by status. Allowed values: <code>draft</code>,
          <code>open</code>, <code>billed</code>, <code>cancelled</code>. Example:
          Filter POs by current workflow state.'
        required: false
        schema:
          type: string
        example: draft
      - name: item_description
        in: query
        description: 'Search purchase order by item description. Variants: <code>item_description_startswith</code>
          and <code>item_description_contains</code>. Example: Discover POs containing
          specific items.'
        required: false
        schema:
          type: string
        example: ''
      - name: vendor_name
        in: query
        description: 'Search purchase order by vendor name. Variants: <code>vendor_name_startswith</code>
          and <code>vendor_name_contains</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: total
        in: query
        description: 'Search purchase order by total amount. Variants: <code>total_start</code>,
          <code>total_end</code>, <code>total_less_than</code>, <code>total_less_equals</code>,
          <code>total_greater_than</code>, <code>total_greater_equals</code>. Example:
          Identify POs within price range.'
        required: false
        schema:
          type: number
          format: double
        example: 40
      - name: vendor_id
        in: query
        description: 'Search purchase order by vendor ID. Use the unique identifier
          of the vendor to filter purchase orders. Example: Group POs by specific
          vendor. Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000026049'
      - name: last_modified_time
        in: query
        description: 'Search purchase order by last modified time. Use ISO 8601 format
          (YYYY-MM-DDTHH:MM:SS+/-HH:MM). Example: Identify recently updated POs.'
        required: false
        schema:
          type: string
        example: 2014-02-10T15:26:26+0530
      - name: item_id
        in: query
        description: 'Search purchase order by item ID. Use the unique identifier
          of the specific item. Example: Locate all POs containing particular item.
          Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000027009'
      - name: filter_by
        in: query
        description: 'Filter purchase order by status. Allowed values: <code>Status.All</code>,
          <code>Status.Draft</code>, <code>Status.Open</code>, <code>Status.Billed</code>,
          <code>Status.Cancelled</code>. Example: Bulk status filtering for reporting.'
        required: false
        schema:
          type: string
        example: Status.All
      - name: search_text
        in: query
        description: 'Search purchase order by number, reference, or vendor name.
          General search across multiple fields. Example: Quick PO lookup without
          specific field.'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort purchase orders by column. Allowed values: <code>vendor_name</code>,
          <code>purchaseorder_number</code>, <code>date</code>, <code>delivery_date</code>,
          <code>total</code>, <code>created_time</code>. Example: Organize results
          for analysis.'
        required: false
        schema:
          type: string
        example: created_time
      - name: custom_field
        in: query
        description: 'Search purchase order by purchase order''s custom field. Variants:
          <code>custom_field_startswith</code>, <code>custom_field_contains</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-purchase-orders-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.READ
    put:
      tags:
      - purchase-order
      operationId: update_purchase_order_using_custom_field
      summary: Update a purchase order using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a purchase order by providing its API name in the X-Unique-Identifier-Key
        header and its value in the X-Unique-Identifier-Value header. Based on this
        value, the corresponding purchase order will be retrieved and updated. Additionally,
        there is an optional X-Upsert header. If the X-Upsert header is true and the
        custom field's unique value is not found in any of the existing purchase orders,
        a new purchase order will be created if the necessary payload details are
        available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-purchase-order-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}:
    x-mcp-group:
    - Purchase Order
    put:
      tags:
      - purchase-order
      operationId: update_purchase_order
      summary: Update a purchase order
      description: Update an existing purchase order.
      parameters:
      - name: attachment
        in: query
        description: 'Allowed Extensions: <code>gif</code>, <code>png</code>, <code>jpeg</code>,
          <code>jpg</code>, <code>bmp</code>, <code>pdf</code>, <code>xls</code>,
          <code>xlsx</code>, <code>doc</code> and <code>docx</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto purchase order number generation for this purchase
          order. This mandates the purchase order number.
        required: false
        schema:
          type: boolean
        example: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-purchase-order-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    get:
      tags:
      - purchase-order
      operationId: get_purchase_order
      summary: Get a purchase order
      description: Get the details of a purchase order.
      parameters:
      - name: print
        in: query
        description: Print the exported pdf.
        required: false
        schema:
          type: boolean
        example: false
      - name: accept
        in: query
        description: 'Get the details of a particular purchase order in formats such
          as json/ pdf/ html. Default format is json. Allowed Values: <code>json</code>,
          <code>pdf</code> and <code>html</code>.'
        required: false
        schema:
          type: string
        example: json
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.READ
    delete:
      tags:
      - purchase-order
      operationId: delete_purchase_order
      summary: Delete purchase order
      description: Delete an existing purchase order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.DELETE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorder/{purchaseorder_id}/customfields:
    x-mcp-group:
    - Purchase Order
    put:
      tags:
      - purchase-order
      operationId: update_custom_fields_in_purchase_order
      summary: Update custom field in existing purchaseorders
      description: Update the value of the custom field in existing purchaseorders.
      parameters:
      - name: purchaseorder_id
        in: path
        required: true
        description: 'Unique identifier of the purchase order. Note: This value is
          a set of numeric characters.'
        schema:
          type: string
        example: '460000000062001'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-purchaseorder-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
  /purchaseorders/{purchaseorder_id}/status/open:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: mark_purchase_order_open
      summary: Mark a purchase order as open
      description: Mark a draft purchase order as open.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-a-purchase-order-as-open-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/status/billed:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: mark_purchase_order_billed
      summary: Mark as billed
      description: Mark a purchase order as billed.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-billed-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/status/cancelled:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: mark_purchase_order_cancelled
      summary: Cancel a purchase order
      description: Mark a purchase order as cancelled.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/cancel-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/submit:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: submit_purchase_order
      summary: Submit a purchase order for approval
      description: Submit a purchase order for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-a-purchase-order-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/approve:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: approve_purchase_order
      summary: Approve a purchase order
      description: Approve a purchase order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/email:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: email_purchase_order
      summary: Email a purchase order
      description: Email a purchase order to the vendor. Input json string is not
        mandatory. If input json string is empty, mail will be send with default mail
        content.
      parameters:
      - name: attachments
        in: query
        description: The files to be attached with the email.
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: send_attachment
        in: query
        description: Send the purchase order attachment a with the email.
        required: false
        schema:
          type: boolean
        example: false
      - name: file_name
        in: query
        description: Name of the file
        required: false
        schema:
          type: string
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-a-purchase-order-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    get:
      tags:
      - purchase-order
      operationId: get_purchase_order_email
      summary: Get purchase order email content
      description: Get the email content of a purchase order.
      parameters:
      - name: email_template_id
        in: query
        description: 'Get the email content based on a specific email template. If
          this param is not inputted, then the content will be based on the email
          template associated with the customer. If no template is associated with
          the customer, then default template will be used. Note: This value is a
          set of numeric characters.'
        required: false
        schema:
          type: string
        example: '460000000011023'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-purchase-order-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.READ
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/address/billing:
    x-mcp-group:
    - Purchase Order
    put:
      tags:
      - purchase-order
      operationId: update_purchase_order_billing_address
      summary: Update billing address
      description: Updates the billing address for this purchase order alone.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/templates:
    x-mcp-group:
    - Purchase Order
    get:
      tags:
      - purchase-order
      operationId: list_purchase_order_templates
      summary: List purchase order templates
      description: Get all purchase order pdf templates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-purchase-order-templates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/templates/{template_id}:
    x-mcp-group:
    - Purchase Order
    put:
      tags:
      - purchase-order
      operationId: update_purchase_order_template
      summary: Update purchase order template
      description: Update the pdf template associated with the purchase order.
      parameters:
      - name: template_id
        in: path
        required: true
        description: 'Unique identifier of the purchase order template. Note: This
          value is a set of numeric characters.'
        schema:
          type: string
        example: '460000000011003'
      - name: templates
        in: query
        description: List of available templates for the purchase order.
        required: false
        schema:
          type: array
          items:
            type: object
            properties:
              template_name:
                $ref: '#/components/schemas/template_name'
              template_id:
                $ref: '#/components/schemas/template_id'
              template_type:
                $ref: '#/components/schemas/template_type'
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-purchase-order-template-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/attachment:
    x-mcp-group:
    - Purchase Order
    get:
      tags:
      - purchase-order
      operationId: get_purchase_order_attachment
      summary: Get a purchase order attachment
      description: Returns the file attached to the purchase order.
      parameters:
      - name: preview
        in: query
        description: Get the thumbnail of the attachment.
        required: false
        schema:
          type: boolean
        example: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-purchase-order-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.READ
    post:
      tags:
      - purchase-order
      operationId: add_purchase_order_attachment
      summary: Add attachment to a purchase order
      description: Attach a file to a purchase order.
      parameters:
      - name: attachment
        in: query
        description: 'Allowed Extensions: <code>gif</code>, <code>png</code>, <code>jpeg</code>,
          <code>jpg</code>, <code>bmp</code>, <code>pdf</code>, <code>xls</code>,
          <code>xlsx</code>, <code>doc</code> and <code>docx</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-a-purchase-order-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    put:
      tags:
      - purchase-order
      operationId: update_purchase_order_attachment
      summary: Update attachment preference
      description: Set whether you want to send the attached file while emailing the
        purchase order.
      parameters:
      - name: can_send_in_mail
        in: query
        description: Boolean to send the attachment with the purchase order when emailed.
        required: true
        schema:
          type: boolean
        example: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-attachment-preference-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    delete:
      tags:
      - purchase-order
      operationId: delete_purchase_order_attachment
      summary: Delete an attachment
      description: Delete the file attached to the purchase order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.DELETE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/comments:
    x-mcp-group:
    - Purchase Order
    get:
      tags:
      - purchase-order
      operationId: list_purchase_order_comments
      summary: List purchase order comments & history
      description: Get the complete history and comments of purchase order.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-purchase-order-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.READ
    post:
      tags:
      - purchase-order
      operationId: add_purchase_order_comment
      summary: Add comment
      description: Add a comment for a purchase order.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.CREATE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/comments/{comment_id}:
    x-mcp-group:
    - Purchase Order
    put:
      tags:
      - purchase-order
      operationId: update_purchase_order_comment
      summary: Update comment
      description: Update an existing comment of a purchase order.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: '460000000012345'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    delete:
      tags:
      - purchase-order
      operationId: delete_purchase_order_comment
      summary: Delete a comment
      description: Delete a purchase order comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: 'Unique identifier of the comment. Note: This value is a set
          of numeric characters.'
        schema:
          type: string
        example: '460000000012345'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.DELETE
    parameters:
    - name: purchaseorder_id
      in: path
      required: true
      description: 'Unique identifier of the purchase order. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000062001'
    - $ref: '#/components/parameters/organization_id'
  /purchaseorders/{purchaseorder_id}/reject:
    x-mcp-group:
    - Purchase Order
    post:
      tags:
      - purchase-order
      operationId: reject_purchase_orders
      summary: Reject Purchase Order
      description: Reject a purchase order.
      parameters:
      - name: purchaseorder_id
        in: path
        required: true
        description: 'Unique identifier of the purchase order. Note: This value is
          a set of numeric characters.'
        schema:
          type: string
        example: '460000000012345'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reject_purchase_order'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.purchaseorders.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /bills:
    x-mcp-group:
    - Bills
    post:
      tags:
      - bills
      operationId: create_bill
      summary: Create a bill
      description: Create a bill received from your vendor.
      parameters:
      - name: attachment
        in: query
        description: 'File to attach. Allowed Extensions: <code>gif</code>, <code>png</code>,
          <code>jpeg</code>, <code>jpg</code>, <code>bmp</code> and <code>pdf</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-bill-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    get:
      tags:
      - bills
      operationId: list_bills
      summary: List bills
      description: List all bills with pagination.
      parameters:
      - name: bill_number
        in: query
        description: Filter bills by bill number. Accepts exact matches or use <code>bill_number_startswith</code>
          for prefix matching and <code>bill_number_contains</code> for substring
          matching. Useful for finding bills by unique identifier or partial patterns.
        required: false
        schema:
          type: string
        example: '00454'
      - name: reference_number
        in: query
        description: Filter bills by reference number. Accepts exact matches or use
          <code>reference_number_startswith</code> for prefix matching and <code>reference_number_contains</code>
          for substring matching. Useful for finding bills by external references
          or vendor invoice numbers.
        required: false
        schema:
          type: string
        example: '4321133'
      - name: date
        in: query
        description: Filter bills by bill date in YYYY-MM-DD format. Use <code>date_start</code>/<code>date_end</code>
          for ranges, <code>date_before</code>/<code>date_after</code> for relative
          dates. Useful for billing periods and due date management.
        required: false
        schema:
          type: string
        example: '2013-09-11'
      - name: status
        in: query
        description: 'Filter bills by status: <code>paid</code>, <code>open</code>,
          <code>overdue</code>, <code>void</code>, or <code>partially_paid</code>.
          Useful for payment status filtering, accounts payable management, and status-specific
          reporting.'
        required: false
        schema:
          type: string
        example: open
      - name: description
        in: query
        description: Filter bills by description text. Accepts exact matches or use
          <code>description_startswith</code> for prefix matching and <code>description_contains</code>
          for substring matching. Useful for finding bills by line item descriptions
          or vendor notes.
        required: false
        schema:
          type: string
        example: ''
      - name: vendor_name
        in: query
        description: Filter bills by vendor name. Use <code>vendor_name_startswith</code>
          for prefix matching or <code>vendor_name_contains</code> for substring matching.
          Useful for finding bills from specific suppliers.
        required: false
        schema:
          type: string
        example: ''
      - name: total
        in: query
        description: Filter bills by total amount. Use <code>total_less_than</code>,
          <code>total_less_equals</code>, <code>total_greater_than</code>, or <code>total_greater_equals</code>
          for amount-based filtering. Useful for finding bills within specific price
          ranges or above/below certain thresholds.
        required: false
        schema:
          type: number
          format: double
        example: 40
      - name: vendor_id
        in: query
        description: Filter bills by specific vendor ID. Accepts the unique identifier
          for a vendor to retrieve all bills associated with that particular supplier
          or vendor account.
        required: false
        schema:
          type: string
        example: '460000000038029'
      - name: item_id
        in: query
        description: Filter bills by specific item ID. Retrieves all bills containing
          a particular product or service item based on its unique identifier.
        required: false
        schema:
          type: string
        example: '460000000054135'
      - name: recurring_bill_id
        in: query
        description: Filter bills by recurring bill ID. Retrieves all bills generated
          from a specific recurring bill template or schedule.
        required: false
        schema:
          type: string
        example: ''
      - name: purchaseorder_id
        in: query
        description: Filter bills by purchase order ID. Retrieves all bills associated
          with a specific purchase order for tracking procurement workflows.
        required: false
        schema:
          type: string
        example: '460000000068231'
      - name: last_modified_time
        in: query
        description: Filter bills by last modification timestamp. Accepts ISO 8601
          format (YYYY-MM-DDTHH:MM:SS+/-HHMM) to find bills modified at or after a
          specific time.
        required: false
        schema:
          type: string
        example: 2013-09-11T17:18:32+0530
      - name: filter_by
        in: query
        description: Filter bills by status using predefined status constants. Options
          include <code>Status.All</code>, <code>Status.PartiallyPaid</code>, <code>Status.Paid</code>,
          <code>Status.Overdue</code>, <code>Status.Void</code>, and <code>Status.Open</code>.
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Filter bills using general search text. Searches across bill
          number, reference number, and vendor name fields to find matching bills.
          Useful for quick searches when you know part of the bill information.
        required: false
        schema:
          type: string
        example: ''
      - name: page
        in: query
        description: Specify the page number for pagination. Use this parameter to
          navigate through multiple pages of bills when the total number of bills
          exceeds the per_page limit.
        required: false
        schema:
          type: integer
        example: 1
      - name: per_page
        in: query
        description: Specify the number of bills to retrieve per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      - name: sort_column
        in: query
        description: Specify the column to sort bills by. Available options include
          <code>vendor_name</code>, <code>bill_number</code>, <code>date</code>, <code>due_date</code>,
          <code>total</code>, <code>balance</code>, and <code>created_time</code>.
        required: false
        schema:
          type: string
        example: ''
      - name: sort_order
        in: query
        description: Specify the sorting order for bills. Use <code>A</code> for ascending
          order (A-Z, 0-9, earliest to latest) or <code>D</code> for descending order
          (Z-A, 9-0, latest to earliest).
        required: false
        schema:
          type: string
        example: D
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-bills-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    put:
      tags:
      - bills
      operationId: update_bill_using_custom_field
      summary: Update a bill using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a bill by providing its API name in the X-Unique-Identifier-Key header and
        its value in the X-Unique-Identifier-Value header. Based on this value, the
        corresponding bill will be retrieved and updated. Additionally, there is an
        optional X-Upsert header. If the X-Upsert header is true and the custom field's
        unique value is not found in any of the existing bills, a new bill will be
        created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Specify the API name of the custom field that contains unique
          values. This field must be configured to not accept duplicate values for
          the upsert functionality to work properly.
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Provide the unique value from the custom field that identifies
          the specific bill to update. This value will be used to locate the existing
          bill record in the system.
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: When set to true, this header enables upsert functionality. If
          no bill is found with the specified custom field value, a new bill will
          be created using the provided payload data.
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-bill-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}:
    x-mcp-group:
    - Bills
    put:
      tags:
      - bills
      operationId: update_bill
      summary: Update a bill
      description: Update a bill. To delete a line item just remove it from the line_items
        list.
      parameters:
      - name: attachment
        in: query
        description: 'File to attach. Allowed Extensions: <code>gif</code>, <code>png</code>,
          <code>jpeg</code>, <code>jpg</code>, <code>bmp</code> and <code>pdf</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-bill-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.UPDATE
    get:
      tags:
      - bills
      operationId: get_bill
      summary: Get a bill
      description: Get the details of a bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    delete:
      tags:
      - bills
      operationId: delete_bill
      summary: Delete a bill
      description: Delete an existing bill. Bills which have payments applied cannot
        be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.DELETE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bill/{bill_id}/customfields:
    x-mcp-group:
    - Bills
    put:
      tags:
      - bills
      operationId: update_custom_fields_in_bill
      summary: Update custom field in existing bills
      description: Update the value of the custom field in existing bills.
      parameters:
      - name: bill_id
        in: path
        required: true
        description: Unique identifier of the bill.
        schema:
          type: string
        example: '460000000098765'
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-bill-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.UPDATE
  /bills/{bill_id}/status/void:
    x-mcp-group:
    - Bills
    post:
      tags:
      - bills
      operationId: mark_bill_void
      summary: Void a bill
      description: Mark a bill status as void.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/void-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/status/open:
    x-mcp-group:
    - Bills
    post:
      tags:
      - bills
      operationId: mark_bill_open
      summary: Mark a bill as open
      description: Mark a void bill as open.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-a-bill-as-open-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/submit:
    x-mcp-group:
    - Bills
    post:
      tags:
      - bills
      operationId: submit_bill
      summary: Submit a bill for approval
      description: Submit a bill for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-a-bill-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/approve:
    x-mcp-group:
    - Bills
    post:
      tags:
      - bills
      operationId: approve_bill
      summary: Approve a bill
      description: Approve a bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/address/billing:
    x-mcp-group:
    - Bills
    put:
      tags:
      - bills
      operationId: update_bill_billing_address
      summary: Update billing address
      description: Updates the billing address for this bill.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-billing-address-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-billing-address-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.UPDATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/payments:
    x-mcp-group:
    - Bills
    get:
      tags:
      - bills
      operationId: list_bill_payments
      summary: List bill payments
      description: Get the list of payments made for a bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-bill-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/credits:
    x-mcp-group:
    - Bills
    post:
      tags:
      - bills
      operationId: apply_credits_to_bill
      summary: Apply credits
      description: Apply the vendor credits from excess vendor payments to a bill.
        Multiple credits can be applied at once.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/apply-credits-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/apply-credits-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/payments/{bill_payment_id}:
    x-mcp-group:
    - Bills
    delete:
      tags:
      - bills
      operationId: delete_bill_payment
      summary: Delete a payment
      description: Delete a payment made to a bill.
      parameters:
      - name: bill_payment_id
        in: path
        required: true
        description: Unique identifier of the bill payment.
        schema:
          type: string
        example: '460000000042061'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.DELETE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/attachment:
    x-mcp-group:
    - Bills
    get:
      tags:
      - bills
      operationId: get_bill_attachment
      summary: Get a bill attachment
      description: Returns the file attached to the bill.
      parameters:
      - name: preview
        in: query
        description: Get the thumbnail of the attachment.
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-bill-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    post:
      tags:
      - bills
      operationId: add_bill_attachment
      summary: Add attachment to a bill
      description: Attach a file to a bill.
      parameters:
      - name: attachment
        in: query
        description: 'File to attach. Allowed Extensions: <code>gif</code>, <code>png</code>,
          <code>jpeg</code>, <code>jpg</code>, <code>bmp</code> and <code>pdf</code>.'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    delete:
      tags:
      - bills
      operationId: delete_bill_attachment
      summary: Delete an attachment
      description: Delete the file attached to a bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.DELETE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/comments:
    x-mcp-group:
    - Bills
    get:
      tags:
      - bills
      operationId: get_bill_comments
      summary: List bill comments & history
      description: Get the complete history and comments of a bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-bill-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    post:
      tags:
      - bills
      operationId: add_bill_comment
      summary: Add comment
      description: Add a comment for a bill.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/{bill_id}/comments/{comment_id}:
    x-mcp-group:
    - Bills
    delete:
      tags:
      - bills
      operationId: delete_bill_comment
      summary: Delete a comment
      description: Delete a bill comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: Unique identifier of the comment.
        schema:
          type: string
        example: '460000000069037'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.DELETE
    parameters:
    - name: bill_id
      in: path
      required: true
      description: Unique identifier of the bill.
      schema:
        type: string
      example: '460000000098765'
    - $ref: '#/components/parameters/organization_id'
  /bills/editpage/frompurchaseorders:
    x-mcp-group:
    - Bills
    get:
      tags:
      - bills
      operationId: convert_purchase_order_to_bill
      summary: Convert PO to Bill
      description: Create a bill for the selected purchase orders. Use this api to
        fetch the Bill payload by passing the purchaseorder_ids in the query parameters
        and then use the create bill api and pass the payload to create a bill <br>
        <a href=/books/api/v3/bills/#create-a-bill>Create bill API Endpoint</a>.
      parameters:
      - $ref: '#/components/parameters/purchaseorder_ids'
      responses:
        '200':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/convert-po-to-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    parameters:
    - $ref: '#/components/parameters/purchaseorder_ids'
    - $ref: '#/components/parameters/organization_id'
  /recurringbills:
    x-mcp-group:
    - Recurring Bills
    post:
      tags:
      - recurring-bills
      operationId: create_recurring_bill
      summary: Create a recurring bill
      description: Create a recurring bill.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-recurring-bill-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    get:
      tags:
      - recurring-bills
      operationId: list_recurring_bills
      summary: List recurring bills
      description: List all recurring bills with pagination.
      parameters:
      - name: recurring_bill_id
        in: query
        description: Unique identifier of the recurring bill.
        required: false
        schema:
          type: string
        example: 982000000567240
      - name: vendor_id
        in: query
        description: Search recurring bills by Vendor ID
        required: false
        schema:
          type: string
        example: '460000000038029'
      - name: vendor_name
        in: query
        description: 'Search recurring bills by vendor name. Variants: <code>vendor_name_startswith</code>
          and <code>vendor_name_contains</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: status
        in: query
        description: 'Search recurring bills by recurring bill status. Allowed Values:
          <code>active</code>, <code>stopped</code>, <code>expired</code>'
        required: false
        schema:
          type: string
        example: active
      - name: recurrence_name
        in: query
        description: 'Search recurring bills by recurrence number. Variants: <code>recurrence_name_startswith</code>
          and <code>recurrence_name_contains</code>'
        required: false
        schema:
          type: string
        example: Monthly Rental
      - name: currency_id
        in: query
        description: ID of the Currency
        required: false
        schema:
          type: string
        example: '460000000000099'
      - name: currency_code
        in: query
        description: Code of the Currency
        required: false
        schema:
          type: string
        example: INR
      - name: currency_symbol
        in: query
        description: Symbol of the Currency
        required: false
        schema:
          type: string
        example: ''
      - name: start_date
        in: query
        description: 'Search recurring bills by recurring bill start date. Variants:
          <code>start_date_start</code>, <code>start_date_end</code>, <code>start_date_before</code>
          and <code>start_date_after</code>'
        required: false
        schema:
          type: string
        example: '2013-11-18'
      - name: end_date
        in: query
        description: Date on which recurring bill has to expire. Can be left as empty
          to run forever. Format [yyyy-mm-dd].
        required: false
        schema:
          type: string
        example: '2013-12-18'
      - name: source_of_supply
        in: query
        description: Place from where the goods/services are supplied. (If not given,
          <code>place of contact</code> given for the contact will be taken)
        required: false
        schema:
          type: string
        example: AP
        x-node_available_in:
        - in
        x-node_unavailable_in: []
      - name: place_of_supply
        in: query
        description: 'The place of supply is where a transaction is considered to
          have occurred for VAT purposes. For the supply of goods, the place of supply
          is the location of the goods when the supply occurs. For the supply of services,
          the place of supply should be where the supplier is established. (If not
          given, <code>place of contact</code> given for the contact will be taken)<br/>Supported
          codes for UAE emirates are : </br>Abu Dhabi - <code>AB</code>,</br>Ajman
          - <code>AJ</code>,</br>Dubai - <code>DU</code>,</br>Fujairah - <code>FU</code>,</br>Ras
          al-Khaimah - <code>RA</code>,</br>Sharjah - <code>SH</code>,</br>Umm al-Quwain
          - <code>UM</code>.<br/>Supported codes for the GCC countries are : </br>United
          Arab Emirates - <code>AE</code>,</br>Saudi Arabia - <code>SA</code>,</br>Bahrain
          - <code>BH</code>,</br>Kuwait - <code>KW</code>,</br>Oman - <code>OM</code>,</br>Qatar
          - <code>QA</code>.'
        required: false
        schema:
          type: string
        example: DU
        x-node_available_in:
        - gcc
        x-node_unavailable_in: []
      - name: destination_of_supply
        in: query
        description: Place where the goods/services are supplied to. (If not given,
          organisation's home state will be taken)
        required: false
        schema:
          type: string
        example: TN
        x-node_available_in:
        - in
        x-node_unavailable_in: []
      - name: gst_treatment
        in: query
        description: Choose whether the contact is GST registered/unregistered/consumer/overseas.
          Allowed values are <code> business_gst </code> , <code> business_none </code>
          , <code> overseas </code> , <code> consumer </code>.
        required: false
        schema:
          type: string
        example: business_gst
        x-node_available_in:
        - in
        x-node_unavailable_in: []
      - name: gst_no
        in: query
        description: 15 digit GST identification number of the vendor.
        required: false
        schema:
          type: string
        example: 22AAAAA0000A1Z5
        x-node_available_in:
        - in
        x-node_unavailable_in: []
      - name: tax_treatment
        in: query
        description: 'VAT treatment for the bill. Choose whether the vendor falls
          under: <code>vat_registered</code>,<code>vat_not_registered</code>,<code>gcc_vat_not_registered</code>,<code>gcc_vat_registered</code>,<code>non_gcc</code>.</br><code>dz_vat_registered</code>
          and <code>dz_vat_not_registered</code> are supported only for <b>UAE</b>.'
        required: false
        schema:
          type: string
        example: vat_registered
        x-node_available_in:
        - gcc
        - mx
        x-node_unavailable_in: []
      - name: vat_treatment
        in: query
        description: (Optional) VAT treatment for the bill. VAT treatment denotes
          the location of the vendor, if the vendor resides in UK then the VAT treatment
          is <code>uk</code>. If the vendor is in an EU country & VAT registered,
          you are resides in Northen Ireland and purchasing Goods then his VAT treatment
          is <code>eu_vat_registered</code> and if he resides outside the UK then
          his VAT treatment is <code>overseas</code>(For Pre Brexit, this can be split
          as <code>eu_vat_registered</code>, <code>eu_vat_not_registered</code> and
          <code>non_eu</code>).
        required: false
        schema:
          type: string
        x-node_available_in:
        - uk
        x-node_unavailable_in: []
        example: ''
      - name: vat_reg_no
        in: query
        description: <b>For UK Edition:</b> VAT Registration number of a contact with
          length should be between 2 and 12 characters.<br/> <b>For Avalara:</b> If
          you are doing sales in the European Union (EU) then provide VAT Registration
          Number of your customers here. This is used to calculate VAT for B2B sales,
          from Avalara.
        required: false
        schema:
          type: string
        x-node_available_in:
        - uk
        - Avalara Integration
        x-node_unavailable_in: []
        example: ''
      - name: is_abn_quoted
        in: query
        description: Whether the ABN is quoted for the recurring bill.
        required: false
        schema:
          type: string
        x-node_available_in:
        - au
        x-node_unavailable_in: []
        example: ''
      - name: abn
        in: query
        description: Australian Business Number (ABN) of the vendor.
        required: false
        schema:
          type: string
        x-node_available_in:
        - au
        x-node_unavailable_in: []
        example: ''
      - name: is_reverse_charge_applied
        in: query
        description: Applicable for transactions where you pay reverse charge
        required: false
        schema:
          type: boolean
        example: true
        x-node_available_in:
        - in
        x-node_unavailable_in: []
      - name: pricebook_id
        in: query
        description: Enter ID of the price book.
        required: false
        schema:
          type: string
        example: 460000000038090
      - name: pricebook_name
        in: query
        description: Name of the price book
        required: false
        schema:
          type: string
        example: ''
      - name: is_inclusive_tax
        in: query
        description: Used to specify whether the line item rates are inclusive or
          exclusive of tax.
        required: false
        schema:
          type: boolean
        example: false
        x-node_available_in: []
        x-node_unavailable_in:
        - us
      - name: line_items
        in: query
        description: Line items of a recurrence bill.
        required: false
        schema:
          type: array
          items:
            type: object
            properties:
              description:
                $ref: '#/components/schemas/description'
              sku:
                $ref: '#/components/schemas/sku'
              name:
                $ref: '#/components/schemas/name'
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              image_document_id:
                $ref: '#/components/schemas/image_document_id'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              reverse_charge_tax_name:
                $ref: '#/components/schemas/reverse_charge_tax_name'
              reverse_charge_tax_percentage:
                $ref: '#/components/schemas/reverse_charge_tax_percentage'
              reverse_charge_tax_amount:
                $ref: '#/components/schemas/reverse_charge_tax_amount'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              rate:
                $ref: '#/components/schemas/rate'
              tags:
                $ref: '#/components/schemas/tags'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              item_total:
                $ref: '#/components/schemas/item_total'
              item_total_inclusive_of_tax:
                $ref: '#/components/schemas/item_total_inclusive_of_tax'
              item_order:
                $ref: '#/components/schemas/item_order'
              unit:
                $ref: '#/components/schemas/unit'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              acquisition_vat_name:
                $ref: '#/components/schemas/acquisition_vat_name'
              acquisition_vat_percentage:
                $ref: '#/components/schemas/acquisition_vat_percentage'
              acquisition_vat_amount:
                $ref: '#/components/schemas/acquisition_vat_amount'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              reverse_charge_vat_name:
                $ref: '#/components/schemas/reverse_charge_vat_name'
              reverse_charge_vat_percentage:
                $ref: '#/components/schemas/reverse_charge_vat_percentage'
              reverse_charge_vat_amount:
                $ref: '#/components/schemas/reverse_charge_vat_amount'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              project_id:
                $ref: '#/components/schemas/project_id'
              project_name:
                $ref: '#/components/schemas/project_name'
              item_custom_fields:
                $ref: '#/components/schemas/item_custom_fields'
        example: ''
      - name: is_tds_applied
        in: query
        description: Check if TDS is applied
        required: false
        schema:
          type: boolean
        x-node_available_in:
        - in
        - global
        - au
        x-node_unavailable_in: []
      - name: notes
        in: query
        description: Notes for the Bill
        required: false
        schema:
          type: string
        example: Thanks for your business.
      - name: terms
        in: query
        description: Terms and Conditions for the Bill
        required: false
        schema:
          type: string
        example: Terms and conditions apply.
      - name: payment_terms
        in: query
        description: Number Referring to Payment Terms
        required: false
        schema:
          type: integer
        example: 0
      - name: payment_terms_label
        in: query
        description: Label of the Payment Terms
        required: false
        schema:
          type: string
        example: Due on Receipt
      - name: custom_fields
        in: query
        description: List of custom fields for the recurring bill.
        required: false
        schema:
          type: array
          items:
            type: object
            properties:
              custom_field_id:
                $ref: '#/components/schemas/custom_field_id'
              index:
                $ref: '#/components/schemas/index'
              label:
                $ref: '#/components/schemas/label'
              value:
                $ref: '#/components/schemas/value'
        example: ''
      - name: acquisition_vat_summary
        in: query
        description: Summary of the VAT Acquistion
        required: false
        schema:
          type: array
          items:
            type: object
            properties:
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
        x-node_available_in:
        - uk
        - eu
        x-node_unavailable_in: []
        example: ''
      - name: reverse_charge_vat_summary
        in: query
        description: Summary of the Reverse Charge
        required: false
        schema:
          type: array
          items:
            type: object
            properties:
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
        x-node_available_in:
        - uk
        - eu
        x-node_unavailable_in: []
        example: ''
      - name: acquisition_vat_total
        in: query
        description: Total of the VAT Acquistion
        required: false
        schema:
          type: number
          format: double
        x-node_available_in:
        - uk
        - eu
        x-node_unavailable_in: []
        example: ''
      - name: reverse_charge_vat_total
        in: query
        description: Total of the Reverse Charge
        required: false
        schema:
          type: number
          format: double
        x-node_available_in:
        - uk
        - eu
        x-node_unavailable_in: []
        example: ''
      - name: created_time
        in: query
        description: Created time of the bill
        required: false
        schema:
          type: string
        example: 2013-09-11T17:18:32+0530
      - name: created_by_id
        in: query
        description: Name of User who created the Bill
        required: false
        schema:
          type: string
        example: '4600000053001'
      - name: last_modified_time
        in: query
        description: Last Modified Time of the Bill
        required: false
        schema:
          type: string
        example: 2013-09-11T17:18:32+0530
      - name: discount
        in: query
        description: Discount applied to the recurring bill, which can be either a
          percentage or a flat amount. For percentage discounts, the value should
          include the % symbol (e.g., 10%). For example, on Rs.1000, a 10% discount
          results in Rs.900, while a flat Rs.200 discount results in Rs.800.
        required: false
        schema:
          type: string
        example: 30%
      - name: discount_account_id
        in: query
        description: ID of the account associated with the discount account.
        required: false
        schema:
          type: string
        example: '460000000000403'
      - name: is_discount_before_tax
        in: query
        description: To specify discount applied in before /after tax.
        required: false
        schema:
          type: boolean
        example: true
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-recurring-bills-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    put:
      tags:
      - recurring-bills
      operationId: update_recurring_bill_using_custom_field
      summary: Update a recurring bill using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a recurring bill by providing its API name in the X-Unique-Identifier-Key
        header and its value in the X-Unique-Identifier-Value header. Based on this
        value, the corresponding recurring bill will be retrieved and updated. Additionally,
        there is an optional X-Upsert header. If the X-Upsert header is true and the
        custom field's unique value is not found in any of the existing recurring
        bills, a new recurring bill will be created if the necessary payload details
        are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-recurring-bill-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /recurringbills/{recurring_bill_id}:
    x-mcp-group:
    - Recurring Bills
    put:
      tags:
      - recurring-bills
      operationId: update_recurring_bill
      summary: Update a recurring bill
      description: Update a recurring bill. To delete a line item just remove it from
        the line_items list.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-recurring-bill-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.UPDATE
    parameters:
    - name: recurring_bill_id
      in: path
      required: true
      description: Unique identifier of the recurring bill.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurring_bills/{recurring_bill_id}:
    x-mcp-group:
    - Recurring Bills
    get:
      tags:
      - recurring-bills
      operationId: get_recurring_bill
      summary: Get a recurring bill
      description: Get the details of a recurring bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    delete:
      tags:
      - recurring-bills
      operationId: delete_recurring_bill
      summary: Delete a recurring bill
      description: Delete an existing recurring bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.DELETE
    parameters:
    - name: recurring_bill_id
      in: path
      required: true
      description: Unique identifier of the recurring bill.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringbills/{recurring_bill_id}/status/stop:
    x-mcp-group:
    - Recurring Bills
    post:
      tags:
      - recurring-bills
      operationId: stop_recurring_bill
      summary: Stop a recurring bill
      description: Stop an active recurring bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stop-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: recurring_bill_id
      in: path
      required: true
      description: Unique identifier of the recurring bill.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringbills/{recurring_bill_id}/status/resume:
    x-mcp-group:
    - Recurring Bills
    post:
      tags:
      - recurring-bills
      operationId: resume_recurring_bill
      summary: Resume a recurring Bill
      description: Resume a stopped recurring bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/resume-a-recurring-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.CREATE
    parameters:
    - name: recurring_bill_id
      in: path
      required: true
      description: Unique identifier of the recurring bill.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /recurringbills/{recurring_bill_id}/comments:
    x-mcp-group:
    - Recurring Bills
    get:
      tags:
      - recurring-bills
      operationId: list_recurring_bill_history
      summary: List recurring bill history
      description: Get history and comments of a recurring bill.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-recurring-bill-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.bills.READ
    parameters:
    - name: recurring_bill_id
      in: path
      required: true
      description: Unique identifier of the recurring bill.
      schema:
        type: string
      example: 982000000567240
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: create_vendor_credit
      summary: Create a vendor credit
      description: Create a new vendor credit to record credits issued by vendors
        for returned items, overpayments, or adjustments. Supports multi-currency
        transactions, custom line items, tax calculations, and workflows.
      parameters:
      - name: ignore_auto_number_generation
        in: query
        description: Ignore auto number generation for this vendor credit only. On
          enabling this option vendor credit number is mandatory.
        required: false
        schema:
          type: boolean
      - name: bill_id
        in: query
        description: Bill Associated with the Vendor Credit
        required: false
        schema:
          type: string
        example: '460000000057075'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-vendor-credit-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    get:
      tags:
      - vendor-credits
      operationId: list_vendor_credits
      summary: List vendor credits
      description: Retrieve a paginated list of vendor credits with comprehensive
        filtering, sorting, and search capabilities. Use query parameters to filter
        by date, status, amount, vendor details, items, taxes, and custom fields.
      parameters:
      - name: vendor_credit_number
        in: query
        description: Filter vendor credits by specific vendor credit number. Supports
          variants <code>vendor_credit_number_startswith</code> and <code>vendor_credit_number_contains</code>
          for partial matching.
        required: false
        schema:
          type: string
        example: DN-00002
      - name: date
        in: query
        description: 'Filter vendor credits by creation date. Use yyyy-mm-dd format.
          Supports variants: <code>date_start</code>, <code>date_end</code>, <code>date_before</code>
          and <code>date_after</code> for range-based filtering.'
        required: false
        schema:
          type: string
        example: '2014-08-28'
      - name: status
        in: query
        description: 'Filter vendor credits by their current status. Allowed values:
          <code>open</code>, <code>closed</code>, <code>void</code>, or <code>draft</code>.'
        required: false
        schema:
          type: string
        example: open
      - name: total
        in: query
        description: 'Filter vendor credits by their total amount. Supports variants:
          <code>total_start</code>, <code>total_end</code>, <code>total_less_than</code>,
          <code>total_less_equals</code>, <code>total_greater_than</code> and <code>total_greater_equals</code>
          for comparison-based filtering.'
        required: false
        schema:
          type: string
        example: 30
      - name: reference_number
        in: query
        description: 'Filter vendor credits by their reference number. Supports variants:
          <code>reference_number_startswith</code> and <code>reference_number_contains</code>
          for partial matching.'
        required: false
        schema:
          type: string
        example: ''
      - name: customer_name
        in: query
        description: 'Filter vendor credits by vendor name. Supports variants: <code>customer_name_startswith</code>
          and <code>customer_name_contains</code> for partial matching.'
        required: false
        schema:
          type: string
        example: Bowman and Co
      - name: item_name
        in: query
        description: 'Filter vendor credits by item name. Supports variants: <code>item_name_startswith</code>
          and <code>item_name_contains</code> for partial matching.'
        required: false
        schema:
          type: string
        example: ''
      - name: item_description
        in: query
        description: 'Filter vendor credits by item description. Supports variants:
          <code>item_description_startswith</code> and <code>item_description_contains</code>
          for partial matching.'
        required: false
        schema:
          type: string
        example: ''
      - name: notes
        in: query
        description: 'Filter vendor credits by notes content. Supports variants: <code>notes_startswith</code>
          and <code>notes_contains</code> for partial matching.'
        required: false
        schema:
          type: string
        example: ''
      - name: custom_field
        in: query
        description: 'Filter vendor credits by custom field values. Supports variants:
          <code>custom_field_startswith</code> and <code>custom_field_contains</code>
          for partial matching.'
        required: false
        schema:
          type: string
        example: ''
      - name: last_modified_time
        in: query
        description: Filter vendor credits by last modified time. Use ISO 8601 format
          (yyyy-mm-ddThh:mm:ss-hh:mm) to search for vendor credits modified at specific
          times.
        required: false
        schema:
          type: string
        example: 2014-08-28T22:53:31-0700
      - name: customer_id
        in: query
        description: Filter vendor credits by customer ID. Use this parameter to search
          for vendor credits associated with a specific customer. Retrieve customer
          IDs from the contacts API.
        required: false
        schema:
          type: string
        example: ''
      - name: line_item_id
        in: query
        description: Filter vendor credits by line item ID. Use this parameter to
          search for vendor credits containing a specific line item within the vendor
          credit.
        required: false
        schema:
          type: string
        example: '460000000020077'
      - name: item_id
        in: query
        description: Filter vendor credits by item ID. Use this parameter to search
          for vendor credits containing a specific item. Retrieve item IDs from the
          items API.
        required: false
        schema:
          type: string
        example: '460000000020071'
      - name: tax_id
        in: query
        description: Filter vendor credits by tax ID. Use this parameter to search
          for vendor credits with a specific tax applied. Retrieve tax IDs from the
          taxes API.
        required: false
        schema:
          type: string
        example: ''
      - name: filter_by
        in: query
        description: 'Filter vendor credits by status using predefined status values.
          Allowed values: <code>Status.All</code>, <code>Status.Open</code>, <code>Status.Draft</code>,
          <code>Status.Closed</code>, and <code>Status.Void</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Search vendor credits using text search across multiple fields.
          Searches vendor credit number, vendor name, and reference number.
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort vendor credits by specified column. Allowed values: <code>vendor_name</code>,
          <code>vendor_credit_number</code>, <code>balance</code>, <code>total</code>,
          <code>date</code>, <code>created_time</code>, <code>last_modified_time</code>,
          and <code>reference_number</code>.'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number for pagination. Specify which page of results to
          retrieve. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records per page for pagination. Controls how many
          vendor credits are returned in each page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-vendor-credits-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}:
    x-mcp-group:
    - Vendor Credits
    put:
      tags:
      - vendor-credits
      operationId: update_vendor_credit
      summary: Update vendor credit
      description: Update an existing vendor credit.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-vendor-credit-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.UPDATE
    get:
      tags:
      - vendor-credits
      operationId: get_vendor_credit
      summary: Get vendor credit
      description: Get details of a vendor credit.
      parameters:
      - name: print
        in: query
        description: 'Export vendor credit pdf with default print option. Allowed
          Values: <code>true</code>, <code>false</code>, <code>on</code> and <code>off</code>.'
        required: false
        schema:
          type: boolean
      - name: accept
        in: query
        description: 'You can get vendor credit details as json/pdf/html. Default
          format is html. Allowed Values: <code>json</code>, <code>xml</code>, <code>csv</code>,
          <code>xls</code>, <code>pdf</code>, <code>html</code> and <code>jhtml</code>.'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    delete:
      tags:
      - vendor-credits
      operationId: delete_vendor_credit
      summary: Delete vendor credit
      description: Delete a vendor credit.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.DELETE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/status/open:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: mark_vendor_credit_open
      summary: Convert to open
      description: Change an existing vendor credit status to open.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/convert-to-open-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/status/void:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: mark_vendor_credit_void
      summary: Void vendor credit
      description: Mark an existing vendor credit as void.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/void-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/submit:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: submit_vendor_credit
      summary: Submit a Vendor credit for approval
      description: Submit a Vendor credit for approval.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/submit-a-vendor-credit-for-approval-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/approve:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: approve_vendor_credit
      summary: Approve a Vendor credit
      description: Approve a Vendor credit.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/approve-a-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/bills:
    x-mcp-group:
    - Vendor Credits
    get:
      tags:
      - vendor-credits
      operationId: list_bills_credited
      summary: List bills credited
      description: List bills to which the vendor credit is applied.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-bills-credited-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    post:
      tags:
      - vendor-credits
      operationId: apply_credits_to_a_bill
      summary: Apply credits to a bill
      description: Apply vendor credit to existing bills.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/apply-credits-to-a-bill-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/apply-credits-to-a-bill-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/bills/{vendor_credit_bill_id}:
    x-mcp-group:
    - Vendor Credits
    delete:
      tags:
      - vendor-credits
      operationId: delete_vendor_credit_bill
      summary: Delete bills credited
      description: 'Delete the credits applied to a bill. <code>Note: </code>You should
        pass the vendor_credit_bill_id from "Get vendor credits > bills credited >
        vendor_credit_bill_id" section'
      parameters:
      - name: vendor_credit_bill_id
        in: path
        required: true
        description: Unique identifier of the vendor credit bill.
        schema:
          type: string
        example: '460000000057075'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-bills-credited-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.DELETE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/refunds:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: refund_vendor_credit
      summary: Refund a vendor credit
      description: Refund vendor credit amount.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refund-a-vendor-credit-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refund-a-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    get:
      tags:
      - vendor-credits
      operationId: list_vendor_credit_refunds_of_a_vendor_credit
      summary: List refunds of a vendor credit
      description: List all refunds of an existing vendor credit.
      parameters:
      - name: page
        in: query
        description: Page number for pagination. Specify which page of results to
          retrieve. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records per page for pagination. Controls how many
          vendor credits are returned in each page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-refunds-of-a-vendor-credit-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/refunds/{vendor_credit_refund_id}:
    x-mcp-group:
    - Vendor Credits
    put:
      tags:
      - vendor-credits
      operationId: update_vendor_credit_refund
      summary: Update vendor credit refund
      description: Update the refunded transaction.
      parameters:
      - name: vendor_credit_refund_id
        in: path
        required: true
        description: Unique identifier of the vendor credit refund.
        schema:
          type: string
        example: '3000000003151'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-vendor-credit-refund-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-vendor-credit-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.UPDATE
    get:
      tags:
      - vendor-credits
      operationId: get_vendor_credit_refund
      summary: Get vendor credit refund
      description: Get refund of a particular vendor credit.
      parameters:
      - name: vendor_credit_refund_id
        in: path
        required: true
        description: Unique identifier of the vendor credit refund.
        schema:
          type: string
        example: '3000000003151'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-vendor-credit-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    delete:
      tags:
      - vendor-credits
      operationId: delete_vendor_credit_refund
      summary: Delete vendor credit refund
      description: Delete a vendor credit refund.
      parameters:
      - name: vendor_credit_refund_id
        in: path
        required: true
        description: Unique identifier of the vendor credit refund.
        schema:
          type: string
        example: '3000000003151'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-vendor-credit-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.DELETE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/refunds:
    x-mcp-group:
    - Vendor Credits
    get:
      tags:
      - vendor-credits
      operationId: list_vendor_credit_refunds_of_all_vendor_credits
      summary: List vendor credit refunds
      description: List all refunds with pagination.
      parameters:
      - name: customer_id
        in: query
        description: Search vendor credits by vendor credit customer ID
        required: false
        schema:
          type: string
        example: ''
      - name: last_modified_time
        in: query
        description: Search vendor credits by vendor credit last modfified time
        required: false
        schema:
          type: string
        example: 2014-08-28T22:53:31-0700
      - name: sort_column
        in: query
        description: 'Sort vendor credits by following columns vendor_name, vendorcredit_number,
          balance, total, date and created_time. Allowed Values: <code>vendor_name</code>,
          <code>vendor_credit_number</code>, <code>balance</code>, <code>total</code>,
          <code>date</code>, <code>created_time</code>, <code>last_modified_time</code>
          and <code>reference_number</code>.'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number for pagination. Specify which page of results to
          retrieve. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records per page for pagination. Controls how many
          vendor credits are returned in each page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-vendor-credit-refunds-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/comments:
    x-mcp-group:
    - Vendor Credits
    post:
      tags:
      - vendor-credits
      operationId: add_vendor_credit_comment
      summary: Add a comment
      description: Add a comment to an existing vendor credit.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-a-comment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.CREATE
    get:
      tags:
      - vendor-credits
      operationId: list_vendor_credit_comments
      summary: List vendor credit comments & history
      description: Get history and comments of a vendor credit.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-vendor-credit-comments-and-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.READ
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorcredits/{vendor_credit_id}/comments/{comment_id}:
    x-mcp-group:
    - Vendor Credits
    delete:
      tags:
      - vendor-credits
      operationId: delete_vendor_credit_comment
      summary: Delete a comment
      description: Delete a vendor credit comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: Unique identifier of the comment.
        schema:
          type: string
        example: '3000000002089'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.debitnotes.DELETE
    parameters:
    - name: vendor_credit_id
      in: path
      required: true
      description: Unique identifier of the vendor credit.
      schema:
        type: string
      example: '3000000002075'
    - $ref: '#/components/parameters/organization_id'
  /vendorpayments:
    x-mcp-group:
    - Vendor Payments
    post:
      tags:
      - vendor-payments
      operationId: create_vendor_payment
      summary: Create a vendor payment
      description: Create a payment made to your vendor and you can also apply them
        to bills either partially or fully.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-vendor-payment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.CREATE
    get:
      tags:
      - vendor-payments
      operationId: list_vendor_payments
      summary: List vendor payments
      description: List all the payments made to your vendor.
      parameters:
      - name: vendor_name
        in: query
        description: 'Search payments by vendor name. Variants: <code>vendor_name_startswith</code>
          and <code>vendor_name_contains</code>.'
        required: false
        schema:
          type: string
        example: Bowman and Co
      - name: reference_number
        in: query
        description: 'Search payments by reference number. Variants: <code>reference_number_startswith</code>
          and <code>reference_number_contains</code>. In refunds, reference number
          for the refund recorded.'
        required: false
        schema:
          type: string
        example: REF#912300
      - name: payment_number
        in: query
        description: 'Search with Payment Number. Variant: <code>payment_number_startswith</code>,
          <code>payment_number_contains</code>'
        required: false
        schema:
          type: string
        example: '1'
      - name: date
        in: query
        description: 'Date the payment is made. Search payments by payment made date.
          Variants: <code>date_start</code>, <code>date_end</code>, <code>date_before</code>
          and <code>date_after</code>.'
        required: false
        schema:
          type: string
        example: '2013-10-07'
      - name: amount
        in: query
        description: 'Payment amount made to the vendor. Search payments by payment
          amount. Variants: <code>amount_less_than</code>, <code>amount_less_equals</code>,
          <code>amount_greater_than</code> and <code>amount_greater_equals</code>.
          In refunds, Amount refunded from the vendor payment.'
        required: false
        schema:
          type: number
          format: double
        example: 500
      - name: payment_mode
        in: query
        description: 'Search payments by payment mode. Variants: <code>payment_mode_startswith</code>
          and <code>payment_mode_contains</code>.'
        required: false
        schema:
          type: string
        example: Stripe
      - name: notes
        in: query
        description: 'Search with Payment Notes. Variant: <code>notes_startswith</code>,
          <code>notes_contains</code>'
        required: false
        schema:
          type: string
        example: Payment
      - name: vendor_id
        in: query
        description: ID of the vendor. Search payments by vendor id.
        required: false
        schema:
          type: string
        example: '460000000026049'
      - name: last_modified_time
        in: query
        description: Search with the Last Modified Time of the Vendor Payment
        required: false
        schema:
          type: string
        example: ''
      - name: bill_id
        in: query
        description: Search payments by Bill ID.
        required: false
        schema:
          type: string
        example: B-01
      - name: description
        in: query
        description: 'Search payments by description. Variants: <code>description_startswith</code>
          and <code>description_contains</code>.'
        required: false
        schema:
          type: string
        example: Payment
      - name: filter_by
        in: query
        description: 'Filter payments by mode. Allowed Values: <code>PaymentMode.All</code>,
          <code>PaymentMode.Check</code>, <code>PaymentMode.Cash</code>, <code>PaymentMode.BankTransfer</code>,
          <code>PaymentMode.Paypal</code>, <code>PaymentMode.CreditCard</code>, <code>PaymentMode.GoogleCheckout</code>,
          <code>PaymentMode.Credit</code>, <code>PaymentMode.Authorizenet</code>,
          <code>PaymentMode.BankRemittance</code>, <code>PaymentMode.Payflowpro</code>
          and <code>PaymentMode.Others</code>.'
        required: false
        schema:
          type: string
        example: PaymentMode.All
      - name: search_text
        in: query
        description: Search payments by reference number or vendor name or payment
          description.
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort the payment list. Allowed Values: <code>vendor_name</code>,
          <code>date</code>, <code>reference_number</code>, <code>amount</code> and
          <code>balance</code>.'
        required: false
        schema:
          type: string
        example: vendor_name
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-vendor-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.READ
    put:
      tags:
      - vendor-payments
      operationId: update_vendor_payment_using_custom_field
      summary: Update an vendor payment using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a vendor payment by providing its API name in the X-Unique-Identifier-Key
        header and its value in the X-Unique-Identifier-Value header. Based on this
        value, the corresponding vendor payment will be retrieved and updated. Additionally,
        there is an optional X-Upsert header. If the X-Upsert header is true and the
        custom field's unique value is not found in any of the existing vendor payments,
        a new vendor payment will be created if the necessary payload details are
        available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-vendor-payment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.UPDATE
    delete:
      tags:
      - vendor-payments
      operationId: bulk_delete_vendor_payments
      summary: Bulk delete vendor payments
      description: Delete multiple vendor payments.
      parameters:
      - name: vendorpayment_id
        in: query
        description: Comma-separated list of vendor payment IDs to be deleted
        required: true
        schema:
          type: string
        example: 460000000053219,460000000053220,460000000053221
      - name: bulk_delete
        in: query
        description: Flag to indicate bulk delete operation
        required: true
        schema:
          type: boolean
        example: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-delete-vendor-payments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.DELETE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /vendorpayments/{payment_id}:
    x-mcp-group:
    - Vendor Payments
    put:
      tags:
      - vendor-payments
      operationId: update_vendor_payment
      summary: Update a vendor payment
      description: Update an existing vendor payment. You can also modify the amount
        applied to the bills.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-vendor-payment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.UPDATE
    get:
      tags:
      - vendor-payments
      operationId: get_vendor_payment
      summary: Get a vendor payment
      description: Get the details of a vendor payment.
      parameters:
      - name: fetchTaxInfo
        in: query
        description: Check if tax information should be fetched
        required: false
        schema:
          type: boolean
        example: false
      - name: fetchstatementlineinfo
        in: query
        description: Check is Statement Line Information for Vendor Payment be fetched
        required: false
        schema:
          type: boolean
        example: true
      - name: print
        in: query
        description: Check if Vendor Payment must be printed.
        required: false
        schema:
          type: boolean
        example: false
      - name: is_bill_payment_id
        in: query
        description: Check if the ID is Bill Payment or Vendor Payment
        required: false
        schema:
          type: boolean
        example: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.READ
    delete:
      tags:
      - vendor-payments
      operationId: delete_vendor_payment
      summary: Delete a vendor payment
      description: Delete an existing vendor payment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.DELETE
    parameters:
    - name: payment_id
      in: path
      required: true
      description: Unique identifier of the payment.
      schema:
        type: string
      example: '460000000053219'
    - $ref: '#/components/parameters/organization_id'
  /vendorpayments/{payment_id}/refunds:
    x-mcp-group:
    - Vendor Payments
    get:
      tags:
      - vendor-payments
      operationId: list_vendor_payment_refunds
      summary: List refunds of a vendor payment
      description: List all the refunds pertaining to an existing vendor payment.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-refunds-of-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.READ
    post:
      tags:
      - vendor-payments
      operationId: refund_excess_vendor_payment
      summary: Refund an excess vendor payment
      description: Refund the excess amount paid to the vendor.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/refund-an-excess-vendor-payment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/refund-an-excess-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.CREATE
    parameters:
    - name: payment_id
      in: path
      required: true
      description: Unique identifier of the payment.
      schema:
        type: string
      example: '460000000053219'
    - $ref: '#/components/parameters/organization_id'
  /vendorpayments/{payment_id}/refunds/{vendorpayment_refund_id}:
    x-mcp-group:
    - Vendor Payments
    get:
      tags:
      - vendor-payments
      operationId: get_vendor_payment_refund
      summary: Details of a refund
      description: Obtain details of a particular refund of a vendor payment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/details-of-a-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.READ
    put:
      tags:
      - vendor-payments
      operationId: update_vendor_payment_refund
      summary: Update a refund
      description: Update the refunded transaction.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-refund-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.UPDATE
    delete:
      tags:
      - vendor-payments
      operationId: delete_vendor_payment_refund
      summary: Delete a refund
      description: Delete refund pertaining to an existing vendor payment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.DELETE
    parameters:
    - name: payment_id
      in: path
      required: true
      description: Unique identifier of the payment.
      schema:
        type: string
      example: '460000000053219'
    - name: vendorpayment_refund_id
      in: path
      required: true
      description: Unique identifier of the vendor payment refund.
      schema:
        type: string
      example: '460000000003017'
    - $ref: '#/components/parameters/organization_id'
  /vendorpayments/{payment_id}/email:
    x-mcp-group:
    - Vendor Payments
    post:
      tags:
      - vendor-payments
      operationId: email_vendor_payment
      summary: Email a vendor payment
      description: Send a vendor payment receipt to the vendor via email. You can
        customize the email content, attach files, and control sender preferences.
        If the request body is empty, the email will be sent with default content
        based on the email template associated with the vendor or the default template.
      parameters:
      - name: send_attachment
        in: query
        description: Send the vendor payment attachment with the email.
        required: false
        schema:
          type: boolean
        example: false
      - name: attachments
        in: query
        description: Files to be attached to the email
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: file_name
        in: query
        description: Name of the file to be attached
        required: false
        schema:
          type: string
        example: payment_receipt.pdf
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/email-a-vendor-payment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/email-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.CREATE
    get:
      tags:
      - vendor-payments
      operationId: get_vendor_payment_email_content
      summary: Get vendor payment email content
      description: Retrieve the pre-populated email content for a vendor payment,
        including subject, body, recipient contacts, sender options, and attachment
        details. This endpoint provides all the necessary information to compose and
        send a vendor payment receipt email.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-vendor-payment-email-content-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.vendorpayments.READ
    parameters:
    - name: payment_id
      in: path
      required: true
      description: Unique identifier of the payment.
      schema:
        type: string
      example: '460000000053219'
    - $ref: '#/components/parameters/organization_id'
  /settings/modules:
    x-mcp-group:
    - Custom Modules
    get:
      tags:
      - custom-modules
      operationId: list_custom_modules
      summary: List custom modules
      description: List all custom module configurations in the organization. This
        returns the module definitions including field configurations, permissions,
        and portal settings.
      parameters:
      - name: portal_type
        in: query
        description: 'Filter by portal type. Allowed values: <code>customer</code>,
          <code>vendor</code> and <code>employee</code>.'
        required: false
        schema:
          type: string
      - name: sort_order
        in: query
        description: 'Sort order. Allowed values: <code>ascending</code> and <code>descending</code>.'
        required: false
        schema:
          type: string
      - name: page
        in: query
        description: Page number for pagination.
        required: false
        schema:
          type: integer
      - name: per_page
        in: query
        description: Number of records per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-module-configurations-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    post:
      tags:
      - custom-modules
      operationId: create_custom_module
      summary: Create a custom module
      description: Create a new custom module configuration. Define the module name,
        fields, permissions, and other settings.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-module-configuration-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-module-configuration-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /settings/modules/{module_api_name}:
    x-mcp-group:
    - Custom Modules
    get:
      tags:
      - custom-modules
      operationId: get_custom_module
      summary: Get a custom module
      description: Get the configuration details of a specific custom module.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-module-configuration-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - custom-modules
      operationId: update_custom_module
      summary: Update a custom module
      description: Update the configuration of an existing custom module.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-module-configuration-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-module-configuration-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - custom-modules
      operationId: delete_custom_module
      summary: Delete a custom module
      description: Delete an existing custom module configuration and all its records.
      parameters:
      - name: force_delete
        in: query
        description: Force delete even if the module has existing records.
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-module-configuration-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: module_api_name
      in: path
      required: true
      description: API name of the custom module (e.g., <code>cm_debtor</code>).
      schema:
        type: string
      example: cm_debtor
    - $ref: '#/components/parameters/organization_id'
  /{module_name}:
    x-mcp-group:
    - Custom Modules
    get:
      tags:
      - custom-module-records
      operationId: list_custom_module_records
      summary: List custom module records
      description: Get the list of records of a custom module.
      parameters:
      - $ref: '#/components/parameters/module_name'
      - name: customview_id
        in: query
        description: Filter records based on a custom view.
        required: false
        schema:
          type: string
      - name: filter_by
        in: query
        description: 'Filter records by status. Allowed Values: <code>Status.All</code>,
          <code>Status.Active</code>, <code>Status.Inactive</code>'
        required: false
        schema:
          type: string
      - name: search_text
        in: query
        description: Search records by text.
        required: false
        schema:
          type: string
      - name: sort_column
        in: query
        description: 'Sort records by column. Allowed Values: <code>record_name</code>,
          <code>created_time</code>, <code>last_modified_time</code> or any custom
          field API name (<code>cf_*</code>).'
        required: false
        schema:
          type: string
      - name: sort_order
        in: query
        description: 'Sort order. Allowed Values: <code>A</code> (Ascending), <code>D</code>
          (Descending)'
        required: false
        schema:
          type: string
      - name: page
        in: query
        description: Page number for pagination.
        required: false
        schema:
          type: integer
      - name: per_page
        in: query
        description: Number of records per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-record-list-of-a-custom-module-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    put:
      tags:
      - custom-module-records
      operationId: bulk_update_custom_module_records
      summary: Bulk update custom module records
      description: Update existing custom module records in bulk.
      parameters:
      - $ref: '#/components/parameters/module_name'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/bulk-update-custom-module-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-update-custom-module-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    post:
      tags:
      - custom-module-records
      operationId: create_custom_module_record
      summary: Create a custom module record
      description: Create a new record in a custom module.
      parameters:
      - $ref: '#/components/parameters/module_name'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-custom-modules-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-custom-modules-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    delete:
      tags:
      - custom-module-records
      operationId: delete_custom_module_records
      summary: Delete custom module records
      description: Delete records from a custom module.
      parameters:
      - name: record_ids
        in: query
        description: Comma-separated list of record IDs to delete. Maximum 200 IDs.
        required: false
        schema:
          type: string
        example: 460000000639128,460000000639129
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-custom-modules-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    parameters:
    - name: module_name
      in: path
      description: Module Name
      required: true
      schema:
        type: string
      example: cm_debtor
    - $ref: '#/components/parameters/organization_id'
  /{module_name}/{module_id}:
    x-mcp-group:
    - Custom Modules
    get:
      tags:
      - custom-module-records
      operationId: get_custom_module_record
      summary: Get a custom module record
      description: Get the details of an individual record in a custom module.
      parameters:
      - $ref: '#/components/parameters/module_name'
      - $ref: '#/components/parameters/module_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-individual-record-details-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    put:
      tags:
      - custom-module-records
      operationId: update_custom_module_record
      summary: Update a custom module record
      description: Update an existing record in a custom module.
      parameters:
      - $ref: '#/components/parameters/module_name'
      - $ref: '#/components/parameters/module_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-custom-module-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-custom-module-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    delete:
      tags:
      - custom-module-records
      operationId: delete_custom_module_record
      summary: Delete a custom module record
      description: Delete an individual record from a custom module.
      parameters:
      - name: module_id
        in: path
        description: Custom Module ID
        required: true
        schema:
          type: string
        example: 2112155000000652000
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-individual-records-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.custommodules.ALL
    parameters:
    - name: module_name
      in: path
      description: Module Name
      required: true
      schema:
        type: string
      example: cm_debtor
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts:
    x-mcp-group:
    - Bank Accounts
    post:
      tags:
      - bank-accounts
      operationId: create_bank_account
      summary: Create a bank account
      description: Create a bank account or a credit card account for your organization.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-bank-account-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-bank-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    get:
      tags:
      - bank-accounts
      operationId: list_bank_accounts
      summary: List view of accounts
      description: List all bank and credit card accounts for your organization.
      parameters:
      - name: filter_by
        in: query
        description: 'Filter the account by their status. Allowed Values: <code>Status.All</code>,
          <code>Status.Active</code> and <code>Status.Inactive</code>.'
        required: false
        schema:
          type: string
        example: Status.All
      - name: sort_column
        in: query
        description: 'Sort the values based on the allowed values. Allowed Values:
          <code>account_name</code>,<code>account_type</code> and <code>account_code</code>.'
        required: false
        schema:
          type: string
        example: account_name
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-view-of-accounts-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/{account_id}:
    x-mcp-group:
    - Bank Accounts
    put:
      tags:
      - bank-accounts
      operationId: update_bank_account
      summary: Update bank account
      description: Modify the account that was created.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-bank-account-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-bank-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.UPDATE
    get:
      tags:
      - bank-accounts
      operationId: get_bank_account
      summary: Get account details
      description: Get a detailed look of the account specified.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-account-details-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    delete:
      tags:
      - bank-accounts
      operationId: delete_bank_account
      summary: Delete an account
      description: Delete a bank account from your organization.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.DELETE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the bank account.
      schema:
        type: string
      example: '460000000050127'
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/{account_id}/inactive:
    x-mcp-group:
    - Bank Accounts
    post:
      tags:
      - bank-accounts
      operationId: mark_bank_account_inactive
      summary: Deactivate account.
      description: Make an account inactive.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deactivate-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the bank account.
      schema:
        type: string
      example: '460000000050127'
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/{account_id}/active:
    x-mcp-group:
    - Bank Accounts
    post:
      tags:
      - bank-accounts
      operationId: mark_bank_account_active
      summary: Activate account
      description: Make an account active.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/activate-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the bank account.
      schema:
        type: string
      example: '460000000050127'
    - $ref: '#/components/parameters/organization_id'
  /bankstatements:
    x-mcp-group:
    - Bank Accounts
    post:
      tags:
      - bank-accounts
      operationId: import_bank_statements
      summary: Import a Bank/Credit Card Statement
      description: Import your bank/credit card feeds into your account.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/import-a-bankcredit-card-statement-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/import-a-bankcredit-card-statement-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/{account_id}/statement/lastimported:
    x-mcp-group:
    - Bank Accounts
    get:
      tags:
      - bank-accounts
      operationId: get_last_imported_bank_statement
      summary: Get last imported statement
      description: Get the details of previously imported statement for the account.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-last-imported-statement-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the bank account.
      schema:
        type: string
      example: '460000000050127'
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/{account_id}/statement/{statement_id}:
    x-mcp-group:
    - Bank Accounts
    delete:
      tags:
      - bank-accounts
      operationId: delete_last_imported_bank_statement
      summary: Delete last imported statement
      description: Delete the statement that was previously imported.
      parameters:
      - name: statement_id
        in: path
        required: true
        description: Unique identifier of the bank statement.
        schema:
          type: string
        example: '460000000049013'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-last-imported-statement-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.DELETE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the bank account.
      schema:
        type: string
      example: '460000000050127'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: create_bank_transaction
      summary: Create a transaction for an account
      description: Create a bank transaction based on the allowed transaction types.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-transaction-for-an-account-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-transaction-for-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    get:
      tags:
      - bank-transactions
      operationId: list_bank_transactions
      summary: Get transactions list
      description: Get all the transaction details involved in an account.
      parameters:
      - name: account_id
        in: query
        description: ID of the bank account to filter transactions.
        required: false
        schema:
          type: string
        example: '460000000048001'
      - name: transaction_type
        in: query
        description: 'Type of the bank transaction. Allowed Values: <code>sales_without_invoices</code>,
          <code>expense_refund</code>, <code>owner_drawings</code>, <code>owner_contribution</code>,
          <code>interest_income</code>, <code>other_income</code>, <code>expense</code>,
          <code>deposit</code>'
        required: false
        schema:
          type: string
        example: deposit
      - name: date
        in: query
        description: 'Start and end date, to provide a range within which the transaction
          date exist. Variants: <code>date_start</code> and <code>date_end</code>'
        required: false
        schema:
          type: string
        example: '2013-10-01'
      - name: amount
        in: query
        description: 'Start and end amount, to provide a range within which the transaction
          amount exist. Variants: <code>amount_start</code> and <code>amount_end</code>'
        required: false
        schema:
          type: number
          format: double
        example: 2000
      - name: status
        in: query
        description: Transaction status wise list view - All, uncategorized, manually_added,
          matched, excluded, categorized
        required: false
        schema:
          type: string
        example: categorized
      - name: reference_number
        in: query
        description: Search using Reference Number of the transaction
        required: false
        schema:
          type: string
        example: Ref-121
      - name: filter_by
        in: query
        description: 'Filters the transactions based on the allowed types. Allowed
          Values: <code>Status.All</code>, <code>Status.Uncategorized</code>, <code>Status.Categorized</code>,
          <code>Status.ManuallyAdded</code>, <code>Status.Excluded</code> and <code>Status.Matched</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sorts the transactions based on the allowed sort types. Allowed
          Values: <code>date</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: transaction_status
        in: query
        description: Transaction status wise list view - All, uncategorized, manually_added,
          matched, excluded, categorized
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Search Transactions by contact name or description
        required: false
        schema:
          type: string
        example: ''
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-transactions-list-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/{bank_transaction_id}:
    x-mcp-group:
    - Bank Transactions
    put:
      tags:
      - bank-transactions
      operationId: update_bank_transaction
      summary: Update a transaction
      description: Make changes in the applicable fields of a transaction and update
        it.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-transaction-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.UPDATE
    get:
      tags:
      - bank-transactions
      operationId: get_bank_transaction
      summary: Get transaction
      description: Fetch the details of a transaction by specifying the transaction_id.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    delete:
      tags:
      - bank-transactions
      operationId: delete_bank_transaction
      summary: Delete a transaction
      description: Delete a transaction from an account by specifying the transaction_id.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.DELETE
    parameters:
    - name: bank_transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/match:
    x-mcp-group:
    - Bank Transactions
    get:
      tags:
      - bank-transactions
      operationId: get_matching_bank_transactions
      summary: Get matching transactions
      description: Provide criteria to search for matching uncategorised transactions.
        The list of transactions can also include invoices/bills/credit-notes which
        will not be matched directly. Instead, a new (payment/refund) transaction
        is recorded and matched.
      parameters:
      - name: transaction_id
        in: query
        description: ID of the Transaction
        required: false
        schema:
          type: string
        example: '460000000048017'
      - name: transaction_type
        in: query
        description: 'Type of the transaction. <br> <strong>Allowed transaction types</strong>
          : deposit, refund(*Supported only in Credit Card accounts), transfer_fund,
          card_payment, sales_without_invoices, expense_refund, owner_contribution,
          interest_income, other_income, owner_drawings, sales_return <br> <strong>Note:</strong>
          You will not be able to create the following module-specific transaction
          types under Bank Transaction endpoints :<div> <a href="#Expenses">Expense</a>,
          <a href="#Vendor-Payments">Vendor Advance/Vendor Payment/Bill Payment</a>,
          <a href="#Vendor-Credits_Refund_a_vendor_credit">Vendor Credit Refund</a>,
          <a href="#Vendor-Payments_Refund_an_excess_vendor_payment">Vendor Payment
          Refund</a>, <a href="#Customer-Payments">Customer Advance/Customer Payment</a>,
          <a href="#Credit-Notes_Refund_credit_note">Credit Note Refund</a>, <a href="#Customer-Payments_Refund_an_excess_customer_payment">Payment
          Refund</a>. Hence, refer them in their repective modules.</div>'
        required: false
        schema:
          type: string
        example: deposit
      - name: date_after
        in: query
        description: Date after which Transactions are to be filtered
        required: false
        schema:
          type: string
        example: ''
      - name: date_before
        in: query
        description: Date before which Transactions are to be filtered
        required: false
        schema:
          type: string
        example: ''
      - name: amount_start
        in: query
        description: Starting amout with which transactions are to be filtered
        required: false
        schema:
          type: number
          format: double
        example: ''
      - name: amount_end
        in: query
        description: Starting amout with which transactions are to be filtered
        required: false
        schema:
          type: number
          format: double
        example: ''
      - name: contact
        in: query
        description: Contact person name, involved in the transaction.
        required: false
        schema:
          type: string
        example: ''
      - name: reference_number
        in: query
        description: Reference Number of the transaction
        required: false
        schema:
          type: string
        example: Ref-121
      - name: show_all_transactions
        in: query
        description: Check if all transactions must be shown
        required: false
        schema:
          type: boolean
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-matching-transactions-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    post:
      tags:
      - bank-transactions
      operationId: match_bank_transaction
      summary: Match a transaction
      description: Match an uncategorized transaction with an existing transaction
        in the account.
      parameters:
      - name: account_id
        in: query
        description: Mandatory Account id for which transactions are to be listed.
        required: false
        schema:
          type: string
        example: '460000000048001'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/match-a-transaction-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/match-a-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/{transaction_id}/unmatch:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: unmatch_bank_transaction
      summary: Unmatch a matched transaction
      description: Unmatch a transaction that was previously matched and make it uncategorized.
      parameters:
      - name: account_id
        in: query
        description: Mandatory Account id for which transactions are to be listed.
        required: false
        schema:
          type: string
        example: '460000000048001'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/unmatch-a-matched-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/exclude:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: exclude_bank_transaction
      summary: Exclude a transaction
      description: Exclude a transaction from your bank or credit card account.
      parameters:
      - name: account_id
        in: query
        description: Mandatory Account id for which transactions are to be listed.
        required: false
        schema:
          type: string
        example: '460000000048001'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/exclude-a-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/restore:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: restore_bank_transaction
      summary: Restore a transaction
      description: Restore an excluded transaction in your account.
      parameters:
      - name: account_id
        in: query
        description: Mandatory Account id for which transactions are to be listed.
        required: false
        schema:
          type: string
        example: '460000000048001'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/restore-a-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/categorize:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_bank_transaction
      summary: Categorize an uncategorized transaction
      description: Categorize an uncategorized transaction by creating a new transaction.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-an-uncategorized-transaction-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-an-uncategorized-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/categorize/expenses:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_bank_transaction_as_expense
      summary: Categorize as expense
      description: Categorize an Uncategorized transaction as expense.
      parameters:
      - name: doc
        in: query
        description: Document that is to be attached
        required: false
        schema:
          type: string
        example: ''
      - name: totalFiles
        in: query
        description: Total number of files.
        required: false
        schema:
          type: integer
        example: 0
      - name: document_ids
        in: query
        description: Comma-separated list of document IDs to be attached to the transaction.
        required: false
        schema:
          type: string
        example: ''
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-as-expense-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-as-expense-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/{transaction_id}/uncategorize:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: uncategorize_bank_transaction
      summary: Uncategorize a categorized transaction
      description: Revert a categorized transaction as uncategorized.
      parameters:
      - name: account_id
        in: query
        description: Mandatory Account id for which transactions are to be listed.
        required: false
        schema:
          type: string
        example: '460000000048001'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/uncategorize-a-categorized-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/categorize/vendorpayments:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_bank_transaction_as_vendor_payment
      summary: Categorize a vendor payment
      description: Categorize an uncategorized transaction as Vendor Payment.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-a-vendor-payment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-a-vendor-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/categorize/customerpayments:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_bank_transaction_as_customer_payment
      summary: Categorize as customer payment
      description: Categorize an uncategorized transaction as Customer Payment.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-as-customer-payment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-as-customer-payment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/categorize/creditnoterefunds:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_as_credit_note_refunds
      summary: Categorize as credit note refunds
      description: Categorize an Uncategorized transaction as a refund from a credit
        note.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-as-credit-note-refunds-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-as-credit-note-refunds-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{transaction_id}/categorize/vendorcreditrefunds:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_as_vendor_credit_refunds
      summary: Categorize as vendor credit refunds
      description: Categorize an uncategorized transaction as a refund from a vendor
        credit.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-as-vendor-credit-refunds-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-as-vendor-credit-refunds-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the bank transaction.
      schema:
        type: string
      example: '460000000048017'
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{statement_line_id}/categorize/paymentrefunds:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_bank_transaction_as_payment_refund
      summary: Categorize as Customer Payment refund
      description: Categorizing bank transactions as Payment Refund.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-as-customer-payment-refund-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-as-customer-payment-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: statement_line_id
      in: path
      required: true
      description: Unique identifier of the bank statement line.
      schema:
        type: string
      example: ''
    - $ref: '#/components/parameters/organization_id'
  /banktransactions/uncategorized/{statement_line_id}/categorize/vendorpaymentrefunds:
    x-mcp-group:
    - Bank Transactions
    post:
      tags:
      - bank-transactions
      operationId: categorize_as_vendor_payment_refund
      summary: Categorize as Vendor Payment refund
      description: Categorizing bank transactions as Vendor Payment Refund.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/categorize-as-vendor-payment-refund-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/categorize-as-vendor-payment-refund-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - name: statement_line_id
      in: path
      required: true
      description: Unique identifier of the bank statement line.
      schema:
        type: string
      example: ''
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/rules:
    x-mcp-group:
    - Bank Rules
    get:
      tags:
      - bank-rules
      operationId: list_bank_account_rules
      summary: Get Rules List
      description: Fetch all the rules created for a specified bank or credit card
        account ID.
      parameters:
      - name: account_id
        in: query
        description: ID of the Bank Account
        required: true
        schema:
          type: string
        example: 460000000000361
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-rules-list-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    post:
      tags:
      - bank-rules
      operationId: create_bank_account_rule
      summary: Create a rule
      description: Create a rule and apply it on deposit/withdrawal for bank accounts
        and on refund/charges for credit card accounts.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-rule-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-rule-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /bankaccounts/rules/{rule_id}:
    x-mcp-group:
    - Bank Rules
    get:
      tags:
      - bank-rules
      operationId: get_bank_account_rule
      summary: Get a rule
      description: Get details of a specific rule.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-rule-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.READ
    put:
      tags:
      - bank-rules
      operationId: update_bank_account_rule
      summary: Update a rule
      description: Make changes to the rule, add or modify it and update.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-rule-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-rule-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.UPDATE
    delete:
      tags:
      - bank-rules
      operationId: delete_bank_account_rule
      summary: Delete a rule
      description: Delete a rule from your account and make it no longer applicable
        on the transactions.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-rule-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.banking.DELETE
    parameters:
    - name: rule_id
      in: path
      required: true
      description: Unique identifier of the bank account rule.
      schema:
        type: string
      example: '460000000048005'
    - $ref: '#/components/parameters/organization_id'
  /chartofaccounts:
    x-mcp-group:
    - Chart Of Accounts
    post:
      tags:
      - chart-of-accounts
      operationId: create_chart_of_account
      summary: Create an account
      description: Creates an account with the given account type.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-account-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    get:
      tags:
      - chart-of-accounts
      operationId: list_chart_of_accounts
      summary: List chart of accounts
      description: List all chart of accounts along with pagination.
      parameters:
      - name: showbalance
        in: query
        description: Boolean to get current balance of accounts.
        required: false
        schema:
          type: boolean
      - name: filter_by
        in: query
        description: 'Filter accounts based on its account type and status. Allowed
          Values: <code>AccountType.All</code>, <code>AccountType.Active</code>, <code>AccountType.Inactive</code>,
          <code>AccountType.Asset</code>, <code>AccountType.Liability</code>, <code>AccountType.Equity</code>,
          <code>AccountType.Income</code> and <code>AccountType.Expense</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort accounts. Allowed Values: <code>account_name</code> and
          <code>account_type</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: last_modified_time
        in: query
        description: List accounts modified after the given date and time.
        required: false
        schema:
          type: string
        example: 2013-01-17T15:27:23+0530
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-chart-of-accounts-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /chartofaccounts/{account_id}:
    x-mcp-group:
    - Chart Of Accounts
    put:
      tags:
      - chart-of-accounts
      operationId: update_chart_of_account
      summary: Update an account
      description: Updates the account information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-account-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.UPDATE
    get:
      tags:
      - chart-of-accounts
      operationId: get_chart_of_account
      summary: Get an account
      description: Gets the details of an account.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    delete:
      tags:
      - chart-of-accounts
      operationId: delete_chart_of_account
      summary: Delete an account
      description: Deletes the given account. Accounts associated in any transaction/products
        could not be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.DELETE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the account.
      schema:
        type: string
      example: '460000000038079'
    - $ref: '#/components/parameters/organization_id'
  /chartofaccounts/{account_id}/active:
    x-mcp-group:
    - Chart Of Accounts
    post:
      tags:
      - chart-of-accounts
      operationId: mark_chart_of_account_active
      summary: Mark an account as active
      description: Updates the account status as active.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-an-account-as-active-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the account.
      schema:
        type: string
      example: '460000000038079'
    - $ref: '#/components/parameters/organization_id'
  /chartofaccounts/{account_id}/inactive:
    x-mcp-group:
    - Chart Of Accounts
    post:
      tags:
      - chart-of-accounts
      operationId: mark_chart_of_account_inactive
      summary: Mark an account as inactive
      description: Updates the account status as inactive.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-an-account-as-inactive-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    parameters:
    - name: account_id
      in: path
      required: true
      description: Unique identifier of the account.
      schema:
        type: string
      example: '460000000038079'
    - $ref: '#/components/parameters/organization_id'
  /chartofaccounts/transactions:
    x-mcp-group:
    - Chart Of Accounts
    get:
      tags:
      - chart-of-accounts
      operationId: list_chart_of_account_transactions
      summary: List of transactions for an account
      description: List all involved transactions for the given account.
      parameters:
      - name: account_id
        in: query
        description: ID of the Account
        required: true
        schema:
          type: string
        example: '460000000038079'
      - name: date
        in: query
        description: 'Search account transactions with the given date range. Default
          date format is yyyy-mm-dd. Variants: <code>date.start</code>, <code>date.end</code>,
          <code>date.before</code> and <code>date.after</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: amount
        in: query
        description: 'Search account transactions with given amount range. Variants:
          <code>amount.less_than</code>, <code>amount.less_equals</code>, <code>amount.greater_than</code>
          and <code>amount.greater_equals</code>.'
        required: false
        schema:
          type: number
          format: double
        example: ''
      - name: filter_by
        in: query
        description: 'Filter accounts based on its account type and status. Allowed
          Values: <code>AccountType.All</code>, <code>AccountType.Active</code>, <code>AccountType.Inactive</code>,
          <code>AccountType.Asset</code>, <code>AccountType.Liability</code>, <code>AccountType.Equity</code>,
          <code>AccountType.Income</code> and <code>AccountType.Expense</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: transaction_type
        in: query
        description: 'Search transactions based on the given transaction type. Allowed
          Values: <code>invoice</code>, <code>customer_payment</code>, <code>bills</code>,
          <code>vendor_payment</code>, <code>credit_notes</code>, <code>creditnote_refund</code>,
          <code>expense</code>, <code>card_payment</code>, <code>purchase_or_charges</code>,
          <code>journal</code>, <code>deposit</code>, <code>refund</code>, <code>transfer_fund</code>,
          <code>base_currency_adjustment</code>, <code>opening_balance</code>, <code>sales_without_invoices</code>,
          <code>expense_refund</code>, <code>tax_refund</code>, <code>receipt_from_initial_debtors</code>,
          <code>owner_contribution</code>, <code>interest_income</code>, <code>other_income</code>,
          <code>owner_drawings</code> and <code>payment_to_initial_creditors</code>.'
        required: false
        schema:
          type: string
        example: customer_payment
      - name: sort_column
        in: query
        description: 'Sort accounts. Allowed Values: <code>account_name</code> and
          <code>account_type</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-of-transactions-for-an-account-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /chartofaccounts/transactions/{transaction_id}:
    x-mcp-group:
    - Chart Of Accounts
    delete:
      tags:
      - chart-of-accounts
      operationId: delete_chart_of_account_transaction
      summary: Delete a transaction
      description: Deletes the transaction.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-transaction-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.DELETE
    parameters:
    - name: transaction_id
      in: path
      required: true
      description: Unique identifier of the transaction.
      schema:
        type: string
      example: '460000000050163'
    - $ref: '#/components/parameters/organization_id'
  /journals:
    x-mcp-group:
    - Journals
    post:
      tags:
      - journals
      operationId: create_journal
      summary: Create a journal
      description: Create a journal.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-journal-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-journal-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    get:
      tags:
      - journals
      operationId: list_journals
      summary: Get journal list
      description: Get journal list.
      parameters:
      - name: entry_number
        in: query
        description: 'Search journals by journal entry number. Variants: <code>entry_number_startswith</code>
          and <code>entry_number_contains</code>'
        required: false
        schema:
          type: string
        example: '1'
      - name: reference_number
        in: query
        description: 'Search journals by journal reference number. Variants: <code>reference_number_startswith</code>
          and <code>reference_number_contains</code>'
        required: false
        schema:
          type: string
        example: '7355'
      - name: date
        in: query
        description: 'Search journals by journal date. Variants: <code>date_start</code>,
          <code>date_end</code>, <code>date_before and</code> <code>date_after</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: notes
        in: query
        description: 'Search journals by journal notes. Variants: <code>notes_startswith</code>
          and <code>notes_contains</code>'
        required: false
        schema:
          type: string
        example: Loan repayment
      - name: last_modified_time
        in: query
        description: Search the journals using Last Modified Time
        required: false
        schema:
          type: string
        example: 2013-09-05T17:13:31+0530
      - name: total
        in: query
        description: 'Search journals by journal total. Variants: <code>total_less_than</code>,
          <code>total_less_equals</code>, <code>total_greater_than</code> and <code>total_greater_equals</code>'
        required: false
        schema:
          type: number
          format: double
        example: 5000
      - name: customer_id
        in: query
        description: Search Journals using Customer ID
        required: false
        schema:
          type: string
        example: ''
      - name: vendor_id
        in: query
        description: Search the journals using Vendor ID
        required: false
        schema:
          type: string
        example: ''
      - name: filter_by
        in: query
        description: 'Filter journals by journal date. Allowed Values: <code>JournalDate.All</code>,
          <code>JournalDate.Today</code>, <code>JournalDate.ThisWeek</code>, <code>JournalDate.ThisMonth</code>,
          <code>JournalDate.ThisQuarter</code> and <code>JournalDate.ThisYear</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort journal list. Allowed Values: <code>journal_date</code>,
          <code>entry_number</code>, <code>reference_number</code> and <code>total</code>'
        required: false
        schema:
          type: string
        example: journal_date
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-journal-list-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /journals/{journal_id}:
    x-mcp-group:
    - Journals
    put:
      tags:
      - journals
      operationId: update_journal
      summary: Update a journal
      description: Updates the journal with given information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-journal-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-journal-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.UPDATE
    get:
      tags:
      - journals
      operationId: get_journal
      summary: Get journal
      description: Get the details of the journal.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-journal-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    delete:
      tags:
      - journals
      operationId: delete_journal
      summary: Delete a journal
      description: Deletes the given journal.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-journal-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.DELETE
    parameters:
    - name: journal_id
      in: path
      required: true
      description: Unique identifier of the journal.
      schema:
        type: string
      example: '460000000038001'
    - $ref: '#/components/parameters/organization_id'
  /journals/{journal_id}/status/publish:
    x-mcp-group:
    - Journals
    post:
      tags:
      - journals
      operationId: mark_journal_published
      summary: Mark a journal as published
      description: Mark a draft journal as published.
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-a-journal-as-published-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    parameters:
    - name: journal_id
      in: path
      required: true
      description: Unique identifier of the journal.
      schema:
        type: string
      example: '460000000038001'
    - $ref: '#/components/parameters/organization_id'
  /journals/{journal_id}/attachment:
    x-mcp-group:
    - Journals
    post:
      tags:
      - journals
      operationId: add_journal_attachment
      summary: Add attachment to a journal
      description: Attach a file to a journal.
      parameters:
      - name: attachment
        in: query
        description: The file that is to be added as an Attachment in the Journal
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: doc
        in: query
        description: Document that is to be attached
        required: false
        schema:
          type: string
          format: binary
        example: ''
      - name: totalFiles
        in: query
        description: Total number of files.
        required: false
        schema:
          type: integer
        example: 0
      - name: document_ids
        in: query
        description: ID's of the document
        required: false
        schema:
          type: string
        example: ''
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-attachment-to-a-journal-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    parameters:
    - name: journal_id
      in: path
      required: true
      description: Unique identifier of the journal.
      schema:
        type: string
      example: '460000000038001'
    - $ref: '#/components/parameters/organization_id'
  /journals/{journal_id}/comments:
    x-mcp-group:
    - Journals
    post:
      tags:
      - journals
      operationId: add_journal_comment
      summary: Add comment
      description: Add a comment for a journal.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    parameters:
    - name: journal_id
      in: path
      required: true
      description: Unique identifier of the journal.
      schema:
        type: string
      example: '460000000038001'
    - $ref: '#/components/parameters/organization_id'
  /journals/{journal_id}/comments/{comment_id}:
    x-mcp-group:
    - Journals
    delete:
      tags:
      - journals
      operationId: delete_journal_comment
      summary: Delete a comment
      description: Delete a jounral comment.
      parameters:
      - name: comment_id
        in: path
        required: true
        description: Unique identifier of the comment.
        schema:
          type: string
        example: '460000000048023'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.DELETE
    parameters:
    - name: journal_id
      in: path
      required: true
      description: Unique identifier of the journal.
      schema:
        type: string
      example: '460000000038001'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: create_fixed_asset
      summary: Create a fixed asset
      description: Create a fixed asset.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-fixed-asset-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-fixed-asset-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    get:
      tags:
      - fixed-assets
      operationId: list_fixed_assets
      summary: Get fixed asset list
      description: fixed asset list.
      parameters:
      - name: filter_by
        in: query
        description: 'Filter fixed asset by fixed asset status. Allowed Values: <code>Status.All</code>,
          <code>Status.Active</code>, <code>Status.Cancel</code>, <code>Status.FullyDepreciated</code>,
          <code>Status.WriteOff</code>, <code>Status.Sold</code> and <code>Status.Draft</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort fixed asset list. Allowed Values: <code>asset_name</code>,
          <code>asset_number</code>, <code>asset_cost</code>, <code>created_time</code>
          and <code>current_asset_value</code>'
        required: false
        schema:
          type: string
        example: asset_number
      - name: sort_order
        in: query
        description: 'Sort fixed asset list in ascendeing or descending order. Allowed
          Values: <code>A</code> and <code>D</code>'
        required: false
        schema:
          type: string
        example: A
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-fixed-asset-list-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}:
    x-mcp-group:
    - Fixed Assets
    put:
      tags:
      - fixed-assets
      operationId: update_fixed_asset
      summary: Update a fixed asset
      description: Updates the fixed asset with given information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-fixed-asset-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-fixed-asset-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.UPDATE
    get:
      tags:
      - fixed-assets
      operationId: get_fixed_asset
      summary: Get fixed asset
      description: Get the details of the fixed asset.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-fixed-asset-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.READ
    delete:
      tags:
      - fixed-assets
      operationId: delete_fixed_asset
      summary: Delete a fixed asset
      description: Deletes the given fixed asset.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-fixed-asset-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.DELETE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/history:
    x-mcp-group:
    - Fixed Assets
    get:
      tags:
      - fixed-assets
      operationId: get_fixed_asset_history
      summary: Get fixed asset history
      description: It displays a detailed summary of the asset from acquisition till
        write off.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-fixed-asset-history-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.READ
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/forecast:
    x-mcp-group:
    - Fixed Assets
    get:
      tags:
      - fixed-assets
      operationId: get_fixed_asset_forecast
      summary: Get fixed asset's forecast depreciation
      description: It displays a detailed summary of the asset's future depreciation
        rates.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-fixed-asset-forecast-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.READ
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/status/active:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: mark_fixed_asset_active
      summary: Mark fixed asset as active
      description: Mark the fixed asset as active to start calculating depreciation
        for the asset.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-fixed-asset-status'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/status/cancel:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: mark_fixed_asset_cancel
      summary: Cancel fixed asset
      description: Cancel the fixed asset.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-fixed-asset-status'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/status/draft:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: mark_fixed_asset_draft
      summary: Mark fixed asset as draft
      description: Mark the fixed asset as draft.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-fixed-asset-status'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/writeoff:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: write_off_fixed_asset
      summary: Write off fixed asset
      description: Write off the fixed asset.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/write-off-fixed-asset-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/write-off-fixed-asset-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/sell:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: sell_fixed_asset
      summary: Sell fixed asset
      description: Sell the fixed asset.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/sell-fixed-asset-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sell-fixed-asset-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/comments:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: create_fixed_asset_comment
      summary: Add a comment
      description: Add a comment to the fixed asset.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-comment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /fixedassets/{fixed_asset_id}/comments/{comment_id}:
    x-mcp-group:
    - Fixed Assets
    delete:
      tags:
      - fixed-assets
      operationId: delete_fixed_asset_comment
      summary: Delete a comment
      description: Delete the comment of the fixed asset.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.DELETE
    parameters:
    - name: fixed_asset_id
      in: path
      required: true
      description: Unique identifier of the fixed asset.
      schema:
        type: string
      example: '3640355000000319008'
    - name: comment_id
      in: path
      required: true
      description: Unique identifier of the comment.
      schema:
        type: string
      example: '3640355000000319013'
    - $ref: '#/components/parameters/organization_id'
  /fixedassettypes:
    x-mcp-group:
    - Fixed Assets
    post:
      tags:
      - fixed-assets
      operationId: create_fixed_asset_type
      summary: Create a fixed asset type
      description: Create a fixed asset type.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-fixed-asset-type-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-fixed-asset-type-response'
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.CREATE
    get:
      tags:
      - fixed-assets
      operationId: get_fixed_asset_type_list
      summary: Get fixed asset type list
      description: fixed asset type list.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-fixed-asset-type-list-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /fixedassettypes/{fixed_asset_type_id}:
    x-mcp-group:
    - Fixed Assets
    put:
      tags:
      - fixed-assets
      operationId: update_fixed_asset_type
      summary: Update a fixed asset type
      description: Updates the fixed asset type with given information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-fixed-asset-type-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-fixed-asset-type-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.UPDATE
    delete:
      tags:
      - fixed-assets
      operationId: delete_fixed_asset_type
      summary: Delete a fixed asset type
      description: Deletes the given fixed asset type.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-fixed-asset-type-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.fixedasset.DELETE
    parameters:
    - name: fixed_asset_type_id
      in: path
      required: true
      description: Unique identifier of the fixed asset type.
      schema:
        type: string
      example: '3640355000000319008'
    - $ref: '#/components/parameters/organization_id'
  /basecurrencyadjustment:
    x-mcp-group:
    - Base Currency Adjustment
    - Currency
    post:
      tags:
      - base-currency-adjustment
      operationId: create_base_currency_adjustment
      summary: Create a base currency adjustment
      description: Creates a base currency adjustment for the given information.
      parameters:
      - name: account_ids
        in: query
        description: ID of the accounts for which base currency adjustments need to
          be posted.
        required: true
        schema:
          type: string
        example: 460000000000364
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-base-currency-adjustment-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-base-currency-adjustment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.CREATE
    get:
      tags:
      - base-currency-adjustment
      operationId: list_base_currency_adjustments
      summary: List base currency adjustment
      description: Lists base currency adjustment.
      parameters:
      - name: filter_by
        in: query
        description: 'Filter base currency adjustment list. Allowed Values: <code>Date.All</code>,
          <code>Date.Today</code>, <code>Date.ThisWeek</code>, <code>Date.ThisMonth</code>,
          <code>Date.ThisQuarter</code> and <code>Date.ThisYear</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort base currency adjustment list. Allowed Values: <code>adjustment_date</code>,
          <code>exchange_rate</code>, <code>currency_code</code>, <code>debit_or_credit</code>
          and <code>gain_or_loss</code>.'
        required: false
        schema:
          type: string
        example: ''
      - name: last_modified_time
        in: query
        description: Search using the Last Modified Time of the Base Currency Adjustment
        required: false
        schema:
          type: string
        example: ''
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-base-currency-adjustment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /basecurrencyadjustment/{base_currency_adjustment_id}:
    x-mcp-group:
    - Base Currency Adjustment
    - Currency
    get:
      tags:
      - base-currency-adjustment
      operationId: get_base_currency_adjustment
      summary: Get a base currency adjustment
      description: Get the base currency adjustment details.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-base-currency-adjustment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    delete:
      tags:
      - base-currency-adjustment
      operationId: delete_base_currency_adjustment
      summary: Delete a base currency adjustment
      description: Deletes the base currency adjustment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-base-currency-adjustment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.DELETE
    parameters:
    - name: base_currency_adjustment_id
      in: path
      required: true
      description: Unique identifier of the base currency adjustment.
      schema:
        type: string
      example: '460000000039001'
    - $ref: '#/components/parameters/organization_id'
  /basecurrencyadjustment/accounts:
    x-mcp-group:
    - Base Currency Adjustment
    - Currency
    get:
      tags:
      - base-currency-adjustment
      operationId: list_base_currency_adjustment_accounts
      summary: List account details for base currency adjustment
      description: List of accounts having transaction with effect to the given exchange
        rate.
      parameters:
      - name: currency_id
        in: query
        description: ID of currency for which we need to post adjustment.
        required: true
        schema:
          type: string
        example: '460000000000109'
      - name: adjustment_date
        in: query
        description: Date of adjustment.
        required: true
        schema:
          type: string
        example: '2013-09-05'
      - name: exchange_rate
        in: query
        description: Exchange rate of the currency.
        required: true
        schema:
          type: number
          format: double
        example: 1
      - name: notes
        in: query
        description: Notes for base currency adjustment.
        required: true
        schema:
          type: string
        example: Base Currency Adjustment against EUR
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-account-details-for-base-currency-adjustment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.accountants.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /projects:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: create_project
      summary: Create a project
      description: Create a project.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-project-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    get:
      tags:
      - projects
      operationId: list_projects
      summary: List projects
      description: List all projects with pagination.
      parameters:
      - name: filter_by
        in: query
        description: 'Filter projects by any status. Allowed Values: <code>Status.All</code>,
          <code>Status.Active</code> and <code>Status.Inactive</code>'
        required: false
        schema:
          type: string
        example: Status.All
      - name: customer_id
        in: query
        description: Search projects by customer id.
        required: false
        schema:
          type: string
        example: '460000000044001'
      - name: sort_column
        in: query
        description: 'Sort projects. Allowed Values: <code>project_name</code>, <code>customer_name</code>,
          <code>rate</code> and <code>created_time</code>'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-projects-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    put:
      tags:
      - projects
      operationId: update_projects_using_custom_field
      summary: Update a project using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        a project by providing its API name in the X-Unique-Identifier-Key header
        and its value in the X-Unique-Identifier-Value header. Based on this value,
        the corresponding project will be retrieved and updated. Additionally, there
        is an optional X-Upsert header. If the X-Upsert header is true and the custom
        field's unique value is not found in any of the existing projects, a new project
        will be created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-project-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}:
    x-mcp-group:
    - Projects
    put:
      tags:
      - projects
      operationId: update_project
      summary: Update project
      description: Update details of a project.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-project-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.UPDATE
    get:
      tags:
      - projects
      operationId: get_project
      summary: Get a project
      description: Get the details of a project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    delete:
      tags:
      - projects
      operationId: delete_project
      summary: Delete project
      description: Deleting a existing project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.DELETE
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/active:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: mark_project_active
      summary: Activate project
      description: Mark project as active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/activate-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/inactive:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: mark_project_inactive
      summary: Inactivate a project
      description: Marking a project as inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/inactivate-a-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/clone:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: clone_project
      summary: Clone project
      description: Cloning a project.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/clone-project-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/clone-project-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/users:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: add_project_user
      summary: Assign users
      description: Assign a users to a project.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/assign-users-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/assign-users-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    get:
      tags:
      - projects
      operationId: list_project_users
      summary: List Users
      description: Get list of users associated with a project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-users-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    parameters:
    - name: project_id
      in: path
      required: true
      description: Unique identifier of the project.
      schema:
        type: string
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/users/invite:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: invite_project_user
      summary: Invite user
      description: Invite and user to the project.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/invite-user-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invite-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    parameters:
    - name: project_id
      in: path
      required: true
      description: Unique identifier of the project.
      schema:
        type: string
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/users/{user_id}:
    x-mcp-group:
    - Projects
    put:
      tags:
      - projects
      operationId: update_project_user
      summary: Update user
      description: Update details of a user.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-user-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.UPDATE
    get:
      tags:
      - projects
      operationId: get_project_user
      summary: Get a Project User
      description: Get details of a user in project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    delete:
      tags:
      - projects
      operationId: delete_project_user
      summary: Delete user
      description: Remove user from a project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.DELETE
    parameters:
    - name: project_id
      in: path
      required: true
      description: Unique identifier of the project.
      schema:
        type: string
      example: '460000000044019'
    - name: user_id
      in: path
      required: true
      description: Unique identifier of the user.
      schema:
        type: string
      example: '460000000024003'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/comments:
    x-mcp-group:
    - Projects
    post:
      tags:
      - projects
      operationId: add_project_comment
      summary: Post comment
      description: Post comment to a project.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/post-comment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/post-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    get:
      tags:
      - projects
      operationId: list_project_comments
      summary: List comments
      description: Get comments for a project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-comments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/comments/{comment_id}:
    x-mcp-group:
    - Projects
    delete:
      tags:
      - projects
      operationId: delete_project_comment
      summary: Delete comment
      description: Deleting a comment.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.DELETE
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - name: comment_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the comment.
      example: '460000000044027'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/invoices:
    x-mcp-group:
    - Projects
    get:
      tags:
      - projects
      operationId: list_project_invoices
      summary: List invoices
      description: Lists invoices created for this project.
      parameters:
      - name: sort_column
        in: query
        description: 'Sort invoices raised. Allowed Values: <code>invoice_number</code>,
          <code>date</code>, <code>total</code>, <code>balance</code> and <code>created_time</code>'
        required: false
        schema:
          type: string
        example: created_time
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-invoices-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '460000000044019'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/tasks:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: add_project_task
      summary: Add a project task
      description: The project task has been added.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-a-task-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    get:
      tags:
      - tasks
      operationId: list_project_tasks
      summary: List project tasks
      description: Get list of tasks added to a project.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-tasks-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /projects/{project_id}/tasks/{task_id}:
    x-mcp-group:
    - Tasks
    put:
      tags:
      - tasks
      operationId: update_project_task
      summary: Update a project task
      description: Update the details of a project task.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-task-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.UPDATE
    get:
      tags:
      - tasks
      operationId: get_project_task
      summary: Get a project task
      description: Get the details of a project task.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    delete:
      tags:
      - tasks
      operationId: delete_project_task
      summary: Delete project Task
      description: Delete a task added to a project.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.DELETE
    parameters:
    - name: project_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the project.
      example: '90300000072369'
    - name: task_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the task.
      example: '90300000072369'
    - $ref: '#/components/parameters/organization_id'
  /autocomplete/projects:
    x-mcp-group:
    - Projects
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /tasks:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: add_task
      summary: Add a task
      description: Add a task.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-a-task-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - tasks
      operationId: list_tasks
      summary: List tasks
      description: List a task.
      parameters:
      - name: entity_id
        in: query
        description: Search tasks by entity ID.
        required: false
        schema:
          type: string
        example: 982000000567114
      - name: sub_entity_id
        in: query
        description: Search tasks by sub entity ID.
        required: false
        schema:
          type: string
        example: 982000000567115
      - name: owner_id
        in: query
        description: Search tasks by owner ID.
        required: false
        schema:
          type: string
        example: 982000000567116
      - name: contact_id
        in: query
        description: Search tasks by contact ID.
        required: false
        schema:
          type: string
        example: 982000000567117
      - name: client_id
        in: query
        description: Search tasks by client ID.
        required: false
        schema:
          type: string
        example: 982000000567118
      - name: status
        in: query
        description: Search tasks by status. Allowed values are <code>open</code>,
          <code>ongoing</code>, <code>yet_to_started</code>, <code>in_progress</code>,
          <code>completed</code>, <code>deferred</code>, <code>waiting_for_input</code>
          and <code>any custom status</code>
        required: false
        schema:
          type: string
        example: open
      - name: parent_status
        in: query
        description: Search tasks by parent status. Allowed formats are <code>parent_status</code>,
          <code>parent_status_equal</code> and <code>parent_status_not_equal</code>.
          Allowed values are <code>open</code>, <code>ongoing</code> and <code>completed</code>
        required: false
        schema:
          type: string
        example: open
      - name: search_text
        in: query
        description: Search tasks based on a search text.
        required: false
        schema:
          type: string
        example: sample
      - name: priority
        in: query
        description: Search tasks by priority of the task. Allowed values are <code>normal</code>,
          <code>low</code>, <code>high</code>, <code>lowest</code> and <code>highest</code>.
        required: false
        schema:
          type: string
        example: high
      - name: entity_type
        in: query
        description: Search tasks by entity type.
        required: false
        schema:
          type: string
        example: invoice
      - name: title
        in: query
        description: Search tasks by their title.
        required: false
        schema:
          type: string
        example: Sample Task
      - name: task_description
        in: query
        description: Search tasks their description.
        required: false
        schema:
          type: string
        example: Sample Task Description
      - name: is_overdue
        in: query
        description: Filter overdue tasks.
        required: false
        schema:
          type: string
        example: 'true'
      - name: created_by
        in: query
        description: Search tasks by the person who created them.
        required: false
        schema:
          type: string
        example: 982000000567118
      - name: is_accountant_task
        in: query
        description: Filter accountant tasks.
        required: false
        schema:
          type: string
        example: 'false'
      - name: contact_ids
        in: query
        description: Search tasks by contact IDs separated by comma.
        required: false
        schema:
          type: string
        example: 982000000567118,982000000567119,982000000567120
      - name: client_ids
        in: query
        description: Search tasks by client IDs separated by comma.
        required: false
        schema:
          type: string
        example: 982000000567118,982000000567119,982000000567120
      - name: owner_ids
        in: query
        description: Search tasks by owner IDs separated by comma.
        required: false
        schema:
          type: string
        example: 982000000567118,982000000567119,982000000567120
      - name: date_start
        in: query
        description: Search tasks by start date.
        required: false
        schema:
          type: string
        example: '2025-01-01'
      - name: date_end
        in: query
        description: Search tasks by end date.
        required: false
        schema:
          type: string
        example: '2025-12-31'
      - name: task_number
        in: query
        description: Search tasks by task number.
        required: false
        schema:
          type: string
        example: T-001
      - name: sort_column
        in: query
        description: Sort tasks. Allowed values are <code>owner_name</code>, <code>contact_name</code>,
          <code>client_name</code>, <code>due_date</code>, <code>title</code>, <code>created_time</code>,
          <code>completion_percentage</code> and <code>task_number</code>
        required: false
        schema:
          type: string
        example: owner_name
      - name: filter_by
        in: query
        description: 'Filter tasks by status. Allowed values are <code>TaskStatus.All</code>,
          <code>TaskStatus.Overdue</code>, <code>TaskStatus.InProgress</code>, <code>TaskStatus.Completed</code>,
          <code>TaskStatus.YetToStarted</code>, <code>TaskStatus.MyPending</code>,
          <code>TaskStatus.DueToday</code>, <code>TaskStatus.PriorityOverdue</code>
          and <code>TaskStatus.Unassigned</code> '
        required: false
        schema:
          type: string
        example: open
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-tasks-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - tasks
      operationId: update_tasks
      summary: Update tasks
      description: Update tasks.
      parameters:
      - name: bulk_update
        in: query
        description: Indicate whether the request is for bulk update. Allowed values
          are <code>true</code> and <code>false</code>.
        required: true
        schema:
          type: boolean
        example: 'true'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-tasks-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-tasks-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - tasks
      operationId: delete_tasks
      summary: Delete tasks
      description: Delete tasks.
      parameters:
      - name: task_ids
        in: query
        description: Comma separated list of task IDs to be deleted.
        required: true
        schema:
          type: string
        example: 982000000567114,982000000567115,982000000567116
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-tasks-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}:
    x-mcp-group:
    - Tasks
    get:
      tags:
      - tasks
      operationId: get_task
      summary: Get a task
      description: Get a task.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - tasks
      operationId: update_a_task
      summary: Update a tasks
      description: Update a tasks.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-task-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - tasks
      operationId: delete_task
      summary: Delete a tasks
      description: Delete a tasks.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/percentage:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: update_percentage_task
      summary: Update completed percentage
      description: Update completed percentage of a task.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-percentage-task-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-percentage-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/markasopen:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: mark_task_as_open
      summary: Mark task as open
      description: Mark a task as open.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-open-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/markasongoing:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: mark_task_as_ongoing
      summary: Mark task as ongoing
      description: Mark a task as ongoing.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-ongoing-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/markascompleted:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: mark_task_as_completed
      summary: Mark task as completed
      description: Mark a task as completed.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-completed-task-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/comments:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: add_task_comment
      summary: Add comment to task
      description: Add comment to a task.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/add-task-comment-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-task-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - tasks
      operationId: list_task_comments
      summary: List comments of task
      description: List comments of a task.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-task-comments-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/comments/{comment_id}:
    x-mcp-group:
    - Tasks
    delete:
      tags:
      - tasks
      operationId: delete_task_comment
      summary: Delete comment of task
      description: Delete a comment of a task.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-task-comment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - name: comment_id
      in: path
      description: Unique identifier of the comment associated with the task.
      required: true
      schema:
        type: string
      example: 982000000567116
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/attachment:
    x-mcp-group:
    - Tasks
    post:
      tags:
      - tasks
      operationId: add_task_attachment
      summary: Add attachment to task
      description: Add attachment to a task.
      parameters:
      - name: attachment
        in: query
        description: 'The file to be attached.Allowed Extensions: <code>gif</code>,
          <code>png</code>, <code>jpeg</code>, <code>jpg</code>, <code>bmp</code>,
          <code>pdf</code>, <code>xls</code>, <code>xlsx</code>, <code>doc</code>,
          <code>docx</code>, <code>xml</code>, <code>csv</code>, <code>txt</code>,
          <code>tif</code> and <code>tiff</code>'
        required: false
        schema:
          type: string
          format: binary
        example: ''
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/add-task-attachment-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - $ref: '#/components/parameters/organization_id'
  /tasks/{task_id}/documents/{document_id}:
    x-mcp-group:
    - Tasks
    get:
      tags:
      - tasks
      operationId: get_task_document
      summary: Get document of task
      description: Get a document of a task.
      parameters:
      - name: image_size
        in: query
        description: Size of the image to be fetched. Allowed values are <code>xlarge</code>,
          <code>large</code>, <code>small</code> and <code>tiny</code>.
        required: false
        schema:
          type: string
        example: large
      - name: inline
        in: query
        description: Indicate whether the document should be displayed inline. Allowed
          values are <code>true</code> and <code>false</code>.
        required: false
        schema:
          type: boolean
        example: 'false'
      - name: print
        in: query
        description: Indicates whether the document should be printed. Allowed values
          are <code>true</code> and <code>false</code>.
        required: false
        schema:
          type: boolean
        example: 'false'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-task-document-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - tasks
      operationId: delete_task_document
      summary: Delete document of task
      description: Delete a document of a task.
      parameters:
      - name: un_associate
        in: query
        description: Indicates whether the document should be unassociated from the
          task. Allowed values are <code>true</code> and <code>false</code>.
        required: false
        schema:
          type: boolean
        example: 'false'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-task-document-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: task_id
      in: path
      description: Unique identifier of the task.
      required: true
      schema:
        type: string
      example: 982000000567114
    - name: document_id
      in: path
      description: Unique identifier of the document attached to the task.
      required: true
      schema:
        type: string
      example: 982000000567118
    - $ref: '#/components/parameters/organization_id'
  /projects/timeentries:
    x-mcp-group:
    - Time Entries
    post:
      tags:
      - time-entries
      operationId: create_time_entries
      summary: Log time entries
      description: Logging time entries.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/log-time-entries-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/log-time-entries-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    get:
      tags:
      - time-entries
      operationId: list_time_entries
      summary: List time entries.
      description: List all time entries with pagination.
      parameters:
      - name: from_date
        in: query
        description: Date from which the time entries logged to be fetched
        required: false
        schema:
          type: string
        example: '2013-09-17'
      - name: to_date
        in: query
        description: Date up to which the time entries logged to be fetched
        required: false
        schema:
          type: string
        example: '2013-09-17'
      - name: filter_by
        in: query
        description: 'Filter time entries by date and status. Allowed Values: <code>Date.All</code>,
          <code>Date.Today</code>, <code>Date.ThisWeek</code>, <code>Date.ThisMonth</code>,
          <code>Date.ThisQuarter</code>, <code>Date.ThisYear</code>, <code>Date.PreviousDay</code>,
          <code>Date.PreviousWeek</code>, <code>Date.PreviousMonth</code>, <code>Date.PreviousQuarter</code>,
          <code>Date.PreviousYear</code>, <code>Date.CustomDate</code>, <code>Status.Unbilled</code>
          and <code>Status.Invoiced</code>'
        required: false
        schema:
          type: string
        example: Status.Unbilled
      - name: project_id
        in: query
        description: Search time entries by project_id.
        required: false
        schema:
          type: string
        example: '460000000044019'
      - name: user_id
        in: query
        description: Search time entries by user_id.
        required: false
        schema:
          type: string
        example: '460000000024003'
      - name: sort_column
        in: query
        description: 'Sort time entries. Allowed Values: <code>project_name</code>,
          <code>task_name</code>, <code>user_name</code>, <code>log_date</code>, <code>timer_started_at</code>
          and <code>customer_name</code>'
        required: false
        schema:
          type: string
        example: customer_name
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-time-entries-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    delete:
      tags:
      - time-entries
      operationId: delete_time_entries
      summary: Delete time entries
      description: Deleting time entries.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-time-entries-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.DELETE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /projects/timeentries/{time_entry_id}:
    x-mcp-group:
    - Time Entries
    put:
      tags:
      - time-entries
      operationId: update_time_entry
      summary: Update time entry
      description: Update logged time entry.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-time-entry-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-time-entry-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.UPDATE
    get:
      tags:
      - time-entries
      operationId: get_time_entry
      summary: Get a time entry
      description: Get details of a time entry.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-time-entry-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    delete:
      tags:
      - time-entries
      operationId: delete_time_entry
      summary: Delete time entry
      description: Deleting a logged time entry.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-time-entry-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.DELETE
    parameters:
    - name: time_entry_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the time entry.
      example: '460000000044021'
    - $ref: '#/components/parameters/organization_id'
  /projects/timeentries/{time_entry_id}/timer/start:
    x-mcp-group:
    - Time Entries
    post:
      tags:
      - time-entries
      operationId: start_entry_timer
      summary: Start timer
      description: Start tracking time spent.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/start-timer-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    parameters:
    - name: time_entry_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the time entry.
      example: '460000000044021'
    - $ref: '#/components/parameters/organization_id'
  /projects/timeentries/timer/stop:
    x-mcp-group:
    - Time Entries
    post:
      tags:
      - time-entries
      operationId: stop_entry_timer
      summary: Stop timer
      description: Stop tracking time, say taking a break or leaving.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/stop-timer-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /projects/timeentries/runningtimer/me:
    x-mcp-group:
    - Time Entries
    get:
      tags:
      - time-entries
      operationId: get_running_timer
      summary: Get timer
      description: Get current running timer.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-timer-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.projects.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /items:
    x-mcp-group:
    - Items
    post:
      tags:
      - items
      operationId: create_item
      summary: Create an Item
      description: Create a new item.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-item-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-item-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - items
      operationId: list_items
      summary: List items
      description: Get the list of all active items with pagination.
      parameters:
      - name: name
        in: query
        description: 'Search items by name. Max-length [100]. Variants: <code>name_startswith</code>
          and <code>name_contains</code>'
        required: false
        schema:
          type: string
        example: Hard Drive
      - name: name_startswith
        in: query
        description: Search items whose name starts with the specified value. Maximum
          length allowed is 100 characters.
        required: false
        schema:
          type: string
        example: Hard Drive
      - name: name_contains
        in: query
        description: Search items whose name contains the specified value. Maximum
          length allowed is 100 characters.
        required: false
        schema:
          type: string
        example: Hard Drive
      - name: description
        in: query
        description: 'Search items by description. Max-length [100]. Variants: <code>description_startswith</code>
          and <code>description_contains</code>'
        required: false
        schema:
          type: string
        example: 500GB
      - name: description_startswith
        in: query
        description: Search items whose description starts with the specified value.
          Maximum length allowed is 100 characters.
        required: false
        schema:
          type: string
        example: 500GB
      - name: description_contains
        in: query
        description: Search items whose description contains the specified value.
          Maximum length allowed is 100 characters.
        required: false
        schema:
          type: string
        example: 500GB
      - name: rate
        in: query
        description: 'Search items by rate. Variants: <code>rate_less_than</code>,
          <code>rate_less_equals</code>, <code>rate_greater_than</code> and <code>rate_greater_equals</code>'
        required: false
        schema:
          type: string
        example: 120
      - name: rate_less_than
        in: query
        description: Search items whose rate is less than the specified value.
        required: false
        schema:
          type: string
        example: 120
      - name: rate_less_equals
        in: query
        description: Search items whose rate is less than or equal to the specified
          value.
        required: false
        schema:
          type: string
        example: 120
      - name: rate_greater_than
        in: query
        description: Search items whose rate is greater than the specified value.
        required: false
        schema:
          type: string
        example: 120
      - name: rate_greater_equals
        in: query
        description: Search items whose rate is greater than or equal to the specified
          value.
        required: false
        schema:
          type: string
        example: 120
      - name: tax_id
        in: query
        description: 'Search items by tax id. Note: This value is a set of numeric
          characters.'
        required: false
        schema:
          type: string
        example: 982000000037049
      - name: tax_name
        in: query
        description: Search items by tax name. Max-length [100]
        required: false
        schema:
          type: string
        example: Sales Tax
      - name: is_taxable
        in: query
        description: Boolean to track the taxability of the item.
        required: false
        schema:
          type: boolean
        x-node_available_in:
        - in
        - us
        - mx
        x-node_unavailable_in: []
      - name: tax_exemption_id
        in: query
        description: 'ID of the tax exemption. Mandatory, if <code>is_taxable</code>
          is false. Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        x-node_available_in:
        - in
        - us
        - mx
        x-node_unavailable_in: []
        example: ''
      - name: account_id
        in: query
        description: 'ID of the account to which the item has to be associated with.
          Note: This value is a set of numeric characters.'
        required: false
        schema:
          type: string
        example: ''
      - name: filter_by
        in: query
        description: 'Filter items by status. Allowed Values: <code>Status.All</code>,
          <code>Status.Active</code> and <code>Status.Inactive</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: search_text
        in: query
        description: Search items by name or description. Max-length [100]
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort items. Allowed Values: <code>name</code>, <code>rate</code>
          and <code>tax_name</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: sat_item_key_code
        in: query
        description: SAT Item key code
        required: false
        schema:
          type: string
        x-node_available_in:
        - mx
        x-node_unavailable_in: []
        example: ''
      - name: unitkey_code
        in: query
        description: SAT Unit code
        required: false
        schema:
          type: string
        x-node_available_in:
        - mx
        x-node_unavailable_in: []
        example: ''
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-items-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - items
      operationId: update_item_using_custom_field
      summary: Update an item using a custom field's unique value
      description: A custom field will have unique values if it's configured to not
        accept duplicate values. Now, you can use that custom field's value to update
        an item by providing its API name in the X-Unique-Identifier-Key header and
        its value in the X-Unique-Identifier-Value header. Based on this value, the
        corresponding item will be retrieved and updated. Additionally, there is an
        optional X-Upsert header. If the X-Upsert header is true and the custom field's
        unique value is not found in any of the existing items, a new item will be
        created if the necessary payload details are available
      parameters:
      - name: X-Unique-Identifier-Key
        in: header
        description: Unique CustomField Api Name
        required: true
        schema:
          type: string
          example: cf_unique_cf
      - name: X-Unique-Identifier-Value
        in: header
        description: Unique CustomField Value
        required: true
        schema:
          type: string
          example: unique Value
      - name: X-Upsert
        in: header
        description: If there is no record is found unique custom field value , will
          create new invoice
        required: false
        schema:
          type: boolean
          example: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-item-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-item-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /itemdetails:
    x-mcp-group:
    - Items
    get:
      tags:
      - items
      operationId: list_item_details
      summary: Bulk fetch item details
      description: Fetch item details for the mentioned item IDs
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bulk-fetch-item-details-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - name: item_ids
      in: query
      required: true
      description: '<code>List of item ids separated by comma</code>. Note: This value
        is a set of numeric characters separated by comma.'
      schema:
        type: string
      example: 4815000000044208,4815000000044274,4815000000044340
    - $ref: '#/components/parameters/organization_id'
  /items/{item_id}:
    x-mcp-group:
    - Items
    put:
      tags:
      - items
      operationId: update_item
      summary: Update an item
      description: Update the details of an item.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-item-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-item-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - items
      operationId: get_item
      summary: Get an item
      description: Details of an existing item.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-item-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - items
      operationId: delete_item
      summary: Delete an item
      description: Delete the item created.items that are part of transaction cannot
        be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-item-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: item_id
      in: path
      required: true
      description: 'Unique identifier of the item. Note: This value is a set of numeric
        characters.'
      schema:
        type: string
      example: 45667789900
    - $ref: '#/components/parameters/organization_id'
  /item/{item_id}/customfields:
    x-mcp-group:
    - Items
    put:
      tags:
      - items
      operationId: update_custom_fields_in_item
      summary: Update custom field in existing items
      description: Update the value of the custom field in existing items.
      parameters:
      - name: item_id
        in: path
        required: true
        description: 'Unique identifier of the item. Note: This value is a set of
          numeric characters.'
        schema:
          type: string
        example: 45667789900
      - $ref: '#/components/parameters/organization_id'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/custom_fields'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-item-customfield-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
  /items/{item_id}/active:
    x-mcp-group:
    - Items
    post:
      tags:
      - items
      operationId: mark_item_active
      summary: Mark as active
      description: Mark an inactive item as active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-active-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: item_id
      in: path
      required: true
      description: 'Unique identifier of the item. Note: This value is a set of numeric
        characters.'
      schema:
        type: string
      example: 45667789900
    - $ref: '#/components/parameters/organization_id'
  /items/{item_id}/inactive:
    x-mcp-group:
    - Items
    post:
      tags:
      - items
      operationId: mark_item_inactive
      summary: Mark as inactive
      description: Mark an active item as inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-inactive-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: item_id
      in: path
      required: true
      description: 'Unique identifier of the item. Note: This value is a set of numeric
        characters.'
      schema:
        type: string
      example: 45667789900
    - $ref: '#/components/parameters/organization_id'
  /users:
    x-mcp-group:
    - Users
    post:
      tags:
      - users
      operationId: create_user
      summary: Create a user
      description: Create a user for your organization.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-user-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - users
      operationId: list_users
      summary: List Users
      description: Get the list of all users in the organization.
      parameters:
      - name: filter_by
        in: query
        description: 'Allowed Values: <code>Status.All, Status.Active, Status.Inactive,
          Status.Invited and Status.Deleted</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: sort_column
        in: query
        description: 'Sort users. Allowed Values: <code>name, email, user_role and
          status</code>'
        required: false
        schema:
          type: string
        example: name
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-users-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /users/{user_id}:
    x-mcp-group:
    - Users
    put:
      tags:
      - users
      operationId: update_user
      summary: Update a user
      description: Update the details of a user.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-user-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - users
      operationId: get_user
      summary: Get an user details
      description: Get the details of a user.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - users
      operationId: delete_user
      summary: Delete a user
      description: Delete a user associated to the organization.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: user_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the user. Note: This value is a set of numeric
        characters.'
      example: '982000000554041'
    - $ref: '#/components/parameters/organization_id'
  /users/me:
    x-mcp-group:
    - Users
    get:
      tags:
      - users
      operationId: get_current_user
      summary: Get current user
      description: Get the details of the current user.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-current-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /users/{user_id}/invite:
    x-mcp-group:
    - Users
    post:
      tags:
      - users
      operationId: invite_user
      summary: Invite a user
      description: Send invitation email to a user.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invite-a-user-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: user_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the user. Note: This value is a set of numeric
        characters.'
      example: '982000000554041'
    - $ref: '#/components/parameters/organization_id'
  /users/{user_id}/active:
    x-mcp-group:
    - Users
    post:
      tags:
      - users
      operationId: mark_user_active
      summary: Mark user as active
      description: Mark an inactive user as active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-user-as-active-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: user_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the user. Note: This value is a set of numeric
        characters.'
      example: '982000000554041'
    - $ref: '#/components/parameters/organization_id'
  /users/{user_id}/inactive:
    x-mcp-group:
    - Users
    post:
      tags:
      - users
      operationId: mark_user_inactive
      summary: Mark user as inactive
      description: Mark an active user as inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-user-as-inactive-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: user_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the user. Note: This value is a set of numeric
        characters.'
      example: '982000000554041'
    - $ref: '#/components/parameters/organization_id'
  /pricebooks:
    x-mcp-group:
    - Price Lists
    post:
      tags:
      - pricelists
      operationId: create_pricebook
      summary: Create a pricebook
      description: create a new pricebook.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-pricebook-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-pricebook-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - pricelists
      operationId: list_pricebooks
      summary: List all pricebooks
      description: List all the available pricebooks in your zoho books organization.
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page. Default value is 200.
        required: false
        schema:
          type: integer
          default: 200
        example: 200
      - name: filter_by
        in: query
        description: 'Filter by sales or purchase type. Allowed values: SalesOrPurchaseType.All,
          SalesOrPurchaseType.Sales, SalesOrPurchaseType.Purchases.'
        required: false
        schema:
          type: string
      - name: search_text
        in: query
        description: Text to search in the price list (price book name).
        required: false
        schema:
          type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-all-pricebooks-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /pricebooks/{pricebook_id}:
    x-mcp-group:
    - Price Lists
    put:
      tags:
      - pricelists
      operationId: update_pricebook
      summary: Update pricebook
      description: update existing pricebook.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-pricebook-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-pricebook-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - pricelists
      operationId: delete_pricebook
      summary: Delete the pricebook
      description: Delete the pricebook.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-the-pricebook-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: pricebook_id
      in: path
      required: true
      description: Unique identifier of the pricebook.
      schema:
        type: string
      example: 130426000002924000
    - $ref: '#/components/parameters/organization_id'
  /pricebooks/{pricebook_id}/active:
    x-mcp-group:
    - Price Lists
    post:
      tags:
      - pricelists
      operationId: mark_pricebook_active
      summary: Mark as Active
      description: Mark the pricebook as Active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-active-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: pricebook_id
      in: path
      required: true
      description: Unique identifier of the pricebook.
      schema:
        type: string
      example: 130426000002924000
    - $ref: '#/components/parameters/organization_id'
  /pricebooks/{pricebook_id}/inactive:
    x-mcp-group:
    - Price Lists
    post:
      tags:
      - pricelists
      operationId: mark_pricebook_inactive
      summary: Mark as Inactive
      description: Mark the pricebook as Inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-inactive-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: pricebook_id
      in: path
      required: true
      description: Unique identifier of the pricebook.
      schema:
        type: string
      example: 130426000002924000
    - $ref: '#/components/parameters/organization_id'
  /settings/locations/enable:
    x-mcp-group:
    - Locations
    post:
      tags:
      - locations
      operationId: enable_locations
      summary: Enable Locations
      description: Enable Locations for an organisation.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/enable-location-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /locations:
    x-mcp-group:
    - Locations
    post:
      tags:
      - locations
      operationId: create_location
      summary: Create a location
      description: Create a location.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-location-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-location-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - locations
      operationId: list_locations
      summary: List all locations
      description: List all the available locations in your zoho inventory.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-all-location-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /locations/{location_id}:
    x-mcp-group:
    - Locations
    put:
      tags:
      - locations
      operationId: update_location
      summary: Update location
      description: Update location
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-location-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-location-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - locations
      operationId: delete_location
      summary: Delete a location
      description: Delete a location.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-location-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: location_id
      in: path
      required: true
      description: Unique identifier of the location.
      schema:
        type: string
      example: 130426000000664020
    - $ref: '#/components/parameters/organization_id'
  /locations/{location_id}/active:
    x-mcp-group:
    - Locations
    post:
      tags:
      - locations
      operationId: mark_location_active
      summary: Mark as Active
      description: Mark location as Active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-active-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: location_id
      in: path
      required: true
      description: Unique identifier of the location.
      schema:
        type: string
      example: 130426000000664020
    - $ref: '#/components/parameters/organization_id'
  /locations/{location_id}/inactive:
    x-mcp-group:
    - Locations
    post:
      tags:
      - locations
      operationId: mark_location_inactive
      summary: Mark as Inactive
      description: Mark location as Inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-inactive-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: location_id
      in: path
      required: true
      description: Unique identifier of the location.
      schema:
        type: string
      example: 130426000000664020
    - $ref: '#/components/parameters/organization_id'
  /locations/{location_id}/markasprimary:
    x-mcp-group:
    - Locations
    post:
      tags:
      - locations
      operationId: mark_location_primary
      summary: Mark as Primary
      description: Mark location as primary.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-as-primary-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: location_id
      in: path
      required: true
      description: Unique identifier of the location.
      schema:
        type: string
      example: 130426000000664020
    - $ref: '#/components/parameters/organization_id'
  /settings/currencies:
    x-mcp-group:
    - Currency
    post:
      tags:
      - currency
      operationId: create_currency
      summary: Create a Currency
      description: Create a currency for transaction.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-currency-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-currency-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - currency
      operationId: list_currencies
      summary: List Currencies
      description: Get list of currencies configured.
      parameters:
      - name: filter_by
        in: query
        description: 'Filter currencies excluding base currency. Allowed Values: <code>Currencies.ExcludeBaseCurrency</code>'
        required: false
        schema:
          type: string
        example: ''
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-currencies-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /settings/currencies/{currency_id}:
    x-mcp-group:
    - Currency
    put:
      tags:
      - currency
      operationId: update_currency
      summary: Update a Currency
      description: Update the details of a currency.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-currency-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-currency-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - currency
      operationId: get_currency
      summary: Get a Currency
      description: Get the details of a currency.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-currency-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - currency
      operationId: delete_currency
      summary: Delete a currency
      description: Delete a currency. Currency that is associated to transactions
        cannot be deleted.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-currency-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: currency_id
      in: path
      schema:
        type: string
      required: true
      description: Unique identifier of the currency.
      example: '982000000004012'
    - $ref: '#/components/parameters/organization_id'
  /settings/currencies/{currency_id}/exchangerates:
    x-mcp-group:
    - Currency
    get:
      tags:
      - currency
      operationId: list_exchange_rates
      summary: List exchange rates
      description: List of exchange rates configured for the currency.
      parameters:
      - name: from_date
        in: query
        description: Returns the exchange rate details from the given date or from
          previous closest match in the absence of the exchange rate on the given
          date.
        required: false
        schema:
          type: string
        example: '2017-01-02'
      - name: is_current_date
        in: query
        description: To return the exchange rate only if available for current date.
        required: false
        schema:
          type: boolean
        example: true
      - name: sort_column
        in: query
        description: 'Sorts the exchange rate according to this column. Allowed Values
          : <code>effective_date</code>'
        required: false
        schema:
          type: string
        example: ''
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-exchange-rates-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    post:
      tags:
      - currency
      operationId: create_exchange_rate
      summary: Create an exchange rate
      description: Create an exchange rate for the specified currency.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-an-exchange-rate-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-an-exchange-rate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: currency_id
      in: path
      required: true
      description: Unique identifier of the currency.
      schema:
        type: string
      example: '982000000004012'
    - $ref: '#/components/parameters/organization_id'
  /settings/currencies/{currency_id}/exchangerates/{exchange_rate_id}:
    x-mcp-group:
    - Currency
    get:
      tags:
      - currency
      operationId: get_exchange_rate
      summary: Get an exchange rate.
      description: Get the details of an exchange rate that has been asscoiated to
        the currency.
      parameters:
      - name: exchange_rate_id
        in: path
        required: true
        description: Unique identifier of the exchange rate.
        schema:
          type: string
        example: '460000000038035'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-an-exchange-rate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - currency
      operationId: update_exchange_rate
      summary: Update an exchange rate
      description: Update the details of exchange rate for a currency.
      parameters:
      - name: exchange_rate_id
        in: path
        required: true
        description: Unique identifier of the exchange rate.
        schema:
          type: string
        example: '460000000038035'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-an-exchange-rate-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-an-exchange-rate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - currency
      operationId: delete_exchange_rate
      summary: Delete an exchage rate
      description: Delete an exchange rate for the specified currency.
      parameters:
      - name: exchange_rate_id
        in: path
        required: true
        description: Unique identifier of the exchange rate.
        schema:
          type: string
        example: '460000000038035'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-an-exchage-rate-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: currency_id
      in: path
      required: true
      description: Unique identifier of the currency.
      schema:
        type: string
      example: '982000000004012'
    - $ref: '#/components/parameters/organization_id'
  /settings/taxes:
    x-mcp-group:
    - Taxes
    post:
      tags:
      - taxes
      operationId: create_tax
      summary: Create a tax
      description: 'Create a tax which can be associated with an item. Note: You have
        to enable Sales Tax in order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-tax-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-tax-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - taxes
      operationId: list_taxes
      summary: List taxes
      description: 'List of simple and compound taxes with pagination. Note: You have
        to enable Sales Tax in order to perform tax related operations.'
      parameters:
      - name: page
        in: query
        description: Page number to be fetched. Default value is 1.
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: per_page
        in: query
        description: Number of records to be fetched per page.
        required: false
        schema:
          type: integer
          default: 10
        example: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-taxes-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /settings/taxes/{tax_id}:
    x-mcp-group:
    - Taxes
    put:
      tags:
      - taxes
      operationId: update_tax
      summary: Update a tax
      description: 'Update the details of a simple or compound tax. Note: You have
        to enable Sales Tax in order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-tax-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-tax-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - taxes
      operationId: get_tax
      summary: Get a tax
      description: 'Get the details of a simple or compound tax. Note: You have to
        enable Sales Tax in order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-tax-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - taxes
      operationId: delete_tax
      summary: Delete a tax
      description: 'Delete a simple or compound tax. Note: You have to enable Sales
        Tax in order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-tax-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: tax_id
      in: path
      required: true
      description: 'Unique identifier of the tax. Note: This value is a set of numeric
        characters.'
      schema:
        type: string
      example: '982000000566009'
    - $ref: '#/components/parameters/organization_id'
  /settings/taxgroups/{tax_group_id}:
    x-mcp-group:
    - Taxes
    get:
      tags:
      - taxes
      operationId: get_tax_group
      summary: Get a tax group
      description: 'Get the details of a tax group. Note: You have to enable Sales
        Tax in order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-tax-group-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - taxes
      operationId: update_tax_group
      summary: Update a tax group
      description: 'Update the details of the tax group. Note: You have to enable
        Sales Tax in order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-tax-group-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-tax-group-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - taxes
      operationId: delete_tax_group
      summary: Delete a tax group
      description: 'Delete a tax group. Tax group that is associated to transactions
        cannot be deleted. Note: You have to enable Sales Tax in order to perform
        tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-tax-group-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: tax_group_id
      in: path
      schema:
        type: string
      required: true
      description: 'Unique identifier of the tax group. Note: This value is a set
        of numeric characters.'
      example: '982000000566009'
    - $ref: '#/components/parameters/organization_id'
  /settings/taxgroups:
    x-mcp-group:
    - Taxes
    post:
      tags:
      - taxes
      operationId: create_tax_group
      summary: Create a tax group
      description: 'Create a tax group associating multiple taxes. Note: You have
        to enable Sales Tax in order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-tax-group-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-tax-group-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /settings/taxauthorities:
    x-mcp-group:
    - Taxes
    post:
      tags:
      - taxes
      operationId: create_tax_authority
      summary: Create a tax authority [US and CA Edition only]
      description: 'Create a tax authority. Note: You have to enable Sales Tax in
        order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-tax-authority-us-and-ca-edition-only-request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-tax-authority-us-and-ca-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - taxes
      operationId: list_tax_authorities
      summary: List tax authorities [US Edition only]
      description: 'List of tax authorities. Note: You have to enable Sales Tax in
        order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-tax-authorities-us-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /settings/taxauthorities/{tax_authority_id}:
    x-mcp-group:
    - Taxes
    put:
      tags:
      - taxes
      operationId: update_tax_authority
      summary: Update a tax authority [US and CA Edition only]
      description: 'Update the details of a tax authority. Note: You have to enable
        Sales Tax in order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-tax-authority-us-and-ca-edition-only-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-tax-authority-us-and-ca-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - taxes
      operationId: get_tax_authority
      summary: Get a tax authority [US and CA Edition only]
      description: 'Get the details of a tax authority. Note: You have to enable Sales
        Tax in order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-tax-authority-us-and-ca-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - taxes
      operationId: delete_tax_authority
      summary: Delete a tax authority [US and CA Edition only]
      description: 'Delete a tax authority. Note: You have to enable Sales Tax in
        order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-tax-authority-us-and-ca-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: tax_authority_id
      in: path
      required: true
      description: 'Unique identifier of the tax authority. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000066001'
    - $ref: '#/components/parameters/organization_id'
  /settings/taxexemptions:
    x-mcp-group:
    - Taxes
    post:
      tags:
      - taxes
      operationId: create_tax_exemption
      summary: Create a tax exemption [US Edition only]
      description: 'Create a tax exemption. Note: You have to enable Sales Tax in
        order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-a-tax-exemption-us-edition-only-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-a-tax-exemption-us-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    get:
      tags:
      - taxes
      operationId: list_tax_exemptions
      summary: List tax exemptions [US Edition only]
      description: 'List of tax exemptions. Note: You have to enable Sales Tax in
        order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/list-tax-exemptions-us-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /settings/taxexemptions/{tax_exemption_id}:
    x-mcp-group:
    - Taxes
    put:
      tags:
      - taxes
      operationId: update_tax_exemption
      summary: Update a tax exemption [US Edition only]
      description: 'Update the details of a tax exemption. Note: You have to enable
        Sales Tax in order to perform tax related operations.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-a-tax-exemption-us-edition-only-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-a-tax-exemption-us-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - taxes
      operationId: get_tax_exemption
      summary: Get a tax exemption [US Edition only]
      description: 'Get the details of a tax exemption. Note: You have to enable Sales
        Tax in order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-a-tax-exemption-us-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - taxes
      operationId: delete_tax_exemption
      summary: Delete a tax exemption [US Edition only]
      description: 'Delete a tax exemption. Note: You have to enable Sales Tax in
        order to perform tax related operations.'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-a-tax-exemption-us-edition-only-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - name: tax_exemption_id
      in: path
      required: true
      description: 'Unique identifier of the tax exemption. Note: This value is a
        set of numeric characters.'
      schema:
        type: string
      example: '460000000076002'
    - $ref: '#/components/parameters/organization_id'
  /settings/openingbalances:
    x-mcp-group:
    - Opening Balance
    post:
      tags:
      - opening-balance
      operationId: create_opening_balance
      summary: Create opening balance
      description: Creates opening balance with the given information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/create-opening-balance-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/create-opening-balance-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    put:
      tags:
      - opening-balance
      operationId: update_opening_balance
      summary: Update opening balance
      description: Updates the existing opening balance information.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-opening-balance-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-opening-balance-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    get:
      tags:
      - opening-balance
      operationId: get_opening_balance
      summary: Get opening balance
      description: Get opening balance.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/get-opening-balance-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    delete:
      tags:
      - opening-balance
      operationId: delete_opening_balance
      summary: Delete opening balance
      description: Delete the entered opening balance.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/delete-opening-balance-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /reportingtags:
    x-mcp-group:
    - Reporting Tags
    get:
      tags:
      - reporting-tags
      operationId: get_tags
      summary: List of all Reporting Tags
      description: Get a list of all reporting tags in the preferred order that you
        can set.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  reporting_tags:
                    $ref: '#/components/schemas/list-reporting-tags-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    post:
      tags:
      - reporting-tags
      operationId: create_tag
      summary: Create Reporting Tag
      description: Create a reporting tag
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-reporting-tag-request'
      responses:
        '201':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reporting-tags-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /reportingtags/{tag_id}:
    x-mcp-group:
    - Reporting Tags
    post:
      tags:
      - reporting-tags
      operationId: mark_default_option
      summary: Mark an option as default for a reporting tag
      description: Mark an option as the default option or clear default option for
        a reporting tag.
      parameters:
      - name: default_option_id
        in: query
        description: ID of the option to be marked as default. If this parameter is
          not sent or sent empty, the current default option of the tag will be removed.
        required: true
        schema:
          $ref: '#/components/schemas/option_id'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mark-default_option-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    put:
      tags:
      - reporting-tags
      operationId: update_tag
      summary: Update Reporting Tag
      description: Update a reporting tag
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-reporting-tag-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reporting-tags-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    delete:
      tags:
      - reporting-tags
      operationId: delete_tag
      summary: Delete a reporting tag
      description: Delete a reporting tag. If there are any usages of the reporting
        tag in transactions,  custom views or workflows, you will not be able to delete
        the tag.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: The reporting tag has been deleted successfully.
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.DELETE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
  /reportingtags/{tag_id}/options:
    x-mcp-group:
    - Reporting Tags
    put:
      tags:
      - reporting-tags
      operationId: update_tag_options
      summary: Update Reporting Tag Options
      description: Create, update or delete the options of a reporting tag. Reorder
        and arrange them in an hierarchical structure as per your organization requirements.
        <br><b>NOTE:</b> <br><ol> <li>An option cannot be a child option beyond five
        hierarchical level.</li> <li>The overall children of an option cannot exceed
        500 options.</li></ol>
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-options-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-options-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
  /reportingtags/{tag_id}/criteria:
    x-mcp-group:
    - Reporting Tags
    put:
      tags:
      - reporting-tags
      operationId: update_tag_criteria
      summary: Update Reporting Tag - Visibility Conditions
      description: Update the visibility conditions (or filter in some places) of
        a reporting tag. You can set other tags or location as filters for a tag.
        Check our help document to know about the requirements of a tag to be associated
        as a filter to another tag.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/update-criteria-request'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/update-criteria-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
  /reportingtags/{tag_id}/active:
    x-mcp-group:
    - Reporting Tags
    post:
      tags:
      - reporting-tags
      operationId: active_tag
      summary: Mark Reporting Tag as active
      description: Mark a reporting tag as active so that you can use it on entities
        which you allowed.  A newly created tag will be in draft state. Use this to
        mark that tag as ready.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: The reporting tag has been activated.
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
  /reportingtags/{tag_id}/inactive:
    x-mcp-group:
    - Reporting Tags
    post:
      tags:
      - reporting-tags
      operationId: inactive_tag
      summary: Mark Reporting Tag as inactive
      description: Mark a reporting tag as inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: The reporting tag has been deactivated.
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
  /reportingtags/{tag_id}/option/(\d+)/active:
    x-mcp-group:
    - Reporting Tags
    post:
      tags:
      - reporting-tags
      operationId: active_tag_option
      summary: Mark an option as active
      description: Mark a reporting tag's option as active.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: The reporting tag option has been activated.
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
    - $ref: '#/components/parameters/path_option_id'
  /reportingtags/{tag_id}/option/(\d+)/inactive:
    x-mcp-group:
    - Reporting Tags
    post:
      tags:
      - reporting-tags
      operationId: inactive_tag_option
      summary: Mark an option as inactive
      description: Mark a reporting tag's option as inactive.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: The reporting tag option has been deactivated.
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/path_tag_id'
    - $ref: '#/components/parameters/path_option_id'
  /reportingtags/options:
    x-mcp-group:
    - Reporting Tags
    get:
      tags:
      - reporting-tags
      operationId: get_all_tag_options
      summary: Get Reporting Tags Options Detail Page
      description: Get the options and its criteria details of a reporting tag. For
        each page, you can retrieve only 200 options.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/options_detail_page_response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/query_tag_id'
  /reportingtags/(\d+)/options/all:
    x-mcp-group:
    - Reporting Tags
    get:
      tags:
      - reporting-tags
      operationId: all_tag_options
      summary: Get all Options
      description: Get all options for a reporting tag.
      parameters:
      - name: show_untagged
        in: query
        description: '''untagged'' will be included in the response if this parameter
          is set to true.'
        required: false
        schema:
          type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/all_options_response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.READ
    parameters:
    - $ref: '#/components/parameters/organization_id'
    - $ref: '#/components/parameters/query_tag_id'
  /reportingtags/reorder:
    x-mcp-group:
    - Reporting Tags
    put:
      tags:
      - reporting-tags
      operationId: reorder_tags
      summary: Reorder Reporting Tags
      description: Reorder the reporting tags in your organization. The order of tags
        will be followed in transactions and reports.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tag_ids:
                  description: List of reporting tag IDs in the desired order
                  type: array
                  items:
                    $ref: '#/components/schemas/tag_id'
                  example:
                  - '460000000038080'
                  - '460000000038081'
                  - '460000000038082'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    example: 0
                  message:
                    type: string
                    example: The reporting tags have been reordered successfully.
                  tags:
                    $ref: '#/components/schemas/list-reporting-tags-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.UPDATE
    parameters:
    - $ref: '#/components/parameters/organization_id'
  /crm/account/{crm_account_id}/import:
    x-mcp-group:
    - ZOHO CRM Integration
    post:
      tags:
      - integration
      operationId: import_customer_using_crm_account_id
      summary: Import a customer using the CRM account ID
      description: 'Zoho Books must be integrated with Zoho CRM using Accounts and
        Contacts sync or using Accounts only sync to import a customer from CRM with
        its CRM account ID. <br> Note: You can get a contact by CRM account ID by
        using this <a href=/books/api/v3/contacts/#list-contacts/zcrm_account_id>API
        Endpoint</a>'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/import-a-customer-using-the-crm-account-id-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: crm_account_id
      in: path
      required: true
      description: Unique identifier of the zoho crm account.
      schema:
        type: string
      example: '460000000026041'
    - $ref: '#/components/parameters/organization_id'
  /crm/contact/{crm_contact_id}/import:
    x-mcp-group:
    - ZOHO CRM Integration
    post:
      tags:
      - integration
      operationId: import_customer_using_crm_contact_id
      summary: Import a customer using CRM contact ID
      description: 'Zoho Books must be integrated with Zoho CRM using <code>Contacts
        only sync</code> or <code>Accounts & their Contacts and Include contacts that
        are not associated to any accounts </code> sync type contacts that are not
        associated to any accounts in Zoho CRM to import a customer from CRM with
        its CRM contact ID. <br> Note: You can get a contact by CRM contact ID by
        using this <a href=/books/api/v3/contacts/#list-contacts/zcrm_contact_id>API
        Endpoint</a>'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/import-a-customer-using-crm-contact-id-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: crm_contact_id
      in: path
      required: true
      description: Unique identifier of the zoho crm contact.
      schema:
        type: string
      example: '460000000026042'
    - $ref: '#/components/parameters/organization_id'
  /crm/vendor/{crm_vendor_id}/import:
    x-mcp-group:
    - ZOHO CRM Integration
    post:
      tags:
      - integration
      operationId: import_vendor_using_crm_vendor_id
      summary: Import a vendor using the CRM vendor ID
      description: Zoho Books must be integrated with Zoho CRM using Vendor only sync
        to import a vendor from CRM with its CRM vendor ID.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/import-a-vendor-using-the-crm-vendor-id-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.contacts.CREATE
    parameters:
    - name: crm_vendor_id
      in: path
      required: true
      description: Unique identifier of the zoho crm vendor.
      schema:
        type: string
      example: '460000000026043'
    - $ref: '#/components/parameters/organization_id'
  /crm/item/{crm_product_id}/import:
    x-mcp-group:
    - ZOHO CRM Integration
    post:
      tags:
      - integration
      operationId: import_item_using_crm_product_id
      summary: Import an item using the CRM product ID
      description: Zoho Books must be integrated with Zoho CRM using Products only
        sync to import an item from CRM with its CRM product ID.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/import-an-item-using-the-crm-product-id-response'
      deprecated: false
      security:
      - Zoho_Auth:
        - ZohoBooks.settings.CREATE
    parameters:
    - name: crm_product_id
      in: path
      required: true
      description: Unique identifier of the zoho crm product.
      schema:
        type: string
      example: '460000000026055'
    - $ref: '#/components/parameters/organization_id'
components:
  schemas:
    gendoc-attributes-schema:
      $ref: '#/components/schemas/data'
    organization-response:
      description: List of Organisation - details
      type: object
      properties:
        organization_id:
          $ref: '#/components/schemas/organization_id'
        name:
          $ref: '#/components/schemas/name'
        is_logo_uploaded:
          $ref: '#/components/schemas/is_logo_uploaded'
        is_default_org:
          $ref: '#/components/schemas/is_default_org'
        user_role:
          $ref: '#/components/schemas/user_role'
        account_created_date:
          $ref: '#/components/schemas/account_created_date'
        time_zone:
          $ref: '#/components/schemas/time_zone'
        language_code:
          $ref: '#/components/schemas/language_code'
        date_format:
          $ref: '#/components/schemas/date_format'
        field_separator:
          $ref: '#/components/schemas/field_separator'
        fiscal_year_start_month:
          $ref: '#/components/schemas/fiscal_year_start_month'
        tax_group_enabled:
          $ref: '#/components/schemas/tax_group_enabled'
        user_status:
          $ref: '#/components/schemas/user_status'
        contact_name:
          $ref: '#/components/schemas/contact_name'
        industry_type:
          $ref: '#/components/schemas/industry_type'
        address:
          $ref: '#/components/schemas/address'
    email:
      description: Email id for the location
      type: string
      example: willsmith@bowmanfurniture.com
    organization_id:
      description: 'ID of the organisation generated by the server. Note: This value
        is a set of numeric characters.'
      type: string
      example: '10229182'
    is_logo_uploaded:
      description: Boolean to check if logo of the organisation if available
      type: string
      example: ' '
    name:
      description: Name of the pricebook
      type: string
      example: Price List 1
    contact_name:
      description: Name of the contact associated with the task.
      type: string
      example: Jane Smith
    user_role:
      description: 'Role to be assigned. Allowed Values: <code>staff</code>, <code>admin</code>
        and <code>timesheetstaff</code>.'
      type: string
      example: admin
    user_status:
      description: Status of the user
      type: string
      example: ' '
    is_default_org:
      type: boolean
      description: Indicates whether the organization is default or not.
      example: false
    plan_type:
      description: Type of the plan subscribed
      type: number
      format: double
      example: 130
    tax_group_enabled:
      type: boolean
      description: Indicates whether tax group is enabled for the organization.
      example: true
    zi_migration_status:
      type: number
      format: double
      description: Indicates the migration status of the organization to Zoho Invoice.
      example: 0
    plan_name:
      description: Name of the subscribed plan
      type: string
      example: PROFESSIONAL
    plan_period:
      description: Duration/Cycle of the plan cycle
      type: string
      example: Monthly
    language_code:
      description: 'Preferred language for the contact. Determines portal language.
        Allowed values: <code>de,en,es,fr,it,ja,nl,pt,pt_br,sv,zh,en_gb</code>'
      type: string
      example: en
    fiscal_year_start_month:
      description: Starting month of the financial year
      type: number
      format: double
      example: 0
    account_created_date:
      description: Date of creation of the account
      type: string
      example: '2012-02-15'
    account_created_date_formatted:
      type: string
      description: Formatted date of creation of the account.
      example: 15 Feb 2012
    time_zone:
      description: Time Zone of the Organisation's country
      type: string
      example: PST
    is_org_active:
      type: boolean
      description: Indicates whether the organization is active or not.
      example: true
    currency_id:
      description: ID of account currency.
      type: string
      example: '460000000000097'
    currency_code:
      description: Currency code of the account.
      type: string
      example: USD
    currency_symbol:
      description: A unique symbol for the currency. Max-len [4]
      type: string
      example: $
    currency_format:
      description: The format for the currency to be displayed. Max-len [100]
      type: string
      example: 1,234,567.89
    price_precision:
      description: Price Precision of the Values
      type: integer
      example: 2
    portal_name:
      description: Poratal name for the organisation. Length of the portal name should
        be greater than 4 and less than 31. Allowed chars [a-z][A-Z][0-9]
      type: string
      example: zilluminc
    street_address1:
      description: Street Name of the location.
      type: string
      example: No:234,90 Church Street
    street_address2:
      description: Street Name of the location.
      type: string
      example: McMillan Avenue
    city:
      description: City Name of the location.
      type: string
      example: New York City
    state:
      description: State Name of the location.
      type: string
      example: New York
    country:
      description: Country to which the tax belongs.
      type: string
      example: United Kingdom
      x-node_available_in:
      - uk
      - eu
      - global
      x-node_unavailable_in: []
    zip:
      description: Zipcode of the location.
      type: string
      example: 11364
    date_format:
      type: string
      description: Format for representing the date.
      example: dd MMM yyyy
    field_separator:
      description: Separator used to classify fields
      type: string
      example: ' '
    industry_type:
      description: Business type
      type: string
      example: Services
    industry_size:
      description: The size of the industry. The possibe values could be "small scale",
        "medium scale", "large scale"
      type: string
      example: ' '
    unverified_email:
      description: To verify email address of the organisation's contact
      type: string
      example: ' '
    is_transaction_available:
      description: To check for transaction history of the organisation
      type: string
      example: ' '
    show_org_address_as_one_field:
      description: Display the complete address of the organisation in one box
      type: string
      example: ' '
    links:
      description: External links of the organisation
      type: string
      example: ' '
    company_id_label:
      description: Label denoting company ID
      type: string
      example: Company ID
    company_id_value:
      description: Value in the label "company ID"
      type: string
      example: ' '
    tax_id_label:
      description: Label in "Tax ID" field
      type: string
      example: Tax ID
    tax_id_value:
      description: Tax ID of the contact.
      type: string
      x-node_available_in:
      - us
      x-node_unavailable_in: []
    org_address:
      description: Billing address of the organisation
      type: string
      example: ' '
    remit_to_address:
      description: Shipping address of the organisation
      type: string
      example: ' '
    phone:
      description: Mobile number for location
      type: string
      example: +1-925-921-9201
    fax:
      description: Fax Number of the Contact
      type: string
    website:
      description: Official website URL of the contact.
      type: string
      example: www.bowmanfurniture.com
    index:
      description: Index of the Custom Field
      type: integer
      example: 1
    value:
      description: Value of the Custom Field
      type: string
      example: Normal
    label:
      description: Label of the Custom Field
      type: string
    companyid_label:
      description: Label " Company ID "
      type: string
      example: ' '
    companyid_value:
      description: value in field "company ID"
      type: string
      example: ' '
    taxid_label:
      description: Label "tax ID"
      type: string
      example: ' '
    taxid_value:
      description: Value in "tax ID"
      type: string
      example: ' '
    projects:
      description: List of projects run by the organisation
      type: array
      items:
        type: string
    address:
      type: object
      properties:
        city:
          $ref: '#/components/schemas/city'
        state:
          $ref: '#/components/schemas/state'
        country:
          $ref: '#/components/schemas/country'
        attention:
          $ref: '#/components/schemas/attention'
        state_code:
          $ref: '#/components/schemas/state_code'
        street_address1:
          $ref: '#/components/schemas/street_address1'
        street_address2:
          $ref: '#/components/schemas/street_address2'
    organizations:
      description: List of organisatiions under an account
      type: array
      items:
        type: string
    organization:
      description: List of Organisation - details
      type: array
      items:
        type: string
    custom_fields:
      type: string
      description: Custom fields added for the user.
      example: ''
    create-an-organization-request:
      required:
      - name
      - currency_code
      - time_zone
      - portal_name
      type: object
      properties:
        name:
          description: Name of the project.
          type: string
          example: Zillium Inc
        fiscal_year_start_month:
          description: 'Fiscal or financial starting year of your business. Allowed
            Values: <code>january</code>, <code>february</code>, <code>march</code>,
            <code>april</code>,<code> may</code>, <code>june</code>, <code>july</code>,
            <code>august</code>, <code>september</code>, <code>october</code>, <code>november</code>
            and <code>december</code>'
          type: string
          example: january
        currency_code:
          description: Standard code for currency.
          type: string
          example: USD
        time_zone:
          description: Time zone in with the organization is located geographically.
          type: string
          example: PST
        date_format:
          description: Format for representing the date.
          type: string
          example: dd MMM yyyy
        field_separator:
          description: Field separator for components in date.
          type: string
          example: ' '
        language_code:
          description: language code for organization.For instance <code>en</code>
            represents english.
          type: string
          example: en
        industry_type:
          $ref: '#/components/schemas/industry_type'
        industry_size:
          $ref: '#/components/schemas/industry_size'
        portal_name:
          $ref: '#/components/schemas/portal_name'
        org_address:
          $ref: '#/components/schemas/org_address'
        remit_to_address:
          $ref: '#/components/schemas/remit_to_address'
        address:
          $ref: '#/components/schemas/address'
    create-an-organization-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        organization:
          $ref: '#/components/schemas/organization-response'
    list-organizations-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        organizations:
          type: array
          description: List of organisations.
          items:
            type: object
            properties:
              organization_id:
                $ref: '#/components/schemas/organization_id'
              name:
                $ref: '#/components/schemas/name'
              contact_name:
                $ref: '#/components/schemas/contact_name'
              email:
                $ref: '#/components/schemas/email'
              is_default_org:
                $ref: '#/components/schemas/is_default_org'
              plan_type:
                $ref: '#/components/schemas/plan_type'
              tax_group_enabled:
                $ref: '#/components/schemas/tax_group_enabled'
              zi_migration_status:
                $ref: '#/components/schemas/zi_migration_status'
              plan_name:
                $ref: '#/components/schemas/plan_name'
              plan_period:
                $ref: '#/components/schemas/plan_period'
              language_code:
                $ref: '#/components/schemas/language_code'
              fiscal_year_start_month:
                $ref: '#/components/schemas/fiscal_year_start_month'
              account_created_date:
                $ref: '#/components/schemas/account_created_date'
              account_created_date_formatted:
                $ref: '#/components/schemas/account_created_date_formatted'
              time_zone:
                $ref: '#/components/schemas/time_zone'
              is_org_active:
                $ref: '#/components/schemas/is_org_active'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              currency_symbol:
                $ref: '#/components/schemas/currency_symbol'
              currency_format:
                $ref: '#/components/schemas/currency_format'
              price_precision:
                $ref: '#/components/schemas/price_precision'
    update-an-organization-request:
      type: object
      properties:
        name:
          description: Name of the project.
          type: string
          example: Zillium Inc
        fiscal_year_start_month:
          description: 'Fiscal or financial starting year of your business. Allowed
            Values: <code>january</code>, <code>february</code>, <code>march</code>,
            <code>april</code>,<code> may</code>, <code>june</code>, <code>july</code>,
            <code>august</code>, <code>september</code>, <code>october</code>, <code>november</code>
            and <code>december</code>'
          type: string
          example: january
        is_logo_uploaded:
          description: To determine whether logo is uploaded.
          type: boolean
          example: true
        time_zone:
          description: Time zone in with the organization is located geographically.
          type: string
          example: PST
        date_format:
          description: Format for representing the date.
          type: string
          example: dd MMM yyyy
        field_separator:
          description: Field separator for components in date.
          type: string
          example: ' '
        language_code:
          description: language code for organization.For instance <code>en</code>
            represents english.
          type: string
          example: en
        org_address:
          $ref: '#/components/schemas/org_address'
        remit_to_address:
          $ref: '#/components/schemas/remit_to_address'
        address:
          $ref: '#/components/schemas/address'
        contact_name:
          $ref: '#/components/schemas/contact_name'
        phone:
          $ref: '#/components/schemas/phone'
        fax:
          $ref: '#/components/schemas/fax'
        website:
          $ref: '#/components/schemas/website'
        email:
          $ref: '#/components/schemas/email'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        companyid_label:
          $ref: '#/components/schemas/companyid_label'
        companyid_value:
          $ref: '#/components/schemas/companyid_value'
        taxid_label:
          $ref: '#/components/schemas/taxid_label'
        taxid_value:
          $ref: '#/components/schemas/taxid_value'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    update-an-organization-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        organization:
          description: List of Organisation - details
          type: object
          properties:
            organization_id:
              $ref: '#/components/schemas/organization_id'
            name:
              $ref: '#/components/schemas/name'
            is_default_org:
              $ref: '#/components/schemas/is_default_org'
            account_created_date:
              $ref: '#/components/schemas/account_created_date'
            time_zone:
              $ref: '#/components/schemas/time_zone'
            language_code:
              $ref: '#/components/schemas/language_code'
            date_format:
              $ref: '#/components/schemas/date_format'
            field_separator:
              $ref: '#/components/schemas/field_separator'
            fiscal_year_start_month:
              $ref: '#/components/schemas/fiscal_year_start_month'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            industry_type:
              $ref: '#/components/schemas/industry_type'
            industry_size:
              $ref: '#/components/schemas/industry_size'
            company_id_label:
              $ref: '#/components/schemas/company_id_label'
            company_id_value:
              $ref: '#/components/schemas/company_id_value'
            tax_id_label:
              $ref: '#/components/schemas/tax_id_label'
            tax_id_value:
              $ref: '#/components/schemas/tax_id_value'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            currency_format:
              $ref: '#/components/schemas/currency_format'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            org_address:
              $ref: '#/components/schemas/org_address'
            remit_to_address:
              $ref: '#/components/schemas/remit_to_address'
            phone:
              $ref: '#/components/schemas/phone'
            fax:
              $ref: '#/components/schemas/fax'
            website:
              $ref: '#/components/schemas/website'
            email:
              $ref: '#/components/schemas/email'
            is_org_active:
              $ref: '#/components/schemas/is_org_active'
            address:
              $ref: '#/components/schemas/address'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
    get-an-organization-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        organization:
          description: List of Organisation - details
          type: object
          properties:
            organization_id:
              $ref: '#/components/schemas/organization_id'
            name:
              $ref: '#/components/schemas/name'
            is_default_org:
              $ref: '#/components/schemas/is_default_org'
            account_created_date:
              $ref: '#/components/schemas/account_created_date'
            time_zone:
              $ref: '#/components/schemas/time_zone'
            language_code:
              $ref: '#/components/schemas/language_code'
            date_format:
              $ref: '#/components/schemas/date_format'
            field_separator:
              $ref: '#/components/schemas/field_separator'
            fiscal_year_start_month:
              $ref: '#/components/schemas/fiscal_year_start_month'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            industry_type:
              $ref: '#/components/schemas/industry_type'
            industry_size:
              $ref: '#/components/schemas/industry_size'
            company_id_label:
              $ref: '#/components/schemas/company_id_label'
            company_id_value:
              $ref: '#/components/schemas/company_id_value'
            tax_id_label:
              $ref: '#/components/schemas/tax_id_label'
            tax_id_value:
              $ref: '#/components/schemas/tax_id_value'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            currency_format:
              $ref: '#/components/schemas/currency_format'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            org_address:
              $ref: '#/components/schemas/org_address'
            remit_to_address:
              $ref: '#/components/schemas/remit_to_address'
            phone:
              $ref: '#/components/schemas/phone'
            fax:
              $ref: '#/components/schemas/fax'
            website:
              $ref: '#/components/schemas/website'
            email:
              $ref: '#/components/schemas/email'
            is_org_active:
              $ref: '#/components/schemas/is_org_active'
            address:
              $ref: '#/components/schemas/address'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
    estimate-response:
      type: object
      properties:
        estimate_id:
          $ref: '#/components/schemas/estimate_id'
        estimate_number:
          $ref: '#/components/schemas/estimate_number'
        date:
          $ref: '#/components/schemas/date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        status:
          $ref: '#/components/schemas/status'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        expiry_date:
          $ref: '#/components/schemas/expiry_date'
        discount:
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        line_items:
          $ref: '#/components/schemas/line_items'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        total:
          $ref: '#/components/schemas/total'
        tax_total:
          $ref: '#/components/schemas/tax_total'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        taxes:
          $ref: '#/components/schemas/taxes'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        template_id:
          $ref: '#/components/schemas/template_id'
        template_name:
          $ref: '#/components/schemas/template_name'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        salesperson_id:
          $ref: '#/components/schemas/salesperson_id'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        project:
          $ref: '#/components/schemas/project'
        tags:
          $ref: '#/components/schemas/tags_response'
    show_comment_to_clients:
      description: Boolean to check if the comment to be shown to the clients
      type: boolean
      example: true
    commented_by_id:
      description: ID of the user who commented on the task.
      type: string
      example: 982000000567127
    comment_type:
      description: Type of the comment.
      type: string
      example: internal
    commented_by:
      description: Name of the user who commented on the task.
      type: string
      example: Jane Doe
    item_id:
      description: Item ID of the product
      type: string
      example: 460000000000108
    line_item_id:
      type: string
      description: Enter the line item id that is associated with the fixed asset
        account.
      example: '3640355000000319008'
    rate:
      description: Rate of exchange for the currency with respect to base currency.
      type: number
      format: double
      example: 1.23
    unit:
      description: If there is a measurement unit for the items you are adding.
      type: string
      example: 100GB
    quantity:
      description: Number of units being credited for this line item. Must be a positive
        number. Used to calculate the total amount for the line item.
      type: number
      format: double
      example: 1
    description:
      description: Description
      type: string
      example: Description of the reporting tag
    date_description:
      description: Description of the date when the comment was made.
      type: string
      example: One month ago
    is_primary_contact:
      description: To mark contact person as primary for contact
    contact_person_name:
      description: Name of the Contact Person
      type: string
      example: Peter
    first_name:
      description: First Name of the Contact Person.
      type: string
      example: Peter
    contact_person_email:
      description: Email ID of the Contact Person.
      type: string
      example: willsmith@bowmanfurniture.com
    last_name:
      description: Last Name of the Contact Person.
      type: string
      example: Parker
    salutation:
      description: Salutation of the Contact Person.
      type: string
      example: Mr.
    contact_person_id:
      description: ID of the Contact Person
      type: string
    mobile:
      description: Mobile Number of the Contact Person.
      type: string
      example: +1-4054439562
    is_email_enabled:
      description: Used to check if Email communication preference is enabled for
        the contact person at transaction level.
      type: boolean
      example: true
    is_sms_enabled:
      description: Used to check if SMS communication preference is enabled for the
        contact person.
      type: boolean
      example: true
      x-node_available_in:
      - SMS integration
    is_whatsapp_enabled:
      description: Used to check if WhatsApp communication preference is enabled for
        the contact person at transaction level.
      type: boolean
      example: true
      x-node_available_in:
      - WhatsApp integration
    communication_preference:
      description: Preferred modes of communication for the contact person at transaction
        level.
      type: object
      properties:
        is_email_enabled:
          $ref: '#/components/schemas/is_email_enabled'
        is_whatsapp_enabled:
          $ref: '#/components/schemas/is_whatsapp_enabled'
    contact_persons_associated:
      description: Contact Persons associated with the transaction.
      type: array
      items:
        type: object
        properties:
          contact_person_id:
            $ref: '#/components/schemas/contact_person_id'
          contact_person_name:
            $ref: '#/components/schemas/contact_person_name'
          first_name:
            $ref: '#/components/schemas/first_name'
          last_name:
            $ref: '#/components/schemas/last_name'
          contact_person_email:
            $ref: '#/components/schemas/contact_person_email'
          phone:
            $ref: '#/components/schemas/phone'
          mobile:
            $ref: '#/components/schemas/mobile'
          communication_preference:
            $ref: '#/components/schemas/communication_preference'
    contact_persons_associated_payload:
      description: Contact Persons associated with the transaction.
      type: array
      items:
        type: object
        properties:
          contact_person_id:
            $ref: '#/components/schemas/contact_person_id'
          communication_preference:
            $ref: '#/components/schemas/communication_preference'
    time:
      description: Time when the comment was made.
      type: string
      example: 12:00 PM
    operation_type:
      description: Type of operation performed on the task.
      type: string
      example: Added
    transaction_id:
      description: ID of the transaction associated with the task.
      type: string
      example: 982000000567128
    transaction_type:
      description: Type of transaction associated with the task.
      type: string
      example: invoice
    last_modified_time:
      description: Search using the Last Modified Time of the Base Currency Adjustment
      type: string
    has_attachment:
      type: boolean
      example: false
    is_viewed_by_client:
      description: Boolean is retainer invoice viewed by client in client portal.
      type: boolean
      example: true
    client_viewed_time:
      description: client viewed time for retainer invoice in client portal.
      type: boolean
      example: true
    comment_id:
      description: ID of the comment on the task.
      type: string
      example: 982000000567126
    template_type:
      description: Type of the Template
      type: string
      example: standard
    file_name:
      description: Name of the file attached to the task.
      type: string
      example: document.pdf
    from_emails:
      type: array
      items:
        type: object
        properties:
          organization_contact_id:
            $ref: '#/components/schemas/organization_contact_id'
          user_name:
            $ref: '#/components/schemas/user_name'
          selected:
            $ref: '#/components/schemas/selected'
          email:
            $ref: '#/components/schemas/email'
          is_org_email_id:
            $ref: '#/components/schemas/is_org_email_id'
    user_name:
      description: User Name
      type: string
      example: John Doe
    place_of_supply:
      description: 'The place of supply is where a transaction is considered to have
        occurred for VAT purposes. For the supply of goods, the place of supply is
        the location of the goods when the supply occurs. For the supply of services,
        the place of supply should be where the supplier is established. (If not given,
        <code>place of contact</code> given for the contact will be taken)<br/>Supported
        codes for UAE emirates are : </br>Abu Dhabi - <code>AB</code>,</br>Ajman -
        <code>AJ</code>,</br>Dubai - <code>DU</code>,</br>Fujairah - <code>FU</code>,</br>Ras
        al-Khaimah - <code>RA</code>,</br>Sharjah - <code>SH</code>,</br>Umm al-Quwain
        - <code>UM</code>.<br/>Supported codes for the GCC countries are : </br>United
        Arab Emirates - <code>AE</code>,</br>Saudi Arabia - <code>SA</code>,</br>Bahrain
        - <code>BH</code>,</br>Kuwait - <code>KW</code>,</br>Oman - <code>OM</code>,</br>Qatar
        - <code>QA</code>.'
      type: string
      example: DU
      x-node_available_in:
      - gcc
      x-node_unavailable_in: []
    gst_treatment:
      description: Choose whether the contact is GST registered/unregistered/consumer/overseas.
        Allowed values are <code> business_gst </code> , <code> business_none </code>
        , <code> overseas </code> , <code> consumer </code>.
      type: string
      example: business_gst
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    gst_no:
      description: 15 digit GST identification number of the vendor.
      type: string
      example: 22AAAAA0000A1Z5
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    product_type:
      description: Specify the type of an item. Allowed values:<br/><code> goods</code>
        or <code> service</code> or <code> digital_service</code>. <br/><b>For SouthAfrica
        Edition:</b> <code>service</code>, <code>goods</code>, <code>capital_service</code>
        and <code>capital_goods</code>
      type: string
      example: goods
    hsn_or_sac:
      description: HSN Code
      type: string
      example: '123456'
      x-node_available_in:
      - in
      - ke
      - za
      x-node_unavailable_in: []
    sat_item_key_code:
      description: Add SAT Item Key Code for your goods/services. Download the <a
        href= http://omawww.sat.gob.mx/tramitesyservicios/Paginas/documentos/catCFDI_V_4_07122022.xls  >CFDI
        Catalogs.</a>
      type: string
      example: '12345678'
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    unitkey_code:
      description: Add Unit Key Code for your goods/services. Download the <a href=
        http://omawww.sat.gob.mx/tramitesyservicios/Paginas/documentos/catCFDI_V_4_07122022.xls  >CFDI
        Catalogs.</a>
      type: string
      example: '12345678'
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    is_pre_gst:
      description: Applicable for transactions that fall before july 1, 2017
      type: boolean
      example: false
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    to_contacts:
      type: array
      items:
        type: object
        properties:
          first_name:
            $ref: '#/components/schemas/first_name'
          selected:
            $ref: '#/components/schemas/selected'
          phone:
            $ref: '#/components/schemas/phone'
          email:
            $ref: '#/components/schemas/email'
          last_name:
            $ref: '#/components/schemas/last_name'
          salutation:
            $ref: '#/components/schemas/salutation'
          contact_person_id:
            $ref: '#/components/schemas/contact_person_id'
          mobile:
            $ref: '#/components/schemas/mobile'
    project_id:
      description: Search time entries by project_id.
      type: string
      example: '460000000044019'
    project_name:
      type: string
      example: REAL TIME TRAFFIC FLUX
    email_template_id:
      description: 'Get the email content based on a specific email template. If this
        param is not inputted, then the content will be based on the email template
        associated with the customer. If no template is associated with the customer,
        then default template will be used. Note: This value is a set of numeric characters.'
      type: string
      example: '460000000011023'
    error_list:
      type: array
      items:
        type: string
    emailtemplates:
      type: array
      items:
        type: object
        properties:
          selected:
            $ref: '#/components/schemas/selected'
          name:
            $ref: '#/components/schemas/name'
          email_template_id:
            $ref: '#/components/schemas/email_template_id'
    selected:
      type: boolean
      example: false
    estimate_ids:
      description: 'Comma separated estimate ids which are to be emailed. Note: This
        value is a set of numeric characters separated by comma.'
      type: string
    body:
      description: Body of the mail
      type: string
      example: 'Dear Bowman and Co, <br><br>The purchase order (PO-00001) is attached
        with this email. <br><br>An overview of the purchase order is available below:
        <br>Purchase Order # : PO-00001 <br>Date : 10 Feb 2014 <br>Amount : $112.00(in
        USD) <br><br>Please go through it and confirm the order. We look forward to
        working with you again<br><br><br><br>Regards<br><br>Zillium Inc<br><br>'
    subject:
      description: Subject of the mail.
      type: string
      example: 'Purchase Order from Zillium Inc (PO #: PO-00001)'
    cc_mail_ids:
      description: Array of email address of the recipients to be cced.
      type: array
      items:
        type: string
      example:
      - peterparker@bowmanfurniture.com
    to_mail_ids:
      description: Array of email address of the recipients.
      type: array
      items:
        type: string
      example:
      - willsmith@bowmanfurniture.com
    mail_documents:
      description: List of documents to be attached to the email.
      type: array
      items:
        type: object
        properties:
          document_id:
            $ref: '#/components/schemas/document_id'
          file_name:
            $ref: '#/components/schemas/file_name'
    send_from_org_email_id:
      description: Boolean to trigger the email from the organization's email address.
      type: boolean
      example: true
    attachments:
      description: The files to be attached with the email.
      type: string
      format: binary
    ignore_auto_number_generation:
      description: Ignore auto number generation for this vendor credit only. On enabling
        this option vendor credit number is mandatory.
      type: boolean
    custom_subject:
      type: string
      description: The custom subject of the email.
      example: ' '
    custom_body:
      type: string
      description: The custom body of the email.
      example: ' '
    tax_id:
      description: 'ID of the Tax. Note: This value is a set of numeric characters.'
      type: string
      example: '982000000566009'
    tds_tax_id:
      description: TDS Tax ID of the Tax associated with the Vendor Credit
      type: string
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    tax_authority_id:
      description: 'ID of the tax authority. Tax authority depends on the location
        of the customer. For example, if the customer is located in NY, then the tax
        authority is NY tax authority. Note: This value is a set of numeric characters.'
      type: string
      example: '460000000066001'
      x-node_available_in:
      - us
      - mx
      x-node_unavailable_in: []
    tax_exemption_id:
      description: 'ID of the Tax Exemption. Note: This value is a set of numeric
        characters.'
      type: string
      example: '460000000076002'
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    accept_retainer:
      description: 'Boolean flag indicating whether a retainer invoice should be automatically
        created if the customer accepts this estimate. When set to <code>true</code>,
        a retainer invoice is generated upon acceptance, streamlining the process
        for upfront payments or deposits. Allowed values: <code>true</code> and <code>false</code>.'
      type: boolean
      example: true
    retainer_percentage:
      description: Percentage of the estimate amount to be collected as a retainer
        (upfront payment) when the estimate is accepted. Specify a value between 0
        and 100. This determines the portion of the total estimate that will be invoiced
        as a retainer.
      type: integer
      example: 10
    avatax_use_code:
      description: Used to group like customers for exemption purposes. It is a custom
        value that links customers to a tax rule. Select from Avalara [standard codes][1]
        or enter a custom code. Max-length [25]
      type: string
      example: 982000000037049
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    avatax_exempt_no:
      description: Exemption certificate number of the customer. Max-length [25]
      type: string
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    vat_treatment:
      description: (Optional) VAT treatment for the journals. VAT treatment denotes
        the location of the customer, if the customer resides in UK then the VAT treatment
        is <code>uk</code>. If the customer is in an EU country & VAT registered,
        you are resides in Northen Ireland and selling Goods then his VAT treatment
        is <code>eu_vat_registered</code>, if he resides outside of the UK then his
        VAT treatment is <code>overseas</code> (For Pre Brexit, this can be split
        as <code>eu_vat_registered</code>, <code>eu_vat_not_registered</code> and
        <code>non_eu</code>).
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    tax_treatment:
      description: 'VAT treatment for the bank transaction.Choose whether the contact
        falls under: <code>vat_registered</code>,<code>vat_not_registered</code>,<code>gcc_vat_not_registered</code>,<code>gcc_vat_registered</code>,<code>non_gcc</code>.</br><code>dz_vat_registered</code>
        and <code>dz_vat_not_registered</code> are supported only for <b>UAE</b>.<br/><b>For
        Kenya Edition:</b> <code>vat_registered</code> ,<code>vat_not_registered</code>
        ,<code>non_kenya</code>(A business that is located outside Kenya).<br/><b>For
        SouthAfrica Edition:</b> <code>vat_registered</code>, <code>vat_not_registered</code>,
        <code>overseas</code>(A business that is located outside SouthAfrica).'
      type: string
      example: vat_registered
      x-node_available_in:
      - gcc
      - ke
      - za
      x-node_unavailable_in: []
    is_reverse_charge_applied:
      description: (Required if customer tax treatment is <code>vat_registered</code>)<br/>Used
        to specify whether the transaction is applicable for Domestic Reverse Charge
        (DRC) or not.
      type: boolean
      example: true
      x-node_available_in:
      - za
      x-node_unavailable_in: []
    tax_treatment_code:
      description: Specify reason for using out of scope.</br>Supported values for
        <b>UAE</b> are <code>uae_same_tax_group</code>, <code>uae_reimbursed_expense</code>
        and <code>uae_others</code>.</br> Supported values for <b>Bahrain</b> are
        <code>bahrain_same_tax_group</code>, <code>bahrain_transfer_of_concern</code>,
        <code>bahrain_disbursement</code>, <code>bahrain_head_to_branch_transaction</code>,
        <code>bahrain_warranty_repair_services</code> and <code>bahrain_others</code>.</br>
        Supported values for <b>KSA</b> are <code>ksa_reimbursed_expense</code>.
      type: string
      example: uae_others
      x-node_available_in:
      - gcc
      x-node_unavailable_in: []
    send:
      type: boolean
      description: Whether to send the debit note
      example: false
    template_id:
      description: ID of the template used for the task.
      type: string
      example: 982000000567124
    template_name:
      description: Name of the template
      type: string
      example: Standard Template
    print:
      description: Check if Vendor Payment must be printed.
      type: boolean
      example: false
    accept:
      description: 'You can get vendor credit details as json/pdf/html. Default format
        is html. Allowed Values: <code>json</code>, <code>xml</code>, <code>csv</code>,
        <code>xls</code>, <code>pdf</code>, <code>html</code> and <code>jhtml</code>.'
      type: string
    notes:
      type: string
      example: ' '
    terms:
      description: Terms and Conditions for the Bill
      type: string
      example: Terms and conditions apply.
    custom_fields_update:
      description: Custom fields for an invoice.
      type: array
      items:
        type: object
        properties:
          customfield_id:
            type: string
            description: 'Unique identifier for the custom field. Note: This value
              is a set of numeric characters.'
            example: '46000000012845'
          value:
            $ref: '#/components/schemas/value'
    show_on_pdf:
      description: Boolean value to check if the custom field is to be dispplayed
        on the pdf.
      type: boolean
      example: false
    billing_address:
      type: array
      items:
        type: string
      example:
      - address
      - city
      - state
      - zip
      - country
      - fax
      - attention
    shipping_address:
      type: object
      properties:
        address:
          $ref: '#/components/schemas/address'
        city:
          $ref: '#/components/schemas/city'
        state:
          $ref: '#/components/schemas/state'
        zip:
          $ref: '#/components/schemas/zip'
        country:
          $ref: '#/components/schemas/country'
        fax:
          $ref: '#/components/schemas/fax'
    taxes:
      description: Comma Seperated list of tax IDs that are to be associated to the
        tax group.
      type: string
      example: '982000000566009'
    tax_amount:
      description: Amount of Tax
      type: number
      format: double
    shipping_charge:
      description: Shipping charges applied to the invoice.
      type: number
      format: double
      example: 0
    adjustment:
      description: Additional amount added to or subtracted from bill total. Positive
        values increase total, negative values decrease total. Used for discounts,
        surcharges, rounding adjustments, or other modifications. Affects final bill
        amount, tax calculations, and payment processing.
      type: number
      format: double
      example: 0
    adjustment_description:
      description: 'Text description explaining the reason for bill adjustment. Examples:
        "Early payment discount", "Volume discount", "Rounding adjustment", "Handling
        fee". Helps in audit trails and reporting. Required when adjustment amount
        is non-zero. Used in financial reports, tax returns, and vendor communications.
        Should be clear and specific for compliance purposes.'
      type: string
      example: ' '
    sub_total:
      description: Sub Total of the transaction
      type: integer
      example: 33
    tax_total:
      description: Tax Total of the Bill
      type: integer
      example: 0
    exchange_rate:
      description: Exchange rate for the foreign currencies if involved.
      type: number
      format: double
      example: 1
    location_id:
      description: Location ID
      type: string
      example: '460000000038080'
    location_name:
      description: Name of the location.
      type: string
    line_items:
      description: Line items for the journal.
      type: array
      items:
        type: object
        properties:
          line_id:
            $ref: '#/components/schemas/line_id'
          account_id:
            $ref: '#/components/schemas/account_id'
          customer_id:
            $ref: '#/components/schemas/customer_id'
          customer_name:
            $ref: '#/components/schemas/customer_name'
          account_name:
            $ref: '#/components/schemas/account_name'
          description:
            $ref: '#/components/schemas/description'
          debit_or_credit:
            $ref: '#/components/schemas/debit_or_credit'
          tax_exemption_id:
            $ref: '#/components/schemas/tax_exemption_id'
          tax_exemption_type:
            $ref: '#/components/schemas/tax_exemption_type'
          tax_exemption_code:
            $ref: '#/components/schemas/tax_exemption_code'
          tax_authority_id:
            $ref: '#/components/schemas/tax_authority_id'
          tax_authority_name:
            $ref: '#/components/schemas/tax_authority_name'
          tax_id:
            $ref: '#/components/schemas/tax_id'
          tax_name:
            $ref: '#/components/schemas/tax_name'
          tax_type:
            $ref: '#/components/schemas/tax_type'
          tax_percentage:
            $ref: '#/components/schemas/tax_percentage'
          amount:
            $ref: '#/components/schemas/amount'
          bcy_amount:
            $ref: '#/components/schemas/bcy_amount'
          acquisition_vat_id:
            $ref: '#/components/schemas/acquisition_vat_id'
          acquisition_vat_name:
            $ref: '#/components/schemas/acquisition_vat_name'
          acquisition_vat_percentage:
            $ref: '#/components/schemas/acquisition_vat_percentage'
          acquisition_vat_amount:
            $ref: '#/components/schemas/acquisition_vat_amount'
          reverse_charge_vat_id:
            $ref: '#/components/schemas/reverse_charge_vat_id'
          reverse_charge_vat_name:
            $ref: '#/components/schemas/reverse_charge_vat_name'
          reverse_charge_vat_percentage:
            $ref: '#/components/schemas/reverse_charge_vat_percentage'
          reverse_charge_vat_amount:
            $ref: '#/components/schemas/reverse_charge_vat_amount'
          tags:
            $ref: '#/components/schemas/tags'
          location_id:
            $ref: '#/components/schemas/location_id'
          location_name:
            $ref: '#/components/schemas/location_name'
          project_id:
            $ref: '#/components/schemas/project_id'
          project_name:
            $ref: '#/components/schemas/project_name'
    item_order:
      description: Order of the Item
      type: integer
      example: 1
    bcy_rate:
      description: Rate of the Item in Base Currency
      type: number
      format: double
      example: 30
    discount_amount:
      description: Amount of discount in the Invoice
      type: number
      format: double
      example: 20
    is_discount_before_tax:
      description: To specify discount applied in before /after tax.
      type: boolean
      example: true
    discount_type:
      description: 'How the discount is specified. Allowed values: <code>entity_level</code>
        and <code>item_level</code>.'
      type: string
      example: entity level
    is_inclusive_tax:
      description: Check if transaction is tax Inclusive
      type: boolean
      example: false
    discount:
      description: Discount applied to the recurring bill, which can be either a percentage
        or a flat amount. For percentage discounts, the value should include the %
        symbol (e.g., 10%). For example, on Rs.1000, a 10% discount results in Rs.900,
        while a flat Rs.200 discount results in Rs.800.
      type: string
      example: 30%
    tax_name:
      description: Name of the Tax
      type: string
      example: Sales Group
    tax_type:
      description: 'Type to determine whether it is a simple or compound tax. <code>Allowed
        Values: tax and compound_tax</code>'
      type: string
      example: tax
    tax_percentage:
      description: Number of Percentage Taxable.
      type: number
      format: double
      example: 10.5
    item_total:
      description: Total of the Item
      type: number
      format: double
      example: 7500
    salesperson_id:
      type: string
      description: 'Unique identifier of the salesperson associated with the invoice.
        Note: This value is a set of numeric characters.'
      example: ' '
    salesperson_name:
      description: Name of the sales person
      type: string
      example: ' '
      x-node_available_in:
      - us
      - au
      - ca
      x-node_unavailable_in: []
    created_time:
      type: string
      description: Time at which the user is created.
      example: 2016-06-05T02:30:08-0700
    accepted_date:
      description: The date of acceptance of the estimates
      type: string
      example: ' '
    declined_date:
      description: The date of declination of the estimates
      type: string
      example: ' '
    expiry_date:
      description: The date of expiration of the estimates
      type: string
      example: '2013-11-30'
    estimates:
      description: The list of all estimates
      type: array
      items:
        type: object
        properties:
          estimate_id:
            $ref: '#/components/schemas/estimate_id'
          customer_name:
            $ref: '#/components/schemas/customer_name'
          customer_id:
            $ref: '#/components/schemas/customer_id'
          status:
            $ref: '#/components/schemas/status'
          estimate_number:
            $ref: '#/components/schemas/estimate_number'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          date:
            $ref: '#/components/schemas/date'
          currency_id:
            $ref: '#/components/schemas/currency_id'
          currency_code:
            $ref: '#/components/schemas/currency_code'
          total:
            $ref: '#/components/schemas/total'
          created_time:
            $ref: '#/components/schemas/created_time'
          accepted_date:
            $ref: '#/components/schemas/accepted_date'
          declined_date:
            $ref: '#/components/schemas/declined_date'
          expiry_date:
            $ref: '#/components/schemas/expiry_date'
          project_id:
            $ref: '#/components/schemas/project_id'
          project_name:
            $ref: '#/components/schemas/project_name'
    page_context:
      type: object
      properties:
        page:
          $ref: '#/components/schemas/page'
        per_page:
          $ref: '#/components/schemas/per_page'
        has_more_page:
          $ref: '#/components/schemas/has_more_page'
    page:
      description: Number of pages
      type: integer
      example: 1
    per_page:
      description: Per page values
      type: integer
      example: 10
    has_more_page:
      description: Check if has more pages
      type: boolean
      example: false
    sort_order:
      description: The order for sorting
      type: string
      example: D
    sort_column:
      description: 'Sort taxes.Allowed Values: <code>tax_name</code>, <code>taxes</code>,
        <code>date</code>, <code> due_date</code>, <code>total</code>, <code>balance</code>
        and  <code>created_time</code>'
      type: string
      example: created_time
    estimate_id:
      description: 'Unique identifier linking this sales order to its originating
        estimate document. Establishes audit trail continuity, enables estimate-to-order
        conversion tracking, and maintains historical reference for customer approval
        workflows and business process documentation. Note: This value is a set of
        numeric characters.'
      type: string
    estimate_number:
      description: Search estimates by estimate number.Variants<code>estimate_number_startswith</code>
        and <code>estimate_number_contains</code>
      type: string
      example: EST-00002
    reference_number:
      type: string
      example: ' '
    customer_name:
      type: string
      example: SAF Instruments Inc
    total:
      description: Total Amount from the Opening Balance
      type: integer
      example: 10000
    customer_id:
      description: Search projects by customer id.
      type: string
      example: '460000000044001'
    item_name:
      description: 'Search vendor credits by item name. Variants: <code>item_name_startswith</code>
        and <code>item_name_contains</code>.'
      type: string
    applied_filter:
      description: The filer applied for sorting
      type: string
      example: Status.All
    item_description:
      description: 'Search vendor credits by vendor credit item description. Variants:
        <code>item_description_startswith</code> and <code>item_description_contains</code>.'
      type: string
    custom_field:
      description: 'Search journals using customfields. Variants : <code>custom_field_startswith</code>
        and <code>sutom_field_contains</code>'
      type: string
    report_name:
      description: The report name
      type: string
      example: Taxes
    status:
      description: 'Status of the locations. Allowed Values: <code>active</code>,
        <code>inactive</code>'
      example: active
    date:
      description: Date on which the opening balance needs to be recorded. [yyyy-MM-dd]
      type: string
      example: '2013-10-01'
    filter_by:
      description: 'Filter currencies excluding base currency. Allowed Values: <code>Currencies.ExcludeBaseCurrency</code>'
      type: string
    search_text:
      description: Search items by name or description. Max-length [100]
      type: string
    project:
      type: object
    create-an-estimate-request:
      required:
      - customer_id
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        template_id:
          $ref: '#/components/schemas/template_id'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        estimate_number:
          description: Unique identifier for the estimate. If not provided, the system
            auto-generates one. This number is used for tracking, referencing, and
            communicating the estimate to customers. It appears on the estimate PDF
            and in all customer communications.
          $ref: '#/components/schemas/estimate_number'
        reference_number:
          description: External reference associated with the estimate. Use this field
            to link the estimate to external systems, customer requests, or internal
            workflows. The reference number appears on the estimate document for customer
            and internal tracking.
          $ref: '#/components/schemas/reference_number'
        date:
          description: 'Date the estimate is created. This date appears on the estimate
            document and is used for record-keeping, reporting, and determining the
            validity period. Format: YYYY-MM-DD.'
          $ref: '#/components/schemas/date'
        expiry_date:
          description: 'Date when the estimate expires. After this date, the estimate
            is no longer valid for acceptance. Used to set deadlines for customer
            response and manage time-sensitive offers. Format: YYYY-MM-DD.'
          $ref: '#/components/schemas/expiry_date'
        exchange_rate:
          description: Exchange rate used to convert the estimate amount to the base
            currency of your organization. Required for multi-currency transactions.
            The exchange rate affects the total amount and reporting in the base currency.
          $ref: '#/components/schemas/exchange_rate'
        discount:
          description: Discount applied to the estimate, which can be either a percentage
            or a flat amount. For percentage discounts, the value should include the
            % symbol (e.g., 10%). For example, on Rs.1000, a 10% discount results
            in Rs.900, while a flat Rs.200 discount results in Rs.800.
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          description: Indicates whether the discount is applied before or after tax
            calculation. When true, the discount is applied to the subtotal before
            tax. When false, the discount is applied after tax is calculated.
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          description: 'Specifies how the discount is applied: at the entity (entire
            estimate) level or item (line item) level. Allowed values: "entity_level",
            "item_level". This setting determines whether the discount is distributed
            across all items or applied to specific items only.'
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        custom_body:
          $ref: '#/components/schemas/custom_body'
        custom_subject:
          $ref: '#/components/schemas/custom_subject'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        custom_fields:
          description: Custom fields for an estimate.
          type: array
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        line_items:
          description: Line items of an estimate.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              item_order:
                $ref: '#/components/schemas/item_order'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              unit:
                $ref: '#/components/schemas/unit'
              discount_amount:
                $ref: '#/components/schemas/discount_amount'
              discount:
                $ref: '#/components/schemas/discount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              item_total:
                $ref: '#/components/schemas/item_total'
              location_id:
                $ref: '#/components/schemas/location_id'
              tags:
                $ref: '#/components/schemas/tags'
        location_id:
          $ref: '#/components/schemas/location_id'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        item_id:
          $ref: '#/components/schemas/item_id'
        line_item_id:
          $ref: '#/components/schemas/line_item_id'
        name:
          $ref: '#/components/schemas/name'
        description:
          $ref: '#/components/schemas/description'
        rate:
          $ref: '#/components/schemas/rate'
        unit:
          $ref: '#/components/schemas/unit'
        quantity:
          $ref: '#/components/schemas/quantity'
        project_id:
          $ref: '#/components/schemas/project_id'
        accept_retainer:
          $ref: '#/components/schemas/accept_retainer'
        retainer_percentage:
          $ref: '#/components/schemas/retainer_percentage'
        tags:
          $ref: '#/components/schemas/tags'
    create-an-estimate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The estimate has been created
          readOnly: true
        estimate:
          $ref: '#/components/schemas/estimate-response'
    list-estimates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        estimates:
          description: The list of all estimates
          type: array
          items:
            type: object
            properties:
              estimate_id:
                $ref: '#/components/schemas/estimate_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              status:
                $ref: '#/components/schemas/status'
              estimate_number:
                $ref: '#/components/schemas/estimate_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              date:
                $ref: '#/components/schemas/date'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              total:
                $ref: '#/components/schemas/total'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              accepted_date:
                $ref: '#/components/schemas/accepted_date'
              declined_date:
                $ref: '#/components/schemas/declined_date'
              expiry_date:
                $ref: '#/components/schemas/expiry_date'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              is_viewed_by_client:
                $ref: '#/components/schemas/is_viewed_by_client'
              client_viewed_time:
                $ref: '#/components/schemas/client_viewed_time'
        page_context:
          description: The context of a particular page.
          type: array
          items:
            type: object
            properties:
              page:
                description: Number of pages
                type: integer
                example: 1
              per_page:
                description: Per page values
                type: integer
                example: 10
              has_more_page:
                description: Check if has more pages
                type: boolean
                example: false
              report_name:
                description: The report name
                type: string
                example: Estimates
              applied_filter:
                description: The filer applied for sorting
                type: string
                example: Status.All
              sort_column:
                description: Sort estimates.
                type: string
                example: created_time
              sort_order:
                description: The order for sorting
                type: string
                example: D
    update-an-estimate-request:
      required:
      - customer_id
      - custom_fields
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        template_id:
          $ref: '#/components/schemas/template_id'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        estimate_number:
          description: Unique identifier for the estimate. If not provided, the system
            auto-generates one. This number is used for tracking, referencing, and
            communicating the estimate to customers. It appears on the estimate PDF
            and in all customer communications.
          $ref: '#/components/schemas/estimate_number'
        reference_number:
          description: External reference associated with the estimate. Use this field
            to link the estimate to external systems, customer requests, or internal
            workflows. The reference number appears on the estimate document for customer
            and internal tracking.
          $ref: '#/components/schemas/reference_number'
        date:
          description: 'Date the estimate is created. This date appears on the estimate
            document and is used for record-keeping, reporting, and determining the
            validity period. Format: YYYY-MM-DD.'
          $ref: '#/components/schemas/date'
        expiry_date:
          description: 'Date when the estimate expires. After this date, the estimate
            is no longer valid for acceptance. Used to set deadlines for customer
            response and manage time-sensitive offers. Format: YYYY-MM-DD.'
          $ref: '#/components/schemas/expiry_date'
        exchange_rate:
          description: Exchange rate used to convert the estimate amount to the base
            currency of your organization. Required for multi-currency transactions.
            The exchange rate affects the total amount and reporting in the base currency.
          $ref: '#/components/schemas/exchange_rate'
        discount:
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        custom_body:
          $ref: '#/components/schemas/custom_body'
        custom_subject:
          $ref: '#/components/schemas/custom_subject'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        custom_fields:
          description: Custom fields for an estimate.
          type: array
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        line_items:
          description: Line items of an estimate.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              item_order:
                $ref: '#/components/schemas/item_order'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              unit:
                $ref: '#/components/schemas/unit'
              discount_amount:
                $ref: '#/components/schemas/discount_amount'
              discount:
                $ref: '#/components/schemas/discount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              item_total:
                $ref: '#/components/schemas/item_total'
              location_id:
                $ref: '#/components/schemas/location_id'
              tags:
                $ref: '#/components/schemas/tags'
        location_id:
          $ref: '#/components/schemas/location_id'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        item_id:
          $ref: '#/components/schemas/item_id'
        line_item_id:
          $ref: '#/components/schemas/line_item_id'
        name:
          $ref: '#/components/schemas/name'
        description:
          $ref: '#/components/schemas/description'
        rate:
          $ref: '#/components/schemas/rate'
        unit:
          $ref: '#/components/schemas/unit'
        quantity:
          $ref: '#/components/schemas/quantity'
        project_id:
          $ref: '#/components/schemas/project_id'
        accept_retainer:
          $ref: '#/components/schemas/accept_retainer'
        retainer_percentage:
          $ref: '#/components/schemas/retainer_percentage'
        tags:
          $ref: '#/components/schemas/tags'
    update-an-estimate-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    update-an-estimate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Estimate information has been updated.
          readOnly: true
        estimate:
          type: object
          properties:
            estimate_id:
              $ref: '#/components/schemas/estimate_id'
            estimate_number:
              $ref: '#/components/schemas/estimate_number'
            date:
              $ref: '#/components/schemas/date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            status:
              $ref: '#/components/schemas/status'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            expiry_date:
              $ref: '#/components/schemas/expiry_date'
            discount:
              $ref: '#/components/schemas/discount'
            is_discount_before_tax:
              $ref: '#/components/schemas/is_discount_before_tax'
            discount_type:
              $ref: '#/components/schemas/discount_type'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            line_items:
              $ref: '#/components/schemas/line_items'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            shipping_charge:
              $ref: '#/components/schemas/shipping_charge'
            adjustment:
              $ref: '#/components/schemas/adjustment'
            adjustment_description:
              $ref: '#/components/schemas/adjustment_description'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            tax_total:
              $ref: '#/components/schemas/tax_total'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            taxes:
              $ref: '#/components/schemas/taxes'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            salesperson_id:
              $ref: '#/components/schemas/salesperson_id'
            salesperson_name:
              $ref: '#/components/schemas/salesperson_name'
            project:
              $ref: '#/components/schemas/project'
            tags:
              $ref: '#/components/schemas/tags_response'
    get-an-estimate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        estimate:
          type: object
          properties:
            estimate_id:
              $ref: '#/components/schemas/estimate_id'
            estimate_number:
              $ref: '#/components/schemas/estimate_number'
            date:
              $ref: '#/components/schemas/date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            status:
              $ref: '#/components/schemas/status'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            expiry_date:
              $ref: '#/components/schemas/expiry_date'
            discount:
              $ref: '#/components/schemas/discount'
            is_discount_before_tax:
              $ref: '#/components/schemas/is_discount_before_tax'
            discount_type:
              $ref: '#/components/schemas/discount_type'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            is_viewed_by_client:
              $ref: '#/components/schemas/is_viewed_by_client'
            client_viewed_time:
              $ref: '#/components/schemas/client_viewed_time'
            line_items:
              $ref: '#/components/schemas/line_items'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            shipping_charge:
              $ref: '#/components/schemas/shipping_charge'
            adjustment:
              $ref: '#/components/schemas/adjustment'
            adjustment_description:
              $ref: '#/components/schemas/adjustment_description'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            tax_total:
              $ref: '#/components/schemas/tax_total'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            taxes:
              $ref: '#/components/schemas/taxes'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            salesperson_id:
              $ref: '#/components/schemas/salesperson_id'
            salesperson_name:
              $ref: '#/components/schemas/salesperson_name'
            project:
              $ref: '#/components/schemas/project'
            tags:
              $ref: '#/components/schemas/tags_response'
    delete-an-estimate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The estimate has been deleted.
          readOnly: true
    mark-an-estimate-as-sent-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Estimate status has been changed to Sent
          readOnly: true
    mark-an-estimate-as-accepted-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Estimate status has been changed to Accepted.
          readOnly: true
    mark-an-estimate-as-declined-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Estimate status has been changed to Declined.
          readOnly: true
    submit-an-estimate-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The estimate has been successfully submitted for approval
          readOnly: true
    approve-an-estimate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the estimate.
          readOnly: true
    email-an-estimate-request:
      required:
      - to_mail_ids
      type: object
      properties:
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          $ref: '#/components/schemas/body'
        mail_documents:
          $ref: '#/components/schemas/mail_documents'
    email-an-estimate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your estimate has been sent.
          readOnly: true
    get-estimate-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        body:
          $ref: '#/components/schemas/body'
        error_list:
          $ref: '#/components/schemas/error_list'
        subject:
          $ref: '#/components/schemas/subject'
        emailtemplates:
          description: The email template information
          type: array
          items:
            type: object
            properties:
              selected:
                description: Boolean check for the email template
                type: boolean
                example: true
              name:
                description: The name of the line item
                type: string
                example: Default
              email_template_id:
                description: 'Get the email content based on a specific email template.
                  If this param is not inputted, then the content will be based on
                  the email template associated with the customer. If no template
                  is associated with the customer, then default template will be used.
                  Note: This value is a set of numeric characters.'
                type: string
                example: 982000000000079
        to_contacts:
          description: The list of contacts the mail to be sent
          type: array
          items:
            type: object
            properties:
              first_name:
                $ref: '#/components/schemas/first_name'
              selected:
                $ref: '#/components/schemas/selected'
              phone:
                $ref: '#/components/schemas/phone'
              email:
                $ref: '#/components/schemas/email'
              last_name:
                $ref: '#/components/schemas/last_name'
              salutation:
                $ref: '#/components/schemas/salutation'
              contact_person_id:
                $ref: '#/components/schemas/contact_person_id'
              mobile:
                $ref: '#/components/schemas/mobile'
        file_name:
          $ref: '#/components/schemas/file_name'
        from_emails:
          description: The list of from email ids
          type: array
          items:
            type: object
            properties:
              user_name:
                type: string
                example: John Smith
              selected:
                description: Boolean check for the email template
                type: boolean
                example: true
              email:
                $ref: '#/components/schemas/email'
        customer_id:
          $ref: '#/components/schemas/customer_id'
    email-multiple-estimates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Mission accomplished! We've sent all the estimates.
          readOnly: true
    bulk-export-estimates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Invalid value passed for estimate_ids
          readOnly: true
    bulk-print-estimates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    update-billing-address-request:
      type: object
      properties:
        address:
          $ref: '#/components/schemas/address'
        city:
          $ref: '#/components/schemas/city'
        state:
          $ref: '#/components/schemas/state'
        zip:
          $ref: '#/components/schemas/zip'
        country:
          $ref: '#/components/schemas/country'
        fax:
          $ref: '#/components/schemas/fax'
        attention:
          $ref: '#/components/schemas/attention'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
    update-billing-address-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Billing address updated.
          readOnly: true
        billing_address:
          $ref: '#/components/schemas/billing_address'
    update-shipping-address-request:
      type: object
      properties:
        address:
          description: Billing address for the invoice
          type: string
          example: Suite 125, McMillan Avenue
        city:
          description: City of the customer's billing address.
          type: string
          example: San Francisco
        state:
          description: State of the customer's billing address.
          type: string
          example: CA
        zip:
          description: Zip code of the customer's billing address.
          type: string
          example: 94134
        country:
          description: Country of the customer's billing address.
          type: string
          example: USA
        fax:
          description: Customer's fax number.
          type: string
          example: +1-925-924-9600
    update-shipping-address-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Shipping address updated
          readOnly: true
    list-estimate-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          type: array
          items:
            type: object
            properties:
              template_name:
                $ref: '#/components/schemas/template_name'
              template_id:
                $ref: '#/components/schemas/template_id'
              template_type:
                $ref: '#/components/schemas/template_type'
    update-estimate-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Estimate information has been updated.
          readOnly: true
    list-estimate-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              estimate_id:
                $ref: '#/components/schemas/estimate_id'
              description:
                description: The description of the line items
                type: string
                example: Estimate created
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    add-comments-request:
      type: object
      properties:
        description:
          description: The description of the line items
          type: string
          example: Estimate marked as sent
        show_comment_to_clients:
          $ref: '#/components/schemas/show_comment_to_clients'
    add-comments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comments added
          readOnly: true
    update-comment-request:
      type: object
      properties:
        description:
          description: The description of the comment.
          type: string
          example: This is a comment.
        expected_delivery_date:
          $ref: '#/components/schemas/expected_delivery_date'
    update-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comment has been updated.
          readOnly: true
    delete-a-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected journal comment entries have been deleted.
          readOnly: true
    tag_id:
      description: Tag ID
      type: string
      example: '460000000038080'
    tag_name:
      description: Tag Name
      type: string
      example: Business Unit
    tag_option_id:
      description: Tag Option's ID
      type: string
      example: '460000000014044'
    tag_option_name:
      description: Tag Option's Name
      type: string
      example: Sales
    is_tag_mandatory:
      description: Boolean to check if the tag is mandatory
      type: boolean
      example: false
    tags:
      description: Array of reporting tags associated with this line item. Each tag
        consists of a `tag_id` and `option_id` that categorize the line item for reporting
        and analytics purposes. Tags help organize and filter data across reports
        and enable detailed business intelligence analysis.
      type: array
      items:
        type: object
        properties:
          tag_id:
            $ref: '#/components/schemas/tag_id'
          tag_option_id:
            $ref: '#/components/schemas/option_id'
    tags_response:
      description: Array of reporting tags associated with this line item in the response.
        Each tag consists of a `tag_id`, `tag_name`, `option_id`, and `option_name`
        that categorize the line item for reporting and analytics purposes. Tags help
        organize and filter data across reports and enable detailed business intelligence
        analysis.
      type: array
      items:
        type: object
        properties:
          tag_id:
            $ref: '#/components/schemas/tag_id'
          tag_name:
            $ref: '#/components/schemas/tag_name'
          tag_option_id:
            $ref: '#/components/schemas/option_id'
          tag_option_name:
            $ref: '#/components/schemas/option_name'
          is_tag_mandatory:
            $ref: '#/components/schemas/is_tag_mandatory'
    contact_person-response:
      type: object
      properties:
        contact_id:
          $ref: '#/components/schemas/contact_id'
        contact_person_id:
          $ref: '#/components/schemas/contact_person_id'
        salutation:
          $ref: '#/components/schemas/salutation'
        first_name:
          $ref: '#/components/schemas/first_name'
        last_name:
          $ref: '#/components/schemas/last_name'
        email:
          $ref: '#/components/schemas/email'
        phone:
          $ref: '#/components/schemas/phone'
        mobile:
          $ref: '#/components/schemas/mobile'
        is_primary_contact:
          $ref: '#/components/schemas/is_primary_contact'
        skype:
          $ref: '#/components/schemas/skype'
        designation:
          $ref: '#/components/schemas/designation'
        department:
          $ref: '#/components/schemas/department'
        is_added_in_portal:
          $ref: '#/components/schemas/is_added_in_portal'
        communication_preference:
          $ref: '#/components/schemas/communication_preference'
    contact_id:
      description: Contact ID of the contact
      type: string
      example: 460000000026049
    skype:
      description: Skype username of the contact person. Max-length [50].
      type: string
      example: Zoho
    designation:
      description: Designation of the contact person within the contact's company.
        Max-length [100].
      type: string
      example: Sales Executive
    department:
      description: Department of the contact person within the contact's company.
        Max-length [50].
      type: string
      example: Sales and Marketing
    enable_portal:
      description: 'Indicates whether the contact person should have access to the
        client portal. Allowed values: <code>true</code> and <code>false</code>.'
      type: boolean
      example: true
    is_added_in_portal:
      description: tells whether the contact person has portal access/not
      type: boolean
      example: true
    contact_persons:
      description: Array of contact person(s) for whom invoice has to be sent.
      type: array
      items:
        type: string
      example:
      - '460000000870911'
      - '460000000870915'
    create-a-contact-person-request:
      required:
      - first_name
      type: object
      properties:
        contact_id:
          $ref: '#/components/schemas/contact_id'
        salutation:
          $ref: '#/components/schemas/salutation'
        first_name:
          $ref: '#/components/schemas/first_name'
        last_name:
          $ref: '#/components/schemas/last_name'
        email:
          $ref: '#/components/schemas/email'
        phone:
          $ref: '#/components/schemas/phone'
        mobile:
          $ref: '#/components/schemas/mobile'
        skype:
          $ref: '#/components/schemas/skype'
        designation:
          $ref: '#/components/schemas/designation'
        department:
          $ref: '#/components/schemas/department'
        enable_portal:
          $ref: '#/components/schemas/enable_portal'
        communication_preference:
          $ref: '#/components/schemas/communication_preference'
    create-a-contact-person-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The contactperson has been Created
          readOnly: true
        contact_person:
          type: array
          items:
            $ref: '#/components/schemas/contact_person-response'
    update-a-contact-person-request:
      required:
      - contact_id
      - first_name
      type: object
      properties:
        contact_id:
          $ref: '#/components/schemas/contact_id'
        salutation:
          $ref: '#/components/schemas/salutation'
        first_name:
          $ref: '#/components/schemas/first_name'
        last_name:
          $ref: '#/components/schemas/last_name'
        email:
          $ref: '#/components/schemas/email'
        phone:
          $ref: '#/components/schemas/phone'
        mobile:
          $ref: '#/components/schemas/mobile'
        skype:
          $ref: '#/components/schemas/skype'
        designation:
          $ref: '#/components/schemas/designation'
        department:
          $ref: '#/components/schemas/department'
        enable_portal:
          $ref: '#/components/schemas/enable_portal'
        is_primary_contact:
          $ref: '#/components/schemas/is_primary_contact'
        communication_preference:
          $ref: '#/components/schemas/communication_preference'
    update-a-contact-person-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The contactperson details has been updated.
          readOnly: true
        contact_person:
          type: array
          items:
            type: object
            properties:
              contact_id:
                $ref: '#/components/schemas/contact_id'
              contact_person_id:
                $ref: '#/components/schemas/contact_person_id'
              salutation:
                $ref: '#/components/schemas/salutation'
              first_name:
                $ref: '#/components/schemas/first_name'
              last_name:
                $ref: '#/components/schemas/last_name'
              email:
                $ref: '#/components/schemas/email'
              phone:
                $ref: '#/components/schemas/phone'
              mobile:
                $ref: '#/components/schemas/mobile'
              is_primary_contact:
                $ref: '#/components/schemas/is_primary_contact'
              skype:
                $ref: '#/components/schemas/skype'
              designation:
                $ref: '#/components/schemas/designation'
              department:
                $ref: '#/components/schemas/department'
              is_added_in_portal:
                $ref: '#/components/schemas/is_added_in_portal'
              communication_preference:
                $ref: '#/components/schemas/communication_preference'
    delete-a-contact-person-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The contact person has been deleted.
          readOnly: true
    list-contact-persons-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        contact_persons:
          description: Contact persons of a contact.
          type: array
          items:
            type: object
            properties:
              contact_person_id:
                $ref: '#/components/schemas/contact_person_id'
              salutation:
                $ref: '#/components/schemas/salutation'
              first_name:
                $ref: '#/components/schemas/first_name'
              last_name:
                $ref: '#/components/schemas/last_name'
              email:
                $ref: '#/components/schemas/email'
              phone:
                $ref: '#/components/schemas/phone'
              mobile:
                $ref: '#/components/schemas/mobile'
              is_primary_contact:
                $ref: '#/components/schemas/is_primary_contact'
              skype:
                $ref: '#/components/schemas/skype'
              designation:
                $ref: '#/components/schemas/designation'
              department:
                $ref: '#/components/schemas/department'
              is_added_in_portal:
                $ref: '#/components/schemas/is_added_in_portal'
              communication_preference:
                $ref: '#/components/schemas/communication_preference'
        page_context:
          description: The context of a particular page.
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    get-a-contact-person-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        contact_person:
          type: object
          properties:
            contact_id:
              $ref: '#/components/schemas/contact_id'
            contact_person_id:
              $ref: '#/components/schemas/contact_person_id'
            salutation:
              $ref: '#/components/schemas/salutation'
            first_name:
              $ref: '#/components/schemas/first_name'
            last_name:
              $ref: '#/components/schemas/last_name'
            email:
              $ref: '#/components/schemas/email'
            phone:
              $ref: '#/components/schemas/phone'
            mobile:
              $ref: '#/components/schemas/mobile'
            is_primary_contact:
              $ref: '#/components/schemas/is_primary_contact'
            skype:
              $ref: '#/components/schemas/skype'
            designation:
              $ref: '#/components/schemas/designation'
            department:
              $ref: '#/components/schemas/department'
            is_added_in_portal:
              $ref: '#/components/schemas/is_added_in_portal'
            communication_preference:
              $ref: '#/components/schemas/communication_preference'
    mark-as-primary-contact-person-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: This contact person has been marked as your primary contact person.
          readOnly: true
    contact-response:
      type: object
      properties:
        contact_id:
          $ref: '#/components/schemas/contact_id'
        contact_name:
          $ref: '#/components/schemas/contact_name'
        company_name:
          $ref: '#/components/schemas/company_name'
        has_transaction:
          $ref: '#/components/schemas/has_transaction'
        contact_type:
          $ref: '#/components/schemas/contact_type'
        customer_sub_type:
          $ref: '#/components/schemas/customer_sub_type'
        credit_limit:
          $ref: '#/components/schemas/credit_limit'
        is_portal_enabled:
          $ref: '#/components/schemas/is_portal_enabled'
        language_code:
          $ref: '#/components/schemas/language_code'
        is_taxable:
          $ref: '#/components/schemas/is_taxable'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tds_tax_id:
          $ref: '#/components/schemas/tds_tax_id'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        place_of_contact:
          $ref: '#/components/schemas/place_of_contact'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        tax_exemption_certificate_number:
          $ref: '#/components/schemas/tax_exemption_certificate_number'
        tax_regime:
          $ref: '#/components/schemas/tax_regime'
        legal_name:
          $ref: '#/components/schemas/legal_name'
        is_tds_registered:
          $ref: '#/components/schemas/is_tds_registered'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        is_linked_with_zohocrm:
          $ref: '#/components/schemas/is_linked_with_zohocrm'
        website:
          $ref: '#/components/schemas/website'
        owner_id:
          $ref: '#/components/schemas/owner_id'
        primary_contact_id:
          $ref: '#/components/schemas/primary_contact_id'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        contact_number:
          $ref: '#/components/schemas/contact_number'
        ignore_auto_number_generation:
          $ref: '#/components/schemas/ignore_auto_number_generation'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        opening_balances:
          $ref: '#/components/schemas/opening_balances'
        outstanding_receivable_amount:
          $ref: '#/components/schemas/outstanding_receivable_amount'
        outstanding_receivable_amount_bcy:
          $ref: '#/components/schemas/outstanding_receivable_amount_bcy'
        unused_credits_receivable_amount:
          $ref: '#/components/schemas/unused_credits_receivable_amount'
        unused_credits_receivable_amount_bcy:
          $ref: '#/components/schemas/unused_credits_receivable_amount_bcy'
        status:
          $ref: '#/components/schemas/status'
        payment_reminder_enabled:
          $ref: '#/components/schemas/payment_reminder_enabled'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        facebook:
          $ref: '#/components/schemas/facebook'
        twitter:
          $ref: '#/components/schemas/twitter'
        contact_persons:
          type: array
          description: The list of contact persons associated with the contact.
          items:
            type: object
            properties:
              contact_person_id:
                $ref: '#/components/schemas/contact_person_id'
              salutation:
                $ref: '#/components/schemas/salutation'
              first_name:
                $ref: '#/components/schemas/first_name'
              last_name:
                $ref: '#/components/schemas/last_name'
              email:
                $ref: '#/components/schemas/email'
              phone:
                $ref: '#/components/schemas/phone'
              mobile:
                $ref: '#/components/schemas/mobile'
              designation:
                $ref: '#/components/schemas/designation'
              department:
                $ref: '#/components/schemas/department'
              skype:
                $ref: '#/components/schemas/skype'
              is_primary_contact:
                $ref: '#/components/schemas/is_primary_contact'
              communication_preference:
                $ref: '#/components/schemas/communication_preference'
              enable_portal:
                $ref: '#/components/schemas/enable_portal'
        default_templates:
          $ref: '#/components/schemas/default_templates'
        notes:
          $ref: '#/components/schemas/notes'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
    contacts:
      description: Contacts for whom email or snail mail has to be sent.
      type: array
      items:
        type: object
        properties:
          contact_id:
            description: 'ID of the Customer and mail will be sent to invoice associated
              contact persons. Note: This value is a set of numeric characters.'
            type: string
            example: 460000000026049
          email:
            description: the boolean check for Email
            type: boolean
            example: true
          snail_mail:
            description: the boolean check for snail mail
            type: boolean
            example: false
    creditnote_refunds:
      type: array
      items:
        type: string
    creditnote_refund_id:
      type: string
      example: '982000000567158'
    creditnote_id:
      description: ID of the credit note that has to be refunded.
      type: string
      example: '4000000030049'
    refund_mode:
      description: Mode of refund. Ex - Cash, Cheque etc.,
      type: string
      example: Cash
    creditnote_number:
      description: Unique credit note number displayed in the interface. Auto-generated
        with CN prefix unless ignore_auto_number_generation is true. Max-Length [100]
      type: string
      example: CN-29
    amount_bcy:
      type: integer
      example: 10
    amount_fcy:
      type: integer
      example: 10
    is_entity_deleted:
      description: Boolean check
      type: boolean
      example: false
    credit_limit:
      description: <b>For Customer Only :</b> The maximum credit amount that can be
        allowed for the customer. Once the customer's outstanding receivables reach
        this limit, the system will restrict the customer from creating any further
        transactions.
      type: number
      format: double
      example: 1000
    customer_sub_type:
      description: '<b>For Customer Only :</b> Additional classification for customers.
        Allowed values: <code>individual</code>, <code>business</code>.'
      type: string
      example: business
    send_customer_statement:
      description: Send customer statement pdf a with email.
      type: boolean
      example: ' '
    data:
      type: object
      properties:
        contact_id:
          $ref: '#/components/schemas/contact_id'
    is_portal_enabled:
      description: 'Indicates whether portal access is enabled for the primary contact
        person. Allowed value: <code>true</code> and <code>false</code>.'
      type: boolean
      example: true
    track_1099:
      description: Boolean to track a contact for 1099 reporting.
      type: boolean
      x-node_available_in:
      - us
      x-node_unavailable_in: []
    tax_id_type:
      description: Tax ID type of the contact, it can be SSN, ATIN, ITIN or EIN.
      type: string
      x-node_available_in:
      - us
      x-node_unavailable_in: []
    invoice_template_id:
      description: 'Default invoice template id used for this customer while creating
        invoice. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000052069
    estimate_template_id:
      description: 'Default estimate template id used for this customer while creating
        estimate. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000179
    creditnote_template_id:
      description: 'Default credit note template id used for this customer while creating
        credit note. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000211
    invoice_email_template_id:
      description: 'Default invoice email template id used for this customer while
        sending invoices. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000052071
    estimate_email_template_id:
      description: 'Default estimate email template id used for this customer while
        sending estimates. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000052073
    creditnote_email_template_id:
      description: 'Default credit note email template id used for this customer while
        sending credit notes. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000052075
    address_id:
      description: 'Address id of the address. Note: This value is a set of numeric
        characters.'
      type: string
      example: 1053791000000186000
    contact_type:
      description: Type of the contact associated with the task. Allowed values are
        <code>customer</code> and <code>vendor</code>
      type: string
      example: customer
    payment_terms:
      description: Number Referring to Payment Terms
      type: integer
      example: 0
    payment_terms_label:
      description: Label of the Payment Terms
      type: string
      example: Due on Receipt
    facebook:
      description: Facebook profile URL for the contact. Max-length [100].
      type: string
      example: zoho
    twitter:
      description: X profile URL for the contact. Max-length [100].
      type: string
      example: zoho
    invoice_template_name:
      type: string
      example: Custom Classic
    estimate_template_name:
      type: string
      example: Service - Professional
    creditnote_template_name:
      type: string
      example: Fixed Cost - Professional
    invoice_email_template_name:
      type: string
      example: Custom Invoice Notification
    estimate_email_template_name:
      type: string
      example: Custom Estimate Notification
    creditnote_email_template_name:
      type: string
      example: Custom Credit Note Notification
    has_transaction:
      description: Check if the account has transactions associated with it
      type: boolean
      example: false
    is_linked_with_zohocrm:
      description: Indicates whether this contact is linked with Zoho CRM.
      type: boolean
      example: false
    primary_contact_id:
      description: 'Unique identifier for the primary contact person of the contact.
        Note: This value is a set of numeric characters.'
      type: string
      example: 460000000026051
    pricebook_id:
      description: Unique ID generated by server for the price book
      type: string
      example: 130426000002924000
    contact_number:
      description: Contact number associated with the contact for internal tracking
        and identification purposes. Max-length [200].
      type: string
      example: CNT-001234
    outstanding_receivable_amount:
      description: Total amount owed by the customer for all outstanding invoices
        and transactions.
      type: integer
      example: 250
    outstanding_receivable_amount_bcy:
      description: Total amount owed by the customer in the organization's base currency.
      type: integer
      example: 250
    unused_credits_receivable_amount:
      description: Total amount of unused credits available for the customer.
      type: integer
      example: 1369.66
    unused_credits_receivable_amount_bcy:
      description: Total amount of unused credits available for the customer in base
        currency.
      type: integer
      example: 1369.66
    payment_reminder_enabled:
      description: Boolean to check if reminders have been enabled
      type: boolean
      example: true
    owner_id:
      description: ID of the owner of the task.
      type: string
      example: 982000000567116
    company_name:
      description: Name of the company with which the customer is associated.
      type: string
      example: ABC Studios
    statement_template_id:
      description: 'Default statement template id used for this contact while creating
        statement. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000101
    payment_remittance_email_template_id:
      description: 'Default payment remittance email template id used for this contact
        while sending payment remittances. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000102
    bill_template_id:
      description: 'Default bill template id used for this vendor while creating bill.
        Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000103
    is_taxable:
      description: Boolean to track the taxability of the item.
      type: boolean
      example: true
      x-node_available_in:
      - in
      - us
      - mx
      - ke
      - za
      x-node_unavailable_in: []
    tax_regime:
      description: 'Tax Regime of the contact.Allowed Values: <code>general_legal_person</code>,
        <code>legal_entities_non_profit</code>, <code>resident_abroad</code>, <code>production_cooperative_societies</code>,
        <code>agricultural_livestock</code>, <code>optional_group_of_companies</code>,
        <code>coordinated</code>, <code>simplified_trust</code>, <code>wages_salaries_income</code>,
        <code>lease</code>, <code>property_disposal_acquisition</code>, <code>other_income</code>,
        <code>resident_abroad</code>, <code>divident_income</code>, <code>individual_business_professional</code>,
        <code>interest_income</code>, <code>income_obtaining_price</code>, <code>no_tax_obligation</code>,
        <code>tax_incorporation</code>, <code>income_through_technology_platform</code>,
        <code>simplified_trust</code>.'
      type: string
      example: general_legal_person
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    legal_name:
      description: Official legal name of the contact as registered with tax authorities
        in Mexico.
      type: string
      example: ESCUELA KEMPER URGATE
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    is_tds_registered:
      description: Indicates whether the contact is registered for Tax Deducted at
        Source (TDS) in Mexico.
      type: boolean
      example: true
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    country_code:
      description: Two letter country code for the EU country to which the tax belongs.
      type: string
      example: UK
      x-node_available_in:
      - uk
      - eu
      - gcc
      - global
      x-node_unavailable_in: []
    vat_reg_no:
      description: <b>For UK Edition:</b> VAT Registration number of a contact with
        length should be between 2 and 12 characters.<br/> <b>For Avalara:</b> If
        you are doing sales in the European Union (EU) then provide VAT Registration
        Number of your customers here. This is used to calculate VAT for B2B sales,
        from Avalara.
      type: string
      x-node_available_in:
      - uk
      - Avalara Integration
      x-node_unavailable_in: []
    tax_reg_no:
      description: '<b>For GCC Edition:</b> 15 digit Tax Registration number of a
        contact with Tax treatment as <code>vat_registered</code>,<code>gcc_vat_registered</code>,<code>dz_vat_registered</code>.<br/><b>For
        Mexico Edition:</b> 12 digit Tax Registration number of a contact with Tax
        treatment as </br> <code>home_country_mexico</code>, <code>border_region_mexico</code>,
        <code>non_mexico</code>.</br> Consumers generic RFC: <code>XAXX010101000</code>,
        Overseas generic RFC: <code>XEXX010101000</code>.<br/><b>For Kenya Edition:</b>
        11 digit Tax Registration number of a contact with Tax treatment as <code>vat_registered</code><br/><b>For
        SouthAfrica Edition:</b> 10 digit Tax Registration number of a contact with
        Tax treatment as <code>vat_registered</code>'
      type: string
      example: 12345678912345
      x-node_available_in:
      - gcc
      - mx
      - ke
      - za
      x-node_unavailable_in: []
    tax_exemption_certificate_number:
      description: Tax Exemption Certificate number is issued by the Kenya Revenue
        Authority (KRA) to organizations or individuals who qualify for tax exemption
      type: string
      example: KRAEXM0043310521
      x-node_available_in:
      - ke
      x-node_unavailable_in: []
    purchaseorder_template_id:
      description: 'Default purchase order template id used for this vendor while
        creating purchase order. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000213
    salesorder_template_id:
      description: 'Default sales order template id used for this customer while creating
        sales order. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000214
    retainerinvoice_template_id:
      description: 'Default retainer invoice template id used for this customer while
        creating retainer invoice. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000215
    paymentthankyou_template_id:
      description: 'Default payment thank you template id used for this contact while
        sending payment acknowledgment. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000216
    retainerinvoice_paymentthankyou_template_id:
      description: 'Default retainer invoice payment thank you template id used for
        this customer while sending payment acknowledgment for retainer invoices.
        Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000217
    purchaseorder_email_template_id:
      description: 'Default purchase order email template id used for this vendor
        while sending purchase order. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000218
    salesorder_email_template_id:
      description: 'Default sales order email template id used for this customer while
        sending sales order. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000219
    retainerinvoice_email_template_id:
      description: 'Default retainer invoice email template id used for this contact
        while sending retainer invoice. Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000220
    paymentthankyou_email_template_id:
      description: 'Default payment thank you email template id used for this contact
        while sending payment acknowledgment. Note: This value is a set of numeric
        characters.'
      type: string
      example: 460000000000221
    retainerinvoice_paymentthankyou_email_template_id:
      description: 'Default retainer invoice payment thank you email template id used
        for this customer while sending payment acknowledgment for retainer invoices.
        Note: This value is a set of numeric characters.'
      type: string
      example: 460000000000222
    place_of_contact:
      description: State or union territory code where the contact is located for
        Indian tax purposes. (This node identifies the place of supply and source
        of supply when invoices/bills are raised for the customer/vendor respectively.
        This is not applicable for Overseas contacts)
      type: string
      example: TN
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    tax_authority_name:
      description: Name of the Tax Authority
      type: string
      example: Illinois Department of Revenue
    tax_exemption_code:
      description: Code of the Tax Exemption
      type: string
      example: RESELLER
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    end_date:
      description: Greatest date in the transaction set
      type: string
      example: '2019-01-02'
    start_date:
      description: Least date in the transaction set
      type: string
      example: '2019-01-01'
    multipart_or_formdata:
      description: Files to be attached along with the statement.
      type: string
      example: file
    opening_balance_amount:
      description: Initial balance amount for the contact at the time of creation.
      type: number
      format: double
      example: 1200
    opening_balances:
      type: array
      description: List of opening balances for the contact.
      items:
        type: object
        properties:
          location_id:
            $ref: '#/components/schemas/location_id'
          exchange_rate:
            $ref: '#/components/schemas/exchange_rate'
          opening_balance_amount:
            $ref: '#/components/schemas/opening_balance_amount'
    attention:
      description: Attention of the location.
      type: string
    street2:
      description: Address details
      type: string
      example: Suite 310
    state_code:
      description: State code of the location.
      type: string
      example: NY
    address_info:
      description: Additional address information for the contact.
      type: object
      properties:
        address_id:
          $ref: '#/components/schemas/address_id'
        attention:
          $ref: '#/components/schemas/attention'
        address:
          $ref: '#/components/schemas/address'
        street2:
          $ref: '#/components/schemas/street2'
        city:
          $ref: '#/components/schemas/city'
        state:
          $ref: '#/components/schemas/state'
        zip:
          $ref: '#/components/schemas/zip'
        country:
          $ref: '#/components/schemas/country'
        fax:
          $ref: '#/components/schemas/fax'
        phone:
          description: The contact phone number associated with the address. Max-length
            [50]
          type: string
          example: +1-925-921-9201
    default_templates:
      type: object
      properties:
        invoice_template_id:
          $ref: '#/components/schemas/invoice_template_id'
        estimate_template_id:
          $ref: '#/components/schemas/estimate_template_id'
        creditnote_template_id:
          $ref: '#/components/schemas/creditnote_template_id'
        purchaseorder_template_id:
          $ref: '#/components/schemas/purchaseorder_template_id'
        salesorder_template_id:
          $ref: '#/components/schemas/salesorder_template_id'
        retainerinvoice_template_id:
          $ref: '#/components/schemas/retainerinvoice_template_id'
        paymentthankyou_template_id:
          $ref: '#/components/schemas/paymentthankyou_template_id'
        retainerinvoice_paymentthankyou_template_id:
          $ref: '#/components/schemas/retainerinvoice_paymentthankyou_template_id'
        invoice_email_template_id:
          $ref: '#/components/schemas/invoice_email_template_id'
        estimate_email_template_id:
          $ref: '#/components/schemas/estimate_email_template_id'
        creditnote_email_template_id:
          $ref: '#/components/schemas/creditnote_email_template_id'
        purchaseorder_email_template_id:
          $ref: '#/components/schemas/purchaseorder_email_template_id'
        salesorder_email_template_id:
          $ref: '#/components/schemas/salesorder_email_template_id'
        retainerinvoice_email_template_id:
          $ref: '#/components/schemas/retainerinvoice_email_template_id'
        paymentthankyou_email_template_id:
          $ref: '#/components/schemas/paymentthankyou_email_template_id'
        retainerinvoice_paymentthankyou_email_template_id:
          $ref: '#/components/schemas/retainerinvoice_paymentthankyou_email_template_id'
        statement_template_id:
          $ref: '#/components/schemas/statement_template_id'
        payment_remittance_email_template_id:
          $ref: '#/components/schemas/payment_remittance_email_template_id'
        bill_template_id:
          $ref: '#/components/schemas/bill_template_id'
    contact_comments:
      type: array
      items:
        type: object
        properties:
          comment_id:
            $ref: '#/components/schemas/comment_id'
          contact_id:
            $ref: '#/components/schemas/contact_id'
          contact_name:
            $ref: '#/components/schemas/contact_name'
          description:
            $ref: '#/components/schemas/description'
          commented_by_id:
            $ref: '#/components/schemas/commented_by_id'
          commented_by:
            $ref: '#/components/schemas/commented_by'
          date:
            $ref: '#/components/schemas/date'
          date_description:
            $ref: '#/components/schemas/date_description'
          time:
            $ref: '#/components/schemas/time'
          transaction_id:
            $ref: '#/components/schemas/transaction_id'
          transaction_type:
            $ref: '#/components/schemas/transaction_type'
          is_entity_deleted:
            $ref: '#/components/schemas/is_entity_deleted'
          operation_type:
            $ref: '#/components/schemas/operation_type'
    create-a-contact-request:
      required:
      - contact_name
      type: object
      properties:
        contact_name:
          $ref: '#/components/schemas/contact_name'
        company_name:
          $ref: '#/components/schemas/company_name'
        website:
          $ref: '#/components/schemas/website'
        language_code:
          $ref: '#/components/schemas/language_code'
        contact_type:
          $ref: '#/components/schemas/contact_type'
        customer_sub_type:
          $ref: '#/components/schemas/customer_sub_type'
        credit_limit:
          $ref: '#/components/schemas/credit_limit'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        contact_number:
          $ref: '#/components/schemas/contact_number'
        ignore_auto_number_generation:
          $ref: '#/components/schemas/ignore_auto_number_generation'
        tags:
          $ref: '#/components/schemas/tags'
        is_portal_enabled:
          $ref: '#/components/schemas/is_portal_enabled'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        notes:
          $ref: '#/components/schemas/notes'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        contact_persons:
          $ref: '#/components/schemas/contact_persons'
        default_templates:
          $ref: '#/components/schemas/default_templates'
        custom_fields:
          description: Custom fields of the contact.
          type: array
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        opening_balances:
          $ref: '#/components/schemas/opening_balances'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        owner_id:
          $ref: '#/components/schemas/owner_id'
        tax_reg_no:
          $ref: '#/components/schemas/tax_reg_no'
        tax_exemption_certificate_number:
          $ref: '#/components/schemas/tax_exemption_certificate_number'
        country_code:
          $ref: '#/components/schemas/country_code'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        tax_regime:
          $ref: '#/components/schemas/tax_regime'
        legal_name:
          $ref: '#/components/schemas/legal_name'
        is_tds_registered:
          $ref: '#/components/schemas/is_tds_registered'
        place_of_contact:
          $ref: '#/components/schemas/place_of_contact'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tds_tax_id:
          $ref: '#/components/schemas/tds_tax_id'
        is_taxable:
          $ref: '#/components/schemas/is_taxable'
        facebook:
          $ref: '#/components/schemas/facebook'
        twitter:
          $ref: '#/components/schemas/twitter'
        track_1099:
          $ref: '#/components/schemas/track_1099'
        tax_id_type:
          $ref: '#/components/schemas/tax_id_type'
        tax_id_value:
          $ref: '#/components/schemas/tax_id_value'
    create-a-contact-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The contact has been created
          readOnly: true
        contact:
          $ref: '#/components/schemas/contact-response'
    list-contacts-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        contacts:
          description: The list of all contacts.
          type: array
          items:
            type: object
            properties:
              contact_id:
                $ref: '#/components/schemas/contact_id'
              contact_name:
                $ref: '#/components/schemas/contact_name'
              company_name:
                $ref: '#/components/schemas/company_name'
              contact_type:
                $ref: '#/components/schemas/contact_type'
              status:
                $ref: '#/components/schemas/status'
              payment_terms:
                $ref: '#/components/schemas/payment_terms'
              payment_terms_label:
                $ref: '#/components/schemas/payment_terms_label'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              outstanding_receivable_amount:
                $ref: '#/components/schemas/outstanding_receivable_amount'
              unused_credits_receivable_amount:
                $ref: '#/components/schemas/unused_credits_receivable_amount'
              first_name:
                $ref: '#/components/schemas/first_name'
              last_name:
                $ref: '#/components/schemas/last_name'
              email:
                $ref: '#/components/schemas/email'
              phone:
                $ref: '#/components/schemas/phone'
              mobile:
                $ref: '#/components/schemas/mobile'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            applied_filter:
              $ref: '#/components/schemas/applied_filter'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    update-a-contact-request:
      required:
      - contact_name
      type: object
      properties:
        contact_name:
          $ref: '#/components/schemas/contact_name'
        company_name:
          $ref: '#/components/schemas/company_name'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        contact_type:
          $ref: '#/components/schemas/contact_type'
        customer_sub_type:
          $ref: '#/components/schemas/customer_sub_type'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        opening_balances:
          $ref: '#/components/schemas/opening_balances'
        credit_limit:
          $ref: '#/components/schemas/credit_limit'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        contact_number:
          $ref: '#/components/schemas/contact_number'
        ignore_auto_number_generation:
          $ref: '#/components/schemas/ignore_auto_number_generation'
        tags:
          $ref: '#/components/schemas/tags'
        website:
          $ref: '#/components/schemas/website'
        owner_id:
          $ref: '#/components/schemas/owner_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        contact_persons:
          type: array
          description: The list of contact persons associated with the contact.
          items:
            type: object
            properties:
              contact_person_id:
                description: 'ID of the contact person. For an existing contact person,
                  pass the <code>contact_person_id</code>. Note: This value is a set
                  of numeric characters.'
                type: string
                example: 460000000026051
              salutation:
                $ref: '#/components/schemas/salutation'
              first_name:
                $ref: '#/components/schemas/first_name'
              last_name:
                $ref: '#/components/schemas/last_name'
              email:
                $ref: '#/components/schemas/email'
              phone:
                $ref: '#/components/schemas/phone'
              mobile:
                $ref: '#/components/schemas/mobile'
              designation:
                $ref: '#/components/schemas/designation'
              department:
                $ref: '#/components/schemas/department'
              skype:
                $ref: '#/components/schemas/skype'
              is_primary_contact:
                $ref: '#/components/schemas/is_primary_contact'
              enable_portal:
                $ref: '#/components/schemas/enable_portal'
              communication_preference:
                $ref: '#/components/schemas/communication_preference'
        default_templates:
          $ref: '#/components/schemas/default_templates'
        notes:
          $ref: '#/components/schemas/notes'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        tax_reg_no:
          $ref: '#/components/schemas/tax_reg_no'
        country_code:
          $ref: '#/components/schemas/country_code'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        tax_exemption_certificate_number:
          $ref: '#/components/schemas/tax_exemption_certificate_number'
        tax_regime:
          $ref: '#/components/schemas/tax_regime'
        legal_name:
          $ref: '#/components/schemas/legal_name'
        is_tds_registered:
          $ref: '#/components/schemas/is_tds_registered'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        place_of_contact:
          $ref: '#/components/schemas/place_of_contact'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        is_taxable:
          $ref: '#/components/schemas/is_taxable'
        facebook:
          $ref: '#/components/schemas/facebook'
        twitter:
          $ref: '#/components/schemas/twitter'
        track_1099:
          $ref: '#/components/schemas/track_1099'
        tax_id_type:
          $ref: '#/components/schemas/tax_id_type'
        tax_id_value:
          $ref: '#/components/schemas/tax_id_value'
    update-a-contact-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Contact has been updated successfully
          readOnly: true
        contact:
          type: object
          properties:
            contact_id:
              $ref: '#/components/schemas/contact_id'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            company_name:
              $ref: '#/components/schemas/company_name'
            has_transaction:
              $ref: '#/components/schemas/has_transaction'
            contact_type:
              $ref: '#/components/schemas/contact_type'
            customer_sub_type:
              $ref: '#/components/schemas/customer_sub_type'
            credit_limit:
              $ref: '#/components/schemas/credit_limit'
            pricebook_id:
              $ref: '#/components/schemas/pricebook_id'
            contact_number:
              $ref: '#/components/schemas/contact_number'
            ignore_auto_number_generation:
              $ref: '#/components/schemas/ignore_auto_number_generation'
            is_taxable:
              $ref: '#/components/schemas/is_taxable'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            tax_authority_id:
              $ref: '#/components/schemas/tax_authority_id'
            tax_exemption_id:
              $ref: '#/components/schemas/tax_exemption_id'
            tax_authority_name:
              $ref: '#/components/schemas/tax_authority_name'
            tax_exemption_code:
              $ref: '#/components/schemas/tax_exemption_code'
            place_of_contact:
              $ref: '#/components/schemas/place_of_contact'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            tax_regime:
              $ref: '#/components/schemas/tax_regime'
            legal_name:
              $ref: '#/components/schemas/legal_name'
            is_tds_registered:
              $ref: '#/components/schemas/is_tds_registered'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            is_linked_with_zohocrm:
              $ref: '#/components/schemas/is_linked_with_zohocrm'
            website:
              $ref: '#/components/schemas/website'
            owner_id:
              $ref: '#/components/schemas/owner_id'
            primary_contact_id:
              $ref: '#/components/schemas/primary_contact_id'
            payment_terms:
              $ref: '#/components/schemas/payment_terms'
            payment_terms_label:
              $ref: '#/components/schemas/payment_terms_label'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            opening_balances:
              $ref: '#/components/schemas/opening_balances'
            outstanding_receivable_amount:
              $ref: '#/components/schemas/outstanding_receivable_amount'
            outstanding_receivable_amount_bcy:
              $ref: '#/components/schemas/outstanding_receivable_amount_bcy'
            unused_credits_receivable_amount:
              $ref: '#/components/schemas/unused_credits_receivable_amount'
            unused_credits_receivable_amount_bcy:
              $ref: '#/components/schemas/unused_credits_receivable_amount_bcy'
            status:
              $ref: '#/components/schemas/status'
            payment_reminder_enabled:
              $ref: '#/components/schemas/payment_reminder_enabled'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            facebook:
              $ref: '#/components/schemas/facebook'
            twitter:
              $ref: '#/components/schemas/twitter'
            contact_persons:
              type: array
              description: The list of contact persons associated with the contact.
              items:
                type: object
                properties:
                  contact_person_id:
                    $ref: '#/components/schemas/contact_person_id'
                  salutation:
                    $ref: '#/components/schemas/salutation'
                  first_name:
                    $ref: '#/components/schemas/first_name'
                  last_name:
                    $ref: '#/components/schemas/last_name'
                  email:
                    $ref: '#/components/schemas/email'
                  phone:
                    $ref: '#/components/schemas/phone'
                  mobile:
                    $ref: '#/components/schemas/mobile'
                  designation:
                    $ref: '#/components/schemas/designation'
                  department:
                    $ref: '#/components/schemas/department'
                  skype:
                    $ref: '#/components/schemas/skype'
                  is_primary_contact:
                    $ref: '#/components/schemas/is_primary_contact'
                  enable_portal:
                    $ref: '#/components/schemas/enable_portal'
                  communication_preference:
                    $ref: '#/components/schemas/communication_preference'
            default_templates:
              $ref: '#/components/schemas/default_templates'
            notes:
              $ref: '#/components/schemas/notes'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
    get-contact-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        contact:
          type: object
          properties:
            contact_id:
              $ref: '#/components/schemas/contact_id'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            company_name:
              $ref: '#/components/schemas/company_name'
            has_transaction:
              $ref: '#/components/schemas/has_transaction'
            contact_type:
              $ref: '#/components/schemas/contact_type'
            customer_sub_type:
              $ref: '#/components/schemas/customer_sub_type'
            credit_limit:
              $ref: '#/components/schemas/credit_limit'
            pricebook_id:
              $ref: '#/components/schemas/pricebook_id'
            contact_number:
              $ref: '#/components/schemas/contact_number'
            ignore_auto_number_generation:
              $ref: '#/components/schemas/ignore_auto_number_generation'
            is_taxable:
              $ref: '#/components/schemas/is_taxable'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            tax_authority_id:
              $ref: '#/components/schemas/tax_authority_id'
            tax_exemption_id:
              $ref: '#/components/schemas/tax_exemption_id'
            tax_authority_name:
              $ref: '#/components/schemas/tax_authority_name'
            tax_exemption_code:
              $ref: '#/components/schemas/tax_exemption_code'
            place_of_contact:
              $ref: '#/components/schemas/place_of_contact'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            tax_regime:
              $ref: '#/components/schemas/tax_regime'
            legal_name:
              $ref: '#/components/schemas/legal_name'
            is_tds_registered:
              $ref: '#/components/schemas/is_tds_registered'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            is_linked_with_zohocrm:
              $ref: '#/components/schemas/is_linked_with_zohocrm'
            website:
              $ref: '#/components/schemas/website'
            owner_id:
              $ref: '#/components/schemas/owner_id'
            primary_contact_id:
              $ref: '#/components/schemas/primary_contact_id'
            payment_terms:
              $ref: '#/components/schemas/payment_terms'
            payment_terms_label:
              $ref: '#/components/schemas/payment_terms_label'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            opening_balances:
              $ref: '#/components/schemas/opening_balances'
            outstanding_receivable_amount:
              $ref: '#/components/schemas/outstanding_receivable_amount'
            outstanding_receivable_amount_bcy:
              $ref: '#/components/schemas/outstanding_receivable_amount_bcy'
            unused_credits_receivable_amount:
              $ref: '#/components/schemas/unused_credits_receivable_amount'
            unused_credits_receivable_amount_bcy:
              $ref: '#/components/schemas/unused_credits_receivable_amount_bcy'
            status:
              $ref: '#/components/schemas/status'
            facebook:
              $ref: '#/components/schemas/facebook'
            twitter:
              $ref: '#/components/schemas/twitter'
            payment_reminder_enabled:
              $ref: '#/components/schemas/payment_reminder_enabled'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            contact_persons:
              type: array
              description: The list of contact persons associated with the contact.
              items:
                type: object
                properties:
                  contact_person_id:
                    $ref: '#/components/schemas/contact_person_id'
                  salutation:
                    $ref: '#/components/schemas/salutation'
                  first_name:
                    $ref: '#/components/schemas/first_name'
                  last_name:
                    $ref: '#/components/schemas/last_name'
                  email:
                    $ref: '#/components/schemas/email'
                  phone:
                    $ref: '#/components/schemas/phone'
                  mobile:
                    $ref: '#/components/schemas/mobile'
                  designation:
                    $ref: '#/components/schemas/designation'
                  department:
                    $ref: '#/components/schemas/department'
                  skype:
                    $ref: '#/components/schemas/skype'
                  is_primary_contact:
                    $ref: '#/components/schemas/is_primary_contact'
                  communication_preference:
                    $ref: '#/components/schemas/communication_preference'
                  enable_portal:
                    $ref: '#/components/schemas/enable_portal'
            default_templates:
              $ref: '#/components/schemas/default_templates'
            notes:
              $ref: '#/components/schemas/notes'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
    delete-a-contact-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The contact has been deleted.
          readOnly: true
    mark-as-active-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The location has been marked as active.
          readOnly: true
    mark-as-inactive-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The location has been marked as inactive.
          readOnly: true
    enable-portal-access-request:
      required:
      - contact_persons
      type: object
      properties:
        contact_persons:
          type: array
          description: The list of contact persons associated with the contact.
          items:
            type: object
            properties:
              contact_person_id:
                $ref: '#/components/schemas/contact_person_id'
    enable-portal-access-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Client Portal preferences have been updated
          readOnly: true
    enable-payment-reminders-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: All reminders associated with this contact have been enabled.
          readOnly: true
    disable-payment-reminders-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: All reminders associated with this contact have been stopped.
          readOnly: true
    email-statement-request:
      required:
      - to_mail_ids
      - subject
      - body
      type: object
      properties:
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          $ref: '#/components/schemas/body'
    email-statement-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Statement has been sent to the Customer.
          readOnly: true
    get-statement-mail-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          type: object
          properties:
            body:
              $ref: '#/components/schemas/body'
            subject:
              $ref: '#/components/schemas/subject'
            to_contacts:
              $ref: '#/components/schemas/to_contacts'
            file_name:
              $ref: '#/components/schemas/file_name'
            from_emails:
              $ref: '#/components/schemas/from_emails'
            contact_id:
              $ref: '#/components/schemas/contact_id'
    email-contact-request:
      required:
      - to_mail_ids
      - subject
      - body
      type: object
      properties:
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        subject:
          description: Subject of an email has to be sent. Max-length [1000]
          type: string
          example: Welcome to Zillium Inc .
        body:
          $ref: '#/components/schemas/body'
        attachments:
          $ref: '#/components/schemas/attachments'
    email-contact-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Email has been sent.
          readOnly: true
    list-comments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
    get-contact-addresses-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        addresses:
          type: array
          items:
            type: object
            properties:
              address_id:
                $ref: '#/components/schemas/address_id'
              attention:
                $ref: '#/components/schemas/attention'
              address:
                $ref: '#/components/schemas/address'
              street2:
                $ref: '#/components/schemas/street2'
              city:
                $ref: '#/components/schemas/city'
              state:
                $ref: '#/components/schemas/state'
              zip:
                $ref: '#/components/schemas/zip'
              country:
                $ref: '#/components/schemas/country'
              fax:
                $ref: '#/components/schemas/fax'
              phone:
                description: The contact phone number associated with the address.
                  Max-length [50]
                type: string
                example: +1-925-921-9201
    add-additional-address-request:
      type: object
      properties:
        attention:
          $ref: '#/components/schemas/attention'
        address:
          $ref: '#/components/schemas/address'
        street2:
          $ref: '#/components/schemas/street2'
        city:
          $ref: '#/components/schemas/city'
        state:
          $ref: '#/components/schemas/state'
        zip:
          $ref: '#/components/schemas/zip'
        country:
          $ref: '#/components/schemas/country'
        fax:
          $ref: '#/components/schemas/fax'
        phone:
          description: The contact phone number associated with the address. Max-length
            [50]
          type: string
          example: +1-925-921-9201
    add-additional-address-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The address has been created.
          readOnly: true
        address_info:
          $ref: '#/components/schemas/address_info'
    edit-additional-address-request:
      required:
      - address_id
      type: object
      properties:
        attention:
          $ref: '#/components/schemas/attention'
        address:
          $ref: '#/components/schemas/address'
        street2:
          $ref: '#/components/schemas/street2'
        city:
          $ref: '#/components/schemas/city'
        state:
          $ref: '#/components/schemas/state'
        zip:
          $ref: '#/components/schemas/zip'
        country:
          $ref: '#/components/schemas/country'
        fax:
          $ref: '#/components/schemas/fax'
        phone:
          description: The contact phone number associated with the address. Max-length
            [50]
          type: string
          example: +1-925-921-9201
        address_id:
          description: 'Address id of the address. Note: This value is a set of numeric
            characters.'
          type: string
          example: 1053791000000186000
    edit-additional-address-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The address has been updated.
          readOnly: true
        address_info:
          $ref: '#/components/schemas/address_info'
    delete-additional-address-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The address has been deleted.
          readOnly: true
    list-refunds-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        creditnote_refunds:
          type: array
          items:
            type: object
            properties:
              creditnote_refund_id:
                type: string
                description: 'Unique identifier for the credit note refund. Note:
                  This value is a set of numeric characters.'
                example: 982000000567158
              creditnote_id:
                type: string
                description: 'Unique identifier for the credit note associated with
                  the refund. Note: This value is a set of numeric characters.'
                example: 982000000567134
              date:
                type: string
                example: '2013-11-19'
              refund_mode:
                type: string
                example: cash
              reference_number:
                type: string
                example: 782364
              creditnote_number:
                type: string
                example: CN-00001
              customer_name:
                type: string
                example: Bowman & Co
              description:
                type: string
                example: gf
              amount_bcy:
                type: string
                example: 57.15
              amount_fcy:
                type: string
                example: 57.15
        page_context:
          description: The context of a particular page.
          type: object
          properties:
            page:
              description: Number of pages
              type: integer
              example: 1
            per_page:
              description: Per page values
              type: integer
              example: 10
            has_more_page:
              description: Check if has more pages
              type: boolean
              example: false
            report_name:
              description: The displaying the  report name
              type: string
              example: Credit Notes Refund
            sort_column:
              description: Param Description.
              type: string
              example: date
            sort_order:
              description: The order for sorting
              type: string
              example: D
    track-1099-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: 1099 tracking is enabled.
          readOnly: true
    untrack-1099-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: 1099 tracking is disabled.
          readOnly: true
    get-unused-retainer-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        unused_credits_receivable:
          type: array
          items:
            type: object
            properties:
              payment_id:
                description: 'Unique identifier for the retainer payment. Note: This
                  value is a set of numeric characters.'
                type: string
                example: '460000000056789'
              payment_number:
                description: Payment reference number.
                type: string
                example: PAY-00001
              date:
                description: Date when the retainer payment was made.
                type: string
                example: '2023-12-01'
              amount:
                description: Total amount of the retainer payment.
                type: number
                format: double
                example: 1000
              unused_amount:
                description: Amount that remains unused from the retainer payment.
                type: number
                format: double
                example: 750
              currency_id:
                description: 'Currency ID of the payment. Note: This value is a set
                  of numeric characters.'
                type: string
                example: '460000000000097'
              currency_code:
                description: Currency code of the payment.
                type: string
                example: USD
              currency_symbol:
                description: Currency symbol of the payment.
                type: string
                example: $
              reference_number:
                description: External reference number for the payment.
                type: string
                example: REF-12345
              payment_mode:
                description: Mode of payment used.
                type: string
                example: bank_transfer
              description:
                description: Additional notes or description for the payment.
                type: string
                example: Retainer payment for project services
    salesorder-response:
      type: object
      properties:
        salesorder_id:
          $ref: '#/components/schemas/salesorder_id'
        documents:
          $ref: '#/components/schemas/documents'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        zcrm_potential_id:
          $ref: '#/components/schemas/zcrm_potential_id'
        zcrm_potential_name:
          $ref: '#/components/schemas/zcrm_potential_name'
        salesorder_number:
          $ref: '#/components/schemas/salesorder_number'
        date:
          $ref: '#/components/schemas/date'
        status:
          $ref: '#/components/schemas/status'
        shipment_date:
          $ref: '#/components/schemas/shipment_date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        discount_amount:
          $ref: '#/components/schemas/discount_amount'
        discount:
          $ref: '#/components/schemas/discount'
        discount_applied_on_amount:
          $ref: '#/components/schemas/discount_applied_on_amount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        estimate_id:
          $ref: '#/components/schemas/estimate_id'
        delivery_method:
          $ref: '#/components/schemas/delivery_method'
        delivery_method_id:
          $ref: '#/components/schemas/delivery_method_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        line_items:
          $ref: '#/components/schemas/line_items'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        tax_total:
          $ref: '#/components/schemas/tax_total'
        total:
          $ref: '#/components/schemas/total'
        taxes:
          $ref: '#/components/schemas/taxes'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        is_emailed:
          $ref: '#/components/schemas/is_emailed'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags_response'
        template_id:
          $ref: '#/components/schemas/template_id'
        template_name:
          $ref: '#/components/schemas/template_name'
        page_width:
          $ref: '#/components/schemas/page_width'
        page_height:
          $ref: '#/components/schemas/page_height'
        orientation:
          $ref: '#/components/schemas/orientation'
        template_type:
          $ref: '#/components/schemas/template_type'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        created_by_id:
          $ref: '#/components/schemas/created_by_id'
        attachment_name:
          $ref: '#/components/schemas/attachment_name'
        can_send_in_mail:
          $ref: '#/components/schemas/can_send_in_mail'
        salesperson_id:
          $ref: '#/components/schemas/salesperson_id'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        merchant_id:
          $ref: '#/components/schemas/merchant_id'
        merchant_name:
          $ref: '#/components/schemas/merchant_name'
    salesorder_number:
      type: string
      example: SO-00001
    is_update_customer:
      description: Check if Customer should be updated
      type: boolean
      example: false
    shipment_date:
      type: string
      example: ''
    avatax_use_code_desc:
      description: Description of the Avalra Use Code Used.
      type: string
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    avatax_use_code_id:
      description: 'ID of Avalara Tax Code Used. Note: This value is a set of numeric
        characters.'
      type: string
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    update-an-salesorder-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    avatax_tax_code:
      description: A tax code is a unique label used to group Items (products, services,
        or charges) together. Max-length [25]
      type: string
      example: 982000000037049
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    notes_default:
      description: Default notes for purchase order
      type: string
    terms_default:
      description: Default terms for Purchase Orders
      type: string
    delivery_method:
      description: Specification of the transportation and delivery mechanism for
        order fulfillment. Determines shipping costs, delivery timelines, carrier
        selection, and customer communication regarding order status and tracking
        information.
      type: string
      example: Air
    pricebook_name:
      description: Name of the price book
      type: string
    is_invoiced:
      description: Check if it is Invoiced
      type: boolean
      example: false
    sku:
      description: SKU value of item,should be unique throughout the product
      type: string
      example: s12345
    stock_on_hand:
      description: Stock on Hand for that Item
      type: string
    customfield_id:
      type: string
      description: 'ID of the custom field. Note: This value is a set of numeric characters.'
      example: '46000000012845'
    item_custom_fields:
      type: array
      description: Custom fields associated with the specific line item.
      items:
        type: object
        properties:
          custom_field_id:
            $ref: '#/components/schemas/custom_field_id'
          label:
            $ref: '#/components/schemas/label'
          value:
            $ref: '#/components/schemas/value'
          index:
            $ref: '#/components/schemas/index'
    salesorder_id:
      description: 'Unique identifier of the sales order that is linked to this purchase
        order. This field establishes a connection between the sales order and purchase
        order, typically used in drop-shipping scenarios where you need to procure
        items to fulfill a customer''s sales order. The linked sales order information
        may be displayed on the purchase order for vendor reference and internal tracking
        purposes. Note: This value is a set of numeric characters.'
      type: string
      example: '460000124728314'
    delivery_method_id:
      type: string
      description: 'Unique identifier for the delivery method. Note: This value is
        a set of numeric characters.'
    sub_statuses:
      type: array
      items:
        type: object
        properties:
          status_id:
            type: string
            description: 'Unique identifier for the sales order status. Note: This
              value is a set of numeric characters.'
            example: '460000000000097'
          status_code:
            type: string
            example: cs_openshi
          parent_status:
            $ref: '#/components/schemas/status'
          description:
            type: string
            example: Packed is shipped.
          display_name:
            type: string
            example: Open Shipped
          label_name:
            type: string
            example: cs_openshi
          color_code:
            type: string
            example: 208eff
    is_emailed:
      description: Check if the purchase order is emailed
      type: boolean
      example: false
    is_one_off_address:
      type: boolean
      example: true
    attachment_name:
      type: string
    can_send_in_mail:
      description: Boolean to send the attachment with the purchase order when emailed.
      type: boolean
      example: false
    send_attachment:
      description: Send the purchase order attachment a with the email.
      type: boolean
      example: false
    bcc_mail_ids:
      description: Array of email address of the recipients to be BCC ed.
      type: array
      items:
        type: string
      example:
      - mark@safInstruments.com
    salesorder_ids:
      description: 'Comma separated sales order ids which are to be export as pdf.
        Note: This value is a set of numeric characters characters separated by comma.'
      type: string
      example: 460108000000072000
    templates:
      description: List of available templates for the purchase order.
      type: array
      items:
        type: object
        properties:
          template_name:
            $ref: '#/components/schemas/template_name'
          template_id:
            $ref: '#/components/schemas/template_id'
          template_type:
            $ref: '#/components/schemas/template_type'
    preview:
      description: Get the thumbnail of the attachment.
      type: boolean
    documents:
      description: List of files attached to a particular task.
      type: array
      items:
        type: object
        properties:
          can_show_in_portal:
            $ref: '#/components/schemas/can_show_in_portal'
          file_name:
            $ref: '#/components/schemas/file_name'
          file_type:
            $ref: '#/components/schemas/file_type'
          file_size:
            $ref: '#/components/schemas/file_size'
          file_size_formatted:
            $ref: '#/components/schemas/file_size_formatted'
          document_id:
            $ref: '#/components/schemas/document_id'
          attachment_order:
            $ref: '#/components/schemas/attachment_order'
    zcrm_potential_id:
      description: ID of the CRM potential associated with the transaction.
      type: string
    zcrm_potential_name:
      description: Descriptive name of the Zoho CRM potential record associated with
        this sales order.
      type: string
    discount_applied_on_amount:
      type: integer
      example: 0
    page_width:
      description: Width of the Page
      type: string
      example: 8.27in
    page_height:
      description: Height of the Page
      type: string
      example: 11.69in
    orientation:
      description: Orientation of the Page
      type: string
      example: portrait
    created_by_id:
      description: ID of the user who created the task.
      type: string
      example: 982000000567122
    bcc_mails:
      type: array
      items:
        type: string
    bcc_mails_str:
      description: Mails IDs to be BCCed
      type: string
    entity_id:
      description: 'ID of the Entity. Note: This value is a set of numeric characters.'
      type: string
      example: '460000000046005'
    cc_mails_list:
      type: array
      items:
        type: string
    organization_contact_id:
      type: string
      description: 'Unique identifier of the organization contact. Note: This value
        is a set of numeric characters.'
    is_org_email_id:
      description: Check if the Email ID Belongs to the Organisation
      type: boolean
      example: false
    file_name_without_extension:
      type: string
      example: PO-00001
    to_mails_str:
      type: string
    cc_mails_str:
      type: string
    from_email:
      type: string
    from_address:
      type: string
      example: 'null'
    emailtemplate_documents:
      type: array
      items:
        type: string
    entity_level:
      example: null
    salesorder:
      type: array
      items:
        $ref: '#/components/schemas/salesorder-response'
    salesorders:
      type: array
      items:
        type: object
        properties:
          salesorder:
            $ref: '#/components/schemas/salesorder'
    comment:
      description: Comment on the task.
      type: object
      properties:
        comment_id:
          $ref: '#/components/schemas/comment_id'
        description:
          $ref: '#/components/schemas/description'
        commented_by_id:
          $ref: '#/components/schemas/commented_by_id'
        commented_by:
          $ref: '#/components/schemas/commented_by'
        comment_type:
          $ref: '#/components/schemas/comment_type'
        date:
          description: Date when the comment was made.
          type: string
          format: date-time
          example: '2025-01-01T12:00:00Z'
        date_description:
          $ref: '#/components/schemas/date_description'
        time:
          $ref: '#/components/schemas/time'
        operation_type:
          $ref: '#/components/schemas/operation_type'
        transaction_id:
          $ref: '#/components/schemas/transaction_id'
        transaction_type:
          $ref: '#/components/schemas/transaction_type'
    comments:
      description: List of comments for the task.
      type: array
      items:
        type: object
        properties:
          comment_id:
            $ref: '#/components/schemas/comment_id'
          description:
            description: Description of the comment.
            type: string
            example: This is a sample comment.
          commented_by_id:
            $ref: '#/components/schemas/commented_by_id'
          commented_by:
            $ref: '#/components/schemas/commented_by'
          comment_type:
            $ref: '#/components/schemas/comment_type'
          date:
            description: Date when the comment was made.
            type: string
            format: date-time
            example: '2025-01-01T12:00:00Z'
          date_description:
            $ref: '#/components/schemas/date_description'
          time:
            $ref: '#/components/schemas/time'
          operation_type:
            $ref: '#/components/schemas/operation_type'
          transaction_id:
            $ref: '#/components/schemas/transaction_id'
          transaction_type:
            $ref: '#/components/schemas/transaction_type'
    totalFiles:
      description: Total number of files.
      type: integer
      example: 0
    doc:
      description: Document that is to be attached
      type: string
      format: binary
    billing_address_id:
      description: ID of the Billing Address
      type: string
    shipping_address_id:
      description: 'Unique identifier of the shipping address to be used for this
        invoice. Use the `shipping_address_id` returned by the Addresses API for the
        same customer and organization. This address appears on the invoice document
        and is used for delivery purposes. If omitted, the customer''s default shipping
        address will be used. Note: This value is a set of numeric characters.'
      type: string
      example: '218500000000142014'
    crm_owner_id:
      description: 'Unique identifier of the CRM owner assigned to this purchase order.
        This field links the purchase order to a specific CRM user who is responsible
        for managing the vendor relationship and overseeing the procurement process.
        The CRM owner can track, follow up, and manage all aspects of this purchase
        order within the CRM system. Note: This value is a set of numeric characters.'
      type: string
    crm_custom_reference_id:
      description: 'ID of CRM Custom Reference. Note: This value is a set of numeric
        characters.'
      type: string
    shipment_days:
      description: Days of Shipment for the Order
      type: integer
      example: 2
    bcy_total:
      description: Total in Base Currency
      type: number
      format: double
      example: 100
    is_avalara_sync_failed:
      description: Check if Avalara Integration is Failed.
      type: boolean
    tax_source:
      description: Source of the Tax applied
      type: string
    product_exemption_id:
      description: 'ID of the Exemption of the line item in the Sales Order. Note:
        This value is a set of numeric characters.'
      type: string
      x-node_available_in:
      - us
      x-node_unavailable_in: []
    product_exemption_code:
      description: Code of the Exemption of the line item in the Sales Order
      type: string
      x-node_available_in:
      - us
      x-node_unavailable_in: []
    avatax_tax_code_id:
      description: 'ID of the Ava Tax Code Used. Note: This value is a set of numeric
        characters.'
      type: string
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    avatax_tax_code_desc:
      description: Description of the Ava Tax Code Used
      type: string
      x-node_available_in:
      - Avalara Integration
      x-node_unavailable_in: []
    item_total_inclusive_of_tax:
      description: Total of the Item inclusive of Tax
      type: number
      format: double
      example: 7500
    image_id:
      description: 'ID of the Image of the Item. Note: This value is a set of numeric
        characters.'
      type: string
    image_name:
      description: Name of the Image of the Item.
      type: string
    image_type:
      description: Type of the image of the item
      type: string
    from_address_id:
      description: 'ID of From Address of the Email Address. Note: This value is a
        set of numeric characters.'
      type: string
      example: '460000008392548'
    invoice_id:
      type: string
      example: ''
    toprint:
      description: Check if Sales Order is to be printed
      type: boolean
      example: true
    is_verified:
      type: boolean
      example: false
    inline:
      description: Check if inline response is required
      type: boolean
      example: false
    document_ids:
      description: ID's of the document
      type: string
    merchant_id:
      description: 'ID of the merchant. Note: This value is a set of numeric characters.'
      type: string
      example: '460000000000597'
    merchant_name:
      description: Name of the merchant
      type: string
      example: John Louis
    reason:
      description: Reason
      type: string
    customview_id:
      description: 'ID of the customview. Note: This value is a set of numeric characters.'
      type: string
      example: '460000004000597'
    attachment:
      description: The file that is to be added as an Attachment in the Journal
      type: string
      format: binary
    create-a-sales-order-request:
      required:
      - customer_id
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        date:
          $ref: '#/components/schemas/date'
        shipment_date:
          $ref: '#/components/schemas/shipment_date'
        custom_fields:
          description: Custom fields for a sales order.
          type: array
          items:
            type: object
            properties:
              customfield_id:
                $ref: '#/components/schemas/customfield_id'
              value:
                $ref: '#/components/schemas/value'
        tags:
          $ref: '#/components/schemas/tags'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        salesperson_id:
          $ref: '#/components/schemas/salesperson_id'
        merchant_id:
          $ref: '#/components/schemas/merchant_id'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of a sales order.
          type: array
          items:
            type: object
            properties:
              item_order:
                $ref: '#/components/schemas/item_order'
              item_id:
                $ref: '#/components/schemas/item_id'
              rate:
                $ref: '#/components/schemas/rate'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              quantity:
                $ref: '#/components/schemas/quantity'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              location_id:
                $ref: '#/components/schemas/location_id'
              discount:
                $ref: '#/components/schemas/discount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tags:
                $ref: '#/components/schemas/tags'
              unit:
                $ref: '#/components/schemas/unit'
              item_custom_fields:
                description: Custom fields for a sales order items.
                type: array
                items:
                  type: object
                  properties:
                    customfield_id:
                      $ref: '#/components/schemas/customfield_id'
                    value:
                      $ref: '#/components/schemas/value'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              avatax_exempt_no:
                $ref: '#/components/schemas/avatax_exempt_no'
              avatax_use_code:
                $ref: '#/components/schemas/avatax_use_code'
              project_id:
                $ref: '#/components/schemas/project_id'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        billing_address_id:
          $ref: '#/components/schemas/billing_address_id'
        shipping_address_id:
          $ref: '#/components/schemas/shipping_address_id'
        crm_owner_id:
          $ref: '#/components/schemas/crm_owner_id'
        crm_custom_reference_id:
          $ref: '#/components/schemas/crm_custom_reference_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        salesorder_number:
          $ref: '#/components/schemas/salesorder_number'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        discount:
          $ref: '#/components/schemas/discount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        notes_default:
          $ref: '#/components/schemas/notes_default'
        terms_default:
          $ref: '#/components/schemas/terms_default'
        tax_id:
          description: 'Tax ID for the Sales Order. Note: This value is a set of numeric
            characters.'
          type: string
          example: '460000000017094'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        delivery_method:
          $ref: '#/components/schemas/delivery_method'
        estimate_id:
          description: 'ID of the estimate associated with the Sales Order. Note:
            This value is a set of numeric characters.'
          type: string
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        template_id:
          $ref: '#/components/schemas/template_id'
        documents:
          $ref: '#/components/schemas/documents'
        zcrm_potential_id:
          $ref: '#/components/schemas/zcrm_potential_id'
        zcrm_potential_name:
          $ref: '#/components/schemas/zcrm_potential_name'
    create-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Order Created Successfully.
          readOnly: true
        salesorder:
          $ref: '#/components/schemas/salesorder'
    list-sales-orders-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        salesorders:
          type: array
          items:
            type: object
            properties:
              salesorder_id:
                $ref: '#/components/schemas/salesorder_id'
              zcrm_potential_id:
                $ref: '#/components/schemas/zcrm_potential_id'
              zcrm_potential_name:
                $ref: '#/components/schemas/zcrm_potential_name'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              status:
                $ref: '#/components/schemas/status'
              salesorder_number:
                $ref: '#/components/schemas/salesorder_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              date:
                $ref: '#/components/schemas/date'
              shipment_date:
                $ref: '#/components/schemas/shipment_date'
              shipment_days:
                $ref: '#/components/schemas/shipment_days'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              total:
                $ref: '#/components/schemas/total'
              sub_total:
                $ref: '#/components/schemas/sub_total'
              bcy_total:
                $ref: '#/components/schemas/bcy_total'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              is_emailed:
                $ref: '#/components/schemas/is_emailed'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
    update-a-sales-order-request:
      required:
      - customer_id
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        date:
          $ref: '#/components/schemas/date'
        shipment_date:
          $ref: '#/components/schemas/shipment_date'
        custom_fields:
          description: Custom fields for a sales order.
          type: array
          items:
            type: object
            properties:
              customfield_id:
                $ref: '#/components/schemas/customfield_id'
              value:
                $ref: '#/components/schemas/value'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        salesperson_id:
          $ref: '#/components/schemas/salesperson_id'
        merchant_id:
          $ref: '#/components/schemas/merchant_id'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of a sales order.
          type: array
          items:
            type: object
            properties:
              item_order:
                $ref: '#/components/schemas/item_order'
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              rate:
                $ref: '#/components/schemas/rate'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              quantity:
                $ref: '#/components/schemas/quantity'
              discount:
                $ref: '#/components/schemas/discount'
              tax_id:
                description: 'Tax ID for the Sales Order. Note: This value is a set
                  of numeric characters.'
                type: string
                example: '460000000017094'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tags:
                $ref: '#/components/schemas/tags'
              unit:
                $ref: '#/components/schemas/unit'
              item_custom_fields:
                description: Custom fields for a sales order items.
                type: array
                items:
                  type: object
                  properties:
                    customfield_id:
                      $ref: '#/components/schemas/customfield_id'
                    value:
                      $ref: '#/components/schemas/value'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              avatax_exempt_no:
                $ref: '#/components/schemas/avatax_exempt_no'
              avatax_use_code:
                $ref: '#/components/schemas/avatax_use_code'
              project_id:
                $ref: '#/components/schemas/project_id'
              location_id:
                $ref: '#/components/schemas/location_id'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        billing_address_id:
          $ref: '#/components/schemas/billing_address_id'
        shipping_address_id:
          $ref: '#/components/schemas/shipping_address_id'
        crm_owner_id:
          $ref: '#/components/schemas/crm_owner_id'
        crm_custom_reference_id:
          $ref: '#/components/schemas/crm_custom_reference_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        salesorder_number:
          $ref: '#/components/schemas/salesorder_number'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        discount:
          $ref: '#/components/schemas/discount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        notes_default:
          $ref: '#/components/schemas/notes_default'
        terms_default:
          $ref: '#/components/schemas/terms_default'
        tax_id:
          description: 'Tax ID for the Sales Order. Note: This value is a set of numeric
            characters.'
          type: string
          example: '460000000017094'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        delivery_method:
          $ref: '#/components/schemas/delivery_method'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        template_id:
          $ref: '#/components/schemas/template_id'
        documents:
          $ref: '#/components/schemas/documents'
        zcrm_potential_id:
          $ref: '#/components/schemas/zcrm_potential_id'
        zcrm_potential_name:
          $ref: '#/components/schemas/zcrm_potential_name'
        tags:
          $ref: '#/components/schemas/tags'
    update-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Order Updated Successfully.
          readOnly: true
        salesorder:
          $ref: '#/components/schemas/salesorder'
    get-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        salesorder:
          type: array
          items:
            type: object
            properties:
              customer_id:
                $ref: '#/components/schemas/customer_id'
              billing_address_id:
                $ref: '#/components/schemas/billing_address_id'
              shipping_address_id:
                $ref: '#/components/schemas/shipping_address_id'
              is_pre_gst:
                $ref: '#/components/schemas/is_pre_gst'
              gst_no:
                $ref: '#/components/schemas/gst_no'
              gst_treatment:
                $ref: '#/components/schemas/gst_treatment'
              place_of_supply:
                $ref: '#/components/schemas/place_of_supply'
              crm_owner_id:
                $ref: '#/components/schemas/crm_owner_id'
              crm_custom_reference_id:
                $ref: '#/components/schemas/crm_custom_reference_id'
              zcrm_potential_id:
                $ref: '#/components/schemas/zcrm_potential_id'
              vat_treatment:
                $ref: '#/components/schemas/vat_treatment'
              tax_treatment:
                $ref: '#/components/schemas/tax_treatment'
              is_reverse_charge_applied:
                $ref: '#/components/schemas/is_reverse_charge_applied'
              is_update_customer:
                $ref: '#/components/schemas/is_update_customer'
              contact_persons_associated:
                $ref: '#/components/schemas/contact_persons_associated'
              salesorder_number:
                $ref: '#/components/schemas/salesorder_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              template_id:
                $ref: '#/components/schemas/template_id'
              date:
                description: Date of the comment
                type: string
              documents:
                $ref: '#/components/schemas/documents'
              shipment_date:
                $ref: '#/components/schemas/shipment_date'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              discount:
                $ref: '#/components/schemas/discount'
              is_discount_before_tax:
                $ref: '#/components/schemas/is_discount_before_tax'
              discount_type:
                $ref: '#/components/schemas/discount_type'
              salesperson_id:
                $ref: '#/components/schemas/salesperson_id'
              salesperson_name:
                $ref: '#/components/schemas/salesperson_name'
              merchant_id:
                $ref: '#/components/schemas/merchant_id'
              merchant_name:
                $ref: '#/components/schemas/merchant_name'
              estimate_id:
                $ref: '#/components/schemas/estimate_id'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_authority_id:
                $ref: '#/components/schemas/tax_authority_id'
              tax_authority_name:
                $ref: '#/components/schemas/tax_authority_name'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              avatax_exempt_no:
                $ref: '#/components/schemas/avatax_exempt_no'
              avatax_use_code:
                $ref: '#/components/schemas/avatax_use_code'
              is_inclusive_tax:
                $ref: '#/components/schemas/is_inclusive_tax'
              sub_statuses:
                $ref: '#/components/schemas/sub_statuses'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              line_items:
                description: Details of the line items in the Sales Order
                type: array
                items:
                  type: object
                  properties:
                    line_item_id:
                      description: 'ID of the line item. Note: This value is a set
                        of numeric characters.'
                      type: string
                      example: '460000000039131'
                    sku:
                      $ref: '#/components/schemas/sku'
                    bcy_rate:
                      $ref: '#/components/schemas/bcy_rate'
                    tax_name:
                      $ref: '#/components/schemas/tax_name'
                    tax_type:
                      $ref: '#/components/schemas/tax_type'
                    tax_percentage:
                      $ref: '#/components/schemas/tax_percentage'
                    tax_id:
                      $ref: '#/components/schemas/tax_id'
                    tds_tax_id:
                      $ref: '#/components/schemas/tds_tax_id'
                    tax_treatment_code:
                      $ref: '#/components/schemas/tax_treatment_code'
                    is_taxable:
                      description: Check if the Sales Order is taxable.
                      type: boolean
                      example: false
                      x-node_available_in:
                      - us
                      x-node_unavailable_in: []
                    product_exemption_id:
                      $ref: '#/components/schemas/product_exemption_id'
                    product_exemption_code:
                      $ref: '#/components/schemas/product_exemption_code'
                    avatax_use_code_id:
                      $ref: '#/components/schemas/avatax_use_code_id'
                    avatax_use_code_desc:
                      $ref: '#/components/schemas/avatax_use_code_desc'
                    avatax_tax_code_id:
                      $ref: '#/components/schemas/avatax_tax_code_id'
                    avatax_tax_code_desc:
                      $ref: '#/components/schemas/avatax_tax_code_desc'
                    item_total_inclusive_of_tax:
                      $ref: '#/components/schemas/item_total_inclusive_of_tax'
                    product_type:
                      $ref: '#/components/schemas/product_type'
                    hsn_or_sac:
                      $ref: '#/components/schemas/hsn_or_sac'
                    sat_item_key_code:
                      $ref: '#/components/schemas/sat_item_key_code'
                    unitkey_code:
                      $ref: '#/components/schemas/unitkey_code'
                    is_invoiced:
                      $ref: '#/components/schemas/is_invoiced'
                    stock_on_hand:
                      $ref: '#/components/schemas/stock_on_hand'
                    image_id:
                      $ref: '#/components/schemas/image_id'
                    image_name:
                      $ref: '#/components/schemas/image_name'
                    image_type:
                      $ref: '#/components/schemas/image_type'
                    project_id:
                      $ref: '#/components/schemas/project_id'
                    project_name:
                      $ref: '#/components/schemas/project_name'
                    location_id:
                      $ref: '#/components/schemas/location_id'
                    location_name:
                      $ref: '#/components/schemas/location_name'
                    tags:
                      $ref: '#/components/schemas/tags_response'
              pricebook_id:
                $ref: '#/components/schemas/pricebook_id'
              shipping_charge:
                $ref: '#/components/schemas/shipping_charge'
              adjustment:
                $ref: '#/components/schemas/adjustment'
              adjustment_description:
                $ref: '#/components/schemas/adjustment_description'
              delivery_method:
                $ref: '#/components/schemas/delivery_method'
              notes:
                $ref: '#/components/schemas/notes'
              notes_default:
                $ref: '#/components/schemas/notes_default'
              terms:
                $ref: '#/components/schemas/terms'
              terms_default:
                $ref: '#/components/schemas/terms_default'
              tags:
                $ref: '#/components/schemas/tags_response'
    delete-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Order Deleted Successfully.
          readOnly: true
    mark-a-sales-order-as-open-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales order status has been changed to Open.
          readOnly: true
    mark-a-sales-order-as-void-request:
      type: object
      properties:
        reason:
          $ref: '#/components/schemas/reason'
    mark-a-sales-order-as-void-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales order status has been changed to Void.
          readOnly: true
    email-a-sales-order-request:
      required:
      - to_mail_ids
      - subject
      - body
      type: object
      properties:
        from_address_id:
          $ref: '#/components/schemas/from_address_id'
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        bcc_mail_ids:
          $ref: '#/components/schemas/bcc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        documents:
          description: Documents of the Sales Order
          type: string
          format: binary
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        body:
          $ref: '#/components/schemas/body'
    email-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your Sales Order has been sent.
          readOnly: true
    get-sales-order-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/data'
    submit-a-sales-order-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The Sales Order has been successfully submitted for approval.
          readOnly: true
    approve-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the Sales Order.
          readOnly: true
    bulk-export-sales-orders-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    bulk-print-sales-orders-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    list-sales-order-templates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          $ref: '#/components/schemas/templates'
    update-sales-order-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Order Updated Successfully.
          readOnly: true
    get-a-sales-order-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Success
          readOnly: true
    add-attachment-to-a-sales-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your file has been successfully attached to the sales order.
          readOnly: true
    update-attachment-preference-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Purchase Order has been updated.
          readOnly: true
    delete-an-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The attachment has been deleted.
          readOnly: true
    list-sales-order-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments: {}
    add-comment-request:
      type: object
      properties:
        description:
          type: string
          description: It displays the description of the comment added for the fixed
            asset
          example: Fixed asset is in good condition
    add-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comment has been added
          readOnly: true
        comment:
          type: object
          properties:
            comment_id:
              $ref: '#/components/schemas/comment_id'
            description:
              $ref: '#/components/schemas/description'
            commented_by_id:
              $ref: '#/components/schemas/commented_by_id'
            commented_by:
              $ref: '#/components/schemas/commented_by'
            comment_type:
              $ref: '#/components/schemas/comment_type'
            date:
              $ref: '#/components/schemas/date'
            date_description:
              $ref: '#/components/schemas/date_description'
            time:
              $ref: '#/components/schemas/time'
            operation_type:
              $ref: '#/components/schemas/operation_type'
    invoice-response:
      type: object
      properties:
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        ach_payment_initiated:
          $ref: '#/components/schemas/ach_payment_initiated'
        invoice_number:
          $ref: '#/components/schemas/invoice_number'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        cfdi_reference_type:
          $ref: '#/components/schemas/cfdi_reference_type'
        reference_invoice_id:
          description: 'Associate the reference invoice. Note: This value is a set
            of numeric characters.'
          type: string
          example: '132738000000126013'
          x-node_available_in:
          - mx
          x-node_unavailable_in: []
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        date:
          $ref: '#/components/schemas/date'
        status:
          $ref: '#/components/schemas/status'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        due_date:
          $ref: '#/components/schemas/due_date'
        payment_expected_date:
          $ref: '#/components/schemas/payment_expected_date'
        last_payment_date:
          $ref: '#/components/schemas/last_payment_date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        discount:
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        recurring_invoice_id:
          $ref: '#/components/schemas/recurring_invoice_id'
        is_viewed_by_client:
          $ref: '#/components/schemas/is_viewed_by_client'
        has_attachment:
          $ref: '#/components/schemas/has_attachment'
        client_viewed_time:
          $ref: '#/components/schemas/client_viewed_time'
        line_items:
          type: array
          description: List of line items in the invoice.
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              project_name:
                $ref: '#/components/schemas/project_name'
              time_entry_ids:
                $ref: '#/components/schemas/time_entry_ids'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              item_type:
                $ref: '#/components/schemas/item_type'
              product_type:
                $ref: '#/components/schemas/product_type'
              expense_id:
                $ref: '#/components/schemas/expense_id'
              expense_receipt_name:
                $ref: '#/components/schemas/expense_receipt_name'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              item_order:
                $ref: '#/components/schemas/item_order'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              unit:
                $ref: '#/components/schemas/unit'
              discount_amount:
                $ref: '#/components/schemas/discount_amount'
              discount:
                $ref: '#/components/schemas/discount'
              tags:
                $ref: '#/components/schemas/tags_response'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              item_total:
                $ref: '#/components/schemas/item_total'
              header_name:
                $ref: '#/components/schemas/header_name'
              header_id:
                $ref: '#/components/schemas/header_id'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        tax_total:
          $ref: '#/components/schemas/tax_total'
        total:
          $ref: '#/components/schemas/total'
        taxes:
          $ref: '#/components/schemas/taxes'
        payment_reminder_enabled:
          $ref: '#/components/schemas/payment_reminder_enabled'
        payment_made:
          $ref: '#/components/schemas/payment_made'
        credits_applied:
          $ref: '#/components/schemas/credits_applied'
        tax_amount_withheld:
          $ref: '#/components/schemas/tax_amount_withheld'
        balance:
          $ref: '#/components/schemas/balance'
        write_off_amount:
          $ref: '#/components/schemas/write_off_amount'
        allow_partial_payments:
          $ref: '#/components/schemas/allow_partial_payments'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        is_emailed:
          $ref: '#/components/schemas/is_emailed'
        reminders_sent:
          $ref: '#/components/schemas/reminders_sent'
        last_reminder_sent_date:
          $ref: '#/components/schemas/last_reminder_sent_date'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/update-shipping-address-request'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        template_id:
          $ref: '#/components/schemas/template_id'
        template_name:
          $ref: '#/components/schemas/template_name'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        attachment_name:
          $ref: '#/components/schemas/attachment_name'
        can_send_in_mail:
          $ref: '#/components/schemas/can_send_in_mail'
        salesperson_id:
          $ref: '#/components/schemas/salesperson_id'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        invoice_url:
          $ref: '#/components/schemas/invoice_url'
        tags:
          $ref: '#/components/schemas/tags_response'
    cfdi_usage:
      description: 'Choose CFDI Usage.</br>Allowed values: <code>acquisition_of_merchandise</code>,
        <code>return_discount_bonus</code>, <code>general_expense</code>, <code>buildings</code>,
        <code>furniture_office_equipment</code>, <code>transport_equipment</code>,
        <code>computer_equipmentdye_molds_tools</code>, <code>telephone_communication</code>,
        <code>satellite_communication</code>, <code>other_machinery_equipment</code>,
        <code>hospital_expense</code>, <code>medical_expense_disability</code>, <code>funeral_expense</code>,
        <code>donation</code>, <code>interest_mortage_loans</code>, <code>contribution_sar</code>,
        <code>medical_expense_insurance_pormium</code>, <code>school_transportation_expense</code>,
        <code>deposit_saving_account</code>, <code>payment_educational_service</code>,
        <code>no_tax_effect</code>, <code>payment</code>, <code>payroll</code>.'
      type: string
      example: acquisition_of_merchandise
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    cfdi_reference_type:
      description: 'Choose CFDI Reference Type.</br>Allowed values: <code>credit_note</code>,
        <code>debit_note</code>, <code>return_of_merchandise</code>, <code>substitution_previous_cfdi</code>,
        <code>transfer_of_goods</code>, <code>invoice_generated_from_order</code>,
        <code>cfdi_for_advance</code>.'
      type: string
      example: return_of_merchandise
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    reference_invoice_id:
      type: string
      description: Invoice ID
      example: '982000000567005'
    item_type:
      description: 'Type of the item. Allowed values: <code>sales</code>,<code>purchases</code>,<code>sales_and_purchases</code>
        and <code>inventory</code>. Default value will be <code>sales</code>.'
      type: string
      example: ' '
    online_transaction_id:
      type: string
      example: '7843653940324085'
    credits:
      description: The list of credits
      type: array
      items:
        type: string
    creditnotes_invoice_id:
      description: 'The creditnotes invoice id. Note: This value is a set of numeric
        characters.'
      type: string
      example: '982000000567172'
    creditnotes_number:
      description: The creditnotes Number
      type: string
      example: CN-00001
    credited_date:
      description: The date of credit
      type: string
      example: '2013-11-18'
    amount_applied:
      description: Amount applied to the Entity
      type: number
      format: double
      example: 150
    payments:
      type: array
      items:
        type: object
        properties:
          payment_id:
            $ref: '#/components/schemas/payment_id'
          bill_id:
            $ref: '#/components/schemas/bill_id'
          bill_payment_id:
            $ref: '#/components/schemas/bill_payment_id'
          payment_mode:
            $ref: '#/components/schemas/payment_mode'
          payment_number:
            $ref: '#/components/schemas/payment_number'
          description:
            $ref: '#/components/schemas/description'
          date:
            $ref: '#/components/schemas/date'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          exchange_rate:
            $ref: '#/components/schemas/exchange_rate'
          amount:
            $ref: '#/components/schemas/amount'
          paid_through_account_id:
            $ref: '#/components/schemas/paid_through_account_id'
          paid_through_account_name:
            $ref: '#/components/schemas/paid_through_account_name'
          is_single_bill_payment:
            $ref: '#/components/schemas/is_single_bill_payment'
          is_paid_via_print_check:
            $ref: '#/components/schemas/is_paid_via_print_check'
          check_details:
            $ref: '#/components/schemas/check_details'
          is_ach_payment:
            $ref: '#/components/schemas/is_ach_payment'
          ach_payment_status:
            $ref: '#/components/schemas/ach_payment_status'
          ach_gw_transaction_id:
            $ref: '#/components/schemas/ach_gw_transaction_id'
          filed_in_vat_return_id:
            $ref: '#/components/schemas/filed_in_vat_return_id'
          filed_in_vat_return_name:
            $ref: '#/components/schemas/filed_in_vat_return_name'
          filed_in_vat_return_type:
            $ref: '#/components/schemas/filed_in_vat_return_type'
    payment_id:
      description: ID of the Payment
      type: string
      example: '460000000076027'
    payment_number:
      description: Number of the Payment
      type: integer
      example: 8
    invoice_payment_id:
      description: ID of the Invoice Payment
      type: string
      example: '460000000134123'
    payment_mode:
      description: Payment Mode Associated with the Rule
      type: string
      example: Cash
    amount:
      description: Amount of the opening balance.
      type: number
      format: double
      example: 2000
    is_single_invoice_payment:
      description: boolean check
      type: boolean
      example: true
    gateways_configured:
      type: boolean
      example: true
    deprecated_placeholders_used:
      type: array
      items:
        type: string
      example: []
    invoice_ids:
      description: 'Comma separated invoice ids which are to be emailed. Note: This
        value is a set of numeric characters separated by comma.'
      type: string
    invoiced_estimate_id:
      description: 'Unique identifier of the estimate that was converted to create
        this invoice. Use this field when creating an invoice from an existing estimate.
        The `invoiced_estimate_id` links this invoice back to its source estimate,
        enabling tracking of the conversion process and maintaining the relationship
        between estimates and invoices. Note: This value is a set of numeric characters.'
      type: string
      example: ' '
    salesorder_order_status:
      type: string
      example: closed
    total_formatted:
      type: string
      example: ₹10,000.00
    sub_total_formatted:
      type: string
      example: ₹10,000.00
    invoice_url:
      type: string
      example: https://invoice.zoho.com/SecurePayment?CInvoiceID=23d84d0cf64f9a72ea0c66fded25a08c8bafd0ab508aff05323a9f80e2cd03fdc5dd568d3d6407bbda969d3e870d740b6fce549a9438c4ea
    gateways_associated:
      type: boolean
      example: true
    attach_pdf:
      type: boolean
      example: true
    reminders_sent:
      description: The no of reminders sent
      type: integer
      example: 1
    last_reminder_sent_date:
      description: The date the last email was sent
      type: string
      example: ' '
    payment_made:
      description: Amount of Payment made
      type: integer
      example: 0
    credits_applied:
      description: The credits applied
      type: number
      format: float
      example: 22.43
    tax_amount_withheld:
      description: Amount of tax withheld for the transaction.
      type: number
      format: double
      example: 0
    write_off_amount:
      description: The write off amount
      type: number
      format: float
      example: 0
    account_id:
      description: ID of account for which you need to record opening balance.
      type: string
      example: '460000000000358'
    allow_partial_payments:
      description: Boolean to check if partial payments are allowed for the contact
      type: boolean
      example: true
    configured:
      description: Boolean check to see if a payment gateway ahs been configured
      type: boolean
      example: true
    additional_field1:
      description: 'Paypal payment method. Allowed Values: <code>standard</code> and
        <code>adaptive</code>'
      type: string
      example: standard
    time_entry_ids:
      description: ID of the time entries to be deleted.
      type: string
      example: ' '
    expense_id:
      type: string
      example: 982000000567250
    bill_id:
      description: ID of the Bill
      type: string
      example: '460000000053199'
    bill_item_id:
      type: string
      description: 'Unique identifier of the specific bill line item to be converted
        to an invoice line item. This allows you to convert individual line items
        from a bill rather than the entire bill, providing granular control over bill-to-invoice
        conversion. Note: This value is a set of numeric characters.'
      example: ' '
    expense_receipt_name:
      type: string
      example: ' '
    salesorder_item_id:
      description: 'Unique identifier of the sales order line item that is being converted
        to this invoice line item. Use this field when creating an invoice from an
        existing sales order. The `salesorder_item_id` links this invoice line item
        back to its source sales order line item, enabling tracking of the conversion
        process and maintaining the relationship between sales orders and invoices.
        Note: This value is a set of numeric characters.'
      type: string
      example: ' '
    recurring_invoice_id:
      description: Unique ID of the recurring invoice generated by the server.
      type: string
      example: '90300000072369'
    response_option:
      description: Transaction total count
      type: integer
      example: 2
    invoices:
      description: List of invoices associated with the customer payment.
      type: array
      items:
        type: object
        properties:
          invoice_payment_id:
            $ref: '#/components/schemas/invoice_payment_id'
          invoice_id:
            $ref: '#/components/schemas/invoice_id'
          amount_applied:
            $ref: '#/components/schemas/amount_applied'
          tax_amount_withheld:
            $ref: '#/components/schemas/tax_amount_withheld'
          discount_amount:
            $ref: '#/components/schemas/discount_amount'
    invoice_number:
      type: string
      example: INV-00004
    due_date:
      description: Due date of the task.
      type: string
      format: date
      example: '2025-12-31'
    due_days:
      type: string
      example: Due Today
    balance:
      type: string
      example: '48.75'
    payment_expected_date:
      description: Expected Payment date of the Bill
      type: string
    last_payment_date:
      description: The last payment date of the retainer invoice
      type: string
      example: ' '
    ach_payment_initiated:
      type: boolean
      description: Boolean value to check if ACH payment has been initiated for the
        invoice.
      example: false
    schedule_time:
      type: string
      description: The scheduled time for the invoice to be sent to the customer.
      example: ''
    invoice_payments:
      description: List of payments to be applied to the invoice.
      type: array
      items:
        type: object
        properties:
          payment_id:
            $ref: '#/components/schemas/payment_id'
          amount_applied:
            $ref: '#/components/schemas/amount_applied'
    apply_creditnotes:
      description: List of credit notes to be applied to the invoice.
      type: array
      items:
        type: object
        properties:
          creditnote_id:
            $ref: '#/components/schemas/creditnote_id'
          amount_applied:
            $ref: '#/components/schemas/amount_applied'
    gateway_name:
      description: 'Name of the payment gateway associated with the retainer invoice.
        E.g. paypal, stripe.Allowed Values: <code>paypal</code>, <code>authorize_net</code>,
        <code>payflow_pro</code>, <code>stripe</code>, <code>2checkout</code> and
        <code>braintree</code>'
      type: string
      example: paypal
    payment_gateways:
      description: Online payment gateways through which payment can be made.
      type: array
      items:
        type: object
        properties:
          configured:
            $ref: '#/components/schemas/configured'
          additional_field1:
            $ref: '#/components/schemas/additional_field1'
          gateway_name:
            $ref: '#/components/schemas/gateway_name'
    payment_options:
      description: Payment options for the retainer invoice, online payment gateways
        and bank accounts. Will be displayed in the pdf.
      type: object
      properties:
        payment_gateways:
          $ref: '#/components/schemas/payment_gateways'
    header_name:
      type: string
      description: Header name
      example: Header 1
    header_id:
      type: string
      description: Header ID
      example: '982000000567105'
    create-an-invoice-request:
      required:
      - customer_id
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons:
          $ref: '#/components/schemas/contact_persons'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        invoice_number:
          $ref: '#/components/schemas/invoice_number'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        template_id:
          $ref: '#/components/schemas/template_id'
        date:
          $ref: '#/components/schemas/date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        due_date:
          $ref: '#/components/schemas/due_date'
        discount:
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        location_id:
          $ref: '#/components/schemas/location_id'
        recurring_invoice_id:
          $ref: '#/components/schemas/recurring_invoice_id'
        invoiced_estimate_id:
          $ref: '#/components/schemas/invoiced_estimate_id'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
        send:
          $ref: '#/components/schemas/send'
        line_items:
          type: array
          description: List of line items in the invoice.
          items:
            type: object
            required:
            - item_id
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              time_entry_ids:
                $ref: '#/components/schemas/time_entry_ids'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              location_id:
                $ref: '#/components/schemas/location_id'
              expense_id:
                $ref: '#/components/schemas/expense_id'
              bill_id:
                $ref: '#/components/schemas/bill_id'
              bill_item_id:
                $ref: '#/components/schemas/bill_item_id'
              expense_receipt_name:
                $ref: '#/components/schemas/expense_receipt_name'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              item_order:
                $ref: '#/components/schemas/item_order'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              unit:
                $ref: '#/components/schemas/unit'
              discount_amount:
                $ref: '#/components/schemas/discount_amount'
              discount:
                $ref: '#/components/schemas/discount'
              tags:
                $ref: '#/components/schemas/tags'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              header_name:
                $ref: '#/components/schemas/header_name'
              salesorder_item_id:
                $ref: '#/components/schemas/salesorder_item_id'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        allow_partial_payments:
          $ref: '#/components/schemas/allow_partial_payments'
        custom_body:
          $ref: '#/components/schemas/custom_body'
        custom_subject:
          $ref: '#/components/schemas/custom_subject'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        reason:
          $ref: '#/components/schemas/reason'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        billing_address_id:
          $ref: '#/components/schemas/billing_address_id'
        shipping_address_id:
          $ref: '#/components/schemas/shipping_address_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        expense_id:
          $ref: '#/components/schemas/expense_id'
        salesorder_item_id:
          $ref: '#/components/schemas/salesorder_item_id'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
        time_entry_ids:
          $ref: '#/components/schemas/time_entry_ids'
        batch_payments:
          description: Array of batch payment details to be processed with the invoice.
            Each payment includes payment mode, amount, and account information for
            automated payment processing.
          type: array
          items:
            type: object
            properties:
              payment_mode:
                description: Mode of payment for this batch payment entry.
                type: string
                example: Cash
              amount:
                description: Payment amount for this batch payment entry.
                type: string
                example: '100.00'
              account_id:
                description: 'Account ID where the payment will be recorded. Note:
                  This value is a set of numeric characters.'
                type: string
                example: '1000000095342'
          example:
          - payment_mode: Cash
            amount: '100.00'
            account_id: '1000000095342'
    payment-link-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          type: object
          properties:
            share_link:
              type: string
              example: 'https://zohosecurepay.com/books/Payment/secure?CInvoiceID=2-52411db153ee1f4a9ac8ab0dec95f4711f1edb75edc7d550dd3fcb6e100852aa25a28b345d8b67fc967d69ebdba5fc240fd51fd9007c6339e4015cb6e3eadfffa489615aa926242e '
              readOnly: true
    create-an-instant-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The invoice has been created.
          readOnly: true
        invoice:
          type: object
          properties:
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            ach_payment_initiated:
              $ref: '#/components/schemas/ach_payment_initiated'
            invoice_number:
              $ref: '#/components/schemas/invoice_number'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            cfdi_reference_type:
              $ref: '#/components/schemas/cfdi_reference_type'
            reference_invoice_id:
              description: 'Associate the reference invoice. Note: This value is a
                set of numeric characters.'
              type: string
              example: '132738000000126013'
              x-node_available_in:
              - mx
              x-node_unavailable_in: []
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            payment_terms:
              $ref: '#/components/schemas/payment_terms'
            payment_terms_label:
              $ref: '#/components/schemas/payment_terms_label'
            due_date:
              $ref: '#/components/schemas/due_date'
            payment_expected_date:
              $ref: '#/components/schemas/payment_expected_date'
            last_payment_date:
              $ref: '#/components/schemas/last_payment_date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            discount:
              $ref: '#/components/schemas/discount'
            is_discount_before_tax:
              $ref: '#/components/schemas/is_discount_before_tax'
            discount_type:
              $ref: '#/components/schemas/discount_type'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            recurring_invoice_id:
              $ref: '#/components/schemas/recurring_invoice_id'
            is_viewed_by_client:
              $ref: '#/components/schemas/is_viewed_by_client'
            has_attachment:
              $ref: '#/components/schemas/has_attachment'
            client_viewed_time:
              $ref: '#/components/schemas/client_viewed_time'
            line_items:
              type: array
              description: List of line items in the invoice.
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  item_id:
                    $ref: '#/components/schemas/item_id'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  sat_item_key_code:
                    $ref: '#/components/schemas/sat_item_key_code'
                  unitkey_code:
                    $ref: '#/components/schemas/unitkey_code'
                  project_name:
                    $ref: '#/components/schemas/project_name'
                  time_entry_ids:
                    $ref: '#/components/schemas/time_entry_ids'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  item_type:
                    $ref: '#/components/schemas/item_type'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  expense_id:
                    $ref: '#/components/schemas/expense_id'
                  expense_receipt_name:
                    $ref: '#/components/schemas/expense_receipt_name'
                  name:
                    $ref: '#/components/schemas/name'
                  description:
                    $ref: '#/components/schemas/description'
                  item_order:
                    $ref: '#/components/schemas/item_order'
                  bcy_rate:
                    $ref: '#/components/schemas/bcy_rate'
                  rate:
                    $ref: '#/components/schemas/rate'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  unit:
                    $ref: '#/components/schemas/unit'
                  discount_amount:
                    $ref: '#/components/schemas/discount_amount'
                  discount:
                    $ref: '#/components/schemas/discount'
                  tags:
                    $ref: '#/components/schemas/tags_response'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tds_tax_id:
                    $ref: '#/components/schemas/tds_tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  header_name:
                    $ref: '#/components/schemas/header_name'
                  header_id:
                    $ref: '#/components/schemas/header_id'
            shipping_charge:
              $ref: '#/components/schemas/shipping_charge'
            adjustment:
              $ref: '#/components/schemas/adjustment'
            adjustment_description:
              $ref: '#/components/schemas/adjustment_description'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            tax_total:
              $ref: '#/components/schemas/tax_total'
            total:
              $ref: '#/components/schemas/total'
            taxes:
              $ref: '#/components/schemas/taxes'
            payment_reminder_enabled:
              $ref: '#/components/schemas/payment_reminder_enabled'
            payment_made:
              $ref: '#/components/schemas/payment_made'
            credits_applied:
              $ref: '#/components/schemas/credits_applied'
            tax_amount_withheld:
              $ref: '#/components/schemas/tax_amount_withheld'
            balance:
              $ref: '#/components/schemas/balance'
            write_off_amount:
              $ref: '#/components/schemas/write_off_amount'
            allow_partial_payments:
              $ref: '#/components/schemas/allow_partial_payments'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            payment_options:
              $ref: '#/components/schemas/payment_options'
            is_emailed:
              $ref: '#/components/schemas/is_emailed'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            reminders_sent:
              $ref: '#/components/schemas/reminders_sent'
            last_reminder_sent_date:
              $ref: '#/components/schemas/last_reminder_sent_date'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/update-shipping-address-request'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            tags:
              $ref: '#/components/schemas/tags_response'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            can_send_in_mail:
              $ref: '#/components/schemas/can_send_in_mail'
            salesperson_id:
              $ref: '#/components/schemas/salesperson_id'
            salesperson_name:
              $ref: '#/components/schemas/salesperson_name'
            salesorder_id:
              $ref: '#/components/schemas/salesorder_id'
            salesorder_number:
              $ref: '#/components/schemas/salesorder_number'
            salesorders:
              type: object
              properties:
                salesorder_id:
                  $ref: '#/components/schemas/salesorder_id'
                salesorder_number:
                  $ref: '#/components/schemas/salesorder_number'
                reference_number:
                  $ref: '#/components/schemas/reference_number'
                crm_custom_reference_id:
                  $ref: '#/components/schemas/crm_custom_reference_id'
                salesorder_order_status:
                  $ref: '#/components/schemas/salesorder_order_status'
                total:
                  $ref: '#/components/schemas/total'
                total_formatted:
                  $ref: '#/components/schemas/total_formatted'
                sub_total:
                  $ref: '#/components/schemas/sub_total'
                sub_total_formatted:
                  $ref: '#/components/schemas/sub_total_formatted'
                date:
                  $ref: '#/components/schemas/date'
                shipment_date:
                  $ref: '#/components/schemas/shipment_date'
            invoice_url:
              $ref: '#/components/schemas/invoice_url'
    create-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The invoice has been created.
          readOnly: true
        invoice:
          $ref: '#/components/schemas/invoice-response'
    map-invoice-with-salesorder-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The invoices selected have been associated with their sales orders.
          readOnly: true
        errors:
          description: List of validation errors encountered during the mapping process.
            Each error contains details about which invoices failed and why.
          type: array
          items:
            type: object
            properties:
              code:
                description: Error code indicating the type of validation failure
                type: integer
                example: 4001
              message:
                description: Human-readable error message explaining the validation
                  failure
                type: string
                example: Customer mismatch between invoice and sales order
              ids:
                description: Array of invoice IDs that encountered this specific error
                type: array
                items:
                  type: string
                example:
                - '982000000567114'
                - '982000000567115'
          example:
          - code: 4001
            message: Customer mismatch between invoice and sales order
            ids:
            - '982000000567114'
          - code: 4002
            message: Invoice is already linked to another sales order
            ids:
            - '982000000567115'
          - code: 4003
            message: Sales order is in void status and cannot be linked
            ids:
            - '982000000567116'
    list-invoices-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        invoices:
          type: array
          items:
            type: object
            properties:
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              status:
                $ref: '#/components/schemas/status'
              invoice_number:
                $ref: '#/components/schemas/invoice_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              date:
                $ref: '#/components/schemas/date'
              due_date:
                $ref: '#/components/schemas/due_date'
              total:
                $ref: '#/components/schemas/total'
              balance:
                $ref: '#/components/schemas/balance'
              created_time:
                $ref: '#/components/schemas/created_time'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            sort_order:
              $ref: '#/components/schemas/sort_order'
            sort_column:
              $ref: '#/components/schemas/sort_column'
    update-an-invoice-request:
      required:
      - customer_id
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        invoice_number:
          $ref: '#/components/schemas/invoice_number'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        cfdi_reference_type:
          $ref: '#/components/schemas/cfdi_reference_type'
        reference_invoice_id:
          $ref: '#/components/schemas/reference_invoice_id'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        template_id:
          $ref: '#/components/schemas/template_id'
        date:
          $ref: '#/components/schemas/date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        due_date:
          $ref: '#/components/schemas/due_date'
        discount:
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        recurring_invoice_id:
          $ref: '#/components/schemas/recurring_invoice_id'
        invoiced_estimate_id:
          $ref: '#/components/schemas/invoiced_estimate_id'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          type: array
          description: List of line items in the invoice.
          items:
            type: object
            required:
            - item_id
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              time_entry_ids:
                $ref: '#/components/schemas/time_entry_ids'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              location_id:
                $ref: '#/components/schemas/location_id'
              expense_id:
                $ref: '#/components/schemas/expense_id'
              expense_receipt_name:
                $ref: '#/components/schemas/expense_receipt_name'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              item_order:
                $ref: '#/components/schemas/item_order'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              unit:
                $ref: '#/components/schemas/unit'
              discount_amount:
                $ref: '#/components/schemas/discount_amount'
              tags:
                $ref: '#/components/schemas/tags'
              discount:
                $ref: '#/components/schemas/discount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              header_name:
                $ref: '#/components/schemas/header_name'
              header_id:
                $ref: '#/components/schemas/header_id'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        allow_partial_payments:
          $ref: '#/components/schemas/allow_partial_payments'
        custom_body:
          $ref: '#/components/schemas/custom_body'
        custom_subject:
          $ref: '#/components/schemas/custom_subject'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        reason:
          $ref: '#/components/schemas/reason'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        expense_id:
          $ref: '#/components/schemas/expense_id'
        salesorder_item_id:
          $ref: '#/components/schemas/salesorder_item_id'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
        line_item_id:
          $ref: '#/components/schemas/line_item_id'
    update-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Invoice information has been updated.
          readOnly: true
        invoice:
          type: object
          properties:
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            ach_payment_initiated:
              $ref: '#/components/schemas/ach_payment_initiated'
            invoice_number:
              $ref: '#/components/schemas/invoice_number'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            cfdi_reference_type:
              $ref: '#/components/schemas/cfdi_reference_type'
            reference_invoice_id:
              $ref: '#/components/schemas/reference_invoice_id'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            payment_terms:
              $ref: '#/components/schemas/payment_terms'
            payment_terms_label:
              $ref: '#/components/schemas/payment_terms_label'
            due_date:
              $ref: '#/components/schemas/due_date'
            payment_expected_date:
              $ref: '#/components/schemas/payment_expected_date'
            last_payment_date:
              $ref: '#/components/schemas/last_payment_date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            discount:
              $ref: '#/components/schemas/discount'
            is_discount_before_tax:
              $ref: '#/components/schemas/is_discount_before_tax'
            discount_type:
              $ref: '#/components/schemas/discount_type'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            recurring_invoice_id:
              $ref: '#/components/schemas/recurring_invoice_id'
            is_viewed_by_client:
              $ref: '#/components/schemas/is_viewed_by_client'
            has_attachment:
              $ref: '#/components/schemas/has_attachment'
            client_viewed_time:
              $ref: '#/components/schemas/client_viewed_time'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_items:
              type: array
              description: List of line items in the invoice.
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  item_id:
                    $ref: '#/components/schemas/item_id'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  project_name:
                    $ref: '#/components/schemas/project_name'
                  time_entry_ids:
                    $ref: '#/components/schemas/time_entry_ids'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  item_type:
                    $ref: '#/components/schemas/item_type'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  sat_item_key_code:
                    $ref: '#/components/schemas/sat_item_key_code'
                  unitkey_code:
                    $ref: '#/components/schemas/unitkey_code'
                  expense_id:
                    $ref: '#/components/schemas/expense_id'
                  expense_receipt_name:
                    $ref: '#/components/schemas/expense_receipt_name'
                  name:
                    $ref: '#/components/schemas/name'
                  description:
                    $ref: '#/components/schemas/description'
                  item_order:
                    $ref: '#/components/schemas/item_order'
                  bcy_rate:
                    $ref: '#/components/schemas/bcy_rate'
                  rate:
                    $ref: '#/components/schemas/rate'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  unit:
                    $ref: '#/components/schemas/unit'
                  discount_amount:
                    $ref: '#/components/schemas/discount_amount'
                  discount:
                    $ref: '#/components/schemas/discount'
                  tags:
                    $ref: '#/components/schemas/tags_response'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tds_tax_id:
                    $ref: '#/components/schemas/tds_tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  header_name:
                    $ref: '#/components/schemas/header_name'
                  header_id:
                    $ref: '#/components/schemas/header_id'
            shipping_charge:
              $ref: '#/components/schemas/shipping_charge'
            adjustment:
              $ref: '#/components/schemas/adjustment'
            adjustment_description:
              $ref: '#/components/schemas/adjustment_description'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            tax_total:
              $ref: '#/components/schemas/tax_total'
            total:
              $ref: '#/components/schemas/total'
            taxes:
              $ref: '#/components/schemas/taxes'
            payment_reminder_enabled:
              $ref: '#/components/schemas/payment_reminder_enabled'
            payment_made:
              $ref: '#/components/schemas/payment_made'
            credits_applied:
              $ref: '#/components/schemas/credits_applied'
            tax_amount_withheld:
              $ref: '#/components/schemas/tax_amount_withheld'
            balance:
              $ref: '#/components/schemas/balance'
            write_off_amount:
              $ref: '#/components/schemas/write_off_amount'
            allow_partial_payments:
              $ref: '#/components/schemas/allow_partial_payments'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            payment_options:
              $ref: '#/components/schemas/payment_options'
            is_emailed:
              $ref: '#/components/schemas/is_emailed'
            reminders_sent:
              $ref: '#/components/schemas/reminders_sent'
            last_reminder_sent_date:
              $ref: '#/components/schemas/last_reminder_sent_date'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            can_send_in_mail:
              $ref: '#/components/schemas/can_send_in_mail'
            salesperson_id:
              $ref: '#/components/schemas/salesperson_id'
            salesperson_name:
              $ref: '#/components/schemas/salesperson_name'
            invoice_url:
              $ref: '#/components/schemas/invoice_url'
    update-an-invoice-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    get-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        invoice:
          type: object
          properties:
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            ach_payment_initiated:
              $ref: '#/components/schemas/ach_payment_initiated'
            invoice_number:
              $ref: '#/components/schemas/invoice_number'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            payment_terms:
              $ref: '#/components/schemas/payment_terms'
            payment_terms_label:
              $ref: '#/components/schemas/payment_terms_label'
            due_date:
              $ref: '#/components/schemas/due_date'
            payment_expected_date:
              $ref: '#/components/schemas/payment_expected_date'
            last_payment_date:
              $ref: '#/components/schemas/last_payment_date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            discount:
              $ref: '#/components/schemas/discount'
            is_discount_before_tax:
              $ref: '#/components/schemas/is_discount_before_tax'
            discount_type:
              $ref: '#/components/schemas/discount_type'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            recurring_invoice_id:
              $ref: '#/components/schemas/recurring_invoice_id'
            is_viewed_by_client:
              $ref: '#/components/schemas/is_viewed_by_client'
            has_attachment:
              $ref: '#/components/schemas/has_attachment'
            client_viewed_time:
              $ref: '#/components/schemas/client_viewed_time'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_items:
              type: array
              description: List of line items in the invoice.
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  item_id:
                    $ref: '#/components/schemas/item_id'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  project_name:
                    $ref: '#/components/schemas/project_name'
                  time_entry_ids:
                    $ref: '#/components/schemas/time_entry_ids'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  item_type:
                    $ref: '#/components/schemas/item_type'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  expense_id:
                    $ref: '#/components/schemas/expense_id'
                  expense_receipt_name:
                    $ref: '#/components/schemas/expense_receipt_name'
                  name:
                    $ref: '#/components/schemas/name'
                  description:
                    $ref: '#/components/schemas/description'
                  item_order:
                    $ref: '#/components/schemas/item_order'
                  bcy_rate:
                    $ref: '#/components/schemas/bcy_rate'
                  rate:
                    $ref: '#/components/schemas/rate'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  unit:
                    $ref: '#/components/schemas/unit'
                  discount_amount:
                    $ref: '#/components/schemas/discount_amount'
                  discount:
                    $ref: '#/components/schemas/discount'
                  tags:
                    $ref: '#/components/schemas/tags_response'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  header_name:
                    $ref: '#/components/schemas/header_name'
                  header_id:
                    $ref: '#/components/schemas/header_id'
            shipping_charge:
              $ref: '#/components/schemas/shipping_charge'
            adjustment:
              $ref: '#/components/schemas/adjustment'
            adjustment_description:
              $ref: '#/components/schemas/adjustment_description'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            tax_total:
              $ref: '#/components/schemas/tax_total'
            total:
              $ref: '#/components/schemas/total'
            taxes:
              $ref: '#/components/schemas/taxes'
            payment_reminder_enabled:
              $ref: '#/components/schemas/payment_reminder_enabled'
            payment_made:
              $ref: '#/components/schemas/payment_made'
            credits_applied:
              $ref: '#/components/schemas/credits_applied'
            tax_amount_withheld:
              $ref: '#/components/schemas/tax_amount_withheld'
            balance:
              $ref: '#/components/schemas/balance'
            write_off_amount:
              $ref: '#/components/schemas/write_off_amount'
            allow_partial_payments:
              $ref: '#/components/schemas/allow_partial_payments'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            payment_options:
              $ref: '#/components/schemas/payment_options'
            is_emailed:
              $ref: '#/components/schemas/is_emailed'
            reminders_sent:
              $ref: '#/components/schemas/reminders_sent'
            last_reminder_sent_date:
              $ref: '#/components/schemas/last_reminder_sent_date'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            can_send_in_mail:
              $ref: '#/components/schemas/can_send_in_mail'
            salesperson_id:
              $ref: '#/components/schemas/salesperson_id'
            salesperson_name:
              $ref: '#/components/schemas/salesperson_name'
            invoice_url:
              $ref: '#/components/schemas/invoice_url'
    delete-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The invoice has been deleted.
          readOnly: true
    mark-an-invoice-as-sent-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Invoice status has been changed to Sent.
          readOnly: true
    void-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Invoice status has been changed to Void.
          readOnly: true
    mark-as-draft-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Status of retainer invoice changed from void to draft.
          readOnly: true
    email-an-invoice-request:
      required:
      - to_mail_ids
      type: object
      properties:
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
        from_address_id:
          $ref: '#/components/schemas/from_address_id'
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          $ref: '#/components/schemas/body'
    email-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your invoice has been sent.
          readOnly: true
    email-invoices-request:
      type: object
      properties:
        contacts:
          $ref: '#/components/schemas/contacts'
    email-invoices-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your mails are scheduled to be sent in a few minutes. We will notify
            you once it is done.
          readOnly: true
    submit-an-invoice-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The invoice has been submitted for approval successfully.
          readOnly: true
    approve-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the invoice.
          readOnly: true
    get-invoice-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          type: object
          properties:
            bcc_mails:
              $ref: '#/components/schemas/bcc_mails'
            gateways_configured:
              $ref: '#/components/schemas/gateways_configured'
            gateways_associated:
              $ref: '#/components/schemas/gateways_associated'
            bcc_mails_str:
              $ref: '#/components/schemas/bcc_mails_str'
            body:
              $ref: '#/components/schemas/body'
            documents:
              $ref: '#/components/schemas/documents'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            attach_pdf:
              $ref: '#/components/schemas/attach_pdf'
            entity_id:
              $ref: '#/components/schemas/entity_id'
            cc_mails_list:
              type: array
              items:
                type: object
                properties:
                  user_name:
                    $ref: '#/components/schemas/user_name'
                  email:
                    $ref: '#/components/schemas/email'
            file_name_without_extension:
              $ref: '#/components/schemas/file_name_without_extension'
            to_mails_str:
              $ref: '#/components/schemas/to_mails_str'
            cc_mails_str:
              $ref: '#/components/schemas/cc_mails_str'
            from_email:
              $ref: '#/components/schemas/from_email'
            from_address:
              $ref: '#/components/schemas/from_address'
            deprecated_placeholders_used:
              $ref: '#/components/schemas/deprecated_placeholders_used'
            error_list:
              $ref: '#/components/schemas/error_list'
            subject:
              $ref: '#/components/schemas/subject'
            emailtemplates:
              type: array
              items:
                type: object
                properties:
                  selected:
                    type: boolean
                    example: true
                  name:
                    description: The name of the line item. Max-length [100]
                    type: string
                    example: Default
                  email_template_id:
                    description: 'Get the email content based on a specific email
                      template. If this param is not inputted, then the content will
                      be based on the email template associated with the customer.
                      If no template is associated with the customer, then default
                      template will be used. Note: This value is a set of numeric
                      characters.'
                    type: string
                    example: '982000000000067'
            emailtemplate_documents:
              $ref: '#/components/schemas/emailtemplate_documents'
            to_contacts:
              type: array
              items:
                type: object
                properties:
                  first_name:
                    $ref: '#/components/schemas/first_name'
                  selected:
                    $ref: '#/components/schemas/selected'
                  phone:
                    $ref: '#/components/schemas/phone'
                  email:
                    $ref: '#/components/schemas/email'
                  last_name:
                    $ref: '#/components/schemas/last_name'
                  salutation:
                    $ref: '#/components/schemas/salutation'
                  contact_person_id:
                    $ref: '#/components/schemas/contact_person_id'
                  mobile:
                    $ref: '#/components/schemas/mobile'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            file_name:
              $ref: '#/components/schemas/file_name'
            from_emails:
              type: array
              items:
                type: object
                properties:
                  user_name:
                    $ref: '#/components/schemas/user_name'
                  selected:
                    $ref: '#/components/schemas/selected'
                  email:
                    $ref: '#/components/schemas/email'
                  organization_contact_id:
                    $ref: '#/components/schemas/organization_contact_id'
                  is_org_email_id:
                    $ref: '#/components/schemas/is_org_email_id'
            customer_id:
              $ref: '#/components/schemas/customer_id'
    remind-customer-request:
      required:
      - cc_mail_ids
      type: object
      properties:
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          description: The body of the mail
          type: string
          example: '<br>Dear Mr. John,&nbsp;<br><br>You might have missed the payment
            date and the invoice is now overdue by&nbsp;1&nbsp;days.<br><br>----------------------------------------------------------------------------------------<br><h2>Invoice#
            : INV-000004 </h2>Dated : 23 Dec 2016<br>----------------------------------------------------------------------------------------<br><b>&nbsp;Due
            Date &nbsp; &nbsp; &nbsp; &nbsp; : &nbsp;&nbsp;23 Dec 2016</b><br><b>&nbsp;Amount
            &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; : &nbsp;&nbsp;$139.65</b><br>----------------------------------------------------------------------------------------<br><br><span>Not
            to worry at all !&nbsp;</span>View your invoice and take the easy way
            out by making an&nbsp;<a href="https://books.zoho.com/portal/zilliuminc/index#/invoices/invoice/2000000007037
            ">online payment</a>.<br><br>If you have already paid, please accept our
            apologies and kindly ignore this payment reminder.<br><br><br>Regards,<br><br>David
            John<br>Zillium Inc<br><br><br>'
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
    remind-customer-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your payment reminder has been sent.
          readOnly: true
    get-payment-reminder-mail-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          type: object
          properties:
            bcc_mails:
              $ref: '#/components/schemas/bcc_mails'
            gateways_configured:
              $ref: '#/components/schemas/gateways_configured'
            gateways_associated:
              $ref: '#/components/schemas/gateways_associated'
            bcc_mails_str:
              $ref: '#/components/schemas/bcc_mails_str'
            body:
              description: The body of the mail
              type: string
              example: '<br>Dear Mr. John,&nbsp;<br><br>You might have missed the
                payment date and the invoice is now overdue by&nbsp;1&nbsp;days.<br><br>----------------------------------------------------------------------------------------<br><h2>Invoice#
                : INV-000004 </h2>Dated : 23 Dec 2016<br>----------------------------------------------------------------------------------------<br><b>&nbsp;Due
                Date &nbsp; &nbsp; &nbsp; &nbsp; : &nbsp;&nbsp;23 Dec 2016</b><br><b>&nbsp;Amount
                &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; : &nbsp;&nbsp;$139.65</b><br>----------------------------------------------------------------------------------------<br><br><span>Not
                to worry at all !&nbsp;</span>View your invoice and take the easy
                way out by making an&nbsp;<a href="https://books.zoho.com/portal/zilliuminc/index#/invoices/invoice/2000000007037
                ">online payment</a>.<br><br>If you have already paid, please accept
                our apologies and kindly ignore this payment reminder.<br><br><br>Regards,<br><br>David
                John<br>Zillium Inc<br><br><br>'
            documents:
              $ref: '#/components/schemas/documents'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            attach_pdf:
              $ref: '#/components/schemas/attach_pdf'
            entity_id:
              $ref: '#/components/schemas/entity_id'
            cc_mails_list:
              type: array
              items:
                type: object
                properties:
                  user_name:
                    $ref: '#/components/schemas/user_name'
                  email:
                    $ref: '#/components/schemas/email'
            file_name_without_extension:
              $ref: '#/components/schemas/file_name_without_extension'
            to_mails_str:
              $ref: '#/components/schemas/to_mails_str'
            cc_mails_str:
              $ref: '#/components/schemas/cc_mails_str'
            from_email:
              $ref: '#/components/schemas/from_email'
            from_address:
              $ref: '#/components/schemas/from_address'
            deprecated_placeholders_used:
              $ref: '#/components/schemas/deprecated_placeholders_used'
            error_list:
              $ref: '#/components/schemas/error_list'
            subject:
              $ref: '#/components/schemas/subject'
            emailtemplates:
              type: array
              items:
                type: object
                properties:
                  selected:
                    type: boolean
                    example: true
                  name:
                    description: The name of the line item. Max-length [100]
                    type: string
                    example: Default
                  email_template_id:
                    description: 'Get the email content based on a specific email
                      template. If this param is not inputted, then the content will
                      be based on the email template associated with the customer.
                      If no template is associated with the customer, then default
                      template will be used. Note: This value is a set of numeric
                      characters.'
                    type: string
                    example: '982000000000067'
            emailtemplate_documents:
              $ref: '#/components/schemas/emailtemplate_documents'
            to_contacts:
              type: array
              items:
                type: object
                properties:
                  first_name:
                    $ref: '#/components/schemas/first_name'
                  selected:
                    $ref: '#/components/schemas/selected'
                  phone:
                    $ref: '#/components/schemas/phone'
                  email:
                    $ref: '#/components/schemas/email'
                  last_name:
                    $ref: '#/components/schemas/last_name'
                  salutation:
                    $ref: '#/components/schemas/salutation'
                  contact_person_id:
                    $ref: '#/components/schemas/contact_person_id'
                  mobile:
                    $ref: '#/components/schemas/mobile'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            file_name:
              $ref: '#/components/schemas/file_name'
            from_emails:
              type: array
              items:
                type: object
                properties:
                  user_name:
                    $ref: '#/components/schemas/user_name'
                  selected:
                    $ref: '#/components/schemas/selected'
                  email:
                    $ref: '#/components/schemas/email'
                  organization_contact_id:
                    $ref: '#/components/schemas/organization_contact_id'
                  is_org_email_id:
                    $ref: '#/components/schemas/is_org_email_id'
            customer_id:
              $ref: '#/components/schemas/customer_id'
    bulk-invoice-reminder-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        info:
          type: object
          properties:
            email_success_info:
              type: object
              properties:
                message:
                  type: string
                  example: The reminders were successfully sent
                sent_count:
                  type: integer
                  example: 2
            email_errors_info:
              type: array
              items:
                type: object
                properties:
                  message:
                    type: string
                    example: The reminders were successfully sent
                  ids:
                    type: array
                    items:
                      type: string
                    example: '2000000007037'
            code:
              type: integer
              example: 4083
    bulk-export-invoices-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    bulk-print-invoices-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    disable-payment-reminder-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Reminders stopped.
          readOnly: true
    enable-payment-reminder-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Reminders enabled.
          readOnly: true
    write-off-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Invoice has been written off
          readOnly: true
    cancel-write-off-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The write off done for this invoice has been cancelled.
          readOnly: true
    list-invoice-templates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          type: array
          items:
            type: object
            properties:
              template_name:
                $ref: '#/components/schemas/template_name'
              template_id:
                $ref: '#/components/schemas/template_id'
              template_type:
                $ref: '#/components/schemas/template_type'
    update-invoice-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Invoice information has been updated.
          readOnly: true
    list-invoice-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        payments:
          description: The list of payments
          type: array
          items:
            type: object
            properties:
              payment_id:
                description: 'ID of the payment. Note: This value is a set of numeric
                  characters.'
                type: string
                example: '982000000567190'
              payment_number:
                description: The payment Number
                type: integer
                example: 7
              invoice_id:
                description: 'ID of the invoice. Note: This value is a set of numeric
                  characters.'
                type: string
                example: 982000000567036
              invoice_payment_id:
                description: 'The invoice payment id. Note: This value is a set of
                  numeric characters.'
                type: string
                example: 982000000567192
              payment_mode:
                description: The mode of payment for the invoice
                type: string
                example: cash
              description:
                description: Detailed description of the line item providing additional
                  information about the product or service. This description appears
                  on the invoice document and helps customers understand what they
                  are being charged for. Maximum length is 2000 characters.
                type: string
                example: ' '
              date:
                description: 'Search invoices by invoice date. Default date format
                  is yyyy-mm-dd.<code> Variants: due_date_start, due_date_end, due_date_before
                  and due_date_after</code>.'
                type: string
                example: '2013-11-18'
              reference_number:
                description: External reference number or identifier for the invoice,
                  such as a purchase order number, contract number, or any other business
                  reference. When converting from an estimate or sales order, this
                  field can be used to reference the original document number.
                type: string
                example: 99782374
              exchange_rate:
                description: Exchange rate of the currency.
                type: number
                format: float
                example: 1
              amount:
                description: The amount in the payment
                type: number
                format: float
                example: 10.57
              tax_amount_withheld:
                description: The tax amount which has been withheld
                type: number
                format: float
                example: 0
              online_transaction_id:
                $ref: '#/components/schemas/online_transaction_id'
              is_single_invoice_payment:
                description: boolean check
                type: boolean
                example: true
    list-credits-applied-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        credits:
          description: The list of credits
          type: array
          items:
            type: object
            properties:
              creditnote_id:
                $ref: '#/components/schemas/creditnote_id'
              creditnotes_invoice_id:
                $ref: '#/components/schemas/creditnotes_invoice_id'
              creditnotes_number:
                $ref: '#/components/schemas/creditnotes_number'
              credited_date:
                $ref: '#/components/schemas/credited_date'
              amount_applied:
                $ref: '#/components/schemas/amount_applied'
    apply-credits-request:
      type: object
      properties:
        bill_payments:
          $ref: '#/components/schemas/bill_payments'
        apply_vendor_credits:
          $ref: '#/components/schemas/apply_vendor_credits'
    apply-credits-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Credits have been applied to the bill(s)."
          readOnly: true
    delete-a-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The payment has been deleted.
          readOnly: true
    delete-applied-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Credits applied to an invoice have been deleted.
          readOnly: true
    get-an-invoice-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    add-attachment-to-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your file has been successfully attached to the invoice.
          readOnly: true
    delete-the-expense-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The attached expense receipt has been deleted.
          readOnly: true
    list-invoice-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          description: The details of the comments
          type: array
          items:
            type: object
            properties:
              comment_id:
                description: 'ID of the comment. Note: This value is a set of numeric
                  characters.'
                type: string
                example: 982000000567019
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                type: string
                example: 982000000554041
              commented_by:
                type: string
                example: John David
              comment_type:
                type: string
                example: system
              operation_type:
                type: string
                example: Added
              date:
                description: 'Search invoices by invoice date. Default date format
                  is yyyy-mm-dd.<code> Variants: due_date_start, due_date_end, due_date_before
                  and due_date_after</code>.'
                type: string
                example: '2013-11-18'
              date_description:
                type: string
                example: yesterday
              time:
                type: string
                example: 2:38 AM
              transaction_id:
                type: string
                description: 'The transaction ID associated with the comment. Note:
                  This value is a set of numeric characters.'
                example: '982000000567204'
              transaction_type:
                type: string
                example: invoice
    create-a-comment-request:
      type: object
      properties:
        description:
          description: The description of the comment.
          type: string
          example: This is a comment.
        payment_expected_date:
          $ref: '#/components/schemas/payment_expected_date'
        show_comment_to_clients:
          $ref: '#/components/schemas/show_comment_to_clients'
    update-a-comment-request:
      type: object
      properties:
        description:
          description: The description of the comment.
          type: string
          example: This is a comment.
        show_comment_to_clients:
          $ref: '#/components/schemas/show_comment_to_clients'
    get-invoice-document-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        document:
          type: object
          properties:
            document_id:
              description: 'Unique identifier for the document. Note: This value is
                a set of numeric characters.'
              type: string
              example: '982000000567200'
            document_name:
              description: Name of the document file.
              type: string
              example: invoice_attachment.pdf
            file_name:
              description: Original filename of the document.
              type: string
              example: contract_document.pdf
            file_size:
              description: Size of the document file in bytes.
              type: integer
              example: 245760
            file_type:
              description: Type of the document.
              type: string
              example: application/pdf
            document_url:
              description: URL to access the document.
              type: string
              example: https://books.zoho.com/api/v3/invoices/982000000567114/documents/982000000567200
            created_time:
              description: Timestamp when the document was uploaded.
              type: string
              example: 2023-11-18T10:30:45+0530
            last_modified_time:
              description: Timestamp when the document was last modified.
              type: string
              example: 2023-11-18T10:30:45+0530
    delete-invoice-document-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The document has been deleted successfully.
          readOnly: true
    salesreceipt-response:
      type: object
      properties:
        sales_receipt_id:
          description: ID of the Sales Receipt
          type: string
          example: '460000000039129'
        receipt_number:
          description: Unique alphanumeric identifier for the sales receipt. Required
            when automatic numbering is disabled in organization settings.
          type: string
          example: SR-00001
        date:
          description: Date when the sales receipt was created in YYYY-MM-DD format.
          type: string
          example: '2014-07-28'
        status:
          description: Status of the Sales Receipt
          type: string
          example: sent
        payment_mode:
          description: Mode through which payment was received
          type: string
          example: cash
        customer_id:
          description: Unique identifier for the customer
          type: string
          example: '460000000017138'
        customer_name:
          description: Name of the customer
          type: string
          example: Instruments Inc
        currency_id:
          description: Currency ID for the sales receipt
          type: string
          example: '460000000000097'
        currency_code:
          description: Currency code
          type: string
          example: USD
        currency_symbol:
          description: Currency symbol
          type: string
          example: $
        exchange_rate:
          description: Exchange rate for multi-currency transactions
          type: number
          format: double
          example: 1
        tags:
          $ref: '#/components/schemas/tags_response'
        line_items:
          description: Line items of the sales receipt
          type: array
          items:
            type: object
            properties:
              item_id:
                description: ID of the item
                type: string
                example: '460000000017088'
              name:
                description: Name of the line item
                type: string
                example: Hard Drive
              description:
                description: Description of the line item
                type: string
                example: 500GB, USB 2.0 interface
              rate:
                description: Rate of the line item
                type: number
                format: double
                example: 120
              quantity:
                description: Quantity of the line item
                type: number
                format: double
                example: 2
              unit:
                description: Unit of the line item
                type: string
                example: Nos
              item_total:
                description: Total amount for the line item
                type: number
                format: double
                example: 240
              tax_id:
                description: ID of the tax applied
                type: string
                example: '460000000017094'
              tax_name:
                description: Name of the tax
                type: string
                example: Sales Tax
              tax_percentage:
                description: Tax percentage
                type: number
                format: double
                example: 10
              tags:
                $ref: '#/components/schemas/tags_response'
        sub_total:
          description: Sub total of the sales receipt
          type: number
          format: double
          example: 240
        tax_total:
          description: Total tax amount
          type: number
          format: double
          example: 24
        total:
          description: Total amount of the sales receipt
          type: number
          format: double
          example: 264
        notes:
          description: Notes for the sales receipt
          type: string
          example: Thank you for your business
        terms:
          description: Terms and conditions
          type: string
          example: Payment received in full
        created_time:
          description: Creation time of the sales receipt
          type: string
          example: 2014-07-28T08:29:07+0530
        last_modified_time:
          description: Last modified time of the sales receipt
          type: string
          example: 2014-08-25T11:23:26+0530
    create-a-sales-receipt-request:
      required:
      - customer_id
      - payment_mode
      type: object
      properties:
        customer_id:
          description: Unique identifier for the customer
          type: string
          example: '460000000017138'
        receipt_number:
          description: Sales receipt number (required if auto-numbering is disabled)
          type: string
          example: SR-00001
        date:
          description: Date of the sales receipt
          type: string
          example: '2014-07-28'
        payment_mode:
          description: 'Mode through which payment was received. Allowed values: cash,
            check, credit_card, bank_transfer, etc.'
          type: string
          example: cash
        line_items:
          description: Line items for the sales receipt
          type: array
          items:
            type: object
            properties:
              item_id:
                description: ID of the item
                type: string
                example: '460000000017088'
              rate:
                description: Rate of the item
                type: number
                format: double
                example: 120
              quantity:
                description: Quantity of the item
                type: number
                format: double
                example: 2
              tax_id:
                description: ID of the tax to be applied
                type: string
                example: '460000000017094'
              tags:
                $ref: '#/components/schemas/tags'
        notes:
          description: Notes for the sales receipt
          type: string
          example: Thank you for your business
        terms:
          description: Terms and conditions
          type: string
          example: Payment received in full
        tags:
          $ref: '#/components/schemas/tags'
    create-a-sales-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Receipt Created Successfully.
          readOnly: true
        salesreceipt:
          $ref: '#/components/schemas/salesreceipt-response'
    update-a-sales-receipt-request:
      required:
      - customer_id
      type: object
      properties:
        customer_id:
          description: Unique identifier for the customer
          type: string
          example: '460000000017138'
        receipt_number:
          description: Sales receipt number
          type: string
          example: SR-00001
        date:
          description: Date of the sales receipt
          type: string
          example: '2014-07-28'
        payment_mode:
          description: Mode through which payment was received
          type: string
          example: cash
        line_items:
          description: Line items for the sales receipt
          type: array
          items:
            type: object
            properties:
              item_id:
                description: ID of the item
                type: string
                example: '460000000017088'
              rate:
                description: Rate of the item
                type: number
                format: double
                example: 120
              quantity:
                description: Quantity of the item
                type: number
                format: double
                example: 2
              tags:
                $ref: '#/components/schemas/tags'
        notes:
          description: Notes for the sales receipt
          type: string
          example: Thank you for your business
        tags:
          $ref: '#/components/schemas/tags'
    update-a-sales-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Receipt Updated Successfully.
          readOnly: true
        salesreceipt:
          $ref: '#/components/schemas/salesreceipt-response'
    list-sales-receipts-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        salesreceipts:
          type: array
          items:
            $ref: '#/components/schemas/salesreceipt-response'
    get-a-sales-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        salesreceipt:
          $ref: '#/components/schemas/salesreceipt-response'
    delete-a-sales-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Sales Receipt Deleted Successfully.
          readOnly: true
    email-a-sales-receipt-request:
      required:
      - to_mail_ids
      - subject
      - body
      type: object
      properties:
        to_mail_ids:
          description: Array of email addresses of the recipients
          type: array
          items:
            type: string
          example:
          - john@safinstruments.com
        subject:
          description: Subject of the mail
          type: string
          example: Sales Receipt from Zillium Inc
        body:
          description: Body of the mail
          type: string
          example: Please find the sales receipt attached.
    email-a-sales-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your Sales Receipt has been sent.
          readOnly: true
    delivery-challan-response:
      type: object
      properties:
        deliverychallan_id:
          description: Unique identifier of the delivery challan.
          type: string
          example: '982000000567001'
        deliverychallan_number:
          description: Delivery challan number.
          type: string
          example: DC-00001
        date:
          description: Date of the delivery challan in YYYY-MM-DD format.
          type: string
          example: '2024-06-15'
        challan_type:
          description: Type of delivery challan.
          type: string
          example: job_work
        reference_number:
          description: Reference number.
          type: string
          example: REF-DC-001
        status:
          description: Status of the delivery challan.
          type: string
          example: open
        customer_id:
          description: Unique identifier of the customer.
          type: string
          example: '982000000567001'
        customer_name:
          description: Name of the customer.
          type: string
          example: Bowman and Co
        currency_id:
          description: Unique identifier of the currency.
          type: string
          example: '982000000000190'
        currency_code:
          description: Currency code.
          type: string
          example: USD
        currency_symbol:
          description: Currency symbol.
          type: string
          example: $
        exchange_rate:
          description: Exchange rate for the currency.
          type: number
          example: 1
        is_discount_before_tax:
          description: Whether discount is applied before tax.
          type: boolean
          example: true
        discount_type:
          description: Type of discount.
          type: string
          example: entity_level
        is_inclusive_tax:
          description: Whether prices are inclusive of tax.
          type: boolean
          example: false
        discount:
          description: Discount applied.
          type: number
          example: 0
        place_of_supply:
          description: Place of supply (applicable for India edition).
          type: string
        gst_no:
          description: GST number (applicable for India edition).
          type: string
        gst_treatment:
          description: GST treatment (applicable for India edition).
          type: string
        tax_treatment:
          description: Tax treatment.
          type: string
        line_items:
          type: array
          items:
            $ref: '#/components/schemas/line-item-response'
        sub_total:
          description: Sub total of the delivery challan.
          type: number
          example: 5000
        tax_total:
          description: Total tax amount.
          type: number
          example: 450
        total:
          description: Total amount of the delivery challan.
          type: number
          example: 5450
        adjustment:
          description: Adjustment amount.
          type: number
          example: 0
        adjustment_description:
          description: Description for the adjustment.
          type: string
        shipping_address:
          $ref: '#/components/schemas/address-detail'
        notes:
          description: Customer notes for the delivery challan.
          type: string
          example: Deliver to the reception desk
        terms:
          description: Terms and conditions.
          type: string
        template_id:
          description: Unique identifier of the template.
          type: string
          example: '982000000567030'
        template_name:
          description: Name of the template.
          type: string
          example: Standard Template
        custom_fields:
          description: Custom fields associated with the delivery challan.
          type: string
        has_qty_returned:
          description: Indicates whether any quantity has been returned.
          type: boolean
        branch_id:
          description: Unique identifier of the branch.
          type: string
          example: '982000000567040'
        branch_name:
          description: Name of the branch.
          type: string
        location_id:
          description: Unique identifier of the location.
          type: string
          example: '982000000567040'
        location_name:
          description: Name of the location.
          type: string
        created_time:
          description: Time at which the delivery challan was created.
          type: string
          example: 2024-06-15T10:30:00+0530
        last_modified_time:
          description: Time at which the delivery challan was last modified.
          type: string
          example: 2024-06-15T10:30:00+0530
        created_by_id:
          description: Unique identifier of the user who created the delivery challan.
          type: string
          example: '982000000024001'
    line-item-response:
      type: object
      properties:
        line_item_id:
          description: Unique identifier of the line item.
          type: string
          example: '982000000567002'
        item_id:
          description: Unique identifier of the item.
          type: string
          example: '982000000030049'
        name:
          description: Name of the item.
          type: string
          example: Hard Drive
        sku:
          description: SKU of the item.
          type: string
          example: HD-1TB-001
        description:
          description: Description of the line item.
          type: string
          example: 1TB External Hard Drive
        item_order:
          description: Display order of the line item.
          type: integer
          example: 1
        rate:
          description: Rate of the item.
          type: number
          example: 2500
        bcy_rate:
          description: Rate in base currency.
          type: number
          example: 2500
        quantity:
          description: Quantity.
          type: number
          example: 2
        unit:
          description: Unit of measurement.
          type: string
          example: Nos
        discount:
          description: Discount applied.
          type: number
          example: 0
        tax_id:
          description: Unique identifier of the tax.
          type: string
          example: '982000000030010'
        tax_name:
          description: Name of the tax.
          type: string
          example: GST
        tax_type:
          description: Type of the tax.
          type: string
        tax_percentage:
          description: Tax percentage.
          type: number
          example: 9
        item_total:
          description: Total for this line item.
          type: number
          example: 5000
        hsn_or_sac:
          description: HSN or SAC code (applicable for India edition).
          type: string
        tax_exemption_id:
          description: Unique identifier of the tax exemption.
          type: string
        tax_exemption_code:
          description: Tax exemption code.
          type: string
        warehouse_id:
          description: Unique identifier of the warehouse.
          type: string
        warehouse_name:
          description: Name of the warehouse.
          type: string
        quantity_invoiced:
          description: Quantity invoiced for this line item.
          type: number
        quantity_returned:
          description: Quantity returned for this line item.
          type: number
        line_item_taxes:
          type: array
          items:
            $ref: '#/components/schemas/line-item-tax'
    line-item-tax:
      type: object
      properties:
        tax_id:
          description: Unique identifier of the tax.
          type: string
          example: '982000000030010'
        tax_name:
          description: Name of the tax.
          type: string
          example: GST
        tax_amount:
          description: Tax amount.
          type: number
          example: 225
    address-detail:
      type: object
      properties:
        address:
          description: Address line.
          type: string
          example: 123 Main Street
        street2:
          description: Second address line.
          type: string
        city:
          description: City.
          type: string
          example: Chennai
        state:
          description: State.
          type: string
          example: Tamil Nadu
        zip:
          description: Postal or ZIP code.
          type: string
          example: '600001'
        country:
          description: Country.
          type: string
          example: India
        fax:
          description: Fax number.
          type: string
        phone:
          description: Phone number.
          type: string
          example: +91-44-12345678
        attention:
          description: Attention.
          type: string
    list-delivery-challans-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        deliverychallans:
          description: List of delivery challans.
          type: array
          items:
            type: object
            properties:
              deliverychallan_id:
                description: Unique identifier of the delivery challan.
                type: string
                example: '982000000567001'
              customer_name:
                description: Name of the customer.
                type: string
                example: Bowman and Co
              customer_id:
                description: Unique identifier of the customer.
                type: string
                example: '982000000567001'
              status:
                description: Status of the delivery challan.
                type: string
                example: open
              deliverychallan_number:
                description: Delivery challan number.
                type: string
                example: DC-00001
              reference_number:
                description: Reference number.
                type: string
              date:
                description: Date of the delivery challan.
                type: string
                example: '2024-06-15'
              currency_id:
                description: Unique identifier of the currency.
                type: string
                example: '982000000000190'
              currency_code:
                description: Currency code.
                type: string
                example: USD
              total:
                description: Total amount.
                type: number
                example: 5450
              bcy_total:
                description: Total amount in base currency.
                type: number
                example: 5450
              created_time:
                description: Time at which the delivery challan was created.
                type: string
                example: 2024-06-15T10:30:00+0530
              last_modified_time:
                description: Time at which the delivery challan was last modified.
                type: string
                example: 2024-06-15T10:30:00+0530
              has_attachment:
                description: Whether the delivery challan has attachments.
                type: boolean
                example: false
        page_context:
          $ref: '#/components/schemas/page_context'
    get-delivery-challan-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        deliverychallan:
          $ref: '#/components/schemas/delivery-challan-response'
    create-delivery-challan-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The delivery challan has been created
          readOnly: true
        deliverychallan:
          $ref: '#/components/schemas/delivery-challan-response'
    update-delivery-challan-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The delivery challan has been updated
          readOnly: true
        deliverychallan:
          $ref: '#/components/schemas/delivery-challan-response'
    delete-delivery-challan-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The delivery challan has been deleted
          readOnly: true
    action-delivery-challan-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    list-templates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          type: array
          items:
            type: object
            properties:
              template_id:
                description: Unique identifier of the template.
                type: string
                example: '982000000567030'
              template_name:
                description: Name of the template.
                type: string
                example: Standard Template
              template_type:
                description: Type of the template.
                type: string
                example: standard
    create-delivery-challan-request:
      required:
      - customer_id
      type: object
      properties:
        customer_id:
          description: Unique identifier of the customer.
          type: string
          example: '982000000567001'
        currency_id:
          description: Unique identifier of the currency.
          type: string
          example: '982000000000190'
        deliverychallan_number:
          description: Delivery challan number. Max-length [50]
          type: string
          maxLength: 50
          example: DC-00001
        reference_number:
          description: Reference number. Max-length [50]
          type: string
          maxLength: 50
          example: REF-DC-001
        template_id:
          description: Unique identifier of the template.
          type: string
          example: '982000000567030'
        date:
          description: Date of the delivery challan in YYYY-MM-DD format.
          type: string
          example: '2024-06-15'
        challan_type:
          description: Type of delivery challan. Max-length [200]
          type: string
          maxLength: 200
          example: job_work
        discount:
          description: Discount applied at the entity level.
          type: string
        is_discount_before_tax:
          description: Whether discount is applied before tax.
          type: boolean
          example: true
        discount_type:
          description: Type of discount.
          type: string
        place_of_supply:
          description: Place of supply state code (applicable for India edition).
            Max-length [5]
          type: string
          maxLength: 5
        exchange_rate:
          description: Exchange rate for the currency.
          type: number
          example: 1
        is_inclusive_tax:
          description: Whether prices are inclusive of tax.
          type: boolean
          example: false
        gst_treatment:
          description: GST treatment (applicable for India edition).
          type: string
        tax_treatment:
          description: Tax treatment.
          type: string
        gst_no:
          description: GST number (applicable for India edition). Max-length [15]
          type: string
          maxLength: 15
        line_items:
          description: Line items of the delivery challan.
          type: array
          items:
            $ref: '#/components/schemas/line-item-request'
        notes:
          description: Customer notes. Max-length [5000]
          type: string
          maxLength: 5000
          example: Deliver to the reception desk
        terms:
          description: Terms and conditions. Max-length [10000]
          type: string
          maxLength: 10000
        adjustment:
          description: Adjustment amount.
          type: number
          example: 0
        adjustment_description:
          description: Adjustment description. Max-length [200]
          type: string
          maxLength: 200
        tax_id:
          description: Unique identifier of the tax.
          type: string
        tax_authority_id:
          description: Unique identifier of the tax authority.
          type: string
        tax_exemption_id:
          description: Unique identifier of the tax exemption.
          type: string
        shipping_address_id:
          description: Unique identifier of the shipping address.
          type: string
        pricebook_id:
          description: Unique identifier of the price book.
          type: string
        branch_id:
          description: Unique identifier of the branch.
          type: string
        location_id:
          description: Unique identifier of the location.
          type: string
    line-item-request:
      type: object
      properties:
        item_id:
          description: Unique identifier of the item.
          type: string
          example: '982000000030049'
        line_item_id:
          description: Unique identifier of the line item (for updates).
          type: string
        name:
          description: Name of the item. Max-length [200]
          type: string
          maxLength: 200
          example: Hard Drive
        sku:
          description: SKU of the item. Max-length [200]
          type: string
          maxLength: 200
        description:
          description: Description of the line item. Max-length [6000]
          type: string
          maxLength: 6000
          example: 1TB External Hard Drive
        item_order:
          description: Display order of the item.
          type: integer
          example: 1
        rate:
          description: Rate of the item.
          type: number
          example: 2500
        quantity:
          description: Quantity.
          type: number
          example: 2
        unit:
          description: Unit of measurement. Max-length [120]
          type: string
          maxLength: 120
          example: Nos
        discount:
          description: Discount for this line item.
          type: string
        tax_id:
          description: Unique identifier of the tax.
          type: string
          example: '982000000030010'
        tax_exemption_id:
          description: Unique identifier of the tax exemption.
          type: string
        tax_exemption_code:
          description: Tax exemption code. Max-length [50]
          type: string
          maxLength: 50
        hsn_or_sac:
          description: HSN or SAC code (applicable for India edition).
          type: string
        pricebook_id:
          description: Unique identifier of the price book.
          type: string
        warehouse_id:
          description: Unique identifier of the warehouse.
          type: string
        location_id:
          description: Unique identifier of the location.
          type: string
    return-delivery-challan-request:
      type: object
      properties:
        line_items:
          description: Line items with return quantities.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                description: Unique identifier of the line item.
                type: string
                example: '982000000567002'
              qty_returned:
                description: Quantity to return.
                type: number
                example: 1
    recurring_invoice-response:
      type: object
      properties:
        recurring_invoice_id:
          $ref: '#/components/schemas/recurring_invoice_id'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        last_sent_date:
          $ref: '#/components/schemas/last_sent_date'
        next_invoice_date:
          $ref: '#/components/schemas/next_invoice_date'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        tags:
          $ref: '#/components/schemas/tags_response'
        line_items:
          description: Line items of an invoice.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              quantity:
                $ref: '#/components/schemas/quantity'
              name:
                $ref: '#/components/schemas/name'
              item_total:
                $ref: '#/components/schemas/item_total'
              sku:
                $ref: '#/components/schemas/sku'
              product_type:
                $ref: '#/components/schemas/product_type'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              tags:
                $ref: '#/components/schemas/tags_response'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              project_id:
                $ref: '#/components/schemas/project_id'
              project_name:
                $ref: '#/components/schemas/project_name'
              header_name:
                $ref: '#/components/schemas/header_name'
              header_id:
                $ref: '#/components/schemas/header_id'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
    recurrence_name:
      description: Name of the Recurring Bill. Max-length [100]
      type: string
      example: Monthly Rental
    customer_email:
      description: Email address of the customer to whom the recurring invoice is
        raised.
      type: string
      example: emailid@abcstudios.com
    customer_mobile_phone:
      description: Mobile phone number of the customer to whom the recurring invoice
        is raised.
      type: string
      example: 9876543210
    customer_phone:
      description: Phone number of the customer to whom the recurring invoice is raised.
      type: string
      example: 012-12345678
    paid_invoices_total:
      description: Total of the all child invoices that have been paid.
      type: number
      format: double
      example: 100
    unpaid_invoices_balance:
      description: Balance of the all child invoices that have been raised.
      type: number
      format: double
      example: 100
    photo_url:
      type: string
      description: URL of the user's photo.
      example: https://contacts.zoho.com/file?ID=d27344a22bad8bb83a03722b4aa5bc6967c3135f24307fe40db8572782432fd6aae0110f8bb9c4c79e8e0f0cca5904aecfacbf079f13b48c295bacc89ae91fca&fs=thumb
    last_sent_date:
      description: The date at which the last invoice was sent.
      type: string
      example: '2016-06-12'
    next_invoice_date:
      description: The date at which next invoice is to be sent.
      type: string
      example: '2016-06-17'
    recurrence_frequency:
      type: string
      description: Frequency at which the recurring expense is generated.
      example: months
    repeat_every:
      type: string
      description: Interval at which the recurring expense should repeat.
      example: 1
    type:
      description: Type of the tax exemption, can be <code>customer</code> or <code>item</code>.
      type: string
      example: customer
    account_name:
      description: Name of the account.
      type: string
      example: Undeposited Funds
    price:
      description: The price of the item included.
      type: number
      format: double
      example: 100
    comment_description:
      description: The description for comment.
      type: string
      example: Recurring Invoice created
    updated_time:
      description: Time when the payment details were last updated.
      type: string
      example: 2016-06-05T02:15:20-0700
    data_type:
      description: Data Type of the Field
      type: string
      example: string
    recurring_invoice_items:
      type: array
      items:
        type: object
        properties:
          item_id:
            $ref: '#/components/schemas/item_id'
          description:
            $ref: '#/components/schemas/description'
          quantity:
            $ref: '#/components/schemas/quantity'
          price:
            $ref: '#/components/schemas/price'
          item_total:
            $ref: '#/components/schemas/item_total'
          project_id:
            $ref: '#/components/schemas/project_id'
    create-a-recurring-invoice-request:
      required:
      - recurrence_name
      - customer_id
      - recurrence_frequency
      type: object
      properties:
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        allow_partial_payments:
          $ref: '#/components/schemas/allow_partial_payments'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        recurrence_frequency:
          $ref: '#/components/schemas/recurrence_frequency'
        repeat_every:
          $ref: '#/components/schemas/repeat_every'
        location_id:
          $ref: '#/components/schemas/location_id'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: Line items of an invoice.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              quantity:
                $ref: '#/components/schemas/quantity'
              name:
                $ref: '#/components/schemas/name'
              item_total:
                $ref: '#/components/schemas/item_total'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              tags:
                $ref: '#/components/schemas/tags'
              location_id:
                $ref: '#/components/schemas/location_id'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              project_id:
                $ref: '#/components/schemas/project_id'
              header_name:
                $ref: '#/components/schemas/header_name'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        email:
          $ref: '#/components/schemas/email'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        template_id:
          $ref: '#/components/schemas/template_id'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        discount:
          $ref: '#/components/schemas/discount'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        discount_type:
          $ref: '#/components/schemas/discount_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        item_id:
          $ref: '#/components/schemas/item_id'
        name:
          $ref: '#/components/schemas/name'
        description:
          $ref: '#/components/schemas/description'
        rate:
          $ref: '#/components/schemas/rate'
        quantity:
          $ref: '#/components/schemas/quantity'
        unit:
          $ref: '#/components/schemas/unit'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
        salesperson_name:
          $ref: '#/components/schemas/salesperson_name'
        shipping_charge:
          $ref: '#/components/schemas/shipping_charge'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
    create-a-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring invoice has been created.
          readOnly: true
        recurring_invoice:
          $ref: '#/components/schemas/recurring_invoice-response'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            applied_filter:
              $ref: '#/components/schemas/applied_filter'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    list-all-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Details of a recurring invoice is displayed successfully.
          readOnly: true
        recurring_invoices:
          type: array
          items:
            type: object
            properties:
              recurring_invoice_id:
                $ref: '#/components/schemas/recurring_invoice_id'
              recurrence_name:
                $ref: '#/components/schemas/recurrence_name'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              start_date:
                $ref: '#/components/schemas/start_date'
              end_date:
                $ref: '#/components/schemas/end_date'
              last_sent_date:
                $ref: '#/components/schemas/last_sent_date'
              next_invoice_date:
                $ref: '#/components/schemas/next_invoice_date'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
              billing_address:
                $ref: '#/components/schemas/billing_address'
              shipping_address:
                $ref: '#/components/schemas/shipping_address'
              tags:
                $ref: '#/components/schemas/tags'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            applied_filter:
              $ref: '#/components/schemas/applied_filter'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    update-recurring-invoice-request:
      required:
      - recurrence_name
      - customer_id
      - recurrence_frequency
      type: object
      properties:
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        recurrence_frequency:
          $ref: '#/components/schemas/recurrence_frequency'
        repeat_every:
          $ref: '#/components/schemas/repeat_every'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        allow_partial_payments:
          $ref: '#/components/schemas/allow_partial_payments'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        location_id:
          $ref: '#/components/schemas/location_id'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: Line items of an invoice.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              discount:
                $ref: '#/components/schemas/discount'
              tags:
                $ref: '#/components/schemas/tags'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              avatax_tax_code:
                $ref: '#/components/schemas/avatax_tax_code'
              avatax_use_code:
                $ref: '#/components/schemas/avatax_use_code'
              item_total:
                $ref: '#/components/schemas/item_total'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              location_id:
                $ref: '#/components/schemas/location_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              header_name:
                $ref: '#/components/schemas/header_name'
              header_id:
                $ref: '#/components/schemas/header_id'
        email:
          $ref: '#/components/schemas/email'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
    update-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        recurring_invoice:
          type: object
          properties:
            recurring_invoice_id:
              $ref: '#/components/schemas/recurring_invoice_id'
            recurrence_name:
              $ref: '#/components/schemas/recurrence_name'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            start_date:
              $ref: '#/components/schemas/start_date'
            end_date:
              $ref: '#/components/schemas/end_date'
            last_sent_date:
              $ref: '#/components/schemas/last_sent_date'
            next_invoice_date:
              $ref: '#/components/schemas/next_invoice_date'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_items:
              description: Line items of an invoice.
              type: array
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  name:
                    $ref: '#/components/schemas/name'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  sku:
                    $ref: '#/components/schemas/sku'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  tags:
                    $ref: '#/components/schemas/tags_response'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  project_name:
                    $ref: '#/components/schemas/project_name'
                  header_name:
                    $ref: '#/components/schemas/header_name'
                  header_id:
                    $ref: '#/components/schemas/header_id'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            payment_options:
              $ref: '#/components/schemas/payment_options'
            avatax_exempt_no:
              $ref: '#/components/schemas/avatax_exempt_no'
            avatax_use_code:
              $ref: '#/components/schemas/avatax_use_code'
    get-a-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Details of a recurring invoice is displayed successfully.
          readOnly: true
        recurring_invoice:
          type: object
          properties:
            recurring_invoice_id:
              $ref: '#/components/schemas/recurring_invoice_id'
            recurrence_name:
              $ref: '#/components/schemas/recurrence_name'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            company_name:
              $ref: '#/components/schemas/company_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            customer_email:
              $ref: '#/components/schemas/customer_email'
            customer_mobile_phone:
              $ref: '#/components/schemas/customer_mobile_phone'
            customer_phone:
              $ref: '#/components/schemas/customer_phone'
            photo_url:
              $ref: '#/components/schemas/photo_url'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            start_date:
              $ref: '#/components/schemas/start_date'
            end_date:
              $ref: '#/components/schemas/end_date'
            last_sent_date:
              $ref: '#/components/schemas/last_sent_date'
            next_invoice_date:
              $ref: '#/components/schemas/next_invoice_date'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_items:
              description: Line items of an invoice.
              type: array
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  name:
                    $ref: '#/components/schemas/name'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  sku:
                    $ref: '#/components/schemas/sku'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  tags:
                    $ref: '#/components/schemas/tags_response'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  project_name:
                    $ref: '#/components/schemas/project_name'
                  header_name:
                    $ref: '#/components/schemas/header_name'
                  header_id:
                    $ref: '#/components/schemas/header_id'
            paid_invoices_total:
              $ref: '#/components/schemas/paid_invoices_total'
            unpaid_invoices_balance:
              $ref: '#/components/schemas/unpaid_invoices_balance'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            payment_options:
              $ref: '#/components/schemas/payment_options'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            applied_filter:
              $ref: '#/components/schemas/applied_filter'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    delete-a-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring invoice is deleted successfully.
          readOnly: true
    stop-a-recurring-invoice-request:
      type: object
      properties:
        recurring_invoice_id:
          description: Unique ID of the recurring invoice generated by the server.
          type: string
          example: '90300000072369'
    stop-a-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring invoice has been stopped.
          readOnly: true
    resume-a-recurring-invoice-request:
      type: object
      properties:
        recurring_invoice_id:
          description: Unique ID of the recurring invoice generated by the server.
          type: string
          example: '90300000072369'
    resume-a-recurring-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring invoice has been resumed.
          readOnly: true
    update-recurring-invoice-template-request:
      type: object
      properties:
        recurring_invoice_id:
          description: Unique ID of the recurring invoice generated by the server.
          type: string
          example: '90300000072369'
        template_id:
          description: Unique ID of the recurring invoice template.
          type: string
          example: '90300043563547'
    update-recurring-invoice-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The template of the recurring invoice has been updated.
          readOnly: true
    list-recurring-invoice-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The complete history and comments of a recurring invoice are displayed
            successfully.
          readOnly: true
        comments:
          description: The details of the comments
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              recurring_invoice_id:
                $ref: '#/components/schemas/recurring_invoice_id'
              comment_description:
                $ref: '#/components/schemas/comment_description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    creditnote-response:
      type: object
      properties:
        creditnote_id:
          $ref: '#/components/schemas/creditnote_id'
        creditnote_number:
          $ref: '#/components/schemas/creditnote_number'
        date:
          $ref: '#/components/schemas/date'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        cfdi_reference_type:
          $ref: '#/components/schemas/cfdi_reference_type'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        status:
          $ref: '#/components/schemas/status'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        custom_fields:
          description: Additional fields for the Credit-Notes.
          type: array
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
              label:
                $ref: '#/components/schemas/label'
              data_type:
                $ref: '#/components/schemas/data_type'
        tags:
          $ref: '#/components/schemas/tags_response'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        email:
          $ref: '#/components/schemas/email'
        total:
          $ref: '#/components/schemas/total'
        balance:
          $ref: '#/components/schemas/balance'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        line_items:
          $ref: '#/components/schemas/line_items'
        invoices:
          description: List of invoices for which the credit note has been raised.
            This contains <code>invoice_id</code> and <code>amount</code>.
          type: array
          items:
            type: object
            properties:
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              invoice_number:
                $ref: '#/components/schemas/invoice_number'
              amount:
                $ref: '#/components/schemas/amount'
        taxes:
          description: Taxes associated wit the subscription.
          type: array
          items:
            type: object
            properties:
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        billing_address:
          type: object
          properties:
            address:
              $ref: '#/components/schemas/address'
            street2:
              $ref: '#/components/schemas/street2'
            city:
              $ref: '#/components/schemas/city'
            state:
              $ref: '#/components/schemas/state'
            zip:
              $ref: '#/components/schemas/zip'
            country:
              $ref: '#/components/schemas/country'
            fax:
              $ref: '#/components/schemas/fax'
        shipping_address:
          type: object
          properties:
            address:
              $ref: '#/components/schemas/address'
            street2:
              $ref: '#/components/schemas/street2'
            city:
              $ref: '#/components/schemas/city'
            state:
              $ref: '#/components/schemas/state'
            zip:
              $ref: '#/components/schemas/zip'
            country:
              $ref: '#/components/schemas/country'
            fax:
              $ref: '#/components/schemas/fax'
        created_time:
          $ref: '#/components/schemas/created_time'
        updated_time:
          $ref: '#/components/schemas/updated_time'
        template_id:
          $ref: '#/components/schemas/template_id'
        template_name:
          $ref: '#/components/schemas/template_name'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
    serial_numbers:
      type: array
      description: Serial numbers associated with the inventory item in the line item.
      items:
        type: string
    from_account_id:
      description: 'The account ID from which money will be transferred(Mandatory
        for specific type of transactions). These accounts differ with respect to
        transaction_type. Ex: To a bank account, from-account can be: bank , card,
        income, refund. To a card account, from account can be: bank, card, refund.'
      type: string
      example: '460000000070003'
    is_draft:
      description: Is tag still in draft state
      type: boolean
      example: false
    payment_form:
      description: Mode of Vendor Payment
      type: string
      example: cash
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    code:
      description: Unique code or SKU for the line item. Used for product identification
        and inventory tracking. Helps distinguish between different variants of the
        same product.
      type: string
      example: basic-monthly
    invoice_item_id:
      description: Unique identifier of the invoice line item that this credit note
        line item references. Use this when creating a credit note for a specific
        invoice line item.
      type: string
      example: '982000000567021'
    from:
      description: The email ID from which the credit note is to be mailed.
      type: string
      example: gator@zillum.com
    from_mail_id_alias:
      description: The email ID from which the credit note is to be mailed.
      type: string
      example: true
    to_mail_ids_alias:
      description: The email IDs to which the credit note is to be mailed.
      type: array
      items:
        type: string
      example: true
    cc_mail_ids_alias:
      description: The email IDs that have to be copied when the credit note is to
        be mailed.
      type: array
      items:
        type: string
      example: true
    subject_alias:
      description: The subject of the email.
      type: string
      example: true
    body_alias:
      description: The body of the email.
      type: string
      example: true
    creditnote_invoice_id:
      type: string
      example: '982000000567172'
    credited_amount:
      description: The total amount that is credited.
      type: number
      format: double
      example: 12.02
    mailhistory_id:
      type: string
      example: 982000000570005
    template-id:
      type: string
      example: 982000000010019
    create-a-credit-note-request:
      required:
      - customer_id
      - date
      - creditnote_number
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        date:
          $ref: '#/components/schemas/date'
        is_draft:
          $ref: '#/components/schemas/is_draft'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        line_items:
          description: Array of line items for the credit note. Each line item represents
            a product or service being credited to the customer.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              invoice_item_id:
                description: Unique identifier of the invoice line item that this
                  credit note line item references. Use this when creating a credit
                  note for a specific invoice line item.
                type: string
                example: '982000000567021'
              description:
                $ref: '#/components/schemas/description'
              code:
                $ref: '#/components/schemas/code'
              name:
                $ref: '#/components/schemas/name'
              type:
                $ref: '#/components/schemas/type'
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              product_type:
                $ref: '#/components/schemas/product_type'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              serial_numbers:
                $ref: '#/components/schemas/serial_numbers'
              location_id:
                $ref: '#/components/schemas/location_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              tags:
                $ref: '#/components/schemas/tags'
        location_id:
          $ref: '#/components/schemas/location_id'
        creditnote_number:
          $ref: '#/components/schemas/creditnote_number'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        cfdi_reference_type:
          $ref: '#/components/schemas/cfdi_reference_type'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        ignore_auto_number_generation:
          $ref: '#/components/schemas/ignore_auto_number_generation'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        template_id:
          $ref: '#/components/schemas/template_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
    create-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note has been created.
          readOnly: true
        creditnote:
          $ref: '#/components/schemas/creditnote-response'
    list-all-credit-notes-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        creditnotes:
          type: object
          properties:
            creditnote_id:
              $ref: '#/components/schemas/creditnote_id'
            creditnote_number:
              $ref: '#/components/schemas/creditnote_number'
            status:
              $ref: '#/components/schemas/status'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            date:
              $ref: '#/components/schemas/date'
            total:
              $ref: '#/components/schemas/total'
            balance:
              $ref: '#/components/schemas/balance'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              type: string
              example: 2016-06-05T02:30:08-0700
            is_emailed:
              type: boolean
              example: true
    update-a-credit-note-request:
      required:
      - customer_id
      - date
      - creditnote_number
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        date:
          $ref: '#/components/schemas/date'
        is_draft:
          $ref: '#/components/schemas/is_draft'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        line_items:
          $ref: '#/components/schemas/line_items'
        location_id:
          $ref: '#/components/schemas/location_id'
        creditnote_number:
          $ref: '#/components/schemas/creditnote_number'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        cfdi_usage:
          $ref: '#/components/schemas/cfdi_usage'
        cfdi_reference_type:
          $ref: '#/components/schemas/cfdi_reference_type'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        ignore_auto_number_generation:
          $ref: '#/components/schemas/ignore_auto_number_generation'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        template_id:
          $ref: '#/components/schemas/template_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        avatax_exempt_no:
          $ref: '#/components/schemas/avatax_exempt_no'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        item_id:
          $ref: '#/components/schemas/item_id'
        account_id:
          $ref: '#/components/schemas/account_id'
        name:
          $ref: '#/components/schemas/name'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
        description:
          $ref: '#/components/schemas/description'
        unit:
          $ref: '#/components/schemas/unit'
        rate:
          $ref: '#/components/schemas/rate'
        quantity:
          $ref: '#/components/schemas/quantity'
    update-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note has been updated.
          readOnly: true
        creditnote:
          type: object
          properties:
            creditnote_id:
              $ref: '#/components/schemas/creditnote_id'
            creditnote_number:
              $ref: '#/components/schemas/creditnote_number'
            date:
              $ref: '#/components/schemas/date'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            cfdi_reference_type:
              $ref: '#/components/schemas/cfdi_reference_type'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            status:
              $ref: '#/components/schemas/status'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            custom_fields:
              description: Additional fields for the Credit-Notes.
              type: array
              items:
                type: object
                properties:
                  index:
                    $ref: '#/components/schemas/index'
                  value:
                    $ref: '#/components/schemas/value'
                  label:
                    $ref: '#/components/schemas/label'
                  data_type:
                    $ref: '#/components/schemas/data_type'
            tags:
              $ref: '#/components/schemas/tags'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            email:
              $ref: '#/components/schemas/email'
            total:
              $ref: '#/components/schemas/total'
            balance:
              $ref: '#/components/schemas/balance'
            line_items:
              description: Line items of a credit note.
              type: array
              items:
                type: object
                properties:
                  item_id:
                    $ref: '#/components/schemas/item_id'
                  description:
                    $ref: '#/components/schemas/description'
                  code:
                    $ref: '#/components/schemas/code'
                  name:
                    $ref: '#/components/schemas/name'
                  type:
                    $ref: '#/components/schemas/type'
                  account_id:
                    $ref: '#/components/schemas/account_id'
                  account_name:
                    $ref: '#/components/schemas/account_name'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  serial_numbers:
                    $ref: '#/components/schemas/serial_numbers'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  tags:
                    $ref: '#/components/schemas/tags_response'
            invoices:
              description: List of invoices for which the credit note has been raised.
                This contains <code>invoice_id</code> and <code>amount</code>.
              type: array
              items:
                type: object
                properties:
                  invoice_id:
                    $ref: '#/components/schemas/invoice_id'
                  invoice_number:
                    $ref: '#/components/schemas/invoice_number'
                  amount:
                    $ref: '#/components/schemas/amount'
            taxes:
              description: Taxes associated wit the subscription.
              type: array
              items:
                type: object
                properties:
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_amount:
                    $ref: '#/components/schemas/tax_amount'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            billing_address:
              type: object
              properties:
                address:
                  $ref: '#/components/schemas/address'
                street2:
                  $ref: '#/components/schemas/street2'
                city:
                  $ref: '#/components/schemas/city'
                state:
                  $ref: '#/components/schemas/state'
                zip:
                  $ref: '#/components/schemas/zip'
                country:
                  $ref: '#/components/schemas/country'
                fax:
                  $ref: '#/components/schemas/fax'
                attention:
                  $ref: '#/components/schemas/attention'
            shipping_address:
              type: object
              properties:
                address:
                  $ref: '#/components/schemas/address'
                street2:
                  $ref: '#/components/schemas/street2'
                city:
                  $ref: '#/components/schemas/city'
                state:
                  $ref: '#/components/schemas/state'
                zip:
                  $ref: '#/components/schemas/zip'
                country:
                  $ref: '#/components/schemas/country'
                fax:
                  $ref: '#/components/schemas/fax'
                attention:
                  $ref: '#/components/schemas/attention'
            created_time:
              $ref: '#/components/schemas/created_time'
            updated_time:
              $ref: '#/components/schemas/updated_time'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
    get-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        creditnote:
          type: object
          properties:
            creditnote_id:
              $ref: '#/components/schemas/creditnote_id'
            creditnote_number:
              $ref: '#/components/schemas/creditnote_number'
            date:
              $ref: '#/components/schemas/date'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            cfdi_usage:
              $ref: '#/components/schemas/cfdi_usage'
            cfdi_reference_type:
              $ref: '#/components/schemas/cfdi_reference_type'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            status:
              $ref: '#/components/schemas/status'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            custom_fields:
              description: Additional fields for the Credit-Notes.
              type: array
              items:
                type: object
                properties:
                  index:
                    $ref: '#/components/schemas/index'
                  value:
                    $ref: '#/components/schemas/value'
                  label:
                    $ref: '#/components/schemas/label'
                  data_type:
                    $ref: '#/components/schemas/data_type'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            email:
              $ref: '#/components/schemas/email'
            total:
              $ref: '#/components/schemas/total'
            balance:
              $ref: '#/components/schemas/balance'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            line_items:
              description: Line items of a credit note.
              type: array
              items:
                type: object
                properties:
                  item_id:
                    $ref: '#/components/schemas/item_id'
                  description:
                    $ref: '#/components/schemas/description'
                  code:
                    $ref: '#/components/schemas/code'
                  name:
                    $ref: '#/components/schemas/name'
                  type:
                    $ref: '#/components/schemas/type'
                  account_id:
                    $ref: '#/components/schemas/account_id'
                  account_name:
                    $ref: '#/components/schemas/account_name'
                  quantity:
                    $ref: '#/components/schemas/quantity'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_treatment_code:
                    $ref: '#/components/schemas/tax_treatment_code'
                  product_type:
                    $ref: '#/components/schemas/product_type'
                  serial_numbers:
                    $ref: '#/components/schemas/serial_numbers'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
                  project_id:
                    $ref: '#/components/schemas/project_id'
                  project_name:
                    $ref: '#/components/schemas/project_name'
                  tags:
                    $ref: '#/components/schemas/tags_response'
            invoices:
              description: List of invoices for which the credit note has been raised.
                This contains <code>invoice_id</code> and <code>amount</code>.
              type: array
              items:
                type: object
                properties:
                  invoice_id:
                    $ref: '#/components/schemas/invoice_id'
                  invoice_number:
                    $ref: '#/components/schemas/invoice_number'
                  amount:
                    $ref: '#/components/schemas/amount'
            taxes:
              description: Taxes associated wit the subscription.
              type: array
              items:
                type: object
                properties:
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_amount:
                    $ref: '#/components/schemas/tax_amount'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            created_time:
              $ref: '#/components/schemas/created_time'
            updated_time:
              $ref: '#/components/schemas/updated_time'
            billing_address:
              type: object
              properties:
                address:
                  $ref: '#/components/schemas/address'
                street2:
                  $ref: '#/components/schemas/street2'
                city:
                  $ref: '#/components/schemas/city'
                state:
                  $ref: '#/components/schemas/state'
                zip:
                  $ref: '#/components/schemas/zip'
                country:
                  $ref: '#/components/schemas/country'
                fax:
                  $ref: '#/components/schemas/fax'
                attention:
                  $ref: '#/components/schemas/attention'
            shipping_address:
              type: object
              properties:
                address:
                  $ref: '#/components/schemas/address'
                street2:
                  $ref: '#/components/schemas/street2'
                city:
                  $ref: '#/components/schemas/city'
                state:
                  $ref: '#/components/schemas/state'
                zip:
                  $ref: '#/components/schemas/zip'
                country:
                  $ref: '#/components/schemas/country'
                fax:
                  $ref: '#/components/schemas/fax'
                attention:
                  $ref: '#/components/schemas/attention'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            tags:
              $ref: '#/components/schemas/tags_response'
    delete-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note has been deleted.
          readOnly: true
    email-a-credit-note-request:
      required:
      - to_mail_ids
      - subject
      - body
      type: object
      properties:
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          $ref: '#/components/schemas/body'
    email-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your creditnote has been sent.
          readOnly: true
    get-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          type: array
          items:
            type: object
            properties:
              body:
                description: Body of the mail.
                type: string
                example: The credit note is attached with this email.
              error_list:
                description: Array that consists of errors.
                type: array
                items:
                  type: string
                example: []
              subject:
                description: Subject of the email.
                type: string
                example: 'Credit Note from Zillium Inc(Credit Note #: CN-00001)'
              emailtemplates:
                description: Templates of the email.
                type: array
                items:
                  type: object
                  properties:
                    selected:
                      type: boolean
                      example: fasle
                    name:
                      description: Name of the credit
                      type: string
                      example: Default
                    email_template_id:
                      description: Get the email content based on a specific email
                        template. If this param is not inputted, then the content
                        will be based on the email template associated with the customer.
                        If no template is associated with the customer, then default
                        template will be used.
                      type: string
                      example: '460000000000085'
              to_contacts:
                type: array
                items:
                  type: object
                  properties:
                    first_name:
                      type: string
                      example: Will
                    selected:
                      type: boolean
                      example: fasle
                    phone:
                      type: string
                      example: +1-925-921-9201
                    email:
                      description: Email address of the customer.
                      type: string
                      example: willsmith@bowmanfurniture.com
                    contact_person_id:
                      type: string
                      example: '460000000026051'
                    last_name:
                      type: string
                      example: Parker
                    salutation:
                      type: string
                      example: Mr.
                    mobile:
                      type: string
                      example: +1-4054439760
              file_name:
                type: string
                example: CN-00001.pdf
              from_emails:
                type: array
                items:
                  type: object
                  properties:
                    user_name:
                      type: string
                      example: John Smith
                    selected:
                      type: boolean
                      example: fasle
                    email:
                      description: Email address of the customer.
                      type: string
                      example: willsmith@bowmanfurniture.com
              customer_id:
                description: Customer ID of the customer for whom the credit note
                  is raised.
                type: string
                example: '46000000002609'
    void-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note has been marked as void.
          readOnly: true
    convert-credit-note-to-draft-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note has been marked as Draft.
          readOnly: true
    convert-to-open-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The status of the vendor credit has been changed to open.
          readOnly: true
    submit-a-credit-note-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The Credit Note has been successfully submitted for approval.
          readOnly: true
    approve-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the Credit Note.
          readOnly: true
    email-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        email_history:
          type: array
          items:
            type: object
            properties:
              mailhistory_id:
                $ref: '#/components/schemas/mailhistory_id'
              from:
                $ref: '#/components/schemas/from'
              to_mail_ids:
                $ref: '#/components/schemas/to_mail_ids'
              subject:
                $ref: '#/components/schemas/subject'
              date:
                $ref: '#/components/schemas/date'
    list-credit-note-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          type: array
          items:
            type: object
            properties:
              template_name:
                $ref: '#/components/schemas/template_name'
              template_id:
                $ref: '#/components/schemas/template_id'
              template_type:
                $ref: '#/components/schemas/template_type'
    update-a-credit-note-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note has been updated.
          readOnly: true
    list-invoices-credited-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        invoices_credited:
          type: array
          items:
            type: object
            properties:
              creditnote_id:
                $ref: '#/components/schemas/creditnote_id'
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              creditnote_invoice_id:
                $ref: '#/components/schemas/creditnote_invoice_id'
              date:
                $ref: '#/components/schemas/date'
              invoice_number:
                $ref: '#/components/schemas/invoice_number'
              creditnote_number:
                $ref: '#/components/schemas/creditnote_number'
              credited_amount:
                $ref: '#/components/schemas/credited_amount'
    credit-to-an-invoice-request:
      required:
      - invoice_id
      - amount_applied
      type: object
      properties:
        invoices:
          description: List of invoices for which the credit note has been raised.
            This contains <code>invoice_id</code> and <code>amount</code>.
          type: array
          items:
            type: object
            properties:
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              amount_applied:
                $ref: '#/components/schemas/amount_applied'
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        amount_applied:
          $ref: '#/components/schemas/amount_applied'
    credit-to-an-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Credits have been applied to the invoice(s).
          readOnly: true
    delete-invoices-credited-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Credits applied to an invoice have been deleted.
          readOnly: true
    list-credit-note-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comments of the credit note are displayed successfully.
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              creditnote_id:
                $ref: '#/components/schemas/creditnote_id'
              description:
                description: A small description about the item.
                type: string
                example: Credits applied to invoice INV-00004
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    add-a-comment-request:
      required:
      - description
      type: object
      properties:
        description:
          description: Description of the Comment
          type: string
          example: Credits applied to Bill 1
    add-a-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comments added.
          readOnly: true
        comment:
          type: object
          properties:
            comment_id:
              $ref: '#/components/schemas/comment_id'
            vendor_credit_id:
              $ref: '#/components/schemas/vendor_credit_id'
            description:
              description: Description of the Comment
              type: string
              example: Credits applied to Bill 1
            commented_by_id:
              $ref: '#/components/schemas/commented_by_id'
            commented_by:
              $ref: '#/components/schemas/commented_by'
            date:
              description: Date of the Comment
              type: string
              example: '2017-01-18'
    list-credit-note-refunds-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The list of credit note refunds are displayed successfully.
          readOnly: true
        creditnote_refunds:
          type: array
          items:
            type: object
            properties:
              creditnote_refund_id:
                $ref: '#/components/schemas/creditnote_refund_id'
              creditnote_id:
                $ref: '#/components/schemas/creditnote_id'
              date:
                $ref: '#/components/schemas/date'
              refund_mode:
                $ref: '#/components/schemas/refund_mode'
              payment_form:
                $ref: '#/components/schemas/payment_form'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              creditnote_number:
                $ref: '#/components/schemas/creditnote_number'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              description:
                description: A small description about the item.
                type: string
                example: ''
              amount_bcy:
                $ref: '#/components/schemas/amount_bcy'
              amount_fcy:
                $ref: '#/components/schemas/amount_fcy'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    list-refunds-of-a-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refunds of the existing credit note are displayed successfully.
          readOnly: true
        creditnote_refunds:
          type: array
          items:
            type: object
            properties:
              creditnote_refund_id:
                $ref: '#/components/schemas/creditnote_refund_id'
              creditnote_id:
                $ref: '#/components/schemas/creditnote_id'
              date:
                $ref: '#/components/schemas/date'
              refund_mode:
                $ref: '#/components/schemas/refund_mode'
              payment_form:
                $ref: '#/components/schemas/payment_form'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              creditnote_number:
                $ref: '#/components/schemas/creditnote_number'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              description:
                description: A small description about the item.
                type: string
                example: ''
              amount_bcy:
                $ref: '#/components/schemas/amount_bcy'
              amount_fcy:
                $ref: '#/components/schemas/amount_fcy'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    refund-credit-note-request:
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        payment_form:
          $ref: '#/components/schemas/payment_form'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        description:
          $ref: '#/components/schemas/description'
    refund-credit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note amount is refunded successfully.
          readOnly: true
        creditnote_refund:
          type: object
          properties:
            creditnote_refund_id:
              $ref: '#/components/schemas/creditnote_refund_id'
            creditnote_id:
              $ref: '#/components/schemas/creditnote_id'
            date:
              $ref: '#/components/schemas/date'
            refund_mode:
              $ref: '#/components/schemas/refund_mode'
            payment_form:
              $ref: '#/components/schemas/payment_form'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            amount:
              $ref: '#/components/schemas/amount'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            description:
              $ref: '#/components/schemas/description'
    get-credit-note-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund of the credit note is displayed successfully.
          readOnly: true
        creditnote_refund:
          type: object
          properties:
            creditnote_refund_id:
              $ref: '#/components/schemas/creditnote_refund_id'
            creditnote_id:
              $ref: '#/components/schemas/creditnote_id'
            date:
              $ref: '#/components/schemas/date'
            refund_mode:
              $ref: '#/components/schemas/refund_mode'
            payment_form:
              $ref: '#/components/schemas/payment_form'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            amount:
              $ref: '#/components/schemas/amount'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            description:
              $ref: '#/components/schemas/description'
    update-credit-note-refund-request:
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        payment_form:
          $ref: '#/components/schemas/payment_form'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        description:
          $ref: '#/components/schemas/description'
    update-credit-note-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The credit note refund is updated successfully.
          readOnly: true
        creditnote_refund:
          type: object
          properties:
            creditnote_refund_id:
              $ref: '#/components/schemas/creditnote_refund_id'
            creditnote_id:
              $ref: '#/components/schemas/creditnote_id'
            date:
              $ref: '#/components/schemas/date'
            refund_mode:
              $ref: '#/components/schemas/refund_mode'
            payment_form:
              $ref: '#/components/schemas/payment_form'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            amount:
              $ref: '#/components/schemas/amount'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            description:
              $ref: '#/components/schemas/description'
    delete-credit-note-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund has been successfully deleted.
          readOnly: true
    create-a-customer-debit-note-request:
      required:
      - customer_id
      - line_items
      - type
      - reference_invoice_id
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        type:
          type: string
          description: Type of the document. For customer debit notes, this should
            be <code>debit_note</code>
          example: debit_note
          enum:
          - debit_note
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons:
          $ref: '#/components/schemas/contact_persons'
        invoice_number:
          $ref: '#/components/schemas/invoice_number'
        date:
          $ref: '#/components/schemas/date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        due_date:
          $ref: '#/components/schemas/due_date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        location_id:
          $ref: '#/components/schemas/location_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        reference_invoice_id:
          $ref: '#/components/schemas/reference_invoice_id'
        reference_invoice_type:
          $ref: '#/components/schemas/reference_invoice_type'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: List of line items in the debit note.
          type: array
          items:
            type: object
            required:
            - item_id
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              location_id:
                $ref: '#/components/schemas/location_id'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tags:
                $ref: '#/components/schemas/tags'
    update-a-customer-debit-note-request:
      required:
      - customer_id
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        type:
          type: string
          description: Type of the document. For customer debit notes, this should
            be debit_note
          example: debit_note
          enum:
          - debit_note
        currency_id:
          $ref: '#/components/schemas/currency_id'
        invoice_number:
          $ref: '#/components/schemas/invoice_number'
        template_id:
          $ref: '#/components/schemas/template_id'
        date:
          $ref: '#/components/schemas/date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        due_date:
          $ref: '#/components/schemas/due_date'
        discount:
          $ref: '#/components/schemas/discount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        location_id:
          $ref: '#/components/schemas/location_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: List of line items in the debit note.
          type: array
          items:
            type: object
            required:
            - item_id
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              product_type:
                $ref: '#/components/schemas/product_type'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              location_id:
                $ref: '#/components/schemas/location_id'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              unit:
                $ref: '#/components/schemas/unit'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              item_total:
                $ref: '#/components/schemas/item_total'
              tags:
                $ref: '#/components/schemas/tags'
    create-a-customer-debit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The debit note has been created.
        invoice:
          $ref: '#/components/schemas/debit_note'
    update-a-customer-debit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The debit note has been updated.
        invoice:
          $ref: '#/components/schemas/debit_note'
    get-a-customer-debit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        invoice:
          $ref: '#/components/schemas/debit_note'
    list-customer-debit-notes-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        debit_notes:
          type: array
          items:
            $ref: '#/components/schemas/debit_note'
        page_context:
          $ref: '#/components/schemas/page_context'
    delete-a-customer-debit-note-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The invoice has been deleted.
    debit_note:
      type: object
      properties:
        invoice_id:
          type: string
          example: '982000000567114'
        invoice_number:
          type: string
          example: DN-00001
        date:
          type: string
          example: '2013-11-17'
        status:
          type: string
          example: draft
        customer_id:
          type: string
          example: '982000000567001'
        customer_name:
          type: string
          example: Bowman & Co
        total:
          type: number
          example: 40.6
        balance:
          type: number
          example: 40.6
        created_time:
          type: string
          example: 2013-11-17T17:09:00+0530
        last_modified_time:
          type: string
          example: 2013-11-17T17:09:00+0530
        line_items:
          type: array
          items:
            $ref: '#/components/schemas/line_item'
        reference_invoice:
          $ref: '#/components/schemas/reference_invoice_data'
        tags:
          $ref: '#/components/schemas/tags_response'
    reference_invoice_data:
      type: object
      properties:
        reference_invoice_number:
          type: string
          example: INV-000887
        reference_invoice_status:
          type: string
          example: draft
        reference_invoice_amount:
          type: number
          example: 3.52
        reference_invoice_id:
          type: string
          example: '982000000567005'
    line_item:
      type: object
      properties:
        line_item_id:
          type: string
          example: '982000000567115'
        item_id:
          type: string
          example: '982000000030049'
        item_name:
          type: string
          example: Desktop Units
        description:
          type: string
          example: Desktop Units
        rate:
          type: number
          example: 20
        quantity:
          type: number
          example: 2
        unit:
          type: string
          example: Nos
        item_total:
          type: number
          example: 40
        tags:
          $ref: '#/components/schemas/tags_response'
    reference_invoice_type:
      type: string
      description: Invoice type description
      example: registered
    reference_invoice_number:
      type: string
      example: INV-000887
    reference_invoice_status:
      type: string
      example: draft
    reference_invoice_amount:
      type: number
      example: 3.52
    debit_note_number:
      type: string
      description: Debit note number
      example: DN-00001
    payment-response:
      type: object
      properties:
        payment_id:
          $ref: '#/components/schemas/payment_id'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        amount:
          $ref: '#/components/schemas/amount'
        amount_refunded:
          $ref: '#/components/schemas/amount_refunded'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        date:
          $ref: '#/components/schemas/date'
        status:
          $ref: '#/components/schemas/status'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        email:
          $ref: '#/components/schemas/email'
        tags:
          $ref: '#/components/schemas/tags_response'
        invoices:
          description: List of invoices associated with the payment. Each invoice
            object contains <code>invoice_id</code>, <code>invoice_number</code>,
            <code>date</code>, <code>invoice_amount</code>, <code>amount_applied</code>
            and <code>balance_amount</code>.
          type: array
          items:
            type: object
            properties:
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              invoice_number:
                $ref: '#/components/schemas/invoice_number'
              date:
                description: Date on which the invoice was raised.
                example: '2016-06-05'
              invoice_amount:
                $ref: '#/components/schemas/invoice_amount'
              amount_applied:
                $ref: '#/components/schemas/amount_applied'
              balance_amount:
                $ref: '#/components/schemas/balance_amount'
              tax_amount_withheld:
                $ref: '#/components/schemas/tax_amount_withheld'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        custom_fields:
          description: Additional fields for the payments.
          type: array
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
              label:
                $ref: '#/components/schemas/label'
              data_type:
                $ref: '#/components/schemas/data_type'
    amount_refunded:
      description: Amount that is refund. Refunds are applicable only for payments
        whose payment_mode is <code>autotransaction</code>. Refunds would be made
        to the respective card provided by the customer.
      type: number
      format: double
      example: 50
    autotransaction:
      description: If the payment mode is <code>autotransaction</code>, autotransaction
        information will be displayed in the autotransaction object. It contains <code>autotransaction_id</code>,
        <code>payment_gateway</code>, <code>gateway_transaction_id</code>, <code>card_id</code>,
        <code>last_four_digits</code>, <code>expiry_month</code> and <code>expiry_year</code>.
      type: object
      properties:
        autotransaction_id:
          $ref: '#/components/schemas/autotransaction_id'
        payment_gateway:
          $ref: '#/components/schemas/payment_gateway'
        gateway_transaction_id:
          $ref: '#/components/schemas/gateway_transaction_id'
        gateway_error_message:
          $ref: '#/components/schemas/gateway_error_message'
        card_id:
          $ref: '#/components/schemas/card_id'
        last_four_digits:
          $ref: '#/components/schemas/last_four_digits'
        expiry_month:
          $ref: '#/components/schemas/expiry_month'
        expiry_year:
          $ref: '#/components/schemas/expiry_year'
    autotransaction_id:
      description: 'Auto-transaction ID generated for the payment made. Note: This
        value is a set of numeric characters.'
      type: string
      example: '90300000079465'
    payment_gateway:
      description: Name of the payment gateway associated with payment.
      type: string
      example: payflow_pro
    gateway_transaction_id:
      description: 'Transaction ID provided by the gateway for the transaction. Note:
        This value is a set of numeric characters.'
      type: string
      example: B10E6E0F31BD
    gateway_error_message:
      description: Gateway error message for a failed transaction.
      type: string
      example: Gateway error message for a failed transaction.
    card_id:
      description: 'Card ID of the card associated with the transaction. Note: This
        value is a set of numeric characters.'
      type: string
      example: '90300000079226'
    last_four_digits:
      type: string
      example: ''
    expiry_month:
      description: Expiry month of the card.
      type: integer
      example: 9
    expiry_year:
      description: Expiry year of the card.
      type: integer
      example: 2030
    invoice_amount:
      description: Total amount raised for the invoice.
      type: number
      format: double
      example: 450
    balance_amount:
      description: Unpaid amount of the invoice.
      type: number
      format: double
      example: 0
    bank_charges:
      description: Bank Charges applied to the transaction
      type: number
      format: double
      example: 0
    update-an-customerpayment-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    payment_refund_id:
      description: ID of the Payment Refund
      type: string
      example: '460000000078307'
    bcy_amount:
      description: Amount in Base Currency of the Organisation
      type: number
      format: double
      example: 2000
    retainerinvoice_id:
      description: ID of the Retainer Invoice
      type: string
    create-a-payment-request:
      required:
      - customer_id
      - payment_mode
      - amount
      - invoices
      - date
      - invoice_id
      - amount_applied
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        amount:
          $ref: '#/components/schemas/amount'
        date:
          $ref: '#/components/schemas/date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        invoices:
          description: List of invoices associated with the payment. Each invoice
            object contains <code>invoice_id</code>, <code>invoice_number</code>,
            <code>date</code>, <code>invoice_amount</code>, <code>amount_applied</code>
            and <code>balance_amount</code>.
          type: array
          items:
            type: object
            properties:
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              amount_applied:
                $ref: '#/components/schemas/amount_applied'
              tax_amount_withheld:
                $ref: '#/components/schemas/tax_amount_withheld'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        payment_form:
          $ref: '#/components/schemas/payment_form'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        custom_fields:
          description: Additional fields for the payments.
          type: array
          items:
            type: object
            properties:
              label:
                $ref: '#/components/schemas/label'
              value:
                $ref: '#/components/schemas/value'
        location_id:
          $ref: '#/components/schemas/location_id'
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        amount_applied:
          $ref: '#/components/schemas/amount_applied'
        account_id:
          $ref: '#/components/schemas/account_id'
        contact_persons:
          $ref: '#/components/schemas/contact_persons'
        retainerinvoice_id:
          $ref: '#/components/schemas/retainerinvoice_id'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The payment has been created.
          readOnly: true
        payment:
          $ref: '#/components/schemas/payment-response'
    list-customer-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        customer_payments:
          type: array
          items:
            type: object
            properties:
              payment_id:
                $ref: '#/components/schemas/payment_id'
              payment_number:
                $ref: '#/components/schemas/payment_number'
              invoice_number:
                $ref: '#/components/schemas/invoice_number'
              date:
                $ref: '#/components/schemas/date'
              payment_mode:
                $ref: '#/components/schemas/payment_mode'
              amount:
                $ref: '#/components/schemas/amount'
              bcy_amount:
                $ref: '#/components/schemas/bcy_amount'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              tags:
                $ref: '#/components/schemas/tags_response'
        page_context: {}
    update-a-payment-request:
      required:
      - customer_id
      - payment_mode
      - amount
      - invoices
      - invoice_id
      - amount_applied
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        amount:
          $ref: '#/components/schemas/amount'
        date:
          $ref: '#/components/schemas/date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        invoices:
          description: List of invoices associated with the payment. Each invoice
            object contains <code>invoice_id</code>, <code>invoice_number</code>,
            <code>date</code>, <code>invoice_amount</code>, <code>amount_applied</code>
            and <code>balance_amount</code>.
          type: array
          items:
            type: object
            properties:
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              amount_applied:
                $ref: '#/components/schemas/amount_applied'
              tax_amount_withheld:
                $ref: '#/components/schemas/tax_amount_withheld'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        payment_form:
          $ref: '#/components/schemas/payment_form'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        custom_fields:
          description: Additional fields for the payments.
          type: array
          items:
            type: object
            properties:
              label:
                $ref: '#/components/schemas/label'
              value:
                $ref: '#/components/schemas/value'
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        amount_applied:
          $ref: '#/components/schemas/amount_applied'
        tax_amount_withheld:
          $ref: '#/components/schemas/tax_amount_withheld'
        location_id:
          $ref: '#/components/schemas/location_id'
        account_id:
          $ref: '#/components/schemas/account_id'
        retainerinvoice_id:
          $ref: '#/components/schemas/retainerinvoice_id'
        tags:
          $ref: '#/components/schemas/tags'
    update-a-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The payment details have been updated.
          readOnly: true
        payment:
          type: object
          properties:
            payment_id:
              $ref: '#/components/schemas/payment_id'
            payment_mode:
              $ref: '#/components/schemas/payment_mode'
            amount:
              $ref: '#/components/schemas/amount'
            amount_refunded:
              $ref: '#/components/schemas/amount_refunded'
            bank_charges:
              $ref: '#/components/schemas/bank_charges'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            description:
              $ref: '#/components/schemas/description'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            email:
              $ref: '#/components/schemas/email'
            invoices:
              description: List of invoices associated with the payment. Each invoice
                object contains <code>invoice_id</code>, <code>invoice_number</code>,
                <code>date</code>, <code>invoice_amount</code>, <code>amount_applied</code>
                and <code>balance_amount</code>.
              type: array
              items:
                type: object
                properties:
                  invoice_id:
                    $ref: '#/components/schemas/invoice_id'
                  invoice_number:
                    $ref: '#/components/schemas/invoice_number'
                  date:
                    description: Date on which the invoice was raised.
                    example: '2016-06-05'
                  invoice_amount:
                    $ref: '#/components/schemas/invoice_amount'
                  amount_applied:
                    $ref: '#/components/schemas/amount_applied'
                  balance_amount:
                    $ref: '#/components/schemas/balance_amount'
                  tax_amount_withheld:
                    $ref: '#/components/schemas/tax_amount_withheld'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            custom_fields:
              description: Additional fields for the payments.
              type: array
              items:
                type: object
                properties:
                  index:
                    $ref: '#/components/schemas/index'
                  value:
                    $ref: '#/components/schemas/value'
                  label:
                    $ref: '#/components/schemas/label'
                  data_type:
                    $ref: '#/components/schemas/data_type'
    retrieve-a-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        payment:
          type: object
          properties:
            payment_id:
              $ref: '#/components/schemas/payment_id'
            payment_mode:
              $ref: '#/components/schemas/payment_mode'
            amount:
              $ref: '#/components/schemas/amount'
            amount_refunded:
              $ref: '#/components/schemas/amount_refunded'
            bank_charges:
              $ref: '#/components/schemas/bank_charges'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            description:
              $ref: '#/components/schemas/description'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            email:
              $ref: '#/components/schemas/email'
            invoices:
              description: List of invoices associated with the payment. Each invoice
                object contains <code>invoice_id</code>, <code>invoice_number</code>,
                <code>date</code>, <code>invoice_amount</code>, <code>amount_applied</code>
                and <code>balance_amount</code>.
              type: array
              items:
                type: object
                properties:
                  invoice_id:
                    $ref: '#/components/schemas/invoice_id'
                  invoice_number:
                    $ref: '#/components/schemas/invoice_number'
                  date:
                    description: Date on which the invoice was raised.
                    example: '2016-06-05'
                  invoice_amount:
                    $ref: '#/components/schemas/invoice_amount'
                  amount_applied:
                    $ref: '#/components/schemas/amount_applied'
                  balance_amount:
                    $ref: '#/components/schemas/balance_amount'
                  tax_amount_withheld:
                    $ref: '#/components/schemas/tax_amount_withheld'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            custom_fields:
              description: Additional fields for the payments.
              type: array
              items:
                type: object
                properties:
                  index:
                    $ref: '#/components/schemas/index'
                  value:
                    $ref: '#/components/schemas/value'
                  label:
                    $ref: '#/components/schemas/label'
                  data_type:
                    $ref: '#/components/schemas/data_type'
    list-refunds-of-a-customer-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The list of refunds of the customer is displayed successfully.
          readOnly: true
        payment_refunds:
          type: array
          items:
            type: object
            properties:
              payment_refund_id:
                $ref: '#/components/schemas/payment_refund_id'
              payment_id:
                $ref: '#/components/schemas/payment_id'
              date:
                $ref: '#/components/schemas/date'
              refund_mode:
                $ref: '#/components/schemas/refund_mode'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              payment_number:
                $ref: '#/components/schemas/payment_number'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              amount_bcy:
                $ref: '#/components/schemas/amount_bcy'
              amount_fcy:
                $ref: '#/components/schemas/amount_fcy'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    refund-an-excess-customer-payment-request:
      required:
      - date
      - amount
      - from_account_id
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        payment_form:
          $ref: '#/components/schemas/payment_form'
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        description:
          $ref: '#/components/schemas/description'
    refund-an-excess-customer-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund information for this payment has been saved.
          readOnly: true
        payment_refunds:
          type: array
          items:
            type: object
            properties:
              payment_refund_id:
                $ref: '#/components/schemas/payment_refund_id'
              payment_id:
                $ref: '#/components/schemas/payment_id'
              date:
                $ref: '#/components/schemas/date'
              refund_mode:
                $ref: '#/components/schemas/refund_mode'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              amount:
                $ref: '#/components/schemas/amount'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              payment_form:
                $ref: '#/components/schemas/payment_form'
              description:
                $ref: '#/components/schemas/description'
    details-of-a-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendorpayment_refund:
          $ref: '#/components/schemas/vendorpayment_refund'
    update-a-refund-request:
      required:
      - date
      - amount
      - to_account_id
      type: object
      properties:
        date:
          description: Date of the Vendor Payment Refund.
          type: string
          example: '2017-01-10'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          description: Reference Number of the Payment Refund
          type: string
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        description:
          description: Description of the Payment Refund
          type: string
          example: Payment Refund
    update-a-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund information has been saved.
          readOnly: true
        vendorpayment_refund:
          $ref: '#/components/schemas/vendorpayment_refund'
    delete-a-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund has been deleted.
          readOnly: true
    bulk-delete-customer-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected customer payments have been deleted.
          readOnly: true
    expense-response:
      type: object
      properties:
        expense_id:
          $ref: '#/components/schemas/expense_id'
        transaction_id:
          $ref: '#/components/schemas/transaction_id'
        transaction_type:
          $ref: '#/components/schemas/transaction_type'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        destination_of_supply_state:
          $ref: '#/components/schemas/destination_of_supply_state'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        paid_through_account_name:
          $ref: '#/components/schemas/paid_through_account_name'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        reverse_charge_tax_name:
          $ref: '#/components/schemas/reverse_charge_tax_name'
        reverse_charge_tax_percentage:
          $ref: '#/components/schemas/reverse_charge_tax_percentage'
        reverse_charge_tax_amount:
          $ref: '#/components/schemas/reverse_charge_tax_amount'
        tax_amount:
          $ref: '#/components/schemas/tax_amount'
        is_itemized_expense:
          $ref: '#/components/schemas/is_itemized_expense'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        trip_id:
          $ref: '#/components/schemas/trip_id'
        trip_number:
          $ref: '#/components/schemas/trip_number'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        taxes:
          $ref: '#/components/schemas/taxes'
        expense_item_id:
          $ref: '#/components/schemas/expense_item_id'
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        date:
          $ref: '#/components/schemas/date'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        total:
          $ref: '#/components/schemas/total'
        bcy_total:
          $ref: '#/components/schemas/bcy_total'
        amount:
          $ref: '#/components/schemas/amount'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        is_personal:
          $ref: '#/components/schemas/is_personal'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        expense_receipt_name:
          $ref: '#/components/schemas/expense_receipt_name'
        expense_receipt_type:
          $ref: '#/components/schemas/expense_receipt_type'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        status:
          $ref: '#/components/schemas/status'
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        invoice_number:
          $ref: '#/components/schemas/invoice_number'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        project_id:
          $ref: '#/components/schemas/project_id'
        project_name:
          $ref: '#/components/schemas/project_name'
        mileage_rate:
          $ref: '#/components/schemas/mileage_rate'
        mileage_type:
          $ref: '#/components/schemas/mileage_type'
        expense_type:
          $ref: '#/components/schemas/expense_type'
        start_reading:
          $ref: '#/components/schemas/start_reading'
        end_reading:
          $ref: '#/components/schemas/end_reading'
        tags:
          $ref: '#/components/schemas/tags_response'
    source_of_supply:
      description: Place from where the goods/services are supplied. (If not given,
        <code>place of contact</code> given for the contact will be taken)
      type: string
      example: TN
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    destination_of_supply:
      description: Place where the goods/services are supplied to. (If not given,
        organisation's home state will be taken)
      type: string
      example: TN
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    reverse_charge_tax_id:
      description: Enter reverse charge tax ID
      type: string
      example: 982000000567254
      x-node_available_in:
      - in
      - gcc
      - za
    reverse_charge_tax_name:
      description: Enter the tax name of the reverse charge
      type: string
      example: intra
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    reverse_charge_tax_percentage:
      description: Tax percentage of the reverse charge
      type: number
      format: double
      example: 10
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    reverse_charge_tax_amount:
      description: Tax amount of the reverse charge
      type: number
      format: double
      example: 100
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    destination_of_supply_state:
      description: Place to where the goods/services are supplied
      type: string
      example: AP
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    paid_through_account_name:
      description: Name of the Paid through Account
      type: string
    is_itemized_expense:
      type: boolean
      example: false
    trip_id:
      description: 'Enter trip ID. Note: This value is a set of numeric characters.'
      type: string
      example: ''
    trip_number:
      description: Enter trip number
      type: string
      example: ''
    reverse_charge_vat_total:
      description: Total of the Reverse Charge
      type: number
      format: double
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    acquisition_vat_total:
      description: Total of the VAT Acquistion
      type: number
      format: double
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    receipt:
      description: 'Param Description. Allowed Extensions: <code>gif</code>, <code>png</code>,
        <code>jpeg</code>, <code>jpg</code>, <code>bmp</code> and <code>pdf</code>.'
      type: string
      format: binary
    vendor_name:
      description: Name of the Vendor
      type: string
      example: Bowmen and co
    vendor_id:
      description: 'Preferred vendor ID. Note: This value is a set of numeric characters.'
      type: string
      example: ' '
    recurring_expense_id:
      description: ID of the Recurring Expense
      type: string
    paid_through_account_id:
      description: ID of the credit/ bank account the payment is made.
      type: string
      example: '460000000000358'
    bcy_total_without_tax:
      type: number
      format: double
      example: 100
    total_without_tax:
      type: number
      format: double
      example: 100
    is_billable:
      type: boolean
      example: true
    expense_receipt_type:
      type: string
      example: ' '
    expense_item_id:
      type: string
      description: 'Unique identifier of the expense item. Note: This value is a set
        of numeric characters.'
      example: 982000000567220
    mileage_type:
      description: 'Type of Mileage. Allowed Values : <code>gmaps</code>,<code>imaps</code>,<code>gps</code>,<code>odometer</code>,<code>wmaps</code>,<code>manual</code>,<code>non_mileage</code>
        and <code>per_diem</code>'
      type: string
      example: non_mileage
    acquisition_vat_id:
      description: (Optional) This is the ID of the tax applied in case this is an
        EU - goods journal and acquisition VAT needs to be reported.
      type: string
      x-node_available_in:
      - uk
      x-node_unavailable_in: []
    reverse_charge_vat_id:
      description: (Optional) This is the ID of the tax applied in case this is a
        non UK - service journal and reverse charge VAT needs to be reported.
      type: string
      x-node_available_in:
      - uk
      x-node_unavailable_in: []
    start_reading:
      description: Start Reading of the Odometer
      type: string
    end_reading:
      description: End Reading of the Odometer
      type: string
    distance:
      description: Distance Covered
      type: number
      format: double
      example: 4
    mileage_unit:
      description: Unit of Mileage
      type: string
      example: km
    mileage_rate:
      description: Mileage Rate to be Applied
      type: number
      format: double
      example: 0
    employee_id:
      description: ID of the Employee
      type: string
    vehicle_type:
      description: 'Type of Vehicle. Allowed values: <code>car</code>, <code>van</code>,<code>motorcycle</code>
        and <code>bike</code>'
      type: string
      example: car
    can_reclaim_vat_on_mileage:
      description: Check if VAT can be reclaimed on mileage
      type: boolean
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    fuel_type:
      description: 'Type of Fuel Used. Allowed Values : <code>petrol</code>, <code>lpg</code>,
        <code>diesel</code>'
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    engine_capacity_range:
      description: 'Type of Fuel Used. Allowed Values : <code>less_than_1400cc</code>,
        <code>between_1401cc_and_1600cc</code>, <code>between_1601cc_and_2000cc</code>
        and <code>more_than_2000cc</code>'
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    expense_type:
      description: Type of Expense
      type: string
      example: non_mileage
    is_personal:
      type: boolean
      example: false
    delete_receipt:
      type: boolean
      description: To specify whether the existing receipt needs to be deleted when
        a new receipt is being added to an expense.
      example: false
    tax:
      type: array
      items:
        $ref: '#/components/schemas/tax-response'
    acquisition_vat_summary:
      description: Summary of the VAT Acquistion
      type: array
      items:
        type: object
        properties:
          tax:
            $ref: '#/components/schemas/tax'
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    reverse_charge_vat_summary:
      description: Summary of the Reverse Charge
      type: array
      items:
        type: object
        properties:
          tax:
            $ref: '#/components/schemas/tax'
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    create-an-expense-request:
      required:
      - date
      - account_id
      - amount
      - paid_through_account_id
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        date:
          $ref: '#/components/schemas/date'
        amount:
          $ref: '#/components/schemas/amount'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          $ref: '#/components/schemas/line_items'
        taxes:
          $ref: '#/components/schemas/taxes'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        project_id:
          $ref: '#/components/schemas/project_id'
        mileage_type:
          $ref: '#/components/schemas/mileage_type'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        product_type:
          $ref: '#/components/schemas/product_type'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        start_reading:
          $ref: '#/components/schemas/start_reading'
        end_reading:
          $ref: '#/components/schemas/end_reading'
        distance:
          $ref: '#/components/schemas/distance'
        mileage_unit:
          $ref: '#/components/schemas/mileage_unit'
        mileage_rate:
          $ref: '#/components/schemas/mileage_rate'
        employee_id:
          $ref: '#/components/schemas/employee_id'
        vehicle_type:
          $ref: '#/components/schemas/vehicle_type'
        can_reclaim_vat_on_mileage:
          $ref: '#/components/schemas/can_reclaim_vat_on_mileage'
        fuel_type:
          $ref: '#/components/schemas/fuel_type'
        engine_capacity_range:
          $ref: '#/components/schemas/engine_capacity_range'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    create-an-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The expense has been recorded.
          readOnly: true
        expense:
          $ref: '#/components/schemas/expense-response'
    list-expenses-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        expenses:
          type: array
          items:
            type: object
            properties:
              expense_id:
                $ref: '#/components/schemas/expense_id'
              date:
                $ref: '#/components/schemas/date'
              account_name:
                $ref: '#/components/schemas/account_name'
              description:
                $ref: '#/components/schemas/description'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              bcy_total:
                $ref: '#/components/schemas/bcy_total'
              bcy_total_without_tax:
                $ref: '#/components/schemas/bcy_total_without_tax'
              total:
                $ref: '#/components/schemas/total'
              total_without_tax:
                $ref: '#/components/schemas/total_without_tax'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              status:
                $ref: '#/components/schemas/status'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              expense_receipt_name:
                $ref: '#/components/schemas/expense_receipt_name'
              mileage_rate:
                $ref: '#/components/schemas/mileage_rate'
              mileage_unit:
                $ref: '#/components/schemas/mileage_unit'
              expense_type:
                $ref: '#/components/schemas/expense_type'
              start_reading:
                $ref: '#/components/schemas/start_reading'
              end_reading:
                $ref: '#/components/schemas/end_reading'
        page_context:
          type: array
          items:
            type: object
            properties:
              sort_column:
                $ref: '#/components/schemas/sort_column'
              filter_by:
                $ref: '#/components/schemas/filter_by'
              search_text:
                $ref: '#/components/schemas/search_text'
              applied_filter:
                $ref: '#/components/schemas/applied_filter'
              page:
                $ref: '#/components/schemas/page'
              per_page:
                $ref: '#/components/schemas/per_page'
    update-an-expense-request:
      required:
      - date
      - account_id
      - amount
      - paid_through_account_id
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        date:
          $ref: '#/components/schemas/date'
        amount:
          description: Amount of the Expense.
          type: number
          format: double
          example: 120.5
        tax_id:
          $ref: '#/components/schemas/tax_id'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        line_items:
          $ref: '#/components/schemas/line_items'
        location_id:
          $ref: '#/components/schemas/location_id'
        taxes:
          $ref: '#/components/schemas/taxes'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        project_id:
          $ref: '#/components/schemas/project_id'
        mileage_type:
          $ref: '#/components/schemas/mileage_type'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        product_type:
          $ref: '#/components/schemas/product_type'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        start_reading:
          $ref: '#/components/schemas/start_reading'
        end_reading:
          $ref: '#/components/schemas/end_reading'
        distance:
          $ref: '#/components/schemas/distance'
        mileage_unit:
          $ref: '#/components/schemas/mileage_unit'
        mileage_rate:
          $ref: '#/components/schemas/mileage_rate'
        employee_id:
          $ref: '#/components/schemas/employee_id'
        vehicle_type:
          $ref: '#/components/schemas/vehicle_type'
        can_reclaim_vat_on_mileage:
          $ref: '#/components/schemas/can_reclaim_vat_on_mileage'
        fuel_type:
          $ref: '#/components/schemas/fuel_type'
        engine_capacity_range:
          $ref: '#/components/schemas/engine_capacity_range'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    update-an-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Expense information has been updated.
          readOnly: true
        expense:
          type: object
          properties:
            expense_id:
              $ref: '#/components/schemas/expense_id'
            transaction_id:
              $ref: '#/components/schemas/transaction_id'
            transaction_type:
              $ref: '#/components/schemas/transaction_type'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            destination_of_supply:
              $ref: '#/components/schemas/destination_of_supply'
            destination_of_supply_state:
              $ref: '#/components/schemas/destination_of_supply_state'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            source_of_supply:
              $ref: '#/components/schemas/source_of_supply'
            paid_through_account_name:
              $ref: '#/components/schemas/paid_through_account_name'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            reverse_charge_tax_id:
              $ref: '#/components/schemas/reverse_charge_tax_id'
            reverse_charge_tax_name:
              $ref: '#/components/schemas/reverse_charge_tax_name'
            reverse_charge_tax_percentage:
              $ref: '#/components/schemas/reverse_charge_tax_percentage'
            reverse_charge_tax_amount:
              $ref: '#/components/schemas/reverse_charge_tax_amount'
            tax_amount:
              $ref: '#/components/schemas/tax_amount'
            is_itemized_expense:
              $ref: '#/components/schemas/is_itemized_expense'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            trip_id:
              $ref: '#/components/schemas/trip_id'
            trip_number:
              $ref: '#/components/schemas/trip_number'
            reverse_charge_vat_total:
              $ref: '#/components/schemas/reverse_charge_vat_total'
            acquisition_vat_total:
              $ref: '#/components/schemas/acquisition_vat_total'
            acquisition_vat_summary:
              $ref: '#/components/schemas/acquisition_vat_summary'
            reverse_charge_vat_summary:
              $ref: '#/components/schemas/reverse_charge_vat_summary'
            taxes:
              $ref: '#/components/schemas/taxes'
            expense_item_id:
              $ref: '#/components/schemas/expense_item_id'
            account_id:
              $ref: '#/components/schemas/account_id'
            account_name:
              $ref: '#/components/schemas/account_name'
            date:
              $ref: '#/components/schemas/date'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            bcy_total:
              $ref: '#/components/schemas/bcy_total'
            amount:
              description: Amount of the Expense.
              type: number
              format: double
              example: 120.5
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            description:
              $ref: '#/components/schemas/description'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            is_personal:
              $ref: '#/components/schemas/is_personal'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            expense_receipt_name:
              $ref: '#/components/schemas/expense_receipt_name'
            expense_receipt_type:
              $ref: '#/components/schemas/expense_receipt_type'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            status:
              $ref: '#/components/schemas/status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            invoice_number:
              $ref: '#/components/schemas/invoice_number'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            mileage_rate:
              $ref: '#/components/schemas/mileage_rate'
            mileage_type:
              $ref: '#/components/schemas/mileage_type'
            expense_type:
              $ref: '#/components/schemas/expense_type'
            start_reading:
              $ref: '#/components/schemas/start_reading'
            end_reading:
              $ref: '#/components/schemas/end_reading'
            tags:
              $ref: '#/components/schemas/tags_response'
    get-an-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        expense:
          type: object
          properties:
            expense_id:
              $ref: '#/components/schemas/expense_id'
            transaction_id:
              $ref: '#/components/schemas/transaction_id'
            transaction_type:
              $ref: '#/components/schemas/transaction_type'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            destination_of_supply:
              $ref: '#/components/schemas/destination_of_supply'
            destination_of_supply_state:
              $ref: '#/components/schemas/destination_of_supply_state'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            source_of_supply:
              $ref: '#/components/schemas/source_of_supply'
            paid_through_account_name:
              $ref: '#/components/schemas/paid_through_account_name'
            vat_reg_no:
              $ref: '#/components/schemas/vat_reg_no'
            reverse_charge_tax_id:
              $ref: '#/components/schemas/reverse_charge_tax_id'
            reverse_charge_tax_name:
              $ref: '#/components/schemas/reverse_charge_tax_name'
            reverse_charge_tax_percentage:
              $ref: '#/components/schemas/reverse_charge_tax_percentage'
            reverse_charge_tax_amount:
              $ref: '#/components/schemas/reverse_charge_tax_amount'
            tax_amount:
              $ref: '#/components/schemas/tax_amount'
            is_itemized_expense:
              $ref: '#/components/schemas/is_itemized_expense'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            trip_id:
              $ref: '#/components/schemas/trip_id'
            trip_number:
              $ref: '#/components/schemas/trip_number'
            reverse_charge_vat_total:
              $ref: '#/components/schemas/reverse_charge_vat_total'
            acquisition_vat_total:
              $ref: '#/components/schemas/acquisition_vat_total'
            acquisition_vat_summary:
              $ref: '#/components/schemas/acquisition_vat_summary'
            reverse_charge_vat_summary:
              $ref: '#/components/schemas/reverse_charge_vat_summary'
            expense_item_id:
              $ref: '#/components/schemas/expense_item_id'
            account_id:
              $ref: '#/components/schemas/account_id'
            account_name:
              $ref: '#/components/schemas/account_name'
            date:
              $ref: '#/components/schemas/date'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            bcy_total:
              $ref: '#/components/schemas/bcy_total'
            amount:
              $ref: '#/components/schemas/amount'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            description:
              $ref: '#/components/schemas/description'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            is_personal:
              $ref: '#/components/schemas/is_personal'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            expense_receipt_name:
              $ref: '#/components/schemas/expense_receipt_name'
            expense_receipt_type:
              $ref: '#/components/schemas/expense_receipt_type'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            status:
              $ref: '#/components/schemas/status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            invoice_number:
              $ref: '#/components/schemas/invoice_number'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            mileage_rate:
              $ref: '#/components/schemas/mileage_rate'
            mileage_type:
              $ref: '#/components/schemas/mileage_type'
            expense_type:
              $ref: '#/components/schemas/expense_type'
            start_reading:
              $ref: '#/components/schemas/start_reading'
            end_reading:
              $ref: '#/components/schemas/end_reading'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_item:
              type: object
              properties:
                line_item_id:
                  $ref: '#/components/schemas/line_item_id'
                account_id:
                  $ref: '#/components/schemas/account_id'
                account_name:
                  $ref: '#/components/schemas/account_name'
                description:
                  $ref: '#/components/schemas/description'
                tax_amount:
                  $ref: '#/components/schemas/tax_amount'
                tax_id:
                  $ref: '#/components/schemas/tax_id'
                tax_name:
                  $ref: '#/components/schemas/tax_name'
                tax_type:
                  $ref: '#/components/schemas/tax_type'
                tax_percentage:
                  $ref: '#/components/schemas/tax_percentage'
                item_total:
                  $ref: '#/components/schemas/item_total'
                item_order:
                  $ref: '#/components/schemas/item_order'
                amount:
                  $ref: '#/components/schemas/amount'
                hsn_or_sac:
                  $ref: '#/components/schemas/hsn_or_sac'
                location_id:
                  $ref: '#/components/schemas/location_id'
                reverse_charge_tax_id:
                  $ref: '#/components/schemas/reverse_charge_tax_id'
                reverse_charge_tax_name:
                  $ref: '#/components/schemas/reverse_charge_tax_name'
                reverse_charge_tax_percentage:
                  $ref: '#/components/schemas/reverse_charge_tax_percentage'
                reverse_charge_tax_amount:
                  $ref: '#/components/schemas/reverse_charge_tax_amount'
                tags:
                  $ref: '#/components/schemas/tags_response'
    delete-an-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The expense has been deleted.
          readOnly: true
    list-expense-history-and-comments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              expense_id:
                $ref: '#/components/schemas/expense_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    list-employees-request:
      type: object
      properties:
        sort_column:
          $ref: '#/components/schemas/sort_column'
        filter_by:
          $ref: '#/components/schemas/filter_by'
    list-employees-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        employees:
          type: array
          items:
            type: object
            properties:
              employee_id:
                $ref: '#/components/schemas/employee_id'
              name:
                $ref: '#/components/schemas/name'
              email:
                $ref: '#/components/schemas/email'
        page_context:
          type: array
          items:
            type: object
            properties:
              sort_column:
                $ref: '#/components/schemas/sort_column'
              filter_by:
                $ref: '#/components/schemas/filter_by'
              search_text:
                $ref: '#/components/schemas/search_text'
              applied_filter:
                $ref: '#/components/schemas/applied_filter'
              page:
                $ref: '#/components/schemas/page'
              per_page:
                $ref: '#/components/schemas/per_page'
    create-an-employee-request:
      required:
      - name
      - email
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        email:
          $ref: '#/components/schemas/email'
    create-an-employee-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        employee:
          type: object
          properties:
            employee_id:
              $ref: '#/components/schemas/employee_id'
            name:
              $ref: '#/components/schemas/name'
            email:
              $ref: '#/components/schemas/email'
    get-an-employee-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        employee:
          type: object
          properties:
            employee_id:
              $ref: '#/components/schemas/employee_id'
            name:
              $ref: '#/components/schemas/name'
            email:
              $ref: '#/components/schemas/email'
    delete-an-employee-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Employee has been deleted.
          readOnly: true
    get-an-expense-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    add-receipt-to-an-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The expense receipt has been attached.
          readOnly: true
    delete-a-receipt-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The attached expense receipt has been deleted.
          readOnly: true
    add-attachment-to-an-expense-request:
      type: object
      required:
      - attachment
      properties:
        attachment:
          type: string
          format: binary
          description: 'Expense attachment file to attach. Allowed Extensions: <code>gif</code>,
            <code>png</code>, <code>jpeg</code>, <code>jpg</code>, <code>bmp</code>,
            <code>pdf</code>, <code>xls</code>, <code>xlsx</code>, <code>doc</code>,
            <code>docx</code>, <code>txt</code> and <code>csv</code>'
          example: '@/attachment.pdf'
        totalFiles:
          type: integer
          description: Total number of files being uploaded.
          example: 1
        document_ids:
          type: array
          items:
            type: string
          description: 'Array of document IDs for batch processing. Note: This value
            is a set of numeric characters separated by comma.'
          example: []
    add-attachment-to-an-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The expense attachment has been attached.
          readOnly: true
        documents:
          type: array
          description: Details of the uploaded attachments.
          items:
            type: object
            properties:
              document_id:
                type: string
                example: '982000000567123'
                description: 'ID of the uploaded document. Note: This value is a set
                  of numeric characters.'
              file_name:
                type: string
                example: attachment.pdf
                description: Name of the uploaded file
              file_size:
                type: integer
                example: 1024
                description: Size of the uploaded file in bytes
              document_type:
                type: string
                example: attachment
                description: Type of the document
    recurring_expense-response:
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        destination_of_supply_state:
          $ref: '#/components/schemas/destination_of_supply_state'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        reverse_charge_tax_name:
          $ref: '#/components/schemas/reverse_charge_tax_name'
        reverse_charge_tax_percentage:
          $ref: '#/components/schemas/reverse_charge_tax_percentage'
        reverse_charge_tax_amount:
          $ref: '#/components/schemas/reverse_charge_tax_amount'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        recurrence_frequency:
          $ref: '#/components/schemas/recurrence_frequency'
        repeat_every:
          $ref: '#/components/schemas/repeat_every'
        amount:
          $ref: '#/components/schemas/amount'
        total:
          $ref: '#/components/schemas/total'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        bcy_total:
          $ref: '#/components/schemas/bcy_total'
        product_type:
          $ref: '#/components/schemas/product_type'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        project_id:
          $ref: '#/components/schemas/project_id'
        project_name:
          $ref: '#/components/schemas/project_name'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        tags:
          $ref: '#/components/schemas/tags_response'
        line_item:
          type: object
          properties:
            line_item_id:
              $ref: '#/components/schemas/line_item_id'
            account_id:
              $ref: '#/components/schemas/account_id'
            account_name:
              $ref: '#/components/schemas/account_name'
            description:
              $ref: '#/components/schemas/description'
            tax_amount:
              $ref: '#/components/schemas/tax_amount'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_type:
              $ref: '#/components/schemas/tax_type'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            item_total:
              $ref: '#/components/schemas/item_total'
            item_order:
              $ref: '#/components/schemas/item_order'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            reverse_charge_tax_id:
              $ref: '#/components/schemas/reverse_charge_tax_id'
            reverse_charge_tax_name:
              $ref: '#/components/schemas/reverse_charge_tax_name'
            reverse_charge_tax_percentage:
              $ref: '#/components/schemas/reverse_charge_tax_percentage'
            reverse_charge_tax_amount:
              $ref: '#/components/schemas/reverse_charge_tax_amount'
            tags:
              $ref: '#/components/schemas/tags_response'
    last_created_date:
      description: 'Search recurring expenses by date on when last expense was generated.
        Variants: <code>last_created_date_start</code>, <code>last_created_date_end</code>,
        <code>last_created_date_before</code> and <code>last_created_date_after</code>
        . Format [yyyy-mm-dd]'
      type: string
      example: 2013-11-18 00:00:00+00:00
    next_expense_date:
      description: 'Search recurring expenses by date on which next expense will be
        generated. Variants: <code>next_expense_date_start</code>, <code>next_expense_date_end</code>,
        <code>next_expense_date_before</code> and <code>next_expense_date_after</code>
        . Format [yyyy-mm-dd]'
      type: string
      example: 2013-12-18 00:00:00+00:00
    create-a-recurring-expense-request:
      required:
      - account_id
      - recurrence_name
      - start_date
      - recurrence_frequency
      - repeat_every
      - amount
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        recurrence_frequency:
          $ref: '#/components/schemas/recurrence_frequency'
        repeat_every:
          $ref: '#/components/schemas/repeat_every'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          $ref: '#/components/schemas/line_items'
        amount:
          $ref: '#/components/schemas/amount'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        product_type:
          $ref: '#/components/schemas/product_type'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        project_id:
          $ref: '#/components/schemas/project_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-recurring-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring expense has been created
          readOnly: true
        recurring_expense:
          $ref: '#/components/schemas/recurring_expense-response'
    list-recurring-expenses-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        recurring_expenses:
          type: array
          items:
            type: object
            properties:
              recurring_expense_id:
                $ref: '#/components/schemas/recurring_expense_id'
              recurrence_name:
                $ref: '#/components/schemas/recurrence_name'
              recurrence_frequency:
                $ref: '#/components/schemas/recurrence_frequency'
              repeat_every:
                $ref: '#/components/schemas/repeat_every'
              last_created_date:
                $ref: '#/components/schemas/last_created_date'
              next_expense_date:
                $ref: '#/components/schemas/next_expense_date'
              account_name:
                $ref: '#/components/schemas/account_name'
              description:
                $ref: '#/components/schemas/description'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              total:
                $ref: '#/components/schemas/total'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
              status:
                $ref: '#/components/schemas/status'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              tags:
                $ref: '#/components/schemas/tags_response'
        page_context:
          type: array
          items:
            type: object
            properties:
              sort_column:
                $ref: '#/components/schemas/sort_column'
              filter_by:
                $ref: '#/components/schemas/filter_by'
              search_text:
                $ref: '#/components/schemas/search_text'
    update-a-recurring-expense-request:
      required:
      - account_id
      - recurrence_name
      - start_date
      - recurrence_frequency
      - repeat_every
      - amount
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        recurrence_frequency:
          $ref: '#/components/schemas/recurrence_frequency'
        repeat_every:
          $ref: '#/components/schemas/repeat_every'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          $ref: '#/components/schemas/line_items'
        amount:
          $ref: '#/components/schemas/amount'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        product_type:
          $ref: '#/components/schemas/product_type'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        project_id:
          $ref: '#/components/schemas/project_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    update-a-recurring-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Recurring expense information has been updated
          readOnly: true
        recurring_expense:
          type: object
          properties:
            account_id:
              $ref: '#/components/schemas/account_id'
            recurrence_name:
              $ref: '#/components/schemas/recurrence_name'
            start_date:
              $ref: '#/components/schemas/start_date'
            end_date:
              $ref: '#/components/schemas/end_date'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            source_of_supply:
              $ref: '#/components/schemas/source_of_supply'
            destination_of_supply:
              $ref: '#/components/schemas/destination_of_supply'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            destination_of_supply_state:
              $ref: '#/components/schemas/destination_of_supply_state'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            reverse_charge_tax_id:
              $ref: '#/components/schemas/reverse_charge_tax_id'
            reverse_charge_tax_name:
              $ref: '#/components/schemas/reverse_charge_tax_name'
            reverse_charge_tax_percentage:
              $ref: '#/components/schemas/reverse_charge_tax_percentage'
            reverse_charge_tax_amount:
              $ref: '#/components/schemas/reverse_charge_tax_amount'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            acquisition_vat_total:
              $ref: '#/components/schemas/acquisition_vat_total'
            reverse_charge_vat_total:
              $ref: '#/components/schemas/reverse_charge_vat_total'
            acquisition_vat_summary:
              $ref: '#/components/schemas/acquisition_vat_summary'
            reverse_charge_vat_summary:
              $ref: '#/components/schemas/reverse_charge_vat_summary'
            recurrence_frequency:
              $ref: '#/components/schemas/recurrence_frequency'
            repeat_every:
              $ref: '#/components/schemas/repeat_every'
            amount:
              description: Recurring Expense amount.
              type: string
              example: 120.5
            total:
              $ref: '#/components/schemas/total'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            bcy_total:
              $ref: '#/components/schemas/bcy_total'
            product_type:
              $ref: '#/components/schemas/product_type'
            acquisition_vat_id:
              $ref: '#/components/schemas/acquisition_vat_id'
            reverse_charge_vat_id:
              $ref: '#/components/schemas/reverse_charge_vat_id'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_item:
              type: object
              properties:
                line_item_id:
                  $ref: '#/components/schemas/line_item_id'
                account_id:
                  $ref: '#/components/schemas/account_id'
                account_name:
                  $ref: '#/components/schemas/account_name'
                description:
                  $ref: '#/components/schemas/description'
                tax_amount:
                  $ref: '#/components/schemas/tax_amount'
                tax_id:
                  $ref: '#/components/schemas/tax_id'
                tax_name:
                  $ref: '#/components/schemas/tax_name'
                tax_type:
                  $ref: '#/components/schemas/tax_type'
                tax_percentage:
                  $ref: '#/components/schemas/tax_percentage'
                item_total:
                  $ref: '#/components/schemas/item_total'
                item_order:
                  $ref: '#/components/schemas/item_order'
                hsn_or_sac:
                  $ref: '#/components/schemas/hsn_or_sac'
                reverse_charge_tax_id:
                  $ref: '#/components/schemas/reverse_charge_tax_id'
                reverse_charge_tax_name:
                  $ref: '#/components/schemas/reverse_charge_tax_name'
                reverse_charge_tax_percentage:
                  $ref: '#/components/schemas/reverse_charge_tax_percentage'
                reverse_charge_tax_amount:
                  $ref: '#/components/schemas/reverse_charge_tax_amount'
                tags:
                  $ref: '#/components/schemas/tags_response'
    get-a-recurring-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        recurring_expense:
          type: object
          properties:
            recurring_expense_id:
              $ref: '#/components/schemas/recurring_expense_id'
            recurrence_name:
              $ref: '#/components/schemas/recurrence_name'
            start_date:
              $ref: '#/components/schemas/start_date'
            end_date:
              $ref: '#/components/schemas/end_date'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            source_of_supply:
              $ref: '#/components/schemas/source_of_supply'
            destination_of_supply:
              $ref: '#/components/schemas/destination_of_supply'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            destination_of_supply_state:
              $ref: '#/components/schemas/destination_of_supply_state'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            reverse_charge_tax_id:
              $ref: '#/components/schemas/reverse_charge_tax_id'
            reverse_charge_tax_name:
              $ref: '#/components/schemas/reverse_charge_tax_name'
            reverse_charge_tax_percentage:
              $ref: '#/components/schemas/reverse_charge_tax_percentage'
            reverse_charge_tax_amount:
              $ref: '#/components/schemas/reverse_charge_tax_amount'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            acquisition_vat_total:
              $ref: '#/components/schemas/acquisition_vat_total'
            reverse_charge_vat_total:
              $ref: '#/components/schemas/reverse_charge_vat_total'
            acquisition_vat_summary:
              $ref: '#/components/schemas/acquisition_vat_summary'
            reverse_charge_vat_summary:
              $ref: '#/components/schemas/reverse_charge_vat_summary'
            recurrence_frequency:
              $ref: '#/components/schemas/recurrence_frequency'
            repeat_every:
              $ref: '#/components/schemas/repeat_every'
            last_created_date:
              $ref: '#/components/schemas/last_created_date'
            next_expense_date:
              $ref: '#/components/schemas/next_expense_date'
            account_id:
              $ref: '#/components/schemas/account_id'
            account_name:
              $ref: '#/components/schemas/account_name'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            tax_amount:
              $ref: '#/components/schemas/tax_amount'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            bcy_total:
              $ref: '#/components/schemas/bcy_total'
            amount:
              $ref: '#/components/schemas/amount'
            description:
              $ref: '#/components/schemas/description'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            status:
              $ref: '#/components/schemas/status'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_item:
              type: object
              properties:
                line_item_id:
                  $ref: '#/components/schemas/line_item_id'
                account_id:
                  $ref: '#/components/schemas/account_id'
                account_name:
                  $ref: '#/components/schemas/account_name'
                description:
                  $ref: '#/components/schemas/description'
                tax_amount:
                  $ref: '#/components/schemas/tax_amount'
                tax_id:
                  $ref: '#/components/schemas/tax_id'
                tax_name:
                  $ref: '#/components/schemas/tax_name'
                tax_type:
                  $ref: '#/components/schemas/tax_type'
                tax_percentage:
                  $ref: '#/components/schemas/tax_percentage'
                item_total:
                  $ref: '#/components/schemas/item_total'
                item_order:
                  $ref: '#/components/schemas/item_order'
                hsn_or_sac:
                  $ref: '#/components/schemas/hsn_or_sac'
                reverse_charge_tax_id:
                  $ref: '#/components/schemas/reverse_charge_tax_id'
                reverse_charge_tax_name:
                  $ref: '#/components/schemas/reverse_charge_tax_name'
                reverse_charge_tax_percentage:
                  $ref: '#/components/schemas/reverse_charge_tax_percentage'
                reverse_charge_tax_amount:
                  $ref: '#/components/schemas/reverse_charge_tax_amount'
                tags:
                  $ref: '#/components/schemas/tags_response'
    delete-a-recurring-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring expense has been deleted.
          readOnly: true
    stop-a-recurring-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring expense has been stopped.
          readOnly: true
    resume-a-recurring-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring expense has been activated.
          readOnly: true
    list-child-expenses-created-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        expensehistory:
          type: array
          items:
            type: object
            properties:
              expense_id:
                $ref: '#/components/schemas/expense_id'
              date:
                $ref: '#/components/schemas/date'
              account_name:
                $ref: '#/components/schemas/account_name'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              total:
                $ref: '#/components/schemas/total'
              status:
                $ref: '#/components/schemas/status'
              vendor_name:
                $ref: '#/components/schemas/vendor_name'
              paid_through_account_name:
                $ref: '#/components/schemas/paid_through_account_name'
        page_context:
          type: array
          items:
            type: object
            properties:
              sort_column:
                $ref: '#/components/schemas/sort_column'
              filter_by:
                $ref: '#/components/schemas/filter_by'
              search_text:
                $ref: '#/components/schemas/search_text'
    list-recurring-expense-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: object
          properties:
            comment_id:
              $ref: '#/components/schemas/comment_id'
            recurring_expense_id:
              $ref: '#/components/schemas/recurring_expense_id'
            description:
              $ref: '#/components/schemas/description'
            commented_by_id:
              $ref: '#/components/schemas/commented_by_id'
            commented_by:
              $ref: '#/components/schemas/commented_by'
            date:
              $ref: '#/components/schemas/date'
            time:
              $ref: '#/components/schemas/time'
            operation_type:
              $ref: '#/components/schemas/operation_type'
            transaction_id:
              $ref: '#/components/schemas/transaction_id'
            transaction_type:
              $ref: '#/components/schemas/transaction_type'
    retainerinvoice-response:
      type: object
      properties:
        retainerinvoice_id:
          $ref: '#/components/schemas/retainerinvoice_id'
        retainerinvoice_number:
          $ref: '#/components/schemas/retainerinvoice_number'
        date:
          $ref: '#/components/schemas/date'
        status:
          $ref: '#/components/schemas/status'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        project_id:
          $ref: '#/components/schemas/project_id'
        project_name:
          $ref: '#/components/schemas/project_name'
        last_payment_date:
          $ref: '#/components/schemas/last_payment_date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        is_viewed_by_client:
          $ref: '#/components/schemas/is_viewed_by_client'
        client_viewed_time:
          $ref: '#/components/schemas/client_viewed_time'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        tags:
          $ref: '#/components/schemas/tags_response'
        line_items:
          description: Line items of a retainer invoice.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              description:
                $ref: '#/components/schemas/description'
              item_order:
                $ref: '#/components/schemas/item_order'
              rate:
                $ref: '#/components/schemas/rate'
              bcy_rate:
                $ref: '#/components/schemas/bcy_rate'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              item_total:
                $ref: '#/components/schemas/item_total'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        total:
          $ref: '#/components/schemas/total'
        taxes:
          $ref: '#/components/schemas/taxes'
        payment_made:
          $ref: '#/components/schemas/payment_made'
        payment_drawn:
          $ref: '#/components/schemas/payment_drawn'
        balance:
          $ref: '#/components/schemas/balance'
        allow_partial_payments:
          $ref: '#/components/schemas/allow_partial_payments'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        is_emailed:
          $ref: '#/components/schemas/is_emailed'
        documents:
          $ref: '#/components/schemas/documents'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        shipping_address:
          $ref: '#/components/schemas/shipping_address'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        template_id:
          $ref: '#/components/schemas/template_id'
        template_name:
          $ref: '#/components/schemas/template_name'
        page_width:
          $ref: '#/components/schemas/page_width'
        page_height:
          $ref: '#/components/schemas/page_height'
        orientation:
          $ref: '#/components/schemas/orientation'
        template_type:
          $ref: '#/components/schemas/template_type'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        created_by_id:
          $ref: '#/components/schemas/created_by_id'
        attachment_name:
          $ref: '#/components/schemas/attachment_name'
        can_send_in_mail:
          $ref: '#/components/schemas/can_send_in_mail'
        invoice_url:
          $ref: '#/components/schemas/invoice_url'
    retainerinvoice_number:
      description: 'number of the retainer invoice.Variants: <code>retainerinvoice_number_startswith</code>
        and <code>retainerinvoice_number_contains</code>. Max-length [100]'
      type: string
      example: RET-00003
    project_or_estimate_name:
      type: string
      example: new project
    payment_drawn:
      description: The amount drawn
      type: number
      format: float
      example: 26.91
    is_client_review_settings_enabled:
      type: boolean
      example: true
    unused_amount:
      type: number
      format: double
      example: 100
    retainerinvoice_total:
      type: number
      format: double
      example: 3478
    retainerinvoice_balance:
      type: number
      format: double
      example: 3478
    retainerinvoice_date:
      type: string
      example: '2016-12-20'
    payment_refunds:
      type: array
      items:
        type: string
      example: []
    html_string:
      type: string
      example: ''
    is_payment_drawn_details_required:
      type: boolean
      example: true
    create-a-retainerinvoice-request:
      required:
      - customer_id
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        date:
          $ref: '#/components/schemas/date'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of an invoice.
          type: array
          items:
            type: object
            properties:
              description:
                $ref: '#/components/schemas/description'
              item_order:
                $ref: '#/components/schemas/item_order'
              rate:
                $ref: '#/components/schemas/rate'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        template_id:
          $ref: '#/components/schemas/template_id'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-retainerinvoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The retainer invoice has been created.
          readOnly: true
        retainerinvoice:
          $ref: '#/components/schemas/retainerinvoice-response'
    list-a-retainer-invoices-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        retainerinvoices:
          type: array
          items:
            type: object
            properties:
              retainerinvoice_id:
                $ref: '#/components/schemas/retainerinvoice_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              retainerinvoice_number:
                $ref: '#/components/schemas/retainerinvoice_number'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              status:
                $ref: '#/components/schemas/status'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              project_or_estimate_name:
                $ref: '#/components/schemas/project_or_estimate_name'
              date:
                $ref: '#/components/schemas/date'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              is_viewed_by_client:
                $ref: '#/components/schemas/is_viewed_by_client'
              client_viewed_time:
                $ref: '#/components/schemas/client_viewed_time'
              total:
                $ref: '#/components/schemas/total'
              balance:
                $ref: '#/components/schemas/balance'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              is_emailed:
                $ref: '#/components/schemas/is_emailed'
              last_payment_date:
                $ref: '#/components/schemas/last_payment_date'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              tags:
                $ref: '#/components/schemas/tags'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            applied_filter:
              $ref: '#/components/schemas/applied_filter'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    update-a-retainerinvoice-request:
      required:
      - customer_id
      - line_items
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        date:
          $ref: '#/components/schemas/date'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        location_id:
          $ref: '#/components/schemas/location_id'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: Line items of a retainer invoice.
          type: array
          items:
            type: object
            properties:
              description:
                $ref: '#/components/schemas/description'
              item_order:
                $ref: '#/components/schemas/item_order'
              rate:
                $ref: '#/components/schemas/rate'
        payment_options:
          $ref: '#/components/schemas/payment_options'
        template_id:
          $ref: '#/components/schemas/template_id'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        project_id:
          $ref: '#/components/schemas/project_id'
    update-a-retainerinvoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Retainer Invoice information has been updated.
          readOnly: true
        retainerinvoice:
          type: object
          properties:
            retainerinvoice_id:
              $ref: '#/components/schemas/retainerinvoice_id'
            retainerinvoice_number:
              $ref: '#/components/schemas/retainerinvoice_number'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            last_payment_date:
              $ref: '#/components/schemas/last_payment_date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            is_viewed_by_client:
              $ref: '#/components/schemas/is_viewed_by_client'
            client_viewed_time:
              $ref: '#/components/schemas/client_viewed_time'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags'
            line_items:
              description: Line items of an invoice.
              type: array
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  description:
                    $ref: '#/components/schemas/description'
                  item_order:
                    $ref: '#/components/schemas/item_order'
                  rate:
                    $ref: '#/components/schemas/rate'
                  bcy_rate:
                    $ref: '#/components/schemas/bcy_rate'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            taxes:
              $ref: '#/components/schemas/taxes'
            payment_made:
              $ref: '#/components/schemas/payment_made'
            payment_drawn:
              $ref: '#/components/schemas/payment_drawn'
            balance:
              $ref: '#/components/schemas/balance'
            allow_partial_payments:
              $ref: '#/components/schemas/allow_partial_payments'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            payment_options:
              $ref: '#/components/schemas/payment_options'
            is_emailed:
              $ref: '#/components/schemas/is_emailed'
            documents:
              $ref: '#/components/schemas/documents'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            page_width:
              $ref: '#/components/schemas/page_width'
            page_height:
              $ref: '#/components/schemas/page_height'
            orientation:
              $ref: '#/components/schemas/orientation'
            template_type:
              $ref: '#/components/schemas/template_type'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            created_by_id:
              $ref: '#/components/schemas/created_by_id'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            can_send_in_mail:
              $ref: '#/components/schemas/can_send_in_mail'
            invoice_url:
              $ref: '#/components/schemas/invoice_url'
    get-a-retainer-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        retainerinvoice:
          type: object
          properties:
            retainerinvoice_id:
              $ref: '#/components/schemas/retainerinvoice_id'
            retainerinvoice_number:
              $ref: '#/components/schemas/retainerinvoice_number'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            is_pre_gst:
              $ref: '#/components/schemas/is_pre_gst'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            last_payment_date:
              $ref: '#/components/schemas/last_payment_date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            contact_persons_associated:
              $ref: '#/components/schemas/contact_persons_associated'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            is_viewed_by_client:
              $ref: '#/components/schemas/is_viewed_by_client'
            client_viewed_time:
              $ref: '#/components/schemas/client_viewed_time'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags'
            line_items:
              description: Line items of a retainer invoice.
              type: array
              items:
                type: object
                properties:
                  line_item_id:
                    $ref: '#/components/schemas/line_item_id'
                  description:
                    $ref: '#/components/schemas/description'
                  item_order:
                    $ref: '#/components/schemas/item_order'
                  rate:
                    $ref: '#/components/schemas/rate'
                  bcy_rate:
                    $ref: '#/components/schemas/bcy_rate'
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  item_total:
                    $ref: '#/components/schemas/item_total'
                  location_id:
                    $ref: '#/components/schemas/location_id'
                  location_name:
                    $ref: '#/components/schemas/location_name'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            taxes:
              $ref: '#/components/schemas/taxes'
            payment_made:
              $ref: '#/components/schemas/payment_made'
            payment_drawn:
              $ref: '#/components/schemas/payment_drawn'
            balance:
              $ref: '#/components/schemas/balance'
            allow_partial_payments:
              $ref: '#/components/schemas/allow_partial_payments'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            payment_options:
              $ref: '#/components/schemas/payment_options'
            is_emailed:
              $ref: '#/components/schemas/is_emailed'
            documents:
              $ref: '#/components/schemas/documents'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            shipping_address:
              $ref: '#/components/schemas/shipping_address'
            notes:
              $ref: '#/components/schemas/notes'
            terms:
              $ref: '#/components/schemas/terms'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            template_id:
              $ref: '#/components/schemas/template_id'
            template_name:
              $ref: '#/components/schemas/template_name'
            page_width:
              $ref: '#/components/schemas/page_width'
            page_height:
              $ref: '#/components/schemas/page_height'
            orientation:
              $ref: '#/components/schemas/orientation'
            template_type:
              $ref: '#/components/schemas/template_type'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            created_by_id:
              $ref: '#/components/schemas/created_by_id'
            attachment_name:
              $ref: '#/components/schemas/attachment_name'
            can_send_in_mail:
              $ref: '#/components/schemas/can_send_in_mail'
            invoice_url:
              $ref: '#/components/schemas/invoice_url'
    delete-a-retainer-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The retainer invoice has been deleted.
          readOnly: true
    mark-a-retainer-invoice-as-sent-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Retainer Invoice status has been changed to Sent.
          readOnly: true
    update-retainer-invoice-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Retainer Invoice information has been updated.
          readOnly: true
    void-a-retainer-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Retainer Invoice status has been changed to 'Void'.
          readOnly: true
    submit-a-retainer-invoice-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The Retainer Invoice has been successfully submitted for approval.
          readOnly: true
    approve-a-retainer-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the Retainer Invoice.
          readOnly: true
    email-a-retainer-invoice-request:
      required:
      - to_mail_ids
      type: object
      properties:
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          $ref: '#/components/schemas/body'
    email-a-retainer-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your retainer invoice has been sent.
          readOnly: true
    get-retainer-invoice-email-content-request:
      type: object
      properties:
        email_template_id:
          $ref: '#/components/schemas/email_template_id'
    get-retainer-invoice-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        gateways_configured:
          $ref: '#/components/schemas/gateways_configured'
        deprecated_placeholders_used:
          $ref: '#/components/schemas/deprecated_placeholders_used'
        body:
          $ref: '#/components/schemas/body'
        error_list:
          $ref: '#/components/schemas/error_list'
        subject:
          $ref: '#/components/schemas/subject'
        to_contacts:
          type: array
          items:
            type: object
            properties:
              first_name:
                $ref: '#/components/schemas/first_name'
              selected:
                $ref: '#/components/schemas/selected'
              phone:
                $ref: '#/components/schemas/phone'
              email:
                $ref: '#/components/schemas/email'
              last_name:
                $ref: '#/components/schemas/last_name'
              salutation:
                $ref: '#/components/schemas/salutation'
              contact_person_id:
                $ref: '#/components/schemas/contact_person_id'
              mobile:
                $ref: '#/components/schemas/mobile'
        attachment_name:
          $ref: '#/components/schemas/attachment_name'
        email_template_id:
          $ref: '#/components/schemas/email_template_id'
        file_name:
          $ref: '#/components/schemas/file_name'
        from_emails:
          type: array
          items:
            type: object
            properties:
              user_name:
                $ref: '#/components/schemas/user_name'
              selected:
                $ref: '#/components/schemas/selected'
              email:
                $ref: '#/components/schemas/email'
        customer_id:
          $ref: '#/components/schemas/customer_id'
    list-retainer-invoice-templates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          type: array
          items:
            type: object
            properties:
              template_name:
                $ref: '#/components/schemas/template_name'
              template_id:
                $ref: '#/components/schemas/template_id'
              template_type:
                $ref: '#/components/schemas/template_type'
    list-retainer-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        payments:
          type: array
          items:
            type: object
            properties:
              payment_id:
                $ref: '#/components/schemas/payment_id'
              documents:
                $ref: '#/components/schemas/documents'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              retainerinvoice_id:
                $ref: '#/components/schemas/retainerinvoice_id'
              payment_mode:
                $ref: '#/components/schemas/payment_mode'
              date:
                $ref: '#/components/schemas/date'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              amount:
                $ref: '#/components/schemas/amount'
              unused_amount:
                $ref: '#/components/schemas/unused_amount'
              bank_charges:
                $ref: '#/components/schemas/bank_charges'
              is_client_review_settings_enabled:
                $ref: '#/components/schemas/is_client_review_settings_enabled'
              tax_amount_withheld:
                $ref: '#/components/schemas/tax_amount_withheld'
              discount_amount:
                $ref: '#/components/schemas/discount_amount'
              description:
                $ref: '#/components/schemas/description'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              online_transaction_id:
                $ref: '#/components/schemas/online_transaction_id'
              invoices:
                $ref: '#/components/schemas/invoices'
              retainerinvoice:
                type: object
                properties:
                  retainerinvoice_id:
                    $ref: '#/components/schemas/retainerinvoice_id'
                  retainerinvoice_number:
                    $ref: '#/components/schemas/retainerinvoice_number'
                  retainerinvoice_total:
                    $ref: '#/components/schemas/retainerinvoice_total'
                  retainerinvoice_balance:
                    $ref: '#/components/schemas/retainerinvoice_balance'
                  retainerinvoice_date:
                    $ref: '#/components/schemas/retainerinvoice_date'
              payment_refunds:
                $ref: '#/components/schemas/payment_refunds'
              last_four_digits:
                $ref: '#/components/schemas/last_four_digits'
              html_string:
                $ref: '#/components/schemas/html_string'
              template_id:
                $ref: '#/components/schemas/template_id'
              template_name:
                $ref: '#/components/schemas/template_name'
              page_width:
                $ref: '#/components/schemas/page_width'
              page_height:
                $ref: '#/components/schemas/page_height'
              orientation:
                $ref: '#/components/schemas/orientation'
              template_type:
                $ref: '#/components/schemas/template_type'
              attachment_name:
                $ref: '#/components/schemas/attachment_name'
              can_send_in_mail:
                $ref: '#/components/schemas/can_send_in_mail'
              is_payment_drawn_details_required:
                $ref: '#/components/schemas/is_payment_drawn_details_required'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
    delete-a-retainer-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The payment has been deleted.
          readOnly: true
    get-a-retainer-invoice-attachment-request:
      type: object
      properties:
        preview:
          $ref: '#/components/schemas/preview'
    get-a-retainer-invoice-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    add-attachment-to-a-retainer-invoice-request:
      type: object
      properties:
        can_send_in_mail:
          $ref: '#/components/schemas/can_send_in_mail'
        attachment:
          $ref: '#/components/schemas/attachment'
    add-attachment-to-a-retainer-invoice-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your file has been attached.
          readOnly: true
    list-retainer-invoice-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              retainerinvoice_id:
                $ref: '#/components/schemas/retainerinvoice_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    purchaseorder_number:
      description: Number of the Purchase Order
      type: string
      example: PO-0001
    discount_account_id:
      description: ID of the account associated with the discount account.
      type: string
      example: '460000000000403'
    ship_via:
      description: Shipment Preference
      type: string
    acquisition_vat_name:
      description: Name of the VAT Acquistion
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    acquisition_vat_percentage:
      description: Percentage of the VAT Acquistion
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    acquisition_vat_amount:
      description: Amount of the VAT Acquistion
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    reverse_charge_vat_name:
      description: Name of the Reverse Charge
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    reverse_charge_vat_percentage:
      description: Percentage of the Reverse Charge
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    reverse_charge_vat_amount:
      description: Percentage of the Reverse Charge
      type: string
      x-node_available_in:
      - uk
      - eu
      x-node_unavailable_in: []
    delivery_date:
      description: The expected delivery date when the goods or services ordered in
        this purchase order should be received. This date is used for planning, scheduling,
        and tracking deliveries. It helps in managing inventory, coordinating with
        vendors, and ensuring timely receipt of ordered items. The date should be
        in YYYY-MM-DD format and is typically communicated to the vendor as part of
        the purchase order terms.
      type: string
      example: '2014-02-10'
    delivery_org_address_id:
      description: 'Unique identifier of the organization address where the goods
        or services from this purchase order should be delivered. This field specifies
        the delivery location within your organization, such as a warehouse, office,
        or branch location. The address details are retrieved from your organization''s
        address book and displayed on the purchase order for vendor reference. Note:
        This value is a set of numeric characters.'
      type: string
    delivery_customer_id:
      description: 'Unique identifier of the customer for whom the goods or services
        in this purchase order are being procured. This field is used when the purchase
        order is created on behalf of a customer, such as in drop-shipping scenarios
        or when procuring items for specific customer projects. The customer information
        may be displayed on the purchase order for vendor reference. Note: This value
        is a set of numeric characters.'
      type: string
    document_id:
      description: ID of the document attached to the task.
      type: string
      example: 982000000567125
    organization_address_id:
      description: 'ID of organization address. Note: This value is a set of numeric
        characters.'
      type: string
    is_verifiable:
      type: boolean
      example: true
    address1:
      type: string
    address2:
      type: string
    is_valid:
      type: string
    is_primary:
      description: Whether it is primary location or not
      type: boolean
      example: true
    purchaseorder_id:
      description: ID of the Purchase Order
      type: string
      example: '460000000068231'
    expected_delivery_date:
      description: The expected delivery date when the goods or services ordered in
        this purchase order should be received. This date is used for planning, scheduling,
        and tracking deliveries. It helps in managing inventory, coordinating with
        vendors, and ensuring timely receipt of ordered items. The date should be
        in YYYY-MM-DD format.
      type: string
    ship_via_id:
      description: 'ID of the Shipment Preference. Note: This value is a set of numeric
        characters.'
      type: string
    can_mark_as_bill:
      type: boolean
      example: false
    can_mark_as_unbill:
      type: boolean
      example: false
    update-an-purchaseorder-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    delivery_address:
      type: object
      properties:
        zip:
          $ref: '#/components/schemas/zip'
        is_verifiable:
          $ref: '#/components/schemas/is_verifiable'
        state:
          $ref: '#/components/schemas/state'
        address1:
          $ref: '#/components/schemas/address1'
        address2:
          $ref: '#/components/schemas/address2'
        is_valid:
          $ref: '#/components/schemas/is_valid'
        city:
          $ref: '#/components/schemas/city'
        country:
          $ref: '#/components/schemas/country'
        address:
          $ref: '#/components/schemas/address'
        email:
          $ref: '#/components/schemas/email'
        is_primary:
          $ref: '#/components/schemas/is_primary'
        organization_address_id:
          $ref: '#/components/schemas/organization_address_id'
        phone:
          $ref: '#/components/schemas/phone'
        is_verified:
          $ref: '#/components/schemas/is_verified'
    purchaseorder:
      type: object
      properties:
        purchaseorder_id:
          $ref: '#/components/schemas/purchaseorder_id'
        documents:
          $ref: '#/components/schemas/documents'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        pricebook_name:
          $ref: '#/components/schemas/pricebook_name'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        purchaseorder_number:
          $ref: '#/components/schemas/purchaseorder_number'
        date:
          $ref: '#/components/schemas/date'
        expected_delivery_date:
          $ref: '#/components/schemas/expected_delivery_date'
        discount:
          $ref: '#/components/schemas/discount'
        discount_account_id:
          $ref: '#/components/schemas/discount_account_id'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        status:
          $ref: '#/components/schemas/status'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        crm_owner_id:
          $ref: '#/components/schemas/crm_owner_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        delivery_date:
          $ref: '#/components/schemas/delivery_date'
        is_emailed:
          $ref: '#/components/schemas/is_emailed'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        line_items:
          $ref: '#/components/schemas/line_items'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        tax_total:
          $ref: '#/components/schemas/tax_total'
        total:
          $ref: '#/components/schemas/total'
        taxes:
          $ref: '#/components/schemas/taxes'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        ship_via:
          $ref: '#/components/schemas/ship_via'
        ship_via_id:
          $ref: '#/components/schemas/ship_via_id'
        attention:
          $ref: '#/components/schemas/attention'
        delivery_org_address_id:
          $ref: '#/components/schemas/delivery_org_address_id'
        delivery_customer_id:
          $ref: '#/components/schemas/delivery_customer_id'
        delivery_address:
          $ref: '#/components/schemas/delivery_address'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        attachment_name:
          $ref: '#/components/schemas/attachment_name'
        can_send_in_mail:
          $ref: '#/components/schemas/can_send_in_mail'
        template_id:
          $ref: '#/components/schemas/template_id'
        template_name:
          $ref: '#/components/schemas/template_name'
        page_width:
          $ref: '#/components/schemas/page_width'
        page_height:
          $ref: '#/components/schemas/page_height'
        orientation:
          $ref: '#/components/schemas/orientation'
        template_type:
          $ref: '#/components/schemas/template_type'
        created_time:
          $ref: '#/components/schemas/created_time'
        created_by_id:
          $ref: '#/components/schemas/created_by_id'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        can_mark_as_bill:
          $ref: '#/components/schemas/can_mark_as_bill'
        can_mark_as_unbill:
          $ref: '#/components/schemas/can_mark_as_unbill'
        tags:
          $ref: '#/components/schemas/tags_response'
    create-a-purchase-order-request:
      required:
      - vendor_id
      - line_items
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        purchaseorder_number:
          $ref: '#/components/schemas/purchaseorder_number'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        billing_address_id:
          $ref: '#/components/schemas/billing_address_id'
        crm_owner_id:
          $ref: '#/components/schemas/crm_owner_id'
        crm_custom_reference_id:
          $ref: '#/components/schemas/crm_custom_reference_id'
        template_id:
          $ref: '#/components/schemas/template_id'
        date:
          $ref: '#/components/schemas/date'
        delivery_date:
          $ref: '#/components/schemas/delivery_date'
        due_date:
          $ref: '#/components/schemas/due_date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        discount:
          $ref: '#/components/schemas/discount'
        discount_account_id:
          $ref: '#/components/schemas/discount_account_id'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        notes:
          $ref: '#/components/schemas/notes'
        notes_default:
          $ref: '#/components/schemas/notes_default'
        terms:
          $ref: '#/components/schemas/terms'
        terms_default:
          $ref: '#/components/schemas/terms_default'
        ship_via:
          $ref: '#/components/schemas/ship_via'
        delivery_org_address_id:
          $ref: '#/components/schemas/delivery_org_address_id'
        delivery_customer_id:
          $ref: '#/components/schemas/delivery_customer_id'
        attention:
          $ref: '#/components/schemas/attention'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        salesorder_id:
          $ref: '#/components/schemas/salesorder_id'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of purchase order.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              account_id:
                $ref: '#/components/schemas/account_id'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              unit:
                $ref: '#/components/schemas/unit'
              location_id:
                $ref: '#/components/schemas/location_id'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              item_order:
                $ref: '#/components/schemas/item_order'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              item_custom_fields:
                type: array
                description: Custom fields for the line item.
                items:
                  type: object
                  properties:
                    index:
                      $ref: '#/components/schemas/index'
                    value:
                      $ref: '#/components/schemas/value'
              tags:
                $ref: '#/components/schemas/tags'
              project_id:
                $ref: '#/components/schemas/project_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        documents:
          $ref: '#/components/schemas/documents'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Purchase Order has been added.
          readOnly: true
        purchaseorder:
          $ref: '#/components/schemas/purchaseorder'
    list-purchase-orders-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        purchaseorders:
          type: array
          items:
            type: object
            properties:
              purchaseorder_id:
                $ref: '#/components/schemas/purchaseorder_id'
              vendor_id:
                $ref: '#/components/schemas/vendor_id'
              vendor_name:
                $ref: '#/components/schemas/vendor_name'
              status:
                $ref: '#/components/schemas/status'
              purchaseorder_number:
                $ref: '#/components/schemas/purchaseorder_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              date:
                $ref: '#/components/schemas/date'
              delivery_date:
                $ref: '#/components/schemas/delivery_date'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              price_precision:
                $ref: '#/components/schemas/price_precision'
              total:
                $ref: '#/components/schemas/total'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
    update-a-purchase-order-request:
      required:
      - vendor_id
      - line_items
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        contact_persons_associated:
          $ref: '#/components/schemas/contact_persons_associated_payload'
        purchaseorder_number:
          $ref: '#/components/schemas/purchaseorder_number'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        discount:
          $ref: '#/components/schemas/discount'
        discount_account_id:
          $ref: '#/components/schemas/discount_account_id'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        billing_address_id:
          $ref: '#/components/schemas/billing_address_id'
        crm_owner_id:
          $ref: '#/components/schemas/crm_owner_id'
        crm_custom_reference_id:
          $ref: '#/components/schemas/crm_custom_reference_id'
        template_id:
          $ref: '#/components/schemas/template_id'
        date:
          $ref: '#/components/schemas/date'
        delivery_date:
          $ref: '#/components/schemas/delivery_date'
        due_date:
          $ref: '#/components/schemas/due_date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        notes:
          $ref: '#/components/schemas/notes'
        notes_default:
          $ref: '#/components/schemas/notes_default'
        terms:
          $ref: '#/components/schemas/terms'
        terms_default:
          $ref: '#/components/schemas/terms_default'
        ship_via:
          $ref: '#/components/schemas/ship_via'
        delivery_org_address_id:
          $ref: '#/components/schemas/delivery_org_address_id'
        delivery_customer_id:
          $ref: '#/components/schemas/delivery_customer_id'
        attention:
          $ref: '#/components/schemas/attention'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        salesorder_id:
          $ref: '#/components/schemas/salesorder_id'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of purchase order.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              account_id:
                $ref: '#/components/schemas/account_id'
              name:
                $ref: '#/components/schemas/name'
              description:
                $ref: '#/components/schemas/description'
              unit:
                $ref: '#/components/schemas/unit'
              location_id:
                $ref: '#/components/schemas/location_id'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              rate:
                $ref: '#/components/schemas/rate'
              quantity:
                $ref: '#/components/schemas/quantity'
              item_order:
                $ref: '#/components/schemas/item_order'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              item_custom_fields:
                type: array
                description: Custom fields for the line item.
                items:
                  type: object
                  properties:
                    index:
                      $ref: '#/components/schemas/index'
                    value:
                      $ref: '#/components/schemas/value'
              tags:
                $ref: '#/components/schemas/tags'
              project_id:
                $ref: '#/components/schemas/project_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        documents:
          $ref: '#/components/schemas/documents'
        tags:
          $ref: '#/components/schemas/tags'
    update-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Purchase Order has been updated.
          readOnly: true
        purchaseorder:
          $ref: '#/components/schemas/purchaseorder'
    get-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        purchaseorder:
          $ref: '#/components/schemas/purchaseorder'
    delete-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The purchase order has been deleted.
          readOnly: true
    mark-a-purchase-order-as-open-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The purchase order has been marked as open.
          readOnly: true
    mark-as-billed-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The purchase order has been marked as billed.
          readOnly: true
    cancel-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The purchase order has been cancelled.
          readOnly: true
    submit-a-purchase-order-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The purchase order has been successfully submitted for approval.
          readOnly: true
    approve-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the Purchase Order.
          readOnly: true
    email-a-purchase-order-request:
      required:
      - to_mail_ids
      - body
      type: object
      properties:
        send_from_org_email_id:
          $ref: '#/components/schemas/send_from_org_email_id'
        from_address_id:
          $ref: '#/components/schemas/from_address_id'
        to_mail_ids:
          $ref: '#/components/schemas/to_mail_ids'
        cc_mail_ids:
          $ref: '#/components/schemas/cc_mail_ids'
        bcc_mail_ids:
          $ref: '#/components/schemas/bcc_mail_ids'
        subject:
          $ref: '#/components/schemas/subject'
        body:
          $ref: '#/components/schemas/body'
        mail_documents:
          $ref: '#/components/schemas/mail_documents'
    email-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your purchase order has been sent.
          readOnly: true
    get-purchase-order-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    list-purchase-order-templates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        templates:
          $ref: '#/components/schemas/templates'
    update-purchase-order-template-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Purchase Order has been updated.
          readOnly: true
    get-a-purchase-order-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    add-attachment-to-a-purchase-order-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The document has been attached.
          readOnly: true
    list-purchase-order-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    reject_purchase_order:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have rejected the Purchase order successfully.
          readOnly: true
    bill_number:
      description: Number associated with the Bill
      type: string
      example: B-1000
    permit_number:
      description: The permit number for the bill.
      type: string
      x-node_available_in:
      - uae
      x-node_unavailable_in: []
    image_document_id:
      description: ID of the image document
      type: string
      example: 460000000038067
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    is_tds_applied:
      description: Check if TDS is applied
      type: boolean
      x-node_available_in:
      - in
      - global
      - au
      x-node_unavailable_in: []
    filed_in_vat_return_id:
      description: ID of the VAT Return the Vendor Credit is filed in
      type: string
      x-node_available_in:
      - uk
      x-node_unavailable_in: []
    filed_in_vat_return_name:
      description: Name of the VAT Return the Vendor Credit is filed in
      type: string
      x-node_available_in:
      - uk
      x-node_unavailable_in: []
    filed_in_vat_return_type:
      description: Type of the VAT Return the Vendor Credit is filed in
      type: string
      x-node_available_in:
      - uk
      x-node_unavailable_in: []
    is_abn_quoted:
      description: Whether the ABN is quoted for the recurring bill.
      type: string
      x-node_available_in:
      - au
      x-node_unavailable_in: []
    abn:
      description: Australian Business Number (ABN) of the vendor.
      type: string
      x-node_available_in:
      - au
      x-node_unavailable_in: []
    custom_field_id:
      description: ID of the custom field.
      type: string
    update-an-bill-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    purchaseorder_ids:
      description: Array of purchase order identifiers linked to this bill for tracking
        purposes.
      type: array
      items:
        type: string
      example:
      - 460000000068231
      - 460000000068233
    unused_credits_payable_amount:
      description: Unused Credits for this Vendor
      type: integer
      example: 187
    recurring_bill_id:
      description: Unique ID of the recurring bill.
      type: string
      example: 982000000567240
    due_by_days:
      description: Days by which Bill is Due
      type: string
    due_in_days:
      description: Days in which Bill will be Due
      type: integer
      example: 0
    is_item_level_tax_calc:
      description: Flag indicating whether taxes are calculated at individual line
        item level rather than bill total. Enables different tax rates for different
        items. Provides more accurate tax calculations for mixed tax scenarios. When
        enabled, each line item can have different tax rates based on item type, location,
        or tax rules. Improves tax compliance and reporting accuracy.
      type: boolean
      example: false
    vendor_credits_applied:
      description: Amount of credits applied
      type: integer
      example: 0
    is_line_item_invoiced:
      description: Check if line item is invoiced
      type: boolean
      example: false
    purchaseorders:
      type: array
      items:
        type: object
        properties:
          purchaseorder_id:
            $ref: '#/components/schemas/purchaseorder_id'
          purchaseorder_number:
            $ref: '#/components/schemas/purchaseorder_number'
          purchaseorder_date:
            $ref: '#/components/schemas/purchaseorder_date'
          purchaseorder_status:
            $ref: '#/components/schemas/purchaseorder_status'
    vendor_credit_id:
      description: ID of the vendor credit that has to be refunded.
      type: string
      example: '460000000030049'
    vendor_credit_bill_id:
      description: ID of the Vendor Credited Bill ID
      type: string
    vendor_credit_number:
      description: Unique vendor credit number displayed in the interface. Auto-generated
        with DN prefix unless auto number generation is disabled. Max-Length [100]
      type: string
      example: DN-00002
    vendor_credits:
      type: array
      items:
        type: object
        properties:
          vendor_credit_id:
            $ref: '#/components/schemas/vendor_credit_id'
          vendor_credit_bill_id:
            $ref: '#/components/schemas/vendor_credit_bill_id'
          vendor_credit_number:
            $ref: '#/components/schemas/vendor_credit_number'
          date:
            description: Date Vendor Credit was applied
            type: string
            example: 27 Dec 2016
          amount:
            description: Amount of Credit applied to the Bill
            type: number
            format: double
            example: 10
    reference_id:
      type: string
    open_purchaseorders_count:
      description: Number of Open Purchase Orders
      type: integer
      example: 1
    purchaseorder_item_id:
      description: Unique identifier linking this line item to a specific item in
        the purchase order. Used for tracking PO-to-bill conversions and maintaining
        audit trails. Required when converting purchase orders to bills.
      type: string
    bill_payment_id:
      description: ID of the Bill Payment
      type: string
    is_single_bill_payment:
      description: Check if it is a Single Bill Payment
      type: boolean
      example: true
    paid_through:
      description: Account through which Bill is paid
      type: string
      example: Petty Cash
    purchaseorder_date:
      description: Date of the Purchase Order
      type: string
      example: 19 Jan 2017
    purchaseorder_status:
      description: Status of the Purchase Order
      type: string
      example: billed
    is_paid_via_print_check:
      description: Is the entity paid via print check
      type: boolean
      example: false
      x-node_available_in:
      - us
      - ca
      x-node_unavailable_in: []
    check_number:
      description: Number of the Check Created
      type: string
    check_status:
      description: Status of the Check
      type: string
    check_details:
      description: List of check details associated with the payment.
      type: array
      items:
        type: object
        properties:
          memo:
            $ref: '#/components/schemas/memo'
          check_number:
            $ref: '#/components/schemas/check_number'
    is_ach_payment:
      description: Check if transaction is ACH Payment
      type: boolean
      example: false
    ach_payment_status:
      description: Status of the ACH Payment
      type: string
    ach_gw_transaction_id:
      description: ID of the ACH Payment Transaction
      type: string
    bill:
      type: object
      properties:
        bill_id:
          $ref: '#/components/schemas/bill_id'
        purchaseorder_ids:
          $ref: '#/components/schemas/purchaseorder_ids'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        permit_number:
          $ref: '#/components/schemas/permit_number'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_pre_gst:
          $ref: '#/components/schemas/is_pre_gst'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        pricebook_name:
          $ref: '#/components/schemas/pricebook_name'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        unused_credits_payable_amount:
          $ref: '#/components/schemas/unused_credits_payable_amount'
        status:
          $ref: '#/components/schemas/status'
        bill_number:
          $ref: '#/components/schemas/bill_number'
        date:
          $ref: '#/components/schemas/date'
        due_date:
          $ref: '#/components/schemas/due_date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        payment_expected_date:
          $ref: '#/components/schemas/payment_expected_date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        recurring_bill_id:
          $ref: '#/components/schemas/recurring_bill_id'
        due_by_days:
          $ref: '#/components/schemas/due_by_days'
        due_in_days:
          $ref: '#/components/schemas/due_in_days'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        documents:
          $ref: '#/components/schemas/documents'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags_response'
        is_tds_applied:
          $ref: '#/components/schemas/is_tds_applied'
        is_item_level_tax_calc:
          $ref: '#/components/schemas/is_item_level_tax_calc'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        filed_in_vat_return_id:
          $ref: '#/components/schemas/filed_in_vat_return_id'
        filed_in_vat_return_name:
          $ref: '#/components/schemas/filed_in_vat_return_name'
        filed_in_vat_return_type:
          $ref: '#/components/schemas/filed_in_vat_return_type'
        is_abn_quoted:
          $ref: '#/components/schemas/is_abn_quoted'
        line_items:
          $ref: '#/components/schemas/line_items'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        tax_total:
          $ref: '#/components/schemas/tax_total'
        total:
          $ref: '#/components/schemas/total'
        payment_made:
          $ref: '#/components/schemas/payment_made'
        vendor_credits_applied:
          $ref: '#/components/schemas/vendor_credits_applied'
        is_line_item_invoiced:
          $ref: '#/components/schemas/is_line_item_invoiced'
        purchaseorders:
          $ref: '#/components/schemas/purchaseorders'
        taxes:
          $ref: '#/components/schemas/taxes'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        balance:
          $ref: '#/components/schemas/balance'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        payments:
          $ref: '#/components/schemas/payments'
        vendor_credits:
          $ref: '#/components/schemas/vendor_credits'
        created_time:
          $ref: '#/components/schemas/created_time'
        created_by_id:
          $ref: '#/components/schemas/created_by_id'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        reference_id:
          $ref: '#/components/schemas/reference_id'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        attachment_name:
          $ref: '#/components/schemas/attachment_name'
        open_purchaseorders_count:
          $ref: '#/components/schemas/open_purchaseorders_count'
    bill_payments:
      description: List of payments applied to the bill.
      type: array
      items:
        type: object
        properties:
          payment_id:
            $ref: '#/components/schemas/payment_id'
          amount_applied:
            $ref: '#/components/schemas/amount_applied'
    apply_vendor_credits:
      description: List of vendor credits applied to the bill.
      type: array
      items:
        type: object
        properties:
          vendor_credit_id:
            $ref: '#/components/schemas/vendor_credit_id'
          amount_applied:
            $ref: '#/components/schemas/amount_applied'
    approver_id:
      description: ID of the Approver
      type: string
    order:
      description: Order of the Approver
      type: integer
    approvers:
      description: List of approvers for the bill.
      type: array
      items:
        type: object
        properties:
          approver_id:
            $ref: '#/components/schemas/approver_id'
          order:
            $ref: '#/components/schemas/order'
    create-a-bill-request:
      required:
      - vendor_id
      - bill_number
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        purchaseorder_ids:
          $ref: '#/components/schemas/purchaseorder_ids'
        bill_number:
          $ref: '#/components/schemas/bill_number'
        documents:
          $ref: '#/components/schemas/documents'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        permit_number:
          $ref: '#/components/schemas/permit_number'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        date:
          $ref: '#/components/schemas/date'
        due_date:
          $ref: '#/components/schemas/due_date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        recurring_bill_id:
          $ref: '#/components/schemas/recurring_bill_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        is_item_level_tax_calc:
          $ref: '#/components/schemas/is_item_level_tax_calc'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        location_id:
          $ref: '#/components/schemas/location_id'
        custom_fields:
          type: array
          description: List of custom fields for the bill.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: Line items of a bill.
          type: array
          items:
            type: object
            properties:
              purchaseorder_item_id:
                $ref: '#/components/schemas/purchaseorder_item_id'
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              account_id:
                $ref: '#/components/schemas/account_id'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              location_id:
                $ref: '#/components/schemas/location_id'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              item_order:
                $ref: '#/components/schemas/item_order'
              product_type:
                $ref: '#/components/schemas/product_type'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              unit:
                $ref: '#/components/schemas/unit'
              tags:
                $ref: '#/components/schemas/tags'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              project_id:
                $ref: '#/components/schemas/project_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              item_custom_fields:
                $ref: '#/components/schemas/item_custom_fields'
              serial_numbers:
                $ref: '#/components/schemas/serial_numbers'
        taxes:
          type: array
          description: List of taxes applied to the bill.
          items:
            type: object
            properties:
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        approvers:
          $ref: '#/components/schemas/approvers'
    create-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        bill:
          $ref: '#/components/schemas/bill'
    list-bills-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        bills:
          type: array
          items:
            type: object
            properties:
              bill_id:
                $ref: '#/components/schemas/bill_id'
              vendor_id:
                $ref: '#/components/schemas/vendor_id'
              vendor_name:
                $ref: '#/components/schemas/vendor_name'
              status:
                $ref: '#/components/schemas/status'
              bill_number:
                $ref: '#/components/schemas/bill_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              date:
                $ref: '#/components/schemas/date'
              due_date:
                $ref: '#/components/schemas/due_date'
              due_days:
                $ref: '#/components/schemas/due_days'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              price_precision:
                $ref: '#/components/schemas/price_precision'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              total:
                $ref: '#/components/schemas/total'
              balance:
                $ref: '#/components/schemas/balance'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              attachment_name:
                $ref: '#/components/schemas/attachment_name'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              is_tds_applied:
                $ref: '#/components/schemas/is_tds_applied'
              is_abn_quoted:
                $ref: '#/components/schemas/is_abn_quoted'
        page_context:
          $ref: '#/components/schemas/page_context'
    update-a-bill-request:
      required:
      - vendor_id
      - bill_number
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        purchaseorder_ids:
          $ref: '#/components/schemas/purchaseorder_ids'
        bill_number:
          $ref: '#/components/schemas/bill_number'
        documents:
          $ref: '#/components/schemas/documents'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        permit_number:
          $ref: '#/components/schemas/permit_number'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        date:
          $ref: '#/components/schemas/date'
        due_date:
          $ref: '#/components/schemas/due_date'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        recurring_bill_id:
          $ref: '#/components/schemas/recurring_bill_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        is_item_level_tax_calc:
          $ref: '#/components/schemas/is_item_level_tax_calc'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        adjustment:
          $ref: '#/components/schemas/adjustment'
        adjustment_description:
          $ref: '#/components/schemas/adjustment_description'
        custom_fields:
          type: array
          description: List of custom fields for the bill.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        tags:
          $ref: '#/components/schemas/tags'
        line_items:
          description: Line items of a bill.
          type: array
          items:
            type: object
            properties:
              purchaseorder_item_id:
                $ref: '#/components/schemas/purchaseorder_item_id'
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              account_id:
                $ref: '#/components/schemas/account_id'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              location_id:
                $ref: '#/components/schemas/location_id'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              item_order:
                $ref: '#/components/schemas/item_order'
              product_type:
                $ref: '#/components/schemas/product_type'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              unit:
                $ref: '#/components/schemas/unit'
              tags:
                $ref: '#/components/schemas/tags'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              project_id:
                $ref: '#/components/schemas/project_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              item_custom_fields:
                $ref: '#/components/schemas/item_custom_fields'
              serial_numbers:
                $ref: '#/components/schemas/serial_numbers'
        location_id:
          $ref: '#/components/schemas/location_id'
        taxes:
          type: array
          description: List of taxes applied to the bill.
          items:
            type: object
            properties:
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        approvers:
          $ref: '#/components/schemas/approvers'
    update-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Bill information has been updated.
          readOnly: true
        bill:
          $ref: '#/components/schemas/bill'
    get-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        bill:
          $ref: '#/components/schemas/bill'
    delete-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bill has been deleted.
          readOnly: true
    void-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bill has been marked as void.
          readOnly: true
    mark-a-bill-as-open-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The status of the bill has been chaged to open.
          readOnly: true
    submit-a-bill-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bill has been submitted for approval successfully.
          readOnly: true
    approve-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    list-bill-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        payments:
          type: array
          items:
            type: object
            properties:
              payment_id:
                $ref: '#/components/schemas/payment_id'
              bill_id:
                $ref: '#/components/schemas/bill_id'
              bill_payment_id:
                $ref: '#/components/schemas/bill_payment_id'
              vendor_id:
                $ref: '#/components/schemas/vendor_id'
              vendor_name:
                $ref: '#/components/schemas/vendor_name'
              payment_mode:
                $ref: '#/components/schemas/payment_mode'
              description:
                $ref: '#/components/schemas/description'
              date:
                $ref: '#/components/schemas/date'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              amount:
                $ref: '#/components/schemas/amount'
              paid_through:
                $ref: '#/components/schemas/paid_through'
              is_single_bill_payment:
                $ref: '#/components/schemas/is_single_bill_payment'
    get-a-bill-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    add-attachment-to-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The document has been attached.
          readOnly: true
    convert-po-to-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        purchaseorder_ids:
          $ref: '#/components/schemas/purchaseorder_ids'
        bill:
          $ref: '#/components/schemas/bill'
    list-bill-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              bill_id:
                $ref: '#/components/schemas/bill_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              date:
                description: Date of the Comment
                type: string
                example: 17 Nov 2016
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    recurring_bill:
      type: object
      properties:
        recurring_bill_id:
          $ref: '#/components/schemas/recurring_bill_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        status:
          $ref: '#/components/schemas/status'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        is_abn_quoted:
          $ref: '#/components/schemas/is_abn_quoted'
        abn:
          $ref: '#/components/schemas/abn'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        pricebook_name:
          $ref: '#/components/schemas/pricebook_name'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        line_items:
          $ref: '#/components/schemas/line_items'
        is_tds_applied:
          $ref: '#/components/schemas/is_tds_applied'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        created_time:
          $ref: '#/components/schemas/created_time'
        created_by_id:
          $ref: '#/components/schemas/created_by_id'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        discount:
          $ref: '#/components/schemas/discount'
        discount_account_id:
          $ref: '#/components/schemas/discount_account_id'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        tags:
          $ref: '#/components/schemas/tags_response'
    create-a-recurring-bill-request:
      required:
      - vendor_id
      - recurrence_name
      - start_date
      - repeat_every
      - recurrence_frequency
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        is_abn_quoted:
          $ref: '#/components/schemas/is_abn_quoted'
        abn:
          $ref: '#/components/schemas/abn'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of a recurrence bill.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              account_id:
                $ref: '#/components/schemas/account_id'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              location_id:
                $ref: '#/components/schemas/location_id'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              item_order:
                $ref: '#/components/schemas/item_order'
              product_type:
                $ref: '#/components/schemas/product_type'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              unit:
                $ref: '#/components/schemas/unit'
              tags:
                $ref: '#/components/schemas/tags'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              project_id:
                $ref: '#/components/schemas/project_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              item_custom_fields:
                $ref: '#/components/schemas/item_custom_fields'
              serial_numbers:
                $ref: '#/components/schemas/serial_numbers'
        is_tds_applied:
          $ref: '#/components/schemas/is_tds_applied'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        custom_fields:
          type: array
          description: List of custom fields for the recurring bill.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        discount:
          $ref: '#/components/schemas/discount'
        discount_account_id:
          $ref: '#/components/schemas/discount_account_id'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        repeat_every:
          description: Description for repeat_every
          type: string
          example: ''
        recurrence_frequency:
          description: Description for recurrence_frequency
          type: string
          example: ''
        tags:
          $ref: '#/components/schemas/tags'
    create-a-recurring-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        recurring_bill:
          $ref: '#/components/schemas/recurring_bill'
    list-recurring-bills-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        recurringbills:
          type: array
          items:
            $ref: '#/components/schemas/recurring_bill'
    update-a-recurring-bill-request:
      required:
      - vendor_id
      - recurrence_name
      - start_date
      - repeat_every
      - recurrence_frequency
      type: object
      properties:
        recurring_bill_id:
          $ref: '#/components/schemas/recurring_bill_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        status:
          $ref: '#/components/schemas/status'
        recurrence_name:
          $ref: '#/components/schemas/recurrence_name'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vat_reg_no:
          $ref: '#/components/schemas/vat_reg_no'
        is_abn_quoted:
          $ref: '#/components/schemas/is_abn_quoted'
        abn:
          $ref: '#/components/schemas/abn'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        pricebook_name:
          $ref: '#/components/schemas/pricebook_name'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items of a recurrence bill.
          type: array
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              account_id:
                $ref: '#/components/schemas/account_id'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              hsn_or_sac:
                $ref: '#/components/schemas/hsn_or_sac'
              reverse_charge_tax_id:
                $ref: '#/components/schemas/reverse_charge_tax_id'
              location_id:
                $ref: '#/components/schemas/location_id'
              quantity:
                $ref: '#/components/schemas/quantity'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tds_tax_id:
                $ref: '#/components/schemas/tds_tax_id'
              tax_treatment_code:
                $ref: '#/components/schemas/tax_treatment_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              item_order:
                $ref: '#/components/schemas/item_order'
              product_type:
                $ref: '#/components/schemas/product_type'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              unit:
                $ref: '#/components/schemas/unit'
              tags:
                $ref: '#/components/schemas/tags'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              project_id:
                $ref: '#/components/schemas/project_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              item_custom_fields:
                $ref: '#/components/schemas/item_custom_fields'
              serial_numbers:
                $ref: '#/components/schemas/serial_numbers'
        is_tds_applied:
          $ref: '#/components/schemas/is_tds_applied'
        notes:
          $ref: '#/components/schemas/notes'
        terms:
          $ref: '#/components/schemas/terms'
        payment_terms:
          $ref: '#/components/schemas/payment_terms'
        payment_terms_label:
          $ref: '#/components/schemas/payment_terms_label'
        custom_fields:
          type: array
          description: List of custom fields for the recurring bill.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        discount:
          $ref: '#/components/schemas/discount'
        discount_account_id:
          $ref: '#/components/schemas/discount_account_id'
        is_discount_before_tax:
          $ref: '#/components/schemas/is_discount_before_tax'
        repeat_every:
          description: Description for repeat_every
          type: string
          example: ''
        recurrence_frequency:
          description: Description for recurrence_frequency
          type: string
          example: ''
        tags:
          $ref: '#/components/schemas/tags'
    update-a-recurring-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Recurring Bill information has been updated.
          readOnly: true
    get-a-recurring-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        recurring_bill:
          $ref: '#/components/schemas/recurring_bill'
    delete-a-recurring-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring bill has been deleted.
          readOnly: true
    stop-a-recurring-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring bill has been stopped.
          readOnly: true
    resume-a-recurring-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The recurring bill has been activated.
          readOnly: true
    list-recurring-bill-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    vendor_credit-response:
      type: object
      properties:
        vendor_credit_id:
          $ref: '#/components/schemas/vendor_credit_id'
        vendor_credit_number:
          $ref: '#/components/schemas/vendor_credit_number'
        date:
          $ref: '#/components/schemas/date'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        status:
          $ref: '#/components/schemas/status'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        filed_in_vat_return_id:
          $ref: '#/components/schemas/filed_in_vat_return_id'
        filed_in_vat_return_name:
          $ref: '#/components/schemas/filed_in_vat_return_name'
        filed_in_vat_return_type:
          $ref: '#/components/schemas/filed_in_vat_return_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        tags:
          $ref: '#/components/schemas/tags_response'
        line_items:
          description: Line items of a vendor credit.
          type: object
          properties:
            item_id:
              $ref: '#/components/schemas/item_id'
            line_item_id:
              $ref: '#/components/schemas/line_item_id'
            sku:
              $ref: '#/components/schemas/sku'
            account_id:
              $ref: '#/components/schemas/account_id'
            product_type:
              $ref: '#/components/schemas/product_type'
            reverse_charge_tax_id:
              $ref: '#/components/schemas/reverse_charge_tax_id'
            reverse_charge_tax_name:
              $ref: '#/components/schemas/reverse_charge_tax_name'
            reverse_charge_tax_percentage:
              $ref: '#/components/schemas/reverse_charge_tax_percentage'
            reverse_charge_tax_amount:
              $ref: '#/components/schemas/reverse_charge_tax_amount'
            account_name:
              $ref: '#/components/schemas/account_name'
            name:
              $ref: '#/components/schemas/name'
            description:
              $ref: '#/components/schemas/description'
            item_order:
              $ref: '#/components/schemas/item_order'
            quantity:
              $ref: '#/components/schemas/quantity'
            unit:
              $ref: '#/components/schemas/unit'
            bcy_rate:
              $ref: '#/components/schemas/bcy_rate'
            rate:
              $ref: '#/components/schemas/rate'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            tds_tax_id:
              $ref: '#/components/schemas/tds_tax_id'
            tax_treatment_code:
              $ref: '#/components/schemas/tax_treatment_code'
            tax_exemption_id:
              $ref: '#/components/schemas/tax_exemption_id'
            tax_exemption_code:
              $ref: '#/components/schemas/tax_exemption_code'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_type:
              $ref: '#/components/schemas/tax_type'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            item_total:
              $ref: '#/components/schemas/item_total'
            item_total_inclusive_of_tax:
              $ref: '#/components/schemas/item_total_inclusive_of_tax'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            acquisition_vat_id:
              $ref: '#/components/schemas/acquisition_vat_id'
            acquisition_vat_name:
              $ref: '#/components/schemas/acquisition_vat_name'
            acquisition_vat_percentage:
              $ref: '#/components/schemas/acquisition_vat_percentage'
            acquisition_vat_amount:
              $ref: '#/components/schemas/acquisition_vat_amount'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            reverse_charge_vat_id:
              $ref: '#/components/schemas/reverse_charge_vat_id'
            reverse_charge_vat_name:
              $ref: '#/components/schemas/reverse_charge_vat_name'
            reverse_charge_vat_percentage:
              $ref: '#/components/schemas/reverse_charge_vat_percentage'
            reverse_charge_vat_amount:
              $ref: '#/components/schemas/reverse_charge_vat_amount'
            stock_on_hand:
              $ref: '#/components/schemas/stock_on_hand'
            tags:
              $ref: '#/components/schemas/tags'
            item_custom_fields:
              $ref: '#/components/schemas/item_custom_fields'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        documents:
          $ref: '#/components/schemas/documents'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        total:
          $ref: '#/components/schemas/total'
        total_credits_used:
          $ref: '#/components/schemas/total_credits_used'
        total_refunded_amount:
          $ref: '#/components/schemas/total_refunded_amount'
        balance:
          $ref: '#/components/schemas/balance'
        notes:
          $ref: '#/components/schemas/notes'
        comments:
          $ref: '#/components/schemas/comments'
        vendor_credit_refunds:
          $ref: '#/components/schemas/vendor_credit_refunds'
        bills_credited:
          $ref: '#/components/schemas/bills_credited'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
    bills:
      description: List of bills associated with the vendor payment.
      type: array
      items:
        type: object
        properties:
          bill_payment_id:
            $ref: '#/components/schemas/bill_payment_id'
          bill_id:
            $ref: '#/components/schemas/bill_id'
          amount_applied:
            $ref: '#/components/schemas/amount_applied'
          tax_amount_withheld:
            $ref: '#/components/schemas/tax_amount_withheld'
    apply_to_bills:
      type: object
      properties:
        bills:
          $ref: '#/components/schemas/bills'
    total_credits_used:
      type: number
      format: double
      example: 0
    total_refunded_amount:
      type: number
      format: double
      example: 0
    bills_credited:
      type: array
      items:
        type: object
        properties:
          bill_id:
            description: ID of the Bill Credited
            type: string
            example: '460000000057075'
          vendor_credit_bill_id:
            $ref: '#/components/schemas/vendor_credit_bill_id'
          date:
            description: Date of the Bill Credit
            type: string
            example: '2014-08-28'
          bill_number:
            description: Number of the Bill Credited
            type: string
          amount:
            description: Amount that is credited in the bill
            type: number
            format: double
            example: 13
    vendor_credit_refund_id:
      type: string
      example: '460000000078265'
    imported_transaction_id:
      description: ID of the Imported Transaction
      type: string
      example: '460000000013297'
    payee:
      type: string
      example: Richards Electric Company
    imported_transactions:
      type: array
      items:
        type: object
        properties:
          imported_transaction_id:
            $ref: '#/components/schemas/imported_transaction_id'
          date:
            description: Date of the transaction Imported
            type: string
            example: '2013-01-22'
          amount:
            description: Amount in the transaction
            type: number
            format: double
            example: 10
          payee:
            $ref: '#/components/schemas/payee'
          description:
            description: Description of the transaction
            type: string
            example: Cheque Deposit
          reference_number:
            description: Reference Number of the transaction
            type: string
            example: Ref-1900
          status:
            description: Status of the transaction
            type: string
            example: categorized
          account_id:
            description: Account ID of the transaction
            type: string
            example: 460000000006294
    vendor_credit_refunds:
      type: array
      items:
        type: object
        properties:
          vendor_credit_refund_id:
            $ref: '#/components/schemas/vendor_credit_refund_id'
          vendor_credit_id:
            $ref: '#/components/schemas/vendor_credit_id'
          date:
            $ref: '#/components/schemas/date'
          refund_mode:
            $ref: '#/components/schemas/refund_mode'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          description:
            $ref: '#/components/schemas/description'
          amount_bcy:
            $ref: '#/components/schemas/amount_bcy'
          amount_fcy:
            $ref: '#/components/schemas/amount_fcy'
          filed_in_vat_return_id:
            $ref: '#/components/schemas/filed_in_vat_return_id'
          filed_in_vat_return_name:
            $ref: '#/components/schemas/filed_in_vat_return_name'
          filed_in_vat_return_type:
            $ref: '#/components/schemas/filed_in_vat_return_type'
    vendor_credit_refund:
      type: object
      properties:
        vendor_credit_refund_id:
          $ref: '#/components/schemas/vendor_credit_refund_id'
        vendor_credit_id:
          $ref: '#/components/schemas/vendor_credit_id'
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        description:
          $ref: '#/components/schemas/description'
        imported_transactions:
          $ref: '#/components/schemas/imported_transactions'
    vendor_credit:
      type: object
      properties:
        vendor_credit_id:
          $ref: '#/components/schemas/vendor_credit_id'
        vendor_credit_number:
          $ref: '#/components/schemas/vendor_credit_number'
        date:
          $ref: '#/components/schemas/date'
        status:
          $ref: '#/components/schemas/status'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        filed_in_vat_return_id:
          $ref: '#/components/schemas/filed_in_vat_return_id'
        filed_in_vat_return_name:
          $ref: '#/components/schemas/filed_in_vat_return_name'
        filed_in_vat_return_type:
          $ref: '#/components/schemas/filed_in_vat_return_type'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        line_items:
          $ref: '#/components/schemas/line_items'
        acquisition_vat_summary:
          $ref: '#/components/schemas/acquisition_vat_summary'
        acquisition_vat_total:
          $ref: '#/components/schemas/acquisition_vat_total'
        reverse_charge_vat_summary:
          $ref: '#/components/schemas/reverse_charge_vat_summary'
        reverse_charge_vat_total:
          $ref: '#/components/schemas/reverse_charge_vat_total'
        documents:
          $ref: '#/components/schemas/documents'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        total:
          $ref: '#/components/schemas/total'
        total_credits_used:
          $ref: '#/components/schemas/total_credits_used'
        total_refunded_amount:
          $ref: '#/components/schemas/total_refunded_amount'
        balance:
          $ref: '#/components/schemas/balance'
        notes:
          $ref: '#/components/schemas/notes'
        comments:
          $ref: '#/components/schemas/comments'
        vendor_credit_refunds:
          $ref: '#/components/schemas/vendor_credit_refunds'
        bills_credited:
          $ref: '#/components/schemas/bills_credited'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-vendor-credit-request:
      required:
      - vendor_id
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vendor_credit_number:
          description: Mandatory if auto number generation is disabled.
          type: string
          example: DN-00002
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        date:
          $ref: '#/components/schemas/date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          $ref: '#/components/schemas/line_items'
        notes:
          $ref: '#/components/schemas/notes'
        documents:
          $ref: '#/components/schemas/documents'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Vendor credit has been created.
          readOnly: true
        vendor_credit:
          $ref: '#/components/schemas/vendor_credit-response'
    list-vendor-credits-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendorcredits:
          type: array
          items:
            type: object
            properties:
              vendor_credit_id:
                $ref: '#/components/schemas/vendor_credit_id'
              vendor_credit_number:
                $ref: '#/components/schemas/vendor_credit_number'
              status:
                $ref: '#/components/schemas/status'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              date:
                $ref: '#/components/schemas/date'
              total:
                $ref: '#/components/schemas/total'
              balance:
                $ref: '#/components/schemas/balance'
              vendor_id:
                $ref: '#/components/schemas/vendor_id'
              vendor_name:
                $ref: '#/components/schemas/vendor_name'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              tags:
                $ref: '#/components/schemas/tags_response'
    update-vendor-credit-request:
      required:
      - vendor_id
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        vendor_credit_number:
          description: Mandatory if auto number generation is disabled.
          type: string
          example: DN-00002
        gst_treatment:
          $ref: '#/components/schemas/gst_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        gst_no:
          $ref: '#/components/schemas/gst_no'
        source_of_supply:
          $ref: '#/components/schemas/source_of_supply'
        destination_of_supply:
          $ref: '#/components/schemas/destination_of_supply'
        place_of_supply:
          $ref: '#/components/schemas/place_of_supply'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        date:
          $ref: '#/components/schemas/date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          $ref: '#/components/schemas/line_items'
        notes:
          $ref: '#/components/schemas/notes'
        documents:
          $ref: '#/components/schemas/documents'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    update-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Vendor credit information has been updated.
          readOnly: true
        vendor_credit:
          type: object
          properties:
            vendor_credit_id:
              $ref: '#/components/schemas/vendor_credit_id'
            vendor_credit_number:
              $ref: '#/components/schemas/vendor_credit_number'
            date:
              $ref: '#/components/schemas/date'
            source_of_supply:
              $ref: '#/components/schemas/source_of_supply'
            destination_of_supply:
              $ref: '#/components/schemas/destination_of_supply'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            pricebook_id:
              $ref: '#/components/schemas/pricebook_id'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            status:
              $ref: '#/components/schemas/status'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            vendor_id:
              $ref: '#/components/schemas/vendor_id'
            vendor_name:
              $ref: '#/components/schemas/vendor_name'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            taxes:
              $ref: '#/components/schemas/taxes'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            line_items:
              description: Line items of a vendor credit.
              type: object
              properties:
                item_id:
                  $ref: '#/components/schemas/item_id'
                line_item_id:
                  $ref: '#/components/schemas/line_item_id'
                sku:
                  $ref: '#/components/schemas/sku'
                account_id:
                  $ref: '#/components/schemas/account_id'
                product_type:
                  $ref: '#/components/schemas/product_type'
                reverse_charge_tax_id:
                  $ref: '#/components/schemas/reverse_charge_tax_id'
                reverse_charge_tax_name:
                  $ref: '#/components/schemas/reverse_charge_tax_name'
                reverse_charge_tax_percentage:
                  $ref: '#/components/schemas/reverse_charge_tax_percentage'
                reverse_charge_tax_amount:
                  $ref: '#/components/schemas/reverse_charge_tax_amount'
                account_name:
                  $ref: '#/components/schemas/account_name'
                name:
                  $ref: '#/components/schemas/name'
                description:
                  $ref: '#/components/schemas/description'
                item_order:
                  $ref: '#/components/schemas/item_order'
                quantity:
                  $ref: '#/components/schemas/quantity'
                unit:
                  $ref: '#/components/schemas/unit'
                bcy_rate:
                  $ref: '#/components/schemas/bcy_rate'
                rate:
                  $ref: '#/components/schemas/rate'
                tax_id:
                  $ref: '#/components/schemas/tax_id'
                tds_tax_id:
                  $ref: '#/components/schemas/tds_tax_id'
                tax_treatment_code:
                  $ref: '#/components/schemas/tax_treatment_code'
                tax_exemption_id:
                  $ref: '#/components/schemas/tax_exemption_id'
                tax_exemption_code:
                  $ref: '#/components/schemas/tax_exemption_code'
                tax_name:
                  $ref: '#/components/schemas/tax_name'
                tax_type:
                  $ref: '#/components/schemas/tax_type'
                tax_percentage:
                  $ref: '#/components/schemas/tax_percentage'
                project_id:
                  $ref: '#/components/schemas/project_id'
                project_name:
                  $ref: '#/components/schemas/project_name'
                item_total:
                  $ref: '#/components/schemas/item_total'
                item_total_inclusive_of_tax:
                  $ref: '#/components/schemas/item_total_inclusive_of_tax'
                hsn_or_sac:
                  $ref: '#/components/schemas/hsn_or_sac'
                acquisition_vat_id:
                  $ref: '#/components/schemas/acquisition_vat_id'
                acquisition_vat_name:
                  $ref: '#/components/schemas/acquisition_vat_name'
                acquisition_vat_percentage:
                  $ref: '#/components/schemas/acquisition_vat_percentage'
                acquisition_vat_amount:
                  $ref: '#/components/schemas/acquisition_vat_amount'
                location_id:
                  $ref: '#/components/schemas/location_id'
                location_name:
                  $ref: '#/components/schemas/location_name'
                reverse_charge_vat_id:
                  $ref: '#/components/schemas/reverse_charge_vat_id'
                reverse_charge_vat_name:
                  $ref: '#/components/schemas/reverse_charge_vat_name'
                reverse_charge_vat_percentage:
                  $ref: '#/components/schemas/reverse_charge_vat_percentage'
                reverse_charge_vat_amount:
                  $ref: '#/components/schemas/reverse_charge_vat_amount'
                stock_on_hand:
                  $ref: '#/components/schemas/stock_on_hand'
                tags:
                  $ref: '#/components/schemas/tags_response'
                item_custom_fields:
                  $ref: '#/components/schemas/item_custom_fields'
            acquisition_vat_summary:
              $ref: '#/components/schemas/acquisition_vat_summary'
            acquisition_vat_total:
              $ref: '#/components/schemas/acquisition_vat_total'
            reverse_charge_vat_summary:
              $ref: '#/components/schemas/reverse_charge_vat_summary'
            reverse_charge_vat_total:
              $ref: '#/components/schemas/reverse_charge_vat_total'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            total_credits_used:
              $ref: '#/components/schemas/total_credits_used'
            total_refunded_amount:
              $ref: '#/components/schemas/total_refunded_amount'
            balance:
              $ref: '#/components/schemas/balance'
            notes:
              $ref: '#/components/schemas/notes'
            comments:
              $ref: '#/components/schemas/comments'
            vendor_credit_refunds:
              type: array
              items:
                type: object
                properties:
                  vendor_credit_refund_id:
                    $ref: '#/components/schemas/vendor_credit_refund_id'
                  vendor_credit_id:
                    $ref: '#/components/schemas/vendor_credit_id'
                  date:
                    description: Date of the Vendor Credit Refund
                    type: string
                  refund_mode:
                    $ref: '#/components/schemas/refund_mode'
                  reference_number:
                    $ref: '#/components/schemas/reference_number'
                  description:
                    $ref: '#/components/schemas/description'
                  amount_bcy:
                    $ref: '#/components/schemas/amount_bcy'
                  amount_fcy:
                    $ref: '#/components/schemas/amount_fcy'
            bills_credited:
              $ref: '#/components/schemas/bills_credited'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            tags:
              $ref: '#/components/schemas/tags_response'
    get-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendor_credit:
          type: object
          properties:
            vendor_credit_id:
              $ref: '#/components/schemas/vendor_credit_id'
            vendor_credit_number:
              $ref: '#/components/schemas/vendor_credit_number'
            date:
              $ref: '#/components/schemas/date'
            status:
              $ref: '#/components/schemas/status'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            source_of_supply:
              $ref: '#/components/schemas/source_of_supply'
            destination_of_supply:
              $ref: '#/components/schemas/destination_of_supply'
            place_of_supply:
              $ref: '#/components/schemas/place_of_supply'
            gst_no:
              $ref: '#/components/schemas/gst_no'
            gst_treatment:
              $ref: '#/components/schemas/gst_treatment'
            tax_treatment:
              $ref: '#/components/schemas/tax_treatment'
            pricebook_id:
              $ref: '#/components/schemas/pricebook_id'
            is_reverse_charge_applied:
              $ref: '#/components/schemas/is_reverse_charge_applied'
            vendor_id:
              $ref: '#/components/schemas/vendor_id'
            vendor_name:
              $ref: '#/components/schemas/vendor_name'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            price_precision:
              $ref: '#/components/schemas/price_precision'
            vat_treatment:
              $ref: '#/components/schemas/vat_treatment'
            filed_in_vat_return_id:
              $ref: '#/components/schemas/filed_in_vat_return_id'
            filed_in_vat_return_name:
              $ref: '#/components/schemas/filed_in_vat_return_name'
            filed_in_vat_return_type:
              $ref: '#/components/schemas/filed_in_vat_return_type'
            is_inclusive_tax:
              $ref: '#/components/schemas/is_inclusive_tax'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            tags:
              $ref: '#/components/schemas/tags_response'
            line_items:
              description: Line items of a vendor credit.
              type: object
              properties:
                item_id:
                  $ref: '#/components/schemas/item_id'
                line_item_id:
                  $ref: '#/components/schemas/line_item_id'
                sku:
                  $ref: '#/components/schemas/sku'
                account_id:
                  $ref: '#/components/schemas/account_id'
                product_type:
                  $ref: '#/components/schemas/product_type'
                reverse_charge_tax_id:
                  $ref: '#/components/schemas/reverse_charge_tax_id'
                reverse_charge_tax_name:
                  $ref: '#/components/schemas/reverse_charge_tax_name'
                reverse_charge_tax_percentage:
                  $ref: '#/components/schemas/reverse_charge_tax_percentage'
                reverse_charge_tax_amount:
                  $ref: '#/components/schemas/reverse_charge_tax_amount'
                account_name:
                  $ref: '#/components/schemas/account_name'
                location_id:
                  $ref: '#/components/schemas/location_id'
                location_name:
                  $ref: '#/components/schemas/location_name'
                name:
                  $ref: '#/components/schemas/name'
                description:
                  $ref: '#/components/schemas/description'
                item_order:
                  $ref: '#/components/schemas/item_order'
                quantity:
                  $ref: '#/components/schemas/quantity'
                unit:
                  $ref: '#/components/schemas/unit'
                bcy_rate:
                  $ref: '#/components/schemas/bcy_rate'
                rate:
                  $ref: '#/components/schemas/rate'
                tax_id:
                  $ref: '#/components/schemas/tax_id'
                tax_treatment_code:
                  $ref: '#/components/schemas/tax_treatment_code'
                tax_exemption_id:
                  $ref: '#/components/schemas/tax_exemption_id'
                tax_exemption_code:
                  $ref: '#/components/schemas/tax_exemption_code'
                tax_name:
                  $ref: '#/components/schemas/tax_name'
                tax_type:
                  $ref: '#/components/schemas/tax_type'
                tax_percentage:
                  $ref: '#/components/schemas/tax_percentage'
                item_total:
                  $ref: '#/components/schemas/item_total'
                item_total_inclusive_of_tax:
                  $ref: '#/components/schemas/item_total_inclusive_of_tax'
                hsn_or_sac:
                  $ref: '#/components/schemas/hsn_or_sac'
                acquisition_vat_id:
                  $ref: '#/components/schemas/acquisition_vat_id'
                acquisition_vat_name:
                  $ref: '#/components/schemas/acquisition_vat_name'
                acquisition_vat_percentage:
                  $ref: '#/components/schemas/acquisition_vat_percentage'
                acquisition_vat_amount:
                  $ref: '#/components/schemas/acquisition_vat_amount'
                reverse_charge_vat_id:
                  $ref: '#/components/schemas/reverse_charge_vat_id'
                reverse_charge_vat_name:
                  $ref: '#/components/schemas/reverse_charge_vat_name'
                reverse_charge_vat_percentage:
                  $ref: '#/components/schemas/reverse_charge_vat_percentage'
                reverse_charge_vat_amount:
                  $ref: '#/components/schemas/reverse_charge_vat_amount'
                stock_on_hand:
                  $ref: '#/components/schemas/stock_on_hand'
                tags:
                  $ref: '#/components/schemas/tags'
                item_custom_fields:
                  $ref: '#/components/schemas/item_custom_fields'
                project_id:
                  $ref: '#/components/schemas/project_id'
                project_name:
                  $ref: '#/components/schemas/project_name'
            acquisition_vat_summary:
              $ref: '#/components/schemas/acquisition_vat_summary'
            acquisition_vat_total:
              $ref: '#/components/schemas/acquisition_vat_total'
            reverse_charge_vat_summary:
              $ref: '#/components/schemas/reverse_charge_vat_summary'
            reverse_charge_vat_total:
              $ref: '#/components/schemas/reverse_charge_vat_total'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            sub_total:
              $ref: '#/components/schemas/sub_total'
            total:
              $ref: '#/components/schemas/total'
            total_credits_used:
              $ref: '#/components/schemas/total_credits_used'
            total_refunded_amount:
              $ref: '#/components/schemas/total_refunded_amount'
            balance:
              $ref: '#/components/schemas/balance'
            notes:
              $ref: '#/components/schemas/notes'
            comments:
              $ref: '#/components/schemas/comments'
            vendor_credit_refunds:
              $ref: '#/components/schemas/vendor_credit_refunds'
            bills_credited:
              $ref: '#/components/schemas/bills_credited'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
    delete-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The vendor credit has been deleted.
          readOnly: true
    void-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The vendor credit has been voided.
          readOnly: true
    submit-a-vendor-credit-for-approval-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The vendor credit has been submitted for approval successfully.
          readOnly: true
    approve-a-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have approved the vendor credit.
          readOnly: true
    list-bills-credited-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        bills_credited:
          type: array
          items:
            type: object
            properties:
              vendor_credit_id:
                $ref: '#/components/schemas/vendor_credit_id'
              bill_id:
                $ref: '#/components/schemas/bill_id'
              vendor_credit_bill_id:
                $ref: '#/components/schemas/vendor_credit_bill_id'
              date:
                $ref: '#/components/schemas/date'
              bill_number:
                $ref: '#/components/schemas/bill_number'
    apply-credits-to-a-bill-request:
      required:
      - bills
      type: object
      properties:
        bills:
          description: Amount applied from vendor credits to specified bills.
          type: array
          items:
            type: object
            required:
            - bill_id
            - amount_applied
            properties:
              bill_id:
                description: Bill Associated with the Vendor Credit
                type: string
                example: '460000000057075'
              amount_applied:
                description: Amount applied to the bill.
                type: number
                format: double
                example: 10
    apply-credits-to-a-bill-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Credits have been applied to the bill(s).
          readOnly: true
        bills:
          description: Amount applied from vendor credits to specified bills.
    delete-bills-credited-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Credits applied to the bill have been deleted.
          readOnly: true
    refund-a-vendor-credit-request:
      required:
      - date
      - amount
      - account_id
      type: object
      properties:
        date:
          description: Date of Vendor Credit Refund
          type: string
          example: '2014-08-30'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        account_id:
          $ref: '#/components/schemas/account_id'
        description:
          $ref: '#/components/schemas/description'
    refund-a-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund information for this vendor credit has been saved.
          readOnly: true
        vendor_credit_refund:
          $ref: '#/components/schemas/vendor_credit_refund'
    list-refunds-of-a-vendor-credit-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendor_credit_refunds:
          type: array
          items:
            type: object
            properties:
              vendor_credit_refund_id:
                $ref: '#/components/schemas/vendor_credit_refund_id'
              vendor_credit_id:
                $ref: '#/components/schemas/vendor_credit_id'
              date:
                description: Date of the Vendor Credit Refund
                type: string
                example: '2017-01-22'
              refund_mode:
                $ref: '#/components/schemas/refund_mode'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              amount:
                $ref: '#/components/schemas/amount'
              vendor_credit_number:
                $ref: '#/components/schemas/vendor_credit_number'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              description:
                description: Description of the Vendor Credit Refund
                type: string
                example: Cheque deposit
              amount_bcy:
                $ref: '#/components/schemas/amount_bcy'
              amount_fcy:
                $ref: '#/components/schemas/amount_fcy'
    update-vendor-credit-refund-request:
      required:
      - date
      - amount
      - account_id
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        account_id:
          $ref: '#/components/schemas/account_id'
        description:
          $ref: '#/components/schemas/description'
    update-vendor-credit-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund information has been saved.
          readOnly: true
        vendor_credit_refund:
          $ref: '#/components/schemas/vendor_credit_refund'
    get-vendor-credit-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendor_credit_refund:
          $ref: '#/components/schemas/vendor_credit_refund'
    delete-vendor-credit-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The refund has been successfully deleted.
          readOnly: true
    list-vendor-credit-refunds-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendor_credit_refunds:
          type: array
          items:
            type: object
            properties:
              vendor_credit_refund_id:
                $ref: '#/components/schemas/vendor_credit_refund_id'
              vendor_credit_id:
                $ref: '#/components/schemas/vendor_credit_id'
              date:
                description: Date of the Vendor Credit Refund
                type: string
                example: '2017-01-22'
              refund_mode:
                $ref: '#/components/schemas/refund_mode'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              amount:
                $ref: '#/components/schemas/amount'
              vendor_credit_number:
                $ref: '#/components/schemas/vendor_credit_number'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              description:
                description: Description of the Vendor Credit Refund
                type: string
                example: Cheque deposit
              amount_bcy:
                $ref: '#/components/schemas/amount_bcy'
              amount_fcy:
                $ref: '#/components/schemas/amount_fcy'
    list-vendor-credit-comments-and-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              comment_id:
                $ref: '#/components/schemas/comment_id'
              vendor_credit_id:
                $ref: '#/components/schemas/vendor_credit_id'
              description:
                description: Description of the Comment
                type: string
                example: Credits applied to Bill 1
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              comment_type:
                $ref: '#/components/schemas/comment_type'
              date:
                description: Date of the Comment
                type: string
                example: '2017-01-18'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
              operation_type:
                $ref: '#/components/schemas/operation_type'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
    vendorpayment_refund_id:
      description: ID of the Vendor Payment
      type: string
      example: '460000000000007'
    vendorpayment_id:
      description: Vendor Payment to which you want to record the refund.
      type: string
      example: '460000000012345'
    vendorpayment_number:
      description: Number of the Vendor Payment
      type: string
      example: '2'
    to_account_id:
      description: 'ID of the account to which the money gets transferred(Mandatory
        for specific type of transactions). Ex: From a bank account, to-account can
        be: bank, card, drawings, expense,credit notes. From a card account, to-account
        can be: card, bank, expense.'
      type: string
      example: '460000000048001'
    tax_account_name:
      description: Name of the Tax Account
      type: string
    paid_through_account_type:
      description: Type of the Paid through Account
      type: string
      example: bank
    check_id:
      description: ID of the Check created
      type: string
    memo:
      description: Memo or note associated with the check.
      type: string
    amount_in_words:
      description: Amount in Words
      type: string
    bcy_balance:
      description: Balance in Base Currency of the Organisation
      type: number
      format: double
      example: 171.47
    vendorpayment_refunds:
      type: string
    to_account_name:
      type: string
      example: Corporate Account
    bill_numbers:
      description: Numbers of the bill
      type: string
    vendorpayment:
      type: array
      items:
        type: object
        properties:
          payment_id:
            $ref: '#/components/schemas/payment_id'
          vendor_id:
            $ref: '#/components/schemas/vendor_id'
          vendor_name:
            $ref: '#/components/schemas/vendor_name'
          payment_mode:
            $ref: '#/components/schemas/payment_mode'
          payment_number:
            $ref: '#/components/schemas/payment_number'
          custom_fields:
            $ref: '#/components/schemas/custom_fields'
          description:
            $ref: '#/components/schemas/description'
          documents:
            $ref: '#/components/schemas/documents'
          date:
            $ref: '#/components/schemas/date'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          exchange_rate:
            $ref: '#/components/schemas/exchange_rate'
          tax_account_name:
            $ref: '#/components/schemas/tax_account_name'
          tax_amount_withheld:
            $ref: '#/components/schemas/tax_amount_withheld'
          amount:
            $ref: '#/components/schemas/amount'
          balance:
            $ref: '#/components/schemas/balance'
          currency_id:
            $ref: '#/components/schemas/currency_id'
          currency_symbol:
            $ref: '#/components/schemas/currency_symbol'
          location_id:
            $ref: '#/components/schemas/location_id'
          location_name:
            $ref: '#/components/schemas/location_name'
          created_time:
            $ref: '#/components/schemas/created_time'
          last_modified_time:
            $ref: '#/components/schemas/last_modified_time'
          paid_through_account_id:
            $ref: '#/components/schemas/paid_through_account_id'
          paid_through_account_name:
            $ref: '#/components/schemas/paid_through_account_name'
          paid_through_account_type:
            $ref: '#/components/schemas/paid_through_account_type'
          is_paid_via_print_check:
            $ref: '#/components/schemas/is_paid_via_print_check'
          is_ach_payment:
            $ref: '#/components/schemas/is_ach_payment'
          ach_payment_status:
            $ref: '#/components/schemas/ach_payment_status'
          check_details:
            $ref: '#/components/schemas/check_details'
          billing_address:
            $ref: '#/components/schemas/billing_address'
          bills:
            $ref: '#/components/schemas/bills'
          vendorpayment_refunds:
            $ref: '#/components/schemas/vendorpayment_refunds'
          imported_transactions:
            $ref: '#/components/schemas/imported_transactions'
          tags:
            $ref: '#/components/schemas/tags_response'
    vendorpayments:
      type: array
      items:
        type: object
        properties:
          payment_id:
            $ref: '#/components/schemas/payment_id'
          vendor_id:
            $ref: '#/components/schemas/vendor_id'
          vendor_name:
            $ref: '#/components/schemas/vendor_name'
          bill_numbers:
            $ref: '#/components/schemas/bill_numbers'
          payment_mode:
            $ref: '#/components/schemas/payment_mode'
          payment_number:
            $ref: '#/components/schemas/payment_number'
          description:
            $ref: '#/components/schemas/description'
          date:
            $ref: '#/components/schemas/date'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          exchange_rate:
            $ref: '#/components/schemas/exchange_rate'
          amount:
            $ref: '#/components/schemas/amount'
          bcy_amount:
            $ref: '#/components/schemas/bcy_amount'
          paid_through_account_id:
            $ref: '#/components/schemas/paid_through_account_id'
          paid_through_account_name:
            $ref: '#/components/schemas/paid_through_account_name'
          balance:
            $ref: '#/components/schemas/balance'
          bcy_balance:
            $ref: '#/components/schemas/bcy_balance'
          created_time:
            $ref: '#/components/schemas/created_time'
          last_modified_time:
            $ref: '#/components/schemas/last_modified_time'
          is_paid_via_print_check:
            $ref: '#/components/schemas/is_paid_via_print_check'
          has_attachment:
            $ref: '#/components/schemas/has_attachment'
          is_ach_payment:
            $ref: '#/components/schemas/is_ach_payment'
          ach_payment_status:
            $ref: '#/components/schemas/ach_payment_status'
          ach_gw_transaction_id:
            $ref: '#/components/schemas/ach_gw_transaction_id'
          check_details:
            $ref: '#/components/schemas/check_details'
          tags:
            $ref: '#/components/schemas/tags_response'
    vendorpayment_refund:
      type: array
      items:
        type: object
        properties:
          vendorpayment_refund_id:
            $ref: '#/components/schemas/vendorpayment_refund_id'
          vendorpayment_id:
            $ref: '#/components/schemas/vendorpayment_id'
          date:
            $ref: '#/components/schemas/date'
          refund_mode:
            $ref: '#/components/schemas/refund_mode'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          amount:
            $ref: '#/components/schemas/amount'
          exchange_rate:
            $ref: '#/components/schemas/exchange_rate'
          to_account_id:
            $ref: '#/components/schemas/to_account_id'
          to_account_name:
            $ref: '#/components/schemas/to_account_name'
          description:
            $ref: '#/components/schemas/description'
    fetchTaxInfo:
      description: Check if tax information should be fetched
      type: boolean
      example: false
    fetchstatementlineinfo:
      description: Check is Statement Line Information for Vendor Payment be fetched
      type: boolean
      example: true
    is_bill_payment_id:
      description: Check if the ID is Bill Payment or Vendor Payment
      type: boolean
      example: true
    create-a-vendor-payment-request:
      required:
      - amount
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        bills:
          $ref: '#/components/schemas/bills'
        date:
          $ref: '#/components/schemas/date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        amount:
          $ref: '#/components/schemas/amount'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        description:
          $ref: '#/components/schemas/description'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        check_details:
          $ref: '#/components/schemas/check_details'
        is_paid_via_print_check:
          $ref: '#/components/schemas/is_paid_via_print_check'
        location_id:
          $ref: '#/components/schemas/location_id'
        tags:
          $ref: '#/components/schemas/tags'
        custom_fields:
          type: array
          description: Custom fields associated with the vendor payment.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
    create-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The payment made to the vendor has been recorded.
          readOnly: true
        ach_payment_status:
          $ref: '#/components/schemas/ach_payment_status'
        amount:
          $ref: '#/components/schemas/amount'
        balance:
          $ref: '#/components/schemas/balance'
        billing_address:
          $ref: '#/components/schemas/billing_address'
        bills:
          description: Individual bill payment details as list.
          type: array
          items:
            type: object
            properties:
              bill_payment_id:
                $ref: '#/components/schemas/bill_payment_id'
              bill_id:
                $ref: '#/components/schemas/bill_id'
              amount_applied:
                $ref: '#/components/schemas/amount_applied'
              tax_amount_withheld:
                $ref: '#/components/schemas/tax_amount_withheld'
              balance:
                $ref: '#/components/schemas/balance'
              bill_number:
                $ref: '#/components/schemas/bill_number'
              date:
                $ref: '#/components/schemas/date'
              due_date:
                $ref: '#/components/schemas/due_date'
              price_precision:
                $ref: '#/components/schemas/price_precision'
              total:
                $ref: '#/components/schemas/total'
        check_details:
          type: object
          description: Details of the check when the payment mode is Check.
          properties:
            amount_in_words:
              $ref: '#/components/schemas/amount_in_words'
            check_id:
              $ref: '#/components/schemas/check_id'
            check_number:
              $ref: '#/components/schemas/check_number'
            check_status:
              $ref: '#/components/schemas/check_status'
            memo:
              $ref: '#/components/schemas/memo'
        comments:
          $ref: '#/components/schemas/comments'
        created_time:
          $ref: '#/components/schemas/created_time'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        date:
          $ref: '#/components/schemas/date'
        description:
          $ref: '#/components/schemas/description'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        imported_transactions:
          $ref: '#/components/schemas/imported_transactions'
        documents:
          $ref: '#/components/schemas/documents'
        is_ach_payment:
          $ref: '#/components/schemas/is_ach_payment'
        is_paid_via_print_check:
          $ref: '#/components/schemas/is_paid_via_print_check'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        paid_through_account_name:
          $ref: '#/components/schemas/paid_through_account_name'
        paid_through_account_type:
          $ref: '#/components/schemas/paid_through_account_type'
        payment_id:
          $ref: '#/components/schemas/payment_id'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        payment_number:
          $ref: '#/components/schemas/payment_number'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        tags:
          $ref: '#/components/schemas/tags_response'
        tax_account_name:
          $ref: '#/components/schemas/tax_account_name'
        tax_amount_withheld:
          $ref: '#/components/schemas/tax_amount_withheld'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        vendorpayment_refunds:
          $ref: '#/components/schemas/vendorpayment_refunds'
    list-vendor-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendorpayments:
          type: array
          items:
            type: object
            properties:
              payment_id:
                $ref: '#/components/schemas/payment_id'
              vendor_id:
                $ref: '#/components/schemas/vendor_id'
              vendor_name:
                $ref: '#/components/schemas/vendor_name'
              bill_numbers:
                $ref: '#/components/schemas/bill_numbers'
              payment_mode:
                $ref: '#/components/schemas/payment_mode'
              payment_number:
                $ref: '#/components/schemas/payment_number'
              description:
                $ref: '#/components/schemas/description'
              date:
                $ref: '#/components/schemas/date'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              amount:
                $ref: '#/components/schemas/amount'
              bcy_amount:
                $ref: '#/components/schemas/bcy_amount'
              paid_through_account_id:
                $ref: '#/components/schemas/paid_through_account_id'
              paid_through_account_name:
                $ref: '#/components/schemas/paid_through_account_name'
              balance:
                $ref: '#/components/schemas/balance'
              bcy_balance:
                $ref: '#/components/schemas/bcy_balance'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
              is_paid_via_print_check:
                $ref: '#/components/schemas/is_paid_via_print_check'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              is_ach_payment:
                $ref: '#/components/schemas/is_ach_payment'
              ach_payment_status:
                $ref: '#/components/schemas/ach_payment_status'
              ach_gw_transaction_id:
                $ref: '#/components/schemas/ach_gw_transaction_id'
              tags:
                $ref: '#/components/schemas/tags_response'
              check_details:
                type: array
                description: Details of the check when the payment mode is Check.
                items:
                  type: object
                  properties:
                    check_id:
                      $ref: '#/components/schemas/check_id'
                    check_status:
                      $ref: '#/components/schemas/check_status'
                    check_number:
                      $ref: '#/components/schemas/check_number'
                    memo:
                      $ref: '#/components/schemas/memo'
    update-a-vendor-payment-request:
      required:
      - amount
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        bills:
          $ref: '#/components/schemas/bills'
        date:
          $ref: '#/components/schemas/date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        amount:
          $ref: '#/components/schemas/amount'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        description:
          $ref: '#/components/schemas/description'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        is_paid_via_print_check:
          $ref: '#/components/schemas/is_paid_via_print_check'
        check_details:
          $ref: '#/components/schemas/check_details'
        location_id:
          $ref: '#/components/schemas/location_id'
        tags:
          $ref: '#/components/schemas/tags'
        custom_fields:
          type: array
          description: Custom fields associated with the vendor payment.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
    update-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The details of the payment made to the vendor has been updated
          readOnly: true
        vendorpayment:
          $ref: '#/components/schemas/vendorpayment'
    get-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendorpayment:
          type: object
          properties:
            payment_id:
              $ref: '#/components/schemas/payment_id'
            vendor_id:
              $ref: '#/components/schemas/vendor_id'
            vendor_name:
              $ref: '#/components/schemas/vendor_name'
            payment_mode:
              $ref: '#/components/schemas/payment_mode'
            payment_number:
              $ref: '#/components/schemas/payment_number'
            description:
              $ref: '#/components/schemas/description'
            date:
              $ref: '#/components/schemas/date'
            reference_number:
              $ref: '#/components/schemas/reference_number'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            tax_account_name:
              $ref: '#/components/schemas/tax_account_name'
            tax_amount_withheld:
              $ref: '#/components/schemas/tax_amount_withheld'
            amount:
              $ref: '#/components/schemas/amount'
            balance:
              $ref: '#/components/schemas/balance'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            currency_symbol:
              $ref: '#/components/schemas/currency_symbol'
            created_time:
              $ref: '#/components/schemas/created_time'
            last_modified_time:
              $ref: '#/components/schemas/last_modified_time'
            paid_through_account_id:
              $ref: '#/components/schemas/paid_through_account_id'
            paid_through_account_name:
              $ref: '#/components/schemas/paid_through_account_name'
            paid_through_account_type:
              $ref: '#/components/schemas/paid_through_account_type'
            is_paid_via_print_check:
              $ref: '#/components/schemas/is_paid_via_print_check'
            is_ach_payment:
              $ref: '#/components/schemas/is_ach_payment'
            ach_payment_status:
              $ref: '#/components/schemas/ach_payment_status'
            check_details:
              type: object
              description: Details of the check when the payment mode is Check.
              properties:
                amount_in_words:
                  $ref: '#/components/schemas/amount_in_words'
                check_id:
                  $ref: '#/components/schemas/check_id'
                check_number:
                  $ref: '#/components/schemas/check_number'
                check_status:
                  $ref: '#/components/schemas/check_status'
                memo:
                  $ref: '#/components/schemas/memo'
            billing_address:
              $ref: '#/components/schemas/billing_address'
            comments:
              $ref: '#/components/schemas/comments'
            vendorpayment_refunds:
              $ref: '#/components/schemas/vendorpayment_refunds'
            bills:
              $ref: '#/components/schemas/bills'
            location_id:
              $ref: '#/components/schemas/location_id'
            location_name:
              $ref: '#/components/schemas/location_name'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            tags:
              $ref: '#/components/schemas/tags_response'
    delete-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The payment has been deleted.
          readOnly: true
    list-refunds-of-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        vendorpayment_refunds:
          $ref: '#/components/schemas/vendorpayment_refunds'
    refund-an-excess-vendor-payment-request:
      required:
      - date
      - amount
      - to_account_id
      type: object
      properties:
        date:
          description: Date of the Vendor Payment Refund.
          type: string
          example: '2017-01-10'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          description: Reference Number of the Payment Refund
          type: string
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        description:
          description: Description of the Payment Refund
          type: string
          example: Payment Refund
    refund-an-excess-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Vendor Payment Refunded successfully
          readOnly: true
        vendorpayment_refund:
          $ref: '#/components/schemas/vendorpayment_refund'
    bulk-delete-vendor-payments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected vendor payments have been deleted.
          readOnly: true
    email-a-vendor-payment-request:
      required:
      - to_mail_ids
      - subject
      - body
      type: object
      properties:
        send_from_org_email_id:
          description: Boolean to trigger the email from the organization's email
            address
          type: boolean
          example: false
        from_address_id:
          description: From email address id
          type: string
          example: '2000000011993'
        to_mail_ids:
          description: Array of email address of the recipients.
          type: array
          items:
            type: string
          example:
          - vendor@example.com
        cc_mail_ids:
          description: Array of email address of the recipients to be CC'd.
          type: array
          items:
            type: string
          example:
          - manager@example.com
        subject:
          description: Subject of the email
          type: string
          example: 'Vendor Payment Receipt from Your Company (Payment#: VP-00001)'
        body:
          description: Body of the email
          type: string
          example: 'Dear Vendor,<br><br>Thank you for your services.<br><br>Please
            find attached the payment receipt for Payment#: VP-00001.<br><br>Payment
            Details:<br>Payment Number: VP-00001<br>Date: 2024-01-15<br>Amount: $1,500.00<br><br>Best
            regards,<br>Your Company'
        email_template_id:
          description: Get the email content based on a specific email template. If
            this param is not inputted, then the content will be based on the email
            template associated with the vendor. If no template is associated with
            the vendor, then default template will be used.
          type: string
    email-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your vendor payment receipt has been sent.
          readOnly: true
    get-vendor-payment-email-content-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        body:
          description: The body of the email
          type: string
          example: 'Dear Vendor,<br><br>Thank you for your services.<br><br>Please
            find attached the payment receipt for Payment#: VP-00001.<br><br>Payment
            Details:<br>Payment Number: VP-00001<br>Date: 2024-01-15<br>Amount: $1,500.00<br><br>Best
            regards,<br>Your Company'
        subject:
          description: Subject of the email
          type: string
          example: 'Vendor Payment Receipt from Your Company (Payment#: VP-00001)'
        to_contacts:
          description: Recipients of the email
          type: array
          items:
            type: object
            properties:
              first_name:
                description: First name of the contact person
                type: string
                example: John
              last_name:
                description: Last name of the contact person
                type: string
                example: Doe
              email:
                description: Email address of the contact person
                type: string
                example: john.doe@vendor.com
              phone:
                description: Phone number of the contact person
                type: string
                example: +1-555-123-4567
              mobile:
                description: Mobile number of the contact person
                type: string
                example: +1-555-987-6543
              selected:
                description: Whether this contact is selected as recipient
                type: boolean
                example: true
              contact_person_id:
                description: ID of the contact person
                type: string
                example: '460000000026050'
              salutation:
                description: Salutation for the contact person
                type: string
                example: Mr.
        attachment_name:
          description: Name of the attachment file
          type: string
          example: vendor_payment_receipt.pdf
        email_template_id:
          description: ID of the email template used
          type: string
          example: '460000000000123'
        file_name:
          description: Name of the file
          type: string
          example: VP-00001.pdf
        from_emails:
          description: Sender email addresses available
          type: array
          items:
            type: object
            properties:
              user_name:
                description: Name of the sender
                type: string
                example: Your Company
              selected:
                description: Whether this email is selected as sender
                type: boolean
                example: true
              email:
                description: Email address of the sender
                type: string
                example: noreply@yourcompany.com
        gateways_configured:
          description: Whether payment gateways are configured
          type: boolean
          example: false
        deprecated_placeholders_used:
          description: List of deprecated placeholders used in the template
          type: array
          items:
            type: string
          example: []
        error_list:
          description: List of errors if any
          type: array
          items:
            type: string
          example: []
    module_record-response:
      type: object
      properties:
        module_record_id:
          $ref: '#/components/schemas/record_id'
        module_api_name:
          $ref: '#/components/schemas/module_api_name'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        last_modified_time_formatted:
          $ref: '#/components/schemas/last_modified_time_formatted'
        created_time:
          $ref: '#/components/schemas/created_time'
        created_time_formatted:
          $ref: '#/components/schemas/created_time'
        created_by_id:
          $ref: '#/components/schemas/created_time_formatted'
        last_modified_by_id:
          $ref: '#/components/schemas/last_modified_by_id'
        record_name:
          $ref: '#/components/schemas/record_name'
        record_name_formatted:
          $ref: '#/components/schemas/record_name_formatted'
        cf_debt_amount:
          $ref: '#/components/schemas/cf_debt_amount'
        cf_debt_amount_formatted:
          $ref: '#/components/schemas/cf_debt_amount_formatted'
    record_name:
      description: Name of the record
      type: string
      example: Alice
    shared_to:
      description: The users ID to whome it should be shared
      type: array
      items:
        type: string
      example:
      - 460000000639128
      - 460000000639129
    record_id:
      description: ID of the Custom Module Record
      type: string
      example: '460000000639129'
    module_api_name:
      description: Module API Name
      type: string
      example: cm_debtor
    last_modified_by_id:
      description: ID of the user who last modified the task.
      type: string
      example: 982000000567123
    last_modified_time_formatted:
      description: Formatted value of modified Time
      type: string
      example: 18/02/2022 11:00 AM
    created_time_formatted:
      description: Formatted creation time of the task.
      type: string
      example: 01-01-2025 10:00:00
    record_name_formatted:
      description: Name of the Record Formatted
      type: string
      example: Alice
    date_formatted:
      description: Date of the comment
      type: string
      example: 17/02/2022 12:09 AM
    shared_type:
      description: Sharing type
      type: string
      example: read_write
    can_submit:
      description: Whether the record can be submitted
      type: boolean
      example: false
    approvers_list:
      description: List of approvers
      type: string
      example: ''
    can_approve:
      description: To get whether the record can be approved
      type: boolean
      example: false
    module_name:
      description: Name of the Module
      type: string
      example: debtor
    module_record_id:
      description: Reeccord ID
      type: string
      example: 3000000003057
    module_record_ids:
      description: Reeccord IDs
      type: string
      example: 460000000639128,460000000639129
    field_id:
      description: Field ID
      type: string
      example: 1
    is_active:
      description: Check if account is Active or Inactive
      type: boolean
      example: true
    is_mandatory:
      description: Is Mandatory
      type: boolean
      example: false
    api_name:
      description: API name of the field
      type: string
      example: record_name
    data_type_formatted:
      description: Formattd data type of the field
      type: string
      example: Text Box (Single Line)
    value_formatted:
      description: Formatted value of the Field
      type: string
      example: Alice
    pii_type:
      description: PII type of the Field
      type: string
      example: non_pii
    max_length:
      description: Maximum length of the field
      type: number
      example: 255
    help_text:
      description: Help Text
      type: string
      example: ''
    module_plural_name:
      description: Module name in plural
      type: string
      example: debtors
    cf_debt_amount:
      description: Value of the custom Field
      type: number
      example: 10000
    cf_debt_amount_formatted:
      description: The formatted value of the custom Field
      type: string
      example: ₹10,000.00
    id:
      description: User ID
      type: number
      example: 2112155000000069000
    text:
      description: User Text
      type: string
      example: Staff Bob
    is_current_user:
      type: boolean
      description: Indicates whether the user is the current user.
      example: true
    module_record_hash:
      type: object
      properties:
        module_record_id:
          $ref: '#/components/schemas/date'
        module_api_name:
          $ref: '#/components/schemas/module_api_name'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        last_modified_time_formatted:
          $ref: '#/components/schemas/last_modified_time_formatted'
        created_time:
          $ref: '#/components/schemas/created_time'
        created_time_formatted:
          $ref: '#/components/schemas/created_time_formatted'
        created_by_id:
          $ref: '#/components/schemas/created_by_id'
        last_modified_by_id:
          $ref: '#/components/schemas/last_modified_by_id'
        record_name:
          $ref: '#/components/schemas/record_name'
        record_name_formatted:
          $ref: '#/components/schemas/record_name_formatted'
    module_fields:
      type: array
      items:
        type: object
        properties:
          field_id:
            $ref: '#/components/schemas/field_id'
          is_active:
            $ref: '#/components/schemas/is_active'
          is_mandatory:
            $ref: '#/components/schemas/is_mandatory'
          label:
            $ref: '#/components/schemas/label'
          api_name:
            $ref: '#/components/schemas/api_name'
          data_type_formatted:
            $ref: '#/components/schemas/data_type_formatted'
          value_formatted:
            $ref: '#/components/schemas/value_formatted'
          data_type:
            $ref: '#/components/schemas/data_type'
          pii_type:
            $ref: '#/components/schemas/pii_type'
          value:
            $ref: '#/components/schemas/value'
          max_length:
            $ref: '#/components/schemas/max_length'
          help_text:
            $ref: '#/components/schemas/help_text'
    module_record:
      type: array
      items:
        $ref: '#/components/schemas/module_record-response'
    module_record_module_put:
      type: array
      items:
        type: object
        properties:
          module_record_id:
            $ref: '#/components/schemas/record_id'
          module_api_name:
            $ref: '#/components/schemas/module_api_name'
          last_modified_time:
            $ref: '#/components/schemas/last_modified_time'
          last_modified_time_formatted:
            $ref: '#/components/schemas/last_modified_time_formatted'
          created_time:
            $ref: '#/components/schemas/created_time'
          created_time_formatted:
            $ref: '#/components/schemas/created_time'
          created_by_id:
            $ref: '#/components/schemas/created_time_formatted'
          last_modified_by_id:
            $ref: '#/components/schemas/last_modified_by_id'
          record_name:
            $ref: '#/components/schemas/record_name'
          record_name_formatted:
            $ref: '#/components/schemas/record_name_formatted'
          cf_debt_amount:
            $ref: '#/components/schemas/cf_debt_amount'
          cf_debt_amount_formatted:
            $ref: '#/components/schemas/cf_debt_amount_formatted'
          code:
            type: integer
            example: 0
          message:
            type: string
            example: success
    module_record_get:
      type: object
      properties:
        module_plural_name:
          $ref: '#/components/schemas/module_plural_name'
        comments:
          $ref: '#/components/schemas/comments'
        module_api_name:
          $ref: '#/components/schemas/module_api_name'
        shared_type:
          $ref: '#/components/schemas/shared_type'
        can_submit:
          $ref: '#/components/schemas/can_submit'
        approvers_list:
          $ref: '#/components/schemas/approvers_list'
        can_approve:
          $ref: '#/components/schemas/can_approve'
        module_name:
          $ref: '#/components/schemas/module_name'
        module_fields:
          $ref: '#/components/schemas/module_fields'
        module_record_id:
          $ref: '#/components/schemas/module_record_id'
        shared_to:
          $ref: '#/components/schemas/shared_to'
    users:
      type: array
      items:
        type: string
    get-record-list-of-a-custom-module-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        module_record:
          $ref: '#/components/schemas/module_record'
        page_context:
          $ref: '#/components/schemas/page_context'
    bulk-update-custom-module-request:
      required:
      - record_name
      type: object
      properties:
        cf_debt_amount:
          $ref: '#/components/schemas/cf_debt_amount'
        module_record_id:
          $ref: '#/components/schemas/module_record_ids'
        record_name:
          description: Name of the record
          type: string
          example: Alice
    bulk-update-custom-module-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Bulk update successfull
          readOnly: true
        module_record:
          $ref: '#/components/schemas/module_record_module_put'
    create-custom-modules-request:
      required:
      - record_name
      type: object
      properties:
        record_name:
          $ref: '#/components/schemas/record_name'
        cf_debt_amount:
          $ref: '#/components/schemas/cf_debt_amount'
    create-custom-modules-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: debtor is created successfully
          readOnly: true
        module_record:
          $ref: '#/components/schemas/module_record'
    delete-custom-modules-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Module record deleted
          readOnly: true
    get-individual-record-details-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        module_record:
          $ref: '#/components/schemas/module_record_get'
        module_record_hash:
          $ref: '#/components/schemas/module_record_hash'
        users:
          $ref: '#/components/schemas/users'
    update-custom-module-request:
      required:
      - record_name
      type: object
      properties:
        record_name:
          $ref: '#/components/schemas/record_name'
        cf_debt_amount:
          $ref: '#/components/schemas/cf_debt_amount'
    update-custom-module-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: debtor is updated successfully.
          readOnly: true
        module_record:
          $ref: '#/components/schemas/module_record'
    delete-individual-records-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
    module_configuration:
      type: object
      properties:
        module_id:
          type: string
          description: Unique identifier of the module.
          example: '982000000567001'
        module_name:
          type: string
          description: Display name of the custom module.
          example: Debtors
        module_api_name:
          type: string
          description: API name of the custom module (starts with cm_).
          example: cm_debtor
        is_active:
          type: boolean
          description: Whether the module is active.
          example: true
        singular_name:
          type: string
          description: Singular form of the module name.
          example: Debtor
        plural_name:
          type: string
          description: Plural form of the module name.
          example: Debtors
    create-module-configuration-request:
      type: object
      required:
      - module_name
      - plural_name
      properties:
        module_name:
          type: string
          description: Display name of the custom module. <code>Max-length [25]</code>
          example: Debtor
        plural_name:
          type: string
          description: Plural form of the module name. <code>Max-length [30]</code>
          example: Debtors
        parent_module_id:
          type: string
          description: ID of the parent module, if this is a sub-module.
        record_name:
          type: string
          description: Label for the record name field. <code>Max-length [30]</code>
          example: Debtor Name
        description:
          type: string
          description: Description of the custom module. <code>Max-length [150]</code>
          example: Module to track debtors
        shared_type:
          type: string
          description: 'Sharing type for the module. Allowed Values: <code>private</code>,
            <code>read_only</code>, <code>read_write</code> and <code>public</code>.'
          example: private
        is_reporting_tags_supported:
          type: boolean
          description: Whether reporting tags are supported for this module.
          example: false
        can_edit_module:
          type: boolean
          description: Whether records in this module can be edited.
          example: true
        can_create_module:
          type: boolean
          description: Whether records can be created in this module.
          example: true
        can_delete_module:
          type: boolean
          description: Whether records can be deleted from this module.
          example: true
        can_bulk_update_module:
          type: boolean
          description: Whether bulk update is supported.
          example: false
        can_send_mail:
          type: boolean
          description: Whether sending mail is supported from this module.
          example: false
        can_bulk_send_mail:
          type: boolean
          description: Whether bulk mail sending is supported.
          example: false
        can_attach_documents:
          type: boolean
          description: Whether document attachment is supported.
          example: true
        can_show_pdf_view:
          type: boolean
          description: Whether PDF view is available.
          example: false
        can_allow_pdf_print:
          type: boolean
          description: Whether PDF print is allowed.
          example: false
        allow_forclone:
          type: boolean
          description: Whether cloning records is allowed.
          example: false
        can_lock_record:
          type: boolean
          description: Whether record locking is supported.
          example: false
        can_use_widget_for_custommodule:
          type: boolean
          description: Whether widgets can be used in this module.
          example: false
        can_show_custommodule_lhs:
          type: boolean
          description: Whether to show the module in the left-hand side navigation.
          example: true
        show_custommodule_in:
          type: array
          description: Where to show the custom module. Each item has a <code>show_in</code>
            value.
          items:
            type: object
            properties:
              show_in:
                type: string
                description: 'Allowed Values: <code>settings</code>, <code>webui</code>,
                  <code>extension</code> and <code>lhs</code>.'
                example: webui
        module_permission:
          type: array
          description: Role-based permissions for the module.
          items:
            type: object
            properties:
              role_id:
                type: string
                description: ID of the role.
              can_create:
                type: boolean
                description: Permission to create records.
              can_edit:
                type: boolean
                description: Permission to edit records.
              can_view:
                type: boolean
                description: Permission to view records.
              can_delete:
                type: boolean
                description: Permission to delete records.
              can_approve:
                type: boolean
                description: Permission to approve records.
              full_access:
                type: boolean
                description: Full access permission.
        module_visibility:
          type: array
          description: Visibility settings for the module in different contexts.
          items:
            type: object
            properties:
              type:
                type: string
                description: Context type.
              is_hidden:
                type: boolean
                description: Whether the module is hidden in this context.
        allowed_apps:
          type: array
          description: List of apps where this module is available.
          items:
            type: string
          example:
          - books
        default_customview_api_name:
          type: string
          description: API name of the default custom view for this module.
    update-module-configuration-request:
      type: object
      required:
      - module_name
      - plural_name
      properties:
        module_name:
          type: string
          description: Display name of the custom module. <code>Max-length [25]</code>
          example: Debtor
        plural_name:
          type: string
          description: Plural form of the module name. <code>Max-length [30]</code>
          example: Debtors
        parent_module_id:
          type: string
          description: ID of the parent module, if this is a sub-module.
        record_name:
          type: string
          description: Label for the record name field. <code>Max-length [30]</code>
          example: Debtor Name
        description:
          type: string
          description: Description of the custom module. <code>Max-length [150]</code>
          example: Module to track debtors
        shared_type:
          type: string
          description: 'Sharing type for the module. Allowed Values: <code>private</code>,
            <code>read_only</code>, <code>read_write</code> and <code>public</code>.'
          example: private
        is_reporting_tags_supported:
          type: boolean
          description: Whether reporting tags are supported for this module.
          example: false
        can_edit_module:
          type: boolean
          description: Whether records in this module can be edited.
          example: true
        can_bulk_update_module:
          type: boolean
          description: Whether bulk update is supported.
          example: false
        can_send_mail:
          type: boolean
          description: Whether sending mail is supported from this module.
          example: false
        can_bulk_send_mail:
          type: boolean
          description: Whether bulk mail sending is supported.
          example: false
        can_attach_documents:
          type: boolean
          description: Whether document attachment is supported.
          example: true
        can_show_pdf_view:
          type: boolean
          description: Whether PDF view is available.
          example: false
        can_allow_pdf_print:
          type: boolean
          description: Whether PDF print is allowed.
          example: false
        allow_forclone:
          type: boolean
          description: Whether cloning records is allowed.
          example: false
        can_lock_record:
          type: boolean
          description: Whether record locking is supported.
          example: false
        can_use_widget_for_custommodule:
          type: boolean
          description: Whether widgets can be used in this module.
          example: false
        can_show_custommodule_lhs:
          type: boolean
          description: Whether to show the module in the left-hand side navigation.
          example: true
        show_custommodule_in:
          type: array
          description: Where to show the custom module. Each item has a <code>show_in</code>
            value.
          items:
            type: object
            properties:
              show_in:
                type: string
                description: 'Allowed Values: <code>settings</code>, <code>webui</code>,
                  <code>extension</code> and <code>lhs</code>.'
                example: webui
        module_permission:
          type: array
          description: Role-based permissions for the module.
          items:
            type: object
            properties:
              role_id:
                type: string
                description: ID of the role.
              can_create:
                type: boolean
                description: Permission to create records.
              can_edit:
                type: boolean
                description: Permission to edit records.
              can_view:
                type: boolean
                description: Permission to view records.
              can_delete:
                type: boolean
                description: Permission to delete records.
              can_approve:
                type: boolean
                description: Permission to approve records.
              full_access:
                type: boolean
                description: Full access permission.
        module_visibility:
          type: array
          description: Visibility settings for the module in different contexts.
          items:
            type: object
            properties:
              type:
                type: string
                description: Context type.
              is_hidden:
                type: boolean
                description: Whether the module is hidden in this context.
        default_customview_api_name:
          type: string
          description: API name of the default custom view for this module.
    list-module-configurations-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        modules:
          type: array
          items:
            $ref: '#/components/schemas/module_configuration'
    get-module-configuration-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        module:
          $ref: '#/components/schemas/module_configuration'
    create-module-configuration-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The custom module has been created.
        module:
          $ref: '#/components/schemas/module_configuration'
    update-module-configuration-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The custom module has been updated.
        module:
          $ref: '#/components/schemas/module_configuration'
    delete-module-configuration-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The custom module has been deleted.
    as_of_date:
      type: string
      example: '2014-08-30'
    account_code:
      description: Code Associated with the Account
      type: string
    account_type:
      description: 'Type of the account. Allowed Values: <code>other_asset</code>,
        <code>other_current_asset</code>, <code>cash</code>, <code>bank</code>, <code>fixed_asset</code>,
        <code>other_current_liability</code>, <code>credit_card</code>, <code>long_term_liability</code>,
        <code>equity</code>, <code>income</code>, <code>other_income</code>, <code>expense</code>,
        <code>cost_of_goods_sold</code>, <code>other_expense</code>, <code>accounts_receivable</code>,
        <code>accounts_payable</code> and more.'
      type: string
      example: long_term_liability
    uncategorized_transactions:
      description: Number of uncategorized transactions
      type: integer
      example: 0
    total_unprinted_checks:
      description: Number of unprinted checks.
      type: integer
      example: 0
    bank_name:
      description: Name of the Bank
      type: string
      example: Xavier Bank
    bank_balance:
      description: Balance present in the Bank
      type: integer
      example: 0
    account_number:
      description: Number associated with the Bank Account
      type: string
      example: '80000009823'
    routing_number:
      description: Routing Number of the Account
      type: string
      example: '123456789'
    is_primary_account:
      description: Check if the Account is Primary Account in Zoho Books
      type: boolean
      example: false
    is_paypal_account:
      description: Check if the Account is Paypal Account
      type: boolean
      example: true
    paypal_type:
      description: 'The type of Payment for the Paypal Account. Allowed Values : <code>standard</code>
        and <code>adaptive</code>'
      type: string
    paypal_email_address:
      description: Email Address of the Paypal account.
      type: string
      example: johnsmith@zilliuminc.com
    statement_id:
      description: ID of the Statement Imported
      type: string
      example: '460000000049013'
    from_date:
      description: Returns the exchange rate details from the given date or from previous
        closest match in the absence of the exchange rate on the given date.
      type: string
      example: '2017-01-02'
    to_date:
      description: Date up to which the time entries logged to be fetched
      type: string
      example: '2013-09-17'
    source:
      description: Source of the transaction
      type: string
      example: manually_added
    debit_or_credit:
      description: 'Debit or Credit for which the amount needs to be recorded. Allowed
        Values: <code>debit</code> and <code>credit</code>.'
      type: string
      example: debit
    bankaccounts:
      type: array
      items:
        type: object
        properties:
          account_id:
            $ref: '#/components/schemas/account_id'
          account_name:
            $ref: '#/components/schemas/account_name'
          account_code:
            $ref: '#/components/schemas/account_code'
          currency_id:
            $ref: '#/components/schemas/currency_id'
          currency_code:
            $ref: '#/components/schemas/currency_code'
          account_type:
            $ref: '#/components/schemas/account_type'
          uncategorized_transactions:
            $ref: '#/components/schemas/uncategorized_transactions'
          total_unprinted_checks:
            $ref: '#/components/schemas/total_unprinted_checks'
          is_active:
            $ref: '#/components/schemas/is_active'
          balance:
            $ref: '#/components/schemas/balance'
          bank_balance:
            $ref: '#/components/schemas/bank_balance'
          bcy_balance:
            $ref: '#/components/schemas/bcy_balance'
          bank_name:
            $ref: '#/components/schemas/bank_name'
    is_feeds_subscribed:
      description: Check is feeds are subscribed
      type: boolean
      example: false
    is_feeds_active:
      description: Check if feeds are active
      type: boolean
      example: false
    refresh_status_code:
      description: Refresh Status Code of the Bank
      type: string
    feeds_last_refresh_date:
      description: Last Refreshed Date of the Feeds
      type: string
    service_id:
      description: Service ID of the Account
      type: string
    is_system_account:
      description: Check if it is an System Account
      type: boolean
      example: true
    is_show_warning_for_feeds_refresh:
      description: Check if warning should be shown for refreshing Bank Feeds
      type: boolean
      example: false
    bankaccount:
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        account_code:
          $ref: '#/components/schemas/account_code'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        account_type:
          $ref: '#/components/schemas/account_type'
        account_number:
          $ref: '#/components/schemas/account_number'
        uncategorized_transactions:
          $ref: '#/components/schemas/uncategorized_transactions'
        total_unprinted_checks:
          $ref: '#/components/schemas/total_unprinted_checks'
        is_active:
          $ref: '#/components/schemas/is_active'
        is_feeds_subscribed:
          $ref: '#/components/schemas/is_feeds_subscribed'
        is_feeds_active:
          $ref: '#/components/schemas/is_feeds_active'
        balance:
          $ref: '#/components/schemas/balance'
        bank_balance:
          $ref: '#/components/schemas/bank_balance'
        bcy_balance:
          $ref: '#/components/schemas/bcy_balance'
        bank_name:
          $ref: '#/components/schemas/bank_name'
        routing_number:
          $ref: '#/components/schemas/routing_number'
        is_primary_account:
          $ref: '#/components/schemas/is_primary_account'
        is_paypal_account:
          $ref: '#/components/schemas/is_paypal_account'
        description:
          $ref: '#/components/schemas/description'
        refresh_status_code:
          $ref: '#/components/schemas/refresh_status_code'
        feeds_last_refresh_date:
          $ref: '#/components/schemas/feeds_last_refresh_date'
        service_id:
          $ref: '#/components/schemas/service_id'
        is_system_account:
          $ref: '#/components/schemas/is_system_account'
        is_show_warning_for_feeds_refresh:
          $ref: '#/components/schemas/is_show_warning_for_feeds_refresh'
    transactions:
      type: array
      description: List of transactions imported through the statement
      items:
        type: object
        properties:
          transaction_id:
            $ref: '#/components/schemas/transaction_id'
          debit_or_credit:
            $ref: '#/components/schemas/debit_or_credit'
          date:
            $ref: '#/components/schemas/date'
          customer_id:
            $ref: '#/components/schemas/customer_id'
          payee:
            $ref: '#/components/schemas/payee'
          reference_number:
            $ref: '#/components/schemas/reference_number'
          transaction_type:
            $ref: '#/components/schemas/transaction_type'
          amount:
            $ref: '#/components/schemas/amount'
          status:
            $ref: '#/components/schemas/status'
    statement:
      type: object
      properties:
        statement_id:
          $ref: '#/components/schemas/statement_id'
        from_date:
          $ref: '#/components/schemas/from_date'
        to_date:
          $ref: '#/components/schemas/to_date'
        source:
          $ref: '#/components/schemas/source'
        transactions:
          $ref: '#/components/schemas/transactions'
    create-a-bank-account-request:
      required:
      - account_name
      - account_type
      type: object
      properties:
        account_name:
          $ref: '#/components/schemas/account_name'
        account_type:
          $ref: '#/components/schemas/account_type'
        account_number:
          $ref: '#/components/schemas/account_number'
        account_code:
          $ref: '#/components/schemas/account_code'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        description:
          $ref: '#/components/schemas/description'
        bank_name:
          $ref: '#/components/schemas/bank_name'
        routing_number:
          $ref: '#/components/schemas/routing_number'
        is_primary_account:
          $ref: '#/components/schemas/is_primary_account'
        is_paypal_account:
          $ref: '#/components/schemas/is_paypal_account'
        paypal_type:
          $ref: '#/components/schemas/paypal_type'
        paypal_email_address:
          $ref: '#/components/schemas/paypal_email_address'
    create-a-bank-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been created.
          readOnly: true
        bankaccount:
          $ref: '#/components/schemas/bankaccount'
    list-view-of-accounts-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        bankaccounts:
          type: array
          items:
            type: object
            properties:
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              account_code:
                $ref: '#/components/schemas/account_code'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              account_type:
                $ref: '#/components/schemas/account_type'
              account_number:
                $ref: '#/components/schemas/account_number'
              uncategorized_transactions:
                $ref: '#/components/schemas/uncategorized_transactions'
              total_unprinted_checks:
                $ref: '#/components/schemas/total_unprinted_checks'
              is_active:
                $ref: '#/components/schemas/is_active'
              balance:
                $ref: '#/components/schemas/balance'
              bank_balance:
                $ref: '#/components/schemas/bank_balance'
              bcy_balance:
                $ref: '#/components/schemas/bcy_balance'
              bank_name:
                $ref: '#/components/schemas/bank_name'
              routing_number:
                $ref: '#/components/schemas/routing_number'
              is_primary_account:
                $ref: '#/components/schemas/is_primary_account'
              is_paypal_account:
                $ref: '#/components/schemas/is_paypal_account'
    update-bank-account-request:
      required:
      - account_name
      - account_type
      type: object
      properties:
        account_name:
          $ref: '#/components/schemas/account_name'
        account_type:
          $ref: '#/components/schemas/account_type'
        account_number:
          $ref: '#/components/schemas/account_number'
        account_code:
          $ref: '#/components/schemas/account_code'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        description:
          $ref: '#/components/schemas/description'
        bank_name:
          $ref: '#/components/schemas/bank_name'
        routing_number:
          $ref: '#/components/schemas/routing_number'
        is_primary_account:
          $ref: '#/components/schemas/is_primary_account'
        is_paypal_account:
          $ref: '#/components/schemas/is_paypal_account'
        paypal_type:
          $ref: '#/components/schemas/paypal_type'
        paypal_email_address:
          $ref: '#/components/schemas/paypal_email_address'
    update-bank-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The details of the account has been updated.
          readOnly: true
        bankaccount:
          $ref: '#/components/schemas/bankaccount'
    get-account-details-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        bankaccount:
          $ref: '#/components/schemas/bankaccount'
    delete-an-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been deleted.
          readOnly: true
    deactivate-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been marked as inactive.
          readOnly: true
    activate-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been marked as active.
          readOnly: true
    import-a-bankcredit-card-statement-request:
      required:
      - account_id
      - transactions
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        start_date:
          $ref: '#/components/schemas/start_date'
        end_date:
          $ref: '#/components/schemas/end_date'
        transactions:
          type: array
          description: List of bank statement transactions to be imported.
          items:
            type: object
            required:
            - date
            - debit_or_credit
            - amount
            properties:
              transaction_id:
                description: Unique ID that is specific to the transaction
                type: string
                example: XXXXSCD01
              date:
                $ref: '#/components/schemas/date'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              amount:
                $ref: '#/components/schemas/amount'
              payee:
                $ref: '#/components/schemas/payee'
              description:
                description: Transaction description
                type: string
                example: Electronics purchase
              reference_number:
                $ref: '#/components/schemas/reference_number'
    import-a-bankcredit-card-statement-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your bank statement has been imported.
          readOnly: true
    get-last-imported-statement-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        statement:
          type: array
          items:
            type: object
            properties:
              statement_id:
                $ref: '#/components/schemas/statement_id'
              from_date:
                $ref: '#/components/schemas/from_date'
              to_date:
                $ref: '#/components/schemas/to_date'
              source:
                $ref: '#/components/schemas/source'
              transactions:
                $ref: '#/components/schemas/transactions'
    delete-last-imported-statement-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: You have successfully deleted the last imported statement.
          readOnly: true
    offset_account_name:
      description: Name of the Offset Account
      type: string
    is_offsetaccount_matched:
      description: Check if Offset Account is matched
      type: boolean
      example: false
    is_rule_exist:
      description: Check if rule exists for the transaction
      type: boolean
      example: false
    rule_details:
      description: Details of the Rule
      type: array
      items:
        type: string
    from_account_name:
      description: The account Name from which money will be transferred
      type: string
      example: Sales
    contact:
      description: Contact person name, involved in the transaction.
      type: string
    transaction_number:
      description: Number of the transaction
      type: string
      example: INV-000007
    is_best_match:
      description: Check if the transaction is the best match
      type: boolean
      example: true
    matching_documents:
      type: array
      items:
        type: object
        properties:
          document_id:
            $ref: '#/components/schemas/document_id'
          file_name:
            $ref: '#/components/schemas/file_name'
          file_type:
            $ref: '#/components/schemas/file_type'
          file_size:
            $ref: '#/components/schemas/file_size'
          file_size_formatted:
            $ref: '#/components/schemas/file_size_formatted'
          scanned_amount:
            $ref: '#/components/schemas/scanned_amount'
          vendor_name:
            $ref: '#/components/schemas/vendor_name'
          scanned_receipt_date:
            $ref: '#/components/schemas/scanned_receipt_date'
          entity_number:
            $ref: '#/components/schemas/entity_number'
          relevance:
            $ref: '#/components/schemas/relevance'
    file_type:
      description: Type of the file attached to the task.
      type: string
      example: pdf
    file_size:
      description: Size of the file attached to the task in bytes.
      type: integer
      example: 204800
    file_size_formatted:
      description: Formatted size of the file attached to the task.
      type: string
      example: 200 KB
    entity_number:
      type: integer
    relevance:
      description: Says how relevant is the document to the transaction.
      type: string
      example: best
    scanned_amount:
      description: Amount scanned from the file.
      type: string
    scanned_receipt_date:
      description: Receipt date that is file from the document.
      type: string
    transactions_to_be_matched:
      description: List of transactions to be matched with the uncategorized transaction.
      type: array
      items:
        type: object
        properties:
          transaction_id:
            $ref: '#/components/schemas/transaction_id'
          transaction_type:
            $ref: '#/components/schemas/transaction_type'
    banktransaction:
      type: object
      properties:
        transaction_id:
          $ref: '#/components/schemas/transaction_id'
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        from_account_name:
          $ref: '#/components/schemas/from_account_name'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        to_account_name:
          $ref: '#/components/schemas/to_account_name'
        transaction_type:
          $ref: '#/components/schemas/transaction_type'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        date:
          $ref: '#/components/schemas/date'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vendor_name:
          $ref: '#/components/schemas/vendor_name'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        documents:
          $ref: '#/components/schemas/documents'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        tax_amount:
          $ref: '#/components/schemas/tax_amount'
        sub_total:
          $ref: '#/components/schemas/sub_total'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        total:
          $ref: '#/components/schemas/total'
        bcy_total:
          $ref: '#/components/schemas/bcy_total'
        amount:
          $ref: '#/components/schemas/amount'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        product_type:
          $ref: '#/components/schemas/product_type'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        acquisition_vat_name:
          $ref: '#/components/schemas/acquisition_vat_name'
        acquisition_vat_percentage:
          $ref: '#/components/schemas/acquisition_vat_percentage'
        acquisition_vat_amount:
          $ref: '#/components/schemas/acquisition_vat_amount'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        reverse_charge_vat_name:
          $ref: '#/components/schemas/reverse_charge_vat_name'
        reverse_charge_vat_percentage:
          $ref: '#/components/schemas/reverse_charge_vat_percentage'
        reverse_charge_vat_amount:
          $ref: '#/components/schemas/reverse_charge_vat_amount'
        reverse_charge_tax_id:
          $ref: '#/components/schemas/reverse_charge_tax_id'
        filed_in_vat_return_id:
          $ref: '#/components/schemas/filed_in_vat_return_id'
        filed_in_vat_return_name:
          $ref: '#/components/schemas/filed_in_vat_return_name'
        filed_in_vat_return_type:
          $ref: '#/components/schemas/filed_in_vat_return_type'
        imported_transactions:
          $ref: '#/components/schemas/imported_transactions'
        tags:
          $ref: '#/components/schemas/tags_response'
        line_items:
          $ref: '#/components/schemas/line_items'
    transaction_status:
      description: Status of the Transaction
      type: string
    user_id:
      description: User ID
      type: string
      example: '460000000036868'
    date_after:
      description: Date after which Transactions are to be filtered
      type: string
    date_before:
      description: Date before which Transactions are to be filtered
      type: string
    amount_start:
      description: Starting amout with which transactions are to be filtered
      type: number
      format: double
    amount_end:
      description: Starting amout with which transactions are to be filtered
      type: number
      format: double
    show_all_transactions:
      description: Check if all transactions must be shown
      type: boolean
    line_id:
      description: ID of the Line
      type: string
      example: '460000000038005'
    vehicle_id:
      description: ID of the Vehicle
      type: string
    zp_project_id:
      description: ID of the Project
      type: string
    zp_project_name:
      description: Name of the Project
      type: string
    zp_client_id:
      description: ID of client
      type: string
    create-a-transaction-for-an-account-request:
      required:
      - transaction_type
      type: object
      properties:
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        transaction_type:
          $ref: '#/components/schemas/transaction_type'
        amount:
          $ref: '#/components/schemas/amount'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        date:
          $ref: '#/components/schemas/date'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        tags:
          $ref: '#/components/schemas/tags'
        from_account_tags:
          $ref: '#/components/schemas/tags'
        to_account_tags:
          $ref: '#/components/schemas/tags'
        documents:
          $ref: '#/components/schemas/documents'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        user_id:
          $ref: '#/components/schemas/user_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    create-a-transaction-for-an-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bank transaction has been recorded.
          readOnly: true
        banktransaction:
          $ref: '#/components/schemas/banktransaction'
    get-transactions-list-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        banktransactions:
          type: array
          items:
            type: object
            properties:
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              date:
                $ref: '#/components/schemas/date'
              amount:
                $ref: '#/components/schemas/amount'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
              status:
                $ref: '#/components/schemas/status'
              source:
                $ref: '#/components/schemas/source'
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              account_type:
                $ref: '#/components/schemas/account_type'
              price_precision:
                $ref: '#/components/schemas/price_precision'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              payee:
                $ref: '#/components/schemas/payee'
              is_paid_via_print_check:
                $ref: '#/components/schemas/is_paid_via_print_check'
              description:
                $ref: '#/components/schemas/description'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              currency_symbol:
                $ref: '#/components/schemas/currency_symbol'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              offset_account_name:
                $ref: '#/components/schemas/offset_account_name'
              is_offsetaccount_matched:
                $ref: '#/components/schemas/is_offsetaccount_matched'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              imported_transaction_id:
                $ref: '#/components/schemas/imported_transaction_id'
              is_rule_exist:
                $ref: '#/components/schemas/is_rule_exist'
              rule_details:
                $ref: '#/components/schemas/rule_details'
    update-a-transaction-request:
      required:
      - transaction_type
      type: object
      properties:
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        transaction_type:
          $ref: '#/components/schemas/transaction_type'
        amount:
          $ref: '#/components/schemas/amount'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        date:
          $ref: '#/components/schemas/date'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        tags:
          $ref: '#/components/schemas/tags'
        from_account_tags:
          $ref: '#/components/schemas/tags'
        to_account_tags:
          $ref: '#/components/schemas/tags'
        documents:
          $ref: '#/components/schemas/documents'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        user_id:
          $ref: '#/components/schemas/user_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        line_items:
          type: array
          description: List of expense line items associated with the transaction.
          items:
            type: object
            properties:
              line_id:
                $ref: '#/components/schemas/line_id'
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              description:
                $ref: '#/components/schemas/description'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              item_total:
                $ref: '#/components/schemas/item_total'
              item_total_inclusive_of_tax:
                $ref: '#/components/schemas/item_total_inclusive_of_tax'
              item_order:
                $ref: '#/components/schemas/item_order'
              tags:
                $ref: '#/components/schemas/tags'
    update-a-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bank transaction has been updated.
          readOnly: true
        banktransaction:
          $ref: '#/components/schemas/banktransaction'
    get-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        banktransaction:
          $ref: '#/components/schemas/banktransaction'
    delete-a-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction has been deleted.
          readOnly: true
    get-matching-transactions-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        matching_transactions:
          type: array
          items:
            type: object
            properties:
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              date:
                $ref: '#/components/schemas/date'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              amount:
                $ref: '#/components/schemas/amount'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              transaction_number:
                $ref: '#/components/schemas/transaction_number'
              is_paid_via_print_check:
                $ref: '#/components/schemas/is_paid_via_print_check'
              contact_name:
                $ref: '#/components/schemas/contact_name'
              is_best_match:
                $ref: '#/components/schemas/is_best_match'
    match-a-transaction-request:
      type: object
      properties:
        transactions_to_be_matched:
          $ref: '#/components/schemas/transactions_to_be_matched'
    match-a-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction has been matched.
          readOnly: true
    unmatch-a-matched-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction has been unmatched.
          readOnly: true
    exclude-a-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction has been excluded.
          readOnly: true
    restore-a-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The excluded transaction(s) have been restored.
          readOnly: true
    categorize-an-uncategorized-transaction-request:
      required:
      - transaction_type
      type: object
      properties:
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        transaction_type:
          $ref: '#/components/schemas/transaction_type'
        amount:
          $ref: '#/components/schemas/amount'
        date:
          $ref: '#/components/schemas/date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        description:
          $ref: '#/components/schemas/description'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        tags:
          $ref: '#/components/schemas/tags'
        documents:
          $ref: '#/components/schemas/documents'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        to_account_tags:
          $ref: '#/components/schemas/tags'
        from_account_tags:
          $ref: '#/components/schemas/tags'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        user_id:
          $ref: '#/components/schemas/user_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        line_items:
          type: array
          description: List of expense line items associated with the transaction.
          items:
            type: object
            properties:
              line_id:
                $ref: '#/components/schemas/line_id'
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              description:
                $ref: '#/components/schemas/description'
              tax_amount:
                $ref: '#/components/schemas/tax_amount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              item_total:
                $ref: '#/components/schemas/item_total'
              item_total_inclusive_of_tax:
                $ref: '#/components/schemas/item_total_inclusive_of_tax'
              item_order:
                $ref: '#/components/schemas/item_order'
              tags:
                $ref: '#/components/schemas/tags'
    categorize-an-uncategorized-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    categorize-as-expense-request:
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        date:
          $ref: '#/components/schemas/date'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        amount:
          $ref: '#/components/schemas/amount'
        project_id:
          $ref: '#/components/schemas/project_id'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        is_inclusive_tax:
          $ref: '#/components/schemas/is_inclusive_tax'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        description:
          $ref: '#/components/schemas/description'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        zp_project_id:
          $ref: '#/components/schemas/zp_project_id'
        zp_project_name:
          $ref: '#/components/schemas/zp_project_name'
        zp_client_id:
          $ref: '#/components/schemas/zp_client_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        vehicle_id:
          $ref: '#/components/schemas/vehicle_id'
        mileage_unit:
          $ref: '#/components/schemas/mileage_unit'
        mileage_rate:
          $ref: '#/components/schemas/mileage_rate'
        can_reclaim_vat_on_mileage:
          $ref: '#/components/schemas/can_reclaim_vat_on_mileage'
        fuel_type:
          $ref: '#/components/schemas/fuel_type'
        engine_capacity_range:
          $ref: '#/components/schemas/engine_capacity_range'
        employee_id:
          $ref: '#/components/schemas/employee_id'
        mileage_type:
          $ref: '#/components/schemas/mileage_type'
        expense_type:
          $ref: '#/components/schemas/expense_type'
        distance:
          $ref: '#/components/schemas/distance'
        start_reading:
          $ref: '#/components/schemas/start_reading'
        end_reading:
          $ref: '#/components/schemas/end_reading'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        custom_fields:
          type: array
          description: List of custom fields associated with the transaction.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        tags:
          $ref: '#/components/schemas/tags'
        documents:
          $ref: '#/components/schemas/documents'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        recurring_expense_id:
          $ref: '#/components/schemas/recurring_expense_id'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        acquisition_vat_id:
          $ref: '#/components/schemas/acquisition_vat_id'
        reverse_charge_vat_id:
          $ref: '#/components/schemas/reverse_charge_vat_id'
        is_update_customer:
          $ref: '#/components/schemas/is_update_customer'
        product_type:
          $ref: '#/components/schemas/product_type'
        taxes:
          $ref: '#/components/schemas/taxes'
        reason:
          $ref: '#/components/schemas/reason'
        line_items:
          type: array
          description: List of expense line items associated with the transaction.
          items:
            type: object
            properties:
              line_item_id:
                $ref: '#/components/schemas/line_item_id'
              account_id:
                $ref: '#/components/schemas/account_id'
              description:
                $ref: '#/components/schemas/description'
              amount:
                $ref: '#/components/schemas/amount'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              item_order:
                $ref: '#/components/schemas/item_order'
              product_type:
                $ref: '#/components/schemas/product_type'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tags:
                $ref: '#/components/schemas/tags'
        zcrm_potential_id:
          $ref: '#/components/schemas/zcrm_potential_id'
    categorize-as-expense-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    uncategorize-a-categorized-transaction-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Transaction(s) have been uncategorized.
          readOnly: true
    categorize-a-vendor-payment-request:
      type: object
      properties:
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        bills:
          $ref: '#/components/schemas/bills'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        description:
          $ref: '#/components/schemas/description'
        date:
          $ref: '#/components/schemas/date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        paid_through_account_id:
          $ref: '#/components/schemas/paid_through_account_id'
        amount:
          $ref: '#/components/schemas/amount'
        custom_fields:
          type: array
          description: List of custom fields associated with the transaction.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        is_paid_via_print_check:
          $ref: '#/components/schemas/is_paid_via_print_check'
        check_details:
          $ref: '#/components/schemas/check_details'
    categorize-a-vendor-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    categorize-as-customer-payment-request:
      type: object
      properties:
        customer_id:
          $ref: '#/components/schemas/customer_id'
        retainerinvoice_id:
          $ref: '#/components/schemas/retainerinvoice_id'
        invoices:
          $ref: '#/components/schemas/invoices'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        description:
          $ref: '#/components/schemas/description'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        amount:
          $ref: '#/components/schemas/amount'
        bank_charges:
          $ref: '#/components/schemas/bank_charges'
        account_id:
          $ref: '#/components/schemas/account_id'
        custom_fields:
          type: array
          description: List of custom fields associated with the transaction.
          items:
            type: object
            properties:
              index:
                $ref: '#/components/schemas/index'
              value:
                $ref: '#/components/schemas/value'
        documents:
          $ref: '#/components/schemas/documents'
        date:
          $ref: '#/components/schemas/date'
        template_id:
          $ref: '#/components/schemas/template_id'
        contact_persons:
          $ref: '#/components/schemas/contact_persons'
    categorize-as-customer-payment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    categorize-as-credit-note-refunds-request:
      required:
      - creditnote_id
      - date
      type: object
      properties:
        creditnote_id:
          $ref: '#/components/schemas/creditnote_id'
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        description:
          $ref: '#/components/schemas/description'
    categorize-as-credit-note-refunds-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    categorize-as-vendor-credit-refunds-request:
      required:
      - vendor_credit_id
      - date
      type: object
      properties:
        vendor_credit_id:
          $ref: '#/components/schemas/vendor_credit_id'
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        account_id:
          $ref: '#/components/schemas/account_id'
        description:
          $ref: '#/components/schemas/description'
    categorize-as-vendor-credit-refunds-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    categorize-as-customer-payment-refund-request:
      required:
      - date
      - amount
      - from_account_id
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        from_account_id:
          $ref: '#/components/schemas/from_account_id'
        description:
          $ref: '#/components/schemas/description'
    categorize-as-customer-payment-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    categorize-as-vendor-payment-refund-request:
      required:
      - date
      - amount
      - to_account_id
      type: object
      properties:
        vendorpayment_id:
          $ref: '#/components/schemas/vendorpayment_id'
        date:
          $ref: '#/components/schemas/date'
        refund_mode:
          $ref: '#/components/schemas/refund_mode'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        amount:
          $ref: '#/components/schemas/amount'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        to_account_id:
          $ref: '#/components/schemas/to_account_id'
        description:
          $ref: '#/components/schemas/description'
    categorize-as-vendor-payment-refund-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The transaction(s) have been categorized.
          readOnly: true
    rules-response:
      type: object
      properties:
        rule_id:
          $ref: '#/components/schemas/rule_id'
        rule_name:
          $ref: '#/components/schemas/rule_name'
        rule_order:
          $ref: '#/components/schemas/rule_order'
        apply_to:
          $ref: '#/components/schemas/apply_to'
        criteria_type:
          $ref: '#/components/schemas/criteria_type'
        record_as:
          $ref: '#/components/schemas/record_as'
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        criterion:
          $ref: '#/components/schemas/criterion'
    rule_id:
      description: ID of the Rule
      type: string
      example: '460000000048005'
    rule_name:
      description: Name of the Rule
      type: string
      example: Minimum Deposit Rule
    rule_order:
      description: Order of the rule
      type: integer
      example: 0
    apply_to:
      description: Entities to which Rule must be applied
      type: string
      example: deposits
    criteria_type:
      description: Type of Criteria
      type: string
      example: and
    record_as:
      description: Entity as which it should be recorded
      type: string
      example: deposit
    criteria_id:
      description: ID of the Criteria
      type: string
      example: '460000000048009'
    field:
      description: Field involved in the Criteria
      type: string
      example: amount
    comparator:
      description: Comparator used in Criteria
      type: string
      example: greater_than_or_equals
    criterion:
      type: array
      description: List of criteria based on which the rule will be applied.
      items:
        type: object
        properties:
          criteria_id:
            $ref: '#/components/schemas/criteria_id'
          field:
            $ref: '#/components/schemas/field'
          comparator:
            $ref: '#/components/schemas/comparator'
          value:
            $ref: '#/components/schemas/value'
    rule:
      type: object
      properties:
        rule_id:
          $ref: '#/components/schemas/rule_id'
        rule_name:
          $ref: '#/components/schemas/rule_name'
        rule_order:
          $ref: '#/components/schemas/rule_order'
        apply_to:
          $ref: '#/components/schemas/apply_to'
        criteria_type:
          $ref: '#/components/schemas/criteria_type'
        criterion:
          $ref: '#/components/schemas/criterion'
        record_as:
          $ref: '#/components/schemas/record_as'
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        product_type:
          $ref: '#/components/schemas/product_type'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
    target_account_id:
      description: The account on which the rule has to be applied.
      type: string
      example: 460000000048001
    get-rules-list-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        rules:
          type: array
          items:
            $ref: '#/components/schemas/rules-response'
    create-a-rule-request:
      required:
      - rule_name
      - target_account_id
      - apply_to
      - criteria_type
      - criterion
      - record_as
      type: object
      properties:
        rule_name:
          $ref: '#/components/schemas/rule_name'
        target_account_id:
          $ref: '#/components/schemas/target_account_id'
        apply_to:
          description: 'Rule applies to either deposits or withdrawals for bank accounts
            and to refunds or charges for credit card account. Allowed Values : <code>withdrawals</code>,
            <code>deposits</code>, <code>refunds</code> and <code>charges</code>.'
          type: string
          example: deposits
        criteria_type:
          description: 'Specifies whether all the criteria have to be satisfied or
            not. Allowed Values : <code>and</code> and <code>or</code>'
          type: string
          example: and
        criterion:
          type: array
          description: List of criteria used to define conditions for applying the
            bank rule.
          items:
            type: object
            properties:
              field:
                $ref: '#/components/schemas/field'
              comparator:
                $ref: '#/components/schemas/comparator'
              value:
                $ref: '#/components/schemas/value'
        record_as:
          description: 'Record transaction based on value specified in apply_to node.
            For bank accounts: If apply_to is deposits: sales_without_invoices, transfer_fund,
            interest_income, other_income, expense_refund, deposit. If apply_to is
            withdrawals: expense, transfer_fund, card_payment, owner_drawings. For
            credit_card accounts: If apply_to is refunds: card_payment, transfer_fund,
            expense_refund, refund. If apply_to is charges: expense, transfer_fund.
            Allowed Values: <code>expense</code>, <code>deposit</code>, <code>refund</code>,
            <code>transfer_fund</code>, <code>card_payment</code>, <code>sales_without_invoices</code>,
            <code>expense_refund</code>, <code>interest_income</code>, <code>other_income</code>
            and <code>owner_drawings</code>'
          type: string
          example: deposit
        account_id:
          description: Account which is involved in the rule with the target account.
          type: string
          example: 460000000049001
        customer_id:
          description: ID of the customer.(Applicable for sales_without_invoices,deposit,
            expense)
          type: string
          example: 46000000000111
        tax_id:
          $ref: '#/components/schemas/tax_id'
        reference_number:
          description: 'Specifies if Reference number is manual or generated from
            the statement. Allowed Values: <code>manual</code> and <code>from_statement</code>'
          type: string
          example: manual
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        product_type:
          $ref: '#/components/schemas/product_type'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
    create-a-rule-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bank rule has been created.
          readOnly: true
        rule:
          $ref: '#/components/schemas/rule'
    get-a-rule-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        rule_id:
          $ref: '#/components/schemas/rule_id'
        rule_name:
          $ref: '#/components/schemas/rule_name'
        rule_order:
          $ref: '#/components/schemas/rule_order'
        apply_to:
          $ref: '#/components/schemas/apply_to'
        criteria_type:
          $ref: '#/components/schemas/criteria_type'
        criterion:
          $ref: '#/components/schemas/criterion'
        record_as:
          $ref: '#/components/schemas/record_as'
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        payment_mode:
          $ref: '#/components/schemas/payment_mode'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        product_type:
          $ref: '#/components/schemas/product_type'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
    update-a-rule-request:
      required:
      - rule_name
      - target_account_id
      - apply_to
      - criteria_type
      - criterion
      - record_as
      type: object
      properties:
        rule_name:
          $ref: '#/components/schemas/rule_name'
        target_account_id:
          $ref: '#/components/schemas/target_account_id'
        apply_to:
          description: 'Rule applies to either deposits or withdrawals for bank accounts
            and to refunds or charges for credit card account. Allowed Values : <code>withdrawals</code>,
            <code>deposits</code>, <code>refunds</code> and <code>charges</code>.'
          type: string
          example: deposits
        criteria_type:
          description: 'Specifies whether all the criteria have to be satisfied or
            not. Allowed Values : <code>and</code> and <code>or</code>'
          type: string
          example: and
        criterion:
          $ref: '#/components/schemas/criterion'
        record_as:
          description: 'Record transaction based on value specified in apply_to node.
            For bank accounts: If apply_to is deposits: sales_without_invoices, transfer_fund,
            interest_income, other_income, expense_refund, deposit. If apply_to is
            withdrawals: expense, transfer_fund, card_payment, owner_drawings. For
            credit_card accounts: If apply_to is refunds: card_payment, transfer_fund,
            expense_refund, refund. If apply_to is charges: expense, transfer_fund.
            Allowed Values: <code>expense</code>, <code>deposit</code>, <code>refund</code>,
            <code>transfer_fund</code>, <code>card_payment</code>, <code>sales_without_invoices</code>,
            <code>expense_refund</code>, <code>interest_income</code>, <code>other_income</code>
            and <code>owner_drawings</code>'
          type: string
          example: deposit
        account_id:
          description: Account which is involved in the rule with the target account.
          type: string
          example: 460000000049001
        customer_id:
          description: ID of the customer.(Applicable for sales_without_invoices,deposit,
            expense)
          type: string
          example: 46000000000111
        tax_id:
          $ref: '#/components/schemas/tax_id'
        reference_number:
          description: 'Specifies if Reference number is manual or generated from
            the statement. Allowed Values: <code>manual</code> and <code>from_statement</code>'
          type: string
          example: manual
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        tax_treatment:
          $ref: '#/components/schemas/tax_treatment'
        is_reverse_charge_applied:
          $ref: '#/components/schemas/is_reverse_charge_applied'
        product_type:
          $ref: '#/components/schemas/product_type'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
    update-a-rule-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The bank rule has been updated.
          readOnly: true
        rule:
          $ref: '#/components/schemas/rule'
    delete-a-rule-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The rule has been deleted.
          readOnly: true
    can_show_in_ze:
      description: Whether the account can be shown in Zoho Expense.
      type: boolean
      example: false
    parent_account_id:
      description: ID of the Parent Account
      type: string
      example: '460000000009097'
    bank_account_number:
      description: Number of the Bank Account
      type: string
    include_in_vat_return:
      description: Check if Journal should be included in VAT Return
      type: boolean
      x-node_available_in:
      - eu
      - uk
      x-node_unavailable_in: []
    show_on_dashboard:
      description: Whether to show the account balance on the dashboard.
      type: boolean
      example: false
    showbalance:
      description: Boolean to get current balance of accounts.
      type: boolean
    is_user_created:
      description: Check if it is an User Created Account
      type: boolean
      example: true
    is_standalone_account:
      description: Check if it is an Standalone Account
      type: boolean
      example: false
    is_involved_in_transaction:
      description: Check if account is involved in transaction
      type: boolean
      example: false
    parent_account_name:
      description: Name of the Parent Account
      type: string
      example: ' '
    depth:
      description: Depth or how much indentation required of the option in the hierarchy
      type: integer
      example: 1
    is_child_present:
      description: Check if a child account is present for the Account
      type: string
    child_count:
      type: string
    categorized_transaction_id:
      type: string
      example: '460000000052051'
    transaction_date:
      type: string
      example: '2013-10-04'
    entry_number:
      description: Entry Number of the Journal
      type: string
      example: '1'
    reconcile_status:
      type: string
    debit_amount:
      type: string
    closing_balance:
      description: Closing balances of the account
      type: number
      format: double
      example: 0
    credit_amount:
      type: number
      format: double
      example: 25
    chart_of_account:
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        account_code:
          $ref: '#/components/schemas/account_code'
        is_active:
          $ref: '#/components/schemas/is_active'
        account_type:
          $ref: '#/components/schemas/account_type'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        description:
          $ref: '#/components/schemas/description'
        is_system_account:
          $ref: '#/components/schemas/is_system_account'
        is_involved_in_transaction:
          $ref: '#/components/schemas/is_involved_in_transaction'
        can_show_in_ze:
          $ref: '#/components/schemas/can_show_in_ze'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        parent_account_id:
          $ref: '#/components/schemas/parent_account_id'
        documents:
          $ref: '#/components/schemas/documents'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
    current_balance:
      description: Current Balance in the account
    transaction_source:
      description: Source of the transaction
      type: string
    create-an-account-request:
      type: object
      properties:
        account_name:
          $ref: '#/components/schemas/account_name'
        account_code:
          $ref: '#/components/schemas/account_code'
        account_type:
          $ref: '#/components/schemas/account_type'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        description:
          $ref: '#/components/schemas/description'
        show_on_dashboard:
          $ref: '#/components/schemas/show_on_dashboard'
        can_show_in_ze:
          $ref: '#/components/schemas/can_show_in_ze'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        parent_account_id:
          $ref: '#/components/schemas/parent_account_id'
    create-an-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been created.
          readOnly: true
        chart_of_account:
          $ref: '#/components/schemas/chart_of_account'
    list-chart-of-accounts-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        chartofaccounts:
          type: array
          items:
            type: object
            properties:
              account_id:
                $ref: '#/components/schemas/account_id'
              account_name:
                $ref: '#/components/schemas/account_name'
              account_code:
                $ref: '#/components/schemas/account_code'
              account_type:
                $ref: '#/components/schemas/account_type'
              is_user_created:
                $ref: '#/components/schemas/is_user_created'
              is_system_account:
                $ref: '#/components/schemas/is_system_account'
              is_standalone_account:
                $ref: '#/components/schemas/is_standalone_account'
              is_active:
                $ref: '#/components/schemas/is_active'
              can_show_in_ze:
                $ref: '#/components/schemas/can_show_in_ze'
              is_involved_in_transaction:
                $ref: '#/components/schemas/is_involved_in_transaction'
              current_balance:
                $ref: '#/components/schemas/current_balance'
              parent_account_id:
                $ref: '#/components/schemas/parent_account_id'
              parent_account_name:
                $ref: '#/components/schemas/parent_account_name'
              depth:
                $ref: '#/components/schemas/depth'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              is_child_present:
                $ref: '#/components/schemas/is_child_present'
              child_count:
                $ref: '#/components/schemas/child_count'
              documents:
                $ref: '#/components/schemas/documents'
              created_time:
                $ref: '#/components/schemas/created_time'
              last_modified_time:
                $ref: '#/components/schemas/last_modified_time'
    update-an-account-request:
      type: object
      properties:
        account_name:
          $ref: '#/components/schemas/account_name'
        account_code:
          $ref: '#/components/schemas/account_code'
        account_type:
          $ref: '#/components/schemas/account_type'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        description:
          $ref: '#/components/schemas/description'
        show_on_dashboard:
          $ref: '#/components/schemas/show_on_dashboard'
        can_show_in_ze:
          $ref: '#/components/schemas/can_show_in_ze'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        parent_account_id:
          $ref: '#/components/schemas/parent_account_id'
    update-an-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The details of the account have been updated.
          readOnly: true
        chart_of_account:
          $ref: '#/components/schemas/chart_of_account'
    get-an-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        account_id:
          $ref: '#/components/schemas/account_id'
        account_name:
          $ref: '#/components/schemas/account_name'
        account_code:
          $ref: '#/components/schemas/account_code'
        is_active:
          $ref: '#/components/schemas/is_active'
        account_type:
          $ref: '#/components/schemas/account_type'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        description:
          $ref: '#/components/schemas/description'
        is_system_account:
          $ref: '#/components/schemas/is_system_account'
        is_involved_in_transaction:
          $ref: '#/components/schemas/is_involved_in_transaction'
        can_show_in_ze:
          $ref: '#/components/schemas/can_show_in_ze'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        closing_balance:
          $ref: '#/components/schemas/closing_balance'
        parent_account_id:
          $ref: '#/components/schemas/parent_account_id'
        documents:
          $ref: '#/components/schemas/documents'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
    mark-an-account-as-active-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been marked as active.
          readOnly: true
    mark-an-account-as-inactive-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The account has been marked as inactive.
          readOnly: true
    list-of-transactions-for-an-account-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        transactions:
          type: array
          items:
            type: object
            properties:
              categorized_transaction_id:
                $ref: '#/components/schemas/categorized_transaction_id'
              transaction_type:
                $ref: '#/components/schemas/transaction_type'
              transaction_status:
                $ref: '#/components/schemas/transaction_status'
              transaction_source:
                $ref: '#/components/schemas/transaction_source'
              transaction_id:
                $ref: '#/components/schemas/transaction_id'
              transaction_date:
                $ref: '#/components/schemas/transaction_date'
              account_id:
                $ref: '#/components/schemas/account_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              payee:
                $ref: '#/components/schemas/payee'
              description:
                $ref: '#/components/schemas/description'
              entry_number:
                $ref: '#/components/schemas/entry_number'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              offset_account_name:
                $ref: '#/components/schemas/offset_account_name'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              reconcile_status:
                $ref: '#/components/schemas/reconcile_status'
              debit_amount:
                $ref: '#/components/schemas/debit_amount'
              credit_amount:
                $ref: '#/components/schemas/credit_amount'
    journal_date:
      description: Date on which the journal to be recorded.
      type: string
      example: '2013-09-04'
    tax_account:
      description: Account for recording Tax
      type: boolean
    journal_id:
      description: ID of the Journal
      type: string
      example: '460000000038001'
    line_item_total:
      description: Total of the Line Item
      type: number
      format: double
      example: 5000
    journal_type:
      description: 'Type of the Journal. Allowed values: <code> Cash </code> and  <code>
        Both </code>.'
      type: string
      example: both
    tax_exemption_type:
      description: 'Type of the Tax Exemption. Allowed Values : <code>customer</code>
        and <code>item</code>'
      type: string
      x-node_available_in:
      - in
      - us
      - au
      - ca
      - mx
      x-node_unavailable_in: []
    is_bas_adjustment:
      description: Check if Journal is created for BAS Adjustment
      type: boolean
      x-node_available_in:
      - au
      x-node_unavailable_in: []
    entity_type:
      description: Name of the Entity
      type: string
      example: journal
    journal:
      type: object
      properties:
        journal_id:
          $ref: '#/components/schemas/journal_id'
        entry_number:
          $ref: '#/components/schemas/entry_number'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        notes:
          $ref: '#/components/schemas/notes'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        journal_date:
          $ref: '#/components/schemas/journal_date'
        journal_type:
          $ref: '#/components/schemas/journal_type'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        product_type:
          $ref: '#/components/schemas/product_type'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        is_bas_adjustment:
          $ref: '#/components/schemas/is_bas_adjustment'
        line_items:
          $ref: '#/components/schemas/line_items'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        line_item_total:
          $ref: '#/components/schemas/line_item_total'
        total:
          $ref: '#/components/schemas/total'
        bcy_total:
          $ref: '#/components/schemas/bcy_total'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        taxes:
          $ref: '#/components/schemas/taxes'
        created_time:
          $ref: '#/components/schemas/created_time'
        last_modified_time:
          $ref: '#/components/schemas/last_modified_time'
        status:
          $ref: '#/components/schemas/status'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags_response'
    create-a-journal-request:
      required:
      - journal_date
      type: object
      properties:
        journal_date:
          $ref: '#/components/schemas/journal_date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        notes:
          $ref: '#/components/schemas/notes'
        journal_type:
          $ref: '#/components/schemas/journal_type'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        product_type:
          $ref: '#/components/schemas/product_type'
        is_bas_adjustment:
          $ref: '#/components/schemas/is_bas_adjustment'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items for the journal.
          type: array
          items:
            type: object
            required:
            - amount
            - debit_or_credit
            properties:
              account_id:
                $ref: '#/components/schemas/account_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              line_id:
                $ref: '#/components/schemas/line_id'
              description:
                $ref: '#/components/schemas/description'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_authority_id:
                $ref: '#/components/schemas/tax_authority_id'
              tax_exemption_type:
                $ref: '#/components/schemas/tax_exemption_type'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_authority_name:
                $ref: '#/components/schemas/tax_authority_name'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              amount:
                $ref: '#/components/schemas/amount'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              location_id:
                $ref: '#/components/schemas/location_id'
              tags:
                $ref: '#/components/schemas/tags'
              project_id:
                $ref: '#/components/schemas/project_id'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        tax_exemption_type:
          $ref: '#/components/schemas/tax_exemption_type'
        status:
          $ref: '#/components/schemas/status'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    create-a-journal-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The journal entry has been created.
          readOnly: true
        journal:
          $ref: '#/components/schemas/journal'
    get-journal-list-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        journals:
          type: array
          items:
            type: object
            properties:
              journal_id:
                $ref: '#/components/schemas/journal_id'
              journal_date:
                $ref: '#/components/schemas/journal_date'
              entry_number:
                $ref: '#/components/schemas/entry_number'
              reference_number:
                $ref: '#/components/schemas/reference_number'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              notes:
                $ref: '#/components/schemas/notes'
              journal_type:
                $ref: '#/components/schemas/journal_type'
              entity_type:
                $ref: '#/components/schemas/entity_type'
              total:
                $ref: '#/components/schemas/total'
              bcy_total:
                $ref: '#/components/schemas/bcy_total'
              custom_field:
                $ref: '#/components/schemas/custom_field'
              tags:
                $ref: '#/components/schemas/tags_response'
    update-a-journal-request:
      required:
      - journal_date
      type: object
      properties:
        journal_date:
          $ref: '#/components/schemas/journal_date'
        reference_number:
          $ref: '#/components/schemas/reference_number'
        notes:
          $ref: '#/components/schemas/notes'
        journal_type:
          $ref: '#/components/schemas/journal_type'
        vat_treatment:
          $ref: '#/components/schemas/vat_treatment'
        include_in_vat_return:
          $ref: '#/components/schemas/include_in_vat_return'
        product_type:
          $ref: '#/components/schemas/product_type'
        is_bas_adjustment:
          $ref: '#/components/schemas/is_bas_adjustment'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        location_id:
          $ref: '#/components/schemas/location_id'
        line_items:
          description: Line items for the journal.
          type: array
          items:
            type: object
            required:
            - amount
            - debit_or_credit
            properties:
              account_id:
                $ref: '#/components/schemas/account_id'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              line_id:
                $ref: '#/components/schemas/line_id'
              description:
                $ref: '#/components/schemas/description'
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_authority_id:
                $ref: '#/components/schemas/tax_authority_id'
              tax_exemption_type:
                $ref: '#/components/schemas/tax_exemption_type'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              tax_authority_name:
                $ref: '#/components/schemas/tax_authority_name'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              amount:
                $ref: '#/components/schemas/amount'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              acquisition_vat_id:
                $ref: '#/components/schemas/acquisition_vat_id'
              reverse_charge_vat_id:
                $ref: '#/components/schemas/reverse_charge_vat_id'
              tags:
                $ref: '#/components/schemas/tags'
              location_id:
                $ref: '#/components/schemas/location_id'
              project_id:
                $ref: '#/components/schemas/project_id'
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        tax_exemption_type:
          $ref: '#/components/schemas/tax_exemption_type'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        tags:
          $ref: '#/components/schemas/tags'
    update-a-journal-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The journal entry has been updated.
          readOnly: true
        journal:
          $ref: '#/components/schemas/journal'
    get-journal-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        journal:
          $ref: '#/components/schemas/journal'
    delete-a-journal-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected journal entry has been deleted.
          readOnly: true
    mark-a-journal-as-published-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Journal has been published.
          readOnly: true
    add-attachment-to-a-journal-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your file has been successfully attached to the journal.
          readOnly: true
    asset_name:
      description: Enter the name of the fixed asset.
      type: string
      example: Laptop
    fixed_asset_type_id:
      description: Associate the fixed asset with a fixed asset type by specifying
        fixed asset type id.
      type: string
      example: '3640355000000319008'
    asset_account_id:
      description: Enter the account id to track the fixed asset.
      type: string
      example: '3640355000000000367'
    expense_account_id:
      description: Enter the account id to track the expenses associated with the
        asset's depreciation.The available account types are <b>Expense</b>  and <b>Other
        Expense</b>
      type: string
      example: '3640355000000000421'
    depreciation_account_id:
      description: Enter the account id to track the asset's depreciation over time.
      type: string
      example: '3640355000000000367'
    depreciation_method:
      description: 'Enter the depreciation method for your asset.The available methods
        are: <code>declining_method</code> and <code>straight_line</code>. <br/><b>For
        US and GLobal Edition:</b> <code>declining_method</code>, <code>straight_line</code>,
        <code>200_declining_method</code> and <code>150_declining_method</code>'
      type: string
      example: declining_method
    depreciation_frequency:
      description: 'Enter the frequency of depreciation. The available frequencies
        are: <code>yearly</code> and <code>monthly</code>'
      type: string
      example: yearly
    depreciation_percentage:
      description: Enter the depreciation percentage only if the entered depreciation
        method is a declining method. The entered percentage should be within the
        range of 0 to 100
      type: number
      format: double
      example: 12
    total_life:
      description: Enter the total life of the asset (in months)
      type: number
      example: 60
    salvage_value:
      description: Enter the remaining amount of the asset after its useful life or
        the value after it's fully depreciated
      type: string
      format: double
      example: 100
    depreciation_start_date:
      description: Enter the depreciation start date of the asset
      type: string
      example: '2024-12-17'
    asset_cost:
      description: Enter the purchase cost of the asset
      type: number
      format: double
      example: 1000
    computation_type:
      description: 'Enter the computation types of assets. The available types are:
        <code>prorata_basis</code> and <code>no_prorata</code>'
      type: string
      example: prorata_basis
    warranty_expiry_date:
      description: Enter the warranty expiration date of the asset
      type: string
      example: '2027-12-17'
    asset_purchase_date:
      description: Enter the Purchase date of the asset
      type: string
      example: '2024-12-17'
    serial_no:
      description: serial number of the asset
      type: string
      example: SN-0001
    dep_start_value:
      description: Enter the value from which depreciation will be calculated.
      type: number
      format: double
      example: 1000
    fixed_asset_id:
      description: ID of the fixed asset
      type: string
      example: '3640355000000319008'
    asset_number:
      description: Number of the fixed asset
      type: string
      example: FA-00013
    asset_number_prefix:
      description: Prefix of the asset number
      type: string
      example: FA-
    asset_number_suffix:
      description: Suffix of the asset number
      type: integer
      example: 13
    fixed_asset_type_name:
      description: Enter the name of fixed asset type
      type: string
      example: Machines
    current_asset_value:
      description: Current value of the fixed asset
      type: number
      example: 1000
    accumulated_dep_value:
      description: Accumulated depreciation value of the fixed asset
      type: number
      example: 0
    last_depreciation_date:
      description: Last depreciation date of the fixed asset
      type: string
      example: ''
    asset_account_name:
      description: Name of the asset account
      type: string
      example: Furniture and Equipment
    expense_account_name:
      description: Name of the expense account
      type: string
      example: Telephone Expense
    depreciation_account_name:
      description: Name of the depreciation account
      type: string
      example: Furniture and Equipment
    associated_transactions:
      description: Associated transactions of the fixed asset
      type: array
      items:
        type: object
    valuation_id:
      description: ID of the valuation
      type: string
      example: '3640355000000319008'
    valuation_date:
      description: Date of the valuation
      type: string
      example: '2024-06-30'
    depreciated_value:
      description: Depreciated value of the fixed asset
      type: number
      example: 91.65
    current_value:
      description: Current value of the fixed asset
      type: number
      example: 824.78
    accumulated_value:
      description: Accumulated value of the fixed asset
      type: number
      example: 175.22
    is_journal_posted:
      description: Whether the journal is posted or not
      type: boolean
      example: true
    fixed_asset:
      type: object
      properties:
        fixed_asset_id:
          $ref: '#/components/schemas/fixed_asset_id'
        asset_name:
          $ref: '#/components/schemas/asset_name'
        status:
          $ref: '#/components/schemas/status'
        asset_number:
          $ref: '#/components/schemas/asset_number'
        asset_number_prefix:
          $ref: '#/components/schemas/asset_number_prefix'
        asset_number_suffix:
          $ref: '#/components/schemas/asset_number_suffix'
        description:
          $ref: '#/components/schemas/description'
        total_life:
          $ref: '#/components/schemas/total_life'
        fixed_asset_type_id:
          $ref: '#/components/schemas/fixed_asset_type_id'
        fixed_asset_type_name:
          $ref: '#/components/schemas/fixed_asset_type_name'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        asset_cost:
          $ref: '#/components/schemas/asset_cost'
        dep_start_value:
          $ref: '#/components/schemas/dep_start_value'
        salvage_value:
          $ref: '#/components/schemas/salvage_value'
        asset_purchase_date:
          $ref: '#/components/schemas/asset_purchase_date'
        current_asset_value:
          $ref: '#/components/schemas/current_asset_value'
        accumulated_dep_value:
          $ref: '#/components/schemas/accumulated_dep_value'
        serial_no:
          $ref: '#/components/schemas/serial_no'
        depreciation_start_date:
          $ref: '#/components/schemas/depreciation_start_date'
        last_depreciation_date:
          $ref: '#/components/schemas/last_depreciation_date'
        asset_account_id:
          $ref: '#/components/schemas/asset_account_id'
        asset_account_name:
          $ref: '#/components/schemas/asset_account_name'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        expense_account_name:
          $ref: '#/components/schemas/expense_account_name'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_account_name:
          $ref: '#/components/schemas/depreciation_account_name'
        computation_type:
          $ref: '#/components/schemas/computation_type'
        warranty_expiry_date:
          $ref: '#/components/schemas/warranty_expiry_date'
        notes:
          $ref: '#/components/schemas/notes'
        tags:
          $ref: '#/components/schemas/tags_response'
        associated_transactions:
          $ref: '#/components/schemas/associated_transactions'
        comments:
          $ref: '#/components/schemas/comments'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    asset_history:
      type: array
      items:
        type: object
        properties:
          valuation_id:
            $ref: '#/components/schemas/valuation_id'
          description:
            $ref: '#/components/schemas/description'
          valuation_date:
            $ref: '#/components/schemas/valuation_date'
          depreciated_value:
            $ref: '#/components/schemas/depreciated_value'
          current_value:
            $ref: '#/components/schemas/current_value'
          accumulated_value:
            $ref: '#/components/schemas/accumulated_value'
          type:
            $ref: '#/components/schemas/type'
          is_journal_posted:
            $ref: '#/components/schemas/is_journal_posted'
    asset_forcast:
      type: array
      items:
        type: object
        properties:
          valuation_date:
            $ref: '#/components/schemas/valuation_date'
          depreciated_value:
            $ref: '#/components/schemas/depreciated_value'
          current_value:
            $ref: '#/components/schemas/current_value'
          accumulated_value:
            $ref: '#/components/schemas/accumulated_value'
    fixed_asset_type_param:
      type: object
      properties:
        fixed_asset_type_name:
          $ref: '#/components/schemas/fixed_asset_type_name'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        total_life:
          $ref: '#/components/schemas/total_life'
        computation_type:
          $ref: '#/components/schemas/computation_type'
    fixed_asset_type:
      type: object
      properties:
        fixed_asset_type_id:
          $ref: '#/components/schemas/fixed_asset_type_id'
        fixed_asset_type_name:
          $ref: '#/components/schemas/fixed_asset_type_name'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        expense_account_name:
          $ref: '#/components/schemas/expense_account_name'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_account_name:
          $ref: '#/components/schemas/depreciation_account_name'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        total_life:
          $ref: '#/components/schemas/total_life'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        computation_type:
          $ref: '#/components/schemas/computation_type'
    create-a-fixed-asset-request:
      type: object
      required:
      - asset_name
      - fixed_asset_type_id
      - asset_account_id
      - asset_cost
      - asset_purchase_date
      properties:
        asset_name:
          $ref: '#/components/schemas/asset_name'
        fixed_asset_type_id:
          $ref: '#/components/schemas/fixed_asset_type_id'
        asset_account_id:
          $ref: '#/components/schemas/asset_account_id'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        total_life:
          $ref: '#/components/schemas/total_life'
        salvage_value:
          $ref: '#/components/schemas/salvage_value'
        depreciation_start_date:
          $ref: '#/components/schemas/depreciation_start_date'
        description:
          $ref: '#/components/schemas/description'
        asset_cost:
          $ref: '#/components/schemas/asset_cost'
        computation_type:
          $ref: '#/components/schemas/computation_type'
        warranty_expiry_date:
          $ref: '#/components/schemas/warranty_expiry_date'
        asset_purchase_date:
          $ref: '#/components/schemas/asset_purchase_date'
        serial_no:
          $ref: '#/components/schemas/serial_no'
        dep_start_value:
          $ref: '#/components/schemas/dep_start_value'
        notes:
          $ref: '#/components/schemas/notes'
        tags:
          $ref: '#/components/schemas/tags'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    create-a-fixed-asset-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The fixed asset has been created.
          readOnly: true
        fixed_asset:
          $ref: '#/components/schemas/fixed_asset'
    get-fixed-asset-list-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        fixedassets:
          type: array
          items:
            type: object
            properties:
              fixed_asset_id:
                $ref: '#/components/schemas/fixed_asset_id'
              asset_number:
                $ref: '#/components/schemas/asset_number'
              asset_name:
                $ref: '#/components/schemas/asset_name'
              status:
                $ref: '#/components/schemas/status'
              current_asset_value:
                $ref: '#/components/schemas/current_asset_value'
              asset_cost:
                $ref: '#/components/schemas/asset_cost'
              fixed_asset_type_name:
                $ref: '#/components/schemas/fixed_asset_type_name'
    update-a-fixed-asset-request:
      type: object
      required:
      - asset_name
      - fixed_asset_type_id
      - asset_account_id
      - asset_cost
      - asset_purchase_date
      properties:
        asset_name:
          $ref: '#/components/schemas/asset_name'
        fixed_asset_type_id:
          $ref: '#/components/schemas/fixed_asset_type_id'
        asset_account_id:
          $ref: '#/components/schemas/asset_account_id'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        total_life:
          $ref: '#/components/schemas/total_life'
        salvage_value:
          $ref: '#/components/schemas/salvage_value'
        depreciation_start_date:
          $ref: '#/components/schemas/depreciation_start_date'
        description:
          $ref: '#/components/schemas/description'
        asset_cost:
          $ref: '#/components/schemas/asset_cost'
        computation_type:
          $ref: '#/components/schemas/computation_type'
        warranty_expiry_date:
          $ref: '#/components/schemas/warranty_expiry_date'
        asset_purchase_date:
          $ref: '#/components/schemas/asset_purchase_date'
        serial_no:
          $ref: '#/components/schemas/serial_no'
        dep_start_value:
          $ref: '#/components/schemas/dep_start_value'
        notes:
          $ref: '#/components/schemas/notes'
        tags:
          $ref: '#/components/schemas/tags'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    update-a-fixed-asset-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The fixed asset has been updated.
          readOnly: true
        fixed_asset:
          $ref: '#/components/schemas/fixed_asset'
    get-fixed-asset-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        fissed-asset:
          $ref: '#/components/schemas/fixed_asset'
    delete-a-fixed-asset-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected fixed asset has been deleted.
          readOnly: true
    get-fixed-asset-history-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        asset_history:
          $ref: '#/components/schemas/asset_history'
        page_context:
          $ref: '#/components/schemas/page_context'
    get-fixed-asset-forecast-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/asset_forcast'
    update-fixed-asset-status:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Fixed asset's status changed
          readOnly: true
    write-off-fixed-asset-request:
      required:
      - date
      - expense_account_id
      - reason
      type: object
      properties:
        date:
          type: string
          description: Enter the date on which the fixed asset is written off
          example: '2024-12-17'
        expense_account_id:
          type: string
          description: Enter an account id to track the expenses associated with the
            written-off fixed asset
          example: '3640355000000000421'
        reason:
          type: string
          description: Enter the reason for writing off the fixed asset
          example: Asset is damaged
    write-off-fixed-asset-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Fixed asset has been written off
          readOnly: true
    sell-fixed-asset-request:
      required:
      - invoice_id
      - line_item_id
      - reason
      - expense_account_id
      type: object
      properties:
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        invoice_id:
          type: string
          description: Enter the invoice id associated with selling the fixed asset.
          example: '3640355000000319008'
        line_item_id:
          $ref: '#/components/schemas/line_item_id'
        reason:
          type: string
          description: Enter the reason for selling the fixed asset
          example: Asset is no longer required
    sell-fixed-asset-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Fixed asset has been sold
          readOnly: true
    delete-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The comment has been deleted.
          readOnly: true
    create-a-fixed-asset-type-request:
      required:
      - fixed_asset_type_name
      - expense_account_id
      - depreciation_account_id
      - depreciation_method
      - depreciation_frequency
      - depreciation_percentage
      - total_life
      - computation_type
      type: object
      properties:
        fixed_asset_type_name:
          $ref: '#/components/schemas/fixed_asset_type_name'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        total_life:
          $ref: '#/components/schemas/total_life'
        computation_type:
          $ref: '#/components/schemas/computation_type'
    create-a-fixed-asset-type-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The fixed asset type has been created.
          readOnly: true
        fixed_asset_type:
          $ref: '#/components/schemas/fixed_asset_type'
    get-fixed-asset-type-list-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        fixed_asset_types:
          type: array
          items:
            $ref: '#/components/schemas/fixed_asset_type'
    update-a-fixed-asset-type-request:
      required:
      - fixed_asset_type_name
      - expense_account_id
      - depreciation_account_id
      - depreciation_method
      - depreciation_frequency
      - depreciation_percentage
      - total_life
      - computation_type
      type: object
      properties:
        fixed_asset_type_name:
          $ref: '#/components/schemas/fixed_asset_type_name'
        expense_account_id:
          $ref: '#/components/schemas/expense_account_id'
        depreciation_account_id:
          $ref: '#/components/schemas/depreciation_account_id'
        depreciation_method:
          $ref: '#/components/schemas/depreciation_method'
        depreciation_frequency:
          $ref: '#/components/schemas/depreciation_frequency'
        depreciation_percentage:
          $ref: '#/components/schemas/depreciation_percentage'
        total_life:
          $ref: '#/components/schemas/total_life'
        computation_type:
          $ref: '#/components/schemas/computation_type'
    update-a-fixed-asset-type-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The fixed asset type has been update.
          readOnly: true
        fixed_asset_type:
          $ref: '#/components/schemas/fixed_asset_type'
    delete-a-fixed-asset-type-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected fixed asset type has been deleted.
          readOnly: true
    adjustment_date:
      description: Date of adjustment.
      type: string
      example: '2013-09-05'
    account_ids:
      description: ID of the accounts for which base currency adjustments need to
        be posted.
      type: string
      example: 460000000000364
    base_currency_adjustment_id:
      description: ID of the Base Currency Adjustment
      type: string
      example: '460000000039001'
    fcy_balance:
      description: Balance in Foreign Currency
      type: number
      format: double
      example: 139.41
    adjusted_balance:
      description: Adjusted Balance
      type: number
      format: double
      example: 209.12
    gain_or_loss:
      description: Amount in Total Gain/Loss
      type: number
      format: double
      example: 37.65
    gl_specific_type:
      description: GL Specific Type of the Account Involved
      type: integer
      example: 5
    accounts:
      description: List of accounts with their opening balance details.
      type: array
      items:
        type: object
        properties:
          acount_split_id:
            $ref: '#/components/schemas/acount_split_id'
          account_id:
            $ref: '#/components/schemas/account_id'
          account_name:
            $ref: '#/components/schemas/account_name'
          debit_or_credit:
            $ref: '#/components/schemas/debit_or_credit'
          exchange_rate:
            $ref: '#/components/schemas/exchange_rate'
          currency_id:
            $ref: '#/components/schemas/currency_id'
          currency_code:
            $ref: '#/components/schemas/currency_code'
          bcy_amount:
            $ref: '#/components/schemas/bcy_amount'
          amount:
            $ref: '#/components/schemas/amount'
          location_id:
            $ref: '#/components/schemas/location_id'
          location_name:
            $ref: '#/components/schemas/location_name'
    create-a-base-currency-adjustment-request:
      required:
      - currency_id
      - adjustment_date
      - exchange_rate
      - notes
      type: object
      properties:
        currency_id:
          $ref: '#/components/schemas/currency_id'
        adjustment_date:
          $ref: '#/components/schemas/adjustment_date'
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
        notes:
          $ref: '#/components/schemas/notes'
    create-a-base-currency-adjustment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The adjustment has been made. The account balances will now reflect
            the adjustment.
          readOnly: true
        data:
          $ref: '#/components/schemas/data'
    list-base-currency-adjustment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        base_currency_adjustments:
          type: array
          items:
            type: object
            properties:
              base_currency_adjustment_id:
                $ref: '#/components/schemas/base_currency_adjustment_id'
              adjustment_date:
                $ref: '#/components/schemas/adjustment_date'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              notes:
                $ref: '#/components/schemas/notes'
              gain_or_loss:
                $ref: '#/components/schemas/gain_or_loss'
    get-a-base-currency-adjustment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/data'
    delete-a-base-currency-adjustment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected base currency adjustment has been deleted.
          readOnly: true
    list-account-details-for-base-currency-adjustment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          type: object
          properties:
            adjustment_date:
              $ref: '#/components/schemas/adjustment_date'
            exchange_rate:
              $ref: '#/components/schemas/exchange_rate'
            currency_id:
              $ref: '#/components/schemas/currency_id'
            accounts:
              $ref: '#/components/schemas/accounts'
            notes:
              $ref: '#/components/schemas/notes'
            currency_code:
              $ref: '#/components/schemas/currency_code'
    project-response:
      type: object
      properties:
        project_id:
          $ref: '#/components/schemas/project_id'
        project_name:
          $ref: '#/components/schemas/project_name'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        description:
          $ref: '#/components/schemas/description'
        status:
          $ref: '#/components/schemas/status'
        billing_type:
          $ref: '#/components/schemas/billing_type'
        rate:
          $ref: '#/components/schemas/rate'
        budget_type:
          $ref: '#/components/schemas/budget_type'
        total_hours:
          $ref: '#/components/schemas/total_hours'
        total_amount:
          $ref: '#/components/schemas/total_amount'
        billed_hours:
          $ref: '#/components/schemas/billed_hours'
        billed_amount:
          $ref: '#/components/schemas/billed_amount'
        un_billed_hours:
          $ref: '#/components/schemas/un_billed_hours'
        un_billed_amount:
          $ref: '#/components/schemas/un_billed_amount'
        billable_hours:
          $ref: '#/components/schemas/billable_hours'
        billable_amount:
          $ref: '#/components/schemas/billable_amount'
        non_billable_hours:
          $ref: '#/components/schemas/non_billable_hours'
        non_billable_amount:
          $ref: '#/components/schemas/non_billable_amount'
        cost_budget_amount:
          $ref: '#/components/schemas/cost_budget_amount'
        is_recurrence_associated:
          $ref: '#/components/schemas/is_recurrence_associated'
        recurring_invoices:
          $ref: '#/components/schemas/recurring_invoices'
        created_time:
          $ref: '#/components/schemas/created_time'
        show_in_dashboard:
          $ref: '#/components/schemas/show_in_dashboard'
        tasks:
          type: array
          description: List of tasks associated with the project
          items:
            type: object
            properties:
              task_id:
                $ref: '#/components/schemas/task_id'
              task_name:
                $ref: '#/components/schemas/task_name'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              budget_hours:
                $ref: '#/components/schemas/budget_hours'
              total_hours:
                $ref: '#/components/schemas/total_hours'
              billed_hours:
                $ref: '#/components/schemas/billed_hours'
              un_billed_hours:
                $ref: '#/components/schemas/un_billed_hours'
              non_billable_hours:
                $ref: '#/components/schemas/non_billable_hours'
              status:
                $ref: '#/components/schemas/status'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              task_custom_fields:
                $ref: '#/components/schemas/task_custom_fields'
        users:
          type: array
          description: List of users associated with the project
          items:
            type: object
            properties:
              user_id:
                $ref: '#/components/schemas/user_id'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              user_name:
                $ref: '#/components/schemas/user_name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              status:
                $ref: '#/components/schemas/status'
              rate:
                $ref: '#/components/schemas/rate'
              budget_hours:
                $ref: '#/components/schemas/budget_hours'
              total_hours:
                $ref: '#/components/schemas/total_hours'
              billed_hours:
                $ref: '#/components/schemas/billed_hours'
              un_billed_hours:
                $ref: '#/components/schemas/un_billed_hours'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
    billing_type:
      type: string
      example: fixed_cost_for_project
    log_time:
      type: string
      example: 05:00
    budget_type:
      type: string
      example: ' '
    total_hours:
      type: string
      example: '12:26'
    billable_hours:
      type: string
      example: '12:26'
    non_billable_hours:
      type: string
      example: '0.00'
    billed_hours:
      type: string
      example: '12:27'
    un_billed_hours:
      type: string
      example: 00:00
    cost_rate:
      description: Hourly cost rate
      type: number
      format: double
      example: 0
    task_id:
      description: ID of the task.
      type: string
      example: '460000000044009'
    task_name:
      type: string
      example: Distribution Analysis
    budget_hours:
      description: Task budget hours
      type: integer
      example: 0
    budget_amount:
      type: string
      example: ' '
    cost_budget_amount:
      description: Budgeted Cost to complete this project
      type: number
      format: double
      example: '1000.00'
    tasks:
      type: array
      items:
        type: string
    total_amount:
      type: number
      format: double
      example: 500
    billed_amount:
      type: number
      format: double
      example: 500
    un_billed_amount:
      type: number
      format: double
      example: 0
    billable_amount:
      type: number
      format: double
      example: 500
    non_billable_amount:
      type: number
      format: double
      example: 0
    is_recurrence_associated:
      type: boolean
      example: false
    recurring_invoices:
      type: array
      items:
        type: string
    show_in_dashboard:
      type: boolean
      example: true
    task_custom_fields:
      type: string
      example: ''
    create-a-project-request:
      required:
      - project_name
      - customer_id
      - billing_type
      - user_id
      type: object
      properties:
        project_name:
          description: Name of the project. <code>Max-length [100]</code>
          type: string
          example: Network Distribution
        customer_id:
          description: ID of the customer.
          type: string
          example: '460000000044001'
        currency_id:
          type: string
          description: The currency id of the currency
          example: '460000000098001'
        description:
          description: Project description. <code>Max-length [500]</code>
          type: string
          example: Distribution for the system of intermediaries between the producer
            of goods and/or services and the final user
        billing_type:
          description: 'The way you bill your customer. Allowed Values: <code>fixed_cost_for_project</code>,
            <code>based_on_project_hours</code>, <code>based_on_staff_hours</code>
            and <code>based_on_task_hours</code>'
          type: string
          example: based_on_task_hours
        rate:
          description: Hourly rate for a task.
          type: string
          example: ' '
        budget_type:
          description: 'The way you budget. Allowed Values: <code>total_project_cost</code>,
            <code>total_project_hours</code>, <code>hours_per_task</code> and <code>hours_per_staff</code>'
          type: string
          example: ' '
        budget_hours:
          description: Task budget hours
          type: string
          example: ' '
        budget_amount:
          description: Give value, if you are estimating total project revenue budget.
          type: string
          example: ' '
        cost_budget_amount:
          description: Budgeted Cost to complete this project
          type: number
          format: double
          example: '1000.00'
        user_id:
          description: ID of the user to be added to the project.
          type: string
          example: INV-00003
        tasks:
          type: array
          description: List of tasks associated with the project
          items:
            type: object
            required:
            - task_name
            properties:
              task_name:
                description: Name of the task. <code>Max-length [100]</code>
                type: string
                example: INV-00003
              description:
                description: Task description. <code>Max-length [500]</code>
                type: string
                example: INV-00003
              rate:
                description: Hourly rate of a task.
                type: string
                example: INV-00003
              budget_hours:
                description: Task budgeting.
                type: string
                example: INV-00003
        users:
          type: array
          description: List of users associated with the project
          items:
            type: object
            properties:
              user_id:
                description: ID of the user to be added to the project.
                type: string
                example: INV-00003
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              user_name:
                $ref: '#/components/schemas/user_name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              status:
                $ref: '#/components/schemas/status'
              rate:
                description: Hourly rate for a task.
                type: string
                example: ' '
              budget_hours:
                description: Task budget hours
                type: string
                example: ' '
              total_hours:
                $ref: '#/components/schemas/total_hours'
              billed_hours:
                $ref: '#/components/schemas/billed_hours'
              un_billed_hours:
                $ref: '#/components/schemas/un_billed_hours'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
    create-a-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The project has been created.
          readOnly: true
        project:
          $ref: '#/components/schemas/project-response'
    list-projects-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        projects:
          type: array
          items:
            type: object
            properties:
              project_id:
                $ref: '#/components/schemas/project_id'
              project_name:
                $ref: '#/components/schemas/project_name'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              description:
                $ref: '#/components/schemas/description'
              status:
                $ref: '#/components/schemas/status'
              billing_type:
                $ref: '#/components/schemas/billing_type'
              rate:
                $ref: '#/components/schemas/rate'
              created_time:
                $ref: '#/components/schemas/created_time'
              has_attachment:
                $ref: '#/components/schemas/has_attachment'
              total_hours:
                $ref: '#/components/schemas/total_hours'
              billable_hours:
                $ref: '#/components/schemas/billable_hours'
        page_context:
          type: array
          items:
            type: object
            properties:
              page:
                $ref: '#/components/schemas/page'
              per_page:
                $ref: '#/components/schemas/per_page'
              report_name:
                $ref: '#/components/schemas/report_name'
              has_more_page:
                $ref: '#/components/schemas/has_more_page'
              sort_order:
                $ref: '#/components/schemas/sort_order'
              sort_column:
                $ref: '#/components/schemas/sort_column'
    update-project-request:
      required:
      - project_name
      - customer_id
      - billing_type
      - user_id
      type: object
      properties:
        project_name:
          description: Name of the project. <code>Max-length [100]</code>
          type: string
          example: Network Distribution
        customer_id:
          description: ID of the customer.
          type: string
          example: '460000000044001'
        currency_id:
          type: string
          description: The currency id of the currency
          example: '460000000098001'
        description:
          description: Project description. <code>Max-length [500]</code>
          type: string
          example: Distribution for the system of intermediaries between the producer
            of goods and/or services and the final user
        billing_type:
          description: 'The way you bill your customer. Allowed Values: <code>fixed_cost_for_project</code>,
            <code>based_on_project_hours</code>, <code>based_on_staff_hours</code>
            and <code>based_on_task_hours</code>'
          type: string
          example: based_on_task_hours
        rate:
          description: Hourly rate for a task.
          type: string
          example: ' '
        budget_type:
          description: 'The way you budget. Allowed Values: <code>total_project_cost</code>,
            <code>total_project_hours</code>, <code>hours_per_task</code> and <code>hours_per_staff</code>'
          type: string
          example: ' '
        budget_hours:
          description: Task budget hours
          type: string
          example: ' '
        budget_amount:
          description: Give value, if you are estimating total project revenue.
          type: string
          example: ' '
        cost_budget_amount:
          description: Budgeted Cost to complete this project
          type: number
          format: double
          example: '1000.00'
        user_id:
          description: ID of the user to be added to the project.
          type: string
          example: INV-00003
        tasks:
          type: array
          description: List of tasks associated with the project
          items:
            type: object
            required:
            - task_name
            properties:
              task_name:
                description: Name of the task. <code>Max-length [100]</code>
                type: string
                example: INV-00003
              description:
                description: Task description. <code>Max-length [500]</code>
                type: string
                example: INV-00003
              rate:
                description: Hourly rate of a task.
                type: string
                example: INV-00003
              budget_hours:
                description: Task budgeting.
                type: string
                example: INV-00003
        users:
          type: array
          description: List of users associated with the project
          items:
            type: object
            properties:
              user_id:
                description: ID of the user to be added to the project.
                type: string
                example: INV-00003
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              user_name:
                $ref: '#/components/schemas/user_name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              status:
                $ref: '#/components/schemas/status'
              rate:
                description: Hourly rate for a task.
                type: string
                example: ' '
              budget_hours:
                description: Task budget hours
                type: string
                example: ' '
              total_hours:
                $ref: '#/components/schemas/total_hours'
              billed_hours:
                $ref: '#/components/schemas/billed_hours'
              un_billed_hours:
                $ref: '#/components/schemas/un_billed_hours'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
    update-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The project information has been updated.
          readOnly: true
        project:
          type: object
          properties:
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            description:
              $ref: '#/components/schemas/description'
            status:
              $ref: '#/components/schemas/status'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            rate:
              $ref: '#/components/schemas/rate'
            budget_type:
              $ref: '#/components/schemas/budget_type'
            total_hours:
              $ref: '#/components/schemas/total_hours'
            total_amount:
              $ref: '#/components/schemas/total_amount'
            billed_hours:
              $ref: '#/components/schemas/billed_hours'
            billed_amount:
              $ref: '#/components/schemas/billed_amount'
            un_billed_hours:
              $ref: '#/components/schemas/un_billed_hours'
            un_billed_amount:
              $ref: '#/components/schemas/un_billed_amount'
            billable_hours:
              $ref: '#/components/schemas/billable_hours'
            billable_amount:
              $ref: '#/components/schemas/billable_amount'
            non_billable_hours:
              $ref: '#/components/schemas/non_billable_hours'
            non_billable_amount:
              $ref: '#/components/schemas/non_billable_amount'
            cost_budget_amount:
              $ref: '#/components/schemas/cost_budget_amount'
            is_recurrence_associated:
              $ref: '#/components/schemas/is_recurrence_associated'
            recurring_invoices:
              $ref: '#/components/schemas/recurring_invoices'
            created_time:
              $ref: '#/components/schemas/created_time'
            show_in_dashboard:
              $ref: '#/components/schemas/show_in_dashboard'
            tasks:
              type: array
              description: List of tasks associated with the project
              items:
                type: object
                properties:
                  task_id:
                    $ref: '#/components/schemas/task_id'
                  task_name:
                    $ref: '#/components/schemas/task_name'
                  description:
                    $ref: '#/components/schemas/description'
                  rate:
                    $ref: '#/components/schemas/rate'
                  budget_hours:
                    $ref: '#/components/schemas/budget_hours'
                  total_hours:
                    $ref: '#/components/schemas/total_hours'
                  billed_hours:
                    $ref: '#/components/schemas/billed_hours'
                  un_billed_hours:
                    $ref: '#/components/schemas/un_billed_hours'
                  non_billable_hours:
                    $ref: '#/components/schemas/non_billable_hours'
                  status:
                    $ref: '#/components/schemas/status'
                  is_billable:
                    $ref: '#/components/schemas/is_billable'
                  task_custom_fields:
                    $ref: '#/components/schemas/task_custom_fields'
            users:
              type: array
              description: List of users associated with the project
              items:
                type: object
                properties:
                  user_id:
                    $ref: '#/components/schemas/user_id'
                  is_current_user:
                    $ref: '#/components/schemas/is_current_user'
                  user_name:
                    $ref: '#/components/schemas/user_name'
                  email:
                    $ref: '#/components/schemas/email'
                  user_role:
                    $ref: '#/components/schemas/user_role'
                  status:
                    $ref: '#/components/schemas/status'
                  rate:
                    $ref: '#/components/schemas/rate'
                  budget_hours:
                    $ref: '#/components/schemas/budget_hours'
                  total_hours:
                    $ref: '#/components/schemas/total_hours'
                  billed_hours:
                    $ref: '#/components/schemas/billed_hours'
                  un_billed_hours:
                    $ref: '#/components/schemas/un_billed_hours'
                  cost_rate:
                    $ref: '#/components/schemas/cost_rate'
    get-a-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        project:
          type: object
          properties:
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            description:
              $ref: '#/components/schemas/description'
            status:
              $ref: '#/components/schemas/status'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            rate:
              $ref: '#/components/schemas/rate'
            budget_type:
              $ref: '#/components/schemas/budget_type'
            total_hours:
              $ref: '#/components/schemas/total_hours'
            total_amount:
              $ref: '#/components/schemas/total_amount'
            billed_hours:
              $ref: '#/components/schemas/billed_hours'
            billed_amount:
              $ref: '#/components/schemas/billed_amount'
            un_billed_hours:
              $ref: '#/components/schemas/un_billed_hours'
            un_billed_amount:
              $ref: '#/components/schemas/un_billed_amount'
            billable_hours:
              $ref: '#/components/schemas/billable_hours'
            billable_amount:
              $ref: '#/components/schemas/billable_amount'
            non_billable_hours:
              $ref: '#/components/schemas/non_billable_hours'
            non_billable_amount:
              $ref: '#/components/schemas/non_billable_amount'
            cost_budget_amount:
              $ref: '#/components/schemas/cost_budget_amount'
            is_recurrence_associated:
              $ref: '#/components/schemas/is_recurrence_associated'
            recurring_invoices:
              $ref: '#/components/schemas/recurring_invoices'
            created_time:
              $ref: '#/components/schemas/created_time'
            show_in_dashboard:
              $ref: '#/components/schemas/show_in_dashboard'
            tasks:
              type: array
              description: List of tasks associated with the project
              items:
                type: object
                properties:
                  task_id:
                    $ref: '#/components/schemas/task_id'
                  task_name:
                    $ref: '#/components/schemas/task_name'
                  description:
                    $ref: '#/components/schemas/description'
                  rate:
                    $ref: '#/components/schemas/rate'
                  budget_hours:
                    $ref: '#/components/schemas/budget_hours'
                  total_hours:
                    $ref: '#/components/schemas/total_hours'
                  billed_hours:
                    $ref: '#/components/schemas/billed_hours'
                  un_billed_hours:
                    $ref: '#/components/schemas/un_billed_hours'
                  non_billable_hours:
                    $ref: '#/components/schemas/non_billable_hours'
                  status:
                    $ref: '#/components/schemas/status'
                  is_billable:
                    $ref: '#/components/schemas/is_billable'
                  task_custom_fields:
                    $ref: '#/components/schemas/task_custom_fields'
    delete-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The project has been deleted.
          readOnly: true
    activate-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected Projects have been marked as active.
          readOnly: true
    inactivate-a-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected projects have been marked as inactive.
          readOnly: true
    clone-project-request:
      required:
      - project_name
      type: object
      properties:
        project_name:
          description: Name of the project. <code>Max-length [100]</code>
          type: string
          example: Network Distribution
        description:
          description: Project description. <code>Max-length [500]</code>
          type: string
          example: Distribution for the system of intermediaries between the producer
            of goods and/or services and the final user
    clone-project-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Project has been cloned successfully.
          readOnly: true
        project:
          type: object
          properties:
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            currency_code:
              $ref: '#/components/schemas/currency_code'
            description:
              $ref: '#/components/schemas/description'
            status:
              $ref: '#/components/schemas/status'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            rate:
              $ref: '#/components/schemas/rate'
            budget_type:
              $ref: '#/components/schemas/budget_type'
            total_hours:
              $ref: '#/components/schemas/total_hours'
            total_amount:
              $ref: '#/components/schemas/total_amount'
            billed_hours:
              $ref: '#/components/schemas/billed_hours'
            billed_amount:
              $ref: '#/components/schemas/billed_amount'
            un_billed_hours:
              $ref: '#/components/schemas/un_billed_hours'
            un_billed_amount:
              $ref: '#/components/schemas/un_billed_amount'
            billable_hours:
              $ref: '#/components/schemas/billable_hours'
            billable_amount:
              $ref: '#/components/schemas/billable_amount'
            cost_budget_amount:
              $ref: '#/components/schemas/cost_budget_amount'
            non_billable_hours:
              $ref: '#/components/schemas/non_billable_hours'
            non_billable_amount:
              $ref: '#/components/schemas/non_billable_amount'
            is_recurrence_associated:
              $ref: '#/components/schemas/is_recurrence_associated'
            recurring_invoices:
              $ref: '#/components/schemas/recurring_invoices'
            created_time:
              $ref: '#/components/schemas/created_time'
            show_in_dashboard:
              $ref: '#/components/schemas/show_in_dashboard'
            tasks:
              type: array
              description: List of tasks associated with the project
              items:
                type: object
                properties:
                  task_id:
                    $ref: '#/components/schemas/task_id'
                  task_name:
                    $ref: '#/components/schemas/task_name'
                  description:
                    $ref: '#/components/schemas/description'
                  rate:
                    $ref: '#/components/schemas/rate'
                  budget_hours:
                    $ref: '#/components/schemas/budget_hours'
                  total_hours:
                    $ref: '#/components/schemas/total_hours'
                  billed_hours:
                    $ref: '#/components/schemas/billed_hours'
                  un_billed_hours:
                    $ref: '#/components/schemas/un_billed_hours'
                  non_billable_hours:
                    $ref: '#/components/schemas/non_billable_hours'
                  status:
                    $ref: '#/components/schemas/status'
                  is_billable:
                    $ref: '#/components/schemas/is_billable'
                  task_custom_fields:
                    $ref: '#/components/schemas/task_custom_fields'
            users:
              type: array
              description: List of users associated with the project
              items:
                type: object
                properties:
                  user_id:
                    $ref: '#/components/schemas/user_id'
                  is_current_user:
                    $ref: '#/components/schemas/is_current_user'
                  user_name:
                    $ref: '#/components/schemas/user_name'
                  email:
                    $ref: '#/components/schemas/email'
                  user_role:
                    $ref: '#/components/schemas/user_role'
                  status:
                    $ref: '#/components/schemas/status'
                  rate:
                    $ref: '#/components/schemas/rate'
                  budget_hours:
                    $ref: '#/components/schemas/budget_hours'
                  total_hours:
                    $ref: '#/components/schemas/total_hours'
                  billed_hours:
                    $ref: '#/components/schemas/billed_hours'
                  un_billed_hours:
                    $ref: '#/components/schemas/un_billed_hours'
                  cost_rate:
                    $ref: '#/components/schemas/cost_rate'
    assign-users-request:
      required:
      - cost_rate
      type: object
      properties:
        users:
          $ref: '#/components/schemas/users'
        cost_rate:
          type: number
          description: Cost rate per hour for the user or task.
          format: double
          example: '10.00'
    assign-users-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Users added
          readOnly: true
        users:
          type: array
          description: List of users associated with the project
          items:
            type: object
            properties:
              user_id:
                $ref: '#/components/schemas/user_id'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              user_name:
                $ref: '#/components/schemas/user_name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              status:
                $ref: '#/components/schemas/status'
              rate:
                $ref: '#/components/schemas/rate'
              budget_hours:
                $ref: '#/components/schemas/budget_hours'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
    list-users-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        users:
          type: array
          description: List of users in the organization.
          items:
            type: object
            properties:
              user_id:
                $ref: '#/components/schemas/user_id'
              role_id:
                $ref: '#/components/schemas/role_id'
              name:
                $ref: '#/components/schemas/name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              status:
                $ref: '#/components/schemas/status'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              photo_url:
                $ref: '#/components/schemas/photo_url'
              is_customer_segmented:
                $ref: '#/components/schemas/is_customer_segmented'
              is_vendor_segmented:
                $ref: '#/components/schemas/is_vendor_segmented'
              user_type:
                $ref: '#/components/schemas/user_type'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    invite-user-request:
      required:
      - user_name
      - email
      type: object
      properties:
        user_name:
          $ref: '#/components/schemas/user_name'
        email:
          $ref: '#/components/schemas/email'
        user_role:
          $ref: '#/components/schemas/user_role'
        rate:
          description: Hourly rate for a task.
          type: string
          example: ' '
        budget_hours:
          $ref: '#/components/schemas/budget_hours'
        cost_rate:
          $ref: '#/components/schemas/cost_rate'
    invite-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The staff has been added.
          readOnly: true
        users:
          type: array
          description: List of users associated with the project
          items:
            type: object
            properties:
              user_id:
                $ref: '#/components/schemas/user_id'
              user_name:
                $ref: '#/components/schemas/user_name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
    update-user-request:
      type: object
      properties:
        user_name:
          $ref: '#/components/schemas/user_name'
        user_role:
          $ref: '#/components/schemas/user_role'
        rate:
          $ref: '#/components/schemas/rate'
        budget_hours:
          $ref: '#/components/schemas/budget_hours'
        cost_rate:
          $ref: '#/components/schemas/cost_rate'
    update-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The staff information has been updated.
          readOnly: true
        users:
          type: array
          description: List of users associated with the project
          items:
            type: object
            properties:
              user_id:
                $ref: '#/components/schemas/user_id'
              user_name:
                $ref: '#/components/schemas/user_name'
              email:
                $ref: '#/components/schemas/email'
              user_role:
                $ref: '#/components/schemas/user_role'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
    get-a-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        user:
          type: object
          properties:
            user_id:
              $ref: '#/components/schemas/user_id'
            name:
              $ref: '#/components/schemas/name'
            email_ids:
              description: EmailIDs of an user
              type: array
              items:
                type: object
                properties:
                  email:
                    $ref: '#/components/schemas/email'
                  is_selected:
                    $ref: '#/components/schemas/is_selected'
            status:
              $ref: '#/components/schemas/status'
            user_role:
              $ref: '#/components/schemas/user_role'
            user_type:
              $ref: '#/components/schemas/user_type'
            role_id:
              $ref: '#/components/schemas/role_id'
            cost_rate:
              $ref: '#/components/schemas/cost_rate'
            photo_url:
              $ref: '#/components/schemas/photo_url'
            is_employee:
              $ref: '#/components/schemas/is_employee'
            created_time:
              $ref: '#/components/schemas/created_time'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
    delete-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The staff has been removed
          readOnly: true
    post-comment-request:
      required:
      - description
      type: object
      properties:
        description:
          description: Project description. <code>Max-length [500]</code>
          type: string
          example: Billing based on task hours
    post-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comments added.
          readOnly: true
        comments:
          type: array
          items:
            type: object
            properties:
              project_id:
                $ref: '#/components/schemas/project_id'
              comment_id:
                $ref: '#/components/schemas/comment_id'
              description:
                $ref: '#/components/schemas/description'
              commented_by_id:
                $ref: '#/components/schemas/commented_by_id'
              commented_by:
                $ref: '#/components/schemas/commented_by'
              date:
                $ref: '#/components/schemas/date'
              date_description:
                $ref: '#/components/schemas/date_description'
              time:
                $ref: '#/components/schemas/time'
    task-response:
      type: object
      properties:
        project_id:
          $ref: '#/components/schemas/project_id'
        task_id:
          $ref: '#/components/schemas/task_id'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        task_name:
          $ref: '#/components/schemas/task_name'
        project_name:
          $ref: '#/components/schemas/project_name'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        billed_hours:
          $ref: '#/components/schemas/billed_hours'
        log_time:
          $ref: '#/components/schemas/log_time'
        un_billed_hours:
          $ref: '#/components/schemas/un_billed_hours'
    add-a-task-request:
      type: object
      properties:
        task_id:
          $ref: '#/components/schemas/task_id'
        owner_ids:
          $ref: '#/components/schemas/owner_ids'
        owner_id:
          $ref: '#/components/schemas/owner_id'
        task_type:
          $ref: '#/components/schemas/task_type'
        title:
          $ref: '#/components/schemas/title'
        due_date:
          $ref: '#/components/schemas/due_date'
        contact_type:
          $ref: '#/components/schemas/contact_type'
        contact_id:
          $ref: '#/components/schemas/contact_id'
        can_push:
          $ref: '#/components/schemas/can_push'
        related_entity_type:
          $ref: '#/components/schemas/related_entity_type'
        related_entity_id:
          $ref: '#/components/schemas/related_entity_id'
        related_sub_entity_type:
          $ref: '#/components/schemas/related_sub_entity_type'
        related_sub_entity_id:
          $ref: '#/components/schemas/related_sub_entity_id'
        status:
          $ref: '#/components/schemas/status'
        billing_type:
          $ref: '#/components/schemas/billing_type'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        task_rate:
          $ref: '#/components/schemas/task_rate'
        completion_percentage:
          $ref: '#/components/schemas/completion_percentage'
        priority:
          $ref: '#/components/schemas/priority'
        send_email_notification:
          $ref: '#/components/schemas/send_email_notification'
        mail_id:
          $ref: '#/components/schemas/mail_id'
        email_account_type:
          $ref: '#/components/schemas/email_account_type'
        email_folder_id:
          $ref: '#/components/schemas/email_folder_id'
        email_account_id:
          $ref: '#/components/schemas/email_account_id'
        mail_attachments:
          $ref: '#/components/schemas/mail_attachments'
        reminder:
          $ref: '#/components/schemas/reminder'
        task_description:
          $ref: '#/components/schemas/task_description'
        custom_fields:
          type: array
          description: Custom fields associated with the task.
          items:
            type: object
            properties:
              customfield_id:
                description: ID of the custom field.
                type: string
                example: 982000000567114
              index:
                description: Index of the custom field.
                type: integer
                example: 1
              label:
                description: Label of the custom field.
                type: string
                example: Custom Field Label
              value:
                description: Value of the custom field.
                type: string
                example: Custom Field Value
              value_formatted:
                description: Formatted value of the custom field.
                type: string
                example: Formatted Custom Field Value
              api_name:
                description: Name of the custom field.
                type: string
                example: CustomFieldAPIName
        due_after:
          $ref: '#/components/schemas/due_after'
        template_id:
          $ref: '#/components/schemas/template_id'
        task_number:
          $ref: '#/components/schemas/task_number'
        task_number_prefix:
          $ref: '#/components/schemas/task_number_prefix'
        task_number_suffix:
          $ref: '#/components/schemas/task_number_suffix'
        recurring_task_id:
          $ref: '#/components/schemas/recurring_task_id'
        is_sub_task:
          $ref: '#/components/schemas/is_sub_task'
        parent_id:
          $ref: '#/components/schemas/parent_id'
        dependencies:
          $ref: '#/components/schemas/dependencies'
        entity_tags:
          description: List of tags to be associated with the task.
          type: array
          items:
            type: object
            properties:
              name:
                description: Name of the tag.
                type: string
                example: Important
              color:
                description: Color code of the tag.
                type: string
                example: FFB6C1
    add-a-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task created
          readOnly: true
        task:
          type: object
          properties:
            task_id:
              $ref: '#/components/schemas/task_id'
            recurring_task_id:
              $ref: '#/components/schemas/recurring_task_id'
            owner_id:
              $ref: '#/components/schemas/owner_id'
            owner_ids:
              $ref: '#/components/schemas/owner_ids'
            owner_name:
              $ref: '#/components/schemas/owner_name'
            owners:
              $ref: '#/components/schemas/owners'
            title:
              $ref: '#/components/schemas/title'
            due_date:
              $ref: '#/components/schemas/due_date'
            due_date_formatted:
              $ref: '#/components/schemas/due_date_formatted'
            is_overdue:
              $ref: '#/components/schemas/is_overdue'
            contact_type:
              $ref: '#/components/schemas/contact_type'
            contact_id:
              $ref: '#/components/schemas/contact_id'
            color_code:
              $ref: '#/components/schemas/color_code'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            parent_task_id:
              $ref: '#/components/schemas/parent_task_id'
            related_entity_id:
              $ref: '#/components/schemas/related_entity_id'
            related_entity_type:
              $ref: '#/components/schemas/related_entity_type'
            related_entity:
              $ref: '#/components/schemas/related_entity'
            related_entity_value:
              $ref: '#/components/schemas/related_entity_value'
            parent_status:
              $ref: '#/components/schemas/parent_status'
            status:
              $ref: '#/components/schemas/status'
            custom_status_id:
              $ref: '#/components/schemas/custom_status_id'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            task_rate:
              $ref: '#/components/schemas/task_rate'
            completion_percentage:
              $ref: '#/components/schemas/completion_percentage'
            is_subtask:
              $ref: '#/components/schemas/is_subtask'
            priority:
              $ref: '#/components/schemas/priority'
            reminder:
              $ref: '#/components/schemas/reminder'
            task_description:
              $ref: '#/components/schemas/task_description'
            is_accountant_task:
              $ref: '#/components/schemas/is_accountant_task'
            task_number:
              $ref: '#/components/schemas/task_number'
            task_number_prefix:
              $ref: '#/components/schemas/task_number_prefix'
            task_number_suffix:
              $ref: '#/components/schemas/task_number_suffix'
            can_push:
              $ref: '#/components/schemas/can_push'
            created_by_id:
              $ref: '#/components/schemas/created_by_id'
            last_modified_by_id:
              $ref: '#/components/schemas/last_modified_by_id'
            template_id:
              $ref: '#/components/schemas/template_id'
            due_after:
              $ref: '#/components/schemas/due_after'
            parent_task_name:
              $ref: '#/components/schemas/parent_task_name'
            parent_task_number:
              $ref: '#/components/schemas/parent_task_number'
            created_by_name:
              $ref: '#/components/schemas/created_by_name'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            statuses:
              $ref: '#/components/schemas/statuses'
            is_system_task:
              $ref: '#/components/schemas/is_system_task'
            system_task_type:
              $ref: '#/components/schemas/system_task_type'
            comments:
              $ref: '#/components/schemas/comments'
            dependencies:
              $ref: '#/components/schemas/dependencies'
            entity_tags:
              $ref: '#/components/schemas/entity_tags'
    list-tasks-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tasks:
          type: array
          items:
            type: object
            properties:
              task_id:
                $ref: '#/components/schemas/task_id'
              task_number:
                $ref: '#/components/schemas/task_number'
              owner_id:
                $ref: '#/components/schemas/owner_id'
              owner_name:
                $ref: '#/components/schemas/owner_name'
              contact_id:
                $ref: '#/components/schemas/contact_id'
              billing_type:
                $ref: '#/components/schemas/billing_type'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              task_rate:
                $ref: '#/components/schemas/task_rate'
              completion_percentage:
                $ref: '#/components/schemas/completion_percentage'
              contact_name:
                $ref: '#/components/schemas/contact_name'
              contact_type:
                $ref: '#/components/schemas/contact_type'
              client_id:
                $ref: '#/components/schemas/client_id'
              client_name:
                $ref: '#/components/schemas/client_name'
              title:
                $ref: '#/components/schemas/title'
              created_time:
                $ref: '#/components/schemas/created_time'
              created_time_formatted:
                $ref: '#/components/schemas/created_time_formatted'
              due_date:
                $ref: '#/components/schemas/due_date'
              is_accountant_task:
                $ref: '#/components/schemas/is_accountant_task'
              is_overdue:
                $ref: '#/components/schemas/is_overdue'
              parent_task_id:
                $ref: '#/components/schemas/parent_task_id'
              parent_status:
                $ref: '#/components/schemas/parent_status'
              priority:
                $ref: '#/components/schemas/priority'
              status:
                $ref: '#/components/schemas/status'
              color_code:
                $ref: '#/components/schemas/color_code'
              task_description:
                $ref: '#/components/schemas/task_description'
              subtask_count:
                $ref: '#/components/schemas/subtask_count'
              is_subtask:
                $ref: '#/components/schemas/is_subtask'
              parent_task_name:
                $ref: '#/components/schemas/parent_task_name'
              parent_task_number:
                $ref: '#/components/schemas/parent_task_number'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
              owners:
                $ref: '#/components/schemas/owners'
              entity_tags:
                $ref: '#/components/schemas/entity_tags'
    update-a-task-request:
      type: object
      properties:
        task_id:
          $ref: '#/components/schemas/task_id'
        owner_ids:
          $ref: '#/components/schemas/owner_ids'
        owner_id:
          $ref: '#/components/schemas/owner_id'
        task_type:
          $ref: '#/components/schemas/task_type'
        title:
          $ref: '#/components/schemas/title'
        due_date:
          $ref: '#/components/schemas/due_date'
        contact_type:
          $ref: '#/components/schemas/contact_type'
        contact_id:
          $ref: '#/components/schemas/contact_id'
        can_push:
          $ref: '#/components/schemas/can_push'
        related_entity_type:
          $ref: '#/components/schemas/related_entity_type'
        related_entity_id:
          $ref: '#/components/schemas/related_entity_id'
        related_sub_entity_type:
          $ref: '#/components/schemas/related_sub_entity_type'
        related_sub_entity_id:
          $ref: '#/components/schemas/related_sub_entity_id'
        status:
          $ref: '#/components/schemas/status'
        billing_type:
          $ref: '#/components/schemas/billing_type'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        task_rate:
          $ref: '#/components/schemas/task_rate'
        completion_percentage:
          $ref: '#/components/schemas/completion_percentage'
        priority:
          $ref: '#/components/schemas/priority'
        send_email_notification:
          $ref: '#/components/schemas/send_email_notification'
        mail_id:
          $ref: '#/components/schemas/mail_id'
        email_account_type:
          $ref: '#/components/schemas/email_account_type'
        email_folder_id:
          $ref: '#/components/schemas/email_folder_id'
        email_account_id:
          $ref: '#/components/schemas/email_account_id'
        mail_attachments:
          $ref: '#/components/schemas/mail_attachments'
        reminder:
          $ref: '#/components/schemas/reminder'
        task_description:
          $ref: '#/components/schemas/task_description'
        custom_fields:
          type: array
          description: List of custom fields associated with the task
          items:
            type: object
            properties:
              customfield_id:
                description: ID of the custom field.
                type: string
                example: 982000000567114
              index:
                description: Index of the custom field.
                type: integer
                example: 1
              label:
                description: Label of the custom field.
                type: string
                example: Custom Field Label
              value:
                description: Value of the custom field.
                type: string
                example: Custom Field Value
              value_formatted:
                description: Formatted value of the custom field.
                type: string
                example: Formatted Custom Field Value
              api_name:
                description: Name of the custom field.
                type: string
                example: CustomFieldAPIName
        due_after:
          $ref: '#/components/schemas/due_after'
        template_id:
          $ref: '#/components/schemas/template_id'
        task_number:
          $ref: '#/components/schemas/task_number'
        task_number_prefix:
          $ref: '#/components/schemas/task_number_prefix'
        task_number_suffix:
          $ref: '#/components/schemas/task_number_suffix'
        recurring_task_id:
          $ref: '#/components/schemas/recurring_task_id'
        is_sub_task:
          $ref: '#/components/schemas/is_sub_task'
        parent_id:
          $ref: '#/components/schemas/parent_id'
        dependencies:
          $ref: '#/components/schemas/dependencies'
        entity_tags:
          description: List of tags to be associated with the task.
          type: array
          items:
            type: object
            properties:
              name:
                description: Name of the tag.
                type: string
                example: Important
              color:
                description: Color code of the tag.
                type: string
                example: FFB6C1
    update-a-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task updated
          readOnly: true
        task:
          type: object
          properties:
            task_id:
              $ref: '#/components/schemas/task_id'
            recurring_task_id:
              $ref: '#/components/schemas/recurring_task_id'
            owner_id:
              $ref: '#/components/schemas/owner_id'
            owner_ids:
              $ref: '#/components/schemas/owner_ids'
            owner_name:
              $ref: '#/components/schemas/owner_name'
            owners:
              $ref: '#/components/schemas/owners'
            title:
              $ref: '#/components/schemas/title'
            due_date:
              $ref: '#/components/schemas/due_date'
            due_date_formatted:
              $ref: '#/components/schemas/due_date_formatted'
            is_overdue:
              $ref: '#/components/schemas/is_overdue'
            contact_type:
              $ref: '#/components/schemas/contact_type'
            contact_id:
              $ref: '#/components/schemas/contact_id'
            color_code:
              $ref: '#/components/schemas/color_code'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            parent_task_id:
              $ref: '#/components/schemas/parent_task_id'
            related_entity_id:
              $ref: '#/components/schemas/related_entity_id'
            related_entity_type:
              $ref: '#/components/schemas/related_entity_type'
            related_entity:
              $ref: '#/components/schemas/related_entity'
            related_entity_value:
              $ref: '#/components/schemas/related_entity_value'
            parent_status:
              $ref: '#/components/schemas/parent_status'
            status:
              $ref: '#/components/schemas/status'
            custom_status_id:
              $ref: '#/components/schemas/custom_status_id'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            task_rate:
              $ref: '#/components/schemas/task_rate'
            completion_percentage:
              $ref: '#/components/schemas/completion_percentage'
            is_subtask:
              $ref: '#/components/schemas/is_subtask'
            priority:
              $ref: '#/components/schemas/priority'
            reminder:
              $ref: '#/components/schemas/reminder'
            task_description:
              $ref: '#/components/schemas/task_description'
            is_accountant_task:
              $ref: '#/components/schemas/is_accountant_task'
            task_number:
              $ref: '#/components/schemas/task_number'
            task_number_prefix:
              $ref: '#/components/schemas/task_number_prefix'
            task_number_suffix:
              $ref: '#/components/schemas/task_number_suffix'
            can_push:
              $ref: '#/components/schemas/can_push'
            created_by_id:
              $ref: '#/components/schemas/created_by_id'
            last_modified_by_id:
              $ref: '#/components/schemas/last_modified_by_id'
            template_id:
              $ref: '#/components/schemas/template_id'
            due_after:
              $ref: '#/components/schemas/due_after'
            parent_task_name:
              $ref: '#/components/schemas/parent_task_name'
            parent_task_number:
              $ref: '#/components/schemas/parent_task_number'
            created_by_name:
              $ref: '#/components/schemas/created_by_name'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            statuses:
              $ref: '#/components/schemas/statuses'
            is_system_task:
              $ref: '#/components/schemas/is_system_task'
            system_task_type:
              $ref: '#/components/schemas/system_task_type'
            comments:
              $ref: '#/components/schemas/comments'
            dependencies:
              $ref: '#/components/schemas/dependencies'
            entity_tags:
              $ref: '#/components/schemas/entity_tags'
    get-a-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        task:
          type: object
          properties:
            task_id:
              $ref: '#/components/schemas/task_id'
            recurring_task_id:
              $ref: '#/components/schemas/recurring_task_id'
            owner_id:
              $ref: '#/components/schemas/owner_id'
            owner_ids:
              $ref: '#/components/schemas/owner_ids'
            owner_name:
              $ref: '#/components/schemas/owner_name'
            owners:
              $ref: '#/components/schemas/owners'
            title:
              $ref: '#/components/schemas/title'
            due_date:
              $ref: '#/components/schemas/due_date'
            due_date_formatted:
              $ref: '#/components/schemas/due_date_formatted'
            is_overdue:
              $ref: '#/components/schemas/is_overdue'
            contact_type:
              $ref: '#/components/schemas/contact_type'
            contact_id:
              $ref: '#/components/schemas/contact_id'
            color_code:
              $ref: '#/components/schemas/color_code'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            parent_task_id:
              $ref: '#/components/schemas/parent_task_id'
            related_entity_id:
              $ref: '#/components/schemas/related_entity_id'
            related_entity_type:
              $ref: '#/components/schemas/related_entity_type'
            related_entity:
              $ref: '#/components/schemas/related_entity'
            related_entity_value:
              $ref: '#/components/schemas/related_entity_value'
            parent_status:
              $ref: '#/components/schemas/parent_status'
            status:
              $ref: '#/components/schemas/status'
            custom_status_id:
              $ref: '#/components/schemas/custom_status_id'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            task_rate:
              $ref: '#/components/schemas/task_rate'
            completion_percentage:
              $ref: '#/components/schemas/completion_percentage'
            is_subtask:
              $ref: '#/components/schemas/is_subtask'
            priority:
              $ref: '#/components/schemas/priority'
            reminder:
              $ref: '#/components/schemas/reminder'
            task_description:
              $ref: '#/components/schemas/task_description'
            is_accountant_task:
              $ref: '#/components/schemas/is_accountant_task'
            task_number:
              $ref: '#/components/schemas/task_number'
            task_number_prefix:
              $ref: '#/components/schemas/task_number_prefix'
            task_number_suffix:
              $ref: '#/components/schemas/task_number_suffix'
            can_push:
              $ref: '#/components/schemas/can_push'
            created_by_id:
              $ref: '#/components/schemas/created_by_id'
            last_modified_by_id:
              $ref: '#/components/schemas/last_modified_by_id'
            template_id:
              $ref: '#/components/schemas/template_id'
            due_after:
              $ref: '#/components/schemas/due_after'
            parent_task_name:
              $ref: '#/components/schemas/parent_task_name'
            parent_task_number:
              $ref: '#/components/schemas/parent_task_number'
            created_by_name:
              $ref: '#/components/schemas/created_by_name'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            statuses:
              $ref: '#/components/schemas/statuses'
            is_system_task:
              $ref: '#/components/schemas/is_system_task'
            system_task_type:
              $ref: '#/components/schemas/system_task_type'
            comments:
              $ref: '#/components/schemas/comments'
            dependencies:
              $ref: '#/components/schemas/dependencies'
            entity_tags:
              $ref: '#/components/schemas/entity_tags'
    delete-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The project task has been deleted successfully.
          readOnly: true
    autocomplete-projects-response:
      type: object
      description: Standard Books response envelope with autocomplete rows and pagination
        context.
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        results:
          type: array
          description: Matching projects; use <code>id</code> as project id in report
            parameters.
          items:
            $ref: '#/components/schemas/autocomplete-project-row'
        page_context:
          type: object
          properties:
            page:
              type: integer
              example: 1
            per_page:
              type: integer
              example: 1000
            has_more_page:
              type: boolean
              example: false
            sort_column:
              type: string
              example: text
            sort_order:
              type: string
              example: A
    autocomplete-project-row:
      type: object
      properties:
        id:
          type: string
          description: Project id. Use in <code>rule</code> (<code>project_ids</code>)
            or <code>compare_entities</code> with group <code>project</code>.
          example: '251737000000142619'
        text:
          type: string
          description: Project name shown in autocomplete.
          example: Marriage Cooking
        customer_name:
          type: string
          example: Ganesh
        billing_type:
          type: string
          example: fixed_cost_for_project
        currency_id:
          type: string
          example: '251737000000000099'
        project_code:
          type: string
          example: ''
    recurring_task_id:
      description: ID of the recurring task.
      type: string
      example: 982000000567115
    owner_ids:
      description: Comma separated list of owner IDs of the task.
      type: string
      example: 982000000567116,982000000567117
    owner_name:
      description: Name of the owner of the task.
      type: string
      example: John Doe
    owners:
      description: List of owners of the task.
      type: array
      items:
        type: object
        properties:
          owner_id:
            $ref: '#/components/schemas/owner_id'
          owner_name:
            $ref: '#/components/schemas/owner_name'
          owner_mail:
            description: Email of the owner of the task.
            type: string
            example: johndoe@example.com
          photo_url:
            description: URL of the photo of the owner of the task.
            type: string
            example: https://example.com/photo.jpg
    title:
      description: Title of the task.
      type: string
      example: Sample Task
    due_date_formatted:
      description: Formatted due date of the task.
      type: string
      example: 31-12-2025
    is_overdue:
      description: Indicates whether the task is overdue.
      type: boolean
      example: true
    color_code:
      description: Color code of the task.
      type: string
      example: '#FF5733'
    parent_task_id:
      description: ID of the parent task.
      type: string
      example: 982000000567119
    related_entity_id:
      description: ID of the related entity associated with the task.
      type: string
      example: 982000000567120
    related_entity_type:
      description: Type of the related entity associated with the task.
      type: string
      example: invoice
    related_entity:
      description: Unique value assigned to each entity.
      type: integer
      example: 20
    related_entity_value:
      description: Value of the related entity associated with the task.
      type: string
      example: INV-001
    parent_status:
      description: Parent status of the task.
      type: string
      example: open
    custom_status_id:
      description: ID of the custom status of the task.
      type: string
      example: 982000000567121
    task_rate:
      description: Rate of the task.
      type: number
      format: double
      example: 50
    completion_percentage:
      description: Completion percentage of the task.
      type: integer
      example: 75
    is_subtask:
      description: Indicates whether the task is a subtask.
      type: boolean
      example: false
    priority:
      description: Priority of the task. Allowed values are <code>normal</code>, <code>low</code>,
        <code>high</code>, <code>lowest</code> and <code>highest</code>
      type: string
      example: high
    reminder:
      description: Details of the reminder set for the task.
      type: object
      properties:
        remind_before:
          description: Number of days, months or years before the task due date to
            send a reminder.
          type: integer
          example: 2
        remind_at:
          description: Specific date to send the reminder.
          type: string
          format: date
          example: '2025-12-29'
        remind_at_formatted:
          description: Formatted date to send the reminder.
          type: string
          example: 29-12-2025
        remind_type:
          description: Type of reminder which can be days, months or years.
          type: string
          example: days
        remind_type_formatted:
          description: Formatted type of reminder which can be days, months or years.
          type: string
          example: Days
        alert_type:
          description: Type of alert for the reminder.
          type: string
          example: email
        alert_type_formatted:
          description: Formatted type of alert for the reminder.
          type: string
          example: Email
    send_email_notification:
      description: Indicates whether to send email notification for the task.
      type: boolean
      example: true
    mail_id:
      description: Email ID to which the notification has to be sent.
      type: string
      example: johndoe@gmail.com
    email_account_type:
      description: Email account type to which the notification has to be sent.
      type: string
      example: gmail
    email_folder_id:
      description: Folder ID of the email account to which the notification has to
        be sent.
      type: string
      example: '982000000567126'
    email_account_id:
      description: Email account ID to which the notification has to be sent.
      type: string
      example: '982000000567127'
    mail_attachments:
      description: List of attachments to be sent along with the email notification.
      type: array
      items:
        type: object
        properties:
          attachment_id:
            description: ID of the attachment to be sent along with the email notification.
            type: string
            example: '982000000567128'
          file_name:
            description: Name of the file attached to the email notification.
            type: string
            example: document.pdf
    task_type:
      description: Type of the task.
      type: string
      example: accountant task
    is_sub_task:
      description: Indicates whether the task is a sub task.
      type: boolean
      example: false
    subtask_count:
      description: Number of subtasks under the task.
      type: integer
      example: 3
    related_sub_entity_id:
      description: ID of the related sub entity associated with the task.
      type: string
      example: 982000000567122
    related_sub_entity_type:
      description: Type of the related sub entity associated with the task.
      type: string
      example: expense_report
    client_id:
      description: ID of the client associated with the task.
      type: string
      example: 982000000567118
    client_name:
      description: Name of the client associated with the task.
      type: string
      example: Acme Corporation
    task_description:
      description: Description of the task.
      type: string
      example: This is a sample task description.
    is_accountant_task:
      description: Indicates whether the task is an accountant task.
      type: boolean
      example: false
    task_number:
      description: Unique number assigned to the task.
      type: string
      example: T-001
    task_number_prefix:
      description: Prefix of the task number.
      type: string
      example: T-
    task_number_suffix:
      description: Suffix of the task number.
      type: string
      example: '-001'
    can_push:
      description: Indicates if the task can be sync to client
      type: boolean
      example: true
    due_after:
      description: Number of days after which the task is due.
      type: integer
      example: 7
    parent_task_name:
      description: Name of the parent task.
      type: string
      example: Parent Task
    parent_task_number:
      description: Number of the parent task.
      type: string
      example: PT-001
    created_by_name:
      description: Name of the user who created the task.
      type: string
      example: John Doe
    can_show_in_portal:
      description: Indicates whether the document can be shown in the customer portal.
      type: boolean
      example: true
    attachment_order:
      description: Order of the attachment in the task.
      type: integer
      example: 1
    document:
      description: Details of the document attached to the task.
      type: object
      properties:
        can_show_in_portal:
          $ref: '#/components/schemas/can_show_in_portal'
        file_name:
          $ref: '#/components/schemas/file_name'
        file_type:
          $ref: '#/components/schemas/file_type'
        file_size:
          $ref: '#/components/schemas/file_size'
        file_size_formatted:
          $ref: '#/components/schemas/file_size_formatted'
        document_id:
          $ref: '#/components/schemas/document_id'
        attachment_order:
          $ref: '#/components/schemas/attachment_order'
    statuses:
      description: List of statuses for the task.
      type: array
      items:
        type: string
      example:
      - open
      - closed
      - pending
    is_system_task:
      description: Indicates whether the task is a system task.
      type: boolean
      example: false
    system_task_type:
      description: Type of the system task.
      type: integer
      example: 2
    parent_id:
      description: ID of the parent task.
      type: string
      example: 982000000567114
    entity_tags:
      description: List of tags associated with the task.
      type: array
      items:
        type: object
        properties:
          id:
            description: ID of the tag.
            type: string
            example: 982000000567129
          name:
            description: Name of the tag.
            type: string
            example: Important
          color:
            description: Color code of the tag.
            type: string
            example: FFB6C1
    dependencies:
      description: List of dependencies for the task.
      type: array
      items:
        type: object
        properties:
          dependency_id:
            description: ID of the dependency.
            type: string
            example: 982000000567192
          dependency_type:
            description: Type of the dependency.
            type: integer
            example: 1
          depends_on_id:
            description: ID of the entity on which the current task depends.
            type: string
            example: 982000000567193
          depends_on_entity_type:
            description: Type of the entity on which the current task depends.
            type: integer
            example: 20
    update-tasks-request:
      type: array
      description: List of tasks to be updated.
      items:
        type: object
        properties:
          task_id:
            $ref: '#/components/schemas/task_id'
          owner_ids:
            $ref: '#/components/schemas/owner_ids'
          owner_id:
            $ref: '#/components/schemas/owner_id'
          task_type:
            $ref: '#/components/schemas/task_type'
          title:
            $ref: '#/components/schemas/title'
          due_date:
            $ref: '#/components/schemas/due_date'
          contact_type:
            $ref: '#/components/schemas/contact_type'
          contact_id:
            $ref: '#/components/schemas/contact_id'
          can_push:
            $ref: '#/components/schemas/can_push'
          related_entity_type:
            $ref: '#/components/schemas/related_entity_type'
          related_entity_id:
            $ref: '#/components/schemas/related_entity_id'
          related_sub_entity_type:
            $ref: '#/components/schemas/related_sub_entity_type'
          related_sub_entity_id:
            $ref: '#/components/schemas/related_sub_entity_id'
          status:
            $ref: '#/components/schemas/status'
          billing_type:
            $ref: '#/components/schemas/billing_type'
          is_billable:
            $ref: '#/components/schemas/is_billable'
          task_rate:
            $ref: '#/components/schemas/task_rate'
          completion_percentage:
            $ref: '#/components/schemas/completion_percentage'
          priority:
            $ref: '#/components/schemas/priority'
          send_email_notification:
            $ref: '#/components/schemas/send_email_notification'
          mail_id:
            $ref: '#/components/schemas/mail_id'
          email_account_type:
            $ref: '#/components/schemas/email_account_type'
          email_folder_id:
            $ref: '#/components/schemas/email_folder_id'
          email_account_id:
            $ref: '#/components/schemas/email_account_id'
          mail_attachments:
            $ref: '#/components/schemas/mail_attachments'
          reminder:
            $ref: '#/components/schemas/reminder'
          task_description:
            $ref: '#/components/schemas/task_description'
          custom_fields:
            type: array
            description: List of custom fields associated with the task
            items:
              type: object
              properties:
                customfield_id:
                  description: ID of the custom field.
                  type: string
                  example: 982000000567114
                index:
                  description: Index of the custom field.
                  type: integer
                  example: 1
                label:
                  description: Label of the custom field.
                  type: string
                  example: Custom Field Label
                value:
                  description: Value of the custom field.
                  type: string
                  example: Custom Field Value
                value_formatted:
                  description: Formatted value of the custom field.
                  type: string
                  example: Formatted Custom Field Value
                api_name:
                  description: Name of the custom field.
                  type: string
                  example: CustomFieldAPIName
          due_after:
            $ref: '#/components/schemas/due_after'
          template_id:
            $ref: '#/components/schemas/template_id'
          task_number:
            $ref: '#/components/schemas/task_number'
          task_number_prefix:
            $ref: '#/components/schemas/task_number_prefix'
          task_number_suffix:
            $ref: '#/components/schemas/task_number_suffix'
          recurring_task_id:
            $ref: '#/components/schemas/recurring_task_id'
          is_sub_task:
            $ref: '#/components/schemas/is_sub_task'
          parent_id:
            $ref: '#/components/schemas/parent_id'
          dependencies:
            $ref: '#/components/schemas/dependencies'
          entity_tags:
            description: List of tags to be associated with the task.
            type: array
            items:
              type: object
              properties:
                name:
                  description: Name of the tag.
                  type: string
                  example: Important
                color:
                  description: Color code of the tag.
                  type: string
                  example: FFB6C1
    update-tasks-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task updated
          readOnly: true
        tasks:
          type: array
          description: List of tasks updated.
          items:
            type: object
            properties:
              task_id:
                $ref: '#/components/schemas/task_id'
              recurring_task_id:
                $ref: '#/components/schemas/recurring_task_id'
              owner_id:
                $ref: '#/components/schemas/owner_id'
              owner_ids:
                $ref: '#/components/schemas/owner_ids'
              owner_name:
                $ref: '#/components/schemas/owner_name'
              owners:
                $ref: '#/components/schemas/owners'
              title:
                $ref: '#/components/schemas/title'
              due_date:
                $ref: '#/components/schemas/due_date'
              due_date_formatted:
                $ref: '#/components/schemas/due_date_formatted'
              is_overdue:
                $ref: '#/components/schemas/is_overdue'
              contact_type:
                $ref: '#/components/schemas/contact_type'
              contact_id:
                $ref: '#/components/schemas/contact_id'
              color_code:
                $ref: '#/components/schemas/color_code'
              contact_name:
                $ref: '#/components/schemas/contact_name'
              parent_task_id:
                $ref: '#/components/schemas/parent_task_id'
              related_entity_id:
                $ref: '#/components/schemas/related_entity_id'
              related_entity_type:
                $ref: '#/components/schemas/related_entity_type'
              related_entity:
                $ref: '#/components/schemas/related_entity'
              related_entity_value:
                $ref: '#/components/schemas/related_entity_value'
              parent_status:
                $ref: '#/components/schemas/parent_status'
              status:
                $ref: '#/components/schemas/status'
              custom_status_id:
                $ref: '#/components/schemas/custom_status_id'
              billing_type:
                $ref: '#/components/schemas/billing_type'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              task_rate:
                $ref: '#/components/schemas/task_rate'
              completion_percentage:
                $ref: '#/components/schemas/completion_percentage'
              is_subtask:
                $ref: '#/components/schemas/is_subtask'
              priority:
                $ref: '#/components/schemas/priority'
              reminder:
                $ref: '#/components/schemas/reminder'
              task_description:
                $ref: '#/components/schemas/task_description'
              is_accountant_task:
                $ref: '#/components/schemas/is_accountant_task'
              task_number:
                $ref: '#/components/schemas/task_number'
              task_number_prefix:
                $ref: '#/components/schemas/task_number_prefix'
              task_number_suffix:
                $ref: '#/components/schemas/task_number_suffix'
              can_push:
                $ref: '#/components/schemas/can_push'
              created_by_id:
                $ref: '#/components/schemas/created_by_id'
              last_modified_by_id:
                $ref: '#/components/schemas/last_modified_by_id'
              template_id:
                $ref: '#/components/schemas/template_id'
              due_after:
                $ref: '#/components/schemas/due_after'
              parent_task_name:
                $ref: '#/components/schemas/parent_task_name'
              parent_task_number:
                $ref: '#/components/schemas/parent_task_number'
              created_by_name:
                $ref: '#/components/schemas/created_by_name'
              documents:
                $ref: '#/components/schemas/documents'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
              statuses:
                $ref: '#/components/schemas/statuses'
              is_system_task:
                $ref: '#/components/schemas/is_system_task'
              system_task_type:
                $ref: '#/components/schemas/system_task_type'
              comments:
                $ref: '#/components/schemas/comments'
              dependencies:
                $ref: '#/components/schemas/dependencies'
              entity_tags:
                $ref: '#/components/schemas/entity_tags'
    delete-tasks-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task deleted
          readOnly: true
    delete-a-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task deleted
          readOnly: true
    update-percentage-task-request:
      required:
      - completion_percentage
      type: object
      properties:
        completion_percentage:
          $ref: '#/components/schemas/completion_percentage'
    update-percentage-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task percentage updated
          readOnly: true
        task:
          type: object
          properties:
            task_id:
              $ref: '#/components/schemas/task_id'
            recurring_task_id:
              $ref: '#/components/schemas/recurring_task_id'
            owner_id:
              $ref: '#/components/schemas/owner_id'
            owner_ids:
              $ref: '#/components/schemas/owner_ids'
            owner_name:
              $ref: '#/components/schemas/owner_name'
            owners:
              $ref: '#/components/schemas/owners'
            title:
              $ref: '#/components/schemas/title'
            due_date:
              $ref: '#/components/schemas/due_date'
            due_date_formatted:
              $ref: '#/components/schemas/due_date_formatted'
            is_overdue:
              $ref: '#/components/schemas/is_overdue'
            contact_type:
              $ref: '#/components/schemas/contact_type'
            contact_id:
              $ref: '#/components/schemas/contact_id'
            color_code:
              $ref: '#/components/schemas/color_code'
            contact_name:
              $ref: '#/components/schemas/contact_name'
            parent_task_id:
              $ref: '#/components/schemas/parent_task_id'
            related_entity_id:
              $ref: '#/components/schemas/related_entity_id'
            related_entity_type:
              $ref: '#/components/schemas/related_entity_type'
            related_entity:
              $ref: '#/components/schemas/related_entity'
            related_entity_value:
              $ref: '#/components/schemas/related_entity_value'
            parent_status:
              $ref: '#/components/schemas/parent_status'
            status:
              $ref: '#/components/schemas/status'
            custom_status_id:
              $ref: '#/components/schemas/custom_status_id'
            billing_type:
              $ref: '#/components/schemas/billing_type'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            task_rate:
              $ref: '#/components/schemas/task_rate'
            completion_percentage:
              $ref: '#/components/schemas/completion_percentage'
            is_subtask:
              $ref: '#/components/schemas/is_subtask'
            priority:
              $ref: '#/components/schemas/priority'
            reminder:
              $ref: '#/components/schemas/reminder'
            task_description:
              $ref: '#/components/schemas/task_description'
            is_accountant_task:
              $ref: '#/components/schemas/is_accountant_task'
            task_number:
              $ref: '#/components/schemas/task_number'
            task_number_prefix:
              $ref: '#/components/schemas/task_number_prefix'
            task_number_suffix:
              $ref: '#/components/schemas/task_number_suffix'
            can_push:
              $ref: '#/components/schemas/can_push'
            created_by_id:
              $ref: '#/components/schemas/created_by_id'
            last_modified_by_id:
              $ref: '#/components/schemas/last_modified_by_id'
            template_id:
              $ref: '#/components/schemas/template_id'
            due_after:
              $ref: '#/components/schemas/due_after'
            parent_task_name:
              $ref: '#/components/schemas/parent_task_name'
            parent_task_number:
              $ref: '#/components/schemas/parent_task_number'
            created_by_name:
              $ref: '#/components/schemas/created_by_name'
            documents:
              $ref: '#/components/schemas/documents'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
            statuses:
              $ref: '#/components/schemas/statuses'
            is_system_task:
              $ref: '#/components/schemas/is_system_task'
            system_task_type:
              $ref: '#/components/schemas/system_task_type'
            comments:
              $ref: '#/components/schemas/comments'
            dependencies:
              $ref: '#/components/schemas/dependencies'
            entity_tags:
              $ref: '#/components/schemas/entity_tags'
    mark-open-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task status updated
          readOnly: true
    mark-ongoing-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task status updated
          readOnly: true
    mark-completed-task-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Task status updated
          readOnly: true
    add-task-comment-request:
      required:
      - description
      type: object
      properties:
        description:
          description: Description of the comment.
          type: string
          example: This is a sample comment.
    add-task-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comments added
          readOnly: true
        comment:
          $ref: '#/components/schemas/comment'
    list-task-comments-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        comments:
          $ref: '#/components/schemas/comments'
    delete-task-comment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Comments deleted
          readOnly: true
    add-task-attachment-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: File uploaded
          readOnly: true
        documents:
          $ref: '#/components/schemas/documents'
    get-a-task-document-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        documents:
          $ref: '#/components/schemas/document'
    delete-task-document-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Attachment deleted
          readOnly: true
    time_entry-response:
      type: object
      properties:
        time_entry_id:
          $ref: '#/components/schemas/time_entry_id'
        project_id:
          $ref: '#/components/schemas/project_id'
        project_name:
          $ref: '#/components/schemas/project_name'
        customer_id:
          $ref: '#/components/schemas/customer_id'
        customer_name:
          $ref: '#/components/schemas/customer_name'
        task_id:
          $ref: '#/components/schemas/task_id'
        task_name:
          $ref: '#/components/schemas/task_name'
        user_id:
          $ref: '#/components/schemas/user_id'
        user_name:
          $ref: '#/components/schemas/user_name'
        is_current_user:
          $ref: '#/components/schemas/is_current_user'
        log_date:
          $ref: '#/components/schemas/log_date'
        begin_time:
          $ref: '#/components/schemas/begin_time'
        end_time:
          $ref: '#/components/schemas/end_time'
        log_time:
          $ref: '#/components/schemas/log_time'
        is_billable:
          $ref: '#/components/schemas/is_billable'
        billed_status:
          $ref: '#/components/schemas/billed_status'
        invoice_id:
          $ref: '#/components/schemas/invoice_id'
        notes:
          $ref: '#/components/schemas/notes'
        timer_started_at:
          $ref: '#/components/schemas/timer_started_at'
        timer_started_at_utc_time:
          $ref: '#/components/schemas/timer_started_at_utc_time'
        timer_duration_in_minutes:
          $ref: '#/components/schemas/timer_duration_in_minutes'
        timer_duration_in_seconds:
          $ref: '#/components/schemas/timer_duration_in_seconds'
        created_time:
          $ref: '#/components/schemas/created_time'
        timesheet_custom_fields:
          $ref: '#/components/schemas/timesheet_custom_fields'
    time_entry_id:
      type: string
      example: '460000000044021'
    log_date:
      description: Date on which the user spent on the task. <code>Date-Format [HH:mm]</code>
      type: string
      example: '2013-09-17'
    begin_time:
      type: string
      example: 03:00
    end_time:
      type: string
      example: 04:00
    billed_status:
      type: string
      example: unbilled
    start_timer:
      type: string
      example: ' '
    timer_started_at:
      type: string
      example: ' '
    timer_duration_in_minutes:
      type: string
      example: ' '
    cost_amount:
      description: Cost amount
      type: number
      format: double
      example: 0
    timer_started_at_utc_time:
      type: string
      example: ''
    timer_duration_in_seconds:
      type: string
      example: ''
    timesheet_custom_fields:
      type: string
      example: ''
    log-time-entries-request:
      required:
      - project_id
      - task_id
      - user_id
      - log_date
      type: object
      properties:
        project_id:
          description: ID of the project.
          type: string
          example: Network Distribution
        task_id:
          description: ID of the task.
          type: string
          example: '460000000044001'
        user_id:
          description: ID of the user.
          type: string
          example: '460000000024003'
        log_date:
          description: Date on which the user spent on the task. <code>Date-Format
            [HH:mm]</code>
          type: string
          example: '2013-09-17'
        log_time:
          description: Time the user spent on this task. Either send this attribute
            or begin and end time attributes. <code>Time-Format [HH:mm]</code>
          type: string
          example: ' '
        begin_time:
          description: Time the user started working on this task. <code>Time-Format
            [HH:mm]</code>
          type: string
          example: '10:00'
        end_time:
          description: Time the user stopped working on this task. <code>Time-Format
            [HH:mm]</code>
          type: string
          example: '15:00'
        is_billable:
          description: Whether it is billable or not.
          type: boolean
          example: true
        notes:
          description: Description of the work done. <code>Max-length [500]</code>
          type: string
          example: ' '
        start_timer:
          description: Start timer.
          type: string
          example: ' '
        cost_rate:
          description: Hourly cost rate
          type: number
          format: double
          example: 10
    log-time-entries-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your timesheet entry has been added.
          readOnly: true
        time_entry:
          $ref: '#/components/schemas/time_entry-response'
    list-time-entries-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        time_entries:
          type: array
          items:
            type: object
            properties:
              time_entry_id:
                $ref: '#/components/schemas/time_entry_id'
              project_id:
                $ref: '#/components/schemas/project_id'
              project_name:
                $ref: '#/components/schemas/project_name'
              customer_id:
                $ref: '#/components/schemas/customer_id'
              customer_name:
                $ref: '#/components/schemas/customer_name'
              task_id:
                $ref: '#/components/schemas/task_id'
              task_name:
                $ref: '#/components/schemas/task_name'
              user_id:
                $ref: '#/components/schemas/user_id'
              is_current_user:
                $ref: '#/components/schemas/is_current_user'
              user_name:
                $ref: '#/components/schemas/user_name'
              log_date:
                $ref: '#/components/schemas/log_date'
              begin_time:
                $ref: '#/components/schemas/begin_time'
              end_time:
                $ref: '#/components/schemas/end_time'
              log_time:
                $ref: '#/components/schemas/log_time'
              is_billable:
                $ref: '#/components/schemas/is_billable'
              billed_status:
                $ref: '#/components/schemas/billed_status'
              invoice_id:
                $ref: '#/components/schemas/invoice_id'
              notes:
                $ref: '#/components/schemas/notes'
              timer_started_at:
                $ref: '#/components/schemas/timer_started_at'
              timer_duration_in_minutes:
                $ref: '#/components/schemas/timer_duration_in_minutes'
              created_time:
                $ref: '#/components/schemas/created_time'
              cost_rate:
                $ref: '#/components/schemas/cost_rate'
              cost_amount:
                $ref: '#/components/schemas/cost_amount'
        page_context:
          type: array
          items:
            type: object
            properties:
              page:
                $ref: '#/components/schemas/page'
              per_page:
                $ref: '#/components/schemas/per_page'
              report_name:
                $ref: '#/components/schemas/report_name'
              has_more_page:
                $ref: '#/components/schemas/has_more_page'
              sort_order:
                $ref: '#/components/schemas/sort_order'
              sort_column:
                $ref: '#/components/schemas/sort_column'
    delete-time-entries-request:
      required:
      - time_entry_ids
      type: object
      properties:
        time_entry_ids:
          $ref: '#/components/schemas/time_entry_ids'
    delete-time-entries-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The selected timesheet entries have been deleted
          readOnly: true
    update-time-entry-request:
      required:
      - project_id
      - task_id
      - user_id
      - log_date
      type: object
      properties:
        project_id:
          description: ID of the project.
          type: string
          example: Network Distribution
        task_id:
          description: ID of the task.
          type: string
          example: '460000000044001'
        user_id:
          description: ID of the user.
          type: string
          example: '460000000024003'
        log_date:
          description: Date on which the user spent on the task. <code>Date-Format
            [HH:mm]</code>
          type: string
          example: '2013-09-17'
        log_time:
          description: Time the user spent on this task. Either send this attribute
            or begin and end time attributes. <code>Time-Format [HH:mm]</code>
          type: string
          example: ' '
        begin_time:
          description: Time the user started working on this task. <code>Time-Format
            [HH:mm]</code>
          type: string
          example: '10:00'
        end_time:
          description: Time the user stopped working on this task. <code>Time-Format
            [HH:mm]</code>
          type: string
          example: '15:00'
        is_billable:
          description: Whether it is billable or not.
          type: boolean
          example: true
        notes:
          description: Description of the work done. <code>Max-length [500]</code>
          type: string
          example: ' '
        start_timer:
          description: Start timer.
          type: string
          example: ' '
        cost_rate:
          description: Hourly cost rate
          type: number
          format: double
          example: 0
    update-time-entry-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The timesheet's information has been updated.
          readOnly: true
        time_entry:
          type: object
          properties:
            time_entry_id:
              $ref: '#/components/schemas/time_entry_id'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            task_id:
              $ref: '#/components/schemas/task_id'
            task_name:
              $ref: '#/components/schemas/task_name'
            user_id:
              $ref: '#/components/schemas/user_id'
            user_name:
              $ref: '#/components/schemas/user_name'
            is_current_user:
              $ref: '#/components/schemas/is_current_user'
            log_date:
              $ref: '#/components/schemas/log_date'
            begin_time:
              $ref: '#/components/schemas/begin_time'
            end_time:
              $ref: '#/components/schemas/end_time'
            log_time:
              $ref: '#/components/schemas/log_time'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            billed_status:
              $ref: '#/components/schemas/billed_status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            notes:
              $ref: '#/components/schemas/notes'
            timer_started_at:
              $ref: '#/components/schemas/timer_started_at'
            timer_started_at_utc_time:
              $ref: '#/components/schemas/timer_started_at_utc_time'
            timer_duration_in_minutes:
              $ref: '#/components/schemas/timer_duration_in_minutes'
            timer_duration_in_seconds:
              $ref: '#/components/schemas/timer_duration_in_seconds'
            created_time:
              $ref: '#/components/schemas/created_time'
            timesheet_custom_fields:
              $ref: '#/components/schemas/timesheet_custom_fields'
    get-a-time-entry-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        time_entry:
          type: object
          properties:
            time_entry_id:
              $ref: '#/components/schemas/time_entry_id'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            task_id:
              $ref: '#/components/schemas/task_id'
            task_name:
              $ref: '#/components/schemas/task_name'
            user_id:
              $ref: '#/components/schemas/user_id'
            user_name:
              $ref: '#/components/schemas/user_name'
            is_current_user:
              $ref: '#/components/schemas/is_current_user'
            log_date:
              $ref: '#/components/schemas/log_date'
            begin_time:
              $ref: '#/components/schemas/begin_time'
            end_time:
              $ref: '#/components/schemas/end_time'
            log_time:
              $ref: '#/components/schemas/log_time'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            billed_status:
              $ref: '#/components/schemas/billed_status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            notes:
              $ref: '#/components/schemas/notes'
            timer_started_at:
              $ref: '#/components/schemas/timer_started_at'
            timer_started_at_utc_time:
              $ref: '#/components/schemas/timer_started_at_utc_time'
            timer_duration_in_minutes:
              $ref: '#/components/schemas/timer_duration_in_minutes'
            timer_duration_in_seconds:
              $ref: '#/components/schemas/timer_duration_in_seconds'
            created_time:
              $ref: '#/components/schemas/created_time'
            timesheet_custom_fields:
              $ref: '#/components/schemas/timesheet_custom_fields'
            cost_rate:
              $ref: '#/components/schemas/cost_rate'
    delete-time-entry-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The time entry has been deleted.
          readOnly: true
    start-timer-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The timer has been started.
          readOnly: true
        time_entry:
          type: object
          properties:
            time_entry_id:
              $ref: '#/components/schemas/time_entry_id'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            task_id:
              $ref: '#/components/schemas/task_id'
            task_name:
              $ref: '#/components/schemas/task_name'
            user_id:
              $ref: '#/components/schemas/user_id'
            user_name:
              $ref: '#/components/schemas/user_name'
            is_current_user:
              $ref: '#/components/schemas/is_current_user'
            log_date:
              $ref: '#/components/schemas/log_date'
            begin_time:
              $ref: '#/components/schemas/begin_time'
            end_time:
              $ref: '#/components/schemas/end_time'
            log_time:
              $ref: '#/components/schemas/log_time'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            billed_status:
              $ref: '#/components/schemas/billed_status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            notes:
              $ref: '#/components/schemas/notes'
            timer_started_at:
              $ref: '#/components/schemas/timer_started_at'
            timer_started_at_utc_time:
              $ref: '#/components/schemas/timer_started_at_utc_time'
            timer_duration_in_minutes:
              $ref: '#/components/schemas/timer_duration_in_minutes'
            timer_duration_in_seconds:
              $ref: '#/components/schemas/timer_duration_in_seconds'
            created_time:
              $ref: '#/components/schemas/created_time'
            timesheet_custom_fields:
              $ref: '#/components/schemas/timesheet_custom_fields'
    stop-timer-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Timer has been stopped successfully.
          readOnly: true
        time_entry:
          type: object
          properties:
            time_entry_id:
              $ref: '#/components/schemas/time_entry_id'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            task_id:
              $ref: '#/components/schemas/task_id'
            task_name:
              $ref: '#/components/schemas/task_name'
            user_id:
              $ref: '#/components/schemas/user_id'
            user_name:
              $ref: '#/components/schemas/user_name'
            is_current_user:
              $ref: '#/components/schemas/is_current_user'
            log_date:
              $ref: '#/components/schemas/log_date'
            begin_time:
              $ref: '#/components/schemas/begin_time'
            end_time:
              $ref: '#/components/schemas/end_time'
            log_time:
              $ref: '#/components/schemas/log_time'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            billed_status:
              $ref: '#/components/schemas/billed_status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            notes:
              $ref: '#/components/schemas/notes'
            timer_started_at:
              $ref: '#/components/schemas/timer_started_at'
            timer_started_at_utc_time:
              $ref: '#/components/schemas/timer_started_at_utc_time'
            timer_duration_in_minutes:
              $ref: '#/components/schemas/timer_duration_in_minutes'
            timer_duration_in_seconds:
              $ref: '#/components/schemas/timer_duration_in_seconds'
            created_time:
              $ref: '#/components/schemas/created_time'
            timesheet_custom_fields:
              $ref: '#/components/schemas/timesheet_custom_fields'
    get-timer-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        time_entry:
          type: object
          properties:
            time_entry_id:
              $ref: '#/components/schemas/time_entry_id'
            project_id:
              $ref: '#/components/schemas/project_id'
            project_name:
              $ref: '#/components/schemas/project_name'
            customer_id:
              $ref: '#/components/schemas/customer_id'
            customer_name:
              $ref: '#/components/schemas/customer_name'
            task_id:
              $ref: '#/components/schemas/task_id'
            task_name:
              $ref: '#/components/schemas/task_name'
            user_id:
              $ref: '#/components/schemas/user_id'
            user_name:
              $ref: '#/components/schemas/user_name'
            is_current_user:
              $ref: '#/components/schemas/is_current_user'
            log_date:
              $ref: '#/components/schemas/log_date'
            begin_time:
              $ref: '#/components/schemas/begin_time'
            end_time:
              $ref: '#/components/schemas/end_time'
            log_time:
              $ref: '#/components/schemas/log_time'
            is_billable:
              $ref: '#/components/schemas/is_billable'
            billed_status:
              $ref: '#/components/schemas/billed_status'
            invoice_id:
              $ref: '#/components/schemas/invoice_id'
            notes:
              $ref: '#/components/schemas/notes'
            timer_started_at:
              $ref: '#/components/schemas/timer_started_at'
            timer_started_at_utc_time:
              $ref: '#/components/schemas/timer_started_at_utc_time'
            timer_duration_in_minutes:
              $ref: '#/components/schemas/timer_duration_in_minutes'
            timer_duration_in_seconds:
              $ref: '#/components/schemas/timer_duration_in_seconds'
            created_time:
              $ref: '#/components/schemas/created_time'
            timesheet_custom_fields:
              $ref: '#/components/schemas/timesheet_custom_fields'
    item-response:
      type: object
      properties:
        item_id:
          $ref: '#/components/schemas/item_id'
        name:
          $ref: '#/components/schemas/name'
        status:
          $ref: '#/components/schemas/status'
        description:
          $ref: '#/components/schemas/description'
        rate:
          $ref: '#/components/schemas/rate'
        unit:
          $ref: '#/components/schemas/unit'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        purchase_tax_rule_id:
          $ref: '#/components/schemas/purchase_tax_rule_id'
        sales_tax_rule_id:
          $ref: '#/components/schemas/sales_tax_rule_id'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        sat_item_key_code:
          $ref: '#/components/schemas/sat_item_key_code'
        unitkey_code:
          $ref: '#/components/schemas/unitkey_code'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        tax_type:
          $ref: '#/components/schemas/tax_type'
        sku:
          $ref: '#/components/schemas/sku'
        product_type:
          $ref: '#/components/schemas/product_type'
        item_tax_preferences:
          $ref: '#/components/schemas/item_tax_preferences'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
        locations:
          $ref: '#/components/schemas/locations'
    purchase_tax_rule_id:
      description: 'Id of the purchase tax rule. Note: This value is a set of numeric
        characters.'
      type: string
      example: 127919000000106780
      x-node_available_in:
      - mx
      - ke
      - za
      x-node_unavailable_in: []
    sales_tax_rule_id:
      description: 'Id of the sales tax rule. Note: This value is a set of numeric
        characters.'
      type: string
      example: 127919000000106780
      x-node_available_in:
      - mx
      - ke
      - za
      x-node_unavailable_in: []
    tax_precentage:
      type: string
      example: 10.5
      x-node_available_in: []
      x-node_unavailable_in:
      - us
    purchase_tax_exemption_id:
      description: 'ID of the purchase tax exemption. Mandatory, if <code>is_taxable</code>
        is false. Note: This value is a set of numeric characters.'
      type: string
      example: '123456789'
      x-node_available_in:
      - ke
      - za
      x-node_unavailable_in: []
    purchase_description:
      description: Purchase description for the item.
      type: string
      example: ' '
    purchase_rate:
      description: Purchase price of the item.
      type: string
      example: ' '
    purchase_account_id:
      description: 'ID of the COGS account to which the item has to be associated
        with. Mandatory, if item_type is purchase / sales and purchase / inventory.
        Note: This value is a set of numeric characters.'
      type: string
      example: ' '
    inventory_account_id:
      description: 'ID of the stock account to which the item has to be associated
        with. Mandatory, if item_type is inventory. Note: This value is a set of numeric
        characters.'
      type: string
      example: ' '
    reorder_level:
      description: Reorder level of the item.
      type: string
      example: ' '
    initial_stock:
      description: Opening stock of the item.
      type: string
      example: ' '
    initial_stock_rate:
      description: Unit price of the opening stock.
      type: string
      example: ' '
    tax_specification:
      description: Set whether the tax type is intra/interstate
      type: string
      example: intra
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    item_tax_preferences:
      type: array
      description: Tax preferences for the item.
      items:
        type: object
        properties:
          tax_id:
            description: 'ID of the tax to be associated to the item. Note: This value
              is a set of numeric characters.'
            type: string
            example: 982000000037049
            x-node_available_in:
            - in
            x-node_unavailable_in: []
          tax_specification:
            $ref: '#/components/schemas/tax_specification'
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    location_stock_on_hand:
      description: Current available stock in your location.
      type: string
      example: ''
    location_available_stock:
      description: Available stock in your location.
      type: string
      example: ''
    location_actual_available_stock:
      description: Actual available stock in your location.
      type: string
      example: ''
    locations:
      type: object
      properties:
        address:
          $ref: '#/components/schemas/address'
        email:
          $ref: '#/components/schemas/email'
        is_primary:
          $ref: '#/components/schemas/is_primary'
        phone:
          $ref: '#/components/schemas/phone'
        status:
          $ref: '#/components/schemas/status'
        location_id:
          $ref: '#/components/schemas/location_id'
        location_name:
          $ref: '#/components/schemas/location_name'
        type:
          $ref: '#/components/schemas/type'
        parent_location_id:
          $ref: '#/components/schemas/parent_location_id'
        associated_series_ids:
          $ref: '#/components/schemas/associated_series_ids'
        auto_number_generation_id:
          $ref: '#/components/schemas/autonumbergenerationgroup_id'
        associated_users:
          type: array
          items:
            type: object
            properties:
              user_id:
                $ref: '#/components/schemas/user_id'
              user_name:
                $ref: '#/components/schemas/user_name'
        tax_settings_id:
          $ref: '#/components/schemas/tax_settings_id'
    create-an-item-request:
      required:
      - name
      - rate
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        rate:
          $ref: '#/components/schemas/rate'
        description:
          $ref: '#/components/schemas/description'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        locations:
          type: array
          description: Locations for the item.
          items:
            type: object
            properties:
              location_id:
                $ref: '#/components/schemas/location_id'
              initial_stock:
                $ref: '#/components/schemas/initial_stock'
              initial_stock_rate:
                $ref: '#/components/schemas/initial_stock_rate'
        purchase_tax_rule_id:
          $ref: '#/components/schemas/purchase_tax_rule_id'
        sales_tax_rule_id:
          $ref: '#/components/schemas/sales_tax_rule_id'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        sku:
          $ref: '#/components/schemas/sku'
        product_type:
          $ref: '#/components/schemas/product_type'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        sat_item_key_code:
          $ref: '#/components/schemas/sat_item_key_code'
        unitkey_code:
          $ref: '#/components/schemas/unitkey_code'
        is_taxable:
          $ref: '#/components/schemas/is_taxable'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        purchase_tax_exemption_id:
          $ref: '#/components/schemas/purchase_tax_exemption_id'
        account_id:
          $ref: '#/components/schemas/account_id'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        item_type:
          $ref: '#/components/schemas/item_type'
        purchase_description:
          $ref: '#/components/schemas/purchase_description'
        purchase_rate:
          $ref: '#/components/schemas/purchase_rate'
        purchase_account_id:
          $ref: '#/components/schemas/purchase_account_id'
        inventory_account_id:
          $ref: '#/components/schemas/inventory_account_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        reorder_level:
          $ref: '#/components/schemas/reorder_level'
        item_tax_preferences:
          $ref: '#/components/schemas/item_tax_preferences'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    create-an-item-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The item has been added.
          readOnly: true
        item:
          $ref: '#/components/schemas/item-response'
    list-items-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        items:
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              status:
                $ref: '#/components/schemas/status'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              unit:
                $ref: '#/components/schemas/unit'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              purchase_tax_rule_id:
                $ref: '#/components/schemas/purchase_tax_rule_id'
              sales_tax_rule_id:
                $ref: '#/components/schemas/sales_tax_rule_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              sku:
                $ref: '#/components/schemas/sku'
              product_type:
                $ref: '#/components/schemas/product_type'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
        page_context:
          description: The context of a particular page.
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    bulk-fetch-item-details-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        items:
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              name:
                $ref: '#/components/schemas/name'
              status:
                $ref: '#/components/schemas/status'
              description:
                $ref: '#/components/schemas/description'
              rate:
                $ref: '#/components/schemas/rate'
              unit:
                $ref: '#/components/schemas/unit'
              tax_id:
                $ref: '#/components/schemas/tax_id'
              purchase_tax_rule_id:
                $ref: '#/components/schemas/purchase_tax_rule_id'
              sales_tax_rule_id:
                $ref: '#/components/schemas/sales_tax_rule_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              sku:
                $ref: '#/components/schemas/sku'
              product_type:
                $ref: '#/components/schemas/product_type'
              sat_item_key_code:
                $ref: '#/components/schemas/sat_item_key_code'
              unitkey_code:
                $ref: '#/components/schemas/unitkey_code'
              custom_fields:
                $ref: '#/components/schemas/custom_fields'
        page_context:
          description: The context of a particular page.
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    update-an-item-request:
      required:
      - name
      - rate
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        rate:
          $ref: '#/components/schemas/rate'
        description:
          $ref: '#/components/schemas/description'
        tax_id:
          $ref: '#/components/schemas/tax_id'
        purchase_tax_rule_id:
          $ref: '#/components/schemas/purchase_tax_rule_id'
        sales_tax_rule_id:
          $ref: '#/components/schemas/sales_tax_rule_id'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        hsn_or_sac:
          $ref: '#/components/schemas/hsn_or_sac'
        sat_item_key_code:
          $ref: '#/components/schemas/sat_item_key_code'
        unitkey_code:
          $ref: '#/components/schemas/unitkey_code'
        sku:
          $ref: '#/components/schemas/sku'
        product_type:
          $ref: '#/components/schemas/product_type'
        is_taxable:
          $ref: '#/components/schemas/is_taxable'
        tax_exemption_id:
          $ref: '#/components/schemas/tax_exemption_id'
        purchase_tax_exemption_id:
          $ref: '#/components/schemas/purchase_tax_exemption_id'
        account_id:
          $ref: '#/components/schemas/account_id'
        avatax_tax_code:
          $ref: '#/components/schemas/avatax_tax_code'
        avatax_use_code:
          $ref: '#/components/schemas/avatax_use_code'
        item_type:
          $ref: '#/components/schemas/item_type'
        purchase_description:
          $ref: '#/components/schemas/purchase_description'
        purchase_rate:
          $ref: '#/components/schemas/purchase_rate'
        purchase_account_id:
          $ref: '#/components/schemas/purchase_account_id'
        inventory_account_id:
          $ref: '#/components/schemas/inventory_account_id'
        vendor_id:
          $ref: '#/components/schemas/vendor_id'
        reorder_level:
          $ref: '#/components/schemas/reorder_level'
        locations:
          type: array
          description: Locations for the item.
          items:
            type: object
            properties:
              location_id:
                $ref: '#/components/schemas/location_id'
              initial_stock:
                $ref: '#/components/schemas/initial_stock'
              initial_stock_rate:
                $ref: '#/components/schemas/initial_stock_rate'
        item_tax_preferences:
          $ref: '#/components/schemas/item_tax_preferences'
        custom_fields:
          $ref: '#/components/schemas/custom_fields'
    update-an-item-customfield-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Custom Fields Updated Successfully
          readOnly: true
    update-an-item-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Item details have been saved.
          readOnly: true
        item:
          type: object
          properties:
            item_id:
              $ref: '#/components/schemas/item_id'
            name:
              $ref: '#/components/schemas/name'
            status:
              $ref: '#/components/schemas/status'
            description:
              $ref: '#/components/schemas/description'
            rate:
              $ref: '#/components/schemas/rate'
            unit:
              $ref: '#/components/schemas/unit'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            sat_item_key_code:
              $ref: '#/components/schemas/sat_item_key_code'
            unitkey_code:
              $ref: '#/components/schemas/unitkey_code'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            purchase_tax_rule_id:
              $ref: '#/components/schemas/purchase_tax_rule_id'
            sales_tax_rule_id:
              $ref: '#/components/schemas/sales_tax_rule_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            tax_type:
              $ref: '#/components/schemas/tax_type'
            sku:
              $ref: '#/components/schemas/sku'
            product_type:
              $ref: '#/components/schemas/product_type'
            item_tax_preferences:
              $ref: '#/components/schemas/item_tax_preferences'
            locations:
              $ref: '#/components/schemas/locations'
    get-an-item-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        item:
          type: object
          properties:
            item_id:
              $ref: '#/components/schemas/item_id'
            name:
              $ref: '#/components/schemas/name'
            status:
              $ref: '#/components/schemas/status'
            description:
              $ref: '#/components/schemas/description'
            rate:
              $ref: '#/components/schemas/rate'
            unit:
              $ref: '#/components/schemas/unit'
            hsn_or_sac:
              $ref: '#/components/schemas/hsn_or_sac'
            sat_item_key_code:
              $ref: '#/components/schemas/sat_item_key_code'
            unitkey_code:
              $ref: '#/components/schemas/unitkey_code'
            tax_id:
              $ref: '#/components/schemas/tax_id'
            purchase_tax_rule_id:
              $ref: '#/components/schemas/purchase_tax_rule_id'
            sales_tax_rule_id:
              $ref: '#/components/schemas/sales_tax_rule_id'
            tax_name:
              $ref: '#/components/schemas/tax_name'
            tax_percentage:
              $ref: '#/components/schemas/tax_percentage'
            tax_type:
              $ref: '#/components/schemas/tax_type'
            sku:
              $ref: '#/components/schemas/sku'
            product_type:
              $ref: '#/components/schemas/product_type'
            item_tax_preferences:
              $ref: '#/components/schemas/item_tax_preferences'
            locations:
              $ref: '#/components/schemas/locations'
    delete-an-item-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The item has been deleted.
          readOnly: true
    role_id:
      type: string
      description: 'Unique identifier of the role assigned to the user. Note: This
        value is a set of numeric characters.'
      example: '982000000006005'
    is_selected:
      type: boolean
      description: Indicates whether the email is selected as primary email or not.
      example: true
    email_ids:
      type: array
      items:
        type: string
    is_customer_segmented:
      type: boolean
      description: Indicates whether the user is segmented as customer.
      example: false
    is_vendor_segmented:
      type: boolean
      description: Indicates whether the user is segmented as vendor.
      example: false
    user_type:
      type: string
      description: Type of the user.
      example: zoho
    is_employee:
      type: boolean
      description: Indicates whether the user is an employee.
      example: true
    create-a-user-request:
      required:
      - name
      - email
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        email:
          $ref: '#/components/schemas/email'
        role_id:
          $ref: '#/components/schemas/role_id'
        cost_rate:
          $ref: '#/components/schemas/cost_rate'
    create-a-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your invitation has been sent.
          readOnly: true
    update-a-user-request:
      required:
      - name
      - email
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        email:
          $ref: '#/components/schemas/email'
        role_id:
          $ref: '#/components/schemas/role_id'
        cost_rate:
          $ref: '#/components/schemas/cost_rate'
    update-a-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The user information has been updated.
          readOnly: true
    delete-a-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The user has been removed from your organization.
          readOnly: true
    get-current-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        user:
          type: object
          properties:
            user_id:
              $ref: '#/components/schemas/user_id'
            name:
              $ref: '#/components/schemas/name'
            email_ids:
              description: EmailIDs of an user
              type: array
              items:
                type: object
                properties:
                  email:
                    $ref: '#/components/schemas/email'
                  is_selected:
                    $ref: '#/components/schemas/is_selected'
            status:
              $ref: '#/components/schemas/status'
            user_role:
              $ref: '#/components/schemas/user_role'
            user_type:
              $ref: '#/components/schemas/user_type'
            role_id:
              $ref: '#/components/schemas/role_id'
            cost_rate:
              $ref: '#/components/schemas/cost_rate'
            photo_url:
              $ref: '#/components/schemas/photo_url'
            is_employee:
              $ref: '#/components/schemas/is_employee'
            created_time:
              $ref: '#/components/schemas/created_time'
            custom_fields:
              $ref: '#/components/schemas/custom_fields'
    invite-a-user-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Your invitation has been sent.
          readOnly: true
    mark-user-as-active-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The user has been marked as active.
          readOnly: true
    mark-user-as-inactive-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The user has been marked as inactive.
          readOnly: true
    decimal_place:
      description: Decimal place for pricebook.
      type: integer
      enum:
      - 1
      - 2
      - 3
      example: 2
    is_default:
      description: 'To check the default pricebook.Allowed values: <code>true</code>,<code>false</code>'
      type: boolean
      example: true
    is_increase:
      description: 'Mark up or Mark down to discounts.Allowed values: <code>true</code>,<code>false</code>'
      type: boolean
      example: true
    percentage:
      description: About percentage of discounts
      type: number
      format: double
      example: 4
    pricebook_type:
      description: 'Type of the pricebook.Allowed values: <code>per_item</code>,<code>fixed_percentage</code>'
      type: string
      example: fixed_percentage
    pricing_scheme:
      description: 'Applicable only when the price book type is per_item. Allowed
        values: unit, volume. unit: Uses pricebook_rate. volume: Uses price_brackets.
        Not applicable for fixed_percentage.'
      type: string
      enum:
      - unit
      - volume
      example: unit
    rounding_type:
      description: 'Type of the rounding.Allowed values: <code>no_rounding</code>,<code>round_to_dollor</code>,<code>round_to_dollar_minus_01</code>,<code>round_to_half_dollar</code>,<code>round_to_half_dollar_minus_01</code>'
      type: string
      example: round_to_dollar_minus_01
    sales_or_purchase_type:
      description: 'Whether its sales or purchase type.Allowed values: <code>sales</code>,<code>purchases</code>'
      type: string
      example: sales
    pricebook_rate:
      description: Rate of the price book for the Items
      type: number
      format: double
      example: 22
    pricebook_item_id:
      description: Unique ID generated by the server for each pricebook line item
      type: string
      example: 17775000000227544
    pricebook_discount:
      description: 'Discount percentage for the line item or volume bracket. The value
        must include a percent sign. Examples: 5%, 10.5%, 0%.'
      type: string
      example: 5%
    start_quantity:
      description: Lower bound of the quantity range for a volume bracket.
      type: number
      format: double
    end_quantity:
      description: Upper bound of the quantity range for a volume bracket.
      type: number
      format: double
    price_bracket:
      description: 'Represents a single tier in price_brackets for volume pricing.
        Includes: start_quantity, end_quantity, pricebook_rate, and optional pricebook_discount.'
      type: object
      properties:
        pricebook_item_id:
          $ref: '#/components/schemas/pricebook_item_id'
        start_quantity:
          $ref: '#/components/schemas/start_quantity'
        end_quantity:
          $ref: '#/components/schemas/end_quantity'
        pricebook_rate:
          $ref: '#/components/schemas/pricebook_rate'
        pricebook_discount:
          $ref: '#/components/schemas/pricebook_discount'
    pricebook_items:
      description: Items included in the price book. Applicable when the price book
        type is set to per_item.
      type: array
      items:
        type: object
        properties:
          item_id:
            $ref: '#/components/schemas/item_id'
          pricebook_rate:
            $ref: '#/components/schemas/pricebook_rate'
          pricebook_item_id:
            $ref: '#/components/schemas/pricebook_item_id'
          pricebook_discount:
            $ref: '#/components/schemas/pricebook_discount'
          pricing_scheme:
            $ref: '#/components/schemas/pricing_scheme'
          price_brackets:
            description: Collection of volume pricing tiers for a line item when pricing_scheme
              is volume.
            type: array
            items:
              $ref: '#/components/schemas/price_bracket'
    pricebook:
      description: List all the pricebook details
      type: object
      properties:
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        decimal_place:
          $ref: '#/components/schemas/decimal_place'
        description:
          $ref: '#/components/schemas/description'
        is_default:
          $ref: '#/components/schemas/is_default'
        is_increase:
          $ref: '#/components/schemas/is_increase'
        name:
          $ref: '#/components/schemas/name'
        percentage:
          $ref: '#/components/schemas/percentage'
        pricebook_rate:
          $ref: '#/components/schemas/pricebook_rate'
        pricebook_id:
          $ref: '#/components/schemas/pricebook_id'
        pricebook_items:
          $ref: '#/components/schemas/pricebook_items'
        pricebook_type:
          $ref: '#/components/schemas/pricebook_type'
        pricing_scheme:
          $ref: '#/components/schemas/pricing_scheme'
        rounding_type:
          $ref: '#/components/schemas/rounding_type'
        sales_or_purchase_type:
          $ref: '#/components/schemas/sales_or_purchase_type'
        status:
          $ref: '#/components/schemas/status'
    create-a-pricebook-request:
      required:
      - name
      - pricebook_type
      - sales_or_purchase_type
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        description:
          $ref: '#/components/schemas/description'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        pricebook_type:
          $ref: '#/components/schemas/pricebook_type'
        percentage:
          $ref: '#/components/schemas/percentage'
        pricebook_rate:
          $ref: '#/components/schemas/pricebook_rate'
        pricing_scheme:
          $ref: '#/components/schemas/pricing_scheme'
        is_increase:
          $ref: '#/components/schemas/is_increase'
        rounding_type:
          $ref: '#/components/schemas/rounding_type'
        decimal_place:
          $ref: '#/components/schemas/decimal_place'
        pricebook_items:
          description: Items included in the price book. Applicable when the price
            book type is set to per_item.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              pricebook_rate:
                $ref: '#/components/schemas/pricebook_rate'
              pricebook_discount:
                $ref: '#/components/schemas/pricebook_discount'
              pricing_scheme:
                $ref: '#/components/schemas/pricing_scheme'
              price_brackets:
                description: Collection of volume pricing tiers for a line item when
                  pricing_scheme is volume.
                type: array
                items:
                  type: object
                  properties:
                    start_quantity:
                      $ref: '#/components/schemas/start_quantity'
                    end_quantity:
                      $ref: '#/components/schemas/end_quantity'
                    pricebook_rate:
                      $ref: '#/components/schemas/pricebook_rate'
                    pricebook_discount:
                      $ref: '#/components/schemas/pricebook_discount'
        sales_or_purchase_type:
          $ref: '#/components/schemas/sales_or_purchase_type'
    create-a-pricebook-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Price list has been created.
          readOnly: true
        pricebook:
          $ref: '#/components/schemas/pricebook'
    list-all-pricebooks-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        pricebooks:
          type: array
          items:
            type: object
            properties:
              currency_code:
                $ref: '#/components/schemas/currency_code'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              decimal_place:
                $ref: '#/components/schemas/decimal_place'
              description:
                $ref: '#/components/schemas/description'
              is_default:
                $ref: '#/components/schemas/is_default'
              is_increase:
                $ref: '#/components/schemas/is_increase'
              name:
                $ref: '#/components/schemas/name'
              percentage:
                $ref: '#/components/schemas/percentage'
              pricebook_id:
                $ref: '#/components/schemas/pricebook_id'
              pricebook_items:
                $ref: '#/components/schemas/pricebook_items'
              pricebook_type:
                $ref: '#/components/schemas/pricebook_type'
              pricing_scheme:
                $ref: '#/components/schemas/pricing_scheme'
              rounding_type:
                $ref: '#/components/schemas/rounding_type'
              sales_or_purchase_type:
                $ref: '#/components/schemas/sales_or_purchase_type'
              status:
                $ref: '#/components/schemas/status'
    update-pricebook-request:
      required:
      - name
      - pricebook_type
      - sales_or_purchase_type
      type: object
      properties:
        name:
          $ref: '#/components/schemas/name'
        description:
          $ref: '#/components/schemas/description'
        currency_id:
          $ref: '#/components/schemas/currency_id'
        pricebook_type:
          $ref: '#/components/schemas/pricebook_type'
        percentage:
          $ref: '#/components/schemas/percentage'
        pricebook_rate:
          $ref: '#/components/schemas/pricebook_rate'
        pricing_scheme:
          $ref: '#/components/schemas/pricing_scheme'
        is_increase:
          $ref: '#/components/schemas/is_increase'
        rounding_type:
          $ref: '#/components/schemas/rounding_type'
        decimal_place:
          $ref: '#/components/schemas/decimal_place'
        pricebook_items:
          description: Items included in the price book. Applicable when the price
            book type is set to per_item.
          type: array
          items:
            type: object
            properties:
              item_id:
                $ref: '#/components/schemas/item_id'
              pricebook_rate:
                $ref: '#/components/schemas/pricebook_rate'
              pricebook_item_id:
                $ref: '#/components/schemas/pricebook_item_id'
              pricebook_discount:
                $ref: '#/components/schemas/pricebook_discount'
              pricing_scheme:
                $ref: '#/components/schemas/pricing_scheme'
              price_brackets:
                description: Collection of volume pricing tiers for a line item when
                  pricing_scheme is volume.
                type: array
                items:
                  type: object
                  properties:
                    pricebook_item_id:
                      $ref: '#/components/schemas/pricebook_item_id'
                    start_quantity:
                      $ref: '#/components/schemas/start_quantity'
                    end_quantity:
                      $ref: '#/components/schemas/end_quantity'
                    pricebook_rate:
                      $ref: '#/components/schemas/pricebook_rate'
                    pricebook_discount:
                      $ref: '#/components/schemas/pricebook_discount'
        sales_or_purchase_type:
          $ref: '#/components/schemas/sales_or_purchase_type'
    update-pricebook-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Price list has been updated.
          readOnly: true
        pricebook:
          $ref: '#/components/schemas/pricebook'
    delete-the-pricebook-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Price list has been deleted.
          readOnly: true
    parent_location_id:
      description: Parent Location ID
      type: string
      example: '460000000041010'
    tax_settings_id:
      description: Tax Settings ID
      type: string
      example: '460000000038080'
      x-node_available_in:
      - in
      x-node_unavailable_in: []
    autonumbergenerationgroup_id:
      description: Autonumber generation group ID
      type: string
      example: '982000000870911'
    associated_series_ids:
      description: List of series IDs associated with the location.
      type: array
      items:
        type: string
      example:
      - '982000000870911'
      - '982000000870915'
    user_ids:
      description: Comma separated user ids.
      type: string
      example: 460000000036868,460000000036869
    is_all_users_selected:
      description: Whether all users are selected or not
      type: boolean
      example: false
    enable-location-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: We're enabling locations for your organization.
          readOnly: true
    create-a-location-request:
      required:
      - location_name
      - country
      - tax_settings_id
      type: object
      properties:
        type:
          $ref: '#/components/schemas/type'
        email:
          $ref: '#/components/schemas/email'
        phone:
          $ref: '#/components/schemas/phone'
        address:
          $ref: '#/components/schemas/address'
        location_name:
          $ref: '#/components/schemas/location_name'
        country:
          $ref: '#/components/schemas/country'
        tax_settings_id:
          $ref: '#/components/schemas/tax_settings_id'
        parent_location_id:
          $ref: '#/components/schemas/parent_location_id'
        associated_series_ids:
          $ref: '#/components/schemas/associated_series_ids'
        auto_number_generation_id:
          $ref: '#/components/schemas/autonumbergenerationgroup_id'
        is_all_users_selected:
          $ref: '#/components/schemas/is_all_users_selected'
        user_ids:
          $ref: '#/components/schemas/user_ids'
    create-a-location-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Location has been created.
          readOnly: true
        locations:
          $ref: '#/components/schemas/locations'
    list-all-location-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        locations:
          type: array
          items:
            type: object
            properties:
              type:
                $ref: '#/components/schemas/type'
              email:
                $ref: '#/components/schemas/email'
              phone:
                $ref: '#/components/schemas/phone'
              address:
                $ref: '#/components/schemas/address'
              location_id:
                $ref: '#/components/schemas/location_id'
              location_name:
                $ref: '#/components/schemas/location_name'
              tax_settings_id:
                $ref: '#/components/schemas/tax_settings_id'
              parent_location_id:
                $ref: '#/components/schemas/parent_location_id'
              associated_series_ids:
                $ref: '#/components/schemas/associated_series_ids'
              auto_number_generation_id:
                $ref: '#/components/schemas/autonumbergenerationgroup_id'
              is_all_users_selected:
                $ref: '#/components/schemas/is_all_users_selected'
              associated_users:
                type: array
                items:
                  type: object
                  properties:
                    user_id:
                      $ref: '#/components/schemas/user_id'
                    user_name:
                      $ref: '#/components/schemas/user_name'
    update-location-request:
      required:
      - location_name
      - country
      - tax_settings_id
      type: object
      properties:
        type:
          $ref: '#/components/schemas/type'
        email:
          $ref: '#/components/schemas/email'
        phone:
          $ref: '#/components/schemas/phone'
        address:
          $ref: '#/components/schemas/address'
        location_name:
          $ref: '#/components/schemas/location_name'
        country:
          $ref: '#/components/schemas/country'
        tax_settings_id:
          $ref: '#/components/schemas/tax_settings_id'
        parent_location_id:
          $ref: '#/components/schemas/parent_location_id'
        associated_series_ids:
          $ref: '#/components/schemas/associated_series_ids'
        auto_number_generation_id:
          $ref: '#/components/schemas/autonumbergenerationgroup_id'
        is_all_users_selected:
          $ref: '#/components/schemas/is_all_users_selected'
        user_ids:
          $ref: '#/components/schemas/user_ids'
    update-location-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Location has been updated.
          readOnly: true
        locations:
          $ref: '#/components/schemas/locations'
    delete-a-location-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The location has been deleted..
          readOnly: true
    mark-as-primary-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The location has been marked as primary.
          readOnly: true
    currency_name:
      description: The name for the currency.
      type: string
      example: AUD- Australian Dollar
    is_base_currency:
      description: Is it the base currency of the organization.
      type: boolean
      example: false
    exchange_rate_id:
      description: ID of the exchange rate
      type: string
      example: '460000000038035'
    effective_date:
      description: Date which the exchange rate is applicable for the currency.
      type: string
      example: '2013-09-04'
    is_current_date:
      description: To return the exchange rate only if available for current date.
      type: boolean
      example: true
    currency:
      type: object
      properties:
        currency_id:
          $ref: '#/components/schemas/currency_id'
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_name:
          $ref: '#/components/schemas/currency_name'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        currency_format:
          $ref: '#/components/schemas/currency_format'
        is_base_currency:
          $ref: '#/components/schemas/is_base_currency'
    create-a-currency-request:
      required:
      - currency_code
      - currency_format
      type: object
      properties:
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        currency_format:
          $ref: '#/components/schemas/currency_format'
    create-a-currency-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The currency has been added.
          readOnly: true
        currency:
          $ref: '#/components/schemas/currency'
    list-currencies-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        currencies:
          type: array
          items:
            type: object
            properties:
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              currency_name:
                $ref: '#/components/schemas/currency_name'
              currency_symbol:
                $ref: '#/components/schemas/currency_symbol'
              price_precision:
                $ref: '#/components/schemas/price_precision'
              currency_format:
                $ref: '#/components/schemas/currency_format'
              is_base_currency:
                $ref: '#/components/schemas/is_base_currency'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              effective_date:
                $ref: '#/components/schemas/effective_date'
    update-a-currency-request:
      required:
      - currency_code
      - currency_format
      type: object
      properties:
        currency_code:
          $ref: '#/components/schemas/currency_code'
        currency_symbol:
          $ref: '#/components/schemas/currency_symbol'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        currency_format:
          $ref: '#/components/schemas/currency_format'
    update-a-currency-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Currency information has been saved.
          readOnly: true
        currency:
          $ref: '#/components/schemas/currency'
    get-a-currency-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        currency:
          $ref: '#/components/schemas/currency'
    delete-a-currency-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The currency has been deleted.
          readOnly: true
    list-exchange-rates-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        exchange_rates:
          type: array
          items:
            type: object
            properties:
              exchange_rate_id:
                $ref: '#/components/schemas/exchange_rate_id'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              currency_code:
                $ref: '#/components/schemas/currency_code'
              effective_date:
                $ref: '#/components/schemas/effective_date'
              rate:
                $ref: '#/components/schemas/rate'
    create-an-exchange-rate-request:
      type: object
      properties:
        effective_date:
          $ref: '#/components/schemas/effective_date'
        rate:
          $ref: '#/components/schemas/rate'
    create-an-exchange-rate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The exchange rate has been added.
          readOnly: true
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
    get-an-exchange-rate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        exchange_rate:
          $ref: '#/components/schemas/exchange_rate'
    update-an-exchange-rate-request:
      type: object
      properties:
        effective_date:
          $ref: '#/components/schemas/effective_date'
        rate:
          $ref: '#/components/schemas/rate'
    update-an-exchange-rate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The exchange rate has been updated.
          readOnly: true
    delete-an-exchage-rate-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Exchange rate successfully deleted
          readOnly: true
    tax-response:
      type: object
      properties:
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        tax_type:
          $ref: '#/components/schemas/tax_type'
        tax_factor:
          $ref: '#/components/schemas/tax_factor'
        tds_payable_account_id:
          $ref: '#/components/schemas/tds_payable_account_id'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        is_value_added:
          $ref: '#/components/schemas/is_value_added'
        tax_specific_type:
          $ref: '#/components/schemas/tax_specific_type'
        country:
          $ref: '#/components/schemas/country'
        country_code:
          $ref: '#/components/schemas/country_code'
        purchase_tax_expense_account_id:
          $ref: '#/components/schemas/purchase_tax_expense_account_id'
    purchase_tax_expense_account_id:
      description: 'Account ID in which Purchase Tax will be Computed. Note: This
        value is a set of numeric characters.'
      type: string
      example: '132086000000107337'
      x-node_available_in:
      - au
      - ca
      x-node_unavailable_in: []
    tax_factor:
      description: 'Type of Tax Factor. Allowed values: <code>rate</code>, <code>share</code>'
      type: string
      example: rate
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    tax_account_id:
      type: string
      description: 'Account ID in which tax will be Computed. Note: This value is
        a set of numeric characters.'
      example: '132086000000107333'
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    output_tax_account_name:
      type: string
      description: Account Name in which Output Tax will be Computed
      example: string
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    purchase_tax_account_name:
      type: string
      description: Account Name in which Purchase Tax will be Computed
      example: string
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    purchase_tax_account_id:
      description: 'ID of the tax account, in case of withholding tax. Note: This
        value is a set of numeric characters.'
      type: string
      example: '132086000000107337'
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    tds_payable_account_id:
      description: 'Input Tax ID. The amount of money charged to you as Tax on your
        purchases. Note: This value is a set of numeric characters.'
      type: string
      example: '132086000000107337'
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    tax_account_tracking:
      type: string
      example: string
      x-node_available_in:
      - mx
      x-node_unavailable_in: []
    tax_group_id:
      description: 'the id of the tax group. Note: This value is a set of numeric
        characters.'
      type: string
      example: '982000000566009'
    tax_group_name:
      description: Name of the tax group to be created.
      type: string
      example: Sales Group
    tax_group_percentage:
      description: Number of percentage taxable.
      type: number
      format: double
      example: 10.5
    is_value_added:
      description: Check if Tax is Value Added
      type: boolean
      example: false
    tax_specific_type:
      description: 'Type of Tax For <b>Indian</b> Edition. Allowed Values : <code>igst</code>
        ,<code>cgst</code> ,<code>sgst</code> ,<code>nil</code>, <code> cess </code>.</br>Type
        of Tax for <b>Mexico</b> Edition. Allowed Values : <code>isr</code>, <code>iva</code>,
        <code>ieps</code>.</br>Type of Tax for <b>South Africa</b> Edition. Allowed
        Values : </br><code>soa_less_than_28d</code> - Supply of accommodation not
        exceeding 28 days, </br><code>soa_more_than_28d</code> - Supply of accommodation
        exceeding than 28 days, </br><code>ciu_prev_tax_supply</code> - Change in
        use (Taxable supplies), </br><code>ciu_prev_nontax_supply</code> - Change
        in use (Non-Taxable supplies), </br><code>export_of_shg</code> - Export of
        second hand goods.'
      type: string
      example: igst
      x-node_available_in:
      - in
      - mx
      - za
      x-node_unavailable_in: []
    is_default_tax:
      description: Check is Tax is default
      type: boolean
      example: true
    registration_number:
      description: Registration Number of the Tax Authority
      type: string
      x-node_available_in:
      - ca
      x-node_unavailable_in: []
    registration_number_label:
      type: string
      description: Label used to identify the tax authority registration number.
      x-node_available_in:
      - ca
      x-node_unavailable_in: []
    tax_authority:
      type: object
      properties:
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        description:
          $ref: '#/components/schemas/description'
        registration_number:
          $ref: '#/components/schemas/registration_number'
        registration_number_label:
          $ref: '#/components/schemas/registration_number_label'
    is_editable:
      description: Check if tax is editable
      type: boolean
      example: true
    update_recurring_invoice:
      description: Check if recurring invoice should be updated
      type: boolean
      example: false
    update_recurring_expense:
      description: Check if recurring expenses should be updated
      type: boolean
      example: false
    update_draft_invoice:
      description: Check if Draft Invoices should be updated
      type: boolean
      example: false
    update_recurring_bills:
      description: Check if Recurring Bills should be updated
      type: boolean
      example: false
    update_draft_so:
      description: Check if Draft Sales Orders should be updated
      type: boolean
      example: false
    update_subscription:
      description: Check if Subscriptions should be updated
      type: boolean
      example: false
    update_project:
      description: Check if Projects should be updated
      type: boolean
      example: false
    create-a-tax-request:
      type: object
      properties:
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        tax_type:
          $ref: '#/components/schemas/tax_type'
        tax_factor:
          $ref: '#/components/schemas/tax_factor'
        tax_specific_type:
          $ref: '#/components/schemas/tax_specific_type'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        country_code:
          $ref: '#/components/schemas/country_code'
        purchase_tax_expense_account_id:
          $ref: '#/components/schemas/purchase_tax_expense_account_id'
        is_value_added:
          $ref: '#/components/schemas/is_value_added'
        update_recurring_invoice:
          $ref: '#/components/schemas/update_recurring_invoice'
        update_recurring_expense:
          $ref: '#/components/schemas/update_recurring_expense'
        update_draft_invoice:
          $ref: '#/components/schemas/update_draft_invoice'
        update_recurring_bills:
          $ref: '#/components/schemas/update_recurring_bills'
        update_draft_so:
          $ref: '#/components/schemas/update_draft_so'
        update_subscription:
          $ref: '#/components/schemas/update_subscription'
        update_project:
          $ref: '#/components/schemas/update_project'
        is_editable:
          $ref: '#/components/schemas/is_editable'
    create-a-tax-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The tax has been added.
          readOnly: true
        tax:
          $ref: '#/components/schemas/tax'
    list-taxes-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        taxes:
          description: Comma Seperated list of tax IDs that are to be associated to
            the tax group.
          type: array
          items:
            type: object
            properties:
              tax_id:
                $ref: '#/components/schemas/tax_id'
              tax_name:
                $ref: '#/components/schemas/tax_name'
              tax_percentage:
                $ref: '#/components/schemas/tax_percentage'
              tax_type:
                $ref: '#/components/schemas/tax_type'
              tax_factor:
                $ref: '#/components/schemas/tax_factor'
              tax_specific_type:
                $ref: '#/components/schemas/tax_specific_type'
              tax_authority_id:
                $ref: '#/components/schemas/tax_authority_id'
              tax_authority_name:
                $ref: '#/components/schemas/tax_authority_name'
              is_value_added:
                $ref: '#/components/schemas/is_value_added'
              is_default_tax:
                $ref: '#/components/schemas/is_default_tax'
              is_editable:
                $ref: '#/components/schemas/is_editable'
              output_tax_account_name:
                $ref: '#/components/schemas/output_tax_account_name'
              purchase_tax_account_name:
                $ref: '#/components/schemas/purchase_tax_account_name'
              tax_account_id:
                $ref: '#/components/schemas/tax_account_id'
              purchase_tax_account_id:
                $ref: '#/components/schemas/purchase_tax_account_id'
        page_context:
          type: object
          properties:
            page:
              $ref: '#/components/schemas/page'
            per_page:
              $ref: '#/components/schemas/per_page'
            has_more_page:
              $ref: '#/components/schemas/has_more_page'
            report_name:
              $ref: '#/components/schemas/report_name'
            applied_filter:
              $ref: '#/components/schemas/applied_filter'
            sort_column:
              $ref: '#/components/schemas/sort_column'
            sort_order:
              $ref: '#/components/schemas/sort_order'
    update-a-tax-request:
      type: object
      properties:
        tax_id:
          $ref: '#/components/schemas/tax_id'
        tax_name:
          $ref: '#/components/schemas/tax_name'
        tax_percentage:
          $ref: '#/components/schemas/tax_percentage'
        tax_type:
          $ref: '#/components/schemas/tax_type'
        tax_factor:
          $ref: '#/components/schemas/tax_factor'
        tax_specific_type:
          $ref: '#/components/schemas/tax_specific_type'
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        tax_authority_id:
          $ref: '#/components/schemas/tax_authority_id'
        country_code:
          $ref: '#/components/schemas/country_code'
        purchase_tax_expense_account_id:
          $ref: '#/components/schemas/purchase_tax_expense_account_id'
        is_value_added:
          $ref: '#/components/schemas/is_value_added'
        update_recurring_invoice:
          $ref: '#/components/schemas/update_recurring_invoice'
        update_recurring_expense:
          $ref: '#/components/schemas/update_recurring_expense'
        update_draft_invoice:
          $ref: '#/components/schemas/update_draft_invoice'
        update_recurring_bills:
          $ref: '#/components/schemas/update_recurring_bills'
        update_draft_so:
          $ref: '#/components/schemas/update_draft_so'
        update_subscription:
          $ref: '#/components/schemas/update_subscription'
        update_project:
          $ref: '#/components/schemas/update_project'
        is_editable:
          $ref: '#/components/schemas/is_editable'
        tds_payable_account_id:
          $ref: '#/components/schemas/tds_payable_account_id'
    update-a-tax-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax information has been saved.
          readOnly: true
        tax:
          $ref: '#/components/schemas/tax'
    get-a-tax-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax:
          $ref: '#/components/schemas/tax'
    delete-a-tax-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The record has been deleted.
          readOnly: true
    get-a-tax-group-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_group:
          type: object
          properties:
            tax_group_id:
              $ref: '#/components/schemas/tax_group_id'
            tax_group_name:
              $ref: '#/components/schemas/tax_group_name'
            tax_group_percentage:
              $ref: '#/components/schemas/tax_group_percentage'
            taxes:
              description: Comma Seperated list of tax IDs that are to be associated
                to the tax group.
              type: array
              items:
                type: object
                properties:
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_factor:
                    $ref: '#/components/schemas/tax_factor'
                  tax_authority_id:
                    $ref: '#/components/schemas/tax_authority_id'
                  tax_authority_name:
                    $ref: '#/components/schemas/tax_authority_name'
    update-a-tax-group-request:
      type: object
      properties:
        tax_group_name:
          $ref: '#/components/schemas/tax_group_name'
        taxes:
          $ref: '#/components/schemas/taxes'
    update-a-tax-group-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_group:
          type: object
          properties:
            tax_group_id:
              $ref: '#/components/schemas/tax_group_id'
            tax_group_name:
              $ref: '#/components/schemas/tax_group_name'
            tax_group_percentage:
              $ref: '#/components/schemas/tax_group_percentage'
            taxes:
              description: Comma Seperated list of tax IDs that are to be associated
                to the tax group.
              type: array
              items:
                type: object
                properties:
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_factor:
                    $ref: '#/components/schemas/tax_factor'
                  tax_authority_id:
                    $ref: '#/components/schemas/tax_authority_id'
                  tax_authority_name:
                    $ref: '#/components/schemas/tax_authority_name'
    delete-a-tax-group-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The tax group has been deleted.
          readOnly: true
    create-a-tax-group-request:
      type: object
      properties:
        tax_group_name:
          $ref: '#/components/schemas/tax_group_name'
        taxes:
          $ref: '#/components/schemas/taxes'
    create-a-tax-group-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_group:
          type: object
          properties:
            tax_group_id:
              $ref: '#/components/schemas/tax_group_id'
            tax_group_name:
              $ref: '#/components/schemas/tax_group_name'
            tax_group_percentage:
              $ref: '#/components/schemas/tax_group_percentage'
            taxes:
              description: Comma Seperated list of tax IDs that are to be associated
                to the tax group.
              type: array
              items:
                type: object
                properties:
                  tax_id:
                    $ref: '#/components/schemas/tax_id'
                  tax_name:
                    $ref: '#/components/schemas/tax_name'
                  tax_percentage:
                    $ref: '#/components/schemas/tax_percentage'
                  tax_type:
                    $ref: '#/components/schemas/tax_type'
                  tax_factor:
                    $ref: '#/components/schemas/tax_factor'
                  tax_authority_id:
                    $ref: '#/components/schemas/tax_authority_id'
                  tax_authority_name:
                    $ref: '#/components/schemas/tax_authority_name'
    create-a-tax-authority-us-and-ca-edition-only-request:
      required:
      - tax_authority_name
      type: object
      properties:
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        description:
          $ref: '#/components/schemas/description'
        registration_number:
          $ref: '#/components/schemas/registration_number'
        registration_number_label:
          $ref: '#/components/schemas/registration_number_label'
    create-a-tax-authority-us-and-ca-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax Authority has been added.
          readOnly: true
        tax_authority:
          $ref: '#/components/schemas/tax_authority'
    list-tax-authorities-us-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_authorities:
          type: array
          items:
            type: object
            properties:
              tax_authority_id:
                $ref: '#/components/schemas/tax_authority_id'
              tax_authority_name:
                $ref: '#/components/schemas/tax_authority_name'
              description:
                $ref: '#/components/schemas/description'
              registration_number:
                $ref: '#/components/schemas/registration_number'
              registration_number_label:
                $ref: '#/components/schemas/registration_number_label'
    update-a-tax-authority-us-and-ca-edition-only-request:
      type: object
      properties:
        tax_authority_name:
          $ref: '#/components/schemas/tax_authority_name'
        description:
          $ref: '#/components/schemas/description'
        registration_number:
          $ref: '#/components/schemas/registration_number'
        registration_number_label:
          $ref: '#/components/schemas/registration_number_label'
    update-a-tax-authority-us-and-ca-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax Authority information has been updated.
          readOnly: true
        tax_authority:
          $ref: '#/components/schemas/tax_authority'
    get-a-tax-authority-us-and-ca-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_authority:
          $ref: '#/components/schemas/tax_authority'
    delete-a-tax-authority-us-and-ca-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax authority has been deleted.
          readOnly: true
    create-a-tax-exemption-us-edition-only-request:
      required:
      - tax_exemption_code
      - type
      type: object
      properties:
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        description:
          description: Description
          type: string
          example: Tax exempted because the contact is a reseller.
        type:
          $ref: '#/components/schemas/type'
    create-a-tax-exemption-us-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax Exemption has been added.
          readOnly: true
        tax_exemption:
          type: object
          properties:
            tax_exemption_id:
              $ref: '#/components/schemas/tax_exemption_id'
            tax_exemption_code:
              $ref: '#/components/schemas/tax_exemption_code'
            description:
              description: Description
              type: string
              example: Tax exempted because the contact is a reseller.
            type:
              $ref: '#/components/schemas/type'
    list-tax-exemptions-us-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_exemptions:
          type: array
          items:
            type: object
            properties:
              tax_exemption_id:
                $ref: '#/components/schemas/tax_exemption_id'
              tax_exemption_code:
                $ref: '#/components/schemas/tax_exemption_code'
              description:
                description: Description.
                type: string
                example: Tax exempted because the contact is a reseller.
              type:
                description: Type of the tax exemption, can be <code>customer</code>
                  or <code>item</code>.
                type: string
                example: customer
    update-a-tax-exemption-us-edition-only-request:
      required:
      - tax_exemption_code
      - type
      type: object
      properties:
        tax_exemption_code:
          $ref: '#/components/schemas/tax_exemption_code'
        description:
          description: Description
          type: string
          example: Tax exempted because the contact is a reseller.
        type:
          $ref: '#/components/schemas/type'
    update-a-tax-exemption-us-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax Exemption has been updated.
          readOnly: true
        tax_exemption:
          type: object
          properties:
            tax_exemption_id:
              $ref: '#/components/schemas/tax_exemption_id'
            tax_exemption_code:
              $ref: '#/components/schemas/tax_exemption_code'
            description:
              description: Description
              type: string
              example: Tax exempted because the contact is a reseller.
            type:
              $ref: '#/components/schemas/type'
    get-a-tax-exemption-us-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        tax_exemption:
          type: object
          properties:
            tax_exemption_id:
              $ref: '#/components/schemas/tax_exemption_id'
            tax_exemption_code:
              $ref: '#/components/schemas/tax_exemption_code'
            description:
              description: Description of the Tax Exemption.
              type: string
              example: Tax exempted because the contact is a reseller.
            type:
              description: Type of the tax exemption, can be <code>customer</code>
                or <code>item</code>.
              type: string
              example: customer
    delete-a-tax-exemption-us-edition-only-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: Tax exemption has been deleted successfully.
          readOnly: true
    opening_balance_id:
      description: ID of opening balance.
      type: string
      example: '460000000050041'
    acount_split_id:
      description: ID of split account that you want to update.
      type: string
      example: '460000000050045'
    opening_balance:
      type: object
      properties:
        opening_balance_id:
          $ref: '#/components/schemas/opening_balance_id'
        date:
          $ref: '#/components/schemas/date'
        price_precision:
          $ref: '#/components/schemas/price_precision'
        accounts:
          $ref: '#/components/schemas/accounts'
        total:
          $ref: '#/components/schemas/total'
    create-opening-balance-request:
      required:
      - date
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        accounts:
          type: array
          description: List of accounts with their opening balance details.
          items:
            type: object
            required:
            - debit_or_credit
            properties:
              account_id:
                $ref: '#/components/schemas/account_id'
              debit_or_credit:
                $ref: '#/components/schemas/debit_or_credit'
              exchange_rate:
                $ref: '#/components/schemas/exchange_rate'
              currency_id:
                $ref: '#/components/schemas/currency_id'
              amount:
                $ref: '#/components/schemas/amount'
              location_id:
                $ref: '#/components/schemas/location_id'
    create-opening-balance-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The opening balances are saved!
          readOnly: true
        opening_balance:
          $ref: '#/components/schemas/opening_balance'
    update-opening-balance-request:
      required:
      - opening_balance_id
      type: object
      properties:
        date:
          $ref: '#/components/schemas/date'
        accounts:
          $ref: '#/components/schemas/accounts'
        opening_balance_id:
          $ref: '#/components/schemas/opening_balance_id'
    update-opening-balance-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The opening balances are saved!
          readOnly: true
        opening_balance:
          $ref: '#/components/schemas/opening_balance'
    get-opening-balance-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        opening_balance:
          $ref: '#/components/schemas/opening_balance'
    delete-opening-balance-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: The entered opening balance has been deleted.
          readOnly: true
    tag_order:
      description: The order in which the tag appears
      type: integer
      example: 1
    is_class:
      description: Is segmentation enabled for the tag
      type: boolean
      example: false
    option_id:
      description: Option ID
      type: string
      example: '460000000038081'
    option_name:
      description: Option Name
      type: string
      example: Sales
    filter_tag_id:
      type: string
      description: Filter tag ID. If the filter 'type' is location, <code>null</code>
        should be passed
      example: '460000000038081'
    filter_type:
      description: 'Type of filter entity. <br>Allowed values: Tag <code>tag</code>
        and Location <code>location</code>. <br>'
      type: string
      example: tag
    filter_option_id:
      description: 'Option ID for the filter. It can be an option ID or flags.  <br>Allowed
        flags: <code>all</code> - Instead of selecting all options, you can send this
        flag to denote all options of the filter tag or location are selected for
        the filter to the respective tag. <code>untagged</code> - When you want to
        show an option when no option is selected for the filter tag. <b>Note: This
        flag cannot be used for location filter.</b>'
      type: string
      example: '460000000038081'
    filter_option_name:
      description: 'Option Name for the filter. It can be an option name or flags.  <br>Allowed
        flags: <code>All</code> - Instead of selecting all options, you can send this
        flag to denote all options of the filter tag or location are selected for
        the filter to the respective tag. <code>Untagged</code> - When you want to
        show an option when no option is selected for the filter tag. <b>Note: This
        flag cannot be used for location filter.</b>'
      type: string
      example: Sales
    tag_is_active:
      type: boolean
      description: Is the tag active
      example: true
    option_is_default:
      type: boolean
      description: Is the option default for the tag
      example: true
    option_is_active:
      type: boolean
      description: Is the option active for the tag
      example: true
    entities:
      description: Entities that can only be enabled or disabled which can also be
        called as mono-preference entities. <br><i>Note:</i>If you have Payroll joined
        then <code>journal</code> will be sent in this list instead of <code>multiple_preference_entities</code>
      type: array
      items:
        type: object
        properties:
          entity_name:
            description: 'Entity Name<br>Possible Values:  <br>Customer - <code>customers</code>,  <br>Vendor
              - <code>vendors</code>,  <br>Item - <code>items</code>,  <br>Banking
              - <code>banking</code> <br>Fixed Asset - <code>fixed_asset</code>'
            type: string
            enum:
            - customers
            - vendors
            - items
            - banking
          is_enabled:
            description: Is Enabled
            type: boolean
            example: true
      example:
      - entity_name: customers
        is_enabled: true
      - entity_name: vendors
        is_enabled: true
      - entity_name: items
        is_enabled: true
      - entity_name: fixed_asset
        is_enabled: true
      - entity_name: banking
        is_enabled: true
    multi_preference_entities:
      type: object
      properties:
        preference:
          description: Preference type. Currently, only <code>'transaction' and 'line_item'</code>
            is supported for this node
          type: string
          example: line_item
        entities:
          description: Entities that can only be enabled or disabled
          type: array
          items:
            type: object
            properties:
              entity_name:
                description: 'Entity Name. Possible Values:  <br>Sales - <code>sales</code>,  <br>Purchases
                  - <code>purchases</code>,  <br>Journal - <code>journals</code>,  <br>Inventory
                  Adjustment - <code>inventory_adjustment</code>'
                type: string
                enum:
                - Sales
                - Purchases
                - Journal
                - Inventory Adjustment
              is_enabled:
                description: Is Enabled
                type: boolean
                example: true
      example:
        preference: line_item
        entities:
        - entity_name: sales
          is_enabled: true
        - entity_name: purchases
          is_enabled: true
        - entity_name: journals
          is_enabled: false
        - entity_name: inventory_adjustment
          is_enabled: false
    criteria_tags:
      description: Filter tags and location
      type: array
      items:
        type: object
        properties:
          tag_id:
            $ref: '#/components/schemas/filter_tag_id'
          type:
            $ref: '#/components/schemas/filter_type'
    criteria_options:
      description: Options for criteria
      type: array
      items:
        type: object
        properties:
          option_id:
            $ref: '#/components/schemas/option_id'
          criteria_options:
            description: List containing filter tags and location
            type: array
            items:
              type: object
              properties:
                tag_id:
                  $ref: '#/components/schemas/filter_tag_id'
                type:
                  $ref: '#/components/schemas/filter_type'
                options:
                  description: Options selected for the criteria
                  type: array
                  items:
                    $ref: '#/components/schemas/option_id'
    tag_response:
      type: object
      properties:
        tag_id:
          $ref: '#/components/schemas/tag_id'
        tag_name:
          $ref: '#/components/schemas/tag_name'
        description:
          $ref: '#/components/schemas/description'
        is_mandatory:
          $ref: '#/components/schemas/is_mandatory'
        entities:
          $ref: '#/components/schemas/entities'
        multi_preference_entities:
          $ref: '#/components/schemas/multi_preference_entities'
    list-reporting-tags-response:
      type: array
      items:
        type: object
        properties:
          tag_id:
            $ref: '#/components/schemas/tag_id'
          tag_name:
            $ref: '#/components/schemas/tag_name'
          tag_order:
            $ref: '#/components/schemas/tag_order'
          description:
            $ref: '#/components/schemas/description'
          is_active:
            $ref: '#/components/schemas/tag_is_active'
          is_mandatory:
            $ref: '#/components/schemas/is_mandatory'
          is_class:
            $ref: '#/components/schemas/is_class'
          is_draft:
            $ref: '#/components/schemas/is_draft'
      example:
      - tag_id: '460000000038080'
        tag_name: Business Unit
        tag_order: 1
        description: Description of the reporting tag
        is_active: true
        is_mandatory: false
        is_class: false
        is_draft: false
      - tag_id: '460000000038081'
        tag_name: Sales Region
        tag_order: 2
        description: Description of the reporting tag
        is_active: true
        is_mandatory: false
        is_class: true
        is_draft: false
      - tag_id: '460000000038082'
        tag_name: Department
        tag_order: 3
        description: Description of the reporting tag
        is_active: false
        is_mandatory: true
        is_class: false
        is_draft: true
    update-reporting-tag-request:
      required:
      - tag_name
      - entities
      - multi_preference_entities
      type: object
      properties:
        tag_name:
          $ref: '#/components/schemas/tag_name'
        description:
          $ref: '#/components/schemas/description'
        is_mandatory:
          $ref: '#/components/schemas/is_mandatory'
        entities:
          $ref: '#/components/schemas/entities'
        multi_preference_entities:
          $ref: '#/components/schemas/multi_preference_entities'
    update-options-request:
      required:
      - options
      type: object
      properties:
        options:
          type: array
          description: List of options for the reporting tag.
          items:
            type: object
            properties:
              option_name:
                $ref: '#/components/schemas/option_name'
              option_id:
                $ref: '#/components/schemas/option_id'
              is_active:
                $ref: '#/components/schemas/option_is_active'
              is_default:
                $ref: '#/components/schemas/option_is_default'
              children:
                type: array
                description: Child options of an option
                items:
                  type: object
                  properties:
                    option_id:
                      description: Option ID
                      type: string
                      example: '460000000038082'
                    option_name:
                      description: Option Name
                      type: string
                      example: Sales - East
                    is_active:
                      $ref: '#/components/schemas/option_is_active'
                    is_default:
                      $ref: '#/components/schemas/option_is_default'
      example:
        options:
        - option_id: '460000000038082'
          option_name: ES-2003
          is_active: true
          is_default: true
          children:
          - option_id: '460000000038088'
            children: []
            option_name: ES-2003 Sec - B
            is_active: true
          - option_name: ES-2003 Sec - C
            is_active: true
            children:
            - children: []
              option_name: EES-2003
              is_active: true
        - option_id: '460000000038084'
          option_name: ES-N-2016
          is_active: true
          is_default: false
          children: []
        - option_id: '460000000038086'
          option_name: HS & ES-N
          is_active: true
          is_default: false
          children: []
    update-options-response:
      type: object
      properties:
        message:
          type: string
          example: Options updated successfully
    reporting-tags-response:
      type: object
      properties:
        tag:
          $ref: '#/components/schemas/tag_response'
    update-criteria-request:
      required:
      - criteria_tags
      - options
      type: object
      properties:
        criteria_tags:
          $ref: '#/components/schemas/criteria_tags'
        options:
          $ref: '#/components/schemas/criteria_options'
    update-criteria-response:
      type: object
      properties:
        message:
          type: string
          example: The criteria has been updated.
    update-segmentation-response:
      type: object
      properties:
        message:
          type: string
          example: Segmentation modified successfully.
    options-editpage-response:
      type: object
      properties:
        message:
          type: string
          example: success
        is_class:
          $ref: '#/components/schemas/is_class'
        is_draft:
          $ref: '#/components/schemas/is_draft'
        is_filter_configured:
          type: boolean
          description: If filter is configured for this tag, it will be <code>true</code>
          example: true
        tag_id:
          $ref: '#/components/schemas/tag_id'
        tag_name:
          $ref: '#/components/schemas/tag_name'
        options:
          type: array
          description: List of options for the reporting tag.
          items:
            type: object
            properties:
              option_id:
                $ref: '#/components/schemas/option_id'
              option_name:
                $ref: '#/components/schemas/option_name'
              is_active:
                $ref: '#/components/schemas/option_is_active'
              is_default:
                $ref: '#/components/schemas/option_is_default'
              children:
                type: array
                description: Child options of an option
                items:
                  type: object
                  properties:
                    option_id:
                      description: Option ID
                      type: string
                      example: '460000000038082'
                    option_name:
                      description: Option Name
                      type: string
                      example: Sales - East
                    is_active:
                      $ref: '#/components/schemas/option_is_active'
                    is_default:
                      $ref: '#/components/schemas/option_is_default'
    editpage-segmentation-response:
      type: object
      properties:
        is_class:
          $ref: '#/components/schemas/is_class'
        is_draft:
          $ref: '#/components/schemas/is_draft'
        tag_id:
          $ref: '#/components/schemas/tag_id'
        tag_name:
          $ref: '#/components/schemas/tag_name'
    dependent_tags:
      type: array
      description: The tags and location that is associated as filter for the tag
      items:
        type: object
        properties:
          dependent_id:
            type: string
            description: Dependent Tag's ID. If the dependent is location, value will
              be empty
            example: 460000000038081
          dependent_name:
            type: string
            description: Dependent Tag's Name. If the dependent is location, value
              will be <code>Location</code>
            example: Sales
          dependent_order:
            type: integer
            description: The order in which the dependent tags appear. If the dependent
              is location, value will be empty
            example: 1
          dependent_type:
            type: string
            description: 'Type of the dependent. Allowed values: Tag <code>tag</code>,
              Location <code>location</code>'
            example: tag
          dependent_options:
            description: Options selected for the dependent tag. If the dependent
              is location, this will be empty
            type: array
            items:
              type: object
              properties:
                dependent_option_id:
                  type: string
                  description: Dependent Option ID
                  example: 460000000038083
                dependent_option_name:
                  type: string
                  description: Dependent Option Name
                  example: Sales - East
                dependent_option_order:
                  type: integer
                  description: The order in which the options appear.
                  example: 1
          tag_name:
            $ref: '#/components/schemas/tag_name'
    possible_filter_tags:
      description: List of tags that can be used as filter for the tag
      type: array
      items:
        type: object
        properties:
          tag_id:
            $ref: '#/components/schemas/tag_id'
          tag_name:
            $ref: '#/components/schemas/tag_name'
          type:
            type: string
            description: 'Type of the tag. Allowed values: Tag <code>tag</code>, Location
              <code>location</code>'
            example: tag
          is_associated:
            type: boolean
            description: Indicates whether the tag is already associated as filter
              for the tag
            example: true
          is_restricted:
            type: boolean
            description: Indicates whether the tag is restricted to be used as filter
              for the tag
            example: false
          restricted_reason:
            type: string
            description: Reason for restriction, if any
            example: The modules associated with this reporting tag are not set at
              the appropriate level compared to the current tag. Kindly refer to the
              requirements for a filter tag.
    criteria_options_response:
      type: array
      description: Options selected for the criteria
      items:
        type: object
        properties:
          option_id:
            $ref: '#/components/schemas/option_id'
          option_name:
            $ref: '#/components/schemas/option_name'
          option_order:
            type: integer
            description: The order in which the options appear.
            example: 1
          parent_id:
            type: string
            description: Parent Option ID. If the options does not have a parent,
              this will be empty
            example: '460000000038081'
          parent_name:
            type: string
            description: Parent Option Name. If the options does not have a parent,
              this will be empty
            example: Sales
          is_active:
            $ref: '#/components/schemas/option_is_active'
          is_default:
            $ref: '#/components/schemas/option_is_default'
          criteria:
            type: object
            properties:
              criteria_id:
                type: string
                description: Filter ID
                example: '460000000038084'
              tag_id:
                $ref: '#/components/schemas/filter_tag_id'
              type:
                $ref: '#/components/schemas/filter_type'
              options:
                description: Options selected for the criteria
                type: array
                items:
                  $ref: '#/components/schemas/filter_option_id'
          children:
            description: Child options of the option
            type: array
            items:
              type: object
              properties:
                option_id:
                  $ref: '#/components/schemas/option_id'
                option_name:
                  $ref: '#/components/schemas/option_name'
                option_order:
                  type: integer
                  description: The order in which the options appear.
                  example: 1
                parent_id:
                  type: string
                  description: Parent Option ID. If the options does not have a parent,
                    this will be empty
                  example: '460000000038081'
                parent_name:
                  type: string
                  description: Parent Option Name. If the options does not have a
                    parent, this will be empty
                  example: Sales
                is_active:
                  $ref: '#/components/schemas/option_is_active'
                is_default:
                  $ref: '#/components/schemas/option_is_default'
                criteria:
                  type: object
                  properties:
                    criteria_id:
                      type: string
                      description: Filter ID
                      example: '460000000038084'
                    tag_id:
                      $ref: '#/components/schemas/filter_tag_id'
                    type:
                      $ref: '#/components/schemas/filter_type'
                    options:
                      description: Options selected for the criteria
                      type: array
                      items:
                        $ref: '#/components/schemas/filter_option_id'
    criteria_options_response_for_details_page:
      type: array
      description: Options selected for the criteria
      items:
        type: object
        properties:
          option_id:
            $ref: '#/components/schemas/option_id'
          option_name:
            $ref: '#/components/schemas/option_name'
          option_order:
            type: integer
            description: The order in which the options appear.
            example: 1
          parent_id:
            type: string
            description: Parent Option ID. If the options does not have a parent,
              this will be empty
            example: '460000000038081'
          parent_name:
            type: string
            description: Parent Option Name. If the options does not have a parent,
              this will be empty
            example: Sales
          is_active:
            $ref: '#/components/schemas/option_is_active'
          is_default:
            $ref: '#/components/schemas/option_is_default'
          criteria:
            type: object
            properties:
              criteria_id:
                type: string
                description: Filter ID
                example: '460000000038084'
              tag_id:
                $ref: '#/components/schemas/filter_tag_id'
              type:
                $ref: '#/components/schemas/filter_type'
              options:
                description: Options selected for the criteria
                type: array
                items:
                  $ref: '#/components/schemas/filter_option_name'
          children:
            description: Child options of the option
            type: array
            items:
              type: object
              properties:
                option_id:
                  $ref: '#/components/schemas/option_id'
                option_name:
                  $ref: '#/components/schemas/option_name'
                option_order:
                  type: integer
                  description: The order in which the options appear.
                  example: 1
                parent_id:
                  type: string
                  description: Parent Option ID. If the options does not have a parent,
                    this will be empty
                  example: '460000000038081'
                parent_name:
                  type: string
                  description: Parent Option Name. If the options does not have a
                    parent, this will be empty
                  example: Sales
                is_active:
                  $ref: '#/components/schemas/option_is_active'
                is_default:
                  $ref: '#/components/schemas/option_is_default'
                criteria:
                  type: object
                  properties:
                    criteria_id:
                      type: string
                      description: Filter ID
                      example: '460000000038084'
                    tag_id:
                      $ref: '#/components/schemas/filter_tag_id'
                    type:
                      $ref: '#/components/schemas/filter_type'
                    options:
                      description: Options selected for the criteria
                      type: array
                      items:
                        $ref: '#/components/schemas/filter_option_id'
      example:
      - option_id: '460000000038081'
        option_name: Sales
        is_active: true
        is_default: false
        depth: 0
        option_order: 1
        parent_id: ''
        parent_name: ''
        criteria:
        - options:
          - Asia
          - India
          - Europe
          criteria_id: '460000000048081'
          tag_id: ''
          type: location
        - options:
          - Untagged
          - Government
          - Military / Defense
          criteria_id: '460000000048083'
          tag_id: '460000000038078'
          type: tag
        children:
        - option_id: '460000000038083'
          option_name: Sales - Central
          children: []
          is_active: true
          is_default: false
          depth: 0
          option_order: 2
          parent_id: ''
          parent_name: ''
          criteria:
          - options:
            - Asia
            - North America
            criteria_id: '460000000048081'
            tag_id: ''
            type: location
          - options:
            - All
            criteria_id: '460000000048083'
            tag_id: '460000000038078'
            type: tag
    editpage-criteria-response:
      type: object
      properties:
        message:
          type: string
          example: success
        dependent_tags:
          $ref: '#/components/schemas/dependent_tags'
        is_class:
          $ref: '#/components/schemas/is_class'
        is_draft:
          $ref: '#/components/schemas/is_draft'
        tag_id:
          $ref: '#/components/schemas/tag_id'
        tag_name:
          $ref: '#/components/schemas/tag_name'
        tags:
          $ref: '#/components/schemas/possible_filter_tags'
        options:
          $ref: '#/components/schemas/criteria_options_response'
      example:
      - code: 0
        message: success
        tag_id: '460000000038055'
        tag_name: Department
        is_class: false
        is_draft: false
        dependent_tags:
        - dependent_id: ''
          dependent_name: Location
          dependent_order: ''
          dependent_type: location
          dependent_options:
          - dependent_option_id: '460000000039786'
            dependent_option_name: Head Office
            dependent_option_order: ''
          - dependent_option_id: '460000000039788'
            dependent_option_name: Central Warehouse
            dependent_option_order: ''
        - dependent_id: '460000000038081'
          dependent_name: Business Unit
          dependent_order: '1'
          dependent_type: tag
          dependent_options:
          - dependent_option_id: '460000000038038'
            dependent_option_name: Public
            dependent_option_order: '1'
          - dependent_option_id: '460000000038083'
            dependent_option_name: Private
            dependent_option_order: '1'
          - dependent_option_id: '460000000038085'
            dependent_option_name: NGO
            dependent_option_order: '2'
        tags:
        - tag_id: ''
          tag_name: Location
          type: location
          is_restricted: false
          is_associated: true
          restricted_reason: ''
        - tag_id: '460000000038080'
          tag_name: Business Unit
          type: tag
          is_restricted: false
          is_associated: true
          restricted_reason: ''
        - tag_id: '460000000038057'
          tag_name: Feedback
          type: tag
          is_restricted: true
          is_associated: false
          restricted_reason: The modules associated with this reporting tag are not
            set at the appropriate level compared to the current tag. Kindly refer
            to the requirements for a filter tag.
        options:
        - option_id: '460000000054012'
          option_name: Sales
          children:
          - option_id: '460000000054014'
            option_name: Sales - East
            children: []
            is_active: true
            is_default: false
            depth: 0
            option_order: 1
            parent_id: ''
            parent_name: ''
            criteria:
            - options:
              - '460000000038083'
              - untagged
              tag_id: ''
              type: location
          is_active: true
          is_default: false
          depth: 0
          option_order: 1
          parent_id: ''
          parent_name: ''
          criteria:
          - options:
            - all
            tag_id: ''
            type: location
        - option_id: '460000000054016'
          option_name: Marketing
          children: []
          is_active: true
          is_default: false
          depth: 0
          option_order: 2
          parent_id: ''
          parent_name: ''
          criteria:
          - options:
            - '460000000038038'
            - untagged
            tag_id: ''
            type: location
        locations:
        - location_name: Head Office
          depth: 0
          location_id: '460000000039786'
        - location_name: Central Warehouse
          depth: 1
          location_id: '460000000039788'
        - location_name: East Hill
          depth: 2
          location_id: '460000000039790'
    mark-default_option-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: The option has been marked as default.
    autocomplete_response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        results:
          type: array
          description: List of reporting tags matching the search criteria
          items:
            type: object
            properties:
              id:
                $ref: '#/components/schemas/option_id'
              text:
                $ref: '#/components/schemas/option_name'
              depth:
                $ref: '#/components/schemas/depth'
    all_options_response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        results:
          type: array
          description: List of all options under a reporting tag
          items:
            type: object
            properties:
              option_id:
                type: string
                description: Option ID or <code>untagged</code> for untagged option
                  if the show_untagged is true
                example: '460000000038082'
              option_name:
                type: string
                description: Option's name or 'Untagged' for untagged option if the
                  show_untagged is true
                example: Sales
              depth:
                $ref: '#/components/schemas/depth'
          example:
          - option_id: '460000000038081'
            option_name: Sales
            depth: 0
          - option_id: '460000000038083'
            option_name: Sales - Central
            depth: 1
          - option_id: untagged
            option_name: Untagged
            depth: 0
    options_detail_page_response:
      type: object
      properties:
        code:
          type: integer
          example: 0
        message:
          type: string
          example: success
        dependent_tags:
          $ref: '#/components/schemas/dependent_tags'
          example:
          - dependent_id: ''
            dependent_name: Location
            dependent_type: location
            dependent_order: ''
            dependent_options:
            - dependent_option_id: '460000000058078'
              dependent_option_name: Europe
              dependent_option_order: ''
            - dependent_option_id: '460000000058080'
              dependent_option_name: Asia
              dependent_option_order: ''
            - dependent_option_id: '460000000058082'
              dependent_option_name: India
              dependent_option_order: ''
            - dependent_option_id: '460000000058084'
              dependent_option_name: North America
              dependent_option_order: ''
          - dependent_id: '460000000038078'
            dependent_name: Business Type
            dependent_type: tag
            dependent_order: 1
            dependent_options:
            - dependent_option_id: '460000000038080'
              dependent_option_name: Government
              dependent_option_order: ''
            - dependent_option_id: '460000000038076'
              dependent_option_name: Military / Defense
              dependent_option_order: ''
        description:
          $ref: '#/components/schemas/description'
        is_class:
          $ref: '#/components/schemas/is_class'
        is_draft:
          $ref: '#/components/schemas/is_draft'
        tag_id:
          $ref: '#/components/schemas/tag_id'
        tag_name:
          $ref: '#/components/schemas/tag_name'
        options:
          $ref: '#/components/schemas/criteria_options_response_for_details_page'
        page_context:
          type: object
          properties:
            page:
              type: integer
              example: 1
            per_page:
              type: integer
              example: 10
            has_more_page:
              type: boolean
              example: false
    product_data:
      type: object
      properties:
        item_id:
          $ref: '#/components/schemas/item_id'
    import-a-customer-using-the-crm-account-id-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/data'
    import-a-customer-using-crm-contact-id-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/data'
    import-a-vendor-using-the-crm-vendor-id-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/data'
    import-an-item-using-the-crm-product-id-response:
      type: object
      properties:
        code:
          type: integer
          example: 0
          readOnly: true
        message:
          type: string
          example: success
          readOnly: true
        data:
          $ref: '#/components/schemas/product_data'
  parameters:
    organization_id:
      name: organization_id
      description: ID of the organization
      in: query
      required: true
      schema:
        type: string
      example: '10234695'
    transaction_id:
      name: transaction_id
      in: query
      description: 'The ID of the transaction (Invoice ID). Note: This value is a
        set of numeric characters.'
      required: true
      schema:
        type: string
      example: '982000000567114'
    transaction_type:
      name: transaction_type
      in: query
      description: The type of the transaction (Invoice).
      required: true
      schema:
        type: string
      example: invoice
    link_type:
      name: link_type
      in: query
      description: The type of the link (Private or Public).
      required: true
      schema:
        type: string
      example: public
    invoice_id:
      name: debit_note_id
      in: path
      description: Unique identifier for the debit note
      required: true
      schema:
        type: string
        example: '982000000567114'
    expiry_time:
      name: expiry_time
      in: query
      description: 'The expiry time of the payment link. Supported format : <code>yyyy-MM-dd</code>'
      required: true
      schema:
        type: string
      example: '2024-06-27'
    deliverychallan_id:
      name: deliverychallan_id
      in: path
      required: true
      description: 'Unique identifier of the delivery challan. Note: This value is
        a set of numeric characters.'
      schema:
        type: string
      example: '982000000567001'
    purchaseorder_ids:
      name: purchaseorder_ids
      in: query
      required: true
      description: ID of the purchase order
      schema:
        type: string
      example: '460000000098765'
    module_name:
      name: module_name
      in: path
      description: Module Name
      required: true
      schema:
        type: string
        example: cm_debtor
    module_id:
      name: module_id
      in: path
      description: Custom Module ID
      required: true
      schema:
        type: string
        example: 2112155000000652000
    fixed_asset_id:
      name: fixed_asset_id
      description: ID of the fixed asset
      in: query
      required: true
      schema:
        type: string
      example: '3640355000000319008'
    path_tag_id:
      name: tag_id
      in: path
      description: ID of the reporting tag
      required: true
      schema:
        type: string
      example: '460000000038080'
    query_tag_id:
      name: tag_id
      in: query
      description: ID of the reporting tag
      required: true
      schema:
        type: string
      example: '460000000038080'
    path_option_id:
      name: option_id
      in: path
      description: ID of a reporting tag's option
      required: true
      schema:
        type: string
      example: '460000000038081'
  responses: {}
  securitySchemes:
    Zoho_Auth:
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://accounts.zoho.com/oauth/v2/auth
          scopes:
            ZohoBooks.contacts.READ: Read Contacts
            ZohoBooks.contacts.CREATE: Create Contacts
            ZohoBooks.estimates.READ: Read Estimates
            ZohoBooks.salesorders.READ: Read Salesorders
